apiVersion: aim.eai.amd.com/v1alpha1
kind: AIMArtifact
metadata:
  name: basic-lifecycle-s3
spec:
  sourceUri: s3://models/SmolLM-135M
  # PVC sizing only; the synthetic seed is a few MiB. Readiness is gated on the
  # download Job succeeding, not on matching this size.
  size: 64Mi
  env:
    - name: AWS_ACCESS_KEY_ID
      value: minioadmin
    - name: AWS_SECRET_ACCESS_KEY
      value: minioadmin
    - name: AWS_ENDPOINT_URL
      value: http://minio:9000
    - name: S3_ENDPOINT
      value: minio:9000
    - name: S3_USE_HTTPS
      value: "0"
    - name: AWS_DEFAULT_REGION
      value: us-east-1