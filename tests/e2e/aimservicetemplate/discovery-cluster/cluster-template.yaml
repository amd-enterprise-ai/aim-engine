apiVersion: aim.eai.amd.com/v1alpha1
kind: AIMClusterServiceTemplate
metadata:
  name: test-template-discovery-cluster
spec:
  modelName: test-model-discovery-cluster
  gpu:
    requests: 1
    models:
      - MI300X

