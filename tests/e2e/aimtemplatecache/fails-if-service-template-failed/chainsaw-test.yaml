apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: template-cache-fails-if-service-template-failed
spec:
  steps:
    - name: Create the service template
      try:
        - apply:
            file: model.yaml
        - apply:
            file: service-template.yaml
        - script:
            # TODO remove once the template controller is merged
            env:
              - name: NAMESPACE
                value: ($namespace)
            content: |
              kubectl patch aimservicetemplate fails-if-service-template-failed -n $NAMESPACE --type=merge --subresource=status -p '
              {
                "status": {
                  "status": "Failed",
                  "conditions": [
                    {
                      "type": "Ready",
                      "status": "False",
                      "reason": "Failed",
                      "message": "Failed",
                      "lastTransitionTime": "'$(date -u +"%Y-%m-%dT%H:%M:%SZ")'"
                    }
                  ]
                }
              }'

    - name: Create the template cache
      try:
        - apply:
            file: template-cache.yaml

    - name: Ensure the template cache is also failed
      try:
        - assert:
            resource:
              apiVersion: aim.eai.amd.com/v1alpha1
              kind: AIMTemplateCache
              metadata:
                name: fails-if-service-template-failed
              status:
                status: Failed
