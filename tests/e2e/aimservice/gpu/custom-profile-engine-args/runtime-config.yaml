apiVersion: aim.eai.amd.com/v1alpha1
kind: AIMRuntimeConfig
metadata:
  name: test-cp-engine-args
spec:
  routing:
    enabled: true
    gatewayRef:
      name: kserve-ingress-gateway
      namespace: kgateway-system
    pathTemplate: "/integration/gpu-cp-engine-args/{.metadata.namespace}/{.metadata.name}"
    requestTimeout: "180s"
