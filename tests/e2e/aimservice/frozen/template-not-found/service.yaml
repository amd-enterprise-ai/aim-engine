# AIMService referencing the model (but no template exists for this model)
apiVersion: aim.eai.amd.com/v1alpha1
kind: AIMService
metadata:
  name: test-service
spec:
  model:
    name: test-model
  replicas: 1
