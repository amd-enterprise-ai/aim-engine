---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: aimartifacts.aim.eai.amd.com
spec:
  group: aim.eai.amd.com
  names:
    categories:
    - aim
    - all
    kind: AIMArtifact
    listKind: AIMArtifactList
    plural: aimartifacts
    shortNames:
    - aimart
    singular: aimartifact
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      priority: 1
      type: string
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.mode
      name: Mode
      type: string
    - jsonPath: .status.displaySize
      name: Model Size
      type: string
    - jsonPath: .status.progress.displayPercentage
      name: Progress
      type: string
    - jsonPath: .status.download.protocol
      name: Protocol
      priority: 1
      type: string
    - jsonPath: .status.download.attempt
      name: Attempt
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AIMArtifact is the Schema for the artifacts API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AIMArtifactSpec defines the desired state of AIMArtifact
            properties:
              adapterDisk:
                description: |-
                  AdapterDisk, when set on a type=model artifact, provisions a shared ReadWriteMany
                  adapter disk owned by this model artifact and partitioned per consuming service.
                  Only allowed when type=model.
                properties:
                  size:
                    anyOf:
                    - type: integer
                    - type: string
                    description: |-
                      Size is the requested size of the adapter disk PVC.
                      Defaults to 50Gi when unset (a cascade default may override it).
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  storageClassName:
                    description: |-
                      StorageClassName specifies the storage class for the adapter disk.
                      When empty, the cluster default storage class is used.
                      The access mode is fixed at ReadWriteMany by the controller.
                    type: string
                type: object
              downloadFilter:
                description: |-
                  DownloadFilter controls which files are included or excluded when downloading from HuggingFace.
                  Overrides any filter set in the runtime config's storage.downloadFilter.
                  When neither is set, subdirectory files are excluded by default (equivalent to exclude: ["*/*"]).
                  To download all files including subdirectories, set this to an empty object: downloadFilter: {}.
                  This field is immutable — to change the filter, recreate the artifact.
                properties:
                  exclude:
                    description: |-
                      Exclude specifies glob patterns for files to skip.
                      Files matching any exclude pattern are removed after include filtering.
                      Patterns use fnmatch syntax (e.g., ["*/*", "*.bin"]).
                      Use ["*/*"] to exclude all files in subdirectories (the default when no filter is set).
                    items:
                      type: string
                    type: array
                  include:
                    description: |-
                      Include specifies glob patterns for files to download.
                      Only files matching at least one pattern are considered.
                      If empty, all files pass the include check.
                      Patterns use fnmatch syntax (e.g., ["*.safetensors", "config.json"]).
                    items:
                      type: string
                    type: array
                type: object
                x-kubernetes-validations:
                - message: downloadFilter is immutable
                  rule: self == oldSelf
              env:
                description: |-
                  Env lists the environment variables to use for authentication when downloading models.
                  These variables are used for authentication with model registries (e.g., HuggingFace tokens).
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: |-
                        Name of the environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        fileKeyRef:
                          description: |-
                            FileKeyRef selects a key of the env file.
                            Requires the EnvFiles feature gate to be enabled.
                          properties:
                            key:
                              description: |-
                                The key within the env file. An invalid key will prevent the pod from starting.
                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                              type: string
                            optional:
                              default: false
                              description: |-
                                Specify whether the file or its key must be defined. If the file or key
                                does not exist, then the env var is not published.
                                If optional is set to true and the specified key does not exist,
                                the environment variable will not be set in the Pod's containers.

                                If optional is set to false and the specified key does not exist,
                                an error will be returned during Pod creation.
                              type: boolean
                            path:
                              description: |-
                                The path within the volume from which to select the file.
                                Must be relative and may not contain the '..' path or start with '..'.
                              type: string
                            volumeName:
                              description: The name of the volume mount containing
                                the env file.
                              type: string
                          required:
                          - key
                          - path
                          - volumeName
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              imagePullSecrets:
                description: ImagePullSecrets references secrets for pulling AIM container
                  images.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              modelDownloadImage:
                description: |-
                  ModelDownloadImage specifies the container image used to download and initialize the artifact.
                  This image runs as a job to download model artifacts from the source URI to the cache volume.
                  When not specified, the controller uses its built-in default (matching the release version).
                type: string
              modelId:
                description: |-
                  ModelID is the canonical identifier in {org}/{name} format.
                  Determines the cache download path: /workspace/cache/{modelId}
                  For HuggingFace sources, this is typically derived from the URI (e.g., "meta-llama/Llama-3-8B").
                  For S3 sources, this must be explicitly provided (e.g., "my-team/fine-tuned-llama").
                  When not specified, derived from SourceURI for HuggingFace sources.
                pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                type: string
              parentArtifact:
                description: |-
                  ParentArtifact names the base model AIMArtifact (type=model) this adapter is
                  compatible with. Required and only allowed when type=adapter; immutable.
                  The adapter is owned by (cascade-deleted with) the parent. Compatibility is
                  keyed on the parent's modelId.
                type: string
                x-kubernetes-validations:
                - message: parentArtifact is immutable
                  rule: self == oldSelf
              rank:
                description: Rank is the LoRA rank of the adapter. Optional; only
                  meaningful when type=adapter.
                format: int32
                minimum: 1
                type: integer
              retentionPriority:
                description: |-
                  RetentionPriority marks this artifact as eligible for automatic eviction
                  when storage quota is exceeded. Lower values are evicted first.
                  Artifacts without this field are only evictable if a defaultRetentionPriority
                  is configured in the runtime config. Use the aim.eai.amd.com/eviction-protected
                  annotation to exempt an artifact from eviction entirely.
                format: int32
                minimum: 0
                type: integer
              runtimeConfigName:
                description: |-
                  Name is the name of the runtime config to use for this resource. If a runtime config with this name exists both
                  as a namespace and a cluster runtime config, the values are merged together, the namespace config taking priority
                  over the cluster config when there are conflicts. If this field is empty or set to `default`, the namespace / cluster
                  runtime config with the name `default` is used, if it exists.
                type: string
              size:
                anyOf:
                - type: integer
                - type: string
                description: Size specifies the size of the cache volume
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              sourceUri:
                description: |-
                  SourceURI specifies the source location of the model to download.
                  Supported protocols: hf:// (HuggingFace) and s3:// (S3-compatible storage).
                  This field uniquely identifies the artifact and is immutable after creation.
                  Example: hf://meta-llama/Llama-3-8B
                minLength: 1
                pattern: ^(hf|s3)://[^ \t\r\n]+$
                type: string
                x-kubernetes-validations:
                - message: sourceUri is immutable
                  rule: self == oldSelf
              storageClassName:
                description: |-
                  StorageClassName specifies the storage class for the cache volume.
                  When not specified, uses the cluster default storage class.
                type: string
              type:
                default: model
                description: |-
                  Type discriminates a base model artifact (`model`) from a LoRA adapter
                  definition (`adapter`). Defaults to `model`; immutable after creation.
                  Adapter artifacts require parentArtifact and modelId, and do not get their
                  own cache PVC.
                enum:
                - model
                - adapter
                type: string
            required:
            - sourceUri
            type: object
          status:
            description: AIMArtifactStatus defines the observed state of AIMArtifact
            properties:
              adapterPath:
                description: |-
                  AdapterPath is the resolved on-disk directory name for a type=adapter artifact,
                  frozen at first resolution (defaults to metadata.name). This is the canonical
                  copy, mirrored into consuming services' status.
                type: string
              adapterPersistentVolumeClaim:
                description: |-
                  AdapterPersistentVolumeClaim is the name of the shared adapter disk PVC
                  provisioned for a type=model artifact that declares an adapterDisk. Empty
                  otherwise.
                type: string
              allocatedSize:
                anyOf:
                - type: integer
                - type: string
                description: AllocatedSize is the actual PVC size requested (including
                  headroom).
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              conditions:
                description: Conditions represent the latest available observations
                  of the artifact's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              discoveredSizeBytes:
                description: |-
                  DiscoveredSizeBytes is the model size discovered via check-size job.
                  Populated when spec.size is not provided.
                format: int64
                type: integer
              displaySize:
                description: DisplaySize is the human-readable effective size (spec
                  or discovered)
                type: string
              download:
                description: |-
                  Download represents the current download attempt state, patched by the downloader pod.
                  Shows which protocol is active, what attempt we're on, etc.
                properties:
                  attempt:
                    description: Attempt is the current attempt number (1-based)
                    format: int32
                    type: integer
                  message:
                    description: Message is a human-readable status message from the
                      downloader
                    type: string
                  protocol:
                    description: Protocol is the download protocol currently in use
                      (e.g., "XET", "HF_TRANSFER", "HTTP")
                    type: string
                  protocolSequence:
                    description: ProtocolSequence is the configured protocol sequence
                      (e.g., "HF_TRANSFER,XET")
                    type: string
                  totalAttempts:
                    description: TotalAttempts is the total number of attempts configured
                      via AIM_DOWNLOADER_PROTOCOL
                    format: int32
                    type: integer
                type: object
              headroomPercent:
                description: HeadroomPercent is the headroom percentage that was applied
                  to the PVC size.
                format: int32
                type: integer
              lastUsed:
                description: LastUsed represents the last time a model was deployed
                  that used this cache
                format: date-time
                type: string
              mode:
                description: |-
                  Mode indicates the ownership mode of this artifact, derived from owner references.
                  - Dedicated: Has owner references, will be garbage collected when owners are deleted.
                  - Shared: No owner references, persists independently and can be shared.
                enum:
                - Dedicated
                - Shared
                type: string
              observedGeneration:
                format: int64
                type: integer
              parentModelId:
                description: |-
                  ParentModelID is the parent model artifact's modelId, denormalized onto the
                  adapter for convenience (refreshed each reconcile).
                type: string
              persistentVolumeClaim:
                description: PersistentVolumeClaim represents the name of the created
                  PVC
                type: string
              progress:
                description: Progress represents the download progress when Status
                  is Progressing
                properties:
                  displayPercentage:
                    description: |-
                      DisplayPercentage is a human-readable progress string (e.g., "45 %")
                      This field is automatically populated from Progress.Percentage
                    type: string
                  downloadedBytes:
                    description: DownloadedBytes is the number of bytes downloaded
                      so far
                    format: int64
                    type: integer
                  percentage:
                    description: Percentage is the download progress as a percentage
                      (0-100)
                    format: int32
                    maximum: 100
                    minimum: 0
                    type: integer
                  totalBytes:
                    description: TotalBytes is the expected total size of the download
                      in bytes
                    format: int64
                    type: integer
                type: object
              resolvedParent:
                description: |-
                  ResolvedParent captures the resolved parent model artifact for a type=adapter
                  artifact, including its UID.
                properties:
                  kind:
                    description: Kind is the fully-qualified kind of the resolved
                      reference, when known.
                    type: string
                  name:
                    description: Name is the resource name that satisfied the reference.
                    type: string
                  namespace:
                    description: |-
                      Namespace identifies where the resource was found when namespace-scoped.
                      Empty indicates a cluster-scoped resource.
                    type: string
                  scope:
                    description: Scope indicates whether the resolved resource was
                      namespace or cluster scoped.
                    enum:
                    - Namespace
                    - Cluster
                    - Merged
                    - Unknown
                    type: string
                  uid:
                    description: UID captures the unique identifier of the resolved
                      reference, when known.
                    type: string
                type: object
              resolvedSourceUri:
                description: |-
                  ResolvedSourceURI is the effective download source after cache resolution.
                  When the S3 artifact cache has a hit, this contains the rewritten s3:// URI.
                  When empty, spec.sourceUri is used directly.
                type: string
              status:
                default: Pending
                description: Status represents the current status of the artifact
                enum:
                - Pending
                - Progressing
                - Ready
                - Degraded
                - Failed
                - NotAvailable
                type: string
            type: object
        type: object
        x-kubernetes-validations:
        - message: spec.type is immutable
          optionalOldSelf: true
          rule: '!has(self.spec.type) || !oldSelf.hasValue() || !has(oldSelf.value().spec.type)
            || self.spec.type == oldSelf.value().spec.type'
        - message: spec.parentArtifact is required when spec.type is adapter
          rule: '!has(self.spec.type) || self.spec.type != ''adapter'' || has(self.spec.parentArtifact)'
        - message: spec.modelId is required when spec.type is adapter
          rule: '!has(self.spec.type) || self.spec.type != ''adapter'' || (has(self.spec.modelId)
            && size(self.spec.modelId) > 0)'
        - message: spec.parentArtifact is only allowed when spec.type is adapter
          rule: '!has(self.spec.parentArtifact) || (has(self.spec.type) && self.spec.type
            == ''adapter'')'
        - message: spec.adapterDisk is only allowed when spec.type is model
          rule: '!has(self.spec.adapterDisk) || !has(self.spec.type) || self.spec.type
            == ''model'''
        - message: spec.rank is only allowed when spec.type is adapter
          rule: '!has(self.spec.rank) || (has(self.spec.type) && self.spec.type ==
            ''adapter'')'
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: aimclustermodels.aim.eai.amd.com
spec:
  group: aim.eai.amd.com
  names:
    categories:
    - aim
    - all
    kind: AIMClusterModel
    listKind: AIMClusterModelList
    plural: aimclustermodels
    shortNames:
    - aimclmdl
    singular: aimclustermodel
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.sourceType
      name: Source
      type: string
    - jsonPath: .spec.image
      name: Image
      type: string
    - jsonPath: .status.version
      name: Version
      type: string
    - jsonPath: .status.imageMetadata.model.canonicalName
      name: Model
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    deprecated: true
    deprecationWarning: AIMClusterModel v1alpha1 is deprecated; use aim.eai.amd.com/v1alpha2
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          AIMClusterModel is a cluster-scoped model catalog entry for AIM container images.

          Cluster-scoped models can be referenced by AIMServices in any namespace, making them ideal for
          shared model deployments across teams and projects. Like namespace-scoped AIMModels, cluster models
          trigger discovery jobs to extract metadata and generate service templates.

          When both cluster and namespace models exist for the same container image, services will preferentially
          use the namespace-scoped AIMModel when referenced by image URI.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              AIMModelSpec defines the desired state of AIMModel.

              Per-version constraints (v1alpha1 forbids derivedFrom and profiles;
              v1alpha2 forbids profileCopy/custom/customTemplates/top-level modelSources,
              requires image XOR profiles, and forbids profiles mixed with legacy scalar
              fields) live on the version-specific AIMModel / AIMClusterModel root types
              in the per-version *_types.go files.
            properties:
              aimId:
                description: |-
                  AimId is the AIM product family identifier (e.g., "qwen/qwen3-32b").
                  When set together with modelSources, enables aimId-based template matching:
                  the controller finds official templates by aimId, filters by versionPolicy,
                  matches by modelId, and creates copies with the custom weight source.
                type: string
              custom:
                description: |-
                  Custom contains configuration for custom models (models with inline modelSources).
                  Only used when modelSources are specified; ignored for image-based models.
                properties:
                  hardware:
                    description: |-
                      Hardware specifies default hardware requirements for all templates.
                      Individual templates can override these defaults.
                      Required when modelSources is set and customTemplates is empty (unless aimId is set).
                    properties:
                      cpu:
                        description: CPU specifies CPU requirements.
                        properties:
                          limits:
                            anyOf:
                            - type: integer
                            - type: string
                            description: Limits is the maximum number of CPU cores
                              to allow.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          requests:
                            anyOf:
                            - type: integer
                            - type: string
                            description: Requests is the number of CPU cores to request.
                              Required and must be > 0.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                        required:
                        - requests
                        type: object
                      gpu:
                        description: GPU specifies GPU requirements. If not set, no
                          GPUs are requested (CPU-only model).
                        properties:
                          minVram:
                            anyOf:
                            - type: integer
                            - type: string
                            description: |-
                              MinVRAM limits deployment to GPUs having at least this much VRAM.
                              Used for capacity planning when the model size is known but any GPU with
                              sufficient VRAM is acceptable.
                              Cannot be combined with model.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          model:
                            description: |-
                              Model limits deployment to a specific GPU model.
                              Example: "MI300X"
                              Cannot be combined with minVram.
                            maxLength: 64
                            type: string
                          requests:
                            description: |-
                              Requests is the number of GPUs to set as requests/limits.
                              Set to 0 to target GPU nodes without consuming GPU resources (useful for testing).
                            format: int32
                            minimum: 0
                            type: integer
                          resourceName:
                            default: amd.com/gpu
                            description: |-
                              ResourceName is the Kubernetes resource name for GPU resources.
                              Defaults to "amd.com/gpu" if not specified.
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: model and minVram are mutually exclusive; specify
                            either a specific GPU model OR a minimum VRAM requirement,
                            not both
                          rule: '!(has(self.model) && size(self.model) > 0 && has(self.minVram))'
                    type: object
                    x-kubernetes-validations:
                    - message: at least one of gpu or cpu must be specified
                      rule: has(self.gpu) || has(self.cpu)
                  type:
                    allOf:
                    - enum:
                      - optimized
                      - general
                      - preview
                      - unoptimized
                    - enum:
                      - optimized
                      - general
                      - preview
                      - unoptimized
                    description: |-
                      Type specifies default type for all templates.
                      Individual templates can override this default.
                      When nil, templates default to "unoptimized".
                    type: string
                  versionPolicy:
                    default: pinned
                    description: |-
                      VersionPolicy controls how template versions are filtered during aimId-based matching.
                      - pinned (default): match templates whose status.version equals the model's image tag
                      - latest: match only templates at the newest available status.version
                      - all: match templates at any version
                      - any: deprecated alias of all, kept for backward compatibility
                      Only used when spec.aimId is set.
                    enum:
                    - pinned
                    - latest
                    - any
                    - all
                    type: string
                type: object
              customTemplates:
                description: |-
                  CustomTemplates defines explicit template configurations for this model.
                  These templates are created directly without running a discovery job.
                  Can be used with or without modelSources to define custom deployment configurations.
                  If omitted when modelSources is set, a single template is auto-generated
                  using the custom.hardware requirements.
                items:
                  description: |-
                    AIMCustomTemplate defines a custom template configuration for a model.
                    When modelSources are specified directly on AIMModel, customTemplates allow
                    defining explicit hardware requirements and profiles, skipping the discovery job.
                    This is an existing struct (not a CRD); it appears as an element of AIMModel.spec.customTemplates[].
                  properties:
                    aimId:
                      description: |-
                        AimId is the AIM product family identifier (e.g., "meta-llama/Llama-3-8B").
                        Required when customProfile is set.
                      type: string
                    customProfile:
                      description: |-
                        CustomProfile defines inline custom profile data for the inference engine.
                        When set, the resulting template will have a custom profile ConfigMap mounted.
                        Requires aimId, modelId, hardware, profile.metric, and profile.precision.
                      properties:
                        engineArgs:
                          description: |-
                            EngineArgs contains inference engine arguments as a free-form JSON object.
                            These are passed as CLI arguments to the inference engine (e.g., vLLM).
                            Do not include "model" — it is injected separately by the runtime.
                          x-kubernetes-preserve-unknown-fields: true
                        envVars:
                          additionalProperties:
                            type: string
                          description: |-
                            EnvVars contains environment variables applied to the inference engine process.
                            These are written into the profile YAML and applied by the AIM runtime via os.execv,
                            distinct from container-level Env which targets the AIM runtime container itself.
                            Keys must match ^[A-Z0-9_]+$ (uppercase with underscores).
                          type: object
                      type: object
                    env:
                      description: |-
                        Env specifies environment variable overrides when this template is selected.
                        These are container-level env vars applied to the AIM runtime container.
                      items:
                        description: EnvVar represents an environment variable present
                          in a Container.
                        properties:
                          name:
                            description: |-
                              Name of the environment variable.
                              May consist of any printable ASCII characters except '='.
                            type: string
                          value:
                            description: |-
                              Variable references $(VAR_NAME) are expanded
                              using the previously defined environment variables in the container and
                              any service environment variables. If a variable cannot be resolved,
                              the reference in the input string will be unchanged. Double $$ are reduced
                              to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                              "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                              Escaped references will never be expanded, regardless of whether the variable
                              exists or not.
                              Defaults to "".
                            type: string
                          valueFrom:
                            description: Source for the environment variable's value.
                              Cannot be used if value is not empty.
                            properties:
                              configMapKeyRef:
                                description: Selects a key of a ConfigMap.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the ConfigMap or
                                      its key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              fieldRef:
                                description: |-
                                  Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                  spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                properties:
                                  apiVersion:
                                    description: Version of the schema the FieldPath
                                      is written in terms of, defaults to "v1".
                                    type: string
                                  fieldPath:
                                    description: Path of the field to select in the
                                      specified API version.
                                    type: string
                                required:
                                - fieldPath
                                type: object
                                x-kubernetes-map-type: atomic
                              fileKeyRef:
                                description: |-
                                  FileKeyRef selects a key of the env file.
                                  Requires the EnvFiles feature gate to be enabled.
                                properties:
                                  key:
                                    description: |-
                                      The key within the env file. An invalid key will prevent the pod from starting.
                                      The keys defined within a source may consist of any printable ASCII characters except '='.
                                      During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                    type: string
                                  optional:
                                    default: false
                                    description: |-
                                      Specify whether the file or its key must be defined. If the file or key
                                      does not exist, then the env var is not published.
                                      If optional is set to true and the specified key does not exist,
                                      the environment variable will not be set in the Pod's containers.

                                      If optional is set to false and the specified key does not exist,
                                      an error will be returned during Pod creation.
                                    type: boolean
                                  path:
                                    description: |-
                                      The path within the volume from which to select the file.
                                      Must be relative and may not contain the '..' path or start with '..'.
                                    type: string
                                  volumeName:
                                    description: The name of the volume mount containing
                                      the env file.
                                    type: string
                                required:
                                - key
                                - path
                                - volumeName
                                type: object
                                x-kubernetes-map-type: atomic
                              resourceFieldRef:
                                description: |-
                                  Selects a resource of the container: only resources limits and requests
                                  (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                properties:
                                  containerName:
                                    description: 'Container name: required for volumes,
                                      optional for env vars'
                                    type: string
                                  divisor:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: Specifies the output format of the
                                      exposed resources, defaults to "1"
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  resource:
                                    description: 'Required: resource to select'
                                    type: string
                                required:
                                - resource
                                type: object
                                x-kubernetes-map-type: atomic
                              secretKeyRef:
                                description: Selects a key of a secret in the pod's
                                  namespace
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                        required:
                        - name
                        type: object
                      maxItems: 64
                      type: array
                      x-kubernetes-list-map-keys:
                      - name
                      x-kubernetes-list-type: map
                    hardware:
                      description: |-
                        Hardware specifies GPU and CPU requirements for this template.
                        Optional when spec.hardware is set (inherits from spec).
                        When both are set, values are merged field-by-field with template taking precedence.
                      properties:
                        cpu:
                          description: CPU specifies CPU requirements.
                          properties:
                            limits:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Limits is the maximum number of CPU cores
                                to allow.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            requests:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Requests is the number of CPU cores to
                                request. Required and must be > 0.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                          required:
                          - requests
                          type: object
                        gpu:
                          description: GPU specifies GPU requirements. If not set,
                            no GPUs are requested (CPU-only model).
                          properties:
                            minVram:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                MinVRAM limits deployment to GPUs having at least this much VRAM.
                                Used for capacity planning when the model size is known but any GPU with
                                sufficient VRAM is acceptable.
                                Cannot be combined with model.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            model:
                              description: |-
                                Model limits deployment to a specific GPU model.
                                Example: "MI300X"
                                Cannot be combined with minVram.
                              maxLength: 64
                              type: string
                            requests:
                              description: |-
                                Requests is the number of GPUs to set as requests/limits.
                                Set to 0 to target GPU nodes without consuming GPU resources (useful for testing).
                              format: int32
                              minimum: 0
                              type: integer
                            resourceName:
                              default: amd.com/gpu
                              description: |-
                                ResourceName is the Kubernetes resource name for GPU resources.
                                Defaults to "amd.com/gpu" if not specified.
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: model and minVram are mutually exclusive; specify
                              either a specific GPU model OR a minimum VRAM requirement,
                              not both
                            rule: '!(has(self.model) && size(self.model) > 0 && has(self.minVram))'
                      type: object
                      x-kubernetes-validations:
                      - message: at least one of gpu or cpu must be specified
                        rule: has(self.gpu) || has(self.cpu)
                    modelId:
                      description: |-
                        ModelId is the specific model identifier / HuggingFace URI (e.g., "Qwen/Qwen3-32B-FP8").
                        Required when customProfile is set.
                      type: string
                    name:
                      description: Name is the template name. If not provided, auto-generated
                        from model name + profile.
                      maxLength: 63
                      type: string
                    profile:
                      description: |-
                        Profile declares runtime profile variables for template selection.
                        Used when multiple templates exist to select based on metric/precision.
                      properties:
                        metric:
                          allOf:
                          - enum:
                            - latency
                            - throughput
                          - enum:
                            - latency
                            - throughput
                          description: Metric specifies the optimization target (e.g.,
                            latency, throughput).
                          type: string
                        precision:
                          allOf:
                          - enum:
                            - auto
                            - fp4
                            - fp8
                            - fp16
                            - fp32
                            - bf16
                            - int4
                            - int8
                          - enum:
                            - auto
                            - fp4
                            - fp8
                            - fp16
                            - fp32
                            - bf16
                            - int4
                            - int8
                          description: Precision specifies the numerical precision
                            (e.g., fp8, fp16, bf16).
                          type: string
                      type: object
                    type:
                      allOf:
                      - enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                      - enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                      default: unoptimized
                      description: |-
                        Type indicates the optimization status of this template.
                        - optimized: Template has been tuned for performance
                        - general: General-purpose tuning between optimized and preview
                        - preview: Template is experimental/pre-release
                        - unoptimized: Default, no specific optimizations applied
                      type: string
                  type: object
                  x-kubernetes-validations:
                  - message: when customProfile is set, aimId, modelId, hardware,
                      profile.metric, and profile.precision are required
                    rule: '!has(self.customProfile) || (has(self.aimId) && has(self.modelId)
                      && has(self.hardware) && has(self.profile) && has(self.profile.metric)
                      && has(self.profile.precision))'
                maxItems: 16
                type: array
              defaultServiceTemplate:
                description: |-
                  DefaultServiceTemplate specifies the default AIMServiceTemplate to use when creating services for this model.
                  When set, services that reference this model will use this template if no template is explicitly specified.
                  If this is not set, a template will be automatically selected.
                type: string
              derivedFrom:
                description: |-
                  DerivedFrom is the legacy flat shape of v1alpha2's profile-derivation
                  onboarding surface. New objects must use spec.profiles instead; the
                  field is retained so existing v1alpha2 objects (and the v1alpha2
                  reconciler reading them) round-trip cleanly.

                  DEPRECATED: prefer spec.profiles.derivedFrom on v1alpha2.
                properties:
                  image:
                    description: Image overrides the runtime image used by the derived
                      profiles.
                    type: string
                  imagePullSecrets:
                    description: ImagePullSecrets lists secrets used for inspecting
                      and pulling container images.
                    items:
                      description: |-
                        LocalObjectReference contains enough information to let you locate the
                        referenced object inside the same namespace.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    type: array
                  overrides:
                    description: Overrides mutates the copied profile spec after selection
                      and version filtering.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount replaces the copied profile's
                          acceleratorCount.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel replaces the copied profile's
                          acceleratorModel.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode replaces the copied profile's
                          acceleratorPartitioningMode. Complete replacement, not a merge; an empty
                          override string leaves the source profile's mode untouched. Same reserved
                          values as AIMProfileSpecCommon.AcceleratorPartitioningMode. Whenever this
                          override is set (to any value, including "unpartitioned"), the CEL rule on the
                          enclosing spec also requires AcceleratorCount to be set: partition mode
                          changes the per-unit interpretation of acceleratorCount, and CEL cannot
                          read the base profile to tell whether the meaning actually changed, so it
                          conservatively requires the count be restated.
                        type: string
                      aimId:
                        description: |-
                          AimId stamps the derived profile's spec.aimId. When set, wins over
                          the source profile's aimId. REQUIRED when the enclosing selector has
                          role=base (the source base profile carries no aimId of its own).
                        type: string
                      containerEnv:
                        description: ContainerEnv merges by env var name, overriding
                          matching source entries.
                        items:
                          description: EnvVar represents an environment variable present
                            in a Container.
                          properties:
                            name:
                              description: |-
                                Name of the environment variable.
                                May consist of any printable ASCII characters except '='.
                              type: string
                            value:
                              description: |-
                                Variable references $(VAR_NAME) are expanded
                                using the previously defined environment variables in the container and
                                any service environment variables. If a variable cannot be resolved,
                                the reference in the input string will be unchanged. Double $$ are reduced
                                to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                Escaped references will never be expanded, regardless of whether the variable
                                exists or not.
                                Defaults to "".
                              type: string
                            valueFrom:
                              description: Source for the environment variable's value.
                                Cannot be used if value is not empty.
                              properties:
                                configMapKeyRef:
                                  description: Selects a key of a ConfigMap.
                                  properties:
                                    key:
                                      description: The key to select.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the ConfigMap or
                                        its key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fieldRef:
                                  description: |-
                                    Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                    spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                  properties:
                                    apiVersion:
                                      description: Version of the schema the FieldPath
                                        is written in terms of, defaults to "v1".
                                      type: string
                                    fieldPath:
                                      description: Path of the field to select in
                                        the specified API version.
                                      type: string
                                  required:
                                  - fieldPath
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fileKeyRef:
                                  description: |-
                                    FileKeyRef selects a key of the env file.
                                    Requires the EnvFiles feature gate to be enabled.
                                  properties:
                                    key:
                                      description: |-
                                        The key within the env file. An invalid key will prevent the pod from starting.
                                        The keys defined within a source may consist of any printable ASCII characters except '='.
                                        During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                      type: string
                                    optional:
                                      default: false
                                      description: |-
                                        Specify whether the file or its key must be defined. If the file or key
                                        does not exist, then the env var is not published.
                                        If optional is set to true and the specified key does not exist,
                                        the environment variable will not be set in the Pod's containers.

                                        If optional is set to false and the specified key does not exist,
                                        an error will be returned during Pod creation.
                                      type: boolean
                                    path:
                                      description: |-
                                        The path within the volume from which to select the file.
                                        Must be relative and may not contain the '..' path or start with '..'.
                                      type: string
                                    volumeName:
                                      description: The name of the volume mount containing
                                        the env file.
                                      type: string
                                  required:
                                  - key
                                  - path
                                  - volumeName
                                  type: object
                                  x-kubernetes-map-type: atomic
                                resourceFieldRef:
                                  description: |-
                                    Selects a resource of the container: only resources limits and requests
                                    (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                  properties:
                                    containerName:
                                      description: 'Container name: required for volumes,
                                        optional for env vars'
                                      type: string
                                    divisor:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      description: Specifies the output format of
                                        the exposed resources, defaults to "1"
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    resource:
                                      description: 'Required: resource to select'
                                      type: string
                                  required:
                                  - resource
                                  type: object
                                  x-kubernetes-map-type: atomic
                                secretKeyRef:
                                  description: Selects a key of a secret in the pod's
                                    namespace
                                  properties:
                                    key:
                                      description: The key of the secret to select
                                        from.  Must be a valid secret key.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the Secret or its
                                        key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      engineArgs:
                        description: EngineArgs shallow-merges on top of the source
                          engineArgs, overriding matching keys.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      engineEnv:
                        additionalProperties:
                          type: string
                        description: EngineEnv merges by key, overriding matching
                          source entries.
                        type: object
                      image:
                        description: |-
                          Image overrides the runtime container image used by the derived
                          profiles. When empty the deployment image is rebased onto the source
                          profile's base-image (status.baseImage) so private mirrors stay
                          self-contained.
                        type: string
                      modelId:
                        description: |-
                          ModelId stamps the derived profile's spec.modelId. When set, wins
                          over both the source profile's modelId AND the auto-derivation from
                          modelSources[0].modelId. REQUIRED when the enclosing selector has
                          role=base.
                        type: string
                      modelSources:
                        description: |-
                          ModelSources replaces the copied profile's modelSources. When
                          modelSources[0].modelId is set and overrides.modelId is unset, the
                          derived profile's modelId is auto-derived from modelSources[0]; an
                          explicit overrides.modelId always wins.
                        items:
                          description: |-
                            AIMModelSource describes a model artifact that must be downloaded for inference.
                            Discovery extracts these from the container's configuration to enable caching and validation.
                          properties:
                            env:
                              description: |-
                                Env specifies per-source credential overrides.
                                These variables are used for authentication when downloading this specific source.
                                Takes precedence over base-level env for the same variable name.
                              items:
                                description: EnvVar represents an environment variable
                                  present in a Container.
                                properties:
                                  name:
                                    description: |-
                                      Name of the environment variable.
                                      May consist of any printable ASCII characters except '='.
                                    type: string
                                  value:
                                    description: |-
                                      Variable references $(VAR_NAME) are expanded
                                      using the previously defined environment variables in the container and
                                      any service environment variables. If a variable cannot be resolved,
                                      the reference in the input string will be unchanged. Double $$ are reduced
                                      to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                      "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                      Escaped references will never be expanded, regardless of whether the variable
                                      exists or not.
                                      Defaults to "".
                                    type: string
                                  valueFrom:
                                    description: Source for the environment variable's
                                      value. Cannot be used if value is not empty.
                                    properties:
                                      configMapKeyRef:
                                        description: Selects a key of a ConfigMap.
                                        properties:
                                          key:
                                            description: The key to select.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the ConfigMap
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fieldRef:
                                        description: |-
                                          Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                          spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                        properties:
                                          apiVersion:
                                            description: Version of the schema the
                                              FieldPath is written in terms of, defaults
                                              to "v1".
                                            type: string
                                          fieldPath:
                                            description: Path of the field to select
                                              in the specified API version.
                                            type: string
                                        required:
                                        - fieldPath
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fileKeyRef:
                                        description: |-
                                          FileKeyRef selects a key of the env file.
                                          Requires the EnvFiles feature gate to be enabled.
                                        properties:
                                          key:
                                            description: |-
                                              The key within the env file. An invalid key will prevent the pod from starting.
                                              The keys defined within a source may consist of any printable ASCII characters except '='.
                                              During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                            type: string
                                          optional:
                                            default: false
                                            description: |-
                                              Specify whether the file or its key must be defined. If the file or key
                                              does not exist, then the env var is not published.
                                              If optional is set to true and the specified key does not exist,
                                              the environment variable will not be set in the Pod's containers.

                                              If optional is set to false and the specified key does not exist,
                                              an error will be returned during Pod creation.
                                            type: boolean
                                          path:
                                            description: |-
                                              The path within the volume from which to select the file.
                                              Must be relative and may not contain the '..' path or start with '..'.
                                            type: string
                                          volumeName:
                                            description: The name of the volume mount
                                              containing the env file.
                                            type: string
                                        required:
                                        - key
                                        - path
                                        - volumeName
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      resourceFieldRef:
                                        description: |-
                                          Selects a resource of the container: only resources limits and requests
                                          (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                        properties:
                                          containerName:
                                            description: 'Container name: required
                                              for volumes, optional for env vars'
                                            type: string
                                          divisor:
                                            anyOf:
                                            - type: integer
                                            - type: string
                                            description: Specifies the output format
                                              of the exposed resources, defaults to
                                              "1"
                                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                            x-kubernetes-int-or-string: true
                                          resource:
                                            description: 'Required: resource to select'
                                            type: string
                                        required:
                                        - resource
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      secretKeyRef:
                                        description: Selects a key of a secret in
                                          the pod's namespace
                                        properties:
                                          key:
                                            description: The key of the secret to
                                              select from.  Must be a valid secret
                                              key.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the Secret
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                    type: object
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            modelId:
                              description: |-
                                ModelID is the canonical identifier in {org}/{name} format.
                                Determines the cache mount path: /workspace/cache/{modelId}
                                For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                                For S3 sources, users define their own organizational structure.
                              pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                              type: string
                            precision:
                              allOf:
                              - enum:
                                - auto
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              - enum:
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              description: |-
                                Precision describes the runtime precision this source is compatible with.
                                Used to match model sources to profiles during custom weight onboarding.
                              type: string
                            size:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                Size is the expected storage space required for this model artifact.
                                Used for PVC sizing and capacity planning during cache creation.
                                Optional - if not specified, the download job will discover the size automatically.
                                Can be set explicitly to pre-allocate storage or override auto-discovery.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            sourceUri:
                              description: |-
                                SourceURI is the location from which the model should be downloaded.
                                Supported schemes:
                                - hf://org/model - Hugging Face Hub model
                                - s3://bucket/key - S3-compatible storage
                              pattern: ^(hf|s3)://[^ \t\r\n]+$
                              type: string
                          required:
                          - modelId
                          - sourceUri
                          type: object
                        type: array
                      profileId:
                        description: |-
                          ProfileId stamps the derived profile's spec.profileId. Optional —
                          most callers leave this empty and let the source profile's profileId
                          carry through (or the reconciler synthesise one).
                        type: string
                    type: object
                  selector:
                    description: Selector chooses which source profiles to derive
                      from.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount filters by accelerator unit
                          count.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel filters by accelerator identifier.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode filters candidates by their declared
                          partitioning mode. Partial-order match (NOT strict equality):
                            ""              - no filter on this field.
                            "unpartitioned" - matches profiles with mode "" or "unpartitioned".
                            "partitioned"   - matches profiles whose mode is non-trivial (anything
                                              other than "" / "unpartitioned").
                            "<C>"           - selector-only convenience: matches profiles with mode
                                              "<C>-*" (prefix on the scheme). Not a valid profile-spec
                                              value (e.g. selector "CPX" matches "CPX-NPS1", "CPX-NPS4").
                            "<C>-<M>"       - exact-string match on the scheme.
                        type: string
                      acceleratorType:
                        description: AcceleratorType filters by accelerator resource
                          type.
                        enum:
                        - gpu
                        - cpu
                        type: string
                      aimId:
                        description: AimId filters by model architecture identifier
                          (e.g., "qwen/qwen3-32b").
                        type: string
                      engine:
                        description: Engine filters by inference engine.
                        type: string
                      engineArgs:
                        description: |-
                          EngineArgs partially matches source engineArgs: every provided top-level key must
                          exist in the source object with an equal value.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      metric:
                        description: Metric filters by optimization target.
                        enum:
                        - latency
                        - throughput
                        type: string
                      minimumType:
                        description: |-
                          MinimumType filters by a minimum optimization level: candidates whose
                          type is this tier OR BETTER are accepted (hierarchy: optimized > general
                          > preview > unoptimized). This is the floor counterpart to the exact-match
                          Type field; the two AND together when both are set.

                          The sentinel "any" disables the floor (accept every tier). When this
                          field is empty the AIMService resolver applies a default floor of
                          "optimized" so auto-selection prefers production-grade profiles and never
                          silently picks an unoptimized one; to opt a service into lower tiers
                          (e.g. CPU/EPYC profiles published as unoptimized) set minimumType
                          explicitly to "unoptimized" or "any". Derivation selectors
                          (AIMProfileSet / AIMModel.profiles) treat empty as "any" so copying is
                          never tier-restricted by default.
                        enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                        - any
                        type: string
                      modelId:
                        description: ModelId filters by the source profile's specific
                          model identifier.
                        type: string
                      modelRef:
                        description: |-
                          ModelRef narrows candidates to those produced by a specific
                          AIM(Cluster)Model, matched via the `aim.eai.amd.com/source-model[-scope]`
                          labels stamped by the AIMModel reconcilers. Iteration 1 (v1alpha2 only).
                        properties:
                          name:
                            description: Name is the owning AIM(Cluster)Model name.
                              Required.
                            minLength: 1
                            type: string
                          scope:
                            default: Auto
                            description: Scope controls how Name is resolved against
                              AIMModel vs AIMClusterModel.
                            enum:
                            - Auto
                            - Namespace
                            - Cluster
                            type: string
                        required:
                        - name
                        type: object
                      origin:
                        description: |-
                          Origin filters by the `aim.eai.amd.com/profile-origin` label. When unset
                          (empty) the selector does not filter by origin. Iteration 1 (v1alpha2
                          only).
                        enum:
                        - discovered
                        - derived
                        - user-authored
                        type: string
                      precision:
                        description: Precision filters by numeric precision.
                        enum:
                        - auto
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                        type: string
                      profileId:
                        description: ProfileId filters by the source profile's profile
                          identifier.
                        type: string
                      role:
                        default: deployable
                        description: |-
                          Role filters by the `aim.eai.amd.com/profile-role` label. Defaults to
                          `deployable`. `base` filters to base profiles emitted by base-image
                          discovery (custom-model derivation source material).
                        enum:
                        - base
                        - deployable
                        type: string
                      type:
                        description: Type filters by optimization level (exact match).
                        enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                        type: string
                    type: object
                  serviceAccountName:
                    description: ServiceAccountName is propagated to managed profiles
                      for downstream workloads.
                    type: string
                  sourceRef:
                    description: |-
                      SourceRef points to an alternate discovery cache source.
                      When omitted, derivation uses visible AIMProfile and AIMClusterProfile objects.
                      For AIMProfileSet, the referenced ConfigMap is read from the same namespace.
                      For AIMClusterProfileSet, it is read from the operator namespace.
                    properties:
                      name:
                        description: Name is the discovery cache ConfigMap name.
                        type: string
                    required:
                    - name
                    type: object
                  version:
                    description: Version pins matching to a specific source profile
                      version when VersionPolicy is pinned.
                    type: string
                  versionPolicy:
                    default: pinned
                    description: VersionPolicy controls how matching profiles are
                      filtered by version.
                    enum:
                    - pinned
                    - latest
                    - all
                    type: string
                type: object
                x-kubernetes-validations:
                - message: selector must set at least one matching field, or set sourceRef
                  rule: has(self.sourceRef) || has(self.selector.aimId) || has(self.selector.modelId)
                    || has(self.selector.profileId) || has(self.selector.engine) ||
                    has(self.selector.metric) || has(self.selector.precision) || has(self.selector.type)
                    || has(self.selector.acceleratorModel) || has(self.selector.acceleratorType)
                    || has(self.selector.acceleratorCount) || has(self.selector.engineArgs)
                    || has(self.selector.modelRef) || has(self.selector.origin)
                - message: version is required when versionPolicy is pinned
                  rule: has(self.version) || (has(self.versionPolicy) && self.versionPolicy
                    != 'pinned')
                - message: latest/all versionPolicy must not set version
                  rule: '!has(self.versionPolicy) || (self.versionPolicy != ''latest''
                    && self.versionPolicy != ''all'') || !has(self.version)'
                - message: selector.aimId/modelId/profileId are not allowed when selector.role=base;
                    set overrides.aimId/modelId/profileId instead (base profiles have
                    no source identity to filter on)
                  rule: '!(has(self.selector.role) && self.selector.role == ''base'')
                    || (!has(self.selector.aimId) && !has(self.selector.modelId) &&
                    !has(self.selector.profileId))'
                - message: selector.role=base requires overrides.aimId and overrides.modelId
                    (base profiles carry no identity of their own; overrides supply
                    it)
                  rule: '!(has(self.selector.role) && self.selector.role == ''base'')
                    || (has(self.overrides) && has(self.overrides.aimId) && has(self.overrides.modelId))'
                - message: overrides.acceleratorCount must be specified together with
                    any overrides.acceleratorPartitioningMode; partition mode changes
                    the per-unit interpretation of acceleratorCount
                  rule: '!has(self.overrides) || !has(self.overrides.acceleratorPartitioningMode)
                    || size(self.overrides.acceleratorPartitioningMode) == 0 || has(self.overrides.acceleratorCount)'
              discovery:
                description: |-
                  Discovery controls discovery behavior for this model.
                  When unset, uses runtime config defaults.
                properties:
                  createServiceTemplates:
                    default: true
                    description: CreateServiceTemplates controls whether (cluster)
                      service templates are auto-created from the image metadata.
                    type: boolean
                  extractMetadata:
                    default: true
                    description: |-
                      ExtractMetadata controls whether metadata extraction runs for this model.
                      During metadata extraction, the controller connects to the image registry and
                      extracts the image's labels.
                    type: boolean
                type: object
              env:
                description: |-
                  Env specifies environment variables for authentication during model discovery and metadata extraction.
                  These variables are used for authentication with model registries (e.g., HuggingFace tokens).
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: |-
                        Name of the environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        fileKeyRef:
                          description: |-
                            FileKeyRef selects a key of the env file.
                            Requires the EnvFiles feature gate to be enabled.
                          properties:
                            key:
                              description: |-
                                The key within the env file. An invalid key will prevent the pod from starting.
                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                              type: string
                            optional:
                              default: false
                              description: |-
                                Specify whether the file or its key must be defined. If the file or key
                                does not exist, then the env var is not published.
                                If optional is set to true and the specified key does not exist,
                                the environment variable will not be set in the Pod's containers.

                                If optional is set to false and the specified key does not exist,
                                an error will be returned during Pod creation.
                              type: boolean
                            path:
                              description: |-
                                The path within the volume from which to select the file.
                                Must be relative and may not contain the '..' path or start with '..'.
                              type: string
                            volumeName:
                              description: The name of the volume mount containing
                                the env file.
                              type: string
                          required:
                          - key
                          - path
                          - volumeName
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              image:
                description: |-
                  Image is the container image URI for this AIM model.
                  This image is inspected by the operator to select runtime profiles used by templates.
                  Discovery behavior is controlled by the discovery field and runtime config's AutoDiscovery setting.
                  Required unless aimId is set with versionPolicy latest or any, or profileCopy is set.
                type: string
              imageMetadata:
                description: |-
                  ImageMetadata is the metadata that is used to determine which recommended service templates to create,
                  and to drive clients with richer metadata regarding this particular model. For most cases the user does
                  not need to set this field manually, for images that have the supported labels embedded in them
                  the `AIM(Cluster)Model.status.imageMetadata` field is automatically filled from the container image labels.
                  This field is intended to be used when there are network restrictions, or in other similar situations.
                  If this field is set, the remote extraction will not be performed at all.
                properties:
                  baseImageRef:
                    description: |-
                      BaseImageRef is the value of the AIM_BASE_IMAGE_REF environment variable
                      baked into the image's OCI config at build time. For AIM model images this
                      records the base image (e.g. "ghcr.io/silogen/aim-base:0.8.5") that the
                      model image was built from. Used by the AIMModel controller to resolve
                      the deployment image for fine-tuned models whose spec.image is omitted
                      (versionPolicy=latest or any).
                    type: string
                  model:
                    description: Model contains AMD Silogen model-specific metadata.
                    properties:
                      canonicalName:
                        description: |-
                          CanonicalName is the canonical model identifier (e.g., mistralai/Mixtral-8x22B-Instruct-v0.1).
                          Extracted from: org.amd.silogen.model.canonicalName
                        type: string
                      descriptionFull:
                        description: |-
                          DescriptionFull is the full description.
                          Extracted from: org.amd.silogen.description.full
                        type: string
                      hfTokenRequired:
                        description: |-
                          HFTokenRequired indicates if a HuggingFace token is required.
                          Extracted from: org.amd.silogen.hfToken.required
                        type: boolean
                      recommendedDeployments:
                        description: |-
                          RecommendedDeployments contains recommended deployment configurations.
                          Extracted from: org.amd.silogen.model.recommendedDeployments (parsed from JSON array)
                        items:
                          description: RecommendedDeployment describes a recommended
                            deployment configuration for a model.
                          properties:
                            description:
                              description: Description provides additional context
                                about this deployment configuration
                              type: string
                            gpuCount:
                              description: GPUCount is the number of GPUs required
                              format: int32
                              type: integer
                            gpuModel:
                              description: |-
                                GPUModel is the GPU model name (e.g., MI300X, MI325X).
                                The legacy v1alpha1 schema only models GPU accelerators; CPU profiles
                                emitted via OCI labels (gpuModel="CPU") are not first-class here and
                                are handled exclusively by the v1alpha2 native discovery pipeline.
                              type: string
                            metric:
                              description: Metric is the optimization target (e.g.,
                                latency, throughput)
                              type: string
                            precision:
                              description: Precision is the recommended precision
                                (e.g., fp8, fp16, bf16)
                              type: string
                            profileId:
                              description: |-
                                ProfileId is the unique identifier of the AIM profile for this deployment.
                                When set, templates created from this deployment will use this profile ID
                                to deterministically select the correct runtime profile in the AIM container.
                              type: string
                          type: object
                        type: array
                      releaseNotes:
                        description: |-
                          ReleaseNotes contains release notes for this version.
                          Extracted from: org.amd.silogen.release.notes
                        type: string
                      source:
                        description: |-
                          Source is the URL where the model can be found.
                          Extracted from: org.amd.silogen.model.source
                        type: string
                      tags:
                        description: |-
                          Tags are descriptive tags (e.g., ["text-generation", "chat", "instruction"]).
                          Extracted from: org.amd.silogen.model.tags (comma-separated)
                        items:
                          type: string
                        type: array
                      title:
                        description: |-
                          Title is the Silogen-specific title for the model.
                          Extracted from: org.amd.silogen.title
                        type: string
                      variants:
                        description: |-
                          Variants lists model variants.
                          Extracted from: org.amd.silogen.model.variants (comma-separated)
                        items:
                          type: string
                        type: array
                      versions:
                        description: |-
                          Versions lists available versions.
                          Extracted from: org.amd.silogen.model.versions (comma-separated)
                        items:
                          type: string
                        type: array
                    type: object
                  oci:
                    description: OCI contains standard OCI image metadata.
                    properties:
                      authors:
                        description: |-
                          Authors is contact details of the authors.
                          Extracted from: org.opencontainers.image.authors
                        type: string
                      created:
                        description: |-
                          Created is the creation timestamp.
                          Extracted from: org.opencontainers.image.created
                        type: string
                      description:
                        description: |-
                          Description is a brief description.
                          Extracted from: org.opencontainers.image.description
                        type: string
                      documentation:
                        description: |-
                          Documentation is the URL to documentation.
                          Extracted from: org.opencontainers.image.documentation
                        type: string
                      licenses:
                        description: |-
                          Licenses is the SPDX license identifier(s).
                          Extracted from: org.opencontainers.image.licenses
                        type: string
                      revision:
                        description: |-
                          Revision is the source control revision.
                          Extracted from: org.opencontainers.image.revision
                        type: string
                      source:
                        description: |-
                          Source is the URL to the source code repository.
                          Extracted from: org.opencontainers.image.source
                        type: string
                      title:
                        description: |-
                          Title is the human-readable title.
                          Extracted from: org.opencontainers.image.title
                        type: string
                      vendor:
                        description: |-
                          Vendor is the organization that produced the image.
                          Extracted from: org.opencontainers.image.vendor
                        type: string
                      version:
                        description: |-
                          Version is the image version.
                          Extracted from: org.opencontainers.image.version
                        type: string
                    type: object
                  originalLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      OriginalLabels contains the raw OCI image labels as a JSON object.
                      This preserves all labels from the image, including those not mapped to structured fields.
                    type: object
                type: object
              imagePullSecrets:
                description: |-
                  ImagePullSecrets lists secrets containing credentials for pulling the model container image.
                  These secrets are used for:
                  - OCI registry metadata extraction during discovery
                  - Pulling the image for inference services
                  The secrets are merged with any runtime config defaults.
                  For namespace-scoped models, secrets must exist in the same namespace.
                  For cluster-scoped models, secrets must exist in the operator namespace.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              modelSources:
                description: |-
                  ModelSources specifies the model sources to use for this model.
                  When specified, these sources are used instead of auto-discovery from the container image.
                  This enables pre-creating custom models with explicit model sources.
                  The size field is optional - if not specified, it will be discovered by the download job.
                  AIM runtime currently supports only one model source.
                items:
                  description: |-
                    AIMModelSource describes a model artifact that must be downloaded for inference.
                    Discovery extracts these from the container's configuration to enable caching and validation.
                  properties:
                    env:
                      description: |-
                        Env specifies per-source credential overrides.
                        These variables are used for authentication when downloading this specific source.
                        Takes precedence over base-level env for the same variable name.
                      items:
                        description: EnvVar represents an environment variable present
                          in a Container.
                        properties:
                          name:
                            description: |-
                              Name of the environment variable.
                              May consist of any printable ASCII characters except '='.
                            type: string
                          value:
                            description: |-
                              Variable references $(VAR_NAME) are expanded
                              using the previously defined environment variables in the container and
                              any service environment variables. If a variable cannot be resolved,
                              the reference in the input string will be unchanged. Double $$ are reduced
                              to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                              "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                              Escaped references will never be expanded, regardless of whether the variable
                              exists or not.
                              Defaults to "".
                            type: string
                          valueFrom:
                            description: Source for the environment variable's value.
                              Cannot be used if value is not empty.
                            properties:
                              configMapKeyRef:
                                description: Selects a key of a ConfigMap.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the ConfigMap or
                                      its key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              fieldRef:
                                description: |-
                                  Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                  spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                properties:
                                  apiVersion:
                                    description: Version of the schema the FieldPath
                                      is written in terms of, defaults to "v1".
                                    type: string
                                  fieldPath:
                                    description: Path of the field to select in the
                                      specified API version.
                                    type: string
                                required:
                                - fieldPath
                                type: object
                                x-kubernetes-map-type: atomic
                              fileKeyRef:
                                description: |-
                                  FileKeyRef selects a key of the env file.
                                  Requires the EnvFiles feature gate to be enabled.
                                properties:
                                  key:
                                    description: |-
                                      The key within the env file. An invalid key will prevent the pod from starting.
                                      The keys defined within a source may consist of any printable ASCII characters except '='.
                                      During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                    type: string
                                  optional:
                                    default: false
                                    description: |-
                                      Specify whether the file or its key must be defined. If the file or key
                                      does not exist, then the env var is not published.
                                      If optional is set to true and the specified key does not exist,
                                      the environment variable will not be set in the Pod's containers.

                                      If optional is set to false and the specified key does not exist,
                                      an error will be returned during Pod creation.
                                    type: boolean
                                  path:
                                    description: |-
                                      The path within the volume from which to select the file.
                                      Must be relative and may not contain the '..' path or start with '..'.
                                    type: string
                                  volumeName:
                                    description: The name of the volume mount containing
                                      the env file.
                                    type: string
                                required:
                                - key
                                - path
                                - volumeName
                                type: object
                                x-kubernetes-map-type: atomic
                              resourceFieldRef:
                                description: |-
                                  Selects a resource of the container: only resources limits and requests
                                  (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                properties:
                                  containerName:
                                    description: 'Container name: required for volumes,
                                      optional for env vars'
                                    type: string
                                  divisor:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: Specifies the output format of the
                                      exposed resources, defaults to "1"
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  resource:
                                    description: 'Required: resource to select'
                                    type: string
                                required:
                                - resource
                                type: object
                                x-kubernetes-map-type: atomic
                              secretKeyRef:
                                description: Selects a key of a secret in the pod's
                                  namespace
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                        required:
                        - name
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - name
                      x-kubernetes-list-type: map
                    modelId:
                      description: |-
                        ModelID is the canonical identifier in {org}/{name} format.
                        Determines the cache mount path: /workspace/cache/{modelId}
                        For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                        For S3 sources, users define their own organizational structure.
                      pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                      type: string
                    precision:
                      allOf:
                      - enum:
                        - auto
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                      - enum:
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                      description: |-
                        Precision describes the runtime precision this source is compatible with.
                        Used to match model sources to profiles during custom weight onboarding.
                      type: string
                    size:
                      anyOf:
                      - type: integer
                      - type: string
                      description: |-
                        Size is the expected storage space required for this model artifact.
                        Used for PVC sizing and capacity planning during cache creation.
                        Optional - if not specified, the download job will discover the size automatically.
                        Can be set explicitly to pre-allocate storage or override auto-discovery.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    sourceUri:
                      description: |-
                        SourceURI is the location from which the model should be downloaded.
                        Supported schemes:
                        - hf://org/model - Hugging Face Hub model
                        - s3://bucket/key - S3-compatible storage
                      pattern: ^(hf|s3)://[^ \t\r\n]+$
                      type: string
                  required:
                  - modelId
                  - sourceUri
                  type: object
                maxItems: 1
                type: array
              profileCopy:
                description: |-
                  ProfileCopy reuses the AIMProfileSet derivation shape so an AIMModel can
                  publish derivative AIMProfiles directly. The controller may synthesize a
                  child AIMProfileSet and fill SourceRef when image-backed discovery is
                  involved. Mutually exclusive with all deprecated v1alpha1 fields.

                  DEPRECATED on v1alpha2: use spec.profiles. v1alpha1 still accepts
                  ProfileCopy. v1alpha2 CRD CEL forbids ProfileCopy.
                properties:
                  image:
                    description: Image overrides the runtime image used by the derived
                      profiles.
                    type: string
                  imagePullSecrets:
                    description: ImagePullSecrets lists secrets used for inspecting
                      and pulling container images.
                    items:
                      description: |-
                        LocalObjectReference contains enough information to let you locate the
                        referenced object inside the same namespace.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    type: array
                  overrides:
                    description: Overrides mutates the copied profile spec after selection
                      and version filtering.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount replaces the copied profile's
                          acceleratorCount.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel replaces the copied profile's
                          acceleratorModel.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode replaces the copied profile's
                          acceleratorPartitioningMode. Complete replacement, not a merge; an empty
                          override string leaves the source profile's mode untouched. Same reserved
                          values as AIMProfileSpecCommon.AcceleratorPartitioningMode. Whenever this
                          override is set (to any value, including "unpartitioned"), the CEL rule on the
                          enclosing spec also requires AcceleratorCount to be set: partition mode
                          changes the per-unit interpretation of acceleratorCount, and CEL cannot
                          read the base profile to tell whether the meaning actually changed, so it
                          conservatively requires the count be restated.
                        type: string
                      aimId:
                        description: |-
                          AimId stamps the derived profile's spec.aimId. When set, wins over
                          the source profile's aimId. REQUIRED when the enclosing selector has
                          role=base (the source base profile carries no aimId of its own).
                        type: string
                      containerEnv:
                        description: ContainerEnv merges by env var name, overriding
                          matching source entries.
                        items:
                          description: EnvVar represents an environment variable present
                            in a Container.
                          properties:
                            name:
                              description: |-
                                Name of the environment variable.
                                May consist of any printable ASCII characters except '='.
                              type: string
                            value:
                              description: |-
                                Variable references $(VAR_NAME) are expanded
                                using the previously defined environment variables in the container and
                                any service environment variables. If a variable cannot be resolved,
                                the reference in the input string will be unchanged. Double $$ are reduced
                                to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                Escaped references will never be expanded, regardless of whether the variable
                                exists or not.
                                Defaults to "".
                              type: string
                            valueFrom:
                              description: Source for the environment variable's value.
                                Cannot be used if value is not empty.
                              properties:
                                configMapKeyRef:
                                  description: Selects a key of a ConfigMap.
                                  properties:
                                    key:
                                      description: The key to select.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the ConfigMap or
                                        its key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fieldRef:
                                  description: |-
                                    Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                    spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                  properties:
                                    apiVersion:
                                      description: Version of the schema the FieldPath
                                        is written in terms of, defaults to "v1".
                                      type: string
                                    fieldPath:
                                      description: Path of the field to select in
                                        the specified API version.
                                      type: string
                                  required:
                                  - fieldPath
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fileKeyRef:
                                  description: |-
                                    FileKeyRef selects a key of the env file.
                                    Requires the EnvFiles feature gate to be enabled.
                                  properties:
                                    key:
                                      description: |-
                                        The key within the env file. An invalid key will prevent the pod from starting.
                                        The keys defined within a source may consist of any printable ASCII characters except '='.
                                        During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                      type: string
                                    optional:
                                      default: false
                                      description: |-
                                        Specify whether the file or its key must be defined. If the file or key
                                        does not exist, then the env var is not published.
                                        If optional is set to true and the specified key does not exist,
                                        the environment variable will not be set in the Pod's containers.

                                        If optional is set to false and the specified key does not exist,
                                        an error will be returned during Pod creation.
                                      type: boolean
                                    path:
                                      description: |-
                                        The path within the volume from which to select the file.
                                        Must be relative and may not contain the '..' path or start with '..'.
                                      type: string
                                    volumeName:
                                      description: The name of the volume mount containing
                                        the env file.
                                      type: string
                                  required:
                                  - key
                                  - path
                                  - volumeName
                                  type: object
                                  x-kubernetes-map-type: atomic
                                resourceFieldRef:
                                  description: |-
                                    Selects a resource of the container: only resources limits and requests
                                    (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                  properties:
                                    containerName:
                                      description: 'Container name: required for volumes,
                                        optional for env vars'
                                      type: string
                                    divisor:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      description: Specifies the output format of
                                        the exposed resources, defaults to "1"
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    resource:
                                      description: 'Required: resource to select'
                                      type: string
                                  required:
                                  - resource
                                  type: object
                                  x-kubernetes-map-type: atomic
                                secretKeyRef:
                                  description: Selects a key of a secret in the pod's
                                    namespace
                                  properties:
                                    key:
                                      description: The key of the secret to select
                                        from.  Must be a valid secret key.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the Secret or its
                                        key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      engineArgs:
                        description: EngineArgs shallow-merges on top of the source
                          engineArgs, overriding matching keys.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      engineEnv:
                        additionalProperties:
                          type: string
                        description: EngineEnv merges by key, overriding matching
                          source entries.
                        type: object
                      image:
                        description: |-
                          Image overrides the runtime container image used by the derived
                          profiles. When empty the deployment image is rebased onto the source
                          profile's base-image (status.baseImage) so private mirrors stay
                          self-contained.
                        type: string
                      modelId:
                        description: |-
                          ModelId stamps the derived profile's spec.modelId. When set, wins
                          over both the source profile's modelId AND the auto-derivation from
                          modelSources[0].modelId. REQUIRED when the enclosing selector has
                          role=base.
                        type: string
                      modelSources:
                        description: |-
                          ModelSources replaces the copied profile's modelSources. When
                          modelSources[0].modelId is set and overrides.modelId is unset, the
                          derived profile's modelId is auto-derived from modelSources[0]; an
                          explicit overrides.modelId always wins.
                        items:
                          description: |-
                            AIMModelSource describes a model artifact that must be downloaded for inference.
                            Discovery extracts these from the container's configuration to enable caching and validation.
                          properties:
                            env:
                              description: |-
                                Env specifies per-source credential overrides.
                                These variables are used for authentication when downloading this specific source.
                                Takes precedence over base-level env for the same variable name.
                              items:
                                description: EnvVar represents an environment variable
                                  present in a Container.
                                properties:
                                  name:
                                    description: |-
                                      Name of the environment variable.
                                      May consist of any printable ASCII characters except '='.
                                    type: string
                                  value:
                                    description: |-
                                      Variable references $(VAR_NAME) are expanded
                                      using the previously defined environment variables in the container and
                                      any service environment variables. If a variable cannot be resolved,
                                      the reference in the input string will be unchanged. Double $$ are reduced
                                      to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                      "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                      Escaped references will never be expanded, regardless of whether the variable
                                      exists or not.
                                      Defaults to "".
                                    type: string
                                  valueFrom:
                                    description: Source for the environment variable's
                                      value. Cannot be used if value is not empty.
                                    properties:
                                      configMapKeyRef:
                                        description: Selects a key of a ConfigMap.
                                        properties:
                                          key:
                                            description: The key to select.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the ConfigMap
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fieldRef:
                                        description: |-
                                          Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                          spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                        properties:
                                          apiVersion:
                                            description: Version of the schema the
                                              FieldPath is written in terms of, defaults
                                              to "v1".
                                            type: string
                                          fieldPath:
                                            description: Path of the field to select
                                              in the specified API version.
                                            type: string
                                        required:
                                        - fieldPath
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fileKeyRef:
                                        description: |-
                                          FileKeyRef selects a key of the env file.
                                          Requires the EnvFiles feature gate to be enabled.
                                        properties:
                                          key:
                                            description: |-
                                              The key within the env file. An invalid key will prevent the pod from starting.
                                              The keys defined within a source may consist of any printable ASCII characters except '='.
                                              During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                            type: string
                                          optional:
                                            default: false
                                            description: |-
                                              Specify whether the file or its key must be defined. If the file or key
                                              does not exist, then the env var is not published.
                                              If optional is set to true and the specified key does not exist,
                                              the environment variable will not be set in the Pod's containers.

                                              If optional is set to false and the specified key does not exist,
                                              an error will be returned during Pod creation.
                                            type: boolean
                                          path:
                                            description: |-
                                              The path within the volume from which to select the file.
                                              Must be relative and may not contain the '..' path or start with '..'.
                                            type: string
                                          volumeName:
                                            description: The name of the volume mount
                                              containing the env file.
                                            type: string
                                        required:
                                        - key
                                        - path
                                        - volumeName
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      resourceFieldRef:
                                        description: |-
                                          Selects a resource of the container: only resources limits and requests
                                          (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                        properties:
                                          containerName:
                                            description: 'Container name: required
                                              for volumes, optional for env vars'
                                            type: string
                                          divisor:
                                            anyOf:
                                            - type: integer
                                            - type: string
                                            description: Specifies the output format
                                              of the exposed resources, defaults to
                                              "1"
                                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                            x-kubernetes-int-or-string: true
                                          resource:
                                            description: 'Required: resource to select'
                                            type: string
                                        required:
                                        - resource
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      secretKeyRef:
                                        description: Selects a key of a secret in
                                          the pod's namespace
                                        properties:
                                          key:
                                            description: The key of the secret to
                                              select from.  Must be a valid secret
                                              key.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the Secret
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                    type: object
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            modelId:
                              description: |-
                                ModelID is the canonical identifier in {org}/{name} format.
                                Determines the cache mount path: /workspace/cache/{modelId}
                                For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                                For S3 sources, users define their own organizational structure.
                              pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                              type: string
                            precision:
                              allOf:
                              - enum:
                                - auto
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              - enum:
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              description: |-
                                Precision describes the runtime precision this source is compatible with.
                                Used to match model sources to profiles during custom weight onboarding.
                              type: string
                            size:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                Size is the expected storage space required for this model artifact.
                                Used for PVC sizing and capacity planning during cache creation.
                                Optional - if not specified, the download job will discover the size automatically.
                                Can be set explicitly to pre-allocate storage or override auto-discovery.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            sourceUri:
                              description: |-
                                SourceURI is the location from which the model should be downloaded.
                                Supported schemes:
                                - hf://org/model - Hugging Face Hub model
                                - s3://bucket/key - S3-compatible storage
                              pattern: ^(hf|s3)://[^ \t\r\n]+$
                              type: string
                          required:
                          - modelId
                          - sourceUri
                          type: object
                        type: array
                      profileId:
                        description: |-
                          ProfileId stamps the derived profile's spec.profileId. Optional —
                          most callers leave this empty and let the source profile's profileId
                          carry through (or the reconciler synthesise one).
                        type: string
                    type: object
                  selector:
                    description: Selector chooses which source profiles to derive
                      from.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount filters by accelerator unit
                          count.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel filters by accelerator identifier.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode filters candidates by their declared
                          partitioning mode. Partial-order match (NOT strict equality):
                            ""              - no filter on this field.
                            "unpartitioned" - matches profiles with mode "" or "unpartitioned".
                            "partitioned"   - matches profiles whose mode is non-trivial (anything
                                              other than "" / "unpartitioned").
                            "<C>"           - selector-only convenience: matches profiles with mode
                                              "<C>-*" (prefix on the scheme). Not a valid profile-spec
                                              value (e.g. selector "CPX" matches "CPX-NPS1", "CPX-NPS4").
                            "<C>-<M>"       - exact-string match on the scheme.
                        type: string
                      acceleratorType:
                        description: AcceleratorType filters by accelerator resource
                          type.
                        enum:
                        - gpu
                        - cpu
                        type: string
                      aimId:
                        description: AimId filters by model architecture identifier
                          (e.g., "qwen/qwen3-32b").
                        type: string
                      engine:
                        description: Engine filters by inference engine.
                        type: string
                      engineArgs:
                        description: |-
                          EngineArgs partially matches source engineArgs: every provided top-level key must
                          exist in the source object with an equal value.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      metric:
                        description: Metric filters by optimization target.
                        enum:
                        - latency
                        - throughput
                        type: string
                      minimumType:
                        description: |-
                          MinimumType filters by a minimum optimization level: candidates whose
                          type is this tier OR BETTER are accepted (hierarchy: optimized > general
                          > preview > unoptimized). This is the floor counterpart to the exact-match
                          Type field; the two AND together when both are set.

                          The sentinel "any" disables the floor (accept every tier). When this
                          field is empty the AIMService resolver applies a default floor of
                          "optimized" so auto-selection prefers production-grade profiles and never
                          silently picks an unoptimized one; to opt a service into lower tiers
                          (e.g. CPU/EPYC profiles published as unoptimized) set minimumType
                          explicitly to "unoptimized" or "any". Derivation selectors
                          (AIMProfileSet / AIMModel.profiles) treat empty as "any" so copying is
                          never tier-restricted by default.
                        enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                        - any
                        type: string
                      modelId:
                        description: ModelId filters by the source profile's specific
                          model identifier.
                        type: string
                      modelRef:
                        description: |-
                          ModelRef narrows candidates to those produced by a specific
                          AIM(Cluster)Model, matched via the `aim.eai.amd.com/source-model[-scope]`
                          labels stamped by the AIMModel reconcilers. Iteration 1 (v1alpha2 only).
                        properties:
                          name:
                            description: Name is the owning AIM(Cluster)Model name.
                              Required.
                            minLength: 1
                            type: string
                          scope:
                            default: Auto
                            description: Scope controls how Name is resolved against
                              AIMModel vs AIMClusterModel.
                            enum:
                            - Auto
                            - Namespace
                            - Cluster
                            type: string
                        required:
                        - name
                        type: object
                      origin:
                        description: |-
                          Origin filters by the `aim.eai.amd.com/profile-origin` label. When unset
                          (empty) the selector does not filter by origin. Iteration 1 (v1alpha2
                          only).
                        enum:
                        - discovered
                        - derived
                        - user-authored
                        type: string
                      precision:
                        description: Precision filters by numeric precision.
                        enum:
                        - auto
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                        type: string
                      profileId:
                        description: ProfileId filters by the source profile's profile
                          identifier.
                        type: string
                      role:
                        default: deployable
                        description: |-
                          Role filters by the `aim.eai.amd.com/profile-role` label. Defaults to
                          `deployable`. `base` filters to base profiles emitted by base-image
                          discovery (custom-model derivation source material).
                        enum:
                        - base
                        - deployable
                        type: string
                      type:
                        description: Type filters by optimization level (exact match).
                        enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                        type: string
                    type: object
                  serviceAccountName:
                    description: ServiceAccountName is propagated to managed profiles
                      for downstream workloads.
                    type: string
                  sourceRef:
                    description: |-
                      SourceRef points to an alternate discovery cache source.
                      When omitted, derivation uses visible AIMProfile and AIMClusterProfile objects.
                      For AIMProfileSet, the referenced ConfigMap is read from the same namespace.
                      For AIMClusterProfileSet, it is read from the operator namespace.
                    properties:
                      name:
                        description: Name is the discovery cache ConfigMap name.
                        type: string
                    required:
                    - name
                    type: object
                  version:
                    description: Version pins matching to a specific source profile
                      version when VersionPolicy is pinned.
                    type: string
                  versionPolicy:
                    default: pinned
                    description: VersionPolicy controls how matching profiles are
                      filtered by version.
                    enum:
                    - pinned
                    - latest
                    - all
                    type: string
                type: object
                x-kubernetes-validations:
                - message: selector must set at least one matching field, or set sourceRef
                  rule: has(self.sourceRef) || has(self.selector.aimId) || has(self.selector.modelId)
                    || has(self.selector.profileId) || has(self.selector.engine) ||
                    has(self.selector.metric) || has(self.selector.precision) || has(self.selector.type)
                    || has(self.selector.acceleratorModel) || has(self.selector.acceleratorType)
                    || has(self.selector.acceleratorCount) || has(self.selector.engineArgs)
                    || has(self.selector.modelRef) || has(self.selector.origin)
                - message: version is required when versionPolicy is pinned
                  rule: has(self.version) || (has(self.versionPolicy) && self.versionPolicy
                    != 'pinned')
                - message: latest/all versionPolicy must not set version
                  rule: '!has(self.versionPolicy) || (self.versionPolicy != ''latest''
                    && self.versionPolicy != ''all'') || !has(self.version)'
                - message: selector.aimId/modelId/profileId are not allowed when selector.role=base;
                    set overrides.aimId/modelId/profileId instead (base profiles have
                    no source identity to filter on)
                  rule: '!(has(self.selector.role) && self.selector.role == ''base'')
                    || (!has(self.selector.aimId) && !has(self.selector.modelId) &&
                    !has(self.selector.profileId))'
                - message: selector.role=base requires overrides.aimId and overrides.modelId
                    (base profiles carry no identity of their own; overrides supply
                    it)
                  rule: '!(has(self.selector.role) && self.selector.role == ''base'')
                    || (has(self.overrides) && has(self.overrides.aimId) && has(self.overrides.modelId))'
                - message: overrides.acceleratorCount must be specified together with
                    any overrides.acceleratorPartitioningMode; partition mode changes
                    the per-unit interpretation of acceleratorCount
                  rule: '!has(self.overrides) || !has(self.overrides.acceleratorPartitioningMode)
                    || size(self.overrides.acceleratorPartitioningMode) == 0 || has(self.overrides.acceleratorCount)'
              profiles:
                description: |-
                  Profiles is the v1alpha2 fine-tune / custom-model onboarding surface.
                  It groups the source descriptor (`profiles.derivedFrom.selector` /
                  `profiles.derivedFrom.sourceRef`), the version filter
                  (`profiles.versionPolicy` / `profiles.version`), and the modifications
                  applied to copies (`profiles.overrides`). When set, the AIMModel
                  reconciler synthesises a child AIMProfileSet from this block.

                  Mutually exclusive with spec.image (exactly one of the two is required
                  for v1alpha2 AIMModel). v1alpha1 rejects spec.profiles via per-version
                  CEL.
                properties:
                  derivedFrom:
                    description: DerivedFrom identifies the source profiles to copy
                      from.
                    properties:
                      selector:
                        description: |-
                          Selector chooses which source profiles to derive from. Discovery of a
                          deployable AIM image stamps role=deployable; selector.role=base targets
                          the base profiles emitted by base-image discovery (custom-model
                          derivation source material).
                        properties:
                          acceleratorCount:
                            description: AcceleratorCount filters by accelerator unit
                              count.
                            format: int32
                            type: integer
                          acceleratorModel:
                            description: AcceleratorModel filters by accelerator identifier.
                            type: string
                          acceleratorPartitioningMode:
                            description: |-
                              AcceleratorPartitioningMode filters candidates by their declared
                              partitioning mode. Partial-order match (NOT strict equality):
                                ""              - no filter on this field.
                                "unpartitioned" - matches profiles with mode "" or "unpartitioned".
                                "partitioned"   - matches profiles whose mode is non-trivial (anything
                                                  other than "" / "unpartitioned").
                                "<C>"           - selector-only convenience: matches profiles with mode
                                                  "<C>-*" (prefix on the scheme). Not a valid profile-spec
                                                  value (e.g. selector "CPX" matches "CPX-NPS1", "CPX-NPS4").
                                "<C>-<M>"       - exact-string match on the scheme.
                            type: string
                          acceleratorType:
                            description: AcceleratorType filters by accelerator resource
                              type.
                            enum:
                            - gpu
                            - cpu
                            type: string
                          aimId:
                            description: AimId filters by model architecture identifier
                              (e.g., "qwen/qwen3-32b").
                            type: string
                          engine:
                            description: Engine filters by inference engine.
                            type: string
                          engineArgs:
                            description: |-
                              EngineArgs partially matches source engineArgs: every provided top-level key must
                              exist in the source object with an equal value.
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          metric:
                            description: Metric filters by optimization target.
                            enum:
                            - latency
                            - throughput
                            type: string
                          minimumType:
                            description: |-
                              MinimumType filters by a minimum optimization level: candidates whose
                              type is this tier OR BETTER are accepted (hierarchy: optimized > general
                              > preview > unoptimized). This is the floor counterpart to the exact-match
                              Type field; the two AND together when both are set.

                              The sentinel "any" disables the floor (accept every tier). When this
                              field is empty the AIMService resolver applies a default floor of
                              "optimized" so auto-selection prefers production-grade profiles and never
                              silently picks an unoptimized one; to opt a service into lower tiers
                              (e.g. CPU/EPYC profiles published as unoptimized) set minimumType
                              explicitly to "unoptimized" or "any". Derivation selectors
                              (AIMProfileSet / AIMModel.profiles) treat empty as "any" so copying is
                              never tier-restricted by default.
                            enum:
                            - optimized
                            - general
                            - preview
                            - unoptimized
                            - any
                            type: string
                          modelId:
                            description: ModelId filters by the source profile's specific
                              model identifier.
                            type: string
                          modelRef:
                            description: |-
                              ModelRef narrows candidates to those produced by a specific
                              AIM(Cluster)Model, matched via the `aim.eai.amd.com/source-model[-scope]`
                              labels stamped by the AIMModel reconcilers. Iteration 1 (v1alpha2 only).
                            properties:
                              name:
                                description: Name is the owning AIM(Cluster)Model
                                  name. Required.
                                minLength: 1
                                type: string
                              scope:
                                default: Auto
                                description: Scope controls how Name is resolved against
                                  AIMModel vs AIMClusterModel.
                                enum:
                                - Auto
                                - Namespace
                                - Cluster
                                type: string
                            required:
                            - name
                            type: object
                          origin:
                            description: |-
                              Origin filters by the `aim.eai.amd.com/profile-origin` label. When unset
                              (empty) the selector does not filter by origin. Iteration 1 (v1alpha2
                              only).
                            enum:
                            - discovered
                            - derived
                            - user-authored
                            type: string
                          precision:
                            description: Precision filters by numeric precision.
                            enum:
                            - auto
                            - fp4
                            - fp8
                            - fp16
                            - fp32
                            - bf16
                            - int4
                            - int8
                            type: string
                          profileId:
                            description: ProfileId filters by the source profile's
                              profile identifier.
                            type: string
                          role:
                            default: deployable
                            description: |-
                              Role filters by the `aim.eai.amd.com/profile-role` label. Defaults to
                              `deployable`. `base` filters to base profiles emitted by base-image
                              discovery (custom-model derivation source material).
                            enum:
                            - base
                            - deployable
                            type: string
                          type:
                            description: Type filters by optimization level (exact
                              match).
                            enum:
                            - optimized
                            - general
                            - preview
                            - unoptimized
                            type: string
                        type: object
                      sourceRef:
                        description: |-
                          SourceRef points to an alternate discovery cache source (a
                          pre-populated ConfigMap of profile YAMLs) instead of using the
                          visible AIMProfile / AIMClusterProfile objects.
                        properties:
                          name:
                            description: Name is the discovery cache ConfigMap name.
                            type: string
                        required:
                        - name
                        type: object
                    type: object
                  overrides:
                    description: |-
                      Overrides mutates the copied profile spec after selection and version
                      filtering. Use overrides.image to override the deployment container
                      image used by the derived profiles.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount replaces the copied profile's
                          acceleratorCount.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel replaces the copied profile's
                          acceleratorModel.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode replaces the copied profile's
                          acceleratorPartitioningMode. Complete replacement, not a merge; an empty
                          override string leaves the source profile's mode untouched. Same reserved
                          values as AIMProfileSpecCommon.AcceleratorPartitioningMode. Whenever this
                          override is set (to any value, including "unpartitioned"), the CEL rule on the
                          enclosing spec also requires AcceleratorCount to be set: partition mode
                          changes the per-unit interpretation of acceleratorCount, and CEL cannot
                          read the base profile to tell whether the meaning actually changed, so it
                          conservatively requires the count be restated.
                        type: string
                      aimId:
                        description: |-
                          AimId stamps the derived profile's spec.aimId. When set, wins over
                          the source profile's aimId. REQUIRED when the enclosing selector has
                          role=base (the source base profile carries no aimId of its own).
                        type: string
                      containerEnv:
                        description: ContainerEnv merges by env var name, overriding
                          matching source entries.
                        items:
                          description: EnvVar represents an environment variable present
                            in a Container.
                          properties:
                            name:
                              description: |-
                                Name of the environment variable.
                                May consist of any printable ASCII characters except '='.
                              type: string
                            value:
                              description: |-
                                Variable references $(VAR_NAME) are expanded
                                using the previously defined environment variables in the container and
                                any service environment variables. If a variable cannot be resolved,
                                the reference in the input string will be unchanged. Double $$ are reduced
                                to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                Escaped references will never be expanded, regardless of whether the variable
                                exists or not.
                                Defaults to "".
                              type: string
                            valueFrom:
                              description: Source for the environment variable's value.
                                Cannot be used if value is not empty.
                              properties:
                                configMapKeyRef:
                                  description: Selects a key of a ConfigMap.
                                  properties:
                                    key:
                                      description: The key to select.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the ConfigMap or
                                        its key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fieldRef:
                                  description: |-
                                    Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                    spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                  properties:
                                    apiVersion:
                                      description: Version of the schema the FieldPath
                                        is written in terms of, defaults to "v1".
                                      type: string
                                    fieldPath:
                                      description: Path of the field to select in
                                        the specified API version.
                                      type: string
                                  required:
                                  - fieldPath
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fileKeyRef:
                                  description: |-
                                    FileKeyRef selects a key of the env file.
                                    Requires the EnvFiles feature gate to be enabled.
                                  properties:
                                    key:
                                      description: |-
                                        The key within the env file. An invalid key will prevent the pod from starting.
                                        The keys defined within a source may consist of any printable ASCII characters except '='.
                                        During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                      type: string
                                    optional:
                                      default: false
                                      description: |-
                                        Specify whether the file or its key must be defined. If the file or key
                                        does not exist, then the env var is not published.
                                        If optional is set to true and the specified key does not exist,
                                        the environment variable will not be set in the Pod's containers.

                                        If optional is set to false and the specified key does not exist,
                                        an error will be returned during Pod creation.
                                      type: boolean
                                    path:
                                      description: |-
                                        The path within the volume from which to select the file.
                                        Must be relative and may not contain the '..' path or start with '..'.
                                      type: string
                                    volumeName:
                                      description: The name of the volume mount containing
                                        the env file.
                                      type: string
                                  required:
                                  - key
                                  - path
                                  - volumeName
                                  type: object
                                  x-kubernetes-map-type: atomic
                                resourceFieldRef:
                                  description: |-
                                    Selects a resource of the container: only resources limits and requests
                                    (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                  properties:
                                    containerName:
                                      description: 'Container name: required for volumes,
                                        optional for env vars'
                                      type: string
                                    divisor:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      description: Specifies the output format of
                                        the exposed resources, defaults to "1"
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    resource:
                                      description: 'Required: resource to select'
                                      type: string
                                  required:
                                  - resource
                                  type: object
                                  x-kubernetes-map-type: atomic
                                secretKeyRef:
                                  description: Selects a key of a secret in the pod's
                                    namespace
                                  properties:
                                    key:
                                      description: The key of the secret to select
                                        from.  Must be a valid secret key.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the Secret or its
                                        key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      engineArgs:
                        description: EngineArgs shallow-merges on top of the source
                          engineArgs, overriding matching keys.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      engineEnv:
                        additionalProperties:
                          type: string
                        description: EngineEnv merges by key, overriding matching
                          source entries.
                        type: object
                      image:
                        description: |-
                          Image overrides the runtime container image used by the derived
                          profiles. When empty the deployment image is rebased onto the source
                          profile's base-image (status.baseImage) so private mirrors stay
                          self-contained.
                        type: string
                      modelId:
                        description: |-
                          ModelId stamps the derived profile's spec.modelId. When set, wins
                          over both the source profile's modelId AND the auto-derivation from
                          modelSources[0].modelId. REQUIRED when the enclosing selector has
                          role=base.
                        type: string
                      modelSources:
                        description: |-
                          ModelSources replaces the copied profile's modelSources. When
                          modelSources[0].modelId is set and overrides.modelId is unset, the
                          derived profile's modelId is auto-derived from modelSources[0]; an
                          explicit overrides.modelId always wins.
                        items:
                          description: |-
                            AIMModelSource describes a model artifact that must be downloaded for inference.
                            Discovery extracts these from the container's configuration to enable caching and validation.
                          properties:
                            env:
                              description: |-
                                Env specifies per-source credential overrides.
                                These variables are used for authentication when downloading this specific source.
                                Takes precedence over base-level env for the same variable name.
                              items:
                                description: EnvVar represents an environment variable
                                  present in a Container.
                                properties:
                                  name:
                                    description: |-
                                      Name of the environment variable.
                                      May consist of any printable ASCII characters except '='.
                                    type: string
                                  value:
                                    description: |-
                                      Variable references $(VAR_NAME) are expanded
                                      using the previously defined environment variables in the container and
                                      any service environment variables. If a variable cannot be resolved,
                                      the reference in the input string will be unchanged. Double $$ are reduced
                                      to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                      "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                      Escaped references will never be expanded, regardless of whether the variable
                                      exists or not.
                                      Defaults to "".
                                    type: string
                                  valueFrom:
                                    description: Source for the environment variable's
                                      value. Cannot be used if value is not empty.
                                    properties:
                                      configMapKeyRef:
                                        description: Selects a key of a ConfigMap.
                                        properties:
                                          key:
                                            description: The key to select.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the ConfigMap
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fieldRef:
                                        description: |-
                                          Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                          spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                        properties:
                                          apiVersion:
                                            description: Version of the schema the
                                              FieldPath is written in terms of, defaults
                                              to "v1".
                                            type: string
                                          fieldPath:
                                            description: Path of the field to select
                                              in the specified API version.
                                            type: string
                                        required:
                                        - fieldPath
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fileKeyRef:
                                        description: |-
                                          FileKeyRef selects a key of the env file.
                                          Requires the EnvFiles feature gate to be enabled.
                                        properties:
                                          key:
                                            description: |-
                                              The key within the env file. An invalid key will prevent the pod from starting.
                                              The keys defined within a source may consist of any printable ASCII characters except '='.
                                              During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                            type: string
                                          optional:
                                            default: false
                                            description: |-
                                              Specify whether the file or its key must be defined. If the file or key
                                              does not exist, then the env var is not published.
                                              If optional is set to true and the specified key does not exist,
                                              the environment variable will not be set in the Pod's containers.

                                              If optional is set to false and the specified key does not exist,
                                              an error will be returned during Pod creation.
                                            type: boolean
                                          path:
                                            description: |-
                                              The path within the volume from which to select the file.
                                              Must be relative and may not contain the '..' path or start with '..'.
                                            type: string
                                          volumeName:
                                            description: The name of the volume mount
                                              containing the env file.
                                            type: string
                                        required:
                                        - key
                                        - path
                                        - volumeName
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      resourceFieldRef:
                                        description: |-
                                          Selects a resource of the container: only resources limits and requests
                                          (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                        properties:
                                          containerName:
                                            description: 'Container name: required
                                              for volumes, optional for env vars'
                                            type: string
                                          divisor:
                                            anyOf:
                                            - type: integer
                                            - type: string
                                            description: Specifies the output format
                                              of the exposed resources, defaults to
                                              "1"
                                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                            x-kubernetes-int-or-string: true
                                          resource:
                                            description: 'Required: resource to select'
                                            type: string
                                        required:
                                        - resource
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      secretKeyRef:
                                        description: Selects a key of a secret in
                                          the pod's namespace
                                        properties:
                                          key:
                                            description: The key of the secret to
                                              select from.  Must be a valid secret
                                              key.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the Secret
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                    type: object
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            modelId:
                              description: |-
                                ModelID is the canonical identifier in {org}/{name} format.
                                Determines the cache mount path: /workspace/cache/{modelId}
                                For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                                For S3 sources, users define their own organizational structure.
                              pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                              type: string
                            precision:
                              allOf:
                              - enum:
                                - auto
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              - enum:
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              description: |-
                                Precision describes the runtime precision this source is compatible with.
                                Used to match model sources to profiles during custom weight onboarding.
                              type: string
                            size:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                Size is the expected storage space required for this model artifact.
                                Used for PVC sizing and capacity planning during cache creation.
                                Optional - if not specified, the download job will discover the size automatically.
                                Can be set explicitly to pre-allocate storage or override auto-discovery.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            sourceUri:
                              description: |-
                                SourceURI is the location from which the model should be downloaded.
                                Supported schemes:
                                - hf://org/model - Hugging Face Hub model
                                - s3://bucket/key - S3-compatible storage
                              pattern: ^(hf|s3)://[^ \t\r\n]+$
                              type: string
                          required:
                          - modelId
                          - sourceUri
                          type: object
                        type: array
                      profileId:
                        description: |-
                          ProfileId stamps the derived profile's spec.profileId. Optional —
                          most callers leave this empty and let the source profile's profileId
                          carry through (or the reconciler synthesise one).
                        type: string
                    type: object
                  version:
                    description: |-
                      Version pins matching to a specific source profile version when
                      VersionPolicy is `pinned`.
                    type: string
                  versionPolicy:
                    default: pinned
                    description: VersionPolicy controls how matching profiles are
                      filtered by version.
                    enum:
                    - pinned
                    - latest
                    - all
                    type: string
                type: object
                x-kubernetes-validations:
                - message: profiles.derivedFrom is required when spec.profiles is
                    set
                  rule: has(self.derivedFrom)
                - message: profiles.version is required when versionPolicy is pinned
                  rule: has(self.version) || (has(self.versionPolicy) && self.versionPolicy
                    != 'pinned')
                - message: latest/all versionPolicy must not set version
                  rule: '!has(self.versionPolicy) || (self.versionPolicy != ''latest''
                    && self.versionPolicy != ''all'') || !has(self.version)'
                - message: derivedFrom.selector.aimId/modelId/profileId are not allowed
                    when selector.role=base; set overrides.aimId/modelId/profileId
                    instead (base profiles have no source identity to filter on)
                  rule: '!(has(self.derivedFrom.selector.role) && self.derivedFrom.selector.role
                    == ''base'') || (!has(self.derivedFrom.selector.aimId) && !has(self.derivedFrom.selector.modelId)
                    && !has(self.derivedFrom.selector.profileId))'
                - message: derivedFrom.selector.role=base requires overrides.aimId
                    and overrides.modelId (base profiles carry no identity of their
                    own; overrides supply it)
                  rule: '!(has(self.derivedFrom.selector.role) && self.derivedFrom.selector.role
                    == ''base'') || (has(self.overrides) && has(self.overrides.aimId)
                    && has(self.overrides.modelId))'
              resources:
                description: |-
                  Resources defines the default resource requirements for services using this model.
                  Template- or service-level values override these defaults.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              runtimeConfigName:
                description: |-
                  Name is the name of the runtime config to use for this resource. If a runtime config with this name exists both
                  as a namespace and a cluster runtime config, the values are merged together, the namespace config taking priority
                  over the cluster config when there are conflicts. If this field is empty or set to `default`, the namespace / cluster
                  runtime config with the name `default` is used, if it exists.
                type: string
              serviceAccountName:
                description: |-
                  ServiceAccountName specifies the Kubernetes service account to use for workloads related to this model.
                  This includes metadata extraction jobs and any other model-related operations.
                  If empty, the default service account for the namespace is used.
                type: string
            type: object
            x-kubernetes-validations:
            - message: when using modelSources without aimId, set custom.hardware
                or set hardware on each customTemplate
              rule: '!has(self.modelSources) || size(self.modelSources) == 0 || has(self.aimId)
                || (has(self.custom) && has(self.custom.hardware)) || !has(self.customTemplates)
                || size(self.customTemplates) == 0 || self.customTemplates.all(t,
                has(t.hardware) || (has(self.custom) && has(self.custom.hardware)))'
            - message: image is required unless aimId is set with versionPolicy latest
                or any, or profileCopy/derivedFrom/profiles is set
              rule: (has(self.image) && size(self.image) > 0) || has(self.profileCopy)
                || has(self.derivedFrom) || has(self.profiles) || (has(self.aimId)
                && has(self.custom) && has(self.custom.versionPolicy) && self.custom.versionPolicy
                != 'pinned')
            - message: profileCopy cannot be combined with deprecated legacy AIMModel
                fields
              rule: '!has(self.profileCopy) || (!has(self.discovery) && !has(self.defaultServiceTemplate)
                && !has(self.custom) && (!has(self.customTemplates) || size(self.customTemplates)
                == 0) && (!has(self.modelSources) || size(self.modelSources) == 0)
                && (!has(self.runtimeConfigName) || size(self.runtimeConfigName) ==
                0) && (!has(self.env) || size(self.env) == 0) && !has(self.imageMetadata))'
          status:
            description: AIMModelStatus defines the observed state of AIMModel.
            properties:
              aimId:
                description: |-
                  AimId is the resolved model architecture identifier for this model.
                  Populated by the v1alpha2 controller from spec.aimId or discovered metadata.
                type: string
              baseImage:
                description: |-
                  BaseImage is the extracted AIM base image reference (AIM_BASE_IMAGE_REF) when known.
                  Used when resolving deployment images for fine-tuned models that have no spec.image.
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the model's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              discoveredProfiles:
                description: DiscoveredProfiles summarizes the profiles found during
                  image discovery.
                properties:
                  byHardware:
                    description: |-
                      ByHardware groups discovered profiles by their hardware footprint and
                      reports whether each group is currently supported by the cluster. The
                      groups are stable across reconciles (sorted by acceleratorType,
                      acceleratorModel, acceleratorCount) so kubectl/jq queries are cheap.
                      Useful when debugging "why doesn't the {metric, precision} profile I
                      expect appear in my cluster?" — the breakdown surfaces every shape the
                      image emits, not only the ones materialised as AIMProfile objects.
                    items:
                      description: |-
                        ProfileHardwareGroup is one accelerator footprint within a model's
                        discovery catalog, with the metric/precision combos shipped under it.
                      properties:
                        acceleratorCount:
                          description: |-
                            AcceleratorCount is the number of accelerator units the profile
                            requests. For gpu: device count (e.g., 1, 2, 4, 8 for tensor-parallel
                            sizes). For cpu: number of CPU cores (e.g., 128 for EPYC_ZEN5,
                            192 for EPYC_9965).
                          format: int32
                          type: integer
                        acceleratorModel:
                          description: |-
                            AcceleratorModel is the accelerator identifier (e.g., "MI300X",
                            "EPYC_ZEN5"). Empty for profiles with no accelerator requirement.
                          type: string
                        acceleratorType:
                          description: AcceleratorType is the resource family (gpu,
                            cpu).
                          enum:
                          - gpu
                          - cpu
                          type: string
                        profiles:
                          description: |-
                            Profiles lists the {metric, precision} combinations discovered under
                            this hardware footprint. Reported even when the group is unsupported
                            so users can see what they're missing.
                          items:
                            description: |-
                              ProfileHardwareGroupEntry identifies one profile within a hardware group
                              by its {metric, precision} pair. Sufficient for users to spot whether the
                              optimization variant they want is shipped at all.
                            properties:
                              metric:
                                description: Metric is the optimization target (latency,
                                  throughput).
                                enum:
                                - latency
                                - throughput
                                type: string
                              precision:
                                description: Precision is the numeric precision (fp4,
                                  fp8, bf16, …).
                                enum:
                                - auto
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                                type: string
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        supported:
                          description: |-
                            Supported reports whether this hardware footprint is currently
                            satisfied by at least one cluster node. When false, all profiles in
                            this group are skipped during materialisation.
                          type: boolean
                      required:
                      - supported
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  supported:
                    description: Supported is the number of discovered profiles currently
                      supported by the cluster.
                    format: int32
                    type: integer
                  total:
                    description: Total is the number of profiles discovered from the
                      image.
                    format: int32
                    type: integer
                  unsupported:
                    description: Unsupported is the number of discovered profiles
                      currently not supported by the cluster.
                    format: int32
                    type: integer
                type: object
              discovery:
                description: Discovery tracks the state of the image-discovery Job
                  used to populate the discovery cache.
                properties:
                  attempts:
                    description: |-
                      Attempts is the number of discovery job attempts that have been made.
                      Increments each time a new discovery job is created after a failure.
                    format: int32
                    type: integer
                  lastAttemptTime:
                    description: |-
                      LastAttemptTime is the timestamp of the most recent discovery job creation.
                      Used to calculate exponential backoff before the next retry.
                    format: date-time
                    type: string
                  lastFailureReason:
                    description: LastFailureReason captures the reason for the most
                      recent discovery failure.
                    type: string
                  specHash:
                    description: |-
                      SpecHash is a hash of the model spec fields that invalidate cached discovery
                      (image, imagePullSecrets, serviceAccountName, discoveryCommandVersion).
                      When it changes, the operator drops the cache and re-runs the discovery Job.
                    type: string
                type: object
              discoveryCacheRef:
                description: |-
                  DiscoveryCacheRef points at the normalized discovery cache ConfigMap for image-backed flows.
                  Populated by the v1alpha2 controller after image inspection succeeds.
                properties:
                  name:
                    description: Name is the ConfigMap name.
                    type: string
                  namespace:
                    description: Namespace is the ConfigMap namespace.
                    type: string
                type: object
              imageMetadata:
                description: ImageMetadata is the metadata extracted from an AIM image
                properties:
                  baseImageRef:
                    description: |-
                      BaseImageRef is the value of the AIM_BASE_IMAGE_REF environment variable
                      baked into the image's OCI config at build time. For AIM model images this
                      records the base image (e.g. "ghcr.io/silogen/aim-base:0.8.5") that the
                      model image was built from. Used by the AIMModel controller to resolve
                      the deployment image for fine-tuned models whose spec.image is omitted
                      (versionPolicy=latest or any).
                    type: string
                  model:
                    description: Model contains AMD Silogen model-specific metadata.
                    properties:
                      canonicalName:
                        description: |-
                          CanonicalName is the canonical model identifier (e.g., mistralai/Mixtral-8x22B-Instruct-v0.1).
                          Extracted from: org.amd.silogen.model.canonicalName
                        type: string
                      descriptionFull:
                        description: |-
                          DescriptionFull is the full description.
                          Extracted from: org.amd.silogen.description.full
                        type: string
                      hfTokenRequired:
                        description: |-
                          HFTokenRequired indicates if a HuggingFace token is required.
                          Extracted from: org.amd.silogen.hfToken.required
                        type: boolean
                      recommendedDeployments:
                        description: |-
                          RecommendedDeployments contains recommended deployment configurations.
                          Extracted from: org.amd.silogen.model.recommendedDeployments (parsed from JSON array)
                        items:
                          description: RecommendedDeployment describes a recommended
                            deployment configuration for a model.
                          properties:
                            description:
                              description: Description provides additional context
                                about this deployment configuration
                              type: string
                            gpuCount:
                              description: GPUCount is the number of GPUs required
                              format: int32
                              type: integer
                            gpuModel:
                              description: |-
                                GPUModel is the GPU model name (e.g., MI300X, MI325X).
                                The legacy v1alpha1 schema only models GPU accelerators; CPU profiles
                                emitted via OCI labels (gpuModel="CPU") are not first-class here and
                                are handled exclusively by the v1alpha2 native discovery pipeline.
                              type: string
                            metric:
                              description: Metric is the optimization target (e.g.,
                                latency, throughput)
                              type: string
                            precision:
                              description: Precision is the recommended precision
                                (e.g., fp8, fp16, bf16)
                              type: string
                            profileId:
                              description: |-
                                ProfileId is the unique identifier of the AIM profile for this deployment.
                                When set, templates created from this deployment will use this profile ID
                                to deterministically select the correct runtime profile in the AIM container.
                              type: string
                          type: object
                        type: array
                      releaseNotes:
                        description: |-
                          ReleaseNotes contains release notes for this version.
                          Extracted from: org.amd.silogen.release.notes
                        type: string
                      source:
                        description: |-
                          Source is the URL where the model can be found.
                          Extracted from: org.amd.silogen.model.source
                        type: string
                      tags:
                        description: |-
                          Tags are descriptive tags (e.g., ["text-generation", "chat", "instruction"]).
                          Extracted from: org.amd.silogen.model.tags (comma-separated)
                        items:
                          type: string
                        type: array
                      title:
                        description: |-
                          Title is the Silogen-specific title for the model.
                          Extracted from: org.amd.silogen.title
                        type: string
                      variants:
                        description: |-
                          Variants lists model variants.
                          Extracted from: org.amd.silogen.model.variants (comma-separated)
                        items:
                          type: string
                        type: array
                      versions:
                        description: |-
                          Versions lists available versions.
                          Extracted from: org.amd.silogen.model.versions (comma-separated)
                        items:
                          type: string
                        type: array
                    type: object
                  oci:
                    description: OCI contains standard OCI image metadata.
                    properties:
                      authors:
                        description: |-
                          Authors is contact details of the authors.
                          Extracted from: org.opencontainers.image.authors
                        type: string
                      created:
                        description: |-
                          Created is the creation timestamp.
                          Extracted from: org.opencontainers.image.created
                        type: string
                      description:
                        description: |-
                          Description is a brief description.
                          Extracted from: org.opencontainers.image.description
                        type: string
                      documentation:
                        description: |-
                          Documentation is the URL to documentation.
                          Extracted from: org.opencontainers.image.documentation
                        type: string
                      licenses:
                        description: |-
                          Licenses is the SPDX license identifier(s).
                          Extracted from: org.opencontainers.image.licenses
                        type: string
                      revision:
                        description: |-
                          Revision is the source control revision.
                          Extracted from: org.opencontainers.image.revision
                        type: string
                      source:
                        description: |-
                          Source is the URL to the source code repository.
                          Extracted from: org.opencontainers.image.source
                        type: string
                      title:
                        description: |-
                          Title is the human-readable title.
                          Extracted from: org.opencontainers.image.title
                        type: string
                      vendor:
                        description: |-
                          Vendor is the organization that produced the image.
                          Extracted from: org.opencontainers.image.vendor
                        type: string
                      version:
                        description: |-
                          Version is the image version.
                          Extracted from: org.opencontainers.image.version
                        type: string
                    type: object
                  originalLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      OriginalLabels contains the raw OCI image labels as a JSON object.
                      This preserves all labels from the image, including those not mapped to structured fields.
                    type: object
                type: object
              kind:
                description: |-
                  Kind classifies the v1alpha2 onboarding flow that produced this
                  model's profiles (Image / Derived / Custom). See AIMModelKind for
                  the per-value semantics. Populated by the v1alpha2 controller from
                  the spec shape; left empty by the v1alpha1 controller.
                enum:
                - Image
                - Derived
                - Custom
                type: string
              managedProfiles:
                description: ManagedProfiles summarizes the direct promoted or derived
                  profiles owned or managed by this model.
                properties:
                  base:
                    description: |-
                      Base is the number of managed profiles whose spec is structurally
                      incomplete (missing aimId or modelSources). Base profiles cannot back
                      an AIMService directly and exist purely as source material for
                      derivation via AIMModel.spec.profiles.derivedFrom with
                      selector.role=base. A non-zero value is the canonical operational
                      signal that this model is a base-image model (the source of
                      custom-model derivation).
                    format: int32
                    type: integer
                  deployable:
                    description: |-
                      Deployable is the number of managed profiles whose spec is materialised
                      enough to back an AIMService (carries aimId and modelSources). For a
                      normal officially-discovered AIMModel this equals Total. For a
                      base-image AIMModel used as a source for custom-model derivation
                      this is 0 — the model only emits base profiles that callers must
                      derive into deployable profiles.
                    format: int32
                    type: integer
                  notAvailable:
                    description: NotAvailable is the number of derivative profiles
                      whose own status is NotAvailable.
                    format: int32
                    type: integer
                  ready:
                    description: Ready is the number of derivative profiles whose
                      own status is Ready.
                    format: int32
                    type: integer
                  total:
                    description: Total is the number of derivative profiles the controller
                      currently manages or intends to manage.
                    format: int32
                    type: integer
                required:
                - base
                - deployable
                - notAvailable
                - ready
                - total
                type: object
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller
                format: int64
                type: integer
              profileSetRef:
                description: ProfileSetRef identifies the child profile set synthesized
                  for derivation flows.
                properties:
                  name:
                    description: Name is the profile set name.
                    type: string
                  namespace:
                    description: Namespace is the profile set namespace.
                    type: string
                type: object
              resolvedRuntimeConfig:
                description: ResolvedRuntimeConfig captures metadata about the runtime
                  config that was resolved.
                properties:
                  kind:
                    description: Kind is the fully-qualified kind of the resolved
                      reference, when known.
                    type: string
                  name:
                    description: Name is the resource name that satisfied the reference.
                    type: string
                  namespace:
                    description: |-
                      Namespace identifies where the resource was found when namespace-scoped.
                      Empty indicates a cluster-scoped resource.
                    type: string
                  scope:
                    description: Scope indicates whether the resolved resource was
                      namespace or cluster scoped.
                    enum:
                    - Namespace
                    - Cluster
                    - Merged
                    - Unknown
                    type: string
                  uid:
                    description: UID captures the unique identifier of the resolved
                      reference, when known.
                    type: string
                type: object
              sourceType:
                description: |-
                  SourceType indicates how this model's artifacts are sourced.
                  - "Image": Model discovered from container image labels
                  - "Custom": Model uses explicit spec.modelSources
                  Set by the controller based on whether spec.modelSources is populated.

                  Note: only populated by the v1alpha1 controller; v1alpha2 consumers
                  should read .status.kind instead, which distinguishes fine-tunes
                  (Derived) from BYO base-image overlays (Custom).
                enum:
                - Image
                - Custom
                type: string
              status:
                default: Pending
                description: Status represents the overall status of the image based
                  on its templates
                enum:
                - Pending
                - Progressing
                - Ready
                - Degraded
                - Failed
                - NotAvailable
                type: string
              version:
                description: |-
                  Version is the effective image version of the model. Populated from
                  the spec.image tag (`amdenterpriseai/aim-qwen-qwen3-32b:0.11.0` →
                  `0.11.0`) during reconciliation. Empty for models that have no
                  spec.image (e.g. fine-tuned models derived from a parent) or that
                  reference an image by digest.

                  This mirrors AIMProfileStatus.Version so the two surfaces stay in
                  lock-step: a single image-tag extraction rule governs what users
                  see in the kubectl printcolumn for both kinds.

                  The image-author-declared version (i.e. the
                  `org.opencontainers.image.version` OCI label) is preserved separately
                  under .status.imageMetadata.oci.version for users that want to inspect
                  what the image build pipeline stamped. The two values usually agree;
                  when the OCI label is missing or empty, this field still surfaces a
                  useful version from the tag itself.
                type: string
            type: object
        type: object
        x-kubernetes-validations:
        - message: spec.derivedFrom is only supported on v1alpha2; use spec.profileCopy
            on v1alpha1
          rule: '!has(self.spec.derivedFrom)'
        - message: spec.profiles is only supported on v1alpha2; use spec.profileCopy
            on v1alpha1
          rule: '!has(self.spec.profiles)'
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.kind
      name: Kind
      type: string
    - jsonPath: .status.aimId
      name: AimID
      type: string
    - jsonPath: .status.version
      name: Version
      type: string
    - jsonPath: .status.managedProfiles.total
      name: Managed
      type: integer
    - jsonPath: .status.managedProfiles.ready
      name: Ready
      type: integer
    - jsonPath: .status.managedProfiles.base
      name: Base
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: |-
          AIMClusterModel is the Schema for cluster-scoped v1alpha2 AIM model resources.
          See AIMModel (api/v1alpha2/aimmodel_types.go) for the rationale of the
          CEL rules below — AIMClusterModel mirrors the namespace-scoped contract.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              AIMModelSpec defines the desired state of AIMModel.

              Per-version constraints (v1alpha1 forbids derivedFrom and profiles;
              v1alpha2 forbids profileCopy/custom/customTemplates/top-level modelSources,
              requires image XOR profiles, and forbids profiles mixed with legacy scalar
              fields) live on the version-specific AIMModel / AIMClusterModel root types
              in the per-version *_types.go files.
            properties:
              aimId:
                description: |-
                  AimId is the AIM product family identifier (e.g., "qwen/qwen3-32b").
                  When set together with modelSources, enables aimId-based template matching:
                  the controller finds official templates by aimId, filters by versionPolicy,
                  matches by modelId, and creates copies with the custom weight source.
                type: string
              custom:
                description: |-
                  Custom contains configuration for custom models (models with inline modelSources).
                  Only used when modelSources are specified; ignored for image-based models.
                properties:
                  hardware:
                    description: |-
                      Hardware specifies default hardware requirements for all templates.
                      Individual templates can override these defaults.
                      Required when modelSources is set and customTemplates is empty (unless aimId is set).
                    properties:
                      cpu:
                        description: CPU specifies CPU requirements.
                        properties:
                          limits:
                            anyOf:
                            - type: integer
                            - type: string
                            description: Limits is the maximum number of CPU cores
                              to allow.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          requests:
                            anyOf:
                            - type: integer
                            - type: string
                            description: Requests is the number of CPU cores to request.
                              Required and must be > 0.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                        required:
                        - requests
                        type: object
                      gpu:
                        description: GPU specifies GPU requirements. If not set, no
                          GPUs are requested (CPU-only model).
                        properties:
                          minVram:
                            anyOf:
                            - type: integer
                            - type: string
                            description: |-
                              MinVRAM limits deployment to GPUs having at least this much VRAM.
                              Used for capacity planning when the model size is known but any GPU with
                              sufficient VRAM is acceptable.
                              Cannot be combined with model.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          model:
                            description: |-
                              Model limits deployment to a specific GPU model.
                              Example: "MI300X"
                              Cannot be combined with minVram.
                            maxLength: 64
                            type: string
                          requests:
                            description: |-
                              Requests is the number of GPUs to set as requests/limits.
                              Set to 0 to target GPU nodes without consuming GPU resources (useful for testing).
                            format: int32
                            minimum: 0
                            type: integer
                          resourceName:
                            default: amd.com/gpu
                            description: |-
                              ResourceName is the Kubernetes resource name for GPU resources.
                              Defaults to "amd.com/gpu" if not specified.
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: model and minVram are mutually exclusive; specify
                            either a specific GPU model OR a minimum VRAM requirement,
                            not both
                          rule: '!(has(self.model) && size(self.model) > 0 && has(self.minVram))'
                    type: object
                    x-kubernetes-validations:
                    - message: at least one of gpu or cpu must be specified
                      rule: has(self.gpu) || has(self.cpu)
                  type:
                    allOf:
                    - enum:
                      - optimized
                      - general
                      - preview
                      - unoptimized
                    - enum:
                      - optimized
                      - general
                      - preview
                      - unoptimized
                    description: |-
                      Type specifies default type for all templates.
                      Individual templates can override this default.
                      When nil, templates default to "unoptimized".
                    type: string
                  versionPolicy:
                    default: pinned
                    description: |-
                      VersionPolicy controls how template versions are filtered during aimId-based matching.
                      - pinned (default): match templates whose status.version equals the model's image tag
                      - latest: match only templates at the newest available status.version
                      - all: match templates at any version
                      - any: deprecated alias of all, kept for backward compatibility
                      Only used when spec.aimId is set.
                    enum:
                    - pinned
                    - latest
                    - any
                    - all
                    type: string
                type: object
              customTemplates:
                description: |-
                  CustomTemplates defines explicit template configurations for this model.
                  These templates are created directly without running a discovery job.
                  Can be used with or without modelSources to define custom deployment configurations.
                  If omitted when modelSources is set, a single template is auto-generated
                  using the custom.hardware requirements.
                items:
                  description: |-
                    AIMCustomTemplate defines a custom template configuration for a model.
                    When modelSources are specified directly on AIMModel, customTemplates allow
                    defining explicit hardware requirements and profiles, skipping the discovery job.
                    This is an existing struct (not a CRD); it appears as an element of AIMModel.spec.customTemplates[].
                  properties:
                    aimId:
                      description: |-
                        AimId is the AIM product family identifier (e.g., "meta-llama/Llama-3-8B").
                        Required when customProfile is set.
                      type: string
                    customProfile:
                      description: |-
                        CustomProfile defines inline custom profile data for the inference engine.
                        When set, the resulting template will have a custom profile ConfigMap mounted.
                        Requires aimId, modelId, hardware, profile.metric, and profile.precision.
                      properties:
                        engineArgs:
                          description: |-
                            EngineArgs contains inference engine arguments as a free-form JSON object.
                            These are passed as CLI arguments to the inference engine (e.g., vLLM).
                            Do not include "model" — it is injected separately by the runtime.
                          x-kubernetes-preserve-unknown-fields: true
                        envVars:
                          additionalProperties:
                            type: string
                          description: |-
                            EnvVars contains environment variables applied to the inference engine process.
                            These are written into the profile YAML and applied by the AIM runtime via os.execv,
                            distinct from container-level Env which targets the AIM runtime container itself.
                            Keys must match ^[A-Z0-9_]+$ (uppercase with underscores).
                          type: object
                      type: object
                    env:
                      description: |-
                        Env specifies environment variable overrides when this template is selected.
                        These are container-level env vars applied to the AIM runtime container.
                      items:
                        description: EnvVar represents an environment variable present
                          in a Container.
                        properties:
                          name:
                            description: |-
                              Name of the environment variable.
                              May consist of any printable ASCII characters except '='.
                            type: string
                          value:
                            description: |-
                              Variable references $(VAR_NAME) are expanded
                              using the previously defined environment variables in the container and
                              any service environment variables. If a variable cannot be resolved,
                              the reference in the input string will be unchanged. Double $$ are reduced
                              to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                              "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                              Escaped references will never be expanded, regardless of whether the variable
                              exists or not.
                              Defaults to "".
                            type: string
                          valueFrom:
                            description: Source for the environment variable's value.
                              Cannot be used if value is not empty.
                            properties:
                              configMapKeyRef:
                                description: Selects a key of a ConfigMap.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the ConfigMap or
                                      its key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              fieldRef:
                                description: |-
                                  Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                  spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                properties:
                                  apiVersion:
                                    description: Version of the schema the FieldPath
                                      is written in terms of, defaults to "v1".
                                    type: string
                                  fieldPath:
                                    description: Path of the field to select in the
                                      specified API version.
                                    type: string
                                required:
                                - fieldPath
                                type: object
                                x-kubernetes-map-type: atomic
                              fileKeyRef:
                                description: |-
                                  FileKeyRef selects a key of the env file.
                                  Requires the EnvFiles feature gate to be enabled.
                                properties:
                                  key:
                                    description: |-
                                      The key within the env file. An invalid key will prevent the pod from starting.
                                      The keys defined within a source may consist of any printable ASCII characters except '='.
                                      During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                    type: string
                                  optional:
                                    default: false
                                    description: |-
                                      Specify whether the file or its key must be defined. If the file or key
                                      does not exist, then the env var is not published.
                                      If optional is set to true and the specified key does not exist,
                                      the environment variable will not be set in the Pod's containers.

                                      If optional is set to false and the specified key does not exist,
                                      an error will be returned during Pod creation.
                                    type: boolean
                                  path:
                                    description: |-
                                      The path within the volume from which to select the file.
                                      Must be relative and may not contain the '..' path or start with '..'.
                                    type: string
                                  volumeName:
                                    description: The name of the volume mount containing
                                      the env file.
                                    type: string
                                required:
                                - key
                                - path
                                - volumeName
                                type: object
                                x-kubernetes-map-type: atomic
                              resourceFieldRef:
                                description: |-
                                  Selects a resource of the container: only resources limits and requests
                                  (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                properties:
                                  containerName:
                                    description: 'Container name: required for volumes,
                                      optional for env vars'
                                    type: string
                                  divisor:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: Specifies the output format of the
                                      exposed resources, defaults to "1"
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  resource:
                                    description: 'Required: resource to select'
                                    type: string
                                required:
                                - resource
                                type: object
                                x-kubernetes-map-type: atomic
                              secretKeyRef:
                                description: Selects a key of a secret in the pod's
                                  namespace
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                        required:
                        - name
                        type: object
                      maxItems: 64
                      type: array
                      x-kubernetes-list-map-keys:
                      - name
                      x-kubernetes-list-type: map
                    hardware:
                      description: |-
                        Hardware specifies GPU and CPU requirements for this template.
                        Optional when spec.hardware is set (inherits from spec).
                        When both are set, values are merged field-by-field with template taking precedence.
                      properties:
                        cpu:
                          description: CPU specifies CPU requirements.
                          properties:
                            limits:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Limits is the maximum number of CPU cores
                                to allow.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            requests:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Requests is the number of CPU cores to
                                request. Required and must be > 0.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                          required:
                          - requests
                          type: object
                        gpu:
                          description: GPU specifies GPU requirements. If not set,
                            no GPUs are requested (CPU-only model).
                          properties:
                            minVram:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                MinVRAM limits deployment to GPUs having at least this much VRAM.
                                Used for capacity planning when the model size is known but any GPU with
                                sufficient VRAM is acceptable.
                                Cannot be combined with model.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            model:
                              description: |-
                                Model limits deployment to a specific GPU model.
                                Example: "MI300X"
                                Cannot be combined with minVram.
                              maxLength: 64
                              type: string
                            requests:
                              description: |-
                                Requests is the number of GPUs to set as requests/limits.
                                Set to 0 to target GPU nodes without consuming GPU resources (useful for testing).
                              format: int32
                              minimum: 0
                              type: integer
                            resourceName:
                              default: amd.com/gpu
                              description: |-
                                ResourceName is the Kubernetes resource name for GPU resources.
                                Defaults to "amd.com/gpu" if not specified.
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: model and minVram are mutually exclusive; specify
                              either a specific GPU model OR a minimum VRAM requirement,
                              not both
                            rule: '!(has(self.model) && size(self.model) > 0 && has(self.minVram))'
                      type: object
                      x-kubernetes-validations:
                      - message: at least one of gpu or cpu must be specified
                        rule: has(self.gpu) || has(self.cpu)
                    modelId:
                      description: |-
                        ModelId is the specific model identifier / HuggingFace URI (e.g., "Qwen/Qwen3-32B-FP8").
                        Required when customProfile is set.
                      type: string
                    name:
                      description: Name is the template name. If not provided, auto-generated
                        from model name + profile.
                      maxLength: 63
                      type: string
                    profile:
                      description: |-
                        Profile declares runtime profile variables for template selection.
                        Used when multiple templates exist to select based on metric/precision.
                      properties:
                        metric:
                          allOf:
                          - enum:
                            - latency
                            - throughput
                          - enum:
                            - latency
                            - throughput
                          description: Metric specifies the optimization target (e.g.,
                            latency, throughput).
                          type: string
                        precision:
                          allOf:
                          - enum:
                            - auto
                            - fp4
                            - fp8
                            - fp16
                            - fp32
                            - bf16
                            - int4
                            - int8
                          - enum:
                            - auto
                            - fp4
                            - fp8
                            - fp16
                            - fp32
                            - bf16
                            - int4
                            - int8
                          description: Precision specifies the numerical precision
                            (e.g., fp8, fp16, bf16).
                          type: string
                      type: object
                    type:
                      allOf:
                      - enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                      - enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                      default: unoptimized
                      description: |-
                        Type indicates the optimization status of this template.
                        - optimized: Template has been tuned for performance
                        - general: General-purpose tuning between optimized and preview
                        - preview: Template is experimental/pre-release
                        - unoptimized: Default, no specific optimizations applied
                      type: string
                  type: object
                  x-kubernetes-validations:
                  - message: when customProfile is set, aimId, modelId, hardware,
                      profile.metric, and profile.precision are required
                    rule: '!has(self.customProfile) || (has(self.aimId) && has(self.modelId)
                      && has(self.hardware) && has(self.profile) && has(self.profile.metric)
                      && has(self.profile.precision))'
                maxItems: 16
                type: array
              defaultServiceTemplate:
                description: |-
                  DefaultServiceTemplate specifies the default AIMServiceTemplate to use when creating services for this model.
                  When set, services that reference this model will use this template if no template is explicitly specified.
                  If this is not set, a template will be automatically selected.
                type: string
              derivedFrom:
                description: |-
                  DerivedFrom is the legacy flat shape of v1alpha2's profile-derivation
                  onboarding surface. New objects must use spec.profiles instead; the
                  field is retained so existing v1alpha2 objects (and the v1alpha2
                  reconciler reading them) round-trip cleanly.

                  DEPRECATED: prefer spec.profiles.derivedFrom on v1alpha2.
                properties:
                  image:
                    description: Image overrides the runtime image used by the derived
                      profiles.
                    type: string
                  imagePullSecrets:
                    description: ImagePullSecrets lists secrets used for inspecting
                      and pulling container images.
                    items:
                      description: |-
                        LocalObjectReference contains enough information to let you locate the
                        referenced object inside the same namespace.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    type: array
                  overrides:
                    description: Overrides mutates the copied profile spec after selection
                      and version filtering.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount replaces the copied profile's
                          acceleratorCount.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel replaces the copied profile's
                          acceleratorModel.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode replaces the copied profile's
                          acceleratorPartitioningMode. Complete replacement, not a merge; an empty
                          override string leaves the source profile's mode untouched. Same reserved
                          values as AIMProfileSpecCommon.AcceleratorPartitioningMode. Whenever this
                          override is set (to any value, including "unpartitioned"), the CEL rule on the
                          enclosing spec also requires AcceleratorCount to be set: partition mode
                          changes the per-unit interpretation of acceleratorCount, and CEL cannot
                          read the base profile to tell whether the meaning actually changed, so it
                          conservatively requires the count be restated.
                        type: string
                      aimId:
                        description: |-
                          AimId stamps the derived profile's spec.aimId. When set, wins over
                          the source profile's aimId. REQUIRED when the enclosing selector has
                          role=base (the source base profile carries no aimId of its own).
                        type: string
                      containerEnv:
                        description: ContainerEnv merges by env var name, overriding
                          matching source entries.
                        items:
                          description: EnvVar represents an environment variable present
                            in a Container.
                          properties:
                            name:
                              description: |-
                                Name of the environment variable.
                                May consist of any printable ASCII characters except '='.
                              type: string
                            value:
                              description: |-
                                Variable references $(VAR_NAME) are expanded
                                using the previously defined environment variables in the container and
                                any service environment variables. If a variable cannot be resolved,
                                the reference in the input string will be unchanged. Double $$ are reduced
                                to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                Escaped references will never be expanded, regardless of whether the variable
                                exists or not.
                                Defaults to "".
                              type: string
                            valueFrom:
                              description: Source for the environment variable's value.
                                Cannot be used if value is not empty.
                              properties:
                                configMapKeyRef:
                                  description: Selects a key of a ConfigMap.
                                  properties:
                                    key:
                                      description: The key to select.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the ConfigMap or
                                        its key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fieldRef:
                                  description: |-
                                    Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                    spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                  properties:
                                    apiVersion:
                                      description: Version of the schema the FieldPath
                                        is written in terms of, defaults to "v1".
                                      type: string
                                    fieldPath:
                                      description: Path of the field to select in
                                        the specified API version.
                                      type: string
                                  required:
                                  - fieldPath
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fileKeyRef:
                                  description: |-
                                    FileKeyRef selects a key of the env file.
                                    Requires the EnvFiles feature gate to be enabled.
                                  properties:
                                    key:
                                      description: |-
                                        The key within the env file. An invalid key will prevent the pod from starting.
                                        The keys defined within a source may consist of any printable ASCII characters except '='.
                                        During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                      type: string
                                    optional:
                                      default: false
                                      description: |-
                                        Specify whether the file or its key must be defined. If the file or key
                                        does not exist, then the env var is not published.
                                        If optional is set to true and the specified key does not exist,
                                        the environment variable will not be set in the Pod's containers.

                                        If optional is set to false and the specified key does not exist,
                                        an error will be returned during Pod creation.
                                      type: boolean
                                    path:
                                      description: |-
                                        The path within the volume from which to select the file.
                                        Must be relative and may not contain the '..' path or start with '..'.
                                      type: string
                                    volumeName:
                                      description: The name of the volume mount containing
                                        the env file.
                                      type: string
                                  required:
                                  - key
                                  - path
                                  - volumeName
                                  type: object
                                  x-kubernetes-map-type: atomic
                                resourceFieldRef:
                                  description: |-
                                    Selects a resource of the container: only resources limits and requests
                                    (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                  properties:
                                    containerName:
                                      description: 'Container name: required for volumes,
                                        optional for env vars'
                                      type: string
                                    divisor:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      description: Specifies the output format of
                                        the exposed resources, defaults to "1"
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    resource:
                                      description: 'Required: resource to select'
                                      type: string
                                  required:
                                  - resource
                                  type: object
                                  x-kubernetes-map-type: atomic
                                secretKeyRef:
                                  description: Selects a key of a secret in the pod's
                                    namespace
                                  properties:
                                    key:
                                      description: The key of the secret to select
                                        from.  Must be a valid secret key.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the Secret or its
                                        key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      engineArgs:
                        description: EngineArgs shallow-merges on top of the source
                          engineArgs, overriding matching keys.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      engineEnv:
                        additionalProperties:
                          type: string
                        description: EngineEnv merges by key, overriding matching
                          source entries.
                        type: object
                      image:
                        description: |-
                          Image overrides the runtime container image used by the derived
                          profiles. When empty the deployment image is rebased onto the source
                          profile's base-image (status.baseImage) so private mirrors stay
                          self-contained.
                        type: string
                      modelId:
                        description: |-
                          ModelId stamps the derived profile's spec.modelId. When set, wins
                          over both the source profile's modelId AND the auto-derivation from
                          modelSources[0].modelId. REQUIRED when the enclosing selector has
                          role=base.
                        type: string
                      modelSources:
                        description: |-
                          ModelSources replaces the copied profile's modelSources. When
                          modelSources[0].modelId is set and overrides.modelId is unset, the
                          derived profile's modelId is auto-derived from modelSources[0]; an
                          explicit overrides.modelId always wins.
                        items:
                          description: |-
                            AIMModelSource describes a model artifact that must be downloaded for inference.
                            Discovery extracts these from the container's configuration to enable caching and validation.
                          properties:
                            env:
                              description: |-
                                Env specifies per-source credential overrides.
                                These variables are used for authentication when downloading this specific source.
                                Takes precedence over base-level env for the same variable name.
                              items:
                                description: EnvVar represents an environment variable
                                  present in a Container.
                                properties:
                                  name:
                                    description: |-
                                      Name of the environment variable.
                                      May consist of any printable ASCII characters except '='.
                                    type: string
                                  value:
                                    description: |-
                                      Variable references $(VAR_NAME) are expanded
                                      using the previously defined environment variables in the container and
                                      any service environment variables. If a variable cannot be resolved,
                                      the reference in the input string will be unchanged. Double $$ are reduced
                                      to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                      "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                      Escaped references will never be expanded, regardless of whether the variable
                                      exists or not.
                                      Defaults to "".
                                    type: string
                                  valueFrom:
                                    description: Source for the environment variable's
                                      value. Cannot be used if value is not empty.
                                    properties:
                                      configMapKeyRef:
                                        description: Selects a key of a ConfigMap.
                                        properties:
                                          key:
                                            description: The key to select.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the ConfigMap
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fieldRef:
                                        description: |-
                                          Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                          spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                        properties:
                                          apiVersion:
                                            description: Version of the schema the
                                              FieldPath is written in terms of, defaults
                                              to "v1".
                                            type: string
                                          fieldPath:
                                            description: Path of the field to select
                                              in the specified API version.
                                            type: string
                                        required:
                                        - fieldPath
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fileKeyRef:
                                        description: |-
                                          FileKeyRef selects a key of the env file.
                                          Requires the EnvFiles feature gate to be enabled.
                                        properties:
                                          key:
                                            description: |-
                                              The key within the env file. An invalid key will prevent the pod from starting.
                                              The keys defined within a source may consist of any printable ASCII characters except '='.
                                              During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                            type: string
                                          optional:
                                            default: false
                                            description: |-
                                              Specify whether the file or its key must be defined. If the file or key
                                              does not exist, then the env var is not published.
                                              If optional is set to true and the specified key does not exist,
                                              the environment variable will not be set in the Pod's containers.

                                              If optional is set to false and the specified key does not exist,
                                              an error will be returned during Pod creation.
                                            type: boolean
                                          path:
                                            description: |-
                                              The path within the volume from which to select the file.
                                              Must be relative and may not contain the '..' path or start with '..'.
                                            type: string
                                          volumeName:
                                            description: The name of the volume mount
                                              containing the env file.
                                            type: string
                                        required:
                                        - key
                                        - path
                                        - volumeName
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      resourceFieldRef:
                                        description: |-
                                          Selects a resource of the container: only resources limits and requests
                                          (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                        properties:
                                          containerName:
                                            description: 'Container name: required
                                              for volumes, optional for env vars'
                                            type: string
                                          divisor:
                                            anyOf:
                                            - type: integer
                                            - type: string
                                            description: Specifies the output format
                                              of the exposed resources, defaults to
                                              "1"
                                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                            x-kubernetes-int-or-string: true
                                          resource:
                                            description: 'Required: resource to select'
                                            type: string
                                        required:
                                        - resource
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      secretKeyRef:
                                        description: Selects a key of a secret in
                                          the pod's namespace
                                        properties:
                                          key:
                                            description: The key of the secret to
                                              select from.  Must be a valid secret
                                              key.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the Secret
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                    type: object
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            modelId:
                              description: |-
                                ModelID is the canonical identifier in {org}/{name} format.
                                Determines the cache mount path: /workspace/cache/{modelId}
                                For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                                For S3 sources, users define their own organizational structure.
                              pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                              type: string
                            precision:
                              allOf:
                              - enum:
                                - auto
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              - enum:
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              description: |-
                                Precision describes the runtime precision this source is compatible with.
                                Used to match model sources to profiles during custom weight onboarding.
                              type: string
                            size:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                Size is the expected storage space required for this model artifact.
                                Used for PVC sizing and capacity planning during cache creation.
                                Optional - if not specified, the download job will discover the size automatically.
                                Can be set explicitly to pre-allocate storage or override auto-discovery.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            sourceUri:
                              description: |-
                                SourceURI is the location from which the model should be downloaded.
                                Supported schemes:
                                - hf://org/model - Hugging Face Hub model
                                - s3://bucket/key - S3-compatible storage
                              pattern: ^(hf|s3)://[^ \t\r\n]+$
                              type: string
                          required:
                          - modelId
                          - sourceUri
                          type: object
                        type: array
                      profileId:
                        description: |-
                          ProfileId stamps the derived profile's spec.profileId. Optional —
                          most callers leave this empty and let the source profile's profileId
                          carry through (or the reconciler synthesise one).
                        type: string
                    type: object
                  selector:
                    description: Selector chooses which source profiles to derive
                      from.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount filters by accelerator unit
                          count.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel filters by accelerator identifier.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode filters candidates by their declared
                          partitioning mode. Partial-order match (NOT strict equality):
                            ""              - no filter on this field.
                            "unpartitioned" - matches profiles with mode "" or "unpartitioned".
                            "partitioned"   - matches profiles whose mode is non-trivial (anything
                                              other than "" / "unpartitioned").
                            "<C>"           - selector-only convenience: matches profiles with mode
                                              "<C>-*" (prefix on the scheme). Not a valid profile-spec
                                              value (e.g. selector "CPX" matches "CPX-NPS1", "CPX-NPS4").
                            "<C>-<M>"       - exact-string match on the scheme.
                        type: string
                      acceleratorType:
                        description: AcceleratorType filters by accelerator resource
                          type.
                        enum:
                        - gpu
                        - cpu
                        type: string
                      aimId:
                        description: AimId filters by model architecture identifier
                          (e.g., "qwen/qwen3-32b").
                        type: string
                      engine:
                        description: Engine filters by inference engine.
                        type: string
                      engineArgs:
                        description: |-
                          EngineArgs partially matches source engineArgs: every provided top-level key must
                          exist in the source object with an equal value.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      metric:
                        description: Metric filters by optimization target.
                        enum:
                        - latency
                        - throughput
                        type: string
                      minimumType:
                        description: |-
                          MinimumType filters by a minimum optimization level: candidates whose
                          type is this tier OR BETTER are accepted (hierarchy: optimized > general
                          > preview > unoptimized). This is the floor counterpart to the exact-match
                          Type field; the two AND together when both are set.

                          The sentinel "any" disables the floor (accept every tier). When this
                          field is empty the AIMService resolver applies a default floor of
                          "optimized" so auto-selection prefers production-grade profiles and never
                          silently picks an unoptimized one; to opt a service into lower tiers
                          (e.g. CPU/EPYC profiles published as unoptimized) set minimumType
                          explicitly to "unoptimized" or "any". Derivation selectors
                          (AIMProfileSet / AIMModel.profiles) treat empty as "any" so copying is
                          never tier-restricted by default.
                        enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                        - any
                        type: string
                      modelId:
                        description: ModelId filters by the source profile's specific
                          model identifier.
                        type: string
                      modelRef:
                        description: |-
                          ModelRef narrows candidates to those produced by a specific
                          AIM(Cluster)Model, matched via the `aim.eai.amd.com/source-model[-scope]`
                          labels stamped by the AIMModel reconcilers. Iteration 1 (v1alpha2 only).
                        properties:
                          name:
                            description: Name is the owning AIM(Cluster)Model name.
                              Required.
                            minLength: 1
                            type: string
                          scope:
                            default: Auto
                            description: Scope controls how Name is resolved against
                              AIMModel vs AIMClusterModel.
                            enum:
                            - Auto
                            - Namespace
                            - Cluster
                            type: string
                        required:
                        - name
                        type: object
                      origin:
                        description: |-
                          Origin filters by the `aim.eai.amd.com/profile-origin` label. When unset
                          (empty) the selector does not filter by origin. Iteration 1 (v1alpha2
                          only).
                        enum:
                        - discovered
                        - derived
                        - user-authored
                        type: string
                      precision:
                        description: Precision filters by numeric precision.
                        enum:
                        - auto
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                        type: string
                      profileId:
                        description: ProfileId filters by the source profile's profile
                          identifier.
                        type: string
                      role:
                        default: deployable
                        description: |-
                          Role filters by the `aim.eai.amd.com/profile-role` label. Defaults to
                          `deployable`. `base` filters to base profiles emitted by base-image
                          discovery (custom-model derivation source material).
                        enum:
                        - base
                        - deployable
                        type: string
                      type:
                        description: Type filters by optimization level (exact match).
                        enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                        type: string
                    type: object
                  serviceAccountName:
                    description: ServiceAccountName is propagated to managed profiles
                      for downstream workloads.
                    type: string
                  sourceRef:
                    description: |-
                      SourceRef points to an alternate discovery cache source.
                      When omitted, derivation uses visible AIMProfile and AIMClusterProfile objects.
                      For AIMProfileSet, the referenced ConfigMap is read from the same namespace.
                      For AIMClusterProfileSet, it is read from the operator namespace.
                    properties:
                      name:
                        description: Name is the discovery cache ConfigMap name.
                        type: string
                    required:
                    - name
                    type: object
                  version:
                    description: Version pins matching to a specific source profile
                      version when VersionPolicy is pinned.
                    type: string
                  versionPolicy:
                    default: pinned
                    description: VersionPolicy controls how matching profiles are
                      filtered by version.
                    enum:
                    - pinned
                    - latest
                    - all
                    type: string
                type: object
                x-kubernetes-validations:
                - message: selector must set at least one matching field, or set sourceRef
                  rule: has(self.sourceRef) || has(self.selector.aimId) || has(self.selector.modelId)
                    || has(self.selector.profileId) || has(self.selector.engine) ||
                    has(self.selector.metric) || has(self.selector.precision) || has(self.selector.type)
                    || has(self.selector.acceleratorModel) || has(self.selector.acceleratorType)
                    || has(self.selector.acceleratorCount) || has(self.selector.engineArgs)
                    || has(self.selector.modelRef) || has(self.selector.origin)
                - message: version is required when versionPolicy is pinned
                  rule: has(self.version) || (has(self.versionPolicy) && self.versionPolicy
                    != 'pinned')
                - message: latest/all versionPolicy must not set version
                  rule: '!has(self.versionPolicy) || (self.versionPolicy != ''latest''
                    && self.versionPolicy != ''all'') || !has(self.version)'
                - message: selector.aimId/modelId/profileId are not allowed when selector.role=base;
                    set overrides.aimId/modelId/profileId instead (base profiles have
                    no source identity to filter on)
                  rule: '!(has(self.selector.role) && self.selector.role == ''base'')
                    || (!has(self.selector.aimId) && !has(self.selector.modelId) &&
                    !has(self.selector.profileId))'
                - message: selector.role=base requires overrides.aimId and overrides.modelId
                    (base profiles carry no identity of their own; overrides supply
                    it)
                  rule: '!(has(self.selector.role) && self.selector.role == ''base'')
                    || (has(self.overrides) && has(self.overrides.aimId) && has(self.overrides.modelId))'
                - message: overrides.acceleratorCount must be specified together with
                    any overrides.acceleratorPartitioningMode; partition mode changes
                    the per-unit interpretation of acceleratorCount
                  rule: '!has(self.overrides) || !has(self.overrides.acceleratorPartitioningMode)
                    || size(self.overrides.acceleratorPartitioningMode) == 0 || has(self.overrides.acceleratorCount)'
              discovery:
                description: |-
                  Discovery controls discovery behavior for this model.
                  When unset, uses runtime config defaults.
                properties:
                  createServiceTemplates:
                    default: true
                    description: CreateServiceTemplates controls whether (cluster)
                      service templates are auto-created from the image metadata.
                    type: boolean
                  extractMetadata:
                    default: true
                    description: |-
                      ExtractMetadata controls whether metadata extraction runs for this model.
                      During metadata extraction, the controller connects to the image registry and
                      extracts the image's labels.
                    type: boolean
                type: object
              env:
                description: |-
                  Env specifies environment variables for authentication during model discovery and metadata extraction.
                  These variables are used for authentication with model registries (e.g., HuggingFace tokens).
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: |-
                        Name of the environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        fileKeyRef:
                          description: |-
                            FileKeyRef selects a key of the env file.
                            Requires the EnvFiles feature gate to be enabled.
                          properties:
                            key:
                              description: |-
                                The key within the env file. An invalid key will prevent the pod from starting.
                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                              type: string
                            optional:
                              default: false
                              description: |-
                                Specify whether the file or its key must be defined. If the file or key
                                does not exist, then the env var is not published.
                                If optional is set to true and the specified key does not exist,
                                the environment variable will not be set in the Pod's containers.

                                If optional is set to false and the specified key does not exist,
                                an error will be returned during Pod creation.
                              type: boolean
                            path:
                              description: |-
                                The path within the volume from which to select the file.
                                Must be relative and may not contain the '..' path or start with '..'.
                              type: string
                            volumeName:
                              description: The name of the volume mount containing
                                the env file.
                              type: string
                          required:
                          - key
                          - path
                          - volumeName
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              image:
                description: |-
                  Image is the container image URI for this AIM model.
                  This image is inspected by the operator to select runtime profiles used by templates.
                  Discovery behavior is controlled by the discovery field and runtime config's AutoDiscovery setting.
                  Required unless aimId is set with versionPolicy latest or any, or profileCopy is set.
                type: string
              imageMetadata:
                description: |-
                  ImageMetadata is the metadata that is used to determine which recommended service templates to create,
                  and to drive clients with richer metadata regarding this particular model. For most cases the user does
                  not need to set this field manually, for images that have the supported labels embedded in them
                  the `AIM(Cluster)Model.status.imageMetadata` field is automatically filled from the container image labels.
                  This field is intended to be used when there are network restrictions, or in other similar situations.
                  If this field is set, the remote extraction will not be performed at all.
                properties:
                  baseImageRef:
                    description: |-
                      BaseImageRef is the value of the AIM_BASE_IMAGE_REF environment variable
                      baked into the image's OCI config at build time. For AIM model images this
                      records the base image (e.g. "ghcr.io/silogen/aim-base:0.8.5") that the
                      model image was built from. Used by the AIMModel controller to resolve
                      the deployment image for fine-tuned models whose spec.image is omitted
                      (versionPolicy=latest or any).
                    type: string
                  model:
                    description: Model contains AMD Silogen model-specific metadata.
                    properties:
                      canonicalName:
                        description: |-
                          CanonicalName is the canonical model identifier (e.g., mistralai/Mixtral-8x22B-Instruct-v0.1).
                          Extracted from: org.amd.silogen.model.canonicalName
                        type: string
                      descriptionFull:
                        description: |-
                          DescriptionFull is the full description.
                          Extracted from: org.amd.silogen.description.full
                        type: string
                      hfTokenRequired:
                        description: |-
                          HFTokenRequired indicates if a HuggingFace token is required.
                          Extracted from: org.amd.silogen.hfToken.required
                        type: boolean
                      recommendedDeployments:
                        description: |-
                          RecommendedDeployments contains recommended deployment configurations.
                          Extracted from: org.amd.silogen.model.recommendedDeployments (parsed from JSON array)
                        items:
                          description: RecommendedDeployment describes a recommended
                            deployment configuration for a model.
                          properties:
                            description:
                              description: Description provides additional context
                                about this deployment configuration
                              type: string
                            gpuCount:
                              description: GPUCount is the number of GPUs required
                              format: int32
                              type: integer
                            gpuModel:
                              description: |-
                                GPUModel is the GPU model name (e.g., MI300X, MI325X).
                                The legacy v1alpha1 schema only models GPU accelerators; CPU profiles
                                emitted via OCI labels (gpuModel="CPU") are not first-class here and
                                are handled exclusively by the v1alpha2 native discovery pipeline.
                              type: string
                            metric:
                              description: Metric is the optimization target (e.g.,
                                latency, throughput)
                              type: string
                            precision:
                              description: Precision is the recommended precision
                                (e.g., fp8, fp16, bf16)
                              type: string
                            profileId:
                              description: |-
                                ProfileId is the unique identifier of the AIM profile for this deployment.
                                When set, templates created from this deployment will use this profile ID
                                to deterministically select the correct runtime profile in the AIM container.
                              type: string
                          type: object
                        type: array
                      releaseNotes:
                        description: |-
                          ReleaseNotes contains release notes for this version.
                          Extracted from: org.amd.silogen.release.notes
                        type: string
                      source:
                        description: |-
                          Source is the URL where the model can be found.
                          Extracted from: org.amd.silogen.model.source
                        type: string
                      tags:
                        description: |-
                          Tags are descriptive tags (e.g., ["text-generation", "chat", "instruction"]).
                          Extracted from: org.amd.silogen.model.tags (comma-separated)
                        items:
                          type: string
                        type: array
                      title:
                        description: |-
                          Title is the Silogen-specific title for the model.
                          Extracted from: org.amd.silogen.title
                        type: string
                      variants:
                        description: |-
                          Variants lists model variants.
                          Extracted from: org.amd.silogen.model.variants (comma-separated)
                        items:
                          type: string
                        type: array
                      versions:
                        description: |-
                          Versions lists available versions.
                          Extracted from: org.amd.silogen.model.versions (comma-separated)
                        items:
                          type: string
                        type: array
                    type: object
                  oci:
                    description: OCI contains standard OCI image metadata.
                    properties:
                      authors:
                        description: |-
                          Authors is contact details of the authors.
                          Extracted from: org.opencontainers.image.authors
                        type: string
                      created:
                        description: |-
                          Created is the creation timestamp.
                          Extracted from: org.opencontainers.image.created
                        type: string
                      description:
                        description: |-
                          Description is a brief description.
                          Extracted from: org.opencontainers.image.description
                        type: string
                      documentation:
                        description: |-
                          Documentation is the URL to documentation.
                          Extracted from: org.opencontainers.image.documentation
                        type: string
                      licenses:
                        description: |-
                          Licenses is the SPDX license identifier(s).
                          Extracted from: org.opencontainers.image.licenses
                        type: string
                      revision:
                        description: |-
                          Revision is the source control revision.
                          Extracted from: org.opencontainers.image.revision
                        type: string
                      source:
                        description: |-
                          Source is the URL to the source code repository.
                          Extracted from: org.opencontainers.image.source
                        type: string
                      title:
                        description: |-
                          Title is the human-readable title.
                          Extracted from: org.opencontainers.image.title
                        type: string
                      vendor:
                        description: |-
                          Vendor is the organization that produced the image.
                          Extracted from: org.opencontainers.image.vendor
                        type: string
                      version:
                        description: |-
                          Version is the image version.
                          Extracted from: org.opencontainers.image.version
                        type: string
                    type: object
                  originalLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      OriginalLabels contains the raw OCI image labels as a JSON object.
                      This preserves all labels from the image, including those not mapped to structured fields.
                    type: object
                type: object
              imagePullSecrets:
                description: |-
                  ImagePullSecrets lists secrets containing credentials for pulling the model container image.
                  These secrets are used for:
                  - OCI registry metadata extraction during discovery
                  - Pulling the image for inference services
                  The secrets are merged with any runtime config defaults.
                  For namespace-scoped models, secrets must exist in the same namespace.
                  For cluster-scoped models, secrets must exist in the operator namespace.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              modelSources:
                description: |-
                  ModelSources specifies the model sources to use for this model.
                  When specified, these sources are used instead of auto-discovery from the container image.
                  This enables pre-creating custom models with explicit model sources.
                  The size field is optional - if not specified, it will be discovered by the download job.
                  AIM runtime currently supports only one model source.
                items:
                  description: |-
                    AIMModelSource describes a model artifact that must be downloaded for inference.
                    Discovery extracts these from the container's configuration to enable caching and validation.
                  properties:
                    env:
                      description: |-
                        Env specifies per-source credential overrides.
                        These variables are used for authentication when downloading this specific source.
                        Takes precedence over base-level env for the same variable name.
                      items:
                        description: EnvVar represents an environment variable present
                          in a Container.
                        properties:
                          name:
                            description: |-
                              Name of the environment variable.
                              May consist of any printable ASCII characters except '='.
                            type: string
                          value:
                            description: |-
                              Variable references $(VAR_NAME) are expanded
                              using the previously defined environment variables in the container and
                              any service environment variables. If a variable cannot be resolved,
                              the reference in the input string will be unchanged. Double $$ are reduced
                              to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                              "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                              Escaped references will never be expanded, regardless of whether the variable
                              exists or not.
                              Defaults to "".
                            type: string
                          valueFrom:
                            description: Source for the environment variable's value.
                              Cannot be used if value is not empty.
                            properties:
                              configMapKeyRef:
                                description: Selects a key of a ConfigMap.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the ConfigMap or
                                      its key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              fieldRef:
                                description: |-
                                  Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                  spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                properties:
                                  apiVersion:
                                    description: Version of the schema the FieldPath
                                      is written in terms of, defaults to "v1".
                                    type: string
                                  fieldPath:
                                    description: Path of the field to select in the
                                      specified API version.
                                    type: string
                                required:
                                - fieldPath
                                type: object
                                x-kubernetes-map-type: atomic
                              fileKeyRef:
                                description: |-
                                  FileKeyRef selects a key of the env file.
                                  Requires the EnvFiles feature gate to be enabled.
                                properties:
                                  key:
                                    description: |-
                                      The key within the env file. An invalid key will prevent the pod from starting.
                                      The keys defined within a source may consist of any printable ASCII characters except '='.
                                      During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                    type: string
                                  optional:
                                    default: false
                                    description: |-
                                      Specify whether the file or its key must be defined. If the file or key
                                      does not exist, then the env var is not published.
                                      If optional is set to true and the specified key does not exist,
                                      the environment variable will not be set in the Pod's containers.

                                      If optional is set to false and the specified key does not exist,
                                      an error will be returned during Pod creation.
                                    type: boolean
                                  path:
                                    description: |-
                                      The path within the volume from which to select the file.
                                      Must be relative and may not contain the '..' path or start with '..'.
                                    type: string
                                  volumeName:
                                    description: The name of the volume mount containing
                                      the env file.
                                    type: string
                                required:
                                - key
                                - path
                                - volumeName
                                type: object
                                x-kubernetes-map-type: atomic
                              resourceFieldRef:
                                description: |-
                                  Selects a resource of the container: only resources limits and requests
                                  (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                properties:
                                  containerName:
                                    description: 'Container name: required for volumes,
                                      optional for env vars'
                                    type: string
                                  divisor:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: Specifies the output format of the
                                      exposed resources, defaults to "1"
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  resource:
                                    description: 'Required: resource to select'
                                    type: string
                                required:
                                - resource
                                type: object
                                x-kubernetes-map-type: atomic
                              secretKeyRef:
                                description: Selects a key of a secret in the pod's
                                  namespace
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                        required:
                        - name
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - name
                      x-kubernetes-list-type: map
                    modelId:
                      description: |-
                        ModelID is the canonical identifier in {org}/{name} format.
                        Determines the cache mount path: /workspace/cache/{modelId}
                        For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                        For S3 sources, users define their own organizational structure.
                      pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                      type: string
                    precision:
                      allOf:
                      - enum:
                        - auto
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                      - enum:
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                      description: |-
                        Precision describes the runtime precision this source is compatible with.
                        Used to match model sources to profiles during custom weight onboarding.
                      type: string
                    size:
                      anyOf:
                      - type: integer
                      - type: string
                      description: |-
                        Size is the expected storage space required for this model artifact.
                        Used for PVC sizing and capacity planning during cache creation.
                        Optional - if not specified, the download job will discover the size automatically.
                        Can be set explicitly to pre-allocate storage or override auto-discovery.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    sourceUri:
                      description: |-
                        SourceURI is the location from which the model should be downloaded.
                        Supported schemes:
                        - hf://org/model - Hugging Face Hub model
                        - s3://bucket/key - S3-compatible storage
                      pattern: ^(hf|s3)://[^ \t\r\n]+$
                      type: string
                  required:
                  - modelId
                  - sourceUri
                  type: object
                maxItems: 1
                type: array
              profileCopy:
                description: |-
                  ProfileCopy reuses the AIMProfileSet derivation shape so an AIMModel can
                  publish derivative AIMProfiles directly. The controller may synthesize a
                  child AIMProfileSet and fill SourceRef when image-backed discovery is
                  involved. Mutually exclusive with all deprecated v1alpha1 fields.

                  DEPRECATED on v1alpha2: use spec.profiles. v1alpha1 still accepts
                  ProfileCopy. v1alpha2 CRD CEL forbids ProfileCopy.
                properties:
                  image:
                    description: Image overrides the runtime image used by the derived
                      profiles.
                    type: string
                  imagePullSecrets:
                    description: ImagePullSecrets lists secrets used for inspecting
                      and pulling container images.
                    items:
                      description: |-
                        LocalObjectReference contains enough information to let you locate the
                        referenced object inside the same namespace.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    type: array
                  overrides:
                    description: Overrides mutates the copied profile spec after selection
                      and version filtering.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount replaces the copied profile's
                          acceleratorCount.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel replaces the copied profile's
                          acceleratorModel.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode replaces the copied profile's
                          acceleratorPartitioningMode. Complete replacement, not a merge; an empty
                          override string leaves the source profile's mode untouched. Same reserved
                          values as AIMProfileSpecCommon.AcceleratorPartitioningMode. Whenever this
                          override is set (to any value, including "unpartitioned"), the CEL rule on the
                          enclosing spec also requires AcceleratorCount to be set: partition mode
                          changes the per-unit interpretation of acceleratorCount, and CEL cannot
                          read the base profile to tell whether the meaning actually changed, so it
                          conservatively requires the count be restated.
                        type: string
                      aimId:
                        description: |-
                          AimId stamps the derived profile's spec.aimId. When set, wins over
                          the source profile's aimId. REQUIRED when the enclosing selector has
                          role=base (the source base profile carries no aimId of its own).
                        type: string
                      containerEnv:
                        description: ContainerEnv merges by env var name, overriding
                          matching source entries.
                        items:
                          description: EnvVar represents an environment variable present
                            in a Container.
                          properties:
                            name:
                              description: |-
                                Name of the environment variable.
                                May consist of any printable ASCII characters except '='.
                              type: string
                            value:
                              description: |-
                                Variable references $(VAR_NAME) are expanded
                                using the previously defined environment variables in the container and
                                any service environment variables. If a variable cannot be resolved,
                                the reference in the input string will be unchanged. Double $$ are reduced
                                to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                Escaped references will never be expanded, regardless of whether the variable
                                exists or not.
                                Defaults to "".
                              type: string
                            valueFrom:
                              description: Source for the environment variable's value.
                                Cannot be used if value is not empty.
                              properties:
                                configMapKeyRef:
                                  description: Selects a key of a ConfigMap.
                                  properties:
                                    key:
                                      description: The key to select.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the ConfigMap or
                                        its key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fieldRef:
                                  description: |-
                                    Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                    spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                  properties:
                                    apiVersion:
                                      description: Version of the schema the FieldPath
                                        is written in terms of, defaults to "v1".
                                      type: string
                                    fieldPath:
                                      description: Path of the field to select in
                                        the specified API version.
                                      type: string
                                  required:
                                  - fieldPath
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fileKeyRef:
                                  description: |-
                                    FileKeyRef selects a key of the env file.
                                    Requires the EnvFiles feature gate to be enabled.
                                  properties:
                                    key:
                                      description: |-
                                        The key within the env file. An invalid key will prevent the pod from starting.
                                        The keys defined within a source may consist of any printable ASCII characters except '='.
                                        During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                      type: string
                                    optional:
                                      default: false
                                      description: |-
                                        Specify whether the file or its key must be defined. If the file or key
                                        does not exist, then the env var is not published.
                                        If optional is set to true and the specified key does not exist,
                                        the environment variable will not be set in the Pod's containers.

                                        If optional is set to false and the specified key does not exist,
                                        an error will be returned during Pod creation.
                                      type: boolean
                                    path:
                                      description: |-
                                        The path within the volume from which to select the file.
                                        Must be relative and may not contain the '..' path or start with '..'.
                                      type: string
                                    volumeName:
                                      description: The name of the volume mount containing
                                        the env file.
                                      type: string
                                  required:
                                  - key
                                  - path
                                  - volumeName
                                  type: object
                                  x-kubernetes-map-type: atomic
                                resourceFieldRef:
                                  description: |-
                                    Selects a resource of the container: only resources limits and requests
                                    (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                  properties:
                                    containerName:
                                      description: 'Container name: required for volumes,
                                        optional for env vars'
                                      type: string
                                    divisor:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      description: Specifies the output format of
                                        the exposed resources, defaults to "1"
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    resource:
                                      description: 'Required: resource to select'
                                      type: string
                                  required:
                                  - resource
                                  type: object
                                  x-kubernetes-map-type: atomic
                                secretKeyRef:
                                  description: Selects a key of a secret in the pod's
                                    namespace
                                  properties:
                                    key:
                                      description: The key of the secret to select
                                        from.  Must be a valid secret key.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the Secret or its
                                        key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      engineArgs:
                        description: EngineArgs shallow-merges on top of the source
                          engineArgs, overriding matching keys.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      engineEnv:
                        additionalProperties:
                          type: string
                        description: EngineEnv merges by key, overriding matching
                          source entries.
                        type: object
                      image:
                        description: |-
                          Image overrides the runtime container image used by the derived
                          profiles. When empty the deployment image is rebased onto the source
                          profile's base-image (status.baseImage) so private mirrors stay
                          self-contained.
                        type: string
                      modelId:
                        description: |-
                          ModelId stamps the derived profile's spec.modelId. When set, wins
                          over both the source profile's modelId AND the auto-derivation from
                          modelSources[0].modelId. REQUIRED when the enclosing selector has
                          role=base.
                        type: string
                      modelSources:
                        description: |-
                          ModelSources replaces the copied profile's modelSources. When
                          modelSources[0].modelId is set and overrides.modelId is unset, the
                          derived profile's modelId is auto-derived from modelSources[0]; an
                          explicit overrides.modelId always wins.
                        items:
                          description: |-
                            AIMModelSource describes a model artifact that must be downloaded for inference.
                            Discovery extracts these from the container's configuration to enable caching and validation.
                          properties:
                            env:
                              description: |-
                                Env specifies per-source credential overrides.
                                These variables are used for authentication when downloading this specific source.
                                Takes precedence over base-level env for the same variable name.
                              items:
                                description: EnvVar represents an environment variable
                                  present in a Container.
                                properties:
                                  name:
                                    description: |-
                                      Name of the environment variable.
                                      May consist of any printable ASCII characters except '='.
                                    type: string
                                  value:
                                    description: |-
                                      Variable references $(VAR_NAME) are expanded
                                      using the previously defined environment variables in the container and
                                      any service environment variables. If a variable cannot be resolved,
                                      the reference in the input string will be unchanged. Double $$ are reduced
                                      to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                      "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                      Escaped references will never be expanded, regardless of whether the variable
                                      exists or not.
                                      Defaults to "".
                                    type: string
                                  valueFrom:
                                    description: Source for the environment variable's
                                      value. Cannot be used if value is not empty.
                                    properties:
                                      configMapKeyRef:
                                        description: Selects a key of a ConfigMap.
                                        properties:
                                          key:
                                            description: The key to select.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the ConfigMap
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fieldRef:
                                        description: |-
                                          Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                          spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                        properties:
                                          apiVersion:
                                            description: Version of the schema the
                                              FieldPath is written in terms of, defaults
                                              to "v1".
                                            type: string
                                          fieldPath:
                                            description: Path of the field to select
                                              in the specified API version.
                                            type: string
                                        required:
                                        - fieldPath
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fileKeyRef:
                                        description: |-
                                          FileKeyRef selects a key of the env file.
                                          Requires the EnvFiles feature gate to be enabled.
                                        properties:
                                          key:
                                            description: |-
                                              The key within the env file. An invalid key will prevent the pod from starting.
                                              The keys defined within a source may consist of any printable ASCII characters except '='.
                                              During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                            type: string
                                          optional:
                                            default: false
                                            description: |-
                                              Specify whether the file or its key must be defined. If the file or key
                                              does not exist, then the env var is not published.
                                              If optional is set to true and the specified key does not exist,
                                              the environment variable will not be set in the Pod's containers.

                                              If optional is set to false and the specified key does not exist,
                                              an error will be returned during Pod creation.
                                            type: boolean
                                          path:
                                            description: |-
                                              The path within the volume from which to select the file.
                                              Must be relative and may not contain the '..' path or start with '..'.
                                            type: string
                                          volumeName:
                                            description: The name of the volume mount
                                              containing the env file.
                                            type: string
                                        required:
                                        - key
                                        - path
                                        - volumeName
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      resourceFieldRef:
                                        description: |-
                                          Selects a resource of the container: only resources limits and requests
                                          (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                        properties:
                                          containerName:
                                            description: 'Container name: required
                                              for volumes, optional for env vars'
                                            type: string
                                          divisor:
                                            anyOf:
                                            - type: integer
                                            - type: string
                                            description: Specifies the output format
                                              of the exposed resources, defaults to
                                              "1"
                                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                            x-kubernetes-int-or-string: true
                                          resource:
                                            description: 'Required: resource to select'
                                            type: string
                                        required:
                                        - resource
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      secretKeyRef:
                                        description: Selects a key of a secret in
                                          the pod's namespace
                                        properties:
                                          key:
                                            description: The key of the secret to
                                              select from.  Must be a valid secret
                                              key.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the Secret
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                    type: object
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            modelId:
                              description: |-
                                ModelID is the canonical identifier in {org}/{name} format.
                                Determines the cache mount path: /workspace/cache/{modelId}
                                For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                                For S3 sources, users define their own organizational structure.
                              pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                              type: string
                            precision:
                              allOf:
                              - enum:
                                - auto
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              - enum:
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              description: |-
                                Precision describes the runtime precision this source is compatible with.
                                Used to match model sources to profiles during custom weight onboarding.
                              type: string
                            size:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                Size is the expected storage space required for this model artifact.
                                Used for PVC sizing and capacity planning during cache creation.
                                Optional - if not specified, the download job will discover the size automatically.
                                Can be set explicitly to pre-allocate storage or override auto-discovery.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            sourceUri:
                              description: |-
                                SourceURI is the location from which the model should be downloaded.
                                Supported schemes:
                                - hf://org/model - Hugging Face Hub model
                                - s3://bucket/key - S3-compatible storage
                              pattern: ^(hf|s3)://[^ \t\r\n]+$
                              type: string
                          required:
                          - modelId
                          - sourceUri
                          type: object
                        type: array
                      profileId:
                        description: |-
                          ProfileId stamps the derived profile's spec.profileId. Optional —
                          most callers leave this empty and let the source profile's profileId
                          carry through (or the reconciler synthesise one).
                        type: string
                    type: object
                  selector:
                    description: Selector chooses which source profiles to derive
                      from.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount filters by accelerator unit
                          count.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel filters by accelerator identifier.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode filters candidates by their declared
                          partitioning mode. Partial-order match (NOT strict equality):
                            ""              - no filter on this field.
                            "unpartitioned" - matches profiles with mode "" or "unpartitioned".
                            "partitioned"   - matches profiles whose mode is non-trivial (anything
                                              other than "" / "unpartitioned").
                            "<C>"           - selector-only convenience: matches profiles with mode
                                              "<C>-*" (prefix on the scheme). Not a valid profile-spec
                                              value (e.g. selector "CPX" matches "CPX-NPS1", "CPX-NPS4").
                            "<C>-<M>"       - exact-string match on the scheme.
                        type: string
                      acceleratorType:
                        description: AcceleratorType filters by accelerator resource
                          type.
                        enum:
                        - gpu
                        - cpu
                        type: string
                      aimId:
                        description: AimId filters by model architecture identifier
                          (e.g., "qwen/qwen3-32b").
                        type: string
                      engine:
                        description: Engine filters by inference engine.
                        type: string
                      engineArgs:
                        description: |-
                          EngineArgs partially matches source engineArgs: every provided top-level key must
                          exist in the source object with an equal value.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      metric:
                        description: Metric filters by optimization target.
                        enum:
                        - latency
                        - throughput
                        type: string
                      minimumType:
                        description: |-
                          MinimumType filters by a minimum optimization level: candidates whose
                          type is this tier OR BETTER are accepted (hierarchy: optimized > general
                          > preview > unoptimized). This is the floor counterpart to the exact-match
                          Type field; the two AND together when both are set.

                          The sentinel "any" disables the floor (accept every tier). When this
                          field is empty the AIMService resolver applies a default floor of
                          "optimized" so auto-selection prefers production-grade profiles and never
                          silently picks an unoptimized one; to opt a service into lower tiers
                          (e.g. CPU/EPYC profiles published as unoptimized) set minimumType
                          explicitly to "unoptimized" or "any". Derivation selectors
                          (AIMProfileSet / AIMModel.profiles) treat empty as "any" so copying is
                          never tier-restricted by default.
                        enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                        - any
                        type: string
                      modelId:
                        description: ModelId filters by the source profile's specific
                          model identifier.
                        type: string
                      modelRef:
                        description: |-
                          ModelRef narrows candidates to those produced by a specific
                          AIM(Cluster)Model, matched via the `aim.eai.amd.com/source-model[-scope]`
                          labels stamped by the AIMModel reconcilers. Iteration 1 (v1alpha2 only).
                        properties:
                          name:
                            description: Name is the owning AIM(Cluster)Model name.
                              Required.
                            minLength: 1
                            type: string
                          scope:
                            default: Auto
                            description: Scope controls how Name is resolved against
                              AIMModel vs AIMClusterModel.
                            enum:
                            - Auto
                            - Namespace
                            - Cluster
                            type: string
                        required:
                        - name
                        type: object
                      origin:
                        description: |-
                          Origin filters by the `aim.eai.amd.com/profile-origin` label. When unset
                          (empty) the selector does not filter by origin. Iteration 1 (v1alpha2
                          only).
                        enum:
                        - discovered
                        - derived
                        - user-authored
                        type: string
                      precision:
                        description: Precision filters by numeric precision.
                        enum:
                        - auto
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                        type: string
                      profileId:
                        description: ProfileId filters by the source profile's profile
                          identifier.
                        type: string
                      role:
                        default: deployable
                        description: |-
                          Role filters by the `aim.eai.amd.com/profile-role` label. Defaults to
                          `deployable`. `base` filters to base profiles emitted by base-image
                          discovery (custom-model derivation source material).
                        enum:
                        - base
                        - deployable
                        type: string
                      type:
                        description: Type filters by optimization level (exact match).
                        enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                        type: string
                    type: object
                  serviceAccountName:
                    description: ServiceAccountName is propagated to managed profiles
                      for downstream workloads.
                    type: string
                  sourceRef:
                    description: |-
                      SourceRef points to an alternate discovery cache source.
                      When omitted, derivation uses visible AIMProfile and AIMClusterProfile objects.
                      For AIMProfileSet, the referenced ConfigMap is read from the same namespace.
                      For AIMClusterProfileSet, it is read from the operator namespace.
                    properties:
                      name:
                        description: Name is the discovery cache ConfigMap name.
                        type: string
                    required:
                    - name
                    type: object
                  version:
                    description: Version pins matching to a specific source profile
                      version when VersionPolicy is pinned.
                    type: string
                  versionPolicy:
                    default: pinned
                    description: VersionPolicy controls how matching profiles are
                      filtered by version.
                    enum:
                    - pinned
                    - latest
                    - all
                    type: string
                type: object
                x-kubernetes-validations:
                - message: selector must set at least one matching field, or set sourceRef
                  rule: has(self.sourceRef) || has(self.selector.aimId) || has(self.selector.modelId)
                    || has(self.selector.profileId) || has(self.selector.engine) ||
                    has(self.selector.metric) || has(self.selector.precision) || has(self.selector.type)
                    || has(self.selector.acceleratorModel) || has(self.selector.acceleratorType)
                    || has(self.selector.acceleratorCount) || has(self.selector.engineArgs)
                    || has(self.selector.modelRef) || has(self.selector.origin)
                - message: version is required when versionPolicy is pinned
                  rule: has(self.version) || (has(self.versionPolicy) && self.versionPolicy
                    != 'pinned')
                - message: latest/all versionPolicy must not set version
                  rule: '!has(self.versionPolicy) || (self.versionPolicy != ''latest''
                    && self.versionPolicy != ''all'') || !has(self.version)'
                - message: selector.aimId/modelId/profileId are not allowed when selector.role=base;
                    set overrides.aimId/modelId/profileId instead (base profiles have
                    no source identity to filter on)
                  rule: '!(has(self.selector.role) && self.selector.role == ''base'')
                    || (!has(self.selector.aimId) && !has(self.selector.modelId) &&
                    !has(self.selector.profileId))'
                - message: selector.role=base requires overrides.aimId and overrides.modelId
                    (base profiles carry no identity of their own; overrides supply
                    it)
                  rule: '!(has(self.selector.role) && self.selector.role == ''base'')
                    || (has(self.overrides) && has(self.overrides.aimId) && has(self.overrides.modelId))'
                - message: overrides.acceleratorCount must be specified together with
                    any overrides.acceleratorPartitioningMode; partition mode changes
                    the per-unit interpretation of acceleratorCount
                  rule: '!has(self.overrides) || !has(self.overrides.acceleratorPartitioningMode)
                    || size(self.overrides.acceleratorPartitioningMode) == 0 || has(self.overrides.acceleratorCount)'
              profiles:
                description: |-
                  Profiles is the v1alpha2 fine-tune / custom-model onboarding surface.
                  It groups the source descriptor (`profiles.derivedFrom.selector` /
                  `profiles.derivedFrom.sourceRef`), the version filter
                  (`profiles.versionPolicy` / `profiles.version`), and the modifications
                  applied to copies (`profiles.overrides`). When set, the AIMModel
                  reconciler synthesises a child AIMProfileSet from this block.

                  Mutually exclusive with spec.image (exactly one of the two is required
                  for v1alpha2 AIMModel). v1alpha1 rejects spec.profiles via per-version
                  CEL.
                properties:
                  derivedFrom:
                    description: DerivedFrom identifies the source profiles to copy
                      from.
                    properties:
                      selector:
                        description: |-
                          Selector chooses which source profiles to derive from. Discovery of a
                          deployable AIM image stamps role=deployable; selector.role=base targets
                          the base profiles emitted by base-image discovery (custom-model
                          derivation source material).
                        properties:
                          acceleratorCount:
                            description: AcceleratorCount filters by accelerator unit
                              count.
                            format: int32
                            type: integer
                          acceleratorModel:
                            description: AcceleratorModel filters by accelerator identifier.
                            type: string
                          acceleratorPartitioningMode:
                            description: |-
                              AcceleratorPartitioningMode filters candidates by their declared
                              partitioning mode. Partial-order match (NOT strict equality):
                                ""              - no filter on this field.
                                "unpartitioned" - matches profiles with mode "" or "unpartitioned".
                                "partitioned"   - matches profiles whose mode is non-trivial (anything
                                                  other than "" / "unpartitioned").
                                "<C>"           - selector-only convenience: matches profiles with mode
                                                  "<C>-*" (prefix on the scheme). Not a valid profile-spec
                                                  value (e.g. selector "CPX" matches "CPX-NPS1", "CPX-NPS4").
                                "<C>-<M>"       - exact-string match on the scheme.
                            type: string
                          acceleratorType:
                            description: AcceleratorType filters by accelerator resource
                              type.
                            enum:
                            - gpu
                            - cpu
                            type: string
                          aimId:
                            description: AimId filters by model architecture identifier
                              (e.g., "qwen/qwen3-32b").
                            type: string
                          engine:
                            description: Engine filters by inference engine.
                            type: string
                          engineArgs:
                            description: |-
                              EngineArgs partially matches source engineArgs: every provided top-level key must
                              exist in the source object with an equal value.
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          metric:
                            description: Metric filters by optimization target.
                            enum:
                            - latency
                            - throughput
                            type: string
                          minimumType:
                            description: |-
                              MinimumType filters by a minimum optimization level: candidates whose
                              type is this tier OR BETTER are accepted (hierarchy: optimized > general
                              > preview > unoptimized). This is the floor counterpart to the exact-match
                              Type field; the two AND together when both are set.

                              The sentinel "any" disables the floor (accept every tier). When this
                              field is empty the AIMService resolver applies a default floor of
                              "optimized" so auto-selection prefers production-grade profiles and never
                              silently picks an unoptimized one; to opt a service into lower tiers
                              (e.g. CPU/EPYC profiles published as unoptimized) set minimumType
                              explicitly to "unoptimized" or "any". Derivation selectors
                              (AIMProfileSet / AIMModel.profiles) treat empty as "any" so copying is
                              never tier-restricted by default.
                            enum:
                            - optimized
                            - general
                            - preview
                            - unoptimized
                            - any
                            type: string
                          modelId:
                            description: ModelId filters by the source profile's specific
                              model identifier.
                            type: string
                          modelRef:
                            description: |-
                              ModelRef narrows candidates to those produced by a specific
                              AIM(Cluster)Model, matched via the `aim.eai.amd.com/source-model[-scope]`
                              labels stamped by the AIMModel reconcilers. Iteration 1 (v1alpha2 only).
                            properties:
                              name:
                                description: Name is the owning AIM(Cluster)Model
                                  name. Required.
                                minLength: 1
                                type: string
                              scope:
                                default: Auto
                                description: Scope controls how Name is resolved against
                                  AIMModel vs AIMClusterModel.
                                enum:
                                - Auto
                                - Namespace
                                - Cluster
                                type: string
                            required:
                            - name
                            type: object
                          origin:
                            description: |-
                              Origin filters by the `aim.eai.amd.com/profile-origin` label. When unset
                              (empty) the selector does not filter by origin. Iteration 1 (v1alpha2
                              only).
                            enum:
                            - discovered
                            - derived
                            - user-authored
                            type: string
                          precision:
                            description: Precision filters by numeric precision.
                            enum:
                            - auto
                            - fp4
                            - fp8
                            - fp16
                            - fp32
                            - bf16
                            - int4
                            - int8
                            type: string
                          profileId:
                            description: ProfileId filters by the source profile's
                              profile identifier.
                            type: string
                          role:
                            default: deployable
                            description: |-
                              Role filters by the `aim.eai.amd.com/profile-role` label. Defaults to
                              `deployable`. `base` filters to base profiles emitted by base-image
                              discovery (custom-model derivation source material).
                            enum:
                            - base
                            - deployable
                            type: string
                          type:
                            description: Type filters by optimization level (exact
                              match).
                            enum:
                            - optimized
                            - general
                            - preview
                            - unoptimized
                            type: string
                        type: object
                      sourceRef:
                        description: |-
                          SourceRef points to an alternate discovery cache source (a
                          pre-populated ConfigMap of profile YAMLs) instead of using the
                          visible AIMProfile / AIMClusterProfile objects.
                        properties:
                          name:
                            description: Name is the discovery cache ConfigMap name.
                            type: string
                        required:
                        - name
                        type: object
                    type: object
                  overrides:
                    description: |-
                      Overrides mutates the copied profile spec after selection and version
                      filtering. Use overrides.image to override the deployment container
                      image used by the derived profiles.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount replaces the copied profile's
                          acceleratorCount.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel replaces the copied profile's
                          acceleratorModel.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode replaces the copied profile's
                          acceleratorPartitioningMode. Complete replacement, not a merge; an empty
                          override string leaves the source profile's mode untouched. Same reserved
                          values as AIMProfileSpecCommon.AcceleratorPartitioningMode. Whenever this
                          override is set (to any value, including "unpartitioned"), the CEL rule on the
                          enclosing spec also requires AcceleratorCount to be set: partition mode
                          changes the per-unit interpretation of acceleratorCount, and CEL cannot
                          read the base profile to tell whether the meaning actually changed, so it
                          conservatively requires the count be restated.
                        type: string
                      aimId:
                        description: |-
                          AimId stamps the derived profile's spec.aimId. When set, wins over
                          the source profile's aimId. REQUIRED when the enclosing selector has
                          role=base (the source base profile carries no aimId of its own).
                        type: string
                      containerEnv:
                        description: ContainerEnv merges by env var name, overriding
                          matching source entries.
                        items:
                          description: EnvVar represents an environment variable present
                            in a Container.
                          properties:
                            name:
                              description: |-
                                Name of the environment variable.
                                May consist of any printable ASCII characters except '='.
                              type: string
                            value:
                              description: |-
                                Variable references $(VAR_NAME) are expanded
                                using the previously defined environment variables in the container and
                                any service environment variables. If a variable cannot be resolved,
                                the reference in the input string will be unchanged. Double $$ are reduced
                                to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                Escaped references will never be expanded, regardless of whether the variable
                                exists or not.
                                Defaults to "".
                              type: string
                            valueFrom:
                              description: Source for the environment variable's value.
                                Cannot be used if value is not empty.
                              properties:
                                configMapKeyRef:
                                  description: Selects a key of a ConfigMap.
                                  properties:
                                    key:
                                      description: The key to select.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the ConfigMap or
                                        its key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fieldRef:
                                  description: |-
                                    Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                    spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                  properties:
                                    apiVersion:
                                      description: Version of the schema the FieldPath
                                        is written in terms of, defaults to "v1".
                                      type: string
                                    fieldPath:
                                      description: Path of the field to select in
                                        the specified API version.
                                      type: string
                                  required:
                                  - fieldPath
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fileKeyRef:
                                  description: |-
                                    FileKeyRef selects a key of the env file.
                                    Requires the EnvFiles feature gate to be enabled.
                                  properties:
                                    key:
                                      description: |-
                                        The key within the env file. An invalid key will prevent the pod from starting.
                                        The keys defined within a source may consist of any printable ASCII characters except '='.
                                        During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                      type: string
                                    optional:
                                      default: false
                                      description: |-
                                        Specify whether the file or its key must be defined. If the file or key
                                        does not exist, then the env var is not published.
                                        If optional is set to true and the specified key does not exist,
                                        the environment variable will not be set in the Pod's containers.

                                        If optional is set to false and the specified key does not exist,
                                        an error will be returned during Pod creation.
                                      type: boolean
                                    path:
                                      description: |-
                                        The path within the volume from which to select the file.
                                        Must be relative and may not contain the '..' path or start with '..'.
                                      type: string
                                    volumeName:
                                      description: The name of the volume mount containing
                                        the env file.
                                      type: string
                                  required:
                                  - key
                                  - path
                                  - volumeName
                                  type: object
                                  x-kubernetes-map-type: atomic
                                resourceFieldRef:
                                  description: |-
                                    Selects a resource of the container: only resources limits and requests
                                    (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                  properties:
                                    containerName:
                                      description: 'Container name: required for volumes,
                                        optional for env vars'
                                      type: string
                                    divisor:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      description: Specifies the output format of
                                        the exposed resources, defaults to "1"
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    resource:
                                      description: 'Required: resource to select'
                                      type: string
                                  required:
                                  - resource
                                  type: object
                                  x-kubernetes-map-type: atomic
                                secretKeyRef:
                                  description: Selects a key of a secret in the pod's
                                    namespace
                                  properties:
                                    key:
                                      description: The key of the secret to select
                                        from.  Must be a valid secret key.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the Secret or its
                                        key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      engineArgs:
                        description: EngineArgs shallow-merges on top of the source
                          engineArgs, overriding matching keys.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      engineEnv:
                        additionalProperties:
                          type: string
                        description: EngineEnv merges by key, overriding matching
                          source entries.
                        type: object
                      image:
                        description: |-
                          Image overrides the runtime container image used by the derived
                          profiles. When empty the deployment image is rebased onto the source
                          profile's base-image (status.baseImage) so private mirrors stay
                          self-contained.
                        type: string
                      modelId:
                        description: |-
                          ModelId stamps the derived profile's spec.modelId. When set, wins
                          over both the source profile's modelId AND the auto-derivation from
                          modelSources[0].modelId. REQUIRED when the enclosing selector has
                          role=base.
                        type: string
                      modelSources:
                        description: |-
                          ModelSources replaces the copied profile's modelSources. When
                          modelSources[0].modelId is set and overrides.modelId is unset, the
                          derived profile's modelId is auto-derived from modelSources[0]; an
                          explicit overrides.modelId always wins.
                        items:
                          description: |-
                            AIMModelSource describes a model artifact that must be downloaded for inference.
                            Discovery extracts these from the container's configuration to enable caching and validation.
                          properties:
                            env:
                              description: |-
                                Env specifies per-source credential overrides.
                                These variables are used for authentication when downloading this specific source.
                                Takes precedence over base-level env for the same variable name.
                              items:
                                description: EnvVar represents an environment variable
                                  present in a Container.
                                properties:
                                  name:
                                    description: |-
                                      Name of the environment variable.
                                      May consist of any printable ASCII characters except '='.
                                    type: string
                                  value:
                                    description: |-
                                      Variable references $(VAR_NAME) are expanded
                                      using the previously defined environment variables in the container and
                                      any service environment variables. If a variable cannot be resolved,
                                      the reference in the input string will be unchanged. Double $$ are reduced
                                      to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                      "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                      Escaped references will never be expanded, regardless of whether the variable
                                      exists or not.
                                      Defaults to "".
                                    type: string
                                  valueFrom:
                                    description: Source for the environment variable's
                                      value. Cannot be used if value is not empty.
                                    properties:
                                      configMapKeyRef:
                                        description: Selects a key of a ConfigMap.
                                        properties:
                                          key:
                                            description: The key to select.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the ConfigMap
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fieldRef:
                                        description: |-
                                          Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                          spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                        properties:
                                          apiVersion:
                                            description: Version of the schema the
                                              FieldPath is written in terms of, defaults
                                              to "v1".
                                            type: string
                                          fieldPath:
                                            description: Path of the field to select
                                              in the specified API version.
                                            type: string
                                        required:
                                        - fieldPath
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fileKeyRef:
                                        description: |-
                                          FileKeyRef selects a key of the env file.
                                          Requires the EnvFiles feature gate to be enabled.
                                        properties:
                                          key:
                                            description: |-
                                              The key within the env file. An invalid key will prevent the pod from starting.
                                              The keys defined within a source may consist of any printable ASCII characters except '='.
                                              During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                            type: string
                                          optional:
                                            default: false
                                            description: |-
                                              Specify whether the file or its key must be defined. If the file or key
                                              does not exist, then the env var is not published.
                                              If optional is set to true and the specified key does not exist,
                                              the environment variable will not be set in the Pod's containers.

                                              If optional is set to false and the specified key does not exist,
                                              an error will be returned during Pod creation.
                                            type: boolean
                                          path:
                                            description: |-
                                              The path within the volume from which to select the file.
                                              Must be relative and may not contain the '..' path or start with '..'.
                                            type: string
                                          volumeName:
                                            description: The name of the volume mount
                                              containing the env file.
                                            type: string
                                        required:
                                        - key
                                        - path
                                        - volumeName
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      resourceFieldRef:
                                        description: |-
                                          Selects a resource of the container: only resources limits and requests
                                          (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                        properties:
                                          containerName:
                                            description: 'Container name: required
                                              for volumes, optional for env vars'
                                            type: string
                                          divisor:
                                            anyOf:
                                            - type: integer
                                            - type: string
                                            description: Specifies the output format
                                              of the exposed resources, defaults to
                                              "1"
                                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                            x-kubernetes-int-or-string: true
                                          resource:
                                            description: 'Required: resource to select'
                                            type: string
                                        required:
                                        - resource
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      secretKeyRef:
                                        description: Selects a key of a secret in
                                          the pod's namespace
                                        properties:
                                          key:
                                            description: The key of the secret to
                                              select from.  Must be a valid secret
                                              key.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the Secret
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                    type: object
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            modelId:
                              description: |-
                                ModelID is the canonical identifier in {org}/{name} format.
                                Determines the cache mount path: /workspace/cache/{modelId}
                                For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                                For S3 sources, users define their own organizational structure.
                              pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                              type: string
                            precision:
                              allOf:
                              - enum:
                                - auto
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              - enum:
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              description: |-
                                Precision describes the runtime precision this source is compatible with.
                                Used to match model sources to profiles during custom weight onboarding.
                              type: string
                            size:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                Size is the expected storage space required for this model artifact.
                                Used for PVC sizing and capacity planning during cache creation.
                                Optional - if not specified, the download job will discover the size automatically.
                                Can be set explicitly to pre-allocate storage or override auto-discovery.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            sourceUri:
                              description: |-
                                SourceURI is the location from which the model should be downloaded.
                                Supported schemes:
                                - hf://org/model - Hugging Face Hub model
                                - s3://bucket/key - S3-compatible storage
                              pattern: ^(hf|s3)://[^ \t\r\n]+$
                              type: string
                          required:
                          - modelId
                          - sourceUri
                          type: object
                        type: array
                      profileId:
                        description: |-
                          ProfileId stamps the derived profile's spec.profileId. Optional —
                          most callers leave this empty and let the source profile's profileId
                          carry through (or the reconciler synthesise one).
                        type: string
                    type: object
                  version:
                    description: |-
                      Version pins matching to a specific source profile version when
                      VersionPolicy is `pinned`.
                    type: string
                  versionPolicy:
                    default: pinned
                    description: VersionPolicy controls how matching profiles are
                      filtered by version.
                    enum:
                    - pinned
                    - latest
                    - all
                    type: string
                type: object
                x-kubernetes-validations:
                - message: profiles.derivedFrom is required when spec.profiles is
                    set
                  rule: has(self.derivedFrom)
                - message: profiles.version is required when versionPolicy is pinned
                  rule: has(self.version) || (has(self.versionPolicy) && self.versionPolicy
                    != 'pinned')
                - message: latest/all versionPolicy must not set version
                  rule: '!has(self.versionPolicy) || (self.versionPolicy != ''latest''
                    && self.versionPolicy != ''all'') || !has(self.version)'
                - message: derivedFrom.selector.aimId/modelId/profileId are not allowed
                    when selector.role=base; set overrides.aimId/modelId/profileId
                    instead (base profiles have no source identity to filter on)
                  rule: '!(has(self.derivedFrom.selector.role) && self.derivedFrom.selector.role
                    == ''base'') || (!has(self.derivedFrom.selector.aimId) && !has(self.derivedFrom.selector.modelId)
                    && !has(self.derivedFrom.selector.profileId))'
                - message: derivedFrom.selector.role=base requires overrides.aimId
                    and overrides.modelId (base profiles carry no identity of their
                    own; overrides supply it)
                  rule: '!(has(self.derivedFrom.selector.role) && self.derivedFrom.selector.role
                    == ''base'') || (has(self.overrides) && has(self.overrides.aimId)
                    && has(self.overrides.modelId))'
              resources:
                description: |-
                  Resources defines the default resource requirements for services using this model.
                  Template- or service-level values override these defaults.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              runtimeConfigName:
                description: |-
                  Name is the name of the runtime config to use for this resource. If a runtime config with this name exists both
                  as a namespace and a cluster runtime config, the values are merged together, the namespace config taking priority
                  over the cluster config when there are conflicts. If this field is empty or set to `default`, the namespace / cluster
                  runtime config with the name `default` is used, if it exists.
                type: string
              serviceAccountName:
                description: |-
                  ServiceAccountName specifies the Kubernetes service account to use for workloads related to this model.
                  This includes metadata extraction jobs and any other model-related operations.
                  If empty, the default service account for the namespace is used.
                type: string
            type: object
            x-kubernetes-validations:
            - message: when using modelSources without aimId, set custom.hardware
                or set hardware on each customTemplate
              rule: '!has(self.modelSources) || size(self.modelSources) == 0 || has(self.aimId)
                || (has(self.custom) && has(self.custom.hardware)) || !has(self.customTemplates)
                || size(self.customTemplates) == 0 || self.customTemplates.all(t,
                has(t.hardware) || (has(self.custom) && has(self.custom.hardware)))'
            - message: image is required unless aimId is set with versionPolicy latest
                or any, or profileCopy/derivedFrom/profiles is set
              rule: (has(self.image) && size(self.image) > 0) || has(self.profileCopy)
                || has(self.derivedFrom) || has(self.profiles) || (has(self.aimId)
                && has(self.custom) && has(self.custom.versionPolicy) && self.custom.versionPolicy
                != 'pinned')
            - message: profileCopy cannot be combined with deprecated legacy AIMModel
                fields
              rule: '!has(self.profileCopy) || (!has(self.discovery) && !has(self.defaultServiceTemplate)
                && !has(self.custom) && (!has(self.customTemplates) || size(self.customTemplates)
                == 0) && (!has(self.modelSources) || size(self.modelSources) == 0)
                && (!has(self.runtimeConfigName) || size(self.runtimeConfigName) ==
                0) && (!has(self.env) || size(self.env) == 0) && !has(self.imageMetadata))'
          status:
            description: AIMModelStatus defines the observed state of AIMModel.
            properties:
              aimId:
                description: |-
                  AimId is the resolved model architecture identifier for this model.
                  Populated by the v1alpha2 controller from spec.aimId or discovered metadata.
                type: string
              baseImage:
                description: |-
                  BaseImage is the extracted AIM base image reference (AIM_BASE_IMAGE_REF) when known.
                  Used when resolving deployment images for fine-tuned models that have no spec.image.
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the model's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              discoveredProfiles:
                description: DiscoveredProfiles summarizes the profiles found during
                  image discovery.
                properties:
                  byHardware:
                    description: |-
                      ByHardware groups discovered profiles by their hardware footprint and
                      reports whether each group is currently supported by the cluster. The
                      groups are stable across reconciles (sorted by acceleratorType,
                      acceleratorModel, acceleratorCount) so kubectl/jq queries are cheap.
                      Useful when debugging "why doesn't the {metric, precision} profile I
                      expect appear in my cluster?" — the breakdown surfaces every shape the
                      image emits, not only the ones materialised as AIMProfile objects.
                    items:
                      description: |-
                        ProfileHardwareGroup is one accelerator footprint within a model's
                        discovery catalog, with the metric/precision combos shipped under it.
                      properties:
                        acceleratorCount:
                          description: |-
                            AcceleratorCount is the number of accelerator units the profile
                            requests. For gpu: device count (e.g., 1, 2, 4, 8 for tensor-parallel
                            sizes). For cpu: number of CPU cores (e.g., 128 for EPYC_ZEN5,
                            192 for EPYC_9965).
                          format: int32
                          type: integer
                        acceleratorModel:
                          description: |-
                            AcceleratorModel is the accelerator identifier (e.g., "MI300X",
                            "EPYC_ZEN5"). Empty for profiles with no accelerator requirement.
                          type: string
                        acceleratorType:
                          description: AcceleratorType is the resource family (gpu,
                            cpu).
                          enum:
                          - gpu
                          - cpu
                          type: string
                        profiles:
                          description: |-
                            Profiles lists the {metric, precision} combinations discovered under
                            this hardware footprint. Reported even when the group is unsupported
                            so users can see what they're missing.
                          items:
                            description: |-
                              ProfileHardwareGroupEntry identifies one profile within a hardware group
                              by its {metric, precision} pair. Sufficient for users to spot whether the
                              optimization variant they want is shipped at all.
                            properties:
                              metric:
                                description: Metric is the optimization target (latency,
                                  throughput).
                                enum:
                                - latency
                                - throughput
                                type: string
                              precision:
                                description: Precision is the numeric precision (fp4,
                                  fp8, bf16, …).
                                enum:
                                - auto
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                                type: string
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        supported:
                          description: |-
                            Supported reports whether this hardware footprint is currently
                            satisfied by at least one cluster node. When false, all profiles in
                            this group are skipped during materialisation.
                          type: boolean
                      required:
                      - supported
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  supported:
                    description: Supported is the number of discovered profiles currently
                      supported by the cluster.
                    format: int32
                    type: integer
                  total:
                    description: Total is the number of profiles discovered from the
                      image.
                    format: int32
                    type: integer
                  unsupported:
                    description: Unsupported is the number of discovered profiles
                      currently not supported by the cluster.
                    format: int32
                    type: integer
                type: object
              discovery:
                description: Discovery tracks the state of the image-discovery Job
                  used to populate the discovery cache.
                properties:
                  attempts:
                    description: |-
                      Attempts is the number of discovery job attempts that have been made.
                      Increments each time a new discovery job is created after a failure.
                    format: int32
                    type: integer
                  lastAttemptTime:
                    description: |-
                      LastAttemptTime is the timestamp of the most recent discovery job creation.
                      Used to calculate exponential backoff before the next retry.
                    format: date-time
                    type: string
                  lastFailureReason:
                    description: LastFailureReason captures the reason for the most
                      recent discovery failure.
                    type: string
                  specHash:
                    description: |-
                      SpecHash is a hash of the model spec fields that invalidate cached discovery
                      (image, imagePullSecrets, serviceAccountName, discoveryCommandVersion).
                      When it changes, the operator drops the cache and re-runs the discovery Job.
                    type: string
                type: object
              discoveryCacheRef:
                description: |-
                  DiscoveryCacheRef points at the normalized discovery cache ConfigMap for image-backed flows.
                  Populated by the v1alpha2 controller after image inspection succeeds.
                properties:
                  name:
                    description: Name is the ConfigMap name.
                    type: string
                  namespace:
                    description: Namespace is the ConfigMap namespace.
                    type: string
                type: object
              imageMetadata:
                description: ImageMetadata is the metadata extracted from an AIM image
                properties:
                  baseImageRef:
                    description: |-
                      BaseImageRef is the value of the AIM_BASE_IMAGE_REF environment variable
                      baked into the image's OCI config at build time. For AIM model images this
                      records the base image (e.g. "ghcr.io/silogen/aim-base:0.8.5") that the
                      model image was built from. Used by the AIMModel controller to resolve
                      the deployment image for fine-tuned models whose spec.image is omitted
                      (versionPolicy=latest or any).
                    type: string
                  model:
                    description: Model contains AMD Silogen model-specific metadata.
                    properties:
                      canonicalName:
                        description: |-
                          CanonicalName is the canonical model identifier (e.g., mistralai/Mixtral-8x22B-Instruct-v0.1).
                          Extracted from: org.amd.silogen.model.canonicalName
                        type: string
                      descriptionFull:
                        description: |-
                          DescriptionFull is the full description.
                          Extracted from: org.amd.silogen.description.full
                        type: string
                      hfTokenRequired:
                        description: |-
                          HFTokenRequired indicates if a HuggingFace token is required.
                          Extracted from: org.amd.silogen.hfToken.required
                        type: boolean
                      recommendedDeployments:
                        description: |-
                          RecommendedDeployments contains recommended deployment configurations.
                          Extracted from: org.amd.silogen.model.recommendedDeployments (parsed from JSON array)
                        items:
                          description: RecommendedDeployment describes a recommended
                            deployment configuration for a model.
                          properties:
                            description:
                              description: Description provides additional context
                                about this deployment configuration
                              type: string
                            gpuCount:
                              description: GPUCount is the number of GPUs required
                              format: int32
                              type: integer
                            gpuModel:
                              description: |-
                                GPUModel is the GPU model name (e.g., MI300X, MI325X).
                                The legacy v1alpha1 schema only models GPU accelerators; CPU profiles
                                emitted via OCI labels (gpuModel="CPU") are not first-class here and
                                are handled exclusively by the v1alpha2 native discovery pipeline.
                              type: string
                            metric:
                              description: Metric is the optimization target (e.g.,
                                latency, throughput)
                              type: string
                            precision:
                              description: Precision is the recommended precision
                                (e.g., fp8, fp16, bf16)
                              type: string
                            profileId:
                              description: |-
                                ProfileId is the unique identifier of the AIM profile for this deployment.
                                When set, templates created from this deployment will use this profile ID
                                to deterministically select the correct runtime profile in the AIM container.
                              type: string
                          type: object
                        type: array
                      releaseNotes:
                        description: |-
                          ReleaseNotes contains release notes for this version.
                          Extracted from: org.amd.silogen.release.notes
                        type: string
                      source:
                        description: |-
                          Source is the URL where the model can be found.
                          Extracted from: org.amd.silogen.model.source
                        type: string
                      tags:
                        description: |-
                          Tags are descriptive tags (e.g., ["text-generation", "chat", "instruction"]).
                          Extracted from: org.amd.silogen.model.tags (comma-separated)
                        items:
                          type: string
                        type: array
                      title:
                        description: |-
                          Title is the Silogen-specific title for the model.
                          Extracted from: org.amd.silogen.title
                        type: string
                      variants:
                        description: |-
                          Variants lists model variants.
                          Extracted from: org.amd.silogen.model.variants (comma-separated)
                        items:
                          type: string
                        type: array
                      versions:
                        description: |-
                          Versions lists available versions.
                          Extracted from: org.amd.silogen.model.versions (comma-separated)
                        items:
                          type: string
                        type: array
                    type: object
                  oci:
                    description: OCI contains standard OCI image metadata.
                    properties:
                      authors:
                        description: |-
                          Authors is contact details of the authors.
                          Extracted from: org.opencontainers.image.authors
                        type: string
                      created:
                        description: |-
                          Created is the creation timestamp.
                          Extracted from: org.opencontainers.image.created
                        type: string
                      description:
                        description: |-
                          Description is a brief description.
                          Extracted from: org.opencontainers.image.description
                        type: string
                      documentation:
                        description: |-
                          Documentation is the URL to documentation.
                          Extracted from: org.opencontainers.image.documentation
                        type: string
                      licenses:
                        description: |-
                          Licenses is the SPDX license identifier(s).
                          Extracted from: org.opencontainers.image.licenses
                        type: string
                      revision:
                        description: |-
                          Revision is the source control revision.
                          Extracted from: org.opencontainers.image.revision
                        type: string
                      source:
                        description: |-
                          Source is the URL to the source code repository.
                          Extracted from: org.opencontainers.image.source
                        type: string
                      title:
                        description: |-
                          Title is the human-readable title.
                          Extracted from: org.opencontainers.image.title
                        type: string
                      vendor:
                        description: |-
                          Vendor is the organization that produced the image.
                          Extracted from: org.opencontainers.image.vendor
                        type: string
                      version:
                        description: |-
                          Version is the image version.
                          Extracted from: org.opencontainers.image.version
                        type: string
                    type: object
                  originalLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      OriginalLabels contains the raw OCI image labels as a JSON object.
                      This preserves all labels from the image, including those not mapped to structured fields.
                    type: object
                type: object
              kind:
                description: |-
                  Kind classifies the v1alpha2 onboarding flow that produced this
                  model's profiles (Image / Derived / Custom). See AIMModelKind for
                  the per-value semantics. Populated by the v1alpha2 controller from
                  the spec shape; left empty by the v1alpha1 controller.
                enum:
                - Image
                - Derived
                - Custom
                type: string
              managedProfiles:
                description: ManagedProfiles summarizes the direct promoted or derived
                  profiles owned or managed by this model.
                properties:
                  base:
                    description: |-
                      Base is the number of managed profiles whose spec is structurally
                      incomplete (missing aimId or modelSources). Base profiles cannot back
                      an AIMService directly and exist purely as source material for
                      derivation via AIMModel.spec.profiles.derivedFrom with
                      selector.role=base. A non-zero value is the canonical operational
                      signal that this model is a base-image model (the source of
                      custom-model derivation).
                    format: int32
                    type: integer
                  deployable:
                    description: |-
                      Deployable is the number of managed profiles whose spec is materialised
                      enough to back an AIMService (carries aimId and modelSources). For a
                      normal officially-discovered AIMModel this equals Total. For a
                      base-image AIMModel used as a source for custom-model derivation
                      this is 0 — the model only emits base profiles that callers must
                      derive into deployable profiles.
                    format: int32
                    type: integer
                  notAvailable:
                    description: NotAvailable is the number of derivative profiles
                      whose own status is NotAvailable.
                    format: int32
                    type: integer
                  ready:
                    description: Ready is the number of derivative profiles whose
                      own status is Ready.
                    format: int32
                    type: integer
                  total:
                    description: Total is the number of derivative profiles the controller
                      currently manages or intends to manage.
                    format: int32
                    type: integer
                required:
                - base
                - deployable
                - notAvailable
                - ready
                - total
                type: object
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller
                format: int64
                type: integer
              profileSetRef:
                description: ProfileSetRef identifies the child profile set synthesized
                  for derivation flows.
                properties:
                  name:
                    description: Name is the profile set name.
                    type: string
                  namespace:
                    description: Namespace is the profile set namespace.
                    type: string
                type: object
              resolvedRuntimeConfig:
                description: ResolvedRuntimeConfig captures metadata about the runtime
                  config that was resolved.
                properties:
                  kind:
                    description: Kind is the fully-qualified kind of the resolved
                      reference, when known.
                    type: string
                  name:
                    description: Name is the resource name that satisfied the reference.
                    type: string
                  namespace:
                    description: |-
                      Namespace identifies where the resource was found when namespace-scoped.
                      Empty indicates a cluster-scoped resource.
                    type: string
                  scope:
                    description: Scope indicates whether the resolved resource was
                      namespace or cluster scoped.
                    enum:
                    - Namespace
                    - Cluster
                    - Merged
                    - Unknown
                    type: string
                  uid:
                    description: UID captures the unique identifier of the resolved
                      reference, when known.
                    type: string
                type: object
              sourceType:
                description: |-
                  SourceType indicates how this model's artifacts are sourced.
                  - "Image": Model discovered from container image labels
                  - "Custom": Model uses explicit spec.modelSources
                  Set by the controller based on whether spec.modelSources is populated.

                  Note: only populated by the v1alpha1 controller; v1alpha2 consumers
                  should read .status.kind instead, which distinguishes fine-tunes
                  (Derived) from BYO base-image overlays (Custom).
                enum:
                - Image
                - Custom
                type: string
              status:
                default: Pending
                description: Status represents the overall status of the image based
                  on its templates
                enum:
                - Pending
                - Progressing
                - Ready
                - Degraded
                - Failed
                - NotAvailable
                type: string
              version:
                description: |-
                  Version is the effective image version of the model. Populated from
                  the spec.image tag (`amdenterpriseai/aim-qwen-qwen3-32b:0.11.0` →
                  `0.11.0`) during reconciliation. Empty for models that have no
                  spec.image (e.g. fine-tuned models derived from a parent) or that
                  reference an image by digest.

                  This mirrors AIMProfileStatus.Version so the two surfaces stay in
                  lock-step: a single image-tag extraction rule governs what users
                  see in the kubectl printcolumn for both kinds.

                  The image-author-declared version (i.e. the
                  `org.opencontainers.image.version` OCI label) is preserved separately
                  under .status.imageMetadata.oci.version for users that want to inspect
                  what the image build pipeline stamped. The two values usually agree;
                  when the OCI label is missing or empty, this field still surfaces a
                  useful version from the tag itself.
                type: string
            type: object
        type: object
        x-kubernetes-validations:
        - message: exactly one of spec.image or spec.profiles must be set on v1alpha2
          optionalOldSelf: true
          rule: (has(self.spec.image) && size(self.spec.image) > 0) != (has(self.spec.profiles)
            || has(self.spec.derivedFrom)) || (oldSelf.hasValue() && !((has(oldSelf.value().spec.image)
            && size(oldSelf.value().spec.image) > 0) != (has(oldSelf.value().spec.profiles)
            || has(oldSelf.value().spec.derivedFrom))))
        - message: spec.profileCopy is forbidden on v1alpha2; use spec.profiles
          optionalOldSelf: true
          rule: '!has(self.spec.profileCopy) || (oldSelf.hasValue() && has(oldSelf.value().spec.profileCopy))'
        - message: spec.derivedFrom is deprecated on v1alpha2; use spec.profiles
          optionalOldSelf: true
          rule: '!has(self.spec.derivedFrom) || (oldSelf.hasValue() && has(oldSelf.value().spec.derivedFrom))'
        - message: spec.custom is forbidden on v1alpha2
          optionalOldSelf: true
          rule: '!has(self.spec.custom) || (oldSelf.hasValue() && has(oldSelf.value().spec.custom))'
        - message: spec.customTemplates is forbidden on v1alpha2
          optionalOldSelf: true
          rule: '!has(self.spec.customTemplates) || size(self.spec.customTemplates)
            == 0 || (oldSelf.hasValue() && has(oldSelf.value().spec.customTemplates)
            && size(oldSelf.value().spec.customTemplates) > 0)'
        - message: spec.modelSources is forbidden on v1alpha2; use spec.profiles.overrides.modelSources
          optionalOldSelf: true
          rule: '!has(self.spec.modelSources) || size(self.spec.modelSources) == 0
            || (oldSelf.hasValue() && has(oldSelf.value().spec.modelSources) && size(oldSelf.value().spec.modelSources)
            > 0)'
        - message: spec.profiles cannot be combined with spec.aimId, spec.discovery,
            spec.defaultServiceTemplate, spec.runtimeConfigName, spec.env, or spec.imageMetadata
          rule: (!has(self.spec.profiles) && !has(self.spec.derivedFrom)) || (!has(self.spec.aimId)
            && !has(self.spec.discovery) && !has(self.spec.defaultServiceTemplate)
            && !has(self.spec.runtimeConfigName) && !has(self.spec.env) && !has(self.spec.imageMetadata))
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: aimclustermodelsources.aim.eai.amd.com
spec:
  group: aim.eai.amd.com
  names:
    categories:
    - aim
    - all
    kind: AIMClusterModelSource
    listKind: AIMClusterModelSourceList
    plural: aimclustermodelsources
    shortNames:
    - aimclsrc
    singular: aimclustermodelsource
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.discoveredModels
      name: Models
      type: integer
    - jsonPath: .status.lastSyncTime
      name: LastSync
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AIMClusterModelSource automatically discovers and syncs AI model
          images from container registries.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AIMClusterModelSourceSpec defines the desired state of AIMClusterModelSource.
            properties:
              filters:
                description: |-
                  Filters define which images to discover and sync.
                  Each filter specifies an image selector with optional version constraints and exclusions.
                  Multiple filters are combined with OR logic (any match includes the image).
                  Use this field for advanced matching options. For simple explicit image lists, use Images instead.
                items:
                  description: |-
                    ModelSourceFilter defines an explicit image selector for discovery.
                    Supports multiple formats:
                    - Repository name: "org/repo" - exact repository match
                    - Repository with tag: "org/repo:1.0.0" - exact tag match
                    - Full URI: "ghcr.io/org/repo:1.0.0" - overrides registry and tag
                    - Full URI: "ghcr.io/org/repo" - exact repository on a specific registry
                  properties:
                    exclude:
                      description: |-
                        Exclude lists specific repository names to skip (exact match on repository name only, not registry).
                        Useful for excluding base images or experimental versions.

                        Examples:
                        - ["amdenterpriseai/aim-base", "amdenterpriseai/aim-experimental"]
                        - ["silogen/aim-base"] - works with "ghcr.io/silogen/aim-*" (registry is not checked in exclusion)

                        Note: Exclusions match against repository names (e.g., "silogen/aim-base"), not full URIs.
                      items:
                        type: string
                      type: array
                    image:
                      description: |-
                        Image with explicit repository matching and full URI support.

                        Supported formats:
                        - Repository name: "amdenterpriseai/aim-qwen-qwen3-32b"
                        - Repository with tag: "silogen/aim-llama:1.0.0" (overrides versions field)
                        - Full URI: "ghcr.io/silogen/aim-google-gemma-3-1b-it:0.8.1-rc1" (overrides spec.registry and versions)
                        - Full URI without tag: "ghcr.io/silogen/aim-google-gemma-3-1b-it" (overrides spec.registry)

                        When a full URI is specified (including registry like ghcr.io), only images from that
                        registry will match. When a tag is included, it takes precedence over the versions field.

                        Note: wildcard syntax (e.g., "*") is not supported; use explicit image list entries.
                      maxLength: 512
                      type: string
                    versions:
                      description: |-
                        Versions specifies semantic version constraints for this filter.
                        If specified, overrides the global Versions field.
                        Only tags that parse as valid semver are considered (including prereleases like 0.8.1-rc1).
                        Ignored if the Image field includes an explicit tag (e.g., "repo:1.0.0").

                        Examples: ">=1.0.0", "<2.0.0", "~1.2.0" (patch updates), "^1.0.0" (minor updates)

                        Prerelease versions (e.g., 0.8.1-rc1) are supported and follow semver rules:
                        - 0.8.1-rc1 matches ">=0.8.0" (prerelease is part of version 0.8.1)
                        - Use ">=0.8.1-rc1" to match only that prerelease or higher
                        - Leave empty to match all tags (including prereleases and non-semver tags)
                      items:
                        type: string
                      type: array
                  required:
                  - image
                  type: object
                maxItems: 100
                minItems: 1
                type: array
              imagePullSecrets:
                description: |-
                  ImagePullSecrets contains references to secrets for authenticating to private registries.
                  Secrets must exist in the operator namespace (typically aim-system).
                  Used for both registry catalog listing and image metadata extraction.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              images:
                description: |-
                  Images defines a simple explicit list of images to discover and sync.
                  Use this for straightforward static declarations without per-filter options.

                  Supported image formats:
                  - Repository with tag: "amdenterpriseai/aim-qwen-qwen3-32b:0.8.4"
                  - Repository without tag: "amdenterpriseai/aim-qwen-qwen3-32b" (uses Versions if set)
                  - Full URI with tag: "ghcr.io/silogen/aim-llama:1.0.0"

                  Must not be set together with Filters.
                items:
                  type: string
                maxItems: 100
                minItems: 1
                type: array
              maxModels:
                default: 100
                description: |-
                  MaxModels is the maximum number of AIMClusterModel resources to create from this source.
                  Once this limit is reached, no new models will be created, even if more matching images are discovered.
                  Existing models are never deleted.
                  This prevents runaway model creation from overly broad filters.
                maximum: 10000
                minimum: 1
                type: integer
              registry:
                default: docker.io
                description: |-
                  Registry to sync from (e.g., docker.io, ghcr.io, gcr.io).
                  Defaults to docker.io if not specified.
                type: string
              syncInterval:
                default: 1h
                description: |-
                  SyncInterval defines how often to sync with the registry.
                  Defaults to 1h. Minimum recommended interval is 15m to avoid rate limiting.
                  Format: duration string (e.g., "30m", "1h", "2h30m").
                type: string
              versions:
                description: |-
                  Versions specifies global semantic version constraints applied to all filters.
                  Individual filters can override this with their own version constraints.
                  Constraints use semver syntax: >=1.0.0, <2.0.0, ~1.2.0, ^1.0.0, etc.
                  Non-semver tags (e.g., "latest", "dev") are silently skipped.

                  Version ranges work on all registries (including ghcr.io, gcr.io) when combined with
                  exact repository names (no wildcards). The controller uses the Tags List API to fetch
                  all tags for the repository and filters them by the semver constraint.

                  Example: registry=ghcr.io, filters=[{image: "silogen/aim-llama"}], versions=[">=1.0.0"]
                  will fetch all tags from ghcr.io/silogen/aim-llama and include only those >=1.0.0.
                items:
                  type: string
                type: array
            type: object
            x-kubernetes-validations:
            - message: set exactly one of spec.filters or spec.images
              rule: (has(self.filters) && size(self.filters) > 0) != (has(self.images)
                && size(self.images) > 0)
          status:
            description: AIMClusterModelSourceStatus defines the observed state of
              AIMClusterModelSource.
            properties:
              availableModels:
                description: |-
                  AvailableModels is the total count of images discovered in the registry that match the filters.
                  This may be higher than DiscoveredModels if maxModels limit was reached.
                type: integer
              conditions:
                description: |-
                  Conditions represent the latest available observations of the source's state.
                  Standard conditions: Ready, Syncing, RegistryReachable.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              discoveredModels:
                description: |-
                  DiscoveredModels is the count of AIMClusterModel resources managed by this source.
                  Includes both existing and newly created models.
                type: integer
              lastSyncTime:
                description: |-
                  LastSyncTime is the timestamp of the last successful registry sync.
                  Updated after each successful sync operation.
                format: date-time
                type: string
              modelsLimitReached:
                description: |-
                  ModelsLimitReached indicates whether the maxModels limit has been reached.
                  When true, no new models will be created even if more matching images are discovered.
                type: boolean
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed spec.
                format: int64
                type: integer
              status:
                description: Status represents the overall state of the model source.
                enum:
                - Pending
                - Starting
                - Progressing
                - Ready
                - Running
                - Degraded
                - NotAvailable
                - Failed
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: aimclusterprofiles.aim.eai.amd.com
spec:
  group: aim.eai.amd.com
  names:
    categories:
    - aim
    - all
    kind: AIMClusterProfile
    listKind: AIMClusterProfileList
    plural: aimclusterprofiles
    shortNames:
    - aimclprf
    singular: aimclusterprofile
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.aimId
      name: AimId
      type: string
    - jsonPath: .spec.engine
      name: Engine
      type: string
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.origin
      name: Origin
      type: string
    - jsonPath: .status.hardwareSummary
      name: Hardware
      type: string
    - jsonPath: .spec.metric
      name: Metric
      type: string
    - jsonPath: .spec.precision
      name: Precision
      type: string
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .spec.acceleratorPartitioningMode
      name: Partitioning
      priority: 1
      type: string
    - jsonPath: .spec.primary
      name: Primary
      type: boolean
    - jsonPath: .status.version
      name: Version
      type: string
    - jsonPath: .spec.manualSelectionOnly
      name: Manual
      priority: 1
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: |-
          AIMClusterProfile is the Schema for cluster-scoped AIM profiles.
          Cluster profiles are visible across all namespaces. They can be created manually
          or, in the future, automatically during model discovery by a v1alpha2 model controller.
          Unlike namespace-scoped AIMProfiles, cluster profiles do not support caching
          configuration since caches are namespace-scoped.

          Deployable profiles have both aimId and modelSources populated; base
          profiles (custom-model derivation source material) have neither. Mixed
          (one of the two set) is rejected to keep status.deployable derivable from
          spec.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AIMClusterProfileSpec defines the desired state of a cluster-scoped
              AIMClusterProfile.
            properties:
              acceleratorCount:
                description: |-
                  AcceleratorCount is the number of accelerator units required.
                  For AcceleratorType=gpu, this is the device count (e.g., 1, 2, 4, 8
                  for tensor-parallel sizes). For AcceleratorType=cpu, this is the
                  number of CPU cores (e.g., 128 for EPYC_ZEN5, 192 for EPYC_9965).
                  Combined with cluster-level configuration to compute default
                  resource requests in status.resources.

                  For AcceleratorType=gpu, the per-unit interpretation depends on
                  AcceleratorPartitioningMode: under "unpartitioned" (default) one unit is
                  one whole GPU; under "partitioned" or a specific scheme one unit is one
                  partition slice (e.g. CPX-NPS4 = 1/8 of a GPU).
                format: int32
                minimum: 0
                type: integer
              acceleratorModel:
                description: |-
                  AcceleratorModel is the accelerator identifier for node selection.
                  Maps to a node label key using the Exists operator:
                    feature.node.kubernetes.io/aim-accelerator.{value}: Exists
                  Supports both specific models (e.g., "MI300X") and architecture-level
                  fallbacks (e.g., "EPYC_ZEN5") — the AcceleratorDetector labels nodes
                  with all applicable identifiers.
                maxLength: 63
                pattern: ^[A-Za-z0-9]([A-Za-z0-9._-]*[A-Za-z0-9])?$
                type: string
              acceleratorPartitioningMode:
                default: unpartitioned
                description: |-
                  AcceleratorPartitioningMode declares the GPU partition state the profile
                  requires. Free-form string with reserved values:

                    ""              - omitted; CRD-defaulted to "unpartitioned".
                    "unpartitioned" - hardware-default partition state. Matches unpartitioned
                                      MI300X (canonical SPX-NPS1) AND non-partitionable
                                      hardware (Radeon, etc.) — any node whose detector
                                      stamped aim-accelerator.partitioning-scheme.default.
                    "partitioned"   - any actively partitioned mode. Excludes both
                                      unpartitioned partitionable hardware and
                                      non-partitionable hardware.
                    "<C>-<M>"       - specific compute+memory scheme, e.g. "CPX-NPS4". Matches
                                      only nodes carrying that exact scheme label; does not
                                      match non-partitionable hardware.

                  Other values (e.g. "CPX" alone, or typos) are accepted but fail-safe to
                  zero matching nodes under this iteration's label schema — compute-only /
                  memory-only matching is not supported here. AIM Engine does NOT validate
                  against AMD's hardware compatibility matrix; invalid combinations simply
                  report MatchingNodes == 0.

                  Resolves to a single Exists or DoesNotExist node-affinity term on the
                  aim-accelerator.partitioning-scheme.* labels published by the
                  AcceleratorDetector, AND-ed with the acceleratorModel term.
                maxLength: 63
                pattern: ^[A-Za-z0-9]([A-Za-z0-9._-]*[A-Za-z0-9])?$
                type: string
              acceleratorType:
                allOf:
                - enum:
                  - gpu
                  - cpu
                - enum:
                  - gpu
                  - cpu
                description: |-
                  AcceleratorType determines the resource derivation strategy: gpu or cpu.
                  AIM Engine computes default resource requests from this field combined
                  with AcceleratorCount and cluster-level configuration.
                type: string
              aimId:
                description: |-
                  AimId is the model architecture identifier (e.g., "qwen/qwen3-32b").
                  Primary matching axis for profile selection and custom weight onboarding.

                  AimId is required for deployable profiles. Iteration 1 producers always
                  emit deployable profiles, so AimId is effectively required there. Empty
                  AimId is reserved for base profiles emitted by base-image discovery
                  (custom-model derivation source material), which are not deployable
                  until derived.

                  Once set, AimId is immutable.
                type: string
                x-kubernetes-validations:
                - message: aimId is immutable once set
                  rule: oldSelf == '' || self == oldSelf
              containerEnv:
                description: ContainerEnv specifies container-level env vars for the
                  AIM runtime process (K8s pod spec).
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: |-
                        Name of the environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        fileKeyRef:
                          description: |-
                            FileKeyRef selects a key of the env file.
                            Requires the EnvFiles feature gate to be enabled.
                          properties:
                            key:
                              description: |-
                                The key within the env file. An invalid key will prevent the pod from starting.
                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                              type: string
                            optional:
                              default: false
                              description: |-
                                Specify whether the file or its key must be defined. If the file or key
                                does not exist, then the env var is not published.
                                If optional is set to true and the specified key does not exist,
                                the environment variable will not be set in the Pod's containers.

                                If optional is set to false and the specified key does not exist,
                                an error will be returned during Pod creation.
                              type: boolean
                            path:
                              description: |-
                                The path within the volume from which to select the file.
                                Must be relative and may not contain the '..' path or start with '..'.
                              type: string
                            volumeName:
                              description: The name of the volume mount containing
                                the env file.
                              type: string
                          required:
                          - key
                          - path
                          - volumeName
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              engine:
                description: Engine identifies the inference engine (e.g., "vllm",
                  "tgi").
                type: string
              engineArgs:
                description: |-
                  EngineArgs contains inference engine CLI arguments as a free-form JSON object.
                  Passed to the inference engine (e.g., vLLM) at startup.
                x-kubernetes-preserve-unknown-fields: true
              engineEnv:
                additionalProperties:
                  type: string
                description: |-
                  EngineEnv contains environment variables for the inference engine subprocess.
                  Applied via os.execv, distinct from container-level ContainerEnv.
                type: object
              features:
                description: |-
                  Features lists optional capabilities the profile's image honours, e.g.
                  "adapters" for LoRA serving. A service declaring spec.adapters is rejected
                  (ConfigValid=False) unless its resolved profile lists "adapters" here.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              image:
                description: |-
                  Image is the deployment container image. Required.
                  For purpose-built profiles: the full AIM image.
                  For custom weight profiles: the base image (e.g., aim-base:0.8.5).
                minLength: 1
                type: string
              imagePullSecrets:
                description: ImagePullSecrets lists secrets for pulling container
                  images.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              manualSelectionOnly:
                default: false
                description: |-
                  ManualSelectionOnly is DEPRECATED and no longer honored by the resolver.
                  It was a binary gate excluding a profile from automatic AIMService
                  selection; that intent is now expressed through the graded `type`
                  hierarchy (optimized > general > preview > unoptimized) combined with the
                  selector's `minimumType` floor. The field is retained for backward
                  compatibility (existing objects and aim-build profile YAMLs still set it)
                  but has no effect on selection; it will be removed in a future API
                  version. Use `type: unoptimized` (+ a selector `minimumType`) instead.

                  Deprecated: superseded by `type` + selector `minimumType`; ignored by the resolver.
                type: boolean
              metric:
                allOf:
                - enum:
                  - latency
                  - throughput
                - enum:
                  - latency
                  - throughput
                description: Metric is the optimization target for this profile.
                type: string
              modelId:
                description: |-
                  ModelId is the specific model / HuggingFace URI (e.g., "qwen/qwen3-32b-fp8").
                  Determines the cache path (/workspace/cache/{modelId}) and serves as a secondary
                  discriminator for custom weight matching.
                type: string
              modelSources:
                description: |-
                  ModelSources specifies model artifact sources for this profile.
                  Populated during discovery or set by user.
                items:
                  description: |-
                    AIMModelSource describes a model artifact that must be downloaded for inference.
                    Discovery extracts these from the container's configuration to enable caching and validation.
                  properties:
                    env:
                      description: |-
                        Env specifies per-source credential overrides.
                        These variables are used for authentication when downloading this specific source.
                        Takes precedence over base-level env for the same variable name.
                      items:
                        description: EnvVar represents an environment variable present
                          in a Container.
                        properties:
                          name:
                            description: |-
                              Name of the environment variable.
                              May consist of any printable ASCII characters except '='.
                            type: string
                          value:
                            description: |-
                              Variable references $(VAR_NAME) are expanded
                              using the previously defined environment variables in the container and
                              any service environment variables. If a variable cannot be resolved,
                              the reference in the input string will be unchanged. Double $$ are reduced
                              to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                              "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                              Escaped references will never be expanded, regardless of whether the variable
                              exists or not.
                              Defaults to "".
                            type: string
                          valueFrom:
                            description: Source for the environment variable's value.
                              Cannot be used if value is not empty.
                            properties:
                              configMapKeyRef:
                                description: Selects a key of a ConfigMap.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the ConfigMap or
                                      its key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              fieldRef:
                                description: |-
                                  Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                  spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                properties:
                                  apiVersion:
                                    description: Version of the schema the FieldPath
                                      is written in terms of, defaults to "v1".
                                    type: string
                                  fieldPath:
                                    description: Path of the field to select in the
                                      specified API version.
                                    type: string
                                required:
                                - fieldPath
                                type: object
                                x-kubernetes-map-type: atomic
                              fileKeyRef:
                                description: |-
                                  FileKeyRef selects a key of the env file.
                                  Requires the EnvFiles feature gate to be enabled.
                                properties:
                                  key:
                                    description: |-
                                      The key within the env file. An invalid key will prevent the pod from starting.
                                      The keys defined within a source may consist of any printable ASCII characters except '='.
                                      During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                    type: string
                                  optional:
                                    default: false
                                    description: |-
                                      Specify whether the file or its key must be defined. If the file or key
                                      does not exist, then the env var is not published.
                                      If optional is set to true and the specified key does not exist,
                                      the environment variable will not be set in the Pod's containers.

                                      If optional is set to false and the specified key does not exist,
                                      an error will be returned during Pod creation.
                                    type: boolean
                                  path:
                                    description: |-
                                      The path within the volume from which to select the file.
                                      Must be relative and may not contain the '..' path or start with '..'.
                                    type: string
                                  volumeName:
                                    description: The name of the volume mount containing
                                      the env file.
                                    type: string
                                required:
                                - key
                                - path
                                - volumeName
                                type: object
                                x-kubernetes-map-type: atomic
                              resourceFieldRef:
                                description: |-
                                  Selects a resource of the container: only resources limits and requests
                                  (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                properties:
                                  containerName:
                                    description: 'Container name: required for volumes,
                                      optional for env vars'
                                    type: string
                                  divisor:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: Specifies the output format of the
                                      exposed resources, defaults to "1"
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  resource:
                                    description: 'Required: resource to select'
                                    type: string
                                required:
                                - resource
                                type: object
                                x-kubernetes-map-type: atomic
                              secretKeyRef:
                                description: Selects a key of a secret in the pod's
                                  namespace
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                        required:
                        - name
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - name
                      x-kubernetes-list-type: map
                    modelId:
                      description: |-
                        ModelID is the canonical identifier in {org}/{name} format.
                        Determines the cache mount path: /workspace/cache/{modelId}
                        For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                        For S3 sources, users define their own organizational structure.
                      pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                      type: string
                    precision:
                      allOf:
                      - enum:
                        - auto
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                      - enum:
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                      description: |-
                        Precision describes the runtime precision this source is compatible with.
                        Used to match model sources to profiles during custom weight onboarding.
                      type: string
                    size:
                      anyOf:
                      - type: integer
                      - type: string
                      description: |-
                        Size is the expected storage space required for this model artifact.
                        Used for PVC sizing and capacity planning during cache creation.
                        Optional - if not specified, the download job will discover the size automatically.
                        Can be set explicitly to pre-allocate storage or override auto-discovery.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    sourceUri:
                      description: |-
                        SourceURI is the location from which the model should be downloaded.
                        Supported schemes:
                        - hf://org/model - Hugging Face Hub model
                        - s3://bucket/key - S3-compatible storage
                      pattern: ^(hf|s3)://[^ \t\r\n]+$
                      type: string
                  required:
                  - modelId
                  - sourceUri
                  type: object
                type: array
              precision:
                allOf:
                - enum:
                  - auto
                  - fp4
                  - fp8
                  - fp16
                  - fp32
                  - bf16
                  - int4
                  - int8
                - enum:
                  - fp4
                  - fp8
                  - fp16
                  - fp32
                  - bf16
                  - int4
                  - int8
                description: Precision is the numeric precision used by this profile.
                type: string
              primary:
                default: false
                description: |-
                  Primary marks this as a default/recommended profile. When true, the profile is
                  advertised for standard deployment and copied automatically for custom weight models.
                  Defaults to false when not specified.
                type: boolean
              profileId:
                description: |-
                  ProfileId is the on-disk profile identifier from the AIM image
                  (e.g., "vllm-mi300x-fp8-tp1-latency"). Populated during discovery to link this
                  CRD back to the profile YAML inside the container. Not required for manually
                  created profiles.
                type: string
              resources:
                description: |-
                  Resources is an optional override for K8s resource requests/limits.
                  When set, merged on top of the defaults that AIM Engine computes from
                  AcceleratorType, AcceleratorCount, and cluster-level configuration.
                  The resolved result is written to status.resources.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              serviceAccountName:
                description: ServiceAccountName specifies the service account for
                  workloads.
                type: string
              type:
                allOf:
                - enum:
                  - optimized
                  - general
                  - preview
                  - unoptimized
                - enum:
                  - optimized
                  - general
                  - preview
                  - unoptimized
                description: 'Type indicates the optimization level. Hierarchy: optimized
                  > general > preview > unoptimized.'
                type: string
            required:
            - image
            - primary
            type: object
          status:
            description: AIMProfileStatus defines the observed state of AIMProfile
              / AIMClusterProfile.
            properties:
              baseImage:
                description: |-
                  BaseImage is the AIM_BASE_IMAGE_REF the inspector extracted from the
                  source image when this profile was materialised by AIMModel discovery.
                  Used by derivation flows (AIMService overlays, AIMProfileSet) to rebase
                  the deployment image onto the source's base when overriding model
                  sources, so private mirrors stay self-contained. Empty for
                  user-authored profiles.
                type: string
              conditions:
                description: Conditions represent the latest observations of profile
                  state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              deployable:
                default: false
                description: |-
                  Deployable reports whether the profile is materialised enough to back an
                  AIMService: true when spec.aimId and spec.modelSources are both
                  populated, false for base profiles awaiting derivation.

                  Image discovery of a deployable AIM image emits deployable profiles;
                  base-image discovery emits base profiles (no aimId/modelSources) that
                  a custom-model AIMModel derives into deployable copies.
                type: boolean
              hardwareSummary:
                description: |-
                  HardwareSummary is a human-readable string describing the hardware requirements.
                  Format: "{count} x {model}" for GPU (e.g., "1 x MI300X") or "CPU" for CPU-only.
                type: string
              matchingNodes:
                description: |-
                  MatchingNodes is the count of cluster nodes matching both the accelerator
                  model label and status.resources requests. Zero means NotAvailable.
                format: int32
                type: integer
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              origin:
                description: |-
                  Origin classifies how this profile was produced:
                    - discovered: emitted by image discovery (AIMModel.spec.image).
                    - derived: emitted by an AIMProfileSet or
                      AIMModel.spec.profiles.derivedFrom.
                    - user-authored: created independently by a user.

                  Backfilled by the AIMProfile reconciler when not stamped at creation
                  time; user-authored profiles default to `user-authored`.
                enum:
                - discovered
                - derived
                - user-authored
                type: string
              resolvedNodeAffinity:
                description: |-
                  ResolvedNodeAffinity contains the computed node affinity rules derived from
                  spec.acceleratorModel. Used by AIMService when building InferenceService pods.
                properties:
                  preferredDuringSchedulingIgnoredDuringExecution:
                    description: |-
                      The scheduler will prefer to schedule pods to nodes that satisfy
                      the affinity expressions specified by this field, but it may choose
                      a node that violates one or more of the expressions. The node that is
                      most preferred is the one with the greatest sum of weights, i.e.
                      for each node that meets all of the scheduling requirements (resource
                      request, requiredDuringScheduling affinity expressions, etc.),
                      compute a sum by iterating through the elements of this field and adding
                      "weight" to the sum if the node matches the corresponding matchExpressions; the
                      node(s) with the highest sum are the most preferred.
                    items:
                      description: |-
                        An empty preferred scheduling term matches all objects with implicit weight 0
                        (i.e. it's a no-op). A null preferred scheduling term matches no objects (i.e. is also a no-op).
                      properties:
                        preference:
                          description: A node selector term, associated with the corresponding
                            weight.
                          properties:
                            matchExpressions:
                              description: A list of node selector requirements by
                                node's labels.
                              items:
                                description: |-
                                  A node selector requirement is a selector that contains values, a key, and an operator
                                  that relates the key and values.
                                properties:
                                  key:
                                    description: The label key that the selector applies
                                      to.
                                    type: string
                                  operator:
                                    description: |-
                                      Represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                    type: string
                                  values:
                                    description: |-
                                      An array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. If the operator is Gt or Lt, the values
                                      array must have a single element, which will be interpreted as an integer.
                                      This array is replaced during a strategic merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchFields:
                              description: A list of node selector requirements by
                                node's fields.
                              items:
                                description: |-
                                  A node selector requirement is a selector that contains values, a key, and an operator
                                  that relates the key and values.
                                properties:
                                  key:
                                    description: The label key that the selector applies
                                      to.
                                    type: string
                                  operator:
                                    description: |-
                                      Represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                    type: string
                                  values:
                                    description: |-
                                      An array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. If the operator is Gt or Lt, the values
                                      array must have a single element, which will be interpreted as an integer.
                                      This array is replaced during a strategic merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                          type: object
                          x-kubernetes-map-type: atomic
                        weight:
                          description: Weight associated with matching the corresponding
                            nodeSelectorTerm, in the range 1-100.
                          format: int32
                          type: integer
                      required:
                      - preference
                      - weight
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  requiredDuringSchedulingIgnoredDuringExecution:
                    description: |-
                      If the affinity requirements specified by this field are not met at
                      scheduling time, the pod will not be scheduled onto the node.
                      If the affinity requirements specified by this field cease to be met
                      at some point during pod execution (e.g. due to an update), the system
                      may or may not try to eventually evict the pod from its node.
                    properties:
                      nodeSelectorTerms:
                        description: Required. A list of node selector terms. The
                          terms are ORed.
                        items:
                          description: |-
                            A null or empty node selector term matches no objects. The requirements of
                            them are ANDed.
                            The TopologySelectorTerm type implements a subset of the NodeSelectorTerm.
                          properties:
                            matchExpressions:
                              description: A list of node selector requirements by
                                node's labels.
                              items:
                                description: |-
                                  A node selector requirement is a selector that contains values, a key, and an operator
                                  that relates the key and values.
                                properties:
                                  key:
                                    description: The label key that the selector applies
                                      to.
                                    type: string
                                  operator:
                                    description: |-
                                      Represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                    type: string
                                  values:
                                    description: |-
                                      An array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. If the operator is Gt or Lt, the values
                                      array must have a single element, which will be interpreted as an integer.
                                      This array is replaced during a strategic merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchFields:
                              description: A list of node selector requirements by
                                node's fields.
                              items:
                                description: |-
                                  A node selector requirement is a selector that contains values, a key, and an operator
                                  that relates the key and values.
                                properties:
                                  key:
                                    description: The label key that the selector applies
                                      to.
                                    type: string
                                  operator:
                                    description: |-
                                      Represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                    type: string
                                  values:
                                    description: |-
                                      An array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. If the operator is Gt or Lt, the values
                                      array must have a single element, which will be interpreted as an integer.
                                      This array is replaced during a strategic merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                          type: object
                          x-kubernetes-map-type: atomic
                        type: array
                        x-kubernetes-list-type: atomic
                    required:
                    - nodeSelectorTerms
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
              resources:
                description: |-
                  Resources contains the definitive K8s resource requests/limits used for deployment.
                  Computed by AIM Engine from AcceleratorType, AcceleratorCount, and cluster-level
                  configuration, then merged with any spec.resources override.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              sourceModel:
                description: |-
                  SourceModel identifies the producing AIM(Cluster)Model for profiles
                  owned by AIMModel reconcilers. Empty for user-authored profiles.
                properties:
                  kind:
                    description: Kind is the producing model's kind ("AIMModel" or
                      "AIMClusterModel").
                    enum:
                    - AIMModel
                    - AIMClusterModel
                    type: string
                  name:
                    description: Name is the producing model's name.
                    type: string
                  namespace:
                    description: |-
                      Namespace is the producing model's namespace. Empty when Kind is
                      AIMClusterModel (cluster-scoped).
                    type: string
                required:
                - kind
                - name
                type: object
              status:
                default: Pending
                description: |-
                  Status represents the current high-level status of this profile.
                  Ready: at least one cluster node matches the profile's accelerator labels and resource requests.
                  NotAvailable: no matching nodes found.
                enum:
                - Pending
                - Progressing
                - Ready
                - Degraded
                - Failed
                - NotAvailable
                type: string
              version:
                description: Version is extracted from the spec.image tag during reconciliation
                  (e.g., "0.8.5").
                type: string
            required:
            - deployable
            type: object
        type: object
        x-kubernetes-validations:
        - message: spec.aimId and spec.modelSources must both be set (deployable profile)
            or both be empty (base profile)
          rule: (has(self.spec.aimId) && size(self.spec.aimId) > 0) == (has(self.spec.modelSources)
            && size(self.spec.modelSources) > 0)
        - message: acceleratorPartitioningMode must be 'unpartitioned' (or unset)
            when acceleratorType is cpu
          rule: '!(has(self.spec.acceleratorType) && self.spec.acceleratorType ==
            ''cpu'') || !has(self.spec.acceleratorPartitioningMode) || size(self.spec.acceleratorPartitioningMode)
            == 0 || self.spec.acceleratorPartitioningMode == ''unpartitioned'''
    selectableFields:
    - jsonPath: .spec.aimId
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: aimclusterprofilesets.aim.eai.amd.com
spec:
  group: aim.eai.amd.com
  names:
    categories:
    - aim
    - all
    kind: AIMClusterProfileSet
    listKind: AIMClusterProfileSetList
    plural: aimclusterprofilesets
    shortNames:
    - aimclprfset
    singular: aimclusterprofileset
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.managedProfiles.total
      name: Managed
      type: integer
    - jsonPath: .status.managedProfiles.ready
      name: Ready
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: AIMClusterProfileSet is the Schema for cluster-scoped profile
          derivation resources.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              AIMProfileSetSpec defines the desired state of AIMProfileSet and is also reused by AIMModel.profileCopy.

              The selector-non-empty rule accepts every documented narrowing field
              (including modelRef and origin) and lets sourceRef-only specs through:
              when sourceRef is set, the discovery cache itself acts as the source
              scope and a separate selector is not required.
            properties:
              image:
                description: Image overrides the runtime image used by the derived
                  profiles.
                type: string
              imagePullSecrets:
                description: ImagePullSecrets lists secrets used for inspecting and
                  pulling container images.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              overrides:
                description: Overrides mutates the copied profile spec after selection
                  and version filtering.
                properties:
                  acceleratorCount:
                    description: AcceleratorCount replaces the copied profile's acceleratorCount.
                    format: int32
                    type: integer
                  acceleratorModel:
                    description: AcceleratorModel replaces the copied profile's acceleratorModel.
                    type: string
                  acceleratorPartitioningMode:
                    description: |-
                      AcceleratorPartitioningMode replaces the copied profile's
                      acceleratorPartitioningMode. Complete replacement, not a merge; an empty
                      override string leaves the source profile's mode untouched. Same reserved
                      values as AIMProfileSpecCommon.AcceleratorPartitioningMode. Whenever this
                      override is set (to any value, including "unpartitioned"), the CEL rule on the
                      enclosing spec also requires AcceleratorCount to be set: partition mode
                      changes the per-unit interpretation of acceleratorCount, and CEL cannot
                      read the base profile to tell whether the meaning actually changed, so it
                      conservatively requires the count be restated.
                    type: string
                  aimId:
                    description: |-
                      AimId stamps the derived profile's spec.aimId. When set, wins over
                      the source profile's aimId. REQUIRED when the enclosing selector has
                      role=base (the source base profile carries no aimId of its own).
                    type: string
                  containerEnv:
                    description: ContainerEnv merges by env var name, overriding matching
                      source entries.
                    items:
                      description: EnvVar represents an environment variable present
                        in a Container.
                      properties:
                        name:
                          description: |-
                            Name of the environment variable.
                            May consist of any printable ASCII characters except '='.
                          type: string
                        value:
                          description: |-
                            Variable references $(VAR_NAME) are expanded
                            using the previously defined environment variables in the container and
                            any service environment variables. If a variable cannot be resolved,
                            the reference in the input string will be unchanged. Double $$ are reduced
                            to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                            "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                            Escaped references will never be expanded, regardless of whether the variable
                            exists or not.
                            Defaults to "".
                          type: string
                        valueFrom:
                          description: Source for the environment variable's value.
                            Cannot be used if value is not empty.
                          properties:
                            configMapKeyRef:
                              description: Selects a key of a ConfigMap.
                              properties:
                                key:
                                  description: The key to select.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the ConfigMap or its
                                    key must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                            fieldRef:
                              description: |-
                                Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                              properties:
                                apiVersion:
                                  description: Version of the schema the FieldPath
                                    is written in terms of, defaults to "v1".
                                  type: string
                                fieldPath:
                                  description: Path of the field to select in the
                                    specified API version.
                                  type: string
                              required:
                              - fieldPath
                              type: object
                              x-kubernetes-map-type: atomic
                            fileKeyRef:
                              description: |-
                                FileKeyRef selects a key of the env file.
                                Requires the EnvFiles feature gate to be enabled.
                              properties:
                                key:
                                  description: |-
                                    The key within the env file. An invalid key will prevent the pod from starting.
                                    The keys defined within a source may consist of any printable ASCII characters except '='.
                                    During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                  type: string
                                optional:
                                  default: false
                                  description: |-
                                    Specify whether the file or its key must be defined. If the file or key
                                    does not exist, then the env var is not published.
                                    If optional is set to true and the specified key does not exist,
                                    the environment variable will not be set in the Pod's containers.

                                    If optional is set to false and the specified key does not exist,
                                    an error will be returned during Pod creation.
                                  type: boolean
                                path:
                                  description: |-
                                    The path within the volume from which to select the file.
                                    Must be relative and may not contain the '..' path or start with '..'.
                                  type: string
                                volumeName:
                                  description: The name of the volume mount containing
                                    the env file.
                                  type: string
                              required:
                              - key
                              - path
                              - volumeName
                              type: object
                              x-kubernetes-map-type: atomic
                            resourceFieldRef:
                              description: |-
                                Selects a resource of the container: only resources limits and requests
                                (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                              properties:
                                containerName:
                                  description: 'Container name: required for volumes,
                                    optional for env vars'
                                  type: string
                                divisor:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: Specifies the output format of the
                                    exposed resources, defaults to "1"
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                resource:
                                  description: 'Required: resource to select'
                                  type: string
                              required:
                              - resource
                              type: object
                              x-kubernetes-map-type: atomic
                            secretKeyRef:
                              description: Selects a key of a secret in the pod's
                                namespace
                              properties:
                                key:
                                  description: The key of the secret to select from.  Must
                                    be a valid secret key.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the Secret or its key
                                    must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  engineArgs:
                    description: EngineArgs shallow-merges on top of the source engineArgs,
                      overriding matching keys.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  engineEnv:
                    additionalProperties:
                      type: string
                    description: EngineEnv merges by key, overriding matching source
                      entries.
                    type: object
                  image:
                    description: |-
                      Image overrides the runtime container image used by the derived
                      profiles. When empty the deployment image is rebased onto the source
                      profile's base-image (status.baseImage) so private mirrors stay
                      self-contained.
                    type: string
                  modelId:
                    description: |-
                      ModelId stamps the derived profile's spec.modelId. When set, wins
                      over both the source profile's modelId AND the auto-derivation from
                      modelSources[0].modelId. REQUIRED when the enclosing selector has
                      role=base.
                    type: string
                  modelSources:
                    description: |-
                      ModelSources replaces the copied profile's modelSources. When
                      modelSources[0].modelId is set and overrides.modelId is unset, the
                      derived profile's modelId is auto-derived from modelSources[0]; an
                      explicit overrides.modelId always wins.
                    items:
                      description: |-
                        AIMModelSource describes a model artifact that must be downloaded for inference.
                        Discovery extracts these from the container's configuration to enable caching and validation.
                      properties:
                        env:
                          description: |-
                            Env specifies per-source credential overrides.
                            These variables are used for authentication when downloading this specific source.
                            Takes precedence over base-level env for the same variable name.
                          items:
                            description: EnvVar represents an environment variable
                              present in a Container.
                            properties:
                              name:
                                description: |-
                                  Name of the environment variable.
                                  May consist of any printable ASCII characters except '='.
                                type: string
                              value:
                                description: |-
                                  Variable references $(VAR_NAME) are expanded
                                  using the previously defined environment variables in the container and
                                  any service environment variables. If a variable cannot be resolved,
                                  the reference in the input string will be unchanged. Double $$ are reduced
                                  to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                  "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                  Escaped references will never be expanded, regardless of whether the variable
                                  exists or not.
                                  Defaults to "".
                                type: string
                              valueFrom:
                                description: Source for the environment variable's
                                  value. Cannot be used if value is not empty.
                                properties:
                                  configMapKeyRef:
                                    description: Selects a key of a ConfigMap.
                                    properties:
                                      key:
                                        description: The key to select.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the ConfigMap
                                          or its key must be defined
                                        type: boolean
                                    required:
                                    - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  fieldRef:
                                    description: |-
                                      Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                      spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                    properties:
                                      apiVersion:
                                        description: Version of the schema the FieldPath
                                          is written in terms of, defaults to "v1".
                                        type: string
                                      fieldPath:
                                        description: Path of the field to select in
                                          the specified API version.
                                        type: string
                                    required:
                                    - fieldPath
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  fileKeyRef:
                                    description: |-
                                      FileKeyRef selects a key of the env file.
                                      Requires the EnvFiles feature gate to be enabled.
                                    properties:
                                      key:
                                        description: |-
                                          The key within the env file. An invalid key will prevent the pod from starting.
                                          The keys defined within a source may consist of any printable ASCII characters except '='.
                                          During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                        type: string
                                      optional:
                                        default: false
                                        description: |-
                                          Specify whether the file or its key must be defined. If the file or key
                                          does not exist, then the env var is not published.
                                          If optional is set to true and the specified key does not exist,
                                          the environment variable will not be set in the Pod's containers.

                                          If optional is set to false and the specified key does not exist,
                                          an error will be returned during Pod creation.
                                        type: boolean
                                      path:
                                        description: |-
                                          The path within the volume from which to select the file.
                                          Must be relative and may not contain the '..' path or start with '..'.
                                        type: string
                                      volumeName:
                                        description: The name of the volume mount
                                          containing the env file.
                                        type: string
                                    required:
                                    - key
                                    - path
                                    - volumeName
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  resourceFieldRef:
                                    description: |-
                                      Selects a resource of the container: only resources limits and requests
                                      (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                    properties:
                                      containerName:
                                        description: 'Container name: required for
                                          volumes, optional for env vars'
                                        type: string
                                      divisor:
                                        anyOf:
                                        - type: integer
                                        - type: string
                                        description: Specifies the output format of
                                          the exposed resources, defaults to "1"
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      resource:
                                        description: 'Required: resource to select'
                                        type: string
                                    required:
                                    - resource
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  secretKeyRef:
                                    description: Selects a key of a secret in the
                                      pod's namespace
                                    properties:
                                      key:
                                        description: The key of the secret to select
                                          from.  Must be a valid secret key.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the Secret or
                                          its key must be defined
                                        type: boolean
                                    required:
                                    - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                type: object
                            required:
                            - name
                            type: object
                          type: array
                          x-kubernetes-list-map-keys:
                          - name
                          x-kubernetes-list-type: map
                        modelId:
                          description: |-
                            ModelID is the canonical identifier in {org}/{name} format.
                            Determines the cache mount path: /workspace/cache/{modelId}
                            For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                            For S3 sources, users define their own organizational structure.
                          pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                          type: string
                        precision:
                          allOf:
                          - enum:
                            - auto
                            - fp4
                            - fp8
                            - fp16
                            - fp32
                            - bf16
                            - int4
                            - int8
                          - enum:
                            - fp4
                            - fp8
                            - fp16
                            - fp32
                            - bf16
                            - int4
                            - int8
                          description: |-
                            Precision describes the runtime precision this source is compatible with.
                            Used to match model sources to profiles during custom weight onboarding.
                          type: string
                        size:
                          anyOf:
                          - type: integer
                          - type: string
                          description: |-
                            Size is the expected storage space required for this model artifact.
                            Used for PVC sizing and capacity planning during cache creation.
                            Optional - if not specified, the download job will discover the size automatically.
                            Can be set explicitly to pre-allocate storage or override auto-discovery.
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        sourceUri:
                          description: |-
                            SourceURI is the location from which the model should be downloaded.
                            Supported schemes:
                            - hf://org/model - Hugging Face Hub model
                            - s3://bucket/key - S3-compatible storage
                          pattern: ^(hf|s3)://[^ \t\r\n]+$
                          type: string
                      required:
                      - modelId
                      - sourceUri
                      type: object
                    type: array
                  profileId:
                    description: |-
                      ProfileId stamps the derived profile's spec.profileId. Optional —
                      most callers leave this empty and let the source profile's profileId
                      carry through (or the reconciler synthesise one).
                    type: string
                type: object
              selector:
                description: Selector chooses which source profiles to derive from.
                properties:
                  acceleratorCount:
                    description: AcceleratorCount filters by accelerator unit count.
                    format: int32
                    type: integer
                  acceleratorModel:
                    description: AcceleratorModel filters by accelerator identifier.
                    type: string
                  acceleratorPartitioningMode:
                    description: |-
                      AcceleratorPartitioningMode filters candidates by their declared
                      partitioning mode. Partial-order match (NOT strict equality):
                        ""              - no filter on this field.
                        "unpartitioned" - matches profiles with mode "" or "unpartitioned".
                        "partitioned"   - matches profiles whose mode is non-trivial (anything
                                          other than "" / "unpartitioned").
                        "<C>"           - selector-only convenience: matches profiles with mode
                                          "<C>-*" (prefix on the scheme). Not a valid profile-spec
                                          value (e.g. selector "CPX" matches "CPX-NPS1", "CPX-NPS4").
                        "<C>-<M>"       - exact-string match on the scheme.
                    type: string
                  acceleratorType:
                    description: AcceleratorType filters by accelerator resource type.
                    enum:
                    - gpu
                    - cpu
                    type: string
                  aimId:
                    description: AimId filters by model architecture identifier (e.g.,
                      "qwen/qwen3-32b").
                    type: string
                  engine:
                    description: Engine filters by inference engine.
                    type: string
                  engineArgs:
                    description: |-
                      EngineArgs partially matches source engineArgs: every provided top-level key must
                      exist in the source object with an equal value.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  metric:
                    description: Metric filters by optimization target.
                    enum:
                    - latency
                    - throughput
                    type: string
                  minimumType:
                    description: |-
                      MinimumType filters by a minimum optimization level: candidates whose
                      type is this tier OR BETTER are accepted (hierarchy: optimized > general
                      > preview > unoptimized). This is the floor counterpart to the exact-match
                      Type field; the two AND together when both are set.

                      The sentinel "any" disables the floor (accept every tier). When this
                      field is empty the AIMService resolver applies a default floor of
                      "optimized" so auto-selection prefers production-grade profiles and never
                      silently picks an unoptimized one; to opt a service into lower tiers
                      (e.g. CPU/EPYC profiles published as unoptimized) set minimumType
                      explicitly to "unoptimized" or "any". Derivation selectors
                      (AIMProfileSet / AIMModel.profiles) treat empty as "any" so copying is
                      never tier-restricted by default.
                    enum:
                    - optimized
                    - general
                    - preview
                    - unoptimized
                    - any
                    type: string
                  modelId:
                    description: ModelId filters by the source profile's specific
                      model identifier.
                    type: string
                  modelRef:
                    description: |-
                      ModelRef narrows candidates to those produced by a specific
                      AIM(Cluster)Model, matched via the `aim.eai.amd.com/source-model[-scope]`
                      labels stamped by the AIMModel reconcilers. Iteration 1 (v1alpha2 only).
                    properties:
                      name:
                        description: Name is the owning AIM(Cluster)Model name. Required.
                        minLength: 1
                        type: string
                      scope:
                        default: Auto
                        description: Scope controls how Name is resolved against AIMModel
                          vs AIMClusterModel.
                        enum:
                        - Auto
                        - Namespace
                        - Cluster
                        type: string
                    required:
                    - name
                    type: object
                  origin:
                    description: |-
                      Origin filters by the `aim.eai.amd.com/profile-origin` label. When unset
                      (empty) the selector does not filter by origin. Iteration 1 (v1alpha2
                      only).
                    enum:
                    - discovered
                    - derived
                    - user-authored
                    type: string
                  precision:
                    description: Precision filters by numeric precision.
                    enum:
                    - auto
                    - fp4
                    - fp8
                    - fp16
                    - fp32
                    - bf16
                    - int4
                    - int8
                    type: string
                  profileId:
                    description: ProfileId filters by the source profile's profile
                      identifier.
                    type: string
                  role:
                    default: deployable
                    description: |-
                      Role filters by the `aim.eai.amd.com/profile-role` label. Defaults to
                      `deployable`. `base` filters to base profiles emitted by base-image
                      discovery (custom-model derivation source material).
                    enum:
                    - base
                    - deployable
                    type: string
                  type:
                    description: Type filters by optimization level (exact match).
                    enum:
                    - optimized
                    - general
                    - preview
                    - unoptimized
                    type: string
                type: object
              serviceAccountName:
                description: ServiceAccountName is propagated to managed profiles
                  for downstream workloads.
                type: string
              sourceRef:
                description: |-
                  SourceRef points to an alternate discovery cache source.
                  When omitted, derivation uses visible AIMProfile and AIMClusterProfile objects.
                  For AIMProfileSet, the referenced ConfigMap is read from the same namespace.
                  For AIMClusterProfileSet, it is read from the operator namespace.
                properties:
                  name:
                    description: Name is the discovery cache ConfigMap name.
                    type: string
                required:
                - name
                type: object
              version:
                description: Version pins matching to a specific source profile version
                  when VersionPolicy is pinned.
                type: string
              versionPolicy:
                default: pinned
                description: VersionPolicy controls how matching profiles are filtered
                  by version.
                enum:
                - pinned
                - latest
                - all
                type: string
            type: object
            x-kubernetes-validations:
            - message: selector must set at least one matching field, or set sourceRef
              rule: has(self.sourceRef) || has(self.selector.aimId) || has(self.selector.modelId)
                || has(self.selector.profileId) || has(self.selector.engine) || has(self.selector.metric)
                || has(self.selector.precision) || has(self.selector.type) || has(self.selector.acceleratorModel)
                || has(self.selector.acceleratorType) || has(self.selector.acceleratorCount)
                || has(self.selector.engineArgs) || has(self.selector.modelRef) ||
                has(self.selector.origin)
            - message: version is required when versionPolicy is pinned
              rule: has(self.version) || (has(self.versionPolicy) && self.versionPolicy
                != 'pinned')
            - message: latest/all versionPolicy must not set version
              rule: '!has(self.versionPolicy) || (self.versionPolicy != ''latest''
                && self.versionPolicy != ''all'') || !has(self.version)'
            - message: selector.aimId/modelId/profileId are not allowed when selector.role=base;
                set overrides.aimId/modelId/profileId instead (base profiles have
                no source identity to filter on)
              rule: '!(has(self.selector.role) && self.selector.role == ''base'')
                || (!has(self.selector.aimId) && !has(self.selector.modelId) && !has(self.selector.profileId))'
            - message: selector.role=base requires overrides.aimId and overrides.modelId
                (base profiles carry no identity of their own; overrides supply it)
              rule: '!(has(self.selector.role) && self.selector.role == ''base'')
                || (has(self.overrides) && has(self.overrides.aimId) && has(self.overrides.modelId))'
            - message: overrides.acceleratorCount must be specified together with
                any overrides.acceleratorPartitioningMode; partition mode changes
                the per-unit interpretation of acceleratorCount
              rule: '!has(self.overrides) || !has(self.overrides.acceleratorPartitioningMode)
                || size(self.overrides.acceleratorPartitioningMode) == 0 || has(self.overrides.acceleratorCount)'
          status:
            description: AIMProfileSetStatus defines the observed state of AIMProfileSet
              / AIMClusterProfileSet.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the profile set's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              managedProfiles:
                description: ManagedProfiles summarizes the direct derivative profiles
                  owned or managed by this profile set.
                properties:
                  base:
                    description: |-
                      Base is the number of managed profiles whose spec is structurally
                      incomplete (missing aimId or modelSources). Base profiles cannot back
                      an AIMService directly and exist purely as source material for
                      derivation via AIMModel.spec.profiles.derivedFrom with
                      selector.role=base. A non-zero value is the canonical operational
                      signal that this model is a base-image model (the source of
                      custom-model derivation).
                    format: int32
                    type: integer
                  deployable:
                    description: |-
                      Deployable is the number of managed profiles whose spec is materialised
                      enough to back an AIMService (carries aimId and modelSources). For a
                      normal officially-discovered AIMModel this equals Total. For a
                      base-image AIMModel used as a source for custom-model derivation
                      this is 0 — the model only emits base profiles that callers must
                      derive into deployable profiles.
                    format: int32
                    type: integer
                  notAvailable:
                    description: NotAvailable is the number of derivative profiles
                      whose own status is NotAvailable.
                    format: int32
                    type: integer
                  ready:
                    description: Ready is the number of derivative profiles whose
                      own status is Ready.
                    format: int32
                    type: integer
                  total:
                    description: Total is the number of derivative profiles the controller
                      currently manages or intends to manage.
                    format: int32
                    type: integer
                required:
                - base
                - deployable
                - notAvailable
                - ready
                - total
                type: object
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              status:
                default: Pending
                description: Status represents the overall high-level status for the
                  profile set.
                enum:
                - Pending
                - Progressing
                - Ready
                - Degraded
                - Failed
                - NotAvailable
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: aimclusterruntimeconfigs.aim.eai.amd.com
spec:
  group: aim.eai.amd.com
  names:
    categories:
    - aim
    - all
    kind: AIMClusterRuntimeConfig
    listKind: AIMClusterRuntimeConfigList
    plural: aimclusterruntimeconfigs
    shortNames:
    - aimclrc
    singular: aimclusterruntimeconfig
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          AIMClusterRuntimeConfig is a cluster-scoped runtime configuration for AIM services, models, and templates.

          Cluster-scoped runtime configs provide platform-wide defaults that apply to all namespaces,
          making them ideal for organization-level policies such as storage classes, discovery behavior,
          model creation scope, and routing configuration.

          When both cluster and namespace runtime configs exist with the same name, the configs are merged, and
          the namespace-scoped AIMRuntimeConfig takes precedence for any field that is set in both.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AIMClusterRuntimeConfigSpec defines cluster-wide defaults
              for AIM resources.
            properties:
              artifact:
                description: |-
                  Artifact controls artifact-level defaults such as eviction policy.
                  This field only applies to RuntimeConfig/ClusterRuntimeConfig and is not available for services.
                properties:
                  defaultRetentionPriority:
                    description: |-
                      DefaultRetentionPriority sets the default retention priority for AIMArtifacts
                      that do not specify one in their spec. When set, artifacts without an explicit
                      retentionPriority become eligible for automatic eviction at this priority level.
                      Lower values are evicted first. If not set, artifacts without an explicit
                      retentionPriority are never automatically evicted.
                    format: int32
                    minimum: 0
                    type: integer
                  modelDownloadImage:
                    description: |-
                      ModelDownloadImage specifies the default container image for artifact
                      download and size-check jobs. Applies when an AIMArtifact does not set
                      spec.modelDownloadImage. When neither is set, the operator falls back
                      to its build-time default (matching the release version).
                    type: string
                type: object
              artifactCache:
                description: |-
                  ArtifactCache configures the S3-backed artifact cache for HuggingFace models.
                  When enabled, the controller checks internal S3 before downloading from HuggingFace.
                properties:
                  enabled:
                    description: Enabled controls whether the S3 artifact cache is
                      active.
                    type: boolean
                  env:
                    description: |-
                      Env provides S3 endpoint configuration for the cache bucket.
                      Injected into download jobs when the source is rewritten to s3://.
                      Typical vars: AWS_ENDPOINT_URL, S3_NO_SSL. Credentials optional (anonymous access).
                    items:
                      description: EnvVar represents an environment variable present
                        in a Container.
                      properties:
                        name:
                          description: |-
                            Name of the environment variable.
                            May consist of any printable ASCII characters except '='.
                          type: string
                        value:
                          description: |-
                            Variable references $(VAR_NAME) are expanded
                            using the previously defined environment variables in the container and
                            any service environment variables. If a variable cannot be resolved,
                            the reference in the input string will be unchanged. Double $$ are reduced
                            to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                            "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                            Escaped references will never be expanded, regardless of whether the variable
                            exists or not.
                            Defaults to "".
                          type: string
                        valueFrom:
                          description: Source for the environment variable's value.
                            Cannot be used if value is not empty.
                          properties:
                            configMapKeyRef:
                              description: Selects a key of a ConfigMap.
                              properties:
                                key:
                                  description: The key to select.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the ConfigMap or its
                                    key must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                            fieldRef:
                              description: |-
                                Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                              properties:
                                apiVersion:
                                  description: Version of the schema the FieldPath
                                    is written in terms of, defaults to "v1".
                                  type: string
                                fieldPath:
                                  description: Path of the field to select in the
                                    specified API version.
                                  type: string
                              required:
                              - fieldPath
                              type: object
                              x-kubernetes-map-type: atomic
                            fileKeyRef:
                              description: |-
                                FileKeyRef selects a key of the env file.
                                Requires the EnvFiles feature gate to be enabled.
                              properties:
                                key:
                                  description: |-
                                    The key within the env file. An invalid key will prevent the pod from starting.
                                    The keys defined within a source may consist of any printable ASCII characters except '='.
                                    During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                  type: string
                                optional:
                                  default: false
                                  description: |-
                                    Specify whether the file or its key must be defined. If the file or key
                                    does not exist, then the env var is not published.
                                    If optional is set to true and the specified key does not exist,
                                    the environment variable will not be set in the Pod's containers.

                                    If optional is set to false and the specified key does not exist,
                                    an error will be returned during Pod creation.
                                  type: boolean
                                path:
                                  description: |-
                                    The path within the volume from which to select the file.
                                    Must be relative and may not contain the '..' path or start with '..'.
                                  type: string
                                volumeName:
                                  description: The name of the volume mount containing
                                    the env file.
                                  type: string
                              required:
                              - key
                              - path
                              - volumeName
                              type: object
                              x-kubernetes-map-type: atomic
                            resourceFieldRef:
                              description: |-
                                Selects a resource of the container: only resources limits and requests
                                (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                              properties:
                                containerName:
                                  description: 'Container name: required for volumes,
                                    optional for env vars'
                                  type: string
                                divisor:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: Specifies the output format of the
                                    exposed resources, defaults to "1"
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                resource:
                                  description: 'Required: resource to select'
                                  type: string
                              required:
                              - resource
                              type: object
                              x-kubernetes-map-type: atomic
                            secretKeyRef:
                              description: Selects a key of a secret in the pod's
                                namespace
                              properties:
                                key:
                                  description: The key of the secret to select from.  Must
                                    be a valid secret key.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the Secret or its key
                                    must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  s3Uri:
                    description: S3URI is the base S3 path for cached artifacts (e.g.
                      s3://aim-cache/artifacts).
                    type: string
                type: object
              artifactStorageQuota:
                description: |-
                  ArtifactStorageQuota configures storage limits for AIMArtifacts.
                  These limits control how much total PVC storage artifacts may consume,
                  both cluster-wide and per-namespace.
                properties:
                  clusterLimit:
                    anyOf:
                    - type: integer
                    - type: string
                    description: |-
                      ClusterLimit is the maximum total allocated storage for all AIMArtifacts cluster-wide.
                      When the sum of all artifact PVC sizes across all namespaces would exceed this limit,
                      new artifact PVCs are blocked until evictable artifacts are cleaned up or the limit is raised.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  defaultNamespaceLimit:
                    anyOf:
                    - type: integer
                    - type: string
                    description: |-
                      DefaultNamespaceLimit is the default maximum allocated storage for AIMArtifacts per namespace.
                      Can be overridden for individual namespaces via the aim.eai.amd.com/artifact-storage-quota annotation.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                type: object
              defaultStorageClassName:
                description: |-
                  DEPRECATED: Use Storage.DefaultStorageClassName instead. This field will be removed in a future version.
                  For backward compatibility, if this field is set and Storage.DefaultStorageClassName is not set,
                  the value will be automatically migrated.
                type: string
              env:
                description: |-
                  Env specifies environment variables for inference containers.
                  When set on AIMService, these take highest precedence in the merge hierarchy.
                  When set on RuntimeConfig, these provide namespace/cluster-level defaults.
                  Merge order (highest to lowest): Service.Env > Template.Env > RuntimeConfig.Env > Profile.Env
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: |-
                        Name of the environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        fileKeyRef:
                          description: |-
                            FileKeyRef selects a key of the env file.
                            Requires the EnvFiles feature gate to be enabled.
                          properties:
                            key:
                              description: |-
                                The key within the env file. An invalid key will prevent the pod from starting.
                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                              type: string
                            optional:
                              default: false
                              description: |-
                                Specify whether the file or its key must be defined. If the file or key
                                does not exist, then the env var is not published.
                                If optional is set to true and the specified key does not exist,
                                the environment variable will not be set in the Pod's containers.

                                If optional is set to false and the specified key does not exist,
                                an error will be returned during Pod creation.
                              type: boolean
                            path:
                              description: |-
                                The path within the volume from which to select the file.
                                Must be relative and may not contain the '..' path or start with '..'.
                              type: string
                            volumeName:
                              description: The name of the volume mount containing
                                the env file.
                              type: string
                          required:
                          - key
                          - path
                          - volumeName
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              labelPropagation:
                description: |-
                  LabelPropagation controls how labels from parent AIM resources are propagated to child resources.
                  When enabled, labels matching the specified patterns are automatically copied from parent resources
                  (e.g., AIMService, AIMTemplateCache) to their child resources (e.g., Deployments, Services, PVCs).
                  This is useful for propagating organizational metadata like cost centers, team identifiers,
                  or compliance labels through the resource hierarchy.
                properties:
                  enabled:
                    default: false
                    description: |-
                      Enabled, if true, allows propagating parent labels to all child resources it creates directly
                      Only label keys that match the ones in Match are propagated.
                    type: boolean
                  match:
                    description: |-
                      Match is a list of label keys that will be propagated to any child resources created.
                      Wildcards are supported, so for example `org.my/my-key-*` would match any label with that prefix.
                    items:
                      type: string
                    type: array
                type: object
              model:
                description: |-
                  Model controls model creation and discovery defaults.
                  This field only applies to RuntimeConfig/ClusterRuntimeConfig and is not available for services.
                properties:
                  autoDiscovery:
                    description: |-
                      AutoDiscovery controls whether models run discovery by default.
                      When true, models run discovery jobs to extract metadata and auto-create templates.
                      When false, discovery is skipped. Discovery failures are non-fatal and reported via conditions.
                    type: boolean
                type: object
              pvcHeadroomPercent:
                description: |-
                  DEPRECATED: Use Storage.PVCHeadroomPercent instead. This field will be removed in a future version.
                  For backward compatibility, if this field is set and Storage.PVCHeadroomPercent is not set,
                  the value will be automatically migrated.
                format: int32
                type: integer
              routing:
                description: |-
                  Routing controls HTTP routing configuration for this service.
                  When set, these values override namespace/cluster runtime config defaults.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: |-
                      Annotations defines default annotations to add to all HTTPRoute resources.
                      Services can add additional annotations or override these via spec.routing.annotations.
                      When both are specified, service annotations take precedence for conflicting keys.
                      Common use cases include ingress controller settings, rate limiting, monitoring labels,
                      and security policies that should apply to all services using this config.
                    type: object
                  enabled:
                    description: |-
                      Enabled controls whether HTTP routing is managed for inference services using this config.
                      When true, the operator creates HTTPRoute resources for services that reference this config.
                      When false or unset, routing must be explicitly enabled on each service.
                      This provides a namespace or cluster-wide default that individual services can override.
                    type: boolean
                  gatewayRef:
                    description: |-
                      GatewayRef specifies the Gateway API Gateway resource that should receive HTTPRoutes.
                      This identifies the parent gateway for routing traffic to inference services.
                      The gateway can be in any namespace (cross-namespace references are supported).
                      If routing is enabled but GatewayRef is not specified, service reconciliation will fail
                      with a validation error.
                    properties:
                      group:
                        default: gateway.networking.k8s.io
                        description: |-
                          Group is the group of the referent.
                          When unspecified, "gateway.networking.k8s.io" is inferred.
                          To set the core API group (such as for a "Service" kind referent),
                          Group must be explicitly set to "" (empty string).

                          Support: Core
                        maxLength: 253
                        pattern: ^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                        type: string
                      kind:
                        default: Gateway
                        description: |-
                          Kind is kind of the referent.

                          There are two kinds of parent resources with "Core" support:

                          * Gateway (Gateway conformance profile)
                          * Service (Mesh conformance profile, ClusterIP Services only)

                          Support for other resources is Implementation-Specific.
                        maxLength: 63
                        minLength: 1
                        pattern: ^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$
                        type: string
                      name:
                        description: |-
                          Name is the name of the referent.

                          Support: Core
                        maxLength: 253
                        minLength: 1
                        type: string
                      namespace:
                        description: |-
                          Namespace is the namespace of the referent. When unspecified, this refers
                          to the local namespace of the Route.

                          Note that there are specific rules for ParentRefs which cross namespace
                          boundaries. Cross-namespace references are only valid if they are explicitly
                          allowed by something in the namespace they are referring to. For example:
                          Gateway has the AllowedRoutes field, and ReferenceGrant provides a
                          generic way to enable any other kind of cross-namespace reference.

                          <gateway:experimental:description>
                          ParentRefs from a Route to a Service in the same namespace are "producer"
                          routes, which apply default routing rules to inbound connections from
                          any namespace to the Service.

                          ParentRefs from a Route to a Service in a different namespace are
                          "consumer" routes, and these routing rules are only applied to outbound
                          connections originating from the same namespace as the Route, for which
                          the intended destination of the connections are a Service targeted as a
                          ParentRef of the Route.
                          </gateway:experimental:description>

                          Support: Core
                        maxLength: 63
                        minLength: 1
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                        type: string
                      port:
                        description: |-
                          Port is the network port this Route targets. It can be interpreted
                          differently based on the type of parent resource.

                          When the parent resource is a Gateway, this targets all listeners
                          listening on the specified port that also support this kind of Route(and
                          select this Route). It's not recommended to set `Port` unless the
                          networking behaviors specified in a Route must apply to a specific port
                          as opposed to a listener(s) whose port(s) may be changed. When both Port
                          and SectionName are specified, the name and port of the selected listener
                          must match both specified values.

                          <gateway:experimental:description>
                          When the parent resource is a Service, this targets a specific port in the
                          Service spec. When both Port (experimental) and SectionName are specified,
                          the name and port of the selected port must match both specified values.
                          </gateway:experimental:description>

                          Implementations MAY choose to support other parent resources.
                          Implementations supporting other types of parent resources MUST clearly
                          document how/if Port is interpreted.

                          For the purpose of status, an attachment is considered successful as
                          long as the parent resource accepts it partially. For example, Gateway
                          listeners can restrict which Routes can attach to them by Route kind,
                          namespace, or hostname. If 1 of 2 Gateway listeners accept attachment
                          from the referencing Route, the Route MUST be considered successfully
                          attached. If no Gateway listeners accept attachment from this Route,
                          the Route MUST be considered detached from the Gateway.

                          Support: Extended
                        format: int32
                        maximum: 65535
                        minimum: 1
                        type: integer
                      sectionName:
                        description: |-
                          SectionName is the name of a section within the target resource. In the
                          following resources, SectionName is interpreted as the following:

                          * Gateway: Listener name. When both Port (experimental) and SectionName
                          are specified, the name and port of the selected listener must match
                          both specified values.
                          * Service: Port name. When both Port (experimental) and SectionName
                          are specified, the name and port of the selected listener must match
                          both specified values.

                          Implementations MAY choose to support attaching Routes to other resources.
                          If that is the case, they MUST clearly document how SectionName is
                          interpreted.

                          When unspecified (empty string), this will reference the entire resource.
                          For the purpose of status, an attachment is considered successful if at
                          least one section in the parent resource accepts it. For example, Gateway
                          listeners can restrict which Routes can attach to them by Route kind,
                          namespace, or hostname. If 1 of 2 Gateway listeners accept attachment from
                          the referencing Route, the Route MUST be considered successfully
                          attached. If no Gateway listeners accept attachment from this Route, the
                          Route MUST be considered detached from the Gateway.

                          Support: Core
                        maxLength: 253
                        minLength: 1
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                        type: string
                    required:
                    - name
                    type: object
                  hostnames:
                    description: |-
                      Hostnames pins generated HTTPRoutes to these hostnames so a route only
                      attaches to the matching Gateway listener instead of every listener on
                      the parent gateway. Without a hostname, an HTTPRoute matches all of the
                      parent gateway's listener hostnames, which can expose a service on
                      listeners that do not enforce the intended authentication.

                      This field is required when the parent gateway exposes more than one
                      listener: in that case a service with routing enabled but no hostnames
                      configured will not get an HTTPRoute and reports ConfigValid=False with
                      reason RouteHostnameRequired. When the parent gateway has a single
                      listener, leaving this empty preserves the existing behavior (the route
                      inherits that listener's hostnames).

                      Individual services can override this list via spec.routing.hostnames.
                    items:
                      description: |-
                        Hostname is the fully qualified domain name of a network host. This matches
                        the RFC 1123 definition of a hostname with 2 notable exceptions:

                         1. IPs are not allowed.
                         2. A hostname may be prefixed with a wildcard label (`*.`). The wildcard
                            label must appear by itself as the first label.

                        Hostname can be "precise" which is a domain name without the terminating
                        dot of a network host (e.g. "foo.example.com") or "wildcard", which is a
                        domain name prefixed with a single wildcard label (e.g. `*.example.com`).

                        Note that as per RFC1035 and RFC1123, a *label* must consist of lower case
                        alphanumeric characters or '-', and must start and end with an alphanumeric
                        character. No other punctuation is allowed.
                      maxLength: 253
                      minLength: 1
                      pattern: ^(\*\.)?[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                      type: string
                    type: array
                  pathTemplate:
                    description: |-
                      PathTemplate defines the HTTP path template for routes, evaluated using JSONPath expressions.
                      The template is rendered against the AIMService object to generate unique paths.

                      Example templates:
                      - `/{.metadata.namespace}/{.metadata.name}` - namespace and service name
                      - `/{.metadata.namespace}/{.metadata.labels['team']}/inference` - with label
                      - `/models/{.metadata.name}` - based on service name

                      The template must:
                      - Use valid JSONPath expressions wrapped in {...}
                      - Reference fields that exist on the service
                      - Produce a path ≤ 200 characters after rendering
                      - Result in valid URL path segments (lowercase, RFC 1123 compliant)

                      If evaluation fails, the service enters Degraded state with PathTemplateInvalid reason.
                      Individual services can override this template via spec.routing.pathTemplate.
                    type: string
                  requestTimeout:
                    description: |-
                      RequestTimeout defines the HTTP request timeout for routes.
                      This sets the maximum duration for a request to complete before timing out.
                      The timeout applies to the entire request/response cycle.
                      If not specified, no timeout is set on the route.
                      Individual services can override this value via spec.routing.requestTimeout.
                    type: string
                type: object
              storage:
                description: |-
                  Storage configures storage defaults for this service's PVCs and caches.
                  When set, these values override namespace/cluster runtime config defaults.
                properties:
                  adapterDiskSize:
                    anyOf:
                    - type: integer
                    - type: string
                    description: |-
                      AdapterDiskSize is the cluster default size for the shared adapter disk PVC
                      (built-in default when unset). An artifact's adapterDisk.size wins over it.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  adapterDiskStorageClassName:
                    description: |-
                      AdapterDiskStorageClassName is the storage class for the shared
                      ReadWriteMany adapter disk. It must be RWX-capable (e.g. longhorn, NFS) and
                      is resolved before the (typically RWO) DefaultStorageClassName.
                    type: string
                  defaultStorageClassName:
                    description: |-
                      DefaultStorageClassName specifies the storage class to use for artifacts and PVCs
                      when the consuming resource (AIMArtifact, AIMTemplateCache, AIMServiceTemplate) does not
                      specify a storage class. If this field is empty, the cluster's default storage class is used.
                    type: string
                  downloadFilter:
                    description: |-
                      DownloadFilter controls which files are included or excluded during artifact downloads.
                      When set here, applies as the default for all artifacts using this runtime config.
                      Individual artifacts can override this with their own downloadFilter.
                      When no filter is configured at any level, subdirectory files are excluded by default.
                      Set to an empty object (downloadFilter: {}) to explicitly allow all files.
                    properties:
                      exclude:
                        description: |-
                          Exclude specifies glob patterns for files to skip.
                          Files matching any exclude pattern are removed after include filtering.
                          Patterns use fnmatch syntax (e.g., ["*/*", "*.bin"]).
                          Use ["*/*"] to exclude all files in subdirectories (the default when no filter is set).
                        items:
                          type: string
                        type: array
                      include:
                        description: |-
                          Include specifies glob patterns for files to download.
                          Only files matching at least one pattern are considered.
                          If empty, all files pass the include check.
                          Patterns use fnmatch syntax (e.g., ["*.safetensors", "config.json"]).
                        items:
                          type: string
                        type: array
                    type: object
                  pvcHeadroomPercent:
                    default: 10
                    description: |-
                      PVCHeadroomPercent specifies the percentage of extra space to add to PVCs
                      for model storage. This accounts for filesystem overhead and temporary files
                      during model loading. The value represents a percentage (e.g., 10 means 10% extra space).
                      If not specified, defaults to 10%.
                    format: int32
                    minimum: 0
                    type: integer
                type: object
            type: object
          status:
            description: AIMRuntimeConfigStatus records the resolved config reference
              surfaced to consumers.
            properties:
              conditions:
                description: Conditions communicate reconciliation progress.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the last reconciled generation.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: aimclusterservicetemplates.aim.eai.amd.com
spec:
  group: aim.eai.amd.com
  names:
    categories:
    - aim
    - all
    kind: AIMClusterServiceTemplate
    listKind: AIMClusterServiceTemplateList
    plural: aimclusterservicetemplates
    shortNames:
    - aimcltpl
    singular: aimclusterservicetemplate
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.modelName
      name: Model
      type: string
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.hardwareSummary
      name: Hardware
      type: string
    - jsonPath: .status.profile.metadata.metric
      name: Metric
      type: string
    - jsonPath: .status.profile.metadata.precision
      name: Precision
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          AIMClusterServiceTemplate is a cluster-scoped template that defines runtime profiles for AIM services.

          Cluster-scoped templates can be used by AIMServices in any namespace, making them ideal for
          platform-wide model configurations that should be shared across teams and projects.
          Unlike namespace-scoped AIMServiceTemplates, cluster templates do not support caching configuration
          and must be managed by cluster administrators, since caches themselves are namespace-scoped.

          When both cluster and namespace templates exist with the same name, the namespace-scoped template
          takes precedence for services in that namespace.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              AIMClusterServiceTemplateSpec defines the desired state of AIMClusterServiceTemplate (cluster-scoped).

              A cluster-scoped template that selects a runtime profile for a given AIM model.
            properties:
              aimId:
                description: |-
                  AimId is the AIM product family identifier (e.g., "meta-llama/Llama-3-8B").
                  Required when customProfile is set; used to assemble the profile YAML aim_id field
                  and to compute the custom profile ID for AIM_PROFILE_ID.
                type: string
              customProfile:
                description: |-
                  CustomProfile defines inline custom profile data for the inference engine.
                  When set, the controller assembles a profile YAML from this data and template metadata,
                  creates a ConfigMap, and mounts it into discovery and inference containers.
                  Requires aimId, modelId, hardware, metric, and precision to also be set.
                properties:
                  engineArgs:
                    description: |-
                      EngineArgs contains inference engine arguments as a free-form JSON object.
                      These are passed as CLI arguments to the inference engine (e.g., vLLM).
                      Do not include "model" — it is injected separately by the runtime.
                    x-kubernetes-preserve-unknown-fields: true
                  envVars:
                    additionalProperties:
                      type: string
                    description: |-
                      EnvVars contains environment variables applied to the inference engine process.
                      These are written into the profile YAML and applied by the AIM runtime via os.execv,
                      distinct from container-level Env which targets the AIM runtime container itself.
                      Keys must match ^[A-Z0-9_]+$ (uppercase with underscores).
                    type: object
                type: object
              env:
                description: |-
                  Env specifies environment variables for inference containers.
                  These variables are passed to the inference runtime and can be used
                  to configure runtime behavior, authentication, or other settings.
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: |-
                        Name of the environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        fileKeyRef:
                          description: |-
                            FileKeyRef selects a key of the env file.
                            Requires the EnvFiles feature gate to be enabled.
                          properties:
                            key:
                              description: |-
                                The key within the env file. An invalid key will prevent the pod from starting.
                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                              type: string
                            optional:
                              default: false
                              description: |-
                                Specify whether the file or its key must be defined. If the file or key
                                does not exist, then the env var is not published.
                                If optional is set to true and the specified key does not exist,
                                the environment variable will not be set in the Pod's containers.

                                If optional is set to false and the specified key does not exist,
                                an error will be returned during Pod creation.
                              type: boolean
                            path:
                              description: |-
                                The path within the volume from which to select the file.
                                Must be relative and may not contain the '..' path or start with '..'.
                              type: string
                            volumeName:
                              description: The name of the volume mount containing
                                the env file.
                              type: string
                          required:
                          - key
                          - path
                          - volumeName
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              hardware:
                description: |-
                  Hardware specifies GPU and CPU requirements for each replica.
                  For GPU models, defines the GPU count and model types required for deployment.
                  For CPU-only models, defines CPU resource requirements.
                  This field is immutable after creation.
                properties:
                  cpu:
                    description: CPU specifies CPU requirements.
                    properties:
                      limits:
                        anyOf:
                        - type: integer
                        - type: string
                        description: Limits is the maximum number of CPU cores to
                          allow.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      requests:
                        anyOf:
                        - type: integer
                        - type: string
                        description: Requests is the number of CPU cores to request.
                          Required and must be > 0.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    required:
                    - requests
                    type: object
                  gpu:
                    description: GPU specifies GPU requirements. If not set, no GPUs
                      are requested (CPU-only model).
                    properties:
                      minVram:
                        anyOf:
                        - type: integer
                        - type: string
                        description: |-
                          MinVRAM limits deployment to GPUs having at least this much VRAM.
                          Used for capacity planning when the model size is known but any GPU with
                          sufficient VRAM is acceptable.
                          Cannot be combined with model.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      model:
                        description: |-
                          Model limits deployment to a specific GPU model.
                          Example: "MI300X"
                          Cannot be combined with minVram.
                        maxLength: 64
                        type: string
                      requests:
                        description: |-
                          Requests is the number of GPUs to set as requests/limits.
                          Set to 0 to target GPU nodes without consuming GPU resources (useful for testing).
                        format: int32
                        minimum: 0
                        type: integer
                      resourceName:
                        default: amd.com/gpu
                        description: |-
                          ResourceName is the Kubernetes resource name for GPU resources.
                          Defaults to "amd.com/gpu" if not specified.
                        type: string
                    type: object
                    x-kubernetes-validations:
                    - message: model and minVram are mutually exclusive; specify either
                        a specific GPU model OR a minimum VRAM requirement, not both
                      rule: '!(has(self.model) && size(self.model) > 0 && has(self.minVram))'
                type: object
                x-kubernetes-validations:
                - message: hardware is immutable
                  rule: self == oldSelf
                - message: at least one of gpu or cpu must be specified
                  rule: has(self.gpu) || has(self.cpu)
              imagePullSecrets:
                description: |-
                  ImagePullSecrets lists secrets containing credentials for pulling container images.
                  These secrets are used for:
                  - Discovery dry-run jobs that inspect the model container
                  - Pulling the image for inference services
                  The secrets are merged with any model or runtime config defaults.
                  For namespace-scoped templates, secrets must exist in the same namespace.
                  For cluster-scoped templates, secrets must exist in the operator namespace.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              metric:
                allOf:
                - enum:
                  - latency
                  - throughput
                - enum:
                  - latency
                  - throughput
                description: |-
                  Metric selects the optimization goal.

                  - `latency`: prioritize low end‑to‑end latency
                  - `throughput`: prioritize sustained requests/second
                type: string
                x-kubernetes-validations:
                - message: metric is immutable
                  rule: self == oldSelf
              modelId:
                description: |-
                  ModelId is the specific model identifier / HuggingFace URI (e.g., "Qwen/Qwen3-32B-FP8").
                  Required when customProfile is set; used for profile YAML model_id field
                  and for weight pre-caching via the discovery job.
                type: string
              modelName:
                description: |-
                  ModelName is the model name. Matches `metadata.name` of an AIMModel or AIMClusterModel. Immutable.

                  Example: `meta/llama-3-8b:1.1+20240915`
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: model name is immutable
                  rule: self == oldSelf
              modelSources:
                description: |-
                  ModelSources specifies the model sources required to run this template.
                  When provided, the discovery dry-run will be skipped and these sources will be used directly.
                  This allows users to explicitly declare model dependencies without requiring a discovery job.
                  If omitted, a discovery job will be run to automatically determine the required model sources.
                items:
                  description: |-
                    AIMModelSource describes a model artifact that must be downloaded for inference.
                    Discovery extracts these from the container's configuration to enable caching and validation.
                  properties:
                    env:
                      description: |-
                        Env specifies per-source credential overrides.
                        These variables are used for authentication when downloading this specific source.
                        Takes precedence over base-level env for the same variable name.
                      items:
                        description: EnvVar represents an environment variable present
                          in a Container.
                        properties:
                          name:
                            description: |-
                              Name of the environment variable.
                              May consist of any printable ASCII characters except '='.
                            type: string
                          value:
                            description: |-
                              Variable references $(VAR_NAME) are expanded
                              using the previously defined environment variables in the container and
                              any service environment variables. If a variable cannot be resolved,
                              the reference in the input string will be unchanged. Double $$ are reduced
                              to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                              "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                              Escaped references will never be expanded, regardless of whether the variable
                              exists or not.
                              Defaults to "".
                            type: string
                          valueFrom:
                            description: Source for the environment variable's value.
                              Cannot be used if value is not empty.
                            properties:
                              configMapKeyRef:
                                description: Selects a key of a ConfigMap.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the ConfigMap or
                                      its key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              fieldRef:
                                description: |-
                                  Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                  spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                properties:
                                  apiVersion:
                                    description: Version of the schema the FieldPath
                                      is written in terms of, defaults to "v1".
                                    type: string
                                  fieldPath:
                                    description: Path of the field to select in the
                                      specified API version.
                                    type: string
                                required:
                                - fieldPath
                                type: object
                                x-kubernetes-map-type: atomic
                              fileKeyRef:
                                description: |-
                                  FileKeyRef selects a key of the env file.
                                  Requires the EnvFiles feature gate to be enabled.
                                properties:
                                  key:
                                    description: |-
                                      The key within the env file. An invalid key will prevent the pod from starting.
                                      The keys defined within a source may consist of any printable ASCII characters except '='.
                                      During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                    type: string
                                  optional:
                                    default: false
                                    description: |-
                                      Specify whether the file or its key must be defined. If the file or key
                                      does not exist, then the env var is not published.
                                      If optional is set to true and the specified key does not exist,
                                      the environment variable will not be set in the Pod's containers.

                                      If optional is set to false and the specified key does not exist,
                                      an error will be returned during Pod creation.
                                    type: boolean
                                  path:
                                    description: |-
                                      The path within the volume from which to select the file.
                                      Must be relative and may not contain the '..' path or start with '..'.
                                    type: string
                                  volumeName:
                                    description: The name of the volume mount containing
                                      the env file.
                                    type: string
                                required:
                                - key
                                - path
                                - volumeName
                                type: object
                                x-kubernetes-map-type: atomic
                              resourceFieldRef:
                                description: |-
                                  Selects a resource of the container: only resources limits and requests
                                  (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                properties:
                                  containerName:
                                    description: 'Container name: required for volumes,
                                      optional for env vars'
                                    type: string
                                  divisor:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: Specifies the output format of the
                                      exposed resources, defaults to "1"
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  resource:
                                    description: 'Required: resource to select'
                                    type: string
                                required:
                                - resource
                                type: object
                                x-kubernetes-map-type: atomic
                              secretKeyRef:
                                description: Selects a key of a secret in the pod's
                                  namespace
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                        required:
                        - name
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - name
                      x-kubernetes-list-type: map
                    modelId:
                      description: |-
                        ModelID is the canonical identifier in {org}/{name} format.
                        Determines the cache mount path: /workspace/cache/{modelId}
                        For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                        For S3 sources, users define their own organizational structure.
                      pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                      type: string
                    precision:
                      allOf:
                      - enum:
                        - auto
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                      - enum:
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                      description: |-
                        Precision describes the runtime precision this source is compatible with.
                        Used to match model sources to profiles during custom weight onboarding.
                      type: string
                    size:
                      anyOf:
                      - type: integer
                      - type: string
                      description: |-
                        Size is the expected storage space required for this model artifact.
                        Used for PVC sizing and capacity planning during cache creation.
                        Optional - if not specified, the download job will discover the size automatically.
                        Can be set explicitly to pre-allocate storage or override auto-discovery.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    sourceUri:
                      description: |-
                        SourceURI is the location from which the model should be downloaded.
                        Supported schemes:
                        - hf://org/model - Hugging Face Hub model
                        - s3://bucket/key - S3-compatible storage
                      pattern: ^(hf|s3)://[^ \t\r\n]+$
                      type: string
                  required:
                  - modelId
                  - sourceUri
                  type: object
                type: array
              precision:
                allOf:
                - enum:
                  - auto
                  - fp4
                  - fp8
                  - fp16
                  - fp32
                  - bf16
                  - int4
                  - int8
                - enum:
                  - auto
                  - fp4
                  - fp8
                  - fp16
                  - fp32
                  - bf16
                  - int4
                  - int8
                description: Precision selects the numeric precision used by the runtime.
                type: string
                x-kubernetes-validations:
                - message: precision is immutable
                  rule: self == oldSelf
              profileId:
                description: |-
                  ProfileId is the specific AIM profile ID that this template should use.
                  When set, the discovery job will be instructed to use this specific profile.
                type: string
              resources:
                description: |-
                  Resources defines the default container resource requirements applied to services derived from this template.
                  Service-specific values override the template defaults.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              runtimeConfigName:
                description: |-
                  Name is the name of the runtime config to use for this resource. If a runtime config with this name exists both
                  as a namespace and a cluster runtime config, the values are merged together, the namespace config taking priority
                  over the cluster config when there are conflicts. If this field is empty or set to `default`, the namespace / cluster
                  runtime config with the name `default` is used, if it exists.
                type: string
              serviceAccountName:
                description: |-
                  ServiceAccountName specifies the Kubernetes service account to use for workloads related to this template.
                  This includes discovery dry-run jobs and inference services created from this template.
                  If empty, the default service account for the namespace is used.
                type: string
              type:
                allOf:
                - enum:
                  - optimized
                  - general
                  - preview
                  - unoptimized
                - enum:
                  - optimized
                  - general
                  - preview
                  - unoptimized
                description: |-
                  Type indicates the optimization level of this template.
                  - optimized: Template has been tuned for performance
                  - preview: Template is experimental/pre-release
                  - general: General-purpose tuning between optimized and preview
                  - unoptimized: Default, no specific optimizations applied
                  When nil, the type is determined by discovery. When set, overrides discovery.
                type: string
            required:
            - modelName
            type: object
            x-kubernetes-validations:
            - message: when customProfile is set, aimId, modelId, hardware, metric,
                and precision are required
              rule: '!has(self.customProfile) || (has(self.aimId) && has(self.modelId)
                && has(self.hardware) && has(self.metric) && has(self.precision))'
          status:
            description: AIMServiceTemplateStatus defines the observed state of AIMServiceTemplate.
            properties:
              conditions:
                description: Conditions represent the latest observations of template
                  state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              discovery:
                description: |-
                  Discovery contains state tracking for the discovery process, including
                  retry attempts and backoff timing for the circuit breaker pattern.
                properties:
                  attempts:
                    description: |-
                      Attempts is the number of discovery job attempts that have been made.
                      This counter increments each time a new discovery job is created after a failure.
                    format: int32
                    type: integer
                  identityCheckHash:
                    description: |-
                      IdentityCheckHash records the hash from the most recent identity
                      rediscovery attempt for a Ready template. When the current hash matches
                      this value, the controller will not invalidate a Ready template just to
                      retry identity (aimId/modelId) extraction. Bumping the operator-internal
                      hash version forces a one-shot revisit across all eligible templates
                      without operator intervention.
                    type: string
                  lastAttemptTime:
                    description: |-
                      LastAttemptTime is the timestamp of the most recent discovery job creation.
                      Used to calculate exponential backoff before the next retry.
                    format: date-time
                    type: string
                  lastFailureReason:
                    description: |-
                      LastFailureReason captures the reason for the most recent discovery failure.
                      Used to classify failures as terminal vs transient.
                    type: string
                  lastIdentityCheckTime:
                    description: |-
                      LastIdentityCheckTime is the timestamp of the most recent identity
                      rediscovery attempt initiation. Acts as a hard floor against rediscovery
                      thrashing in the presence of bugs that prevent IdentityCheckHash from
                      being recorded.
                    format: date-time
                    type: string
                  specHash:
                    description: |-
                      SpecHash is a hash of the template spec fields that affect discovery.
                      When the spec changes, the circuit breaker resets to allow fresh attempts.
                    type: string
                type: object
              discoveryJob:
                description: DiscoveryJob is a reference to the job that was run for
                  discovery
                properties:
                  kind:
                    description: Kind is the fully-qualified kind of the resolved
                      reference, when known.
                    type: string
                  name:
                    description: Name is the resource name that satisfied the reference.
                    type: string
                  namespace:
                    description: |-
                      Namespace identifies where the resource was found when namespace-scoped.
                      Empty indicates a cluster-scoped resource.
                    type: string
                  scope:
                    description: Scope indicates whether the resolved resource was
                      namespace or cluster scoped.
                    enum:
                    - Namespace
                    - Cluster
                    - Merged
                    - Unknown
                    type: string
                  uid:
                    description: UID captures the unique identifier of the resolved
                      reference, when known.
                    type: string
                type: object
              hardwareSummary:
                description: |-
                  HardwareSummary is a human-readable display string for the hardware requirements.
                  Format: "{count} x {model}" for GPU (e.g., "2 x MI300X") or "CPU" for CPU-only.
                  This is a computed field for display purposes only.
                type: string
              modelSources:
                description: |-
                  ModelSources list the models that this template requires to run. These are the models that will be
                  cached, if this template is cached.
                items:
                  description: |-
                    AIMModelSource describes a model artifact that must be downloaded for inference.
                    Discovery extracts these from the container's configuration to enable caching and validation.
                  properties:
                    env:
                      description: |-
                        Env specifies per-source credential overrides.
                        These variables are used for authentication when downloading this specific source.
                        Takes precedence over base-level env for the same variable name.
                      items:
                        description: EnvVar represents an environment variable present
                          in a Container.
                        properties:
                          name:
                            description: |-
                              Name of the environment variable.
                              May consist of any printable ASCII characters except '='.
                            type: string
                          value:
                            description: |-
                              Variable references $(VAR_NAME) are expanded
                              using the previously defined environment variables in the container and
                              any service environment variables. If a variable cannot be resolved,
                              the reference in the input string will be unchanged. Double $$ are reduced
                              to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                              "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                              Escaped references will never be expanded, regardless of whether the variable
                              exists or not.
                              Defaults to "".
                            type: string
                          valueFrom:
                            description: Source for the environment variable's value.
                              Cannot be used if value is not empty.
                            properties:
                              configMapKeyRef:
                                description: Selects a key of a ConfigMap.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the ConfigMap or
                                      its key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              fieldRef:
                                description: |-
                                  Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                  spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                properties:
                                  apiVersion:
                                    description: Version of the schema the FieldPath
                                      is written in terms of, defaults to "v1".
                                    type: string
                                  fieldPath:
                                    description: Path of the field to select in the
                                      specified API version.
                                    type: string
                                required:
                                - fieldPath
                                type: object
                                x-kubernetes-map-type: atomic
                              fileKeyRef:
                                description: |-
                                  FileKeyRef selects a key of the env file.
                                  Requires the EnvFiles feature gate to be enabled.
                                properties:
                                  key:
                                    description: |-
                                      The key within the env file. An invalid key will prevent the pod from starting.
                                      The keys defined within a source may consist of any printable ASCII characters except '='.
                                      During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                    type: string
                                  optional:
                                    default: false
                                    description: |-
                                      Specify whether the file or its key must be defined. If the file or key
                                      does not exist, then the env var is not published.
                                      If optional is set to true and the specified key does not exist,
                                      the environment variable will not be set in the Pod's containers.

                                      If optional is set to false and the specified key does not exist,
                                      an error will be returned during Pod creation.
                                    type: boolean
                                  path:
                                    description: |-
                                      The path within the volume from which to select the file.
                                      Must be relative and may not contain the '..' path or start with '..'.
                                    type: string
                                  volumeName:
                                    description: The name of the volume mount containing
                                      the env file.
                                    type: string
                                required:
                                - key
                                - path
                                - volumeName
                                type: object
                                x-kubernetes-map-type: atomic
                              resourceFieldRef:
                                description: |-
                                  Selects a resource of the container: only resources limits and requests
                                  (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                properties:
                                  containerName:
                                    description: 'Container name: required for volumes,
                                      optional for env vars'
                                    type: string
                                  divisor:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: Specifies the output format of the
                                      exposed resources, defaults to "1"
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  resource:
                                    description: 'Required: resource to select'
                                    type: string
                                required:
                                - resource
                                type: object
                                x-kubernetes-map-type: atomic
                              secretKeyRef:
                                description: Selects a key of a secret in the pod's
                                  namespace
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                        required:
                        - name
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - name
                      x-kubernetes-list-type: map
                    modelId:
                      description: |-
                        ModelID is the canonical identifier in {org}/{name} format.
                        Determines the cache mount path: /workspace/cache/{modelId}
                        For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                        For S3 sources, users define their own organizational structure.
                      pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                      type: string
                    precision:
                      allOf:
                      - enum:
                        - auto
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                      - enum:
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                      description: |-
                        Precision describes the runtime precision this source is compatible with.
                        Used to match model sources to profiles during custom weight onboarding.
                      type: string
                    size:
                      anyOf:
                      - type: integer
                      - type: string
                      description: |-
                        Size is the expected storage space required for this model artifact.
                        Used for PVC sizing and capacity planning during cache creation.
                        Optional - if not specified, the download job will discover the size automatically.
                        Can be set explicitly to pre-allocate storage or override auto-discovery.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    sourceUri:
                      description: |-
                        SourceURI is the location from which the model should be downloaded.
                        Supported schemes:
                        - hf://org/model - Hugging Face Hub model
                        - s3://bucket/key - S3-compatible storage
                      pattern: ^(hf|s3)://[^ \t\r\n]+$
                      type: string
                  required:
                  - modelId
                  - sourceUri
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              profile:
                description: |-
                  Profile contains the full discovery result profile as a free-form JSON object.
                  This includes metadata, engine args, environment variables, and model details.
                properties:
                  engine_args:
                    description: |-
                      EngineArgs contains runtime-specific engine configuration as a free-form JSON object.
                      The structure depends on the inference engine being used (e.g., vLLM, TGI).
                      These arguments are passed to the runtime container to configure model loading and inference.
                    x-kubernetes-preserve-unknown-fields: true
                  env_vars:
                    additionalProperties:
                      type: string
                    description: |-
                      EnvVars contains environment variables required by the runtime for this profile.
                      These may include engine-specific settings, optimization flags, or hardware configuration.
                    type: object
                  metadata:
                    description: Metadata provides structured information about this
                      deployment profile's characteristics.
                    properties:
                      aimId:
                        description: |-
                          AimID is the model-family identifier from the profile YAML's top-level `aim_id`.
                          Used for cross-scope fine-tune template matching (AIMModel.spec.aimId -> template.spec.aimId).
                        type: string
                      engine:
                        description: Engine identifies the inference engine used for
                          this profile (e.g., "vllm", "tgi").
                        type: string
                      gpu:
                        description: GPU specifies the GPU model this profile is optimized
                          for (e.g., "MI300X", "MI325X").
                        type: string
                      gpuCount:
                        description: GPUCount indicates how many GPUs are required
                          per replica for this profile.
                        format: int32
                        type: integer
                      metric:
                        description: Metric indicates the optimization goal for this
                          profile ("latency" or "throughput").
                        enum:
                        - latency
                        - throughput
                        type: string
                      modelId:
                        description: ModelID is the per-profile model variant identifier
                          from the profile YAML's top-level `model_id`.
                        type: string
                      precision:
                        description: Precision specifies the numeric precision used
                          in this profile (e.g., "fp16", "fp8").
                        enum:
                        - auto
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                        type: string
                      type:
                        description: Type indicates the optimization level of this
                          profile (optimized, preview, unoptimized).
                        enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                        type: string
                    type: object
                  originalDiscoveryOutput:
                    description: |-
                      OriginalDiscoveryOutput contains the raw discovery job JSON output.
                      This preserves the complete discovery result from the dry-run container,
                      including all fields that may not be mapped to structured fields above.
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              resolvedCache:
                description: ResolvedCache captures metadata about which cache is
                  used for this template
                properties:
                  kind:
                    description: Kind is the fully-qualified kind of the resolved
                      reference, when known.
                    type: string
                  name:
                    description: Name is the resource name that satisfied the reference.
                    type: string
                  namespace:
                    description: |-
                      Namespace identifies where the resource was found when namespace-scoped.
                      Empty indicates a cluster-scoped resource.
                    type: string
                  scope:
                    description: Scope indicates whether the resolved resource was
                      namespace or cluster scoped.
                    enum:
                    - Namespace
                    - Cluster
                    - Merged
                    - Unknown
                    type: string
                  uid:
                    description: UID captures the unique identifier of the resolved
                      reference, when known.
                    type: string
                type: object
              resolvedHardware:
                description: |-
                  ResolvedHardware contains the resolved hardware requirements for this template.
                  These values are computed from discovery results and spec defaults, and represent
                  what will actually be used when creating InferenceServices.
                  Resolution order: discovery output > spec values > defaults.
                properties:
                  cpu:
                    description: CPU specifies CPU requirements.
                    properties:
                      limits:
                        anyOf:
                        - type: integer
                        - type: string
                        description: Limits is the maximum number of CPU cores to
                          allow.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      requests:
                        anyOf:
                        - type: integer
                        - type: string
                        description: Requests is the number of CPU cores to request.
                          Required and must be > 0.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    required:
                    - requests
                    type: object
                  gpu:
                    description: GPU specifies GPU requirements. If not set, no GPUs
                      are requested (CPU-only model).
                    properties:
                      minVram:
                        anyOf:
                        - type: integer
                        - type: string
                        description: |-
                          MinVRAM limits deployment to GPUs having at least this much VRAM.
                          Used for capacity planning when the model size is known but any GPU with
                          sufficient VRAM is acceptable.
                          Cannot be combined with model.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      model:
                        description: |-
                          Model limits deployment to a specific GPU model.
                          Example: "MI300X"
                          Cannot be combined with minVram.
                        maxLength: 64
                        type: string
                      requests:
                        description: |-
                          Requests is the number of GPUs to set as requests/limits.
                          Set to 0 to target GPU nodes without consuming GPU resources (useful for testing).
                        format: int32
                        minimum: 0
                        type: integer
                      resourceName:
                        default: amd.com/gpu
                        description: |-
                          ResourceName is the Kubernetes resource name for GPU resources.
                          Defaults to "amd.com/gpu" if not specified.
                        type: string
                    type: object
                    x-kubernetes-validations:
                    - message: model and minVram are mutually exclusive; specify either
                        a specific GPU model OR a minimum VRAM requirement, not both
                      rule: '!(has(self.model) && size(self.model) > 0 && has(self.minVram))'
                type: object
                x-kubernetes-validations:
                - message: at least one of gpu or cpu must be specified
                  rule: has(self.gpu) || has(self.cpu)
              resolvedModel:
                description: ResolvedModel captures metadata about the image that
                  was resolved.
                properties:
                  kind:
                    description: Kind is the fully-qualified kind of the resolved
                      reference, when known.
                    type: string
                  name:
                    description: Name is the resource name that satisfied the reference.
                    type: string
                  namespace:
                    description: |-
                      Namespace identifies where the resource was found when namespace-scoped.
                      Empty indicates a cluster-scoped resource.
                    type: string
                  scope:
                    description: Scope indicates whether the resolved resource was
                      namespace or cluster scoped.
                    enum:
                    - Namespace
                    - Cluster
                    - Merged
                    - Unknown
                    type: string
                  uid:
                    description: UID captures the unique identifier of the resolved
                      reference, when known.
                    type: string
                type: object
              resolvedNodeAffinity:
                description: |-
                  ResolvedNodeAffinity contains the computed node affinity rules for GPU scheduling.
                  This is derived from GPU model and minVRAM requirements, merged with any user-specified
                  affinity from the spec. The service controller uses this directly when creating InferenceServices.
                properties:
                  preferredDuringSchedulingIgnoredDuringExecution:
                    description: |-
                      The scheduler will prefer to schedule pods to nodes that satisfy
                      the affinity expressions specified by this field, but it may choose
                      a node that violates one or more of the expressions. The node that is
                      most preferred is the one with the greatest sum of weights, i.e.
                      for each node that meets all of the scheduling requirements (resource
                      request, requiredDuringScheduling affinity expressions, etc.),
                      compute a sum by iterating through the elements of this field and adding
                      "weight" to the sum if the node matches the corresponding matchExpressions; the
                      node(s) with the highest sum are the most preferred.
                    items:
                      description: |-
                        An empty preferred scheduling term matches all objects with implicit weight 0
                        (i.e. it's a no-op). A null preferred scheduling term matches no objects (i.e. is also a no-op).
                      properties:
                        preference:
                          description: A node selector term, associated with the corresponding
                            weight.
                          properties:
                            matchExpressions:
                              description: A list of node selector requirements by
                                node's labels.
                              items:
                                description: |-
                                  A node selector requirement is a selector that contains values, a key, and an operator
                                  that relates the key and values.
                                properties:
                                  key:
                                    description: The label key that the selector applies
                                      to.
                                    type: string
                                  operator:
                                    description: |-
                                      Represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                    type: string
                                  values:
                                    description: |-
                                      An array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. If the operator is Gt or Lt, the values
                                      array must have a single element, which will be interpreted as an integer.
                                      This array is replaced during a strategic merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchFields:
                              description: A list of node selector requirements by
                                node's fields.
                              items:
                                description: |-
                                  A node selector requirement is a selector that contains values, a key, and an operator
                                  that relates the key and values.
                                properties:
                                  key:
                                    description: The label key that the selector applies
                                      to.
                                    type: string
                                  operator:
                                    description: |-
                                      Represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                    type: string
                                  values:
                                    description: |-
                                      An array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. If the operator is Gt or Lt, the values
                                      array must have a single element, which will be interpreted as an integer.
                                      This array is replaced during a strategic merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                          type: object
                          x-kubernetes-map-type: atomic
                        weight:
                          description: Weight associated with matching the corresponding
                            nodeSelectorTerm, in the range 1-100.
                          format: int32
                          type: integer
                      required:
                      - preference
                      - weight
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  requiredDuringSchedulingIgnoredDuringExecution:
                    description: |-
                      If the affinity requirements specified by this field are not met at
                      scheduling time, the pod will not be scheduled onto the node.
                      If the affinity requirements specified by this field cease to be met
                      at some point during pod execution (e.g. due to an update), the system
                      may or may not try to eventually evict the pod from its node.
                    properties:
                      nodeSelectorTerms:
                        description: Required. A list of node selector terms. The
                          terms are ORed.
                        items:
                          description: |-
                            A null or empty node selector term matches no objects. The requirements of
                            them are ANDed.
                            The TopologySelectorTerm type implements a subset of the NodeSelectorTerm.
                          properties:
                            matchExpressions:
                              description: A list of node selector requirements by
                                node's labels.
                              items:
                                description: |-
                                  A node selector requirement is a selector that contains values, a key, and an operator
                                  that relates the key and values.
                                properties:
                                  key:
                                    description: The label key that the selector applies
                                      to.
                                    type: string
                                  operator:
                                    description: |-
                                      Represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                    type: string
                                  values:
                                    description: |-
                                      An array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. If the operator is Gt or Lt, the values
                                      array must have a single element, which will be interpreted as an integer.
                                      This array is replaced during a strategic merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchFields:
                              description: A list of node selector requirements by
                                node's fields.
                              items:
                                description: |-
                                  A node selector requirement is a selector that contains values, a key, and an operator
                                  that relates the key and values.
                                properties:
                                  key:
                                    description: The label key that the selector applies
                                      to.
                                    type: string
                                  operator:
                                    description: |-
                                      Represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                    type: string
                                  values:
                                    description: |-
                                      An array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. If the operator is Gt or Lt, the values
                                      array must have a single element, which will be interpreted as an integer.
                                      This array is replaced during a strategic merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                          type: object
                          x-kubernetes-map-type: atomic
                        type: array
                        x-kubernetes-list-type: atomic
                    required:
                    - nodeSelectorTerms
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
              resolvedRuntimeConfig:
                description: ResolvedRuntimeConfig captures metadata about the runtime
                  config that was resolved.
                properties:
                  kind:
                    description: Kind is the fully-qualified kind of the resolved
                      reference, when known.
                    type: string
                  name:
                    description: Name is the resource name that satisfied the reference.
                    type: string
                  namespace:
                    description: |-
                      Namespace identifies where the resource was found when namespace-scoped.
                      Empty indicates a cluster-scoped resource.
                    type: string
                  scope:
                    description: Scope indicates whether the resolved resource was
                      namespace or cluster scoped.
                    enum:
                    - Namespace
                    - Cluster
                    - Merged
                    - Unknown
                    type: string
                  uid:
                    description: UID captures the unique identifier of the resolved
                      reference, when known.
                    type: string
                type: object
              status:
                default: Pending
                description: |-
                  Status represents the current high‑level status of the template lifecycle.
                  Values: `Pending`, `Progressing`, `Ready`, `Degraded`, `Failed`.
                enum:
                - Pending
                - Progressing
                - Ready
                - Degraded
                - Failed
                - NotAvailable
                type: string
              version:
                description: |-
                  Version is the AIM version extracted from the owning model's image tag
                  (e.g., "0.8.5" from "aim-base:0.8.5"). Set during discovery.
                  Used for version-based template matching with fine-tuned models.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: aimmodels.aim.eai.amd.com
spec:
  group: aim.eai.amd.com
  names:
    categories:
    - aim
    - all
    kind: AIMModel
    listKind: AIMModelList
    plural: aimmodels
    shortNames:
    - aimmdl
    singular: aimmodel
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.sourceType
      name: Source
      type: string
    - jsonPath: .spec.image
      name: Image
      type: string
    - jsonPath: .status.version
      name: Version
      type: string
    - jsonPath: .status.imageMetadata.model.canonicalName
      name: Model
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    deprecated: true
    deprecationWarning: v1alpha1 AIMModel is deprecated; use v1alpha2 AIMModel
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AIMModel is the Schema for namespace-scoped AIM model catalog
          entries.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              AIMModelSpec defines the desired state of AIMModel.

              Per-version constraints (v1alpha1 forbids derivedFrom and profiles;
              v1alpha2 forbids profileCopy/custom/customTemplates/top-level modelSources,
              requires image XOR profiles, and forbids profiles mixed with legacy scalar
              fields) live on the version-specific AIMModel / AIMClusterModel root types
              in the per-version *_types.go files.
            properties:
              aimId:
                description: |-
                  AimId is the AIM product family identifier (e.g., "qwen/qwen3-32b").
                  When set together with modelSources, enables aimId-based template matching:
                  the controller finds official templates by aimId, filters by versionPolicy,
                  matches by modelId, and creates copies with the custom weight source.
                type: string
              custom:
                description: |-
                  Custom contains configuration for custom models (models with inline modelSources).
                  Only used when modelSources are specified; ignored for image-based models.
                properties:
                  hardware:
                    description: |-
                      Hardware specifies default hardware requirements for all templates.
                      Individual templates can override these defaults.
                      Required when modelSources is set and customTemplates is empty (unless aimId is set).
                    properties:
                      cpu:
                        description: CPU specifies CPU requirements.
                        properties:
                          limits:
                            anyOf:
                            - type: integer
                            - type: string
                            description: Limits is the maximum number of CPU cores
                              to allow.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          requests:
                            anyOf:
                            - type: integer
                            - type: string
                            description: Requests is the number of CPU cores to request.
                              Required and must be > 0.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                        required:
                        - requests
                        type: object
                      gpu:
                        description: GPU specifies GPU requirements. If not set, no
                          GPUs are requested (CPU-only model).
                        properties:
                          minVram:
                            anyOf:
                            - type: integer
                            - type: string
                            description: |-
                              MinVRAM limits deployment to GPUs having at least this much VRAM.
                              Used for capacity planning when the model size is known but any GPU with
                              sufficient VRAM is acceptable.
                              Cannot be combined with model.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          model:
                            description: |-
                              Model limits deployment to a specific GPU model.
                              Example: "MI300X"
                              Cannot be combined with minVram.
                            maxLength: 64
                            type: string
                          requests:
                            description: |-
                              Requests is the number of GPUs to set as requests/limits.
                              Set to 0 to target GPU nodes without consuming GPU resources (useful for testing).
                            format: int32
                            minimum: 0
                            type: integer
                          resourceName:
                            default: amd.com/gpu
                            description: |-
                              ResourceName is the Kubernetes resource name for GPU resources.
                              Defaults to "amd.com/gpu" if not specified.
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: model and minVram are mutually exclusive; specify
                            either a specific GPU model OR a minimum VRAM requirement,
                            not both
                          rule: '!(has(self.model) && size(self.model) > 0 && has(self.minVram))'
                    type: object
                    x-kubernetes-validations:
                    - message: at least one of gpu or cpu must be specified
                      rule: has(self.gpu) || has(self.cpu)
                  type:
                    allOf:
                    - enum:
                      - optimized
                      - general
                      - preview
                      - unoptimized
                    - enum:
                      - optimized
                      - general
                      - preview
                      - unoptimized
                    description: |-
                      Type specifies default type for all templates.
                      Individual templates can override this default.
                      When nil, templates default to "unoptimized".
                    type: string
                  versionPolicy:
                    default: pinned
                    description: |-
                      VersionPolicy controls how template versions are filtered during aimId-based matching.
                      - pinned (default): match templates whose status.version equals the model's image tag
                      - latest: match only templates at the newest available status.version
                      - all: match templates at any version
                      - any: deprecated alias of all, kept for backward compatibility
                      Only used when spec.aimId is set.
                    enum:
                    - pinned
                    - latest
                    - any
                    - all
                    type: string
                type: object
              customTemplates:
                description: |-
                  CustomTemplates defines explicit template configurations for this model.
                  These templates are created directly without running a discovery job.
                  Can be used with or without modelSources to define custom deployment configurations.
                  If omitted when modelSources is set, a single template is auto-generated
                  using the custom.hardware requirements.
                items:
                  description: |-
                    AIMCustomTemplate defines a custom template configuration for a model.
                    When modelSources are specified directly on AIMModel, customTemplates allow
                    defining explicit hardware requirements and profiles, skipping the discovery job.
                    This is an existing struct (not a CRD); it appears as an element of AIMModel.spec.customTemplates[].
                  properties:
                    aimId:
                      description: |-
                        AimId is the AIM product family identifier (e.g., "meta-llama/Llama-3-8B").
                        Required when customProfile is set.
                      type: string
                    customProfile:
                      description: |-
                        CustomProfile defines inline custom profile data for the inference engine.
                        When set, the resulting template will have a custom profile ConfigMap mounted.
                        Requires aimId, modelId, hardware, profile.metric, and profile.precision.
                      properties:
                        engineArgs:
                          description: |-
                            EngineArgs contains inference engine arguments as a free-form JSON object.
                            These are passed as CLI arguments to the inference engine (e.g., vLLM).
                            Do not include "model" — it is injected separately by the runtime.
                          x-kubernetes-preserve-unknown-fields: true
                        envVars:
                          additionalProperties:
                            type: string
                          description: |-
                            EnvVars contains environment variables applied to the inference engine process.
                            These are written into the profile YAML and applied by the AIM runtime via os.execv,
                            distinct from container-level Env which targets the AIM runtime container itself.
                            Keys must match ^[A-Z0-9_]+$ (uppercase with underscores).
                          type: object
                      type: object
                    env:
                      description: |-
                        Env specifies environment variable overrides when this template is selected.
                        These are container-level env vars applied to the AIM runtime container.
                      items:
                        description: EnvVar represents an environment variable present
                          in a Container.
                        properties:
                          name:
                            description: |-
                              Name of the environment variable.
                              May consist of any printable ASCII characters except '='.
                            type: string
                          value:
                            description: |-
                              Variable references $(VAR_NAME) are expanded
                              using the previously defined environment variables in the container and
                              any service environment variables. If a variable cannot be resolved,
                              the reference in the input string will be unchanged. Double $$ are reduced
                              to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                              "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                              Escaped references will never be expanded, regardless of whether the variable
                              exists or not.
                              Defaults to "".
                            type: string
                          valueFrom:
                            description: Source for the environment variable's value.
                              Cannot be used if value is not empty.
                            properties:
                              configMapKeyRef:
                                description: Selects a key of a ConfigMap.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the ConfigMap or
                                      its key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              fieldRef:
                                description: |-
                                  Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                  spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                properties:
                                  apiVersion:
                                    description: Version of the schema the FieldPath
                                      is written in terms of, defaults to "v1".
                                    type: string
                                  fieldPath:
                                    description: Path of the field to select in the
                                      specified API version.
                                    type: string
                                required:
                                - fieldPath
                                type: object
                                x-kubernetes-map-type: atomic
                              fileKeyRef:
                                description: |-
                                  FileKeyRef selects a key of the env file.
                                  Requires the EnvFiles feature gate to be enabled.
                                properties:
                                  key:
                                    description: |-
                                      The key within the env file. An invalid key will prevent the pod from starting.
                                      The keys defined within a source may consist of any printable ASCII characters except '='.
                                      During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                    type: string
                                  optional:
                                    default: false
                                    description: |-
                                      Specify whether the file or its key must be defined. If the file or key
                                      does not exist, then the env var is not published.
                                      If optional is set to true and the specified key does not exist,
                                      the environment variable will not be set in the Pod's containers.

                                      If optional is set to false and the specified key does not exist,
                                      an error will be returned during Pod creation.
                                    type: boolean
                                  path:
                                    description: |-
                                      The path within the volume from which to select the file.
                                      Must be relative and may not contain the '..' path or start with '..'.
                                    type: string
                                  volumeName:
                                    description: The name of the volume mount containing
                                      the env file.
                                    type: string
                                required:
                                - key
                                - path
                                - volumeName
                                type: object
                                x-kubernetes-map-type: atomic
                              resourceFieldRef:
                                description: |-
                                  Selects a resource of the container: only resources limits and requests
                                  (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                properties:
                                  containerName:
                                    description: 'Container name: required for volumes,
                                      optional for env vars'
                                    type: string
                                  divisor:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: Specifies the output format of the
                                      exposed resources, defaults to "1"
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  resource:
                                    description: 'Required: resource to select'
                                    type: string
                                required:
                                - resource
                                type: object
                                x-kubernetes-map-type: atomic
                              secretKeyRef:
                                description: Selects a key of a secret in the pod's
                                  namespace
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                        required:
                        - name
                        type: object
                      maxItems: 64
                      type: array
                      x-kubernetes-list-map-keys:
                      - name
                      x-kubernetes-list-type: map
                    hardware:
                      description: |-
                        Hardware specifies GPU and CPU requirements for this template.
                        Optional when spec.hardware is set (inherits from spec).
                        When both are set, values are merged field-by-field with template taking precedence.
                      properties:
                        cpu:
                          description: CPU specifies CPU requirements.
                          properties:
                            limits:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Limits is the maximum number of CPU cores
                                to allow.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            requests:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Requests is the number of CPU cores to
                                request. Required and must be > 0.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                          required:
                          - requests
                          type: object
                        gpu:
                          description: GPU specifies GPU requirements. If not set,
                            no GPUs are requested (CPU-only model).
                          properties:
                            minVram:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                MinVRAM limits deployment to GPUs having at least this much VRAM.
                                Used for capacity planning when the model size is known but any GPU with
                                sufficient VRAM is acceptable.
                                Cannot be combined with model.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            model:
                              description: |-
                                Model limits deployment to a specific GPU model.
                                Example: "MI300X"
                                Cannot be combined with minVram.
                              maxLength: 64
                              type: string
                            requests:
                              description: |-
                                Requests is the number of GPUs to set as requests/limits.
                                Set to 0 to target GPU nodes without consuming GPU resources (useful for testing).
                              format: int32
                              minimum: 0
                              type: integer
                            resourceName:
                              default: amd.com/gpu
                              description: |-
                                ResourceName is the Kubernetes resource name for GPU resources.
                                Defaults to "amd.com/gpu" if not specified.
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: model and minVram are mutually exclusive; specify
                              either a specific GPU model OR a minimum VRAM requirement,
                              not both
                            rule: '!(has(self.model) && size(self.model) > 0 && has(self.minVram))'
                      type: object
                      x-kubernetes-validations:
                      - message: at least one of gpu or cpu must be specified
                        rule: has(self.gpu) || has(self.cpu)
                    modelId:
                      description: |-
                        ModelId is the specific model identifier / HuggingFace URI (e.g., "Qwen/Qwen3-32B-FP8").
                        Required when customProfile is set.
                      type: string
                    name:
                      description: Name is the template name. If not provided, auto-generated
                        from model name + profile.
                      maxLength: 63
                      type: string
                    profile:
                      description: |-
                        Profile declares runtime profile variables for template selection.
                        Used when multiple templates exist to select based on metric/precision.
                      properties:
                        metric:
                          allOf:
                          - enum:
                            - latency
                            - throughput
                          - enum:
                            - latency
                            - throughput
                          description: Metric specifies the optimization target (e.g.,
                            latency, throughput).
                          type: string
                        precision:
                          allOf:
                          - enum:
                            - auto
                            - fp4
                            - fp8
                            - fp16
                            - fp32
                            - bf16
                            - int4
                            - int8
                          - enum:
                            - auto
                            - fp4
                            - fp8
                            - fp16
                            - fp32
                            - bf16
                            - int4
                            - int8
                          description: Precision specifies the numerical precision
                            (e.g., fp8, fp16, bf16).
                          type: string
                      type: object
                    type:
                      allOf:
                      - enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                      - enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                      default: unoptimized
                      description: |-
                        Type indicates the optimization status of this template.
                        - optimized: Template has been tuned for performance
                        - general: General-purpose tuning between optimized and preview
                        - preview: Template is experimental/pre-release
                        - unoptimized: Default, no specific optimizations applied
                      type: string
                  type: object
                  x-kubernetes-validations:
                  - message: when customProfile is set, aimId, modelId, hardware,
                      profile.metric, and profile.precision are required
                    rule: '!has(self.customProfile) || (has(self.aimId) && has(self.modelId)
                      && has(self.hardware) && has(self.profile) && has(self.profile.metric)
                      && has(self.profile.precision))'
                maxItems: 16
                type: array
              defaultServiceTemplate:
                description: |-
                  DefaultServiceTemplate specifies the default AIMServiceTemplate to use when creating services for this model.
                  When set, services that reference this model will use this template if no template is explicitly specified.
                  If this is not set, a template will be automatically selected.
                type: string
              derivedFrom:
                description: |-
                  DerivedFrom is the legacy flat shape of v1alpha2's profile-derivation
                  onboarding surface. New objects must use spec.profiles instead; the
                  field is retained so existing v1alpha2 objects (and the v1alpha2
                  reconciler reading them) round-trip cleanly.

                  DEPRECATED: prefer spec.profiles.derivedFrom on v1alpha2.
                properties:
                  image:
                    description: Image overrides the runtime image used by the derived
                      profiles.
                    type: string
                  imagePullSecrets:
                    description: ImagePullSecrets lists secrets used for inspecting
                      and pulling container images.
                    items:
                      description: |-
                        LocalObjectReference contains enough information to let you locate the
                        referenced object inside the same namespace.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    type: array
                  overrides:
                    description: Overrides mutates the copied profile spec after selection
                      and version filtering.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount replaces the copied profile's
                          acceleratorCount.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel replaces the copied profile's
                          acceleratorModel.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode replaces the copied profile's
                          acceleratorPartitioningMode. Complete replacement, not a merge; an empty
                          override string leaves the source profile's mode untouched. Same reserved
                          values as AIMProfileSpecCommon.AcceleratorPartitioningMode. Whenever this
                          override is set (to any value, including "unpartitioned"), the CEL rule on the
                          enclosing spec also requires AcceleratorCount to be set: partition mode
                          changes the per-unit interpretation of acceleratorCount, and CEL cannot
                          read the base profile to tell whether the meaning actually changed, so it
                          conservatively requires the count be restated.
                        type: string
                      aimId:
                        description: |-
                          AimId stamps the derived profile's spec.aimId. When set, wins over
                          the source profile's aimId. REQUIRED when the enclosing selector has
                          role=base (the source base profile carries no aimId of its own).
                        type: string
                      containerEnv:
                        description: ContainerEnv merges by env var name, overriding
                          matching source entries.
                        items:
                          description: EnvVar represents an environment variable present
                            in a Container.
                          properties:
                            name:
                              description: |-
                                Name of the environment variable.
                                May consist of any printable ASCII characters except '='.
                              type: string
                            value:
                              description: |-
                                Variable references $(VAR_NAME) are expanded
                                using the previously defined environment variables in the container and
                                any service environment variables. If a variable cannot be resolved,
                                the reference in the input string will be unchanged. Double $$ are reduced
                                to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                Escaped references will never be expanded, regardless of whether the variable
                                exists or not.
                                Defaults to "".
                              type: string
                            valueFrom:
                              description: Source for the environment variable's value.
                                Cannot be used if value is not empty.
                              properties:
                                configMapKeyRef:
                                  description: Selects a key of a ConfigMap.
                                  properties:
                                    key:
                                      description: The key to select.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the ConfigMap or
                                        its key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fieldRef:
                                  description: |-
                                    Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                    spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                  properties:
                                    apiVersion:
                                      description: Version of the schema the FieldPath
                                        is written in terms of, defaults to "v1".
                                      type: string
                                    fieldPath:
                                      description: Path of the field to select in
                                        the specified API version.
                                      type: string
                                  required:
                                  - fieldPath
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fileKeyRef:
                                  description: |-
                                    FileKeyRef selects a key of the env file.
                                    Requires the EnvFiles feature gate to be enabled.
                                  properties:
                                    key:
                                      description: |-
                                        The key within the env file. An invalid key will prevent the pod from starting.
                                        The keys defined within a source may consist of any printable ASCII characters except '='.
                                        During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                      type: string
                                    optional:
                                      default: false
                                      description: |-
                                        Specify whether the file or its key must be defined. If the file or key
                                        does not exist, then the env var is not published.
                                        If optional is set to true and the specified key does not exist,
                                        the environment variable will not be set in the Pod's containers.

                                        If optional is set to false and the specified key does not exist,
                                        an error will be returned during Pod creation.
                                      type: boolean
                                    path:
                                      description: |-
                                        The path within the volume from which to select the file.
                                        Must be relative and may not contain the '..' path or start with '..'.
                                      type: string
                                    volumeName:
                                      description: The name of the volume mount containing
                                        the env file.
                                      type: string
                                  required:
                                  - key
                                  - path
                                  - volumeName
                                  type: object
                                  x-kubernetes-map-type: atomic
                                resourceFieldRef:
                                  description: |-
                                    Selects a resource of the container: only resources limits and requests
                                    (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                  properties:
                                    containerName:
                                      description: 'Container name: required for volumes,
                                        optional for env vars'
                                      type: string
                                    divisor:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      description: Specifies the output format of
                                        the exposed resources, defaults to "1"
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    resource:
                                      description: 'Required: resource to select'
                                      type: string
                                  required:
                                  - resource
                                  type: object
                                  x-kubernetes-map-type: atomic
                                secretKeyRef:
                                  description: Selects a key of a secret in the pod's
                                    namespace
                                  properties:
                                    key:
                                      description: The key of the secret to select
                                        from.  Must be a valid secret key.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the Secret or its
                                        key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      engineArgs:
                        description: EngineArgs shallow-merges on top of the source
                          engineArgs, overriding matching keys.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      engineEnv:
                        additionalProperties:
                          type: string
                        description: EngineEnv merges by key, overriding matching
                          source entries.
                        type: object
                      image:
                        description: |-
                          Image overrides the runtime container image used by the derived
                          profiles. When empty the deployment image is rebased onto the source
                          profile's base-image (status.baseImage) so private mirrors stay
                          self-contained.
                        type: string
                      modelId:
                        description: |-
                          ModelId stamps the derived profile's spec.modelId. When set, wins
                          over both the source profile's modelId AND the auto-derivation from
                          modelSources[0].modelId. REQUIRED when the enclosing selector has
                          role=base.
                        type: string
                      modelSources:
                        description: |-
                          ModelSources replaces the copied profile's modelSources. When
                          modelSources[0].modelId is set and overrides.modelId is unset, the
                          derived profile's modelId is auto-derived from modelSources[0]; an
                          explicit overrides.modelId always wins.
                        items:
                          description: |-
                            AIMModelSource describes a model artifact that must be downloaded for inference.
                            Discovery extracts these from the container's configuration to enable caching and validation.
                          properties:
                            env:
                              description: |-
                                Env specifies per-source credential overrides.
                                These variables are used for authentication when downloading this specific source.
                                Takes precedence over base-level env for the same variable name.
                              items:
                                description: EnvVar represents an environment variable
                                  present in a Container.
                                properties:
                                  name:
                                    description: |-
                                      Name of the environment variable.
                                      May consist of any printable ASCII characters except '='.
                                    type: string
                                  value:
                                    description: |-
                                      Variable references $(VAR_NAME) are expanded
                                      using the previously defined environment variables in the container and
                                      any service environment variables. If a variable cannot be resolved,
                                      the reference in the input string will be unchanged. Double $$ are reduced
                                      to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                      "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                      Escaped references will never be expanded, regardless of whether the variable
                                      exists or not.
                                      Defaults to "".
                                    type: string
                                  valueFrom:
                                    description: Source for the environment variable's
                                      value. Cannot be used if value is not empty.
                                    properties:
                                      configMapKeyRef:
                                        description: Selects a key of a ConfigMap.
                                        properties:
                                          key:
                                            description: The key to select.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the ConfigMap
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fieldRef:
                                        description: |-
                                          Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                          spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                        properties:
                                          apiVersion:
                                            description: Version of the schema the
                                              FieldPath is written in terms of, defaults
                                              to "v1".
                                            type: string
                                          fieldPath:
                                            description: Path of the field to select
                                              in the specified API version.
                                            type: string
                                        required:
                                        - fieldPath
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fileKeyRef:
                                        description: |-
                                          FileKeyRef selects a key of the env file.
                                          Requires the EnvFiles feature gate to be enabled.
                                        properties:
                                          key:
                                            description: |-
                                              The key within the env file. An invalid key will prevent the pod from starting.
                                              The keys defined within a source may consist of any printable ASCII characters except '='.
                                              During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                            type: string
                                          optional:
                                            default: false
                                            description: |-
                                              Specify whether the file or its key must be defined. If the file or key
                                              does not exist, then the env var is not published.
                                              If optional is set to true and the specified key does not exist,
                                              the environment variable will not be set in the Pod's containers.

                                              If optional is set to false and the specified key does not exist,
                                              an error will be returned during Pod creation.
                                            type: boolean
                                          path:
                                            description: |-
                                              The path within the volume from which to select the file.
                                              Must be relative and may not contain the '..' path or start with '..'.
                                            type: string
                                          volumeName:
                                            description: The name of the volume mount
                                              containing the env file.
                                            type: string
                                        required:
                                        - key
                                        - path
                                        - volumeName
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      resourceFieldRef:
                                        description: |-
                                          Selects a resource of the container: only resources limits and requests
                                          (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                        properties:
                                          containerName:
                                            description: 'Container name: required
                                              for volumes, optional for env vars'
                                            type: string
                                          divisor:
                                            anyOf:
                                            - type: integer
                                            - type: string
                                            description: Specifies the output format
                                              of the exposed resources, defaults to
                                              "1"
                                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                            x-kubernetes-int-or-string: true
                                          resource:
                                            description: 'Required: resource to select'
                                            type: string
                                        required:
                                        - resource
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      secretKeyRef:
                                        description: Selects a key of a secret in
                                          the pod's namespace
                                        properties:
                                          key:
                                            description: The key of the secret to
                                              select from.  Must be a valid secret
                                              key.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the Secret
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                    type: object
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            modelId:
                              description: |-
                                ModelID is the canonical identifier in {org}/{name} format.
                                Determines the cache mount path: /workspace/cache/{modelId}
                                For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                                For S3 sources, users define their own organizational structure.
                              pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                              type: string
                            precision:
                              allOf:
                              - enum:
                                - auto
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              - enum:
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              description: |-
                                Precision describes the runtime precision this source is compatible with.
                                Used to match model sources to profiles during custom weight onboarding.
                              type: string
                            size:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                Size is the expected storage space required for this model artifact.
                                Used for PVC sizing and capacity planning during cache creation.
                                Optional - if not specified, the download job will discover the size automatically.
                                Can be set explicitly to pre-allocate storage or override auto-discovery.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            sourceUri:
                              description: |-
                                SourceURI is the location from which the model should be downloaded.
                                Supported schemes:
                                - hf://org/model - Hugging Face Hub model
                                - s3://bucket/key - S3-compatible storage
                              pattern: ^(hf|s3)://[^ \t\r\n]+$
                              type: string
                          required:
                          - modelId
                          - sourceUri
                          type: object
                        type: array
                      profileId:
                        description: |-
                          ProfileId stamps the derived profile's spec.profileId. Optional —
                          most callers leave this empty and let the source profile's profileId
                          carry through (or the reconciler synthesise one).
                        type: string
                    type: object
                  selector:
                    description: Selector chooses which source profiles to derive
                      from.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount filters by accelerator unit
                          count.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel filters by accelerator identifier.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode filters candidates by their declared
                          partitioning mode. Partial-order match (NOT strict equality):
                            ""              - no filter on this field.
                            "unpartitioned" - matches profiles with mode "" or "unpartitioned".
                            "partitioned"   - matches profiles whose mode is non-trivial (anything
                                              other than "" / "unpartitioned").
                            "<C>"           - selector-only convenience: matches profiles with mode
                                              "<C>-*" (prefix on the scheme). Not a valid profile-spec
                                              value (e.g. selector "CPX" matches "CPX-NPS1", "CPX-NPS4").
                            "<C>-<M>"       - exact-string match on the scheme.
                        type: string
                      acceleratorType:
                        description: AcceleratorType filters by accelerator resource
                          type.
                        enum:
                        - gpu
                        - cpu
                        type: string
                      aimId:
                        description: AimId filters by model architecture identifier
                          (e.g., "qwen/qwen3-32b").
                        type: string
                      engine:
                        description: Engine filters by inference engine.
                        type: string
                      engineArgs:
                        description: |-
                          EngineArgs partially matches source engineArgs: every provided top-level key must
                          exist in the source object with an equal value.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      metric:
                        description: Metric filters by optimization target.
                        enum:
                        - latency
                        - throughput
                        type: string
                      minimumType:
                        description: |-
                          MinimumType filters by a minimum optimization level: candidates whose
                          type is this tier OR BETTER are accepted (hierarchy: optimized > general
                          > preview > unoptimized). This is the floor counterpart to the exact-match
                          Type field; the two AND together when both are set.

                          The sentinel "any" disables the floor (accept every tier). When this
                          field is empty the AIMService resolver applies a default floor of
                          "optimized" so auto-selection prefers production-grade profiles and never
                          silently picks an unoptimized one; to opt a service into lower tiers
                          (e.g. CPU/EPYC profiles published as unoptimized) set minimumType
                          explicitly to "unoptimized" or "any". Derivation selectors
                          (AIMProfileSet / AIMModel.profiles) treat empty as "any" so copying is
                          never tier-restricted by default.
                        enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                        - any
                        type: string
                      modelId:
                        description: ModelId filters by the source profile's specific
                          model identifier.
                        type: string
                      modelRef:
                        description: |-
                          ModelRef narrows candidates to those produced by a specific
                          AIM(Cluster)Model, matched via the `aim.eai.amd.com/source-model[-scope]`
                          labels stamped by the AIMModel reconcilers. Iteration 1 (v1alpha2 only).
                        properties:
                          name:
                            description: Name is the owning AIM(Cluster)Model name.
                              Required.
                            minLength: 1
                            type: string
                          scope:
                            default: Auto
                            description: Scope controls how Name is resolved against
                              AIMModel vs AIMClusterModel.
                            enum:
                            - Auto
                            - Namespace
                            - Cluster
                            type: string
                        required:
                        - name
                        type: object
                      origin:
                        description: |-
                          Origin filters by the `aim.eai.amd.com/profile-origin` label. When unset
                          (empty) the selector does not filter by origin. Iteration 1 (v1alpha2
                          only).
                        enum:
                        - discovered
                        - derived
                        - user-authored
                        type: string
                      precision:
                        description: Precision filters by numeric precision.
                        enum:
                        - auto
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                        type: string
                      profileId:
                        description: ProfileId filters by the source profile's profile
                          identifier.
                        type: string
                      role:
                        default: deployable
                        description: |-
                          Role filters by the `aim.eai.amd.com/profile-role` label. Defaults to
                          `deployable`. `base` filters to base profiles emitted by base-image
                          discovery (custom-model derivation source material).
                        enum:
                        - base
                        - deployable
                        type: string
                      type:
                        description: Type filters by optimization level (exact match).
                        enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                        type: string
                    type: object
                  serviceAccountName:
                    description: ServiceAccountName is propagated to managed profiles
                      for downstream workloads.
                    type: string
                  sourceRef:
                    description: |-
                      SourceRef points to an alternate discovery cache source.
                      When omitted, derivation uses visible AIMProfile and AIMClusterProfile objects.
                      For AIMProfileSet, the referenced ConfigMap is read from the same namespace.
                      For AIMClusterProfileSet, it is read from the operator namespace.
                    properties:
                      name:
                        description: Name is the discovery cache ConfigMap name.
                        type: string
                    required:
                    - name
                    type: object
                  version:
                    description: Version pins matching to a specific source profile
                      version when VersionPolicy is pinned.
                    type: string
                  versionPolicy:
                    default: pinned
                    description: VersionPolicy controls how matching profiles are
                      filtered by version.
                    enum:
                    - pinned
                    - latest
                    - all
                    type: string
                type: object
                x-kubernetes-validations:
                - message: selector must set at least one matching field, or set sourceRef
                  rule: has(self.sourceRef) || has(self.selector.aimId) || has(self.selector.modelId)
                    || has(self.selector.profileId) || has(self.selector.engine) ||
                    has(self.selector.metric) || has(self.selector.precision) || has(self.selector.type)
                    || has(self.selector.acceleratorModel) || has(self.selector.acceleratorType)
                    || has(self.selector.acceleratorCount) || has(self.selector.engineArgs)
                    || has(self.selector.modelRef) || has(self.selector.origin)
                - message: version is required when versionPolicy is pinned
                  rule: has(self.version) || (has(self.versionPolicy) && self.versionPolicy
                    != 'pinned')
                - message: latest/all versionPolicy must not set version
                  rule: '!has(self.versionPolicy) || (self.versionPolicy != ''latest''
                    && self.versionPolicy != ''all'') || !has(self.version)'
                - message: selector.aimId/modelId/profileId are not allowed when selector.role=base;
                    set overrides.aimId/modelId/profileId instead (base profiles have
                    no source identity to filter on)
                  rule: '!(has(self.selector.role) && self.selector.role == ''base'')
                    || (!has(self.selector.aimId) && !has(self.selector.modelId) &&
                    !has(self.selector.profileId))'
                - message: selector.role=base requires overrides.aimId and overrides.modelId
                    (base profiles carry no identity of their own; overrides supply
                    it)
                  rule: '!(has(self.selector.role) && self.selector.role == ''base'')
                    || (has(self.overrides) && has(self.overrides.aimId) && has(self.overrides.modelId))'
                - message: overrides.acceleratorCount must be specified together with
                    any overrides.acceleratorPartitioningMode; partition mode changes
                    the per-unit interpretation of acceleratorCount
                  rule: '!has(self.overrides) || !has(self.overrides.acceleratorPartitioningMode)
                    || size(self.overrides.acceleratorPartitioningMode) == 0 || has(self.overrides.acceleratorCount)'
              discovery:
                description: |-
                  Discovery controls discovery behavior for this model.
                  When unset, uses runtime config defaults.
                properties:
                  createServiceTemplates:
                    default: true
                    description: CreateServiceTemplates controls whether (cluster)
                      service templates are auto-created from the image metadata.
                    type: boolean
                  extractMetadata:
                    default: true
                    description: |-
                      ExtractMetadata controls whether metadata extraction runs for this model.
                      During metadata extraction, the controller connects to the image registry and
                      extracts the image's labels.
                    type: boolean
                type: object
              env:
                description: |-
                  Env specifies environment variables for authentication during model discovery and metadata extraction.
                  These variables are used for authentication with model registries (e.g., HuggingFace tokens).
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: |-
                        Name of the environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        fileKeyRef:
                          description: |-
                            FileKeyRef selects a key of the env file.
                            Requires the EnvFiles feature gate to be enabled.
                          properties:
                            key:
                              description: |-
                                The key within the env file. An invalid key will prevent the pod from starting.
                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                              type: string
                            optional:
                              default: false
                              description: |-
                                Specify whether the file or its key must be defined. If the file or key
                                does not exist, then the env var is not published.
                                If optional is set to true and the specified key does not exist,
                                the environment variable will not be set in the Pod's containers.

                                If optional is set to false and the specified key does not exist,
                                an error will be returned during Pod creation.
                              type: boolean
                            path:
                              description: |-
                                The path within the volume from which to select the file.
                                Must be relative and may not contain the '..' path or start with '..'.
                              type: string
                            volumeName:
                              description: The name of the volume mount containing
                                the env file.
                              type: string
                          required:
                          - key
                          - path
                          - volumeName
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              image:
                description: |-
                  Image is the container image URI for this AIM model.
                  This image is inspected by the operator to select runtime profiles used by templates.
                  Discovery behavior is controlled by the discovery field and runtime config's AutoDiscovery setting.
                  Required unless aimId is set with versionPolicy latest or any, or profileCopy is set.
                type: string
              imageMetadata:
                description: |-
                  ImageMetadata is the metadata that is used to determine which recommended service templates to create,
                  and to drive clients with richer metadata regarding this particular model. For most cases the user does
                  not need to set this field manually, for images that have the supported labels embedded in them
                  the `AIM(Cluster)Model.status.imageMetadata` field is automatically filled from the container image labels.
                  This field is intended to be used when there are network restrictions, or in other similar situations.
                  If this field is set, the remote extraction will not be performed at all.
                properties:
                  baseImageRef:
                    description: |-
                      BaseImageRef is the value of the AIM_BASE_IMAGE_REF environment variable
                      baked into the image's OCI config at build time. For AIM model images this
                      records the base image (e.g. "ghcr.io/silogen/aim-base:0.8.5") that the
                      model image was built from. Used by the AIMModel controller to resolve
                      the deployment image for fine-tuned models whose spec.image is omitted
                      (versionPolicy=latest or any).
                    type: string
                  model:
                    description: Model contains AMD Silogen model-specific metadata.
                    properties:
                      canonicalName:
                        description: |-
                          CanonicalName is the canonical model identifier (e.g., mistralai/Mixtral-8x22B-Instruct-v0.1).
                          Extracted from: org.amd.silogen.model.canonicalName
                        type: string
                      descriptionFull:
                        description: |-
                          DescriptionFull is the full description.
                          Extracted from: org.amd.silogen.description.full
                        type: string
                      hfTokenRequired:
                        description: |-
                          HFTokenRequired indicates if a HuggingFace token is required.
                          Extracted from: org.amd.silogen.hfToken.required
                        type: boolean
                      recommendedDeployments:
                        description: |-
                          RecommendedDeployments contains recommended deployment configurations.
                          Extracted from: org.amd.silogen.model.recommendedDeployments (parsed from JSON array)
                        items:
                          description: RecommendedDeployment describes a recommended
                            deployment configuration for a model.
                          properties:
                            description:
                              description: Description provides additional context
                                about this deployment configuration
                              type: string
                            gpuCount:
                              description: GPUCount is the number of GPUs required
                              format: int32
                              type: integer
                            gpuModel:
                              description: |-
                                GPUModel is the GPU model name (e.g., MI300X, MI325X).
                                The legacy v1alpha1 schema only models GPU accelerators; CPU profiles
                                emitted via OCI labels (gpuModel="CPU") are not first-class here and
                                are handled exclusively by the v1alpha2 native discovery pipeline.
                              type: string
                            metric:
                              description: Metric is the optimization target (e.g.,
                                latency, throughput)
                              type: string
                            precision:
                              description: Precision is the recommended precision
                                (e.g., fp8, fp16, bf16)
                              type: string
                            profileId:
                              description: |-
                                ProfileId is the unique identifier of the AIM profile for this deployment.
                                When set, templates created from this deployment will use this profile ID
                                to deterministically select the correct runtime profile in the AIM container.
                              type: string
                          type: object
                        type: array
                      releaseNotes:
                        description: |-
                          ReleaseNotes contains release notes for this version.
                          Extracted from: org.amd.silogen.release.notes
                        type: string
                      source:
                        description: |-
                          Source is the URL where the model can be found.
                          Extracted from: org.amd.silogen.model.source
                        type: string
                      tags:
                        description: |-
                          Tags are descriptive tags (e.g., ["text-generation", "chat", "instruction"]).
                          Extracted from: org.amd.silogen.model.tags (comma-separated)
                        items:
                          type: string
                        type: array
                      title:
                        description: |-
                          Title is the Silogen-specific title for the model.
                          Extracted from: org.amd.silogen.title
                        type: string
                      variants:
                        description: |-
                          Variants lists model variants.
                          Extracted from: org.amd.silogen.model.variants (comma-separated)
                        items:
                          type: string
                        type: array
                      versions:
                        description: |-
                          Versions lists available versions.
                          Extracted from: org.amd.silogen.model.versions (comma-separated)
                        items:
                          type: string
                        type: array
                    type: object
                  oci:
                    description: OCI contains standard OCI image metadata.
                    properties:
                      authors:
                        description: |-
                          Authors is contact details of the authors.
                          Extracted from: org.opencontainers.image.authors
                        type: string
                      created:
                        description: |-
                          Created is the creation timestamp.
                          Extracted from: org.opencontainers.image.created
                        type: string
                      description:
                        description: |-
                          Description is a brief description.
                          Extracted from: org.opencontainers.image.description
                        type: string
                      documentation:
                        description: |-
                          Documentation is the URL to documentation.
                          Extracted from: org.opencontainers.image.documentation
                        type: string
                      licenses:
                        description: |-
                          Licenses is the SPDX license identifier(s).
                          Extracted from: org.opencontainers.image.licenses
                        type: string
                      revision:
                        description: |-
                          Revision is the source control revision.
                          Extracted from: org.opencontainers.image.revision
                        type: string
                      source:
                        description: |-
                          Source is the URL to the source code repository.
                          Extracted from: org.opencontainers.image.source
                        type: string
                      title:
                        description: |-
                          Title is the human-readable title.
                          Extracted from: org.opencontainers.image.title
                        type: string
                      vendor:
                        description: |-
                          Vendor is the organization that produced the image.
                          Extracted from: org.opencontainers.image.vendor
                        type: string
                      version:
                        description: |-
                          Version is the image version.
                          Extracted from: org.opencontainers.image.version
                        type: string
                    type: object
                  originalLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      OriginalLabels contains the raw OCI image labels as a JSON object.
                      This preserves all labels from the image, including those not mapped to structured fields.
                    type: object
                type: object
              imagePullSecrets:
                description: |-
                  ImagePullSecrets lists secrets containing credentials for pulling the model container image.
                  These secrets are used for:
                  - OCI registry metadata extraction during discovery
                  - Pulling the image for inference services
                  The secrets are merged with any runtime config defaults.
                  For namespace-scoped models, secrets must exist in the same namespace.
                  For cluster-scoped models, secrets must exist in the operator namespace.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              modelSources:
                description: |-
                  ModelSources specifies the model sources to use for this model.
                  When specified, these sources are used instead of auto-discovery from the container image.
                  This enables pre-creating custom models with explicit model sources.
                  The size field is optional - if not specified, it will be discovered by the download job.
                  AIM runtime currently supports only one model source.
                items:
                  description: |-
                    AIMModelSource describes a model artifact that must be downloaded for inference.
                    Discovery extracts these from the container's configuration to enable caching and validation.
                  properties:
                    env:
                      description: |-
                        Env specifies per-source credential overrides.
                        These variables are used for authentication when downloading this specific source.
                        Takes precedence over base-level env for the same variable name.
                      items:
                        description: EnvVar represents an environment variable present
                          in a Container.
                        properties:
                          name:
                            description: |-
                              Name of the environment variable.
                              May consist of any printable ASCII characters except '='.
                            type: string
                          value:
                            description: |-
                              Variable references $(VAR_NAME) are expanded
                              using the previously defined environment variables in the container and
                              any service environment variables. If a variable cannot be resolved,
                              the reference in the input string will be unchanged. Double $$ are reduced
                              to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                              "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                              Escaped references will never be expanded, regardless of whether the variable
                              exists or not.
                              Defaults to "".
                            type: string
                          valueFrom:
                            description: Source for the environment variable's value.
                              Cannot be used if value is not empty.
                            properties:
                              configMapKeyRef:
                                description: Selects a key of a ConfigMap.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the ConfigMap or
                                      its key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              fieldRef:
                                description: |-
                                  Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                  spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                properties:
                                  apiVersion:
                                    description: Version of the schema the FieldPath
                                      is written in terms of, defaults to "v1".
                                    type: string
                                  fieldPath:
                                    description: Path of the field to select in the
                                      specified API version.
                                    type: string
                                required:
                                - fieldPath
                                type: object
                                x-kubernetes-map-type: atomic
                              fileKeyRef:
                                description: |-
                                  FileKeyRef selects a key of the env file.
                                  Requires the EnvFiles feature gate to be enabled.
                                properties:
                                  key:
                                    description: |-
                                      The key within the env file. An invalid key will prevent the pod from starting.
                                      The keys defined within a source may consist of any printable ASCII characters except '='.
                                      During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                    type: string
                                  optional:
                                    default: false
                                    description: |-
                                      Specify whether the file or its key must be defined. If the file or key
                                      does not exist, then the env var is not published.
                                      If optional is set to true and the specified key does not exist,
                                      the environment variable will not be set in the Pod's containers.

                                      If optional is set to false and the specified key does not exist,
                                      an error will be returned during Pod creation.
                                    type: boolean
                                  path:
                                    description: |-
                                      The path within the volume from which to select the file.
                                      Must be relative and may not contain the '..' path or start with '..'.
                                    type: string
                                  volumeName:
                                    description: The name of the volume mount containing
                                      the env file.
                                    type: string
                                required:
                                - key
                                - path
                                - volumeName
                                type: object
                                x-kubernetes-map-type: atomic
                              resourceFieldRef:
                                description: |-
                                  Selects a resource of the container: only resources limits and requests
                                  (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                properties:
                                  containerName:
                                    description: 'Container name: required for volumes,
                                      optional for env vars'
                                    type: string
                                  divisor:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: Specifies the output format of the
                                      exposed resources, defaults to "1"
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  resource:
                                    description: 'Required: resource to select'
                                    type: string
                                required:
                                - resource
                                type: object
                                x-kubernetes-map-type: atomic
                              secretKeyRef:
                                description: Selects a key of a secret in the pod's
                                  namespace
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                        required:
                        - name
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - name
                      x-kubernetes-list-type: map
                    modelId:
                      description: |-
                        ModelID is the canonical identifier in {org}/{name} format.
                        Determines the cache mount path: /workspace/cache/{modelId}
                        For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                        For S3 sources, users define their own organizational structure.
                      pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                      type: string
                    precision:
                      allOf:
                      - enum:
                        - auto
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                      - enum:
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                      description: |-
                        Precision describes the runtime precision this source is compatible with.
                        Used to match model sources to profiles during custom weight onboarding.
                      type: string
                    size:
                      anyOf:
                      - type: integer
                      - type: string
                      description: |-
                        Size is the expected storage space required for this model artifact.
                        Used for PVC sizing and capacity planning during cache creation.
                        Optional - if not specified, the download job will discover the size automatically.
                        Can be set explicitly to pre-allocate storage or override auto-discovery.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    sourceUri:
                      description: |-
                        SourceURI is the location from which the model should be downloaded.
                        Supported schemes:
                        - hf://org/model - Hugging Face Hub model
                        - s3://bucket/key - S3-compatible storage
                      pattern: ^(hf|s3)://[^ \t\r\n]+$
                      type: string
                  required:
                  - modelId
                  - sourceUri
                  type: object
                maxItems: 1
                type: array
              profileCopy:
                description: |-
                  ProfileCopy reuses the AIMProfileSet derivation shape so an AIMModel can
                  publish derivative AIMProfiles directly. The controller may synthesize a
                  child AIMProfileSet and fill SourceRef when image-backed discovery is
                  involved. Mutually exclusive with all deprecated v1alpha1 fields.

                  DEPRECATED on v1alpha2: use spec.profiles. v1alpha1 still accepts
                  ProfileCopy. v1alpha2 CRD CEL forbids ProfileCopy.
                properties:
                  image:
                    description: Image overrides the runtime image used by the derived
                      profiles.
                    type: string
                  imagePullSecrets:
                    description: ImagePullSecrets lists secrets used for inspecting
                      and pulling container images.
                    items:
                      description: |-
                        LocalObjectReference contains enough information to let you locate the
                        referenced object inside the same namespace.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    type: array
                  overrides:
                    description: Overrides mutates the copied profile spec after selection
                      and version filtering.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount replaces the copied profile's
                          acceleratorCount.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel replaces the copied profile's
                          acceleratorModel.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode replaces the copied profile's
                          acceleratorPartitioningMode. Complete replacement, not a merge; an empty
                          override string leaves the source profile's mode untouched. Same reserved
                          values as AIMProfileSpecCommon.AcceleratorPartitioningMode. Whenever this
                          override is set (to any value, including "unpartitioned"), the CEL rule on the
                          enclosing spec also requires AcceleratorCount to be set: partition mode
                          changes the per-unit interpretation of acceleratorCount, and CEL cannot
                          read the base profile to tell whether the meaning actually changed, so it
                          conservatively requires the count be restated.
                        type: string
                      aimId:
                        description: |-
                          AimId stamps the derived profile's spec.aimId. When set, wins over
                          the source profile's aimId. REQUIRED when the enclosing selector has
                          role=base (the source base profile carries no aimId of its own).
                        type: string
                      containerEnv:
                        description: ContainerEnv merges by env var name, overriding
                          matching source entries.
                        items:
                          description: EnvVar represents an environment variable present
                            in a Container.
                          properties:
                            name:
                              description: |-
                                Name of the environment variable.
                                May consist of any printable ASCII characters except '='.
                              type: string
                            value:
                              description: |-
                                Variable references $(VAR_NAME) are expanded
                                using the previously defined environment variables in the container and
                                any service environment variables. If a variable cannot be resolved,
                                the reference in the input string will be unchanged. Double $$ are reduced
                                to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                Escaped references will never be expanded, regardless of whether the variable
                                exists or not.
                                Defaults to "".
                              type: string
                            valueFrom:
                              description: Source for the environment variable's value.
                                Cannot be used if value is not empty.
                              properties:
                                configMapKeyRef:
                                  description: Selects a key of a ConfigMap.
                                  properties:
                                    key:
                                      description: The key to select.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the ConfigMap or
                                        its key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fieldRef:
                                  description: |-
                                    Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                    spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                  properties:
                                    apiVersion:
                                      description: Version of the schema the FieldPath
                                        is written in terms of, defaults to "v1".
                                      type: string
                                    fieldPath:
                                      description: Path of the field to select in
                                        the specified API version.
                                      type: string
                                  required:
                                  - fieldPath
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fileKeyRef:
                                  description: |-
                                    FileKeyRef selects a key of the env file.
                                    Requires the EnvFiles feature gate to be enabled.
                                  properties:
                                    key:
                                      description: |-
                                        The key within the env file. An invalid key will prevent the pod from starting.
                                        The keys defined within a source may consist of any printable ASCII characters except '='.
                                        During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                      type: string
                                    optional:
                                      default: false
                                      description: |-
                                        Specify whether the file or its key must be defined. If the file or key
                                        does not exist, then the env var is not published.
                                        If optional is set to true and the specified key does not exist,
                                        the environment variable will not be set in the Pod's containers.

                                        If optional is set to false and the specified key does not exist,
                                        an error will be returned during Pod creation.
                                      type: boolean
                                    path:
                                      description: |-
                                        The path within the volume from which to select the file.
                                        Must be relative and may not contain the '..' path or start with '..'.
                                      type: string
                                    volumeName:
                                      description: The name of the volume mount containing
                                        the env file.
                                      type: string
                                  required:
                                  - key
                                  - path
                                  - volumeName
                                  type: object
                                  x-kubernetes-map-type: atomic
                                resourceFieldRef:
                                  description: |-
                                    Selects a resource of the container: only resources limits and requests
                                    (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                  properties:
                                    containerName:
                                      description: 'Container name: required for volumes,
                                        optional for env vars'
                                      type: string
                                    divisor:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      description: Specifies the output format of
                                        the exposed resources, defaults to "1"
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    resource:
                                      description: 'Required: resource to select'
                                      type: string
                                  required:
                                  - resource
                                  type: object
                                  x-kubernetes-map-type: atomic
                                secretKeyRef:
                                  description: Selects a key of a secret in the pod's
                                    namespace
                                  properties:
                                    key:
                                      description: The key of the secret to select
                                        from.  Must be a valid secret key.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the Secret or its
                                        key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      engineArgs:
                        description: EngineArgs shallow-merges on top of the source
                          engineArgs, overriding matching keys.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      engineEnv:
                        additionalProperties:
                          type: string
                        description: EngineEnv merges by key, overriding matching
                          source entries.
                        type: object
                      image:
                        description: |-
                          Image overrides the runtime container image used by the derived
                          profiles. When empty the deployment image is rebased onto the source
                          profile's base-image (status.baseImage) so private mirrors stay
                          self-contained.
                        type: string
                      modelId:
                        description: |-
                          ModelId stamps the derived profile's spec.modelId. When set, wins
                          over both the source profile's modelId AND the auto-derivation from
                          modelSources[0].modelId. REQUIRED when the enclosing selector has
                          role=base.
                        type: string
                      modelSources:
                        description: |-
                          ModelSources replaces the copied profile's modelSources. When
                          modelSources[0].modelId is set and overrides.modelId is unset, the
                          derived profile's modelId is auto-derived from modelSources[0]; an
                          explicit overrides.modelId always wins.
                        items:
                          description: |-
                            AIMModelSource describes a model artifact that must be downloaded for inference.
                            Discovery extracts these from the container's configuration to enable caching and validation.
                          properties:
                            env:
                              description: |-
                                Env specifies per-source credential overrides.
                                These variables are used for authentication when downloading this specific source.
                                Takes precedence over base-level env for the same variable name.
                              items:
                                description: EnvVar represents an environment variable
                                  present in a Container.
                                properties:
                                  name:
                                    description: |-
                                      Name of the environment variable.
                                      May consist of any printable ASCII characters except '='.
                                    type: string
                                  value:
                                    description: |-
                                      Variable references $(VAR_NAME) are expanded
                                      using the previously defined environment variables in the container and
                                      any service environment variables. If a variable cannot be resolved,
                                      the reference in the input string will be unchanged. Double $$ are reduced
                                      to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                      "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                      Escaped references will never be expanded, regardless of whether the variable
                                      exists or not.
                                      Defaults to "".
                                    type: string
                                  valueFrom:
                                    description: Source for the environment variable's
                                      value. Cannot be used if value is not empty.
                                    properties:
                                      configMapKeyRef:
                                        description: Selects a key of a ConfigMap.
                                        properties:
                                          key:
                                            description: The key to select.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the ConfigMap
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fieldRef:
                                        description: |-
                                          Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                          spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                        properties:
                                          apiVersion:
                                            description: Version of the schema the
                                              FieldPath is written in terms of, defaults
                                              to "v1".
                                            type: string
                                          fieldPath:
                                            description: Path of the field to select
                                              in the specified API version.
                                            type: string
                                        required:
                                        - fieldPath
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fileKeyRef:
                                        description: |-
                                          FileKeyRef selects a key of the env file.
                                          Requires the EnvFiles feature gate to be enabled.
                                        properties:
                                          key:
                                            description: |-
                                              The key within the env file. An invalid key will prevent the pod from starting.
                                              The keys defined within a source may consist of any printable ASCII characters except '='.
                                              During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                            type: string
                                          optional:
                                            default: false
                                            description: |-
                                              Specify whether the file or its key must be defined. If the file or key
                                              does not exist, then the env var is not published.
                                              If optional is set to true and the specified key does not exist,
                                              the environment variable will not be set in the Pod's containers.

                                              If optional is set to false and the specified key does not exist,
                                              an error will be returned during Pod creation.
                                            type: boolean
                                          path:
                                            description: |-
                                              The path within the volume from which to select the file.
                                              Must be relative and may not contain the '..' path or start with '..'.
                                            type: string
                                          volumeName:
                                            description: The name of the volume mount
                                              containing the env file.
                                            type: string
                                        required:
                                        - key
                                        - path
                                        - volumeName
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      resourceFieldRef:
                                        description: |-
                                          Selects a resource of the container: only resources limits and requests
                                          (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                        properties:
                                          containerName:
                                            description: 'Container name: required
                                              for volumes, optional for env vars'
                                            type: string
                                          divisor:
                                            anyOf:
                                            - type: integer
                                            - type: string
                                            description: Specifies the output format
                                              of the exposed resources, defaults to
                                              "1"
                                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                            x-kubernetes-int-or-string: true
                                          resource:
                                            description: 'Required: resource to select'
                                            type: string
                                        required:
                                        - resource
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      secretKeyRef:
                                        description: Selects a key of a secret in
                                          the pod's namespace
                                        properties:
                                          key:
                                            description: The key of the secret to
                                              select from.  Must be a valid secret
                                              key.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the Secret
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                    type: object
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            modelId:
                              description: |-
                                ModelID is the canonical identifier in {org}/{name} format.
                                Determines the cache mount path: /workspace/cache/{modelId}
                                For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                                For S3 sources, users define their own organizational structure.
                              pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                              type: string
                            precision:
                              allOf:
                              - enum:
                                - auto
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              - enum:
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              description: |-
                                Precision describes the runtime precision this source is compatible with.
                                Used to match model sources to profiles during custom weight onboarding.
                              type: string
                            size:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                Size is the expected storage space required for this model artifact.
                                Used for PVC sizing and capacity planning during cache creation.
                                Optional - if not specified, the download job will discover the size automatically.
                                Can be set explicitly to pre-allocate storage or override auto-discovery.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            sourceUri:
                              description: |-
                                SourceURI is the location from which the model should be downloaded.
                                Supported schemes:
                                - hf://org/model - Hugging Face Hub model
                                - s3://bucket/key - S3-compatible storage
                              pattern: ^(hf|s3)://[^ \t\r\n]+$
                              type: string
                          required:
                          - modelId
                          - sourceUri
                          type: object
                        type: array
                      profileId:
                        description: |-
                          ProfileId stamps the derived profile's spec.profileId. Optional —
                          most callers leave this empty and let the source profile's profileId
                          carry through (or the reconciler synthesise one).
                        type: string
                    type: object
                  selector:
                    description: Selector chooses which source profiles to derive
                      from.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount filters by accelerator unit
                          count.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel filters by accelerator identifier.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode filters candidates by their declared
                          partitioning mode. Partial-order match (NOT strict equality):
                            ""              - no filter on this field.
                            "unpartitioned" - matches profiles with mode "" or "unpartitioned".
                            "partitioned"   - matches profiles whose mode is non-trivial (anything
                                              other than "" / "unpartitioned").
                            "<C>"           - selector-only convenience: matches profiles with mode
                                              "<C>-*" (prefix on the scheme). Not a valid profile-spec
                                              value (e.g. selector "CPX" matches "CPX-NPS1", "CPX-NPS4").
                            "<C>-<M>"       - exact-string match on the scheme.
                        type: string
                      acceleratorType:
                        description: AcceleratorType filters by accelerator resource
                          type.
                        enum:
                        - gpu
                        - cpu
                        type: string
                      aimId:
                        description: AimId filters by model architecture identifier
                          (e.g., "qwen/qwen3-32b").
                        type: string
                      engine:
                        description: Engine filters by inference engine.
                        type: string
                      engineArgs:
                        description: |-
                          EngineArgs partially matches source engineArgs: every provided top-level key must
                          exist in the source object with an equal value.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      metric:
                        description: Metric filters by optimization target.
                        enum:
                        - latency
                        - throughput
                        type: string
                      minimumType:
                        description: |-
                          MinimumType filters by a minimum optimization level: candidates whose
                          type is this tier OR BETTER are accepted (hierarchy: optimized > general
                          > preview > unoptimized). This is the floor counterpart to the exact-match
                          Type field; the two AND together when both are set.

                          The sentinel "any" disables the floor (accept every tier). When this
                          field is empty the AIMService resolver applies a default floor of
                          "optimized" so auto-selection prefers production-grade profiles and never
                          silently picks an unoptimized one; to opt a service into lower tiers
                          (e.g. CPU/EPYC profiles published as unoptimized) set minimumType
                          explicitly to "unoptimized" or "any". Derivation selectors
                          (AIMProfileSet / AIMModel.profiles) treat empty as "any" so copying is
                          never tier-restricted by default.
                        enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                        - any
                        type: string
                      modelId:
                        description: ModelId filters by the source profile's specific
                          model identifier.
                        type: string
                      modelRef:
                        description: |-
                          ModelRef narrows candidates to those produced by a specific
                          AIM(Cluster)Model, matched via the `aim.eai.amd.com/source-model[-scope]`
                          labels stamped by the AIMModel reconcilers. Iteration 1 (v1alpha2 only).
                        properties:
                          name:
                            description: Name is the owning AIM(Cluster)Model name.
                              Required.
                            minLength: 1
                            type: string
                          scope:
                            default: Auto
                            description: Scope controls how Name is resolved against
                              AIMModel vs AIMClusterModel.
                            enum:
                            - Auto
                            - Namespace
                            - Cluster
                            type: string
                        required:
                        - name
                        type: object
                      origin:
                        description: |-
                          Origin filters by the `aim.eai.amd.com/profile-origin` label. When unset
                          (empty) the selector does not filter by origin. Iteration 1 (v1alpha2
                          only).
                        enum:
                        - discovered
                        - derived
                        - user-authored
                        type: string
                      precision:
                        description: Precision filters by numeric precision.
                        enum:
                        - auto
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                        type: string
                      profileId:
                        description: ProfileId filters by the source profile's profile
                          identifier.
                        type: string
                      role:
                        default: deployable
                        description: |-
                          Role filters by the `aim.eai.amd.com/profile-role` label. Defaults to
                          `deployable`. `base` filters to base profiles emitted by base-image
                          discovery (custom-model derivation source material).
                        enum:
                        - base
                        - deployable
                        type: string
                      type:
                        description: Type filters by optimization level (exact match).
                        enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                        type: string
                    type: object
                  serviceAccountName:
                    description: ServiceAccountName is propagated to managed profiles
                      for downstream workloads.
                    type: string
                  sourceRef:
                    description: |-
                      SourceRef points to an alternate discovery cache source.
                      When omitted, derivation uses visible AIMProfile and AIMClusterProfile objects.
                      For AIMProfileSet, the referenced ConfigMap is read from the same namespace.
                      For AIMClusterProfileSet, it is read from the operator namespace.
                    properties:
                      name:
                        description: Name is the discovery cache ConfigMap name.
                        type: string
                    required:
                    - name
                    type: object
                  version:
                    description: Version pins matching to a specific source profile
                      version when VersionPolicy is pinned.
                    type: string
                  versionPolicy:
                    default: pinned
                    description: VersionPolicy controls how matching profiles are
                      filtered by version.
                    enum:
                    - pinned
                    - latest
                    - all
                    type: string
                type: object
                x-kubernetes-validations:
                - message: selector must set at least one matching field, or set sourceRef
                  rule: has(self.sourceRef) || has(self.selector.aimId) || has(self.selector.modelId)
                    || has(self.selector.profileId) || has(self.selector.engine) ||
                    has(self.selector.metric) || has(self.selector.precision) || has(self.selector.type)
                    || has(self.selector.acceleratorModel) || has(self.selector.acceleratorType)
                    || has(self.selector.acceleratorCount) || has(self.selector.engineArgs)
                    || has(self.selector.modelRef) || has(self.selector.origin)
                - message: version is required when versionPolicy is pinned
                  rule: has(self.version) || (has(self.versionPolicy) && self.versionPolicy
                    != 'pinned')
                - message: latest/all versionPolicy must not set version
                  rule: '!has(self.versionPolicy) || (self.versionPolicy != ''latest''
                    && self.versionPolicy != ''all'') || !has(self.version)'
                - message: selector.aimId/modelId/profileId are not allowed when selector.role=base;
                    set overrides.aimId/modelId/profileId instead (base profiles have
                    no source identity to filter on)
                  rule: '!(has(self.selector.role) && self.selector.role == ''base'')
                    || (!has(self.selector.aimId) && !has(self.selector.modelId) &&
                    !has(self.selector.profileId))'
                - message: selector.role=base requires overrides.aimId and overrides.modelId
                    (base profiles carry no identity of their own; overrides supply
                    it)
                  rule: '!(has(self.selector.role) && self.selector.role == ''base'')
                    || (has(self.overrides) && has(self.overrides.aimId) && has(self.overrides.modelId))'
                - message: overrides.acceleratorCount must be specified together with
                    any overrides.acceleratorPartitioningMode; partition mode changes
                    the per-unit interpretation of acceleratorCount
                  rule: '!has(self.overrides) || !has(self.overrides.acceleratorPartitioningMode)
                    || size(self.overrides.acceleratorPartitioningMode) == 0 || has(self.overrides.acceleratorCount)'
              profiles:
                description: |-
                  Profiles is the v1alpha2 fine-tune / custom-model onboarding surface.
                  It groups the source descriptor (`profiles.derivedFrom.selector` /
                  `profiles.derivedFrom.sourceRef`), the version filter
                  (`profiles.versionPolicy` / `profiles.version`), and the modifications
                  applied to copies (`profiles.overrides`). When set, the AIMModel
                  reconciler synthesises a child AIMProfileSet from this block.

                  Mutually exclusive with spec.image (exactly one of the two is required
                  for v1alpha2 AIMModel). v1alpha1 rejects spec.profiles via per-version
                  CEL.
                properties:
                  derivedFrom:
                    description: DerivedFrom identifies the source profiles to copy
                      from.
                    properties:
                      selector:
                        description: |-
                          Selector chooses which source profiles to derive from. Discovery of a
                          deployable AIM image stamps role=deployable; selector.role=base targets
                          the base profiles emitted by base-image discovery (custom-model
                          derivation source material).
                        properties:
                          acceleratorCount:
                            description: AcceleratorCount filters by accelerator unit
                              count.
                            format: int32
                            type: integer
                          acceleratorModel:
                            description: AcceleratorModel filters by accelerator identifier.
                            type: string
                          acceleratorPartitioningMode:
                            description: |-
                              AcceleratorPartitioningMode filters candidates by their declared
                              partitioning mode. Partial-order match (NOT strict equality):
                                ""              - no filter on this field.
                                "unpartitioned" - matches profiles with mode "" or "unpartitioned".
                                "partitioned"   - matches profiles whose mode is non-trivial (anything
                                                  other than "" / "unpartitioned").
                                "<C>"           - selector-only convenience: matches profiles with mode
                                                  "<C>-*" (prefix on the scheme). Not a valid profile-spec
                                                  value (e.g. selector "CPX" matches "CPX-NPS1", "CPX-NPS4").
                                "<C>-<M>"       - exact-string match on the scheme.
                            type: string
                          acceleratorType:
                            description: AcceleratorType filters by accelerator resource
                              type.
                            enum:
                            - gpu
                            - cpu
                            type: string
                          aimId:
                            description: AimId filters by model architecture identifier
                              (e.g., "qwen/qwen3-32b").
                            type: string
                          engine:
                            description: Engine filters by inference engine.
                            type: string
                          engineArgs:
                            description: |-
                              EngineArgs partially matches source engineArgs: every provided top-level key must
                              exist in the source object with an equal value.
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          metric:
                            description: Metric filters by optimization target.
                            enum:
                            - latency
                            - throughput
                            type: string
                          minimumType:
                            description: |-
                              MinimumType filters by a minimum optimization level: candidates whose
                              type is this tier OR BETTER are accepted (hierarchy: optimized > general
                              > preview > unoptimized). This is the floor counterpart to the exact-match
                              Type field; the two AND together when both are set.

                              The sentinel "any" disables the floor (accept every tier). When this
                              field is empty the AIMService resolver applies a default floor of
                              "optimized" so auto-selection prefers production-grade profiles and never
                              silently picks an unoptimized one; to opt a service into lower tiers
                              (e.g. CPU/EPYC profiles published as unoptimized) set minimumType
                              explicitly to "unoptimized" or "any". Derivation selectors
                              (AIMProfileSet / AIMModel.profiles) treat empty as "any" so copying is
                              never tier-restricted by default.
                            enum:
                            - optimized
                            - general
                            - preview
                            - unoptimized
                            - any
                            type: string
                          modelId:
                            description: ModelId filters by the source profile's specific
                              model identifier.
                            type: string
                          modelRef:
                            description: |-
                              ModelRef narrows candidates to those produced by a specific
                              AIM(Cluster)Model, matched via the `aim.eai.amd.com/source-model[-scope]`
                              labels stamped by the AIMModel reconcilers. Iteration 1 (v1alpha2 only).
                            properties:
                              name:
                                description: Name is the owning AIM(Cluster)Model
                                  name. Required.
                                minLength: 1
                                type: string
                              scope:
                                default: Auto
                                description: Scope controls how Name is resolved against
                                  AIMModel vs AIMClusterModel.
                                enum:
                                - Auto
                                - Namespace
                                - Cluster
                                type: string
                            required:
                            - name
                            type: object
                          origin:
                            description: |-
                              Origin filters by the `aim.eai.amd.com/profile-origin` label. When unset
                              (empty) the selector does not filter by origin. Iteration 1 (v1alpha2
                              only).
                            enum:
                            - discovered
                            - derived
                            - user-authored
                            type: string
                          precision:
                            description: Precision filters by numeric precision.
                            enum:
                            - auto
                            - fp4
                            - fp8
                            - fp16
                            - fp32
                            - bf16
                            - int4
                            - int8
                            type: string
                          profileId:
                            description: ProfileId filters by the source profile's
                              profile identifier.
                            type: string
                          role:
                            default: deployable
                            description: |-
                              Role filters by the `aim.eai.amd.com/profile-role` label. Defaults to
                              `deployable`. `base` filters to base profiles emitted by base-image
                              discovery (custom-model derivation source material).
                            enum:
                            - base
                            - deployable
                            type: string
                          type:
                            description: Type filters by optimization level (exact
                              match).
                            enum:
                            - optimized
                            - general
                            - preview
                            - unoptimized
                            type: string
                        type: object
                      sourceRef:
                        description: |-
                          SourceRef points to an alternate discovery cache source (a
                          pre-populated ConfigMap of profile YAMLs) instead of using the
                          visible AIMProfile / AIMClusterProfile objects.
                        properties:
                          name:
                            description: Name is the discovery cache ConfigMap name.
                            type: string
                        required:
                        - name
                        type: object
                    type: object
                  overrides:
                    description: |-
                      Overrides mutates the copied profile spec after selection and version
                      filtering. Use overrides.image to override the deployment container
                      image used by the derived profiles.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount replaces the copied profile's
                          acceleratorCount.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel replaces the copied profile's
                          acceleratorModel.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode replaces the copied profile's
                          acceleratorPartitioningMode. Complete replacement, not a merge; an empty
                          override string leaves the source profile's mode untouched. Same reserved
                          values as AIMProfileSpecCommon.AcceleratorPartitioningMode. Whenever this
                          override is set (to any value, including "unpartitioned"), the CEL rule on the
                          enclosing spec also requires AcceleratorCount to be set: partition mode
                          changes the per-unit interpretation of acceleratorCount, and CEL cannot
                          read the base profile to tell whether the meaning actually changed, so it
                          conservatively requires the count be restated.
                        type: string
                      aimId:
                        description: |-
                          AimId stamps the derived profile's spec.aimId. When set, wins over
                          the source profile's aimId. REQUIRED when the enclosing selector has
                          role=base (the source base profile carries no aimId of its own).
                        type: string
                      containerEnv:
                        description: ContainerEnv merges by env var name, overriding
                          matching source entries.
                        items:
                          description: EnvVar represents an environment variable present
                            in a Container.
                          properties:
                            name:
                              description: |-
                                Name of the environment variable.
                                May consist of any printable ASCII characters except '='.
                              type: string
                            value:
                              description: |-
                                Variable references $(VAR_NAME) are expanded
                                using the previously defined environment variables in the container and
                                any service environment variables. If a variable cannot be resolved,
                                the reference in the input string will be unchanged. Double $$ are reduced
                                to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                Escaped references will never be expanded, regardless of whether the variable
                                exists or not.
                                Defaults to "".
                              type: string
                            valueFrom:
                              description: Source for the environment variable's value.
                                Cannot be used if value is not empty.
                              properties:
                                configMapKeyRef:
                                  description: Selects a key of a ConfigMap.
                                  properties:
                                    key:
                                      description: The key to select.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the ConfigMap or
                                        its key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fieldRef:
                                  description: |-
                                    Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                    spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                  properties:
                                    apiVersion:
                                      description: Version of the schema the FieldPath
                                        is written in terms of, defaults to "v1".
                                      type: string
                                    fieldPath:
                                      description: Path of the field to select in
                                        the specified API version.
                                      type: string
                                  required:
                                  - fieldPath
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fileKeyRef:
                                  description: |-
                                    FileKeyRef selects a key of the env file.
                                    Requires the EnvFiles feature gate to be enabled.
                                  properties:
                                    key:
                                      description: |-
                                        The key within the env file. An invalid key will prevent the pod from starting.
                                        The keys defined within a source may consist of any printable ASCII characters except '='.
                                        During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                      type: string
                                    optional:
                                      default: false
                                      description: |-
                                        Specify whether the file or its key must be defined. If the file or key
                                        does not exist, then the env var is not published.
                                        If optional is set to true and the specified key does not exist,
                                        the environment variable will not be set in the Pod's containers.

                                        If optional is set to false and the specified key does not exist,
                                        an error will be returned during Pod creation.
                                      type: boolean
                                    path:
                                      description: |-
                                        The path within the volume from which to select the file.
                                        Must be relative and may not contain the '..' path or start with '..'.
                                      type: string
                                    volumeName:
                                      description: The name of the volume mount containing
                                        the env file.
                                      type: string
                                  required:
                                  - key
                                  - path
                                  - volumeName
                                  type: object
                                  x-kubernetes-map-type: atomic
                                resourceFieldRef:
                                  description: |-
                                    Selects a resource of the container: only resources limits and requests
                                    (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                  properties:
                                    containerName:
                                      description: 'Container name: required for volumes,
                                        optional for env vars'
                                      type: string
                                    divisor:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      description: Specifies the output format of
                                        the exposed resources, defaults to "1"
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    resource:
                                      description: 'Required: resource to select'
                                      type: string
                                  required:
                                  - resource
                                  type: object
                                  x-kubernetes-map-type: atomic
                                secretKeyRef:
                                  description: Selects a key of a secret in the pod's
                                    namespace
                                  properties:
                                    key:
                                      description: The key of the secret to select
                                        from.  Must be a valid secret key.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the Secret or its
                                        key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      engineArgs:
                        description: EngineArgs shallow-merges on top of the source
                          engineArgs, overriding matching keys.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      engineEnv:
                        additionalProperties:
                          type: string
                        description: EngineEnv merges by key, overriding matching
                          source entries.
                        type: object
                      image:
                        description: |-
                          Image overrides the runtime container image used by the derived
                          profiles. When empty the deployment image is rebased onto the source
                          profile's base-image (status.baseImage) so private mirrors stay
                          self-contained.
                        type: string
                      modelId:
                        description: |-
                          ModelId stamps the derived profile's spec.modelId. When set, wins
                          over both the source profile's modelId AND the auto-derivation from
                          modelSources[0].modelId. REQUIRED when the enclosing selector has
                          role=base.
                        type: string
                      modelSources:
                        description: |-
                          ModelSources replaces the copied profile's modelSources. When
                          modelSources[0].modelId is set and overrides.modelId is unset, the
                          derived profile's modelId is auto-derived from modelSources[0]; an
                          explicit overrides.modelId always wins.
                        items:
                          description: |-
                            AIMModelSource describes a model artifact that must be downloaded for inference.
                            Discovery extracts these from the container's configuration to enable caching and validation.
                          properties:
                            env:
                              description: |-
                                Env specifies per-source credential overrides.
                                These variables are used for authentication when downloading this specific source.
                                Takes precedence over base-level env for the same variable name.
                              items:
                                description: EnvVar represents an environment variable
                                  present in a Container.
                                properties:
                                  name:
                                    description: |-
                                      Name of the environment variable.
                                      May consist of any printable ASCII characters except '='.
                                    type: string
                                  value:
                                    description: |-
                                      Variable references $(VAR_NAME) are expanded
                                      using the previously defined environment variables in the container and
                                      any service environment variables. If a variable cannot be resolved,
                                      the reference in the input string will be unchanged. Double $$ are reduced
                                      to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                      "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                      Escaped references will never be expanded, regardless of whether the variable
                                      exists or not.
                                      Defaults to "".
                                    type: string
                                  valueFrom:
                                    description: Source for the environment variable's
                                      value. Cannot be used if value is not empty.
                                    properties:
                                      configMapKeyRef:
                                        description: Selects a key of a ConfigMap.
                                        properties:
                                          key:
                                            description: The key to select.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the ConfigMap
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fieldRef:
                                        description: |-
                                          Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                          spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                        properties:
                                          apiVersion:
                                            description: Version of the schema the
                                              FieldPath is written in terms of, defaults
                                              to "v1".
                                            type: string
                                          fieldPath:
                                            description: Path of the field to select
                                              in the specified API version.
                                            type: string
                                        required:
                                        - fieldPath
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fileKeyRef:
                                        description: |-
                                          FileKeyRef selects a key of the env file.
                                          Requires the EnvFiles feature gate to be enabled.
                                        properties:
                                          key:
                                            description: |-
                                              The key within the env file. An invalid key will prevent the pod from starting.
                                              The keys defined within a source may consist of any printable ASCII characters except '='.
                                              During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                            type: string
                                          optional:
                                            default: false
                                            description: |-
                                              Specify whether the file or its key must be defined. If the file or key
                                              does not exist, then the env var is not published.
                                              If optional is set to true and the specified key does not exist,
                                              the environment variable will not be set in the Pod's containers.

                                              If optional is set to false and the specified key does not exist,
                                              an error will be returned during Pod creation.
                                            type: boolean
                                          path:
                                            description: |-
                                              The path within the volume from which to select the file.
                                              Must be relative and may not contain the '..' path or start with '..'.
                                            type: string
                                          volumeName:
                                            description: The name of the volume mount
                                              containing the env file.
                                            type: string
                                        required:
                                        - key
                                        - path
                                        - volumeName
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      resourceFieldRef:
                                        description: |-
                                          Selects a resource of the container: only resources limits and requests
                                          (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                        properties:
                                          containerName:
                                            description: 'Container name: required
                                              for volumes, optional for env vars'
                                            type: string
                                          divisor:
                                            anyOf:
                                            - type: integer
                                            - type: string
                                            description: Specifies the output format
                                              of the exposed resources, defaults to
                                              "1"
                                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                            x-kubernetes-int-or-string: true
                                          resource:
                                            description: 'Required: resource to select'
                                            type: string
                                        required:
                                        - resource
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      secretKeyRef:
                                        description: Selects a key of a secret in
                                          the pod's namespace
                                        properties:
                                          key:
                                            description: The key of the secret to
                                              select from.  Must be a valid secret
                                              key.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the Secret
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                    type: object
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            modelId:
                              description: |-
                                ModelID is the canonical identifier in {org}/{name} format.
                                Determines the cache mount path: /workspace/cache/{modelId}
                                For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                                For S3 sources, users define their own organizational structure.
                              pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                              type: string
                            precision:
                              allOf:
                              - enum:
                                - auto
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              - enum:
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              description: |-
                                Precision describes the runtime precision this source is compatible with.
                                Used to match model sources to profiles during custom weight onboarding.
                              type: string
                            size:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                Size is the expected storage space required for this model artifact.
                                Used for PVC sizing and capacity planning during cache creation.
                                Optional - if not specified, the download job will discover the size automatically.
                                Can be set explicitly to pre-allocate storage or override auto-discovery.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            sourceUri:
                              description: |-
                                SourceURI is the location from which the model should be downloaded.
                                Supported schemes:
                                - hf://org/model - Hugging Face Hub model
                                - s3://bucket/key - S3-compatible storage
                              pattern: ^(hf|s3)://[^ \t\r\n]+$
                              type: string
                          required:
                          - modelId
                          - sourceUri
                          type: object
                        type: array
                      profileId:
                        description: |-
                          ProfileId stamps the derived profile's spec.profileId. Optional —
                          most callers leave this empty and let the source profile's profileId
                          carry through (or the reconciler synthesise one).
                        type: string
                    type: object
                  version:
                    description: |-
                      Version pins matching to a specific source profile version when
                      VersionPolicy is `pinned`.
                    type: string
                  versionPolicy:
                    default: pinned
                    description: VersionPolicy controls how matching profiles are
                      filtered by version.
                    enum:
                    - pinned
                    - latest
                    - all
                    type: string
                type: object
                x-kubernetes-validations:
                - message: profiles.derivedFrom is required when spec.profiles is
                    set
                  rule: has(self.derivedFrom)
                - message: profiles.version is required when versionPolicy is pinned
                  rule: has(self.version) || (has(self.versionPolicy) && self.versionPolicy
                    != 'pinned')
                - message: latest/all versionPolicy must not set version
                  rule: '!has(self.versionPolicy) || (self.versionPolicy != ''latest''
                    && self.versionPolicy != ''all'') || !has(self.version)'
                - message: derivedFrom.selector.aimId/modelId/profileId are not allowed
                    when selector.role=base; set overrides.aimId/modelId/profileId
                    instead (base profiles have no source identity to filter on)
                  rule: '!(has(self.derivedFrom.selector.role) && self.derivedFrom.selector.role
                    == ''base'') || (!has(self.derivedFrom.selector.aimId) && !has(self.derivedFrom.selector.modelId)
                    && !has(self.derivedFrom.selector.profileId))'
                - message: derivedFrom.selector.role=base requires overrides.aimId
                    and overrides.modelId (base profiles carry no identity of their
                    own; overrides supply it)
                  rule: '!(has(self.derivedFrom.selector.role) && self.derivedFrom.selector.role
                    == ''base'') || (has(self.overrides) && has(self.overrides.aimId)
                    && has(self.overrides.modelId))'
              resources:
                description: |-
                  Resources defines the default resource requirements for services using this model.
                  Template- or service-level values override these defaults.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              runtimeConfigName:
                description: |-
                  Name is the name of the runtime config to use for this resource. If a runtime config with this name exists both
                  as a namespace and a cluster runtime config, the values are merged together, the namespace config taking priority
                  over the cluster config when there are conflicts. If this field is empty or set to `default`, the namespace / cluster
                  runtime config with the name `default` is used, if it exists.
                type: string
              serviceAccountName:
                description: |-
                  ServiceAccountName specifies the Kubernetes service account to use for workloads related to this model.
                  This includes metadata extraction jobs and any other model-related operations.
                  If empty, the default service account for the namespace is used.
                type: string
            type: object
            x-kubernetes-validations:
            - message: when using modelSources without aimId, set custom.hardware
                or set hardware on each customTemplate
              rule: '!has(self.modelSources) || size(self.modelSources) == 0 || has(self.aimId)
                || (has(self.custom) && has(self.custom.hardware)) || !has(self.customTemplates)
                || size(self.customTemplates) == 0 || self.customTemplates.all(t,
                has(t.hardware) || (has(self.custom) && has(self.custom.hardware)))'
            - message: image is required unless aimId is set with versionPolicy latest
                or any, or profileCopy/derivedFrom/profiles is set
              rule: (has(self.image) && size(self.image) > 0) || has(self.profileCopy)
                || has(self.derivedFrom) || has(self.profiles) || (has(self.aimId)
                && has(self.custom) && has(self.custom.versionPolicy) && self.custom.versionPolicy
                != 'pinned')
            - message: profileCopy cannot be combined with deprecated legacy AIMModel
                fields
              rule: '!has(self.profileCopy) || (!has(self.discovery) && !has(self.defaultServiceTemplate)
                && !has(self.custom) && (!has(self.customTemplates) || size(self.customTemplates)
                == 0) && (!has(self.modelSources) || size(self.modelSources) == 0)
                && (!has(self.runtimeConfigName) || size(self.runtimeConfigName) ==
                0) && (!has(self.env) || size(self.env) == 0) && !has(self.imageMetadata))'
          status:
            description: AIMModelStatus defines the observed state of AIMModel.
            properties:
              aimId:
                description: |-
                  AimId is the resolved model architecture identifier for this model.
                  Populated by the v1alpha2 controller from spec.aimId or discovered metadata.
                type: string
              baseImage:
                description: |-
                  BaseImage is the extracted AIM base image reference (AIM_BASE_IMAGE_REF) when known.
                  Used when resolving deployment images for fine-tuned models that have no spec.image.
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the model's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              discoveredProfiles:
                description: DiscoveredProfiles summarizes the profiles found during
                  image discovery.
                properties:
                  byHardware:
                    description: |-
                      ByHardware groups discovered profiles by their hardware footprint and
                      reports whether each group is currently supported by the cluster. The
                      groups are stable across reconciles (sorted by acceleratorType,
                      acceleratorModel, acceleratorCount) so kubectl/jq queries are cheap.
                      Useful when debugging "why doesn't the {metric, precision} profile I
                      expect appear in my cluster?" — the breakdown surfaces every shape the
                      image emits, not only the ones materialised as AIMProfile objects.
                    items:
                      description: |-
                        ProfileHardwareGroup is one accelerator footprint within a model's
                        discovery catalog, with the metric/precision combos shipped under it.
                      properties:
                        acceleratorCount:
                          description: |-
                            AcceleratorCount is the number of accelerator units the profile
                            requests. For gpu: device count (e.g., 1, 2, 4, 8 for tensor-parallel
                            sizes). For cpu: number of CPU cores (e.g., 128 for EPYC_ZEN5,
                            192 for EPYC_9965).
                          format: int32
                          type: integer
                        acceleratorModel:
                          description: |-
                            AcceleratorModel is the accelerator identifier (e.g., "MI300X",
                            "EPYC_ZEN5"). Empty for profiles with no accelerator requirement.
                          type: string
                        acceleratorType:
                          description: AcceleratorType is the resource family (gpu,
                            cpu).
                          enum:
                          - gpu
                          - cpu
                          type: string
                        profiles:
                          description: |-
                            Profiles lists the {metric, precision} combinations discovered under
                            this hardware footprint. Reported even when the group is unsupported
                            so users can see what they're missing.
                          items:
                            description: |-
                              ProfileHardwareGroupEntry identifies one profile within a hardware group
                              by its {metric, precision} pair. Sufficient for users to spot whether the
                              optimization variant they want is shipped at all.
                            properties:
                              metric:
                                description: Metric is the optimization target (latency,
                                  throughput).
                                enum:
                                - latency
                                - throughput
                                type: string
                              precision:
                                description: Precision is the numeric precision (fp4,
                                  fp8, bf16, …).
                                enum:
                                - auto
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                                type: string
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        supported:
                          description: |-
                            Supported reports whether this hardware footprint is currently
                            satisfied by at least one cluster node. When false, all profiles in
                            this group are skipped during materialisation.
                          type: boolean
                      required:
                      - supported
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  supported:
                    description: Supported is the number of discovered profiles currently
                      supported by the cluster.
                    format: int32
                    type: integer
                  total:
                    description: Total is the number of profiles discovered from the
                      image.
                    format: int32
                    type: integer
                  unsupported:
                    description: Unsupported is the number of discovered profiles
                      currently not supported by the cluster.
                    format: int32
                    type: integer
                type: object
              discovery:
                description: Discovery tracks the state of the image-discovery Job
                  used to populate the discovery cache.
                properties:
                  attempts:
                    description: |-
                      Attempts is the number of discovery job attempts that have been made.
                      Increments each time a new discovery job is created after a failure.
                    format: int32
                    type: integer
                  lastAttemptTime:
                    description: |-
                      LastAttemptTime is the timestamp of the most recent discovery job creation.
                      Used to calculate exponential backoff before the next retry.
                    format: date-time
                    type: string
                  lastFailureReason:
                    description: LastFailureReason captures the reason for the most
                      recent discovery failure.
                    type: string
                  specHash:
                    description: |-
                      SpecHash is a hash of the model spec fields that invalidate cached discovery
                      (image, imagePullSecrets, serviceAccountName, discoveryCommandVersion).
                      When it changes, the operator drops the cache and re-runs the discovery Job.
                    type: string
                type: object
              discoveryCacheRef:
                description: |-
                  DiscoveryCacheRef points at the normalized discovery cache ConfigMap for image-backed flows.
                  Populated by the v1alpha2 controller after image inspection succeeds.
                properties:
                  name:
                    description: Name is the ConfigMap name.
                    type: string
                  namespace:
                    description: Namespace is the ConfigMap namespace.
                    type: string
                type: object
              imageMetadata:
                description: ImageMetadata is the metadata extracted from an AIM image
                properties:
                  baseImageRef:
                    description: |-
                      BaseImageRef is the value of the AIM_BASE_IMAGE_REF environment variable
                      baked into the image's OCI config at build time. For AIM model images this
                      records the base image (e.g. "ghcr.io/silogen/aim-base:0.8.5") that the
                      model image was built from. Used by the AIMModel controller to resolve
                      the deployment image for fine-tuned models whose spec.image is omitted
                      (versionPolicy=latest or any).
                    type: string
                  model:
                    description: Model contains AMD Silogen model-specific metadata.
                    properties:
                      canonicalName:
                        description: |-
                          CanonicalName is the canonical model identifier (e.g., mistralai/Mixtral-8x22B-Instruct-v0.1).
                          Extracted from: org.amd.silogen.model.canonicalName
                        type: string
                      descriptionFull:
                        description: |-
                          DescriptionFull is the full description.
                          Extracted from: org.amd.silogen.description.full
                        type: string
                      hfTokenRequired:
                        description: |-
                          HFTokenRequired indicates if a HuggingFace token is required.
                          Extracted from: org.amd.silogen.hfToken.required
                        type: boolean
                      recommendedDeployments:
                        description: |-
                          RecommendedDeployments contains recommended deployment configurations.
                          Extracted from: org.amd.silogen.model.recommendedDeployments (parsed from JSON array)
                        items:
                          description: RecommendedDeployment describes a recommended
                            deployment configuration for a model.
                          properties:
                            description:
                              description: Description provides additional context
                                about this deployment configuration
                              type: string
                            gpuCount:
                              description: GPUCount is the number of GPUs required
                              format: int32
                              type: integer
                            gpuModel:
                              description: |-
                                GPUModel is the GPU model name (e.g., MI300X, MI325X).
                                The legacy v1alpha1 schema only models GPU accelerators; CPU profiles
                                emitted via OCI labels (gpuModel="CPU") are not first-class here and
                                are handled exclusively by the v1alpha2 native discovery pipeline.
                              type: string
                            metric:
                              description: Metric is the optimization target (e.g.,
                                latency, throughput)
                              type: string
                            precision:
                              description: Precision is the recommended precision
                                (e.g., fp8, fp16, bf16)
                              type: string
                            profileId:
                              description: |-
                                ProfileId is the unique identifier of the AIM profile for this deployment.
                                When set, templates created from this deployment will use this profile ID
                                to deterministically select the correct runtime profile in the AIM container.
                              type: string
                          type: object
                        type: array
                      releaseNotes:
                        description: |-
                          ReleaseNotes contains release notes for this version.
                          Extracted from: org.amd.silogen.release.notes
                        type: string
                      source:
                        description: |-
                          Source is the URL where the model can be found.
                          Extracted from: org.amd.silogen.model.source
                        type: string
                      tags:
                        description: |-
                          Tags are descriptive tags (e.g., ["text-generation", "chat", "instruction"]).
                          Extracted from: org.amd.silogen.model.tags (comma-separated)
                        items:
                          type: string
                        type: array
                      title:
                        description: |-
                          Title is the Silogen-specific title for the model.
                          Extracted from: org.amd.silogen.title
                        type: string
                      variants:
                        description: |-
                          Variants lists model variants.
                          Extracted from: org.amd.silogen.model.variants (comma-separated)
                        items:
                          type: string
                        type: array
                      versions:
                        description: |-
                          Versions lists available versions.
                          Extracted from: org.amd.silogen.model.versions (comma-separated)
                        items:
                          type: string
                        type: array
                    type: object
                  oci:
                    description: OCI contains standard OCI image metadata.
                    properties:
                      authors:
                        description: |-
                          Authors is contact details of the authors.
                          Extracted from: org.opencontainers.image.authors
                        type: string
                      created:
                        description: |-
                          Created is the creation timestamp.
                          Extracted from: org.opencontainers.image.created
                        type: string
                      description:
                        description: |-
                          Description is a brief description.
                          Extracted from: org.opencontainers.image.description
                        type: string
                      documentation:
                        description: |-
                          Documentation is the URL to documentation.
                          Extracted from: org.opencontainers.image.documentation
                        type: string
                      licenses:
                        description: |-
                          Licenses is the SPDX license identifier(s).
                          Extracted from: org.opencontainers.image.licenses
                        type: string
                      revision:
                        description: |-
                          Revision is the source control revision.
                          Extracted from: org.opencontainers.image.revision
                        type: string
                      source:
                        description: |-
                          Source is the URL to the source code repository.
                          Extracted from: org.opencontainers.image.source
                        type: string
                      title:
                        description: |-
                          Title is the human-readable title.
                          Extracted from: org.opencontainers.image.title
                        type: string
                      vendor:
                        description: |-
                          Vendor is the organization that produced the image.
                          Extracted from: org.opencontainers.image.vendor
                        type: string
                      version:
                        description: |-
                          Version is the image version.
                          Extracted from: org.opencontainers.image.version
                        type: string
                    type: object
                  originalLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      OriginalLabels contains the raw OCI image labels as a JSON object.
                      This preserves all labels from the image, including those not mapped to structured fields.
                    type: object
                type: object
              kind:
                description: |-
                  Kind classifies the v1alpha2 onboarding flow that produced this
                  model's profiles (Image / Derived / Custom). See AIMModelKind for
                  the per-value semantics. Populated by the v1alpha2 controller from
                  the spec shape; left empty by the v1alpha1 controller.
                enum:
                - Image
                - Derived
                - Custom
                type: string
              managedProfiles:
                description: ManagedProfiles summarizes the direct promoted or derived
                  profiles owned or managed by this model.
                properties:
                  base:
                    description: |-
                      Base is the number of managed profiles whose spec is structurally
                      incomplete (missing aimId or modelSources). Base profiles cannot back
                      an AIMService directly and exist purely as source material for
                      derivation via AIMModel.spec.profiles.derivedFrom with
                      selector.role=base. A non-zero value is the canonical operational
                      signal that this model is a base-image model (the source of
                      custom-model derivation).
                    format: int32
                    type: integer
                  deployable:
                    description: |-
                      Deployable is the number of managed profiles whose spec is materialised
                      enough to back an AIMService (carries aimId and modelSources). For a
                      normal officially-discovered AIMModel this equals Total. For a
                      base-image AIMModel used as a source for custom-model derivation
                      this is 0 — the model only emits base profiles that callers must
                      derive into deployable profiles.
                    format: int32
                    type: integer
                  notAvailable:
                    description: NotAvailable is the number of derivative profiles
                      whose own status is NotAvailable.
                    format: int32
                    type: integer
                  ready:
                    description: Ready is the number of derivative profiles whose
                      own status is Ready.
                    format: int32
                    type: integer
                  total:
                    description: Total is the number of derivative profiles the controller
                      currently manages or intends to manage.
                    format: int32
                    type: integer
                required:
                - base
                - deployable
                - notAvailable
                - ready
                - total
                type: object
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller
                format: int64
                type: integer
              profileSetRef:
                description: ProfileSetRef identifies the child profile set synthesized
                  for derivation flows.
                properties:
                  name:
                    description: Name is the profile set name.
                    type: string
                  namespace:
                    description: Namespace is the profile set namespace.
                    type: string
                type: object
              resolvedRuntimeConfig:
                description: ResolvedRuntimeConfig captures metadata about the runtime
                  config that was resolved.
                properties:
                  kind:
                    description: Kind is the fully-qualified kind of the resolved
                      reference, when known.
                    type: string
                  name:
                    description: Name is the resource name that satisfied the reference.
                    type: string
                  namespace:
                    description: |-
                      Namespace identifies where the resource was found when namespace-scoped.
                      Empty indicates a cluster-scoped resource.
                    type: string
                  scope:
                    description: Scope indicates whether the resolved resource was
                      namespace or cluster scoped.
                    enum:
                    - Namespace
                    - Cluster
                    - Merged
                    - Unknown
                    type: string
                  uid:
                    description: UID captures the unique identifier of the resolved
                      reference, when known.
                    type: string
                type: object
              sourceType:
                description: |-
                  SourceType indicates how this model's artifacts are sourced.
                  - "Image": Model discovered from container image labels
                  - "Custom": Model uses explicit spec.modelSources
                  Set by the controller based on whether spec.modelSources is populated.

                  Note: only populated by the v1alpha1 controller; v1alpha2 consumers
                  should read .status.kind instead, which distinguishes fine-tunes
                  (Derived) from BYO base-image overlays (Custom).
                enum:
                - Image
                - Custom
                type: string
              status:
                default: Pending
                description: Status represents the overall status of the image based
                  on its templates
                enum:
                - Pending
                - Progressing
                - Ready
                - Degraded
                - Failed
                - NotAvailable
                type: string
              version:
                description: |-
                  Version is the effective image version of the model. Populated from
                  the spec.image tag (`amdenterpriseai/aim-qwen-qwen3-32b:0.11.0` →
                  `0.11.0`) during reconciliation. Empty for models that have no
                  spec.image (e.g. fine-tuned models derived from a parent) or that
                  reference an image by digest.

                  This mirrors AIMProfileStatus.Version so the two surfaces stay in
                  lock-step: a single image-tag extraction rule governs what users
                  see in the kubectl printcolumn for both kinds.

                  The image-author-declared version (i.e. the
                  `org.opencontainers.image.version` OCI label) is preserved separately
                  under .status.imageMetadata.oci.version for users that want to inspect
                  what the image build pipeline stamped. The two values usually agree;
                  when the OCI label is missing or empty, this field still surfaces a
                  useful version from the tag itself.
                type: string
            type: object
        type: object
        x-kubernetes-validations:
        - message: spec.derivedFrom is only supported on v1alpha2; use spec.profileCopy
            on v1alpha1
          rule: '!has(self.spec.derivedFrom)'
        - message: spec.profiles is only supported on v1alpha2; use spec.profileCopy
            on v1alpha1
          rule: '!has(self.spec.profiles)'
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.kind
      name: Kind
      type: string
    - jsonPath: .status.aimId
      name: AimID
      type: string
    - jsonPath: .status.version
      name: Version
      type: string
    - jsonPath: .status.managedProfiles.total
      name: Managed
      type: integer
    - jsonPath: .status.managedProfiles.ready
      name: Ready
      type: integer
    - jsonPath: .status.managedProfiles.base
      name: Base
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: |-
          AIMModel is the Schema for the v1alpha2 AIMModel API.

          AIMModel mirrors AIMService: the canonical Spec/Status types live in
          v1alpha1 and the v1alpha2 wrapper is a thin re-export so JSON wire format
          is identical between versions and the None conversion strategy works
          without a webhook.

          v1alpha2 onboarding contract: exactly one of spec.image (official
          discovery) or spec.profiles (fine-tune / custom-model derivation); legacy
          v1alpha1 onboarding fields and the deprecated flat spec.derivedFrom shape
          are forbidden on NEW v1alpha2 objects. Each "forbidden" rule uses
          optionalOldSelf so that objects originally created via v1alpha1 (which
          legally carry spec.custom, spec.modelSources, spec.customTemplates,
          spec.profileCopy) and the early-iteration v1alpha2 objects (carrying
          spec.derivedFrom) can still be updated through the v1alpha2 surface — the
          reconciler must be able to add finalizers and patch the spec of
          legacy-shaped objects without being blocked by the v1alpha2 schema. Adding
          a legacy/deprecated field to an object that did not previously have it is
          still rejected.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              AIMModelSpec defines the desired state of AIMModel.

              Per-version constraints (v1alpha1 forbids derivedFrom and profiles;
              v1alpha2 forbids profileCopy/custom/customTemplates/top-level modelSources,
              requires image XOR profiles, and forbids profiles mixed with legacy scalar
              fields) live on the version-specific AIMModel / AIMClusterModel root types
              in the per-version *_types.go files.
            properties:
              aimId:
                description: |-
                  AimId is the AIM product family identifier (e.g., "qwen/qwen3-32b").
                  When set together with modelSources, enables aimId-based template matching:
                  the controller finds official templates by aimId, filters by versionPolicy,
                  matches by modelId, and creates copies with the custom weight source.
                type: string
              custom:
                description: |-
                  Custom contains configuration for custom models (models with inline modelSources).
                  Only used when modelSources are specified; ignored for image-based models.
                properties:
                  hardware:
                    description: |-
                      Hardware specifies default hardware requirements for all templates.
                      Individual templates can override these defaults.
                      Required when modelSources is set and customTemplates is empty (unless aimId is set).
                    properties:
                      cpu:
                        description: CPU specifies CPU requirements.
                        properties:
                          limits:
                            anyOf:
                            - type: integer
                            - type: string
                            description: Limits is the maximum number of CPU cores
                              to allow.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          requests:
                            anyOf:
                            - type: integer
                            - type: string
                            description: Requests is the number of CPU cores to request.
                              Required and must be > 0.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                        required:
                        - requests
                        type: object
                      gpu:
                        description: GPU specifies GPU requirements. If not set, no
                          GPUs are requested (CPU-only model).
                        properties:
                          minVram:
                            anyOf:
                            - type: integer
                            - type: string
                            description: |-
                              MinVRAM limits deployment to GPUs having at least this much VRAM.
                              Used for capacity planning when the model size is known but any GPU with
                              sufficient VRAM is acceptable.
                              Cannot be combined with model.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          model:
                            description: |-
                              Model limits deployment to a specific GPU model.
                              Example: "MI300X"
                              Cannot be combined with minVram.
                            maxLength: 64
                            type: string
                          requests:
                            description: |-
                              Requests is the number of GPUs to set as requests/limits.
                              Set to 0 to target GPU nodes without consuming GPU resources (useful for testing).
                            format: int32
                            minimum: 0
                            type: integer
                          resourceName:
                            default: amd.com/gpu
                            description: |-
                              ResourceName is the Kubernetes resource name for GPU resources.
                              Defaults to "amd.com/gpu" if not specified.
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: model and minVram are mutually exclusive; specify
                            either a specific GPU model OR a minimum VRAM requirement,
                            not both
                          rule: '!(has(self.model) && size(self.model) > 0 && has(self.minVram))'
                    type: object
                    x-kubernetes-validations:
                    - message: at least one of gpu or cpu must be specified
                      rule: has(self.gpu) || has(self.cpu)
                  type:
                    allOf:
                    - enum:
                      - optimized
                      - general
                      - preview
                      - unoptimized
                    - enum:
                      - optimized
                      - general
                      - preview
                      - unoptimized
                    description: |-
                      Type specifies default type for all templates.
                      Individual templates can override this default.
                      When nil, templates default to "unoptimized".
                    type: string
                  versionPolicy:
                    default: pinned
                    description: |-
                      VersionPolicy controls how template versions are filtered during aimId-based matching.
                      - pinned (default): match templates whose status.version equals the model's image tag
                      - latest: match only templates at the newest available status.version
                      - all: match templates at any version
                      - any: deprecated alias of all, kept for backward compatibility
                      Only used when spec.aimId is set.
                    enum:
                    - pinned
                    - latest
                    - any
                    - all
                    type: string
                type: object
              customTemplates:
                description: |-
                  CustomTemplates defines explicit template configurations for this model.
                  These templates are created directly without running a discovery job.
                  Can be used with or without modelSources to define custom deployment configurations.
                  If omitted when modelSources is set, a single template is auto-generated
                  using the custom.hardware requirements.
                items:
                  description: |-
                    AIMCustomTemplate defines a custom template configuration for a model.
                    When modelSources are specified directly on AIMModel, customTemplates allow
                    defining explicit hardware requirements and profiles, skipping the discovery job.
                    This is an existing struct (not a CRD); it appears as an element of AIMModel.spec.customTemplates[].
                  properties:
                    aimId:
                      description: |-
                        AimId is the AIM product family identifier (e.g., "meta-llama/Llama-3-8B").
                        Required when customProfile is set.
                      type: string
                    customProfile:
                      description: |-
                        CustomProfile defines inline custom profile data for the inference engine.
                        When set, the resulting template will have a custom profile ConfigMap mounted.
                        Requires aimId, modelId, hardware, profile.metric, and profile.precision.
                      properties:
                        engineArgs:
                          description: |-
                            EngineArgs contains inference engine arguments as a free-form JSON object.
                            These are passed as CLI arguments to the inference engine (e.g., vLLM).
                            Do not include "model" — it is injected separately by the runtime.
                          x-kubernetes-preserve-unknown-fields: true
                        envVars:
                          additionalProperties:
                            type: string
                          description: |-
                            EnvVars contains environment variables applied to the inference engine process.
                            These are written into the profile YAML and applied by the AIM runtime via os.execv,
                            distinct from container-level Env which targets the AIM runtime container itself.
                            Keys must match ^[A-Z0-9_]+$ (uppercase with underscores).
                          type: object
                      type: object
                    env:
                      description: |-
                        Env specifies environment variable overrides when this template is selected.
                        These are container-level env vars applied to the AIM runtime container.
                      items:
                        description: EnvVar represents an environment variable present
                          in a Container.
                        properties:
                          name:
                            description: |-
                              Name of the environment variable.
                              May consist of any printable ASCII characters except '='.
                            type: string
                          value:
                            description: |-
                              Variable references $(VAR_NAME) are expanded
                              using the previously defined environment variables in the container and
                              any service environment variables. If a variable cannot be resolved,
                              the reference in the input string will be unchanged. Double $$ are reduced
                              to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                              "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                              Escaped references will never be expanded, regardless of whether the variable
                              exists or not.
                              Defaults to "".
                            type: string
                          valueFrom:
                            description: Source for the environment variable's value.
                              Cannot be used if value is not empty.
                            properties:
                              configMapKeyRef:
                                description: Selects a key of a ConfigMap.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the ConfigMap or
                                      its key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              fieldRef:
                                description: |-
                                  Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                  spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                properties:
                                  apiVersion:
                                    description: Version of the schema the FieldPath
                                      is written in terms of, defaults to "v1".
                                    type: string
                                  fieldPath:
                                    description: Path of the field to select in the
                                      specified API version.
                                    type: string
                                required:
                                - fieldPath
                                type: object
                                x-kubernetes-map-type: atomic
                              fileKeyRef:
                                description: |-
                                  FileKeyRef selects a key of the env file.
                                  Requires the EnvFiles feature gate to be enabled.
                                properties:
                                  key:
                                    description: |-
                                      The key within the env file. An invalid key will prevent the pod from starting.
                                      The keys defined within a source may consist of any printable ASCII characters except '='.
                                      During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                    type: string
                                  optional:
                                    default: false
                                    description: |-
                                      Specify whether the file or its key must be defined. If the file or key
                                      does not exist, then the env var is not published.
                                      If optional is set to true and the specified key does not exist,
                                      the environment variable will not be set in the Pod's containers.

                                      If optional is set to false and the specified key does not exist,
                                      an error will be returned during Pod creation.
                                    type: boolean
                                  path:
                                    description: |-
                                      The path within the volume from which to select the file.
                                      Must be relative and may not contain the '..' path or start with '..'.
                                    type: string
                                  volumeName:
                                    description: The name of the volume mount containing
                                      the env file.
                                    type: string
                                required:
                                - key
                                - path
                                - volumeName
                                type: object
                                x-kubernetes-map-type: atomic
                              resourceFieldRef:
                                description: |-
                                  Selects a resource of the container: only resources limits and requests
                                  (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                properties:
                                  containerName:
                                    description: 'Container name: required for volumes,
                                      optional for env vars'
                                    type: string
                                  divisor:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: Specifies the output format of the
                                      exposed resources, defaults to "1"
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  resource:
                                    description: 'Required: resource to select'
                                    type: string
                                required:
                                - resource
                                type: object
                                x-kubernetes-map-type: atomic
                              secretKeyRef:
                                description: Selects a key of a secret in the pod's
                                  namespace
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                        required:
                        - name
                        type: object
                      maxItems: 64
                      type: array
                      x-kubernetes-list-map-keys:
                      - name
                      x-kubernetes-list-type: map
                    hardware:
                      description: |-
                        Hardware specifies GPU and CPU requirements for this template.
                        Optional when spec.hardware is set (inherits from spec).
                        When both are set, values are merged field-by-field with template taking precedence.
                      properties:
                        cpu:
                          description: CPU specifies CPU requirements.
                          properties:
                            limits:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Limits is the maximum number of CPU cores
                                to allow.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            requests:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Requests is the number of CPU cores to
                                request. Required and must be > 0.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                          required:
                          - requests
                          type: object
                        gpu:
                          description: GPU specifies GPU requirements. If not set,
                            no GPUs are requested (CPU-only model).
                          properties:
                            minVram:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                MinVRAM limits deployment to GPUs having at least this much VRAM.
                                Used for capacity planning when the model size is known but any GPU with
                                sufficient VRAM is acceptable.
                                Cannot be combined with model.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            model:
                              description: |-
                                Model limits deployment to a specific GPU model.
                                Example: "MI300X"
                                Cannot be combined with minVram.
                              maxLength: 64
                              type: string
                            requests:
                              description: |-
                                Requests is the number of GPUs to set as requests/limits.
                                Set to 0 to target GPU nodes without consuming GPU resources (useful for testing).
                              format: int32
                              minimum: 0
                              type: integer
                            resourceName:
                              default: amd.com/gpu
                              description: |-
                                ResourceName is the Kubernetes resource name for GPU resources.
                                Defaults to "amd.com/gpu" if not specified.
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: model and minVram are mutually exclusive; specify
                              either a specific GPU model OR a minimum VRAM requirement,
                              not both
                            rule: '!(has(self.model) && size(self.model) > 0 && has(self.minVram))'
                      type: object
                      x-kubernetes-validations:
                      - message: at least one of gpu or cpu must be specified
                        rule: has(self.gpu) || has(self.cpu)
                    modelId:
                      description: |-
                        ModelId is the specific model identifier / HuggingFace URI (e.g., "Qwen/Qwen3-32B-FP8").
                        Required when customProfile is set.
                      type: string
                    name:
                      description: Name is the template name. If not provided, auto-generated
                        from model name + profile.
                      maxLength: 63
                      type: string
                    profile:
                      description: |-
                        Profile declares runtime profile variables for template selection.
                        Used when multiple templates exist to select based on metric/precision.
                      properties:
                        metric:
                          allOf:
                          - enum:
                            - latency
                            - throughput
                          - enum:
                            - latency
                            - throughput
                          description: Metric specifies the optimization target (e.g.,
                            latency, throughput).
                          type: string
                        precision:
                          allOf:
                          - enum:
                            - auto
                            - fp4
                            - fp8
                            - fp16
                            - fp32
                            - bf16
                            - int4
                            - int8
                          - enum:
                            - auto
                            - fp4
                            - fp8
                            - fp16
                            - fp32
                            - bf16
                            - int4
                            - int8
                          description: Precision specifies the numerical precision
                            (e.g., fp8, fp16, bf16).
                          type: string
                      type: object
                    type:
                      allOf:
                      - enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                      - enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                      default: unoptimized
                      description: |-
                        Type indicates the optimization status of this template.
                        - optimized: Template has been tuned for performance
                        - general: General-purpose tuning between optimized and preview
                        - preview: Template is experimental/pre-release
                        - unoptimized: Default, no specific optimizations applied
                      type: string
                  type: object
                  x-kubernetes-validations:
                  - message: when customProfile is set, aimId, modelId, hardware,
                      profile.metric, and profile.precision are required
                    rule: '!has(self.customProfile) || (has(self.aimId) && has(self.modelId)
                      && has(self.hardware) && has(self.profile) && has(self.profile.metric)
                      && has(self.profile.precision))'
                maxItems: 16
                type: array
              defaultServiceTemplate:
                description: |-
                  DefaultServiceTemplate specifies the default AIMServiceTemplate to use when creating services for this model.
                  When set, services that reference this model will use this template if no template is explicitly specified.
                  If this is not set, a template will be automatically selected.
                type: string
              derivedFrom:
                description: |-
                  DerivedFrom is the legacy flat shape of v1alpha2's profile-derivation
                  onboarding surface. New objects must use spec.profiles instead; the
                  field is retained so existing v1alpha2 objects (and the v1alpha2
                  reconciler reading them) round-trip cleanly.

                  DEPRECATED: prefer spec.profiles.derivedFrom on v1alpha2.
                properties:
                  image:
                    description: Image overrides the runtime image used by the derived
                      profiles.
                    type: string
                  imagePullSecrets:
                    description: ImagePullSecrets lists secrets used for inspecting
                      and pulling container images.
                    items:
                      description: |-
                        LocalObjectReference contains enough information to let you locate the
                        referenced object inside the same namespace.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    type: array
                  overrides:
                    description: Overrides mutates the copied profile spec after selection
                      and version filtering.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount replaces the copied profile's
                          acceleratorCount.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel replaces the copied profile's
                          acceleratorModel.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode replaces the copied profile's
                          acceleratorPartitioningMode. Complete replacement, not a merge; an empty
                          override string leaves the source profile's mode untouched. Same reserved
                          values as AIMProfileSpecCommon.AcceleratorPartitioningMode. Whenever this
                          override is set (to any value, including "unpartitioned"), the CEL rule on the
                          enclosing spec also requires AcceleratorCount to be set: partition mode
                          changes the per-unit interpretation of acceleratorCount, and CEL cannot
                          read the base profile to tell whether the meaning actually changed, so it
                          conservatively requires the count be restated.
                        type: string
                      aimId:
                        description: |-
                          AimId stamps the derived profile's spec.aimId. When set, wins over
                          the source profile's aimId. REQUIRED when the enclosing selector has
                          role=base (the source base profile carries no aimId of its own).
                        type: string
                      containerEnv:
                        description: ContainerEnv merges by env var name, overriding
                          matching source entries.
                        items:
                          description: EnvVar represents an environment variable present
                            in a Container.
                          properties:
                            name:
                              description: |-
                                Name of the environment variable.
                                May consist of any printable ASCII characters except '='.
                              type: string
                            value:
                              description: |-
                                Variable references $(VAR_NAME) are expanded
                                using the previously defined environment variables in the container and
                                any service environment variables. If a variable cannot be resolved,
                                the reference in the input string will be unchanged. Double $$ are reduced
                                to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                Escaped references will never be expanded, regardless of whether the variable
                                exists or not.
                                Defaults to "".
                              type: string
                            valueFrom:
                              description: Source for the environment variable's value.
                                Cannot be used if value is not empty.
                              properties:
                                configMapKeyRef:
                                  description: Selects a key of a ConfigMap.
                                  properties:
                                    key:
                                      description: The key to select.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the ConfigMap or
                                        its key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fieldRef:
                                  description: |-
                                    Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                    spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                  properties:
                                    apiVersion:
                                      description: Version of the schema the FieldPath
                                        is written in terms of, defaults to "v1".
                                      type: string
                                    fieldPath:
                                      description: Path of the field to select in
                                        the specified API version.
                                      type: string
                                  required:
                                  - fieldPath
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fileKeyRef:
                                  description: |-
                                    FileKeyRef selects a key of the env file.
                                    Requires the EnvFiles feature gate to be enabled.
                                  properties:
                                    key:
                                      description: |-
                                        The key within the env file. An invalid key will prevent the pod from starting.
                                        The keys defined within a source may consist of any printable ASCII characters except '='.
                                        During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                      type: string
                                    optional:
                                      default: false
                                      description: |-
                                        Specify whether the file or its key must be defined. If the file or key
                                        does not exist, then the env var is not published.
                                        If optional is set to true and the specified key does not exist,
                                        the environment variable will not be set in the Pod's containers.

                                        If optional is set to false and the specified key does not exist,
                                        an error will be returned during Pod creation.
                                      type: boolean
                                    path:
                                      description: |-
                                        The path within the volume from which to select the file.
                                        Must be relative and may not contain the '..' path or start with '..'.
                                      type: string
                                    volumeName:
                                      description: The name of the volume mount containing
                                        the env file.
                                      type: string
                                  required:
                                  - key
                                  - path
                                  - volumeName
                                  type: object
                                  x-kubernetes-map-type: atomic
                                resourceFieldRef:
                                  description: |-
                                    Selects a resource of the container: only resources limits and requests
                                    (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                  properties:
                                    containerName:
                                      description: 'Container name: required for volumes,
                                        optional for env vars'
                                      type: string
                                    divisor:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      description: Specifies the output format of
                                        the exposed resources, defaults to "1"
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    resource:
                                      description: 'Required: resource to select'
                                      type: string
                                  required:
                                  - resource
                                  type: object
                                  x-kubernetes-map-type: atomic
                                secretKeyRef:
                                  description: Selects a key of a secret in the pod's
                                    namespace
                                  properties:
                                    key:
                                      description: The key of the secret to select
                                        from.  Must be a valid secret key.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the Secret or its
                                        key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      engineArgs:
                        description: EngineArgs shallow-merges on top of the source
                          engineArgs, overriding matching keys.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      engineEnv:
                        additionalProperties:
                          type: string
                        description: EngineEnv merges by key, overriding matching
                          source entries.
                        type: object
                      image:
                        description: |-
                          Image overrides the runtime container image used by the derived
                          profiles. When empty the deployment image is rebased onto the source
                          profile's base-image (status.baseImage) so private mirrors stay
                          self-contained.
                        type: string
                      modelId:
                        description: |-
                          ModelId stamps the derived profile's spec.modelId. When set, wins
                          over both the source profile's modelId AND the auto-derivation from
                          modelSources[0].modelId. REQUIRED when the enclosing selector has
                          role=base.
                        type: string
                      modelSources:
                        description: |-
                          ModelSources replaces the copied profile's modelSources. When
                          modelSources[0].modelId is set and overrides.modelId is unset, the
                          derived profile's modelId is auto-derived from modelSources[0]; an
                          explicit overrides.modelId always wins.
                        items:
                          description: |-
                            AIMModelSource describes a model artifact that must be downloaded for inference.
                            Discovery extracts these from the container's configuration to enable caching and validation.
                          properties:
                            env:
                              description: |-
                                Env specifies per-source credential overrides.
                                These variables are used for authentication when downloading this specific source.
                                Takes precedence over base-level env for the same variable name.
                              items:
                                description: EnvVar represents an environment variable
                                  present in a Container.
                                properties:
                                  name:
                                    description: |-
                                      Name of the environment variable.
                                      May consist of any printable ASCII characters except '='.
                                    type: string
                                  value:
                                    description: |-
                                      Variable references $(VAR_NAME) are expanded
                                      using the previously defined environment variables in the container and
                                      any service environment variables. If a variable cannot be resolved,
                                      the reference in the input string will be unchanged. Double $$ are reduced
                                      to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                      "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                      Escaped references will never be expanded, regardless of whether the variable
                                      exists or not.
                                      Defaults to "".
                                    type: string
                                  valueFrom:
                                    description: Source for the environment variable's
                                      value. Cannot be used if value is not empty.
                                    properties:
                                      configMapKeyRef:
                                        description: Selects a key of a ConfigMap.
                                        properties:
                                          key:
                                            description: The key to select.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the ConfigMap
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fieldRef:
                                        description: |-
                                          Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                          spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                        properties:
                                          apiVersion:
                                            description: Version of the schema the
                                              FieldPath is written in terms of, defaults
                                              to "v1".
                                            type: string
                                          fieldPath:
                                            description: Path of the field to select
                                              in the specified API version.
                                            type: string
                                        required:
                                        - fieldPath
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fileKeyRef:
                                        description: |-
                                          FileKeyRef selects a key of the env file.
                                          Requires the EnvFiles feature gate to be enabled.
                                        properties:
                                          key:
                                            description: |-
                                              The key within the env file. An invalid key will prevent the pod from starting.
                                              The keys defined within a source may consist of any printable ASCII characters except '='.
                                              During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                            type: string
                                          optional:
                                            default: false
                                            description: |-
                                              Specify whether the file or its key must be defined. If the file or key
                                              does not exist, then the env var is not published.
                                              If optional is set to true and the specified key does not exist,
                                              the environment variable will not be set in the Pod's containers.

                                              If optional is set to false and the specified key does not exist,
                                              an error will be returned during Pod creation.
                                            type: boolean
                                          path:
                                            description: |-
                                              The path within the volume from which to select the file.
                                              Must be relative and may not contain the '..' path or start with '..'.
                                            type: string
                                          volumeName:
                                            description: The name of the volume mount
                                              containing the env file.
                                            type: string
                                        required:
                                        - key
                                        - path
                                        - volumeName
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      resourceFieldRef:
                                        description: |-
                                          Selects a resource of the container: only resources limits and requests
                                          (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                        properties:
                                          containerName:
                                            description: 'Container name: required
                                              for volumes, optional for env vars'
                                            type: string
                                          divisor:
                                            anyOf:
                                            - type: integer
                                            - type: string
                                            description: Specifies the output format
                                              of the exposed resources, defaults to
                                              "1"
                                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                            x-kubernetes-int-or-string: true
                                          resource:
                                            description: 'Required: resource to select'
                                            type: string
                                        required:
                                        - resource
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      secretKeyRef:
                                        description: Selects a key of a secret in
                                          the pod's namespace
                                        properties:
                                          key:
                                            description: The key of the secret to
                                              select from.  Must be a valid secret
                                              key.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the Secret
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                    type: object
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            modelId:
                              description: |-
                                ModelID is the canonical identifier in {org}/{name} format.
                                Determines the cache mount path: /workspace/cache/{modelId}
                                For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                                For S3 sources, users define their own organizational structure.
                              pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                              type: string
                            precision:
                              allOf:
                              - enum:
                                - auto
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              - enum:
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              description: |-
                                Precision describes the runtime precision this source is compatible with.
                                Used to match model sources to profiles during custom weight onboarding.
                              type: string
                            size:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                Size is the expected storage space required for this model artifact.
                                Used for PVC sizing and capacity planning during cache creation.
                                Optional - if not specified, the download job will discover the size automatically.
                                Can be set explicitly to pre-allocate storage or override auto-discovery.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            sourceUri:
                              description: |-
                                SourceURI is the location from which the model should be downloaded.
                                Supported schemes:
                                - hf://org/model - Hugging Face Hub model
                                - s3://bucket/key - S3-compatible storage
                              pattern: ^(hf|s3)://[^ \t\r\n]+$
                              type: string
                          required:
                          - modelId
                          - sourceUri
                          type: object
                        type: array
                      profileId:
                        description: |-
                          ProfileId stamps the derived profile's spec.profileId. Optional —
                          most callers leave this empty and let the source profile's profileId
                          carry through (or the reconciler synthesise one).
                        type: string
                    type: object
                  selector:
                    description: Selector chooses which source profiles to derive
                      from.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount filters by accelerator unit
                          count.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel filters by accelerator identifier.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode filters candidates by their declared
                          partitioning mode. Partial-order match (NOT strict equality):
                            ""              - no filter on this field.
                            "unpartitioned" - matches profiles with mode "" or "unpartitioned".
                            "partitioned"   - matches profiles whose mode is non-trivial (anything
                                              other than "" / "unpartitioned").
                            "<C>"           - selector-only convenience: matches profiles with mode
                                              "<C>-*" (prefix on the scheme). Not a valid profile-spec
                                              value (e.g. selector "CPX" matches "CPX-NPS1", "CPX-NPS4").
                            "<C>-<M>"       - exact-string match on the scheme.
                        type: string
                      acceleratorType:
                        description: AcceleratorType filters by accelerator resource
                          type.
                        enum:
                        - gpu
                        - cpu
                        type: string
                      aimId:
                        description: AimId filters by model architecture identifier
                          (e.g., "qwen/qwen3-32b").
                        type: string
                      engine:
                        description: Engine filters by inference engine.
                        type: string
                      engineArgs:
                        description: |-
                          EngineArgs partially matches source engineArgs: every provided top-level key must
                          exist in the source object with an equal value.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      metric:
                        description: Metric filters by optimization target.
                        enum:
                        - latency
                        - throughput
                        type: string
                      minimumType:
                        description: |-
                          MinimumType filters by a minimum optimization level: candidates whose
                          type is this tier OR BETTER are accepted (hierarchy: optimized > general
                          > preview > unoptimized). This is the floor counterpart to the exact-match
                          Type field; the two AND together when both are set.

                          The sentinel "any" disables the floor (accept every tier). When this
                          field is empty the AIMService resolver applies a default floor of
                          "optimized" so auto-selection prefers production-grade profiles and never
                          silently picks an unoptimized one; to opt a service into lower tiers
                          (e.g. CPU/EPYC profiles published as unoptimized) set minimumType
                          explicitly to "unoptimized" or "any". Derivation selectors
                          (AIMProfileSet / AIMModel.profiles) treat empty as "any" so copying is
                          never tier-restricted by default.
                        enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                        - any
                        type: string
                      modelId:
                        description: ModelId filters by the source profile's specific
                          model identifier.
                        type: string
                      modelRef:
                        description: |-
                          ModelRef narrows candidates to those produced by a specific
                          AIM(Cluster)Model, matched via the `aim.eai.amd.com/source-model[-scope]`
                          labels stamped by the AIMModel reconcilers. Iteration 1 (v1alpha2 only).
                        properties:
                          name:
                            description: Name is the owning AIM(Cluster)Model name.
                              Required.
                            minLength: 1
                            type: string
                          scope:
                            default: Auto
                            description: Scope controls how Name is resolved against
                              AIMModel vs AIMClusterModel.
                            enum:
                            - Auto
                            - Namespace
                            - Cluster
                            type: string
                        required:
                        - name
                        type: object
                      origin:
                        description: |-
                          Origin filters by the `aim.eai.amd.com/profile-origin` label. When unset
                          (empty) the selector does not filter by origin. Iteration 1 (v1alpha2
                          only).
                        enum:
                        - discovered
                        - derived
                        - user-authored
                        type: string
                      precision:
                        description: Precision filters by numeric precision.
                        enum:
                        - auto
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                        type: string
                      profileId:
                        description: ProfileId filters by the source profile's profile
                          identifier.
                        type: string
                      role:
                        default: deployable
                        description: |-
                          Role filters by the `aim.eai.amd.com/profile-role` label. Defaults to
                          `deployable`. `base` filters to base profiles emitted by base-image
                          discovery (custom-model derivation source material).
                        enum:
                        - base
                        - deployable
                        type: string
                      type:
                        description: Type filters by optimization level (exact match).
                        enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                        type: string
                    type: object
                  serviceAccountName:
                    description: ServiceAccountName is propagated to managed profiles
                      for downstream workloads.
                    type: string
                  sourceRef:
                    description: |-
                      SourceRef points to an alternate discovery cache source.
                      When omitted, derivation uses visible AIMProfile and AIMClusterProfile objects.
                      For AIMProfileSet, the referenced ConfigMap is read from the same namespace.
                      For AIMClusterProfileSet, it is read from the operator namespace.
                    properties:
                      name:
                        description: Name is the discovery cache ConfigMap name.
                        type: string
                    required:
                    - name
                    type: object
                  version:
                    description: Version pins matching to a specific source profile
                      version when VersionPolicy is pinned.
                    type: string
                  versionPolicy:
                    default: pinned
                    description: VersionPolicy controls how matching profiles are
                      filtered by version.
                    enum:
                    - pinned
                    - latest
                    - all
                    type: string
                type: object
                x-kubernetes-validations:
                - message: selector must set at least one matching field, or set sourceRef
                  rule: has(self.sourceRef) || has(self.selector.aimId) || has(self.selector.modelId)
                    || has(self.selector.profileId) || has(self.selector.engine) ||
                    has(self.selector.metric) || has(self.selector.precision) || has(self.selector.type)
                    || has(self.selector.acceleratorModel) || has(self.selector.acceleratorType)
                    || has(self.selector.acceleratorCount) || has(self.selector.engineArgs)
                    || has(self.selector.modelRef) || has(self.selector.origin)
                - message: version is required when versionPolicy is pinned
                  rule: has(self.version) || (has(self.versionPolicy) && self.versionPolicy
                    != 'pinned')
                - message: latest/all versionPolicy must not set version
                  rule: '!has(self.versionPolicy) || (self.versionPolicy != ''latest''
                    && self.versionPolicy != ''all'') || !has(self.version)'
                - message: selector.aimId/modelId/profileId are not allowed when selector.role=base;
                    set overrides.aimId/modelId/profileId instead (base profiles have
                    no source identity to filter on)
                  rule: '!(has(self.selector.role) && self.selector.role == ''base'')
                    || (!has(self.selector.aimId) && !has(self.selector.modelId) &&
                    !has(self.selector.profileId))'
                - message: selector.role=base requires overrides.aimId and overrides.modelId
                    (base profiles carry no identity of their own; overrides supply
                    it)
                  rule: '!(has(self.selector.role) && self.selector.role == ''base'')
                    || (has(self.overrides) && has(self.overrides.aimId) && has(self.overrides.modelId))'
                - message: overrides.acceleratorCount must be specified together with
                    any overrides.acceleratorPartitioningMode; partition mode changes
                    the per-unit interpretation of acceleratorCount
                  rule: '!has(self.overrides) || !has(self.overrides.acceleratorPartitioningMode)
                    || size(self.overrides.acceleratorPartitioningMode) == 0 || has(self.overrides.acceleratorCount)'
              discovery:
                description: |-
                  Discovery controls discovery behavior for this model.
                  When unset, uses runtime config defaults.
                properties:
                  createServiceTemplates:
                    default: true
                    description: CreateServiceTemplates controls whether (cluster)
                      service templates are auto-created from the image metadata.
                    type: boolean
                  extractMetadata:
                    default: true
                    description: |-
                      ExtractMetadata controls whether metadata extraction runs for this model.
                      During metadata extraction, the controller connects to the image registry and
                      extracts the image's labels.
                    type: boolean
                type: object
              env:
                description: |-
                  Env specifies environment variables for authentication during model discovery and metadata extraction.
                  These variables are used for authentication with model registries (e.g., HuggingFace tokens).
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: |-
                        Name of the environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        fileKeyRef:
                          description: |-
                            FileKeyRef selects a key of the env file.
                            Requires the EnvFiles feature gate to be enabled.
                          properties:
                            key:
                              description: |-
                                The key within the env file. An invalid key will prevent the pod from starting.
                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                              type: string
                            optional:
                              default: false
                              description: |-
                                Specify whether the file or its key must be defined. If the file or key
                                does not exist, then the env var is not published.
                                If optional is set to true and the specified key does not exist,
                                the environment variable will not be set in the Pod's containers.

                                If optional is set to false and the specified key does not exist,
                                an error will be returned during Pod creation.
                              type: boolean
                            path:
                              description: |-
                                The path within the volume from which to select the file.
                                Must be relative and may not contain the '..' path or start with '..'.
                              type: string
                            volumeName:
                              description: The name of the volume mount containing
                                the env file.
                              type: string
                          required:
                          - key
                          - path
                          - volumeName
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              image:
                description: |-
                  Image is the container image URI for this AIM model.
                  This image is inspected by the operator to select runtime profiles used by templates.
                  Discovery behavior is controlled by the discovery field and runtime config's AutoDiscovery setting.
                  Required unless aimId is set with versionPolicy latest or any, or profileCopy is set.
                type: string
              imageMetadata:
                description: |-
                  ImageMetadata is the metadata that is used to determine which recommended service templates to create,
                  and to drive clients with richer metadata regarding this particular model. For most cases the user does
                  not need to set this field manually, for images that have the supported labels embedded in them
                  the `AIM(Cluster)Model.status.imageMetadata` field is automatically filled from the container image labels.
                  This field is intended to be used when there are network restrictions, or in other similar situations.
                  If this field is set, the remote extraction will not be performed at all.
                properties:
                  baseImageRef:
                    description: |-
                      BaseImageRef is the value of the AIM_BASE_IMAGE_REF environment variable
                      baked into the image's OCI config at build time. For AIM model images this
                      records the base image (e.g. "ghcr.io/silogen/aim-base:0.8.5") that the
                      model image was built from. Used by the AIMModel controller to resolve
                      the deployment image for fine-tuned models whose spec.image is omitted
                      (versionPolicy=latest or any).
                    type: string
                  model:
                    description: Model contains AMD Silogen model-specific metadata.
                    properties:
                      canonicalName:
                        description: |-
                          CanonicalName is the canonical model identifier (e.g., mistralai/Mixtral-8x22B-Instruct-v0.1).
                          Extracted from: org.amd.silogen.model.canonicalName
                        type: string
                      descriptionFull:
                        description: |-
                          DescriptionFull is the full description.
                          Extracted from: org.amd.silogen.description.full
                        type: string
                      hfTokenRequired:
                        description: |-
                          HFTokenRequired indicates if a HuggingFace token is required.
                          Extracted from: org.amd.silogen.hfToken.required
                        type: boolean
                      recommendedDeployments:
                        description: |-
                          RecommendedDeployments contains recommended deployment configurations.
                          Extracted from: org.amd.silogen.model.recommendedDeployments (parsed from JSON array)
                        items:
                          description: RecommendedDeployment describes a recommended
                            deployment configuration for a model.
                          properties:
                            description:
                              description: Description provides additional context
                                about this deployment configuration
                              type: string
                            gpuCount:
                              description: GPUCount is the number of GPUs required
                              format: int32
                              type: integer
                            gpuModel:
                              description: |-
                                GPUModel is the GPU model name (e.g., MI300X, MI325X).
                                The legacy v1alpha1 schema only models GPU accelerators; CPU profiles
                                emitted via OCI labels (gpuModel="CPU") are not first-class here and
                                are handled exclusively by the v1alpha2 native discovery pipeline.
                              type: string
                            metric:
                              description: Metric is the optimization target (e.g.,
                                latency, throughput)
                              type: string
                            precision:
                              description: Precision is the recommended precision
                                (e.g., fp8, fp16, bf16)
                              type: string
                            profileId:
                              description: |-
                                ProfileId is the unique identifier of the AIM profile for this deployment.
                                When set, templates created from this deployment will use this profile ID
                                to deterministically select the correct runtime profile in the AIM container.
                              type: string
                          type: object
                        type: array
                      releaseNotes:
                        description: |-
                          ReleaseNotes contains release notes for this version.
                          Extracted from: org.amd.silogen.release.notes
                        type: string
                      source:
                        description: |-
                          Source is the URL where the model can be found.
                          Extracted from: org.amd.silogen.model.source
                        type: string
                      tags:
                        description: |-
                          Tags are descriptive tags (e.g., ["text-generation", "chat", "instruction"]).
                          Extracted from: org.amd.silogen.model.tags (comma-separated)
                        items:
                          type: string
                        type: array
                      title:
                        description: |-
                          Title is the Silogen-specific title for the model.
                          Extracted from: org.amd.silogen.title
                        type: string
                      variants:
                        description: |-
                          Variants lists model variants.
                          Extracted from: org.amd.silogen.model.variants (comma-separated)
                        items:
                          type: string
                        type: array
                      versions:
                        description: |-
                          Versions lists available versions.
                          Extracted from: org.amd.silogen.model.versions (comma-separated)
                        items:
                          type: string
                        type: array
                    type: object
                  oci:
                    description: OCI contains standard OCI image metadata.
                    properties:
                      authors:
                        description: |-
                          Authors is contact details of the authors.
                          Extracted from: org.opencontainers.image.authors
                        type: string
                      created:
                        description: |-
                          Created is the creation timestamp.
                          Extracted from: org.opencontainers.image.created
                        type: string
                      description:
                        description: |-
                          Description is a brief description.
                          Extracted from: org.opencontainers.image.description
                        type: string
                      documentation:
                        description: |-
                          Documentation is the URL to documentation.
                          Extracted from: org.opencontainers.image.documentation
                        type: string
                      licenses:
                        description: |-
                          Licenses is the SPDX license identifier(s).
                          Extracted from: org.opencontainers.image.licenses
                        type: string
                      revision:
                        description: |-
                          Revision is the source control revision.
                          Extracted from: org.opencontainers.image.revision
                        type: string
                      source:
                        description: |-
                          Source is the URL to the source code repository.
                          Extracted from: org.opencontainers.image.source
                        type: string
                      title:
                        description: |-
                          Title is the human-readable title.
                          Extracted from: org.opencontainers.image.title
                        type: string
                      vendor:
                        description: |-
                          Vendor is the organization that produced the image.
                          Extracted from: org.opencontainers.image.vendor
                        type: string
                      version:
                        description: |-
                          Version is the image version.
                          Extracted from: org.opencontainers.image.version
                        type: string
                    type: object
                  originalLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      OriginalLabels contains the raw OCI image labels as a JSON object.
                      This preserves all labels from the image, including those not mapped to structured fields.
                    type: object
                type: object
              imagePullSecrets:
                description: |-
                  ImagePullSecrets lists secrets containing credentials for pulling the model container image.
                  These secrets are used for:
                  - OCI registry metadata extraction during discovery
                  - Pulling the image for inference services
                  The secrets are merged with any runtime config defaults.
                  For namespace-scoped models, secrets must exist in the same namespace.
                  For cluster-scoped models, secrets must exist in the operator namespace.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              modelSources:
                description: |-
                  ModelSources specifies the model sources to use for this model.
                  When specified, these sources are used instead of auto-discovery from the container image.
                  This enables pre-creating custom models with explicit model sources.
                  The size field is optional - if not specified, it will be discovered by the download job.
                  AIM runtime currently supports only one model source.
                items:
                  description: |-
                    AIMModelSource describes a model artifact that must be downloaded for inference.
                    Discovery extracts these from the container's configuration to enable caching and validation.
                  properties:
                    env:
                      description: |-
                        Env specifies per-source credential overrides.
                        These variables are used for authentication when downloading this specific source.
                        Takes precedence over base-level env for the same variable name.
                      items:
                        description: EnvVar represents an environment variable present
                          in a Container.
                        properties:
                          name:
                            description: |-
                              Name of the environment variable.
                              May consist of any printable ASCII characters except '='.
                            type: string
                          value:
                            description: |-
                              Variable references $(VAR_NAME) are expanded
                              using the previously defined environment variables in the container and
                              any service environment variables. If a variable cannot be resolved,
                              the reference in the input string will be unchanged. Double $$ are reduced
                              to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                              "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                              Escaped references will never be expanded, regardless of whether the variable
                              exists or not.
                              Defaults to "".
                            type: string
                          valueFrom:
                            description: Source for the environment variable's value.
                              Cannot be used if value is not empty.
                            properties:
                              configMapKeyRef:
                                description: Selects a key of a ConfigMap.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the ConfigMap or
                                      its key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              fieldRef:
                                description: |-
                                  Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                  spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                properties:
                                  apiVersion:
                                    description: Version of the schema the FieldPath
                                      is written in terms of, defaults to "v1".
                                    type: string
                                  fieldPath:
                                    description: Path of the field to select in the
                                      specified API version.
                                    type: string
                                required:
                                - fieldPath
                                type: object
                                x-kubernetes-map-type: atomic
                              fileKeyRef:
                                description: |-
                                  FileKeyRef selects a key of the env file.
                                  Requires the EnvFiles feature gate to be enabled.
                                properties:
                                  key:
                                    description: |-
                                      The key within the env file. An invalid key will prevent the pod from starting.
                                      The keys defined within a source may consist of any printable ASCII characters except '='.
                                      During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                    type: string
                                  optional:
                                    default: false
                                    description: |-
                                      Specify whether the file or its key must be defined. If the file or key
                                      does not exist, then the env var is not published.
                                      If optional is set to true and the specified key does not exist,
                                      the environment variable will not be set in the Pod's containers.

                                      If optional is set to false and the specified key does not exist,
                                      an error will be returned during Pod creation.
                                    type: boolean
                                  path:
                                    description: |-
                                      The path within the volume from which to select the file.
                                      Must be relative and may not contain the '..' path or start with '..'.
                                    type: string
                                  volumeName:
                                    description: The name of the volume mount containing
                                      the env file.
                                    type: string
                                required:
                                - key
                                - path
                                - volumeName
                                type: object
                                x-kubernetes-map-type: atomic
                              resourceFieldRef:
                                description: |-
                                  Selects a resource of the container: only resources limits and requests
                                  (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                properties:
                                  containerName:
                                    description: 'Container name: required for volumes,
                                      optional for env vars'
                                    type: string
                                  divisor:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: Specifies the output format of the
                                      exposed resources, defaults to "1"
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  resource:
                                    description: 'Required: resource to select'
                                    type: string
                                required:
                                - resource
                                type: object
                                x-kubernetes-map-type: atomic
                              secretKeyRef:
                                description: Selects a key of a secret in the pod's
                                  namespace
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                        required:
                        - name
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - name
                      x-kubernetes-list-type: map
                    modelId:
                      description: |-
                        ModelID is the canonical identifier in {org}/{name} format.
                        Determines the cache mount path: /workspace/cache/{modelId}
                        For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                        For S3 sources, users define their own organizational structure.
                      pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                      type: string
                    precision:
                      allOf:
                      - enum:
                        - auto
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                      - enum:
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                      description: |-
                        Precision describes the runtime precision this source is compatible with.
                        Used to match model sources to profiles during custom weight onboarding.
                      type: string
                    size:
                      anyOf:
                      - type: integer
                      - type: string
                      description: |-
                        Size is the expected storage space required for this model artifact.
                        Used for PVC sizing and capacity planning during cache creation.
                        Optional - if not specified, the download job will discover the size automatically.
                        Can be set explicitly to pre-allocate storage or override auto-discovery.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    sourceUri:
                      description: |-
                        SourceURI is the location from which the model should be downloaded.
                        Supported schemes:
                        - hf://org/model - Hugging Face Hub model
                        - s3://bucket/key - S3-compatible storage
                      pattern: ^(hf|s3)://[^ \t\r\n]+$
                      type: string
                  required:
                  - modelId
                  - sourceUri
                  type: object
                maxItems: 1
                type: array
              profileCopy:
                description: |-
                  ProfileCopy reuses the AIMProfileSet derivation shape so an AIMModel can
                  publish derivative AIMProfiles directly. The controller may synthesize a
                  child AIMProfileSet and fill SourceRef when image-backed discovery is
                  involved. Mutually exclusive with all deprecated v1alpha1 fields.

                  DEPRECATED on v1alpha2: use spec.profiles. v1alpha1 still accepts
                  ProfileCopy. v1alpha2 CRD CEL forbids ProfileCopy.
                properties:
                  image:
                    description: Image overrides the runtime image used by the derived
                      profiles.
                    type: string
                  imagePullSecrets:
                    description: ImagePullSecrets lists secrets used for inspecting
                      and pulling container images.
                    items:
                      description: |-
                        LocalObjectReference contains enough information to let you locate the
                        referenced object inside the same namespace.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    type: array
                  overrides:
                    description: Overrides mutates the copied profile spec after selection
                      and version filtering.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount replaces the copied profile's
                          acceleratorCount.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel replaces the copied profile's
                          acceleratorModel.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode replaces the copied profile's
                          acceleratorPartitioningMode. Complete replacement, not a merge; an empty
                          override string leaves the source profile's mode untouched. Same reserved
                          values as AIMProfileSpecCommon.AcceleratorPartitioningMode. Whenever this
                          override is set (to any value, including "unpartitioned"), the CEL rule on the
                          enclosing spec also requires AcceleratorCount to be set: partition mode
                          changes the per-unit interpretation of acceleratorCount, and CEL cannot
                          read the base profile to tell whether the meaning actually changed, so it
                          conservatively requires the count be restated.
                        type: string
                      aimId:
                        description: |-
                          AimId stamps the derived profile's spec.aimId. When set, wins over
                          the source profile's aimId. REQUIRED when the enclosing selector has
                          role=base (the source base profile carries no aimId of its own).
                        type: string
                      containerEnv:
                        description: ContainerEnv merges by env var name, overriding
                          matching source entries.
                        items:
                          description: EnvVar represents an environment variable present
                            in a Container.
                          properties:
                            name:
                              description: |-
                                Name of the environment variable.
                                May consist of any printable ASCII characters except '='.
                              type: string
                            value:
                              description: |-
                                Variable references $(VAR_NAME) are expanded
                                using the previously defined environment variables in the container and
                                any service environment variables. If a variable cannot be resolved,
                                the reference in the input string will be unchanged. Double $$ are reduced
                                to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                Escaped references will never be expanded, regardless of whether the variable
                                exists or not.
                                Defaults to "".
                              type: string
                            valueFrom:
                              description: Source for the environment variable's value.
                                Cannot be used if value is not empty.
                              properties:
                                configMapKeyRef:
                                  description: Selects a key of a ConfigMap.
                                  properties:
                                    key:
                                      description: The key to select.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the ConfigMap or
                                        its key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fieldRef:
                                  description: |-
                                    Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                    spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                  properties:
                                    apiVersion:
                                      description: Version of the schema the FieldPath
                                        is written in terms of, defaults to "v1".
                                      type: string
                                    fieldPath:
                                      description: Path of the field to select in
                                        the specified API version.
                                      type: string
                                  required:
                                  - fieldPath
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fileKeyRef:
                                  description: |-
                                    FileKeyRef selects a key of the env file.
                                    Requires the EnvFiles feature gate to be enabled.
                                  properties:
                                    key:
                                      description: |-
                                        The key within the env file. An invalid key will prevent the pod from starting.
                                        The keys defined within a source may consist of any printable ASCII characters except '='.
                                        During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                      type: string
                                    optional:
                                      default: false
                                      description: |-
                                        Specify whether the file or its key must be defined. If the file or key
                                        does not exist, then the env var is not published.
                                        If optional is set to true and the specified key does not exist,
                                        the environment variable will not be set in the Pod's containers.

                                        If optional is set to false and the specified key does not exist,
                                        an error will be returned during Pod creation.
                                      type: boolean
                                    path:
                                      description: |-
                                        The path within the volume from which to select the file.
                                        Must be relative and may not contain the '..' path or start with '..'.
                                      type: string
                                    volumeName:
                                      description: The name of the volume mount containing
                                        the env file.
                                      type: string
                                  required:
                                  - key
                                  - path
                                  - volumeName
                                  type: object
                                  x-kubernetes-map-type: atomic
                                resourceFieldRef:
                                  description: |-
                                    Selects a resource of the container: only resources limits and requests
                                    (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                  properties:
                                    containerName:
                                      description: 'Container name: required for volumes,
                                        optional for env vars'
                                      type: string
                                    divisor:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      description: Specifies the output format of
                                        the exposed resources, defaults to "1"
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    resource:
                                      description: 'Required: resource to select'
                                      type: string
                                  required:
                                  - resource
                                  type: object
                                  x-kubernetes-map-type: atomic
                                secretKeyRef:
                                  description: Selects a key of a secret in the pod's
                                    namespace
                                  properties:
                                    key:
                                      description: The key of the secret to select
                                        from.  Must be a valid secret key.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the Secret or its
                                        key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      engineArgs:
                        description: EngineArgs shallow-merges on top of the source
                          engineArgs, overriding matching keys.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      engineEnv:
                        additionalProperties:
                          type: string
                        description: EngineEnv merges by key, overriding matching
                          source entries.
                        type: object
                      image:
                        description: |-
                          Image overrides the runtime container image used by the derived
                          profiles. When empty the deployment image is rebased onto the source
                          profile's base-image (status.baseImage) so private mirrors stay
                          self-contained.
                        type: string
                      modelId:
                        description: |-
                          ModelId stamps the derived profile's spec.modelId. When set, wins
                          over both the source profile's modelId AND the auto-derivation from
                          modelSources[0].modelId. REQUIRED when the enclosing selector has
                          role=base.
                        type: string
                      modelSources:
                        description: |-
                          ModelSources replaces the copied profile's modelSources. When
                          modelSources[0].modelId is set and overrides.modelId is unset, the
                          derived profile's modelId is auto-derived from modelSources[0]; an
                          explicit overrides.modelId always wins.
                        items:
                          description: |-
                            AIMModelSource describes a model artifact that must be downloaded for inference.
                            Discovery extracts these from the container's configuration to enable caching and validation.
                          properties:
                            env:
                              description: |-
                                Env specifies per-source credential overrides.
                                These variables are used for authentication when downloading this specific source.
                                Takes precedence over base-level env for the same variable name.
                              items:
                                description: EnvVar represents an environment variable
                                  present in a Container.
                                properties:
                                  name:
                                    description: |-
                                      Name of the environment variable.
                                      May consist of any printable ASCII characters except '='.
                                    type: string
                                  value:
                                    description: |-
                                      Variable references $(VAR_NAME) are expanded
                                      using the previously defined environment variables in the container and
                                      any service environment variables. If a variable cannot be resolved,
                                      the reference in the input string will be unchanged. Double $$ are reduced
                                      to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                      "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                      Escaped references will never be expanded, regardless of whether the variable
                                      exists or not.
                                      Defaults to "".
                                    type: string
                                  valueFrom:
                                    description: Source for the environment variable's
                                      value. Cannot be used if value is not empty.
                                    properties:
                                      configMapKeyRef:
                                        description: Selects a key of a ConfigMap.
                                        properties:
                                          key:
                                            description: The key to select.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the ConfigMap
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fieldRef:
                                        description: |-
                                          Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                          spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                        properties:
                                          apiVersion:
                                            description: Version of the schema the
                                              FieldPath is written in terms of, defaults
                                              to "v1".
                                            type: string
                                          fieldPath:
                                            description: Path of the field to select
                                              in the specified API version.
                                            type: string
                                        required:
                                        - fieldPath
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fileKeyRef:
                                        description: |-
                                          FileKeyRef selects a key of the env file.
                                          Requires the EnvFiles feature gate to be enabled.
                                        properties:
                                          key:
                                            description: |-
                                              The key within the env file. An invalid key will prevent the pod from starting.
                                              The keys defined within a source may consist of any printable ASCII characters except '='.
                                              During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                            type: string
                                          optional:
                                            default: false
                                            description: |-
                                              Specify whether the file or its key must be defined. If the file or key
                                              does not exist, then the env var is not published.
                                              If optional is set to true and the specified key does not exist,
                                              the environment variable will not be set in the Pod's containers.

                                              If optional is set to false and the specified key does not exist,
                                              an error will be returned during Pod creation.
                                            type: boolean
                                          path:
                                            description: |-
                                              The path within the volume from which to select the file.
                                              Must be relative and may not contain the '..' path or start with '..'.
                                            type: string
                                          volumeName:
                                            description: The name of the volume mount
                                              containing the env file.
                                            type: string
                                        required:
                                        - key
                                        - path
                                        - volumeName
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      resourceFieldRef:
                                        description: |-
                                          Selects a resource of the container: only resources limits and requests
                                          (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                        properties:
                                          containerName:
                                            description: 'Container name: required
                                              for volumes, optional for env vars'
                                            type: string
                                          divisor:
                                            anyOf:
                                            - type: integer
                                            - type: string
                                            description: Specifies the output format
                                              of the exposed resources, defaults to
                                              "1"
                                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                            x-kubernetes-int-or-string: true
                                          resource:
                                            description: 'Required: resource to select'
                                            type: string
                                        required:
                                        - resource
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      secretKeyRef:
                                        description: Selects a key of a secret in
                                          the pod's namespace
                                        properties:
                                          key:
                                            description: The key of the secret to
                                              select from.  Must be a valid secret
                                              key.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the Secret
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                    type: object
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            modelId:
                              description: |-
                                ModelID is the canonical identifier in {org}/{name} format.
                                Determines the cache mount path: /workspace/cache/{modelId}
                                For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                                For S3 sources, users define their own organizational structure.
                              pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                              type: string
                            precision:
                              allOf:
                              - enum:
                                - auto
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              - enum:
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              description: |-
                                Precision describes the runtime precision this source is compatible with.
                                Used to match model sources to profiles during custom weight onboarding.
                              type: string
                            size:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                Size is the expected storage space required for this model artifact.
                                Used for PVC sizing and capacity planning during cache creation.
                                Optional - if not specified, the download job will discover the size automatically.
                                Can be set explicitly to pre-allocate storage or override auto-discovery.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            sourceUri:
                              description: |-
                                SourceURI is the location from which the model should be downloaded.
                                Supported schemes:
                                - hf://org/model - Hugging Face Hub model
                                - s3://bucket/key - S3-compatible storage
                              pattern: ^(hf|s3)://[^ \t\r\n]+$
                              type: string
                          required:
                          - modelId
                          - sourceUri
                          type: object
                        type: array
                      profileId:
                        description: |-
                          ProfileId stamps the derived profile's spec.profileId. Optional —
                          most callers leave this empty and let the source profile's profileId
                          carry through (or the reconciler synthesise one).
                        type: string
                    type: object
                  selector:
                    description: Selector chooses which source profiles to derive
                      from.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount filters by accelerator unit
                          count.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel filters by accelerator identifier.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode filters candidates by their declared
                          partitioning mode. Partial-order match (NOT strict equality):
                            ""              - no filter on this field.
                            "unpartitioned" - matches profiles with mode "" or "unpartitioned".
                            "partitioned"   - matches profiles whose mode is non-trivial (anything
                                              other than "" / "unpartitioned").
                            "<C>"           - selector-only convenience: matches profiles with mode
                                              "<C>-*" (prefix on the scheme). Not a valid profile-spec
                                              value (e.g. selector "CPX" matches "CPX-NPS1", "CPX-NPS4").
                            "<C>-<M>"       - exact-string match on the scheme.
                        type: string
                      acceleratorType:
                        description: AcceleratorType filters by accelerator resource
                          type.
                        enum:
                        - gpu
                        - cpu
                        type: string
                      aimId:
                        description: AimId filters by model architecture identifier
                          (e.g., "qwen/qwen3-32b").
                        type: string
                      engine:
                        description: Engine filters by inference engine.
                        type: string
                      engineArgs:
                        description: |-
                          EngineArgs partially matches source engineArgs: every provided top-level key must
                          exist in the source object with an equal value.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      metric:
                        description: Metric filters by optimization target.
                        enum:
                        - latency
                        - throughput
                        type: string
                      minimumType:
                        description: |-
                          MinimumType filters by a minimum optimization level: candidates whose
                          type is this tier OR BETTER are accepted (hierarchy: optimized > general
                          > preview > unoptimized). This is the floor counterpart to the exact-match
                          Type field; the two AND together when both are set.

                          The sentinel "any" disables the floor (accept every tier). When this
                          field is empty the AIMService resolver applies a default floor of
                          "optimized" so auto-selection prefers production-grade profiles and never
                          silently picks an unoptimized one; to opt a service into lower tiers
                          (e.g. CPU/EPYC profiles published as unoptimized) set minimumType
                          explicitly to "unoptimized" or "any". Derivation selectors
                          (AIMProfileSet / AIMModel.profiles) treat empty as "any" so copying is
                          never tier-restricted by default.
                        enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                        - any
                        type: string
                      modelId:
                        description: ModelId filters by the source profile's specific
                          model identifier.
                        type: string
                      modelRef:
                        description: |-
                          ModelRef narrows candidates to those produced by a specific
                          AIM(Cluster)Model, matched via the `aim.eai.amd.com/source-model[-scope]`
                          labels stamped by the AIMModel reconcilers. Iteration 1 (v1alpha2 only).
                        properties:
                          name:
                            description: Name is the owning AIM(Cluster)Model name.
                              Required.
                            minLength: 1
                            type: string
                          scope:
                            default: Auto
                            description: Scope controls how Name is resolved against
                              AIMModel vs AIMClusterModel.
                            enum:
                            - Auto
                            - Namespace
                            - Cluster
                            type: string
                        required:
                        - name
                        type: object
                      origin:
                        description: |-
                          Origin filters by the `aim.eai.amd.com/profile-origin` label. When unset
                          (empty) the selector does not filter by origin. Iteration 1 (v1alpha2
                          only).
                        enum:
                        - discovered
                        - derived
                        - user-authored
                        type: string
                      precision:
                        description: Precision filters by numeric precision.
                        enum:
                        - auto
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                        type: string
                      profileId:
                        description: ProfileId filters by the source profile's profile
                          identifier.
                        type: string
                      role:
                        default: deployable
                        description: |-
                          Role filters by the `aim.eai.amd.com/profile-role` label. Defaults to
                          `deployable`. `base` filters to base profiles emitted by base-image
                          discovery (custom-model derivation source material).
                        enum:
                        - base
                        - deployable
                        type: string
                      type:
                        description: Type filters by optimization level (exact match).
                        enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                        type: string
                    type: object
                  serviceAccountName:
                    description: ServiceAccountName is propagated to managed profiles
                      for downstream workloads.
                    type: string
                  sourceRef:
                    description: |-
                      SourceRef points to an alternate discovery cache source.
                      When omitted, derivation uses visible AIMProfile and AIMClusterProfile objects.
                      For AIMProfileSet, the referenced ConfigMap is read from the same namespace.
                      For AIMClusterProfileSet, it is read from the operator namespace.
                    properties:
                      name:
                        description: Name is the discovery cache ConfigMap name.
                        type: string
                    required:
                    - name
                    type: object
                  version:
                    description: Version pins matching to a specific source profile
                      version when VersionPolicy is pinned.
                    type: string
                  versionPolicy:
                    default: pinned
                    description: VersionPolicy controls how matching profiles are
                      filtered by version.
                    enum:
                    - pinned
                    - latest
                    - all
                    type: string
                type: object
                x-kubernetes-validations:
                - message: selector must set at least one matching field, or set sourceRef
                  rule: has(self.sourceRef) || has(self.selector.aimId) || has(self.selector.modelId)
                    || has(self.selector.profileId) || has(self.selector.engine) ||
                    has(self.selector.metric) || has(self.selector.precision) || has(self.selector.type)
                    || has(self.selector.acceleratorModel) || has(self.selector.acceleratorType)
                    || has(self.selector.acceleratorCount) || has(self.selector.engineArgs)
                    || has(self.selector.modelRef) || has(self.selector.origin)
                - message: version is required when versionPolicy is pinned
                  rule: has(self.version) || (has(self.versionPolicy) && self.versionPolicy
                    != 'pinned')
                - message: latest/all versionPolicy must not set version
                  rule: '!has(self.versionPolicy) || (self.versionPolicy != ''latest''
                    && self.versionPolicy != ''all'') || !has(self.version)'
                - message: selector.aimId/modelId/profileId are not allowed when selector.role=base;
                    set overrides.aimId/modelId/profileId instead (base profiles have
                    no source identity to filter on)
                  rule: '!(has(self.selector.role) && self.selector.role == ''base'')
                    || (!has(self.selector.aimId) && !has(self.selector.modelId) &&
                    !has(self.selector.profileId))'
                - message: selector.role=base requires overrides.aimId and overrides.modelId
                    (base profiles carry no identity of their own; overrides supply
                    it)
                  rule: '!(has(self.selector.role) && self.selector.role == ''base'')
                    || (has(self.overrides) && has(self.overrides.aimId) && has(self.overrides.modelId))'
                - message: overrides.acceleratorCount must be specified together with
                    any overrides.acceleratorPartitioningMode; partition mode changes
                    the per-unit interpretation of acceleratorCount
                  rule: '!has(self.overrides) || !has(self.overrides.acceleratorPartitioningMode)
                    || size(self.overrides.acceleratorPartitioningMode) == 0 || has(self.overrides.acceleratorCount)'
              profiles:
                description: |-
                  Profiles is the v1alpha2 fine-tune / custom-model onboarding surface.
                  It groups the source descriptor (`profiles.derivedFrom.selector` /
                  `profiles.derivedFrom.sourceRef`), the version filter
                  (`profiles.versionPolicy` / `profiles.version`), and the modifications
                  applied to copies (`profiles.overrides`). When set, the AIMModel
                  reconciler synthesises a child AIMProfileSet from this block.

                  Mutually exclusive with spec.image (exactly one of the two is required
                  for v1alpha2 AIMModel). v1alpha1 rejects spec.profiles via per-version
                  CEL.
                properties:
                  derivedFrom:
                    description: DerivedFrom identifies the source profiles to copy
                      from.
                    properties:
                      selector:
                        description: |-
                          Selector chooses which source profiles to derive from. Discovery of a
                          deployable AIM image stamps role=deployable; selector.role=base targets
                          the base profiles emitted by base-image discovery (custom-model
                          derivation source material).
                        properties:
                          acceleratorCount:
                            description: AcceleratorCount filters by accelerator unit
                              count.
                            format: int32
                            type: integer
                          acceleratorModel:
                            description: AcceleratorModel filters by accelerator identifier.
                            type: string
                          acceleratorPartitioningMode:
                            description: |-
                              AcceleratorPartitioningMode filters candidates by their declared
                              partitioning mode. Partial-order match (NOT strict equality):
                                ""              - no filter on this field.
                                "unpartitioned" - matches profiles with mode "" or "unpartitioned".
                                "partitioned"   - matches profiles whose mode is non-trivial (anything
                                                  other than "" / "unpartitioned").
                                "<C>"           - selector-only convenience: matches profiles with mode
                                                  "<C>-*" (prefix on the scheme). Not a valid profile-spec
                                                  value (e.g. selector "CPX" matches "CPX-NPS1", "CPX-NPS4").
                                "<C>-<M>"       - exact-string match on the scheme.
                            type: string
                          acceleratorType:
                            description: AcceleratorType filters by accelerator resource
                              type.
                            enum:
                            - gpu
                            - cpu
                            type: string
                          aimId:
                            description: AimId filters by model architecture identifier
                              (e.g., "qwen/qwen3-32b").
                            type: string
                          engine:
                            description: Engine filters by inference engine.
                            type: string
                          engineArgs:
                            description: |-
                              EngineArgs partially matches source engineArgs: every provided top-level key must
                              exist in the source object with an equal value.
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          metric:
                            description: Metric filters by optimization target.
                            enum:
                            - latency
                            - throughput
                            type: string
                          minimumType:
                            description: |-
                              MinimumType filters by a minimum optimization level: candidates whose
                              type is this tier OR BETTER are accepted (hierarchy: optimized > general
                              > preview > unoptimized). This is the floor counterpart to the exact-match
                              Type field; the two AND together when both are set.

                              The sentinel "any" disables the floor (accept every tier). When this
                              field is empty the AIMService resolver applies a default floor of
                              "optimized" so auto-selection prefers production-grade profiles and never
                              silently picks an unoptimized one; to opt a service into lower tiers
                              (e.g. CPU/EPYC profiles published as unoptimized) set minimumType
                              explicitly to "unoptimized" or "any". Derivation selectors
                              (AIMProfileSet / AIMModel.profiles) treat empty as "any" so copying is
                              never tier-restricted by default.
                            enum:
                            - optimized
                            - general
                            - preview
                            - unoptimized
                            - any
                            type: string
                          modelId:
                            description: ModelId filters by the source profile's specific
                              model identifier.
                            type: string
                          modelRef:
                            description: |-
                              ModelRef narrows candidates to those produced by a specific
                              AIM(Cluster)Model, matched via the `aim.eai.amd.com/source-model[-scope]`
                              labels stamped by the AIMModel reconcilers. Iteration 1 (v1alpha2 only).
                            properties:
                              name:
                                description: Name is the owning AIM(Cluster)Model
                                  name. Required.
                                minLength: 1
                                type: string
                              scope:
                                default: Auto
                                description: Scope controls how Name is resolved against
                                  AIMModel vs AIMClusterModel.
                                enum:
                                - Auto
                                - Namespace
                                - Cluster
                                type: string
                            required:
                            - name
                            type: object
                          origin:
                            description: |-
                              Origin filters by the `aim.eai.amd.com/profile-origin` label. When unset
                              (empty) the selector does not filter by origin. Iteration 1 (v1alpha2
                              only).
                            enum:
                            - discovered
                            - derived
                            - user-authored
                            type: string
                          precision:
                            description: Precision filters by numeric precision.
                            enum:
                            - auto
                            - fp4
                            - fp8
                            - fp16
                            - fp32
                            - bf16
                            - int4
                            - int8
                            type: string
                          profileId:
                            description: ProfileId filters by the source profile's
                              profile identifier.
                            type: string
                          role:
                            default: deployable
                            description: |-
                              Role filters by the `aim.eai.amd.com/profile-role` label. Defaults to
                              `deployable`. `base` filters to base profiles emitted by base-image
                              discovery (custom-model derivation source material).
                            enum:
                            - base
                            - deployable
                            type: string
                          type:
                            description: Type filters by optimization level (exact
                              match).
                            enum:
                            - optimized
                            - general
                            - preview
                            - unoptimized
                            type: string
                        type: object
                      sourceRef:
                        description: |-
                          SourceRef points to an alternate discovery cache source (a
                          pre-populated ConfigMap of profile YAMLs) instead of using the
                          visible AIMProfile / AIMClusterProfile objects.
                        properties:
                          name:
                            description: Name is the discovery cache ConfigMap name.
                            type: string
                        required:
                        - name
                        type: object
                    type: object
                  overrides:
                    description: |-
                      Overrides mutates the copied profile spec after selection and version
                      filtering. Use overrides.image to override the deployment container
                      image used by the derived profiles.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount replaces the copied profile's
                          acceleratorCount.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel replaces the copied profile's
                          acceleratorModel.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode replaces the copied profile's
                          acceleratorPartitioningMode. Complete replacement, not a merge; an empty
                          override string leaves the source profile's mode untouched. Same reserved
                          values as AIMProfileSpecCommon.AcceleratorPartitioningMode. Whenever this
                          override is set (to any value, including "unpartitioned"), the CEL rule on the
                          enclosing spec also requires AcceleratorCount to be set: partition mode
                          changes the per-unit interpretation of acceleratorCount, and CEL cannot
                          read the base profile to tell whether the meaning actually changed, so it
                          conservatively requires the count be restated.
                        type: string
                      aimId:
                        description: |-
                          AimId stamps the derived profile's spec.aimId. When set, wins over
                          the source profile's aimId. REQUIRED when the enclosing selector has
                          role=base (the source base profile carries no aimId of its own).
                        type: string
                      containerEnv:
                        description: ContainerEnv merges by env var name, overriding
                          matching source entries.
                        items:
                          description: EnvVar represents an environment variable present
                            in a Container.
                          properties:
                            name:
                              description: |-
                                Name of the environment variable.
                                May consist of any printable ASCII characters except '='.
                              type: string
                            value:
                              description: |-
                                Variable references $(VAR_NAME) are expanded
                                using the previously defined environment variables in the container and
                                any service environment variables. If a variable cannot be resolved,
                                the reference in the input string will be unchanged. Double $$ are reduced
                                to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                Escaped references will never be expanded, regardless of whether the variable
                                exists or not.
                                Defaults to "".
                              type: string
                            valueFrom:
                              description: Source for the environment variable's value.
                                Cannot be used if value is not empty.
                              properties:
                                configMapKeyRef:
                                  description: Selects a key of a ConfigMap.
                                  properties:
                                    key:
                                      description: The key to select.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the ConfigMap or
                                        its key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fieldRef:
                                  description: |-
                                    Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                    spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                  properties:
                                    apiVersion:
                                      description: Version of the schema the FieldPath
                                        is written in terms of, defaults to "v1".
                                      type: string
                                    fieldPath:
                                      description: Path of the field to select in
                                        the specified API version.
                                      type: string
                                  required:
                                  - fieldPath
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fileKeyRef:
                                  description: |-
                                    FileKeyRef selects a key of the env file.
                                    Requires the EnvFiles feature gate to be enabled.
                                  properties:
                                    key:
                                      description: |-
                                        The key within the env file. An invalid key will prevent the pod from starting.
                                        The keys defined within a source may consist of any printable ASCII characters except '='.
                                        During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                      type: string
                                    optional:
                                      default: false
                                      description: |-
                                        Specify whether the file or its key must be defined. If the file or key
                                        does not exist, then the env var is not published.
                                        If optional is set to true and the specified key does not exist,
                                        the environment variable will not be set in the Pod's containers.

                                        If optional is set to false and the specified key does not exist,
                                        an error will be returned during Pod creation.
                                      type: boolean
                                    path:
                                      description: |-
                                        The path within the volume from which to select the file.
                                        Must be relative and may not contain the '..' path or start with '..'.
                                      type: string
                                    volumeName:
                                      description: The name of the volume mount containing
                                        the env file.
                                      type: string
                                  required:
                                  - key
                                  - path
                                  - volumeName
                                  type: object
                                  x-kubernetes-map-type: atomic
                                resourceFieldRef:
                                  description: |-
                                    Selects a resource of the container: only resources limits and requests
                                    (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                  properties:
                                    containerName:
                                      description: 'Container name: required for volumes,
                                        optional for env vars'
                                      type: string
                                    divisor:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      description: Specifies the output format of
                                        the exposed resources, defaults to "1"
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    resource:
                                      description: 'Required: resource to select'
                                      type: string
                                  required:
                                  - resource
                                  type: object
                                  x-kubernetes-map-type: atomic
                                secretKeyRef:
                                  description: Selects a key of a secret in the pod's
                                    namespace
                                  properties:
                                    key:
                                      description: The key of the secret to select
                                        from.  Must be a valid secret key.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the Secret or its
                                        key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      engineArgs:
                        description: EngineArgs shallow-merges on top of the source
                          engineArgs, overriding matching keys.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      engineEnv:
                        additionalProperties:
                          type: string
                        description: EngineEnv merges by key, overriding matching
                          source entries.
                        type: object
                      image:
                        description: |-
                          Image overrides the runtime container image used by the derived
                          profiles. When empty the deployment image is rebased onto the source
                          profile's base-image (status.baseImage) so private mirrors stay
                          self-contained.
                        type: string
                      modelId:
                        description: |-
                          ModelId stamps the derived profile's spec.modelId. When set, wins
                          over both the source profile's modelId AND the auto-derivation from
                          modelSources[0].modelId. REQUIRED when the enclosing selector has
                          role=base.
                        type: string
                      modelSources:
                        description: |-
                          ModelSources replaces the copied profile's modelSources. When
                          modelSources[0].modelId is set and overrides.modelId is unset, the
                          derived profile's modelId is auto-derived from modelSources[0]; an
                          explicit overrides.modelId always wins.
                        items:
                          description: |-
                            AIMModelSource describes a model artifact that must be downloaded for inference.
                            Discovery extracts these from the container's configuration to enable caching and validation.
                          properties:
                            env:
                              description: |-
                                Env specifies per-source credential overrides.
                                These variables are used for authentication when downloading this specific source.
                                Takes precedence over base-level env for the same variable name.
                              items:
                                description: EnvVar represents an environment variable
                                  present in a Container.
                                properties:
                                  name:
                                    description: |-
                                      Name of the environment variable.
                                      May consist of any printable ASCII characters except '='.
                                    type: string
                                  value:
                                    description: |-
                                      Variable references $(VAR_NAME) are expanded
                                      using the previously defined environment variables in the container and
                                      any service environment variables. If a variable cannot be resolved,
                                      the reference in the input string will be unchanged. Double $$ are reduced
                                      to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                      "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                      Escaped references will never be expanded, regardless of whether the variable
                                      exists or not.
                                      Defaults to "".
                                    type: string
                                  valueFrom:
                                    description: Source for the environment variable's
                                      value. Cannot be used if value is not empty.
                                    properties:
                                      configMapKeyRef:
                                        description: Selects a key of a ConfigMap.
                                        properties:
                                          key:
                                            description: The key to select.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the ConfigMap
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fieldRef:
                                        description: |-
                                          Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                          spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                        properties:
                                          apiVersion:
                                            description: Version of the schema the
                                              FieldPath is written in terms of, defaults
                                              to "v1".
                                            type: string
                                          fieldPath:
                                            description: Path of the field to select
                                              in the specified API version.
                                            type: string
                                        required:
                                        - fieldPath
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fileKeyRef:
                                        description: |-
                                          FileKeyRef selects a key of the env file.
                                          Requires the EnvFiles feature gate to be enabled.
                                        properties:
                                          key:
                                            description: |-
                                              The key within the env file. An invalid key will prevent the pod from starting.
                                              The keys defined within a source may consist of any printable ASCII characters except '='.
                                              During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                            type: string
                                          optional:
                                            default: false
                                            description: |-
                                              Specify whether the file or its key must be defined. If the file or key
                                              does not exist, then the env var is not published.
                                              If optional is set to true and the specified key does not exist,
                                              the environment variable will not be set in the Pod's containers.

                                              If optional is set to false and the specified key does not exist,
                                              an error will be returned during Pod creation.
                                            type: boolean
                                          path:
                                            description: |-
                                              The path within the volume from which to select the file.
                                              Must be relative and may not contain the '..' path or start with '..'.
                                            type: string
                                          volumeName:
                                            description: The name of the volume mount
                                              containing the env file.
                                            type: string
                                        required:
                                        - key
                                        - path
                                        - volumeName
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      resourceFieldRef:
                                        description: |-
                                          Selects a resource of the container: only resources limits and requests
                                          (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                        properties:
                                          containerName:
                                            description: 'Container name: required
                                              for volumes, optional for env vars'
                                            type: string
                                          divisor:
                                            anyOf:
                                            - type: integer
                                            - type: string
                                            description: Specifies the output format
                                              of the exposed resources, defaults to
                                              "1"
                                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                            x-kubernetes-int-or-string: true
                                          resource:
                                            description: 'Required: resource to select'
                                            type: string
                                        required:
                                        - resource
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      secretKeyRef:
                                        description: Selects a key of a secret in
                                          the pod's namespace
                                        properties:
                                          key:
                                            description: The key of the secret to
                                              select from.  Must be a valid secret
                                              key.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the Secret
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                    type: object
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            modelId:
                              description: |-
                                ModelID is the canonical identifier in {org}/{name} format.
                                Determines the cache mount path: /workspace/cache/{modelId}
                                For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                                For S3 sources, users define their own organizational structure.
                              pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                              type: string
                            precision:
                              allOf:
                              - enum:
                                - auto
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              - enum:
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              description: |-
                                Precision describes the runtime precision this source is compatible with.
                                Used to match model sources to profiles during custom weight onboarding.
                              type: string
                            size:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                Size is the expected storage space required for this model artifact.
                                Used for PVC sizing and capacity planning during cache creation.
                                Optional - if not specified, the download job will discover the size automatically.
                                Can be set explicitly to pre-allocate storage or override auto-discovery.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            sourceUri:
                              description: |-
                                SourceURI is the location from which the model should be downloaded.
                                Supported schemes:
                                - hf://org/model - Hugging Face Hub model
                                - s3://bucket/key - S3-compatible storage
                              pattern: ^(hf|s3)://[^ \t\r\n]+$
                              type: string
                          required:
                          - modelId
                          - sourceUri
                          type: object
                        type: array
                      profileId:
                        description: |-
                          ProfileId stamps the derived profile's spec.profileId. Optional —
                          most callers leave this empty and let the source profile's profileId
                          carry through (or the reconciler synthesise one).
                        type: string
                    type: object
                  version:
                    description: |-
                      Version pins matching to a specific source profile version when
                      VersionPolicy is `pinned`.
                    type: string
                  versionPolicy:
                    default: pinned
                    description: VersionPolicy controls how matching profiles are
                      filtered by version.
                    enum:
                    - pinned
                    - latest
                    - all
                    type: string
                type: object
                x-kubernetes-validations:
                - message: profiles.derivedFrom is required when spec.profiles is
                    set
                  rule: has(self.derivedFrom)
                - message: profiles.version is required when versionPolicy is pinned
                  rule: has(self.version) || (has(self.versionPolicy) && self.versionPolicy
                    != 'pinned')
                - message: latest/all versionPolicy must not set version
                  rule: '!has(self.versionPolicy) || (self.versionPolicy != ''latest''
                    && self.versionPolicy != ''all'') || !has(self.version)'
                - message: derivedFrom.selector.aimId/modelId/profileId are not allowed
                    when selector.role=base; set overrides.aimId/modelId/profileId
                    instead (base profiles have no source identity to filter on)
                  rule: '!(has(self.derivedFrom.selector.role) && self.derivedFrom.selector.role
                    == ''base'') || (!has(self.derivedFrom.selector.aimId) && !has(self.derivedFrom.selector.modelId)
                    && !has(self.derivedFrom.selector.profileId))'
                - message: derivedFrom.selector.role=base requires overrides.aimId
                    and overrides.modelId (base profiles carry no identity of their
                    own; overrides supply it)
                  rule: '!(has(self.derivedFrom.selector.role) && self.derivedFrom.selector.role
                    == ''base'') || (has(self.overrides) && has(self.overrides.aimId)
                    && has(self.overrides.modelId))'
              resources:
                description: |-
                  Resources defines the default resource requirements for services using this model.
                  Template- or service-level values override these defaults.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              runtimeConfigName:
                description: |-
                  Name is the name of the runtime config to use for this resource. If a runtime config with this name exists both
                  as a namespace and a cluster runtime config, the values are merged together, the namespace config taking priority
                  over the cluster config when there are conflicts. If this field is empty or set to `default`, the namespace / cluster
                  runtime config with the name `default` is used, if it exists.
                type: string
              serviceAccountName:
                description: |-
                  ServiceAccountName specifies the Kubernetes service account to use for workloads related to this model.
                  This includes metadata extraction jobs and any other model-related operations.
                  If empty, the default service account for the namespace is used.
                type: string
            type: object
            x-kubernetes-validations:
            - message: when using modelSources without aimId, set custom.hardware
                or set hardware on each customTemplate
              rule: '!has(self.modelSources) || size(self.modelSources) == 0 || has(self.aimId)
                || (has(self.custom) && has(self.custom.hardware)) || !has(self.customTemplates)
                || size(self.customTemplates) == 0 || self.customTemplates.all(t,
                has(t.hardware) || (has(self.custom) && has(self.custom.hardware)))'
            - message: image is required unless aimId is set with versionPolicy latest
                or any, or profileCopy/derivedFrom/profiles is set
              rule: (has(self.image) && size(self.image) > 0) || has(self.profileCopy)
                || has(self.derivedFrom) || has(self.profiles) || (has(self.aimId)
                && has(self.custom) && has(self.custom.versionPolicy) && self.custom.versionPolicy
                != 'pinned')
            - message: profileCopy cannot be combined with deprecated legacy AIMModel
                fields
              rule: '!has(self.profileCopy) || (!has(self.discovery) && !has(self.defaultServiceTemplate)
                && !has(self.custom) && (!has(self.customTemplates) || size(self.customTemplates)
                == 0) && (!has(self.modelSources) || size(self.modelSources) == 0)
                && (!has(self.runtimeConfigName) || size(self.runtimeConfigName) ==
                0) && (!has(self.env) || size(self.env) == 0) && !has(self.imageMetadata))'
          status:
            description: AIMModelStatus defines the observed state of AIMModel.
            properties:
              aimId:
                description: |-
                  AimId is the resolved model architecture identifier for this model.
                  Populated by the v1alpha2 controller from spec.aimId or discovered metadata.
                type: string
              baseImage:
                description: |-
                  BaseImage is the extracted AIM base image reference (AIM_BASE_IMAGE_REF) when known.
                  Used when resolving deployment images for fine-tuned models that have no spec.image.
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the model's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              discoveredProfiles:
                description: DiscoveredProfiles summarizes the profiles found during
                  image discovery.
                properties:
                  byHardware:
                    description: |-
                      ByHardware groups discovered profiles by their hardware footprint and
                      reports whether each group is currently supported by the cluster. The
                      groups are stable across reconciles (sorted by acceleratorType,
                      acceleratorModel, acceleratorCount) so kubectl/jq queries are cheap.
                      Useful when debugging "why doesn't the {metric, precision} profile I
                      expect appear in my cluster?" — the breakdown surfaces every shape the
                      image emits, not only the ones materialised as AIMProfile objects.
                    items:
                      description: |-
                        ProfileHardwareGroup is one accelerator footprint within a model's
                        discovery catalog, with the metric/precision combos shipped under it.
                      properties:
                        acceleratorCount:
                          description: |-
                            AcceleratorCount is the number of accelerator units the profile
                            requests. For gpu: device count (e.g., 1, 2, 4, 8 for tensor-parallel
                            sizes). For cpu: number of CPU cores (e.g., 128 for EPYC_ZEN5,
                            192 for EPYC_9965).
                          format: int32
                          type: integer
                        acceleratorModel:
                          description: |-
                            AcceleratorModel is the accelerator identifier (e.g., "MI300X",
                            "EPYC_ZEN5"). Empty for profiles with no accelerator requirement.
                          type: string
                        acceleratorType:
                          description: AcceleratorType is the resource family (gpu,
                            cpu).
                          enum:
                          - gpu
                          - cpu
                          type: string
                        profiles:
                          description: |-
                            Profiles lists the {metric, precision} combinations discovered under
                            this hardware footprint. Reported even when the group is unsupported
                            so users can see what they're missing.
                          items:
                            description: |-
                              ProfileHardwareGroupEntry identifies one profile within a hardware group
                              by its {metric, precision} pair. Sufficient for users to spot whether the
                              optimization variant they want is shipped at all.
                            properties:
                              metric:
                                description: Metric is the optimization target (latency,
                                  throughput).
                                enum:
                                - latency
                                - throughput
                                type: string
                              precision:
                                description: Precision is the numeric precision (fp4,
                                  fp8, bf16, …).
                                enum:
                                - auto
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                                type: string
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        supported:
                          description: |-
                            Supported reports whether this hardware footprint is currently
                            satisfied by at least one cluster node. When false, all profiles in
                            this group are skipped during materialisation.
                          type: boolean
                      required:
                      - supported
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  supported:
                    description: Supported is the number of discovered profiles currently
                      supported by the cluster.
                    format: int32
                    type: integer
                  total:
                    description: Total is the number of profiles discovered from the
                      image.
                    format: int32
                    type: integer
                  unsupported:
                    description: Unsupported is the number of discovered profiles
                      currently not supported by the cluster.
                    format: int32
                    type: integer
                type: object
              discovery:
                description: Discovery tracks the state of the image-discovery Job
                  used to populate the discovery cache.
                properties:
                  attempts:
                    description: |-
                      Attempts is the number of discovery job attempts that have been made.
                      Increments each time a new discovery job is created after a failure.
                    format: int32
                    type: integer
                  lastAttemptTime:
                    description: |-
                      LastAttemptTime is the timestamp of the most recent discovery job creation.
                      Used to calculate exponential backoff before the next retry.
                    format: date-time
                    type: string
                  lastFailureReason:
                    description: LastFailureReason captures the reason for the most
                      recent discovery failure.
                    type: string
                  specHash:
                    description: |-
                      SpecHash is a hash of the model spec fields that invalidate cached discovery
                      (image, imagePullSecrets, serviceAccountName, discoveryCommandVersion).
                      When it changes, the operator drops the cache and re-runs the discovery Job.
                    type: string
                type: object
              discoveryCacheRef:
                description: |-
                  DiscoveryCacheRef points at the normalized discovery cache ConfigMap for image-backed flows.
                  Populated by the v1alpha2 controller after image inspection succeeds.
                properties:
                  name:
                    description: Name is the ConfigMap name.
                    type: string
                  namespace:
                    description: Namespace is the ConfigMap namespace.
                    type: string
                type: object
              imageMetadata:
                description: ImageMetadata is the metadata extracted from an AIM image
                properties:
                  baseImageRef:
                    description: |-
                      BaseImageRef is the value of the AIM_BASE_IMAGE_REF environment variable
                      baked into the image's OCI config at build time. For AIM model images this
                      records the base image (e.g. "ghcr.io/silogen/aim-base:0.8.5") that the
                      model image was built from. Used by the AIMModel controller to resolve
                      the deployment image for fine-tuned models whose spec.image is omitted
                      (versionPolicy=latest or any).
                    type: string
                  model:
                    description: Model contains AMD Silogen model-specific metadata.
                    properties:
                      canonicalName:
                        description: |-
                          CanonicalName is the canonical model identifier (e.g., mistralai/Mixtral-8x22B-Instruct-v0.1).
                          Extracted from: org.amd.silogen.model.canonicalName
                        type: string
                      descriptionFull:
                        description: |-
                          DescriptionFull is the full description.
                          Extracted from: org.amd.silogen.description.full
                        type: string
                      hfTokenRequired:
                        description: |-
                          HFTokenRequired indicates if a HuggingFace token is required.
                          Extracted from: org.amd.silogen.hfToken.required
                        type: boolean
                      recommendedDeployments:
                        description: |-
                          RecommendedDeployments contains recommended deployment configurations.
                          Extracted from: org.amd.silogen.model.recommendedDeployments (parsed from JSON array)
                        items:
                          description: RecommendedDeployment describes a recommended
                            deployment configuration for a model.
                          properties:
                            description:
                              description: Description provides additional context
                                about this deployment configuration
                              type: string
                            gpuCount:
                              description: GPUCount is the number of GPUs required
                              format: int32
                              type: integer
                            gpuModel:
                              description: |-
                                GPUModel is the GPU model name (e.g., MI300X, MI325X).
                                The legacy v1alpha1 schema only models GPU accelerators; CPU profiles
                                emitted via OCI labels (gpuModel="CPU") are not first-class here and
                                are handled exclusively by the v1alpha2 native discovery pipeline.
                              type: string
                            metric:
                              description: Metric is the optimization target (e.g.,
                                latency, throughput)
                              type: string
                            precision:
                              description: Precision is the recommended precision
                                (e.g., fp8, fp16, bf16)
                              type: string
                            profileId:
                              description: |-
                                ProfileId is the unique identifier of the AIM profile for this deployment.
                                When set, templates created from this deployment will use this profile ID
                                to deterministically select the correct runtime profile in the AIM container.
                              type: string
                          type: object
                        type: array
                      releaseNotes:
                        description: |-
                          ReleaseNotes contains release notes for this version.
                          Extracted from: org.amd.silogen.release.notes
                        type: string
                      source:
                        description: |-
                          Source is the URL where the model can be found.
                          Extracted from: org.amd.silogen.model.source
                        type: string
                      tags:
                        description: |-
                          Tags are descriptive tags (e.g., ["text-generation", "chat", "instruction"]).
                          Extracted from: org.amd.silogen.model.tags (comma-separated)
                        items:
                          type: string
                        type: array
                      title:
                        description: |-
                          Title is the Silogen-specific title for the model.
                          Extracted from: org.amd.silogen.title
                        type: string
                      variants:
                        description: |-
                          Variants lists model variants.
                          Extracted from: org.amd.silogen.model.variants (comma-separated)
                        items:
                          type: string
                        type: array
                      versions:
                        description: |-
                          Versions lists available versions.
                          Extracted from: org.amd.silogen.model.versions (comma-separated)
                        items:
                          type: string
                        type: array
                    type: object
                  oci:
                    description: OCI contains standard OCI image metadata.
                    properties:
                      authors:
                        description: |-
                          Authors is contact details of the authors.
                          Extracted from: org.opencontainers.image.authors
                        type: string
                      created:
                        description: |-
                          Created is the creation timestamp.
                          Extracted from: org.opencontainers.image.created
                        type: string
                      description:
                        description: |-
                          Description is a brief description.
                          Extracted from: org.opencontainers.image.description
                        type: string
                      documentation:
                        description: |-
                          Documentation is the URL to documentation.
                          Extracted from: org.opencontainers.image.documentation
                        type: string
                      licenses:
                        description: |-
                          Licenses is the SPDX license identifier(s).
                          Extracted from: org.opencontainers.image.licenses
                        type: string
                      revision:
                        description: |-
                          Revision is the source control revision.
                          Extracted from: org.opencontainers.image.revision
                        type: string
                      source:
                        description: |-
                          Source is the URL to the source code repository.
                          Extracted from: org.opencontainers.image.source
                        type: string
                      title:
                        description: |-
                          Title is the human-readable title.
                          Extracted from: org.opencontainers.image.title
                        type: string
                      vendor:
                        description: |-
                          Vendor is the organization that produced the image.
                          Extracted from: org.opencontainers.image.vendor
                        type: string
                      version:
                        description: |-
                          Version is the image version.
                          Extracted from: org.opencontainers.image.version
                        type: string
                    type: object
                  originalLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      OriginalLabels contains the raw OCI image labels as a JSON object.
                      This preserves all labels from the image, including those not mapped to structured fields.
                    type: object
                type: object
              kind:
                description: |-
                  Kind classifies the v1alpha2 onboarding flow that produced this
                  model's profiles (Image / Derived / Custom). See AIMModelKind for
                  the per-value semantics. Populated by the v1alpha2 controller from
                  the spec shape; left empty by the v1alpha1 controller.
                enum:
                - Image
                - Derived
                - Custom
                type: string
              managedProfiles:
                description: ManagedProfiles summarizes the direct promoted or derived
                  profiles owned or managed by this model.
                properties:
                  base:
                    description: |-
                      Base is the number of managed profiles whose spec is structurally
                      incomplete (missing aimId or modelSources). Base profiles cannot back
                      an AIMService directly and exist purely as source material for
                      derivation via AIMModel.spec.profiles.derivedFrom with
                      selector.role=base. A non-zero value is the canonical operational
                      signal that this model is a base-image model (the source of
                      custom-model derivation).
                    format: int32
                    type: integer
                  deployable:
                    description: |-
                      Deployable is the number of managed profiles whose spec is materialised
                      enough to back an AIMService (carries aimId and modelSources). For a
                      normal officially-discovered AIMModel this equals Total. For a
                      base-image AIMModel used as a source for custom-model derivation
                      this is 0 — the model only emits base profiles that callers must
                      derive into deployable profiles.
                    format: int32
                    type: integer
                  notAvailable:
                    description: NotAvailable is the number of derivative profiles
                      whose own status is NotAvailable.
                    format: int32
                    type: integer
                  ready:
                    description: Ready is the number of derivative profiles whose
                      own status is Ready.
                    format: int32
                    type: integer
                  total:
                    description: Total is the number of derivative profiles the controller
                      currently manages or intends to manage.
                    format: int32
                    type: integer
                required:
                - base
                - deployable
                - notAvailable
                - ready
                - total
                type: object
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller
                format: int64
                type: integer
              profileSetRef:
                description: ProfileSetRef identifies the child profile set synthesized
                  for derivation flows.
                properties:
                  name:
                    description: Name is the profile set name.
                    type: string
                  namespace:
                    description: Namespace is the profile set namespace.
                    type: string
                type: object
              resolvedRuntimeConfig:
                description: ResolvedRuntimeConfig captures metadata about the runtime
                  config that was resolved.
                properties:
                  kind:
                    description: Kind is the fully-qualified kind of the resolved
                      reference, when known.
                    type: string
                  name:
                    description: Name is the resource name that satisfied the reference.
                    type: string
                  namespace:
                    description: |-
                      Namespace identifies where the resource was found when namespace-scoped.
                      Empty indicates a cluster-scoped resource.
                    type: string
                  scope:
                    description: Scope indicates whether the resolved resource was
                      namespace or cluster scoped.
                    enum:
                    - Namespace
                    - Cluster
                    - Merged
                    - Unknown
                    type: string
                  uid:
                    description: UID captures the unique identifier of the resolved
                      reference, when known.
                    type: string
                type: object
              sourceType:
                description: |-
                  SourceType indicates how this model's artifacts are sourced.
                  - "Image": Model discovered from container image labels
                  - "Custom": Model uses explicit spec.modelSources
                  Set by the controller based on whether spec.modelSources is populated.

                  Note: only populated by the v1alpha1 controller; v1alpha2 consumers
                  should read .status.kind instead, which distinguishes fine-tunes
                  (Derived) from BYO base-image overlays (Custom).
                enum:
                - Image
                - Custom
                type: string
              status:
                default: Pending
                description: Status represents the overall status of the image based
                  on its templates
                enum:
                - Pending
                - Progressing
                - Ready
                - Degraded
                - Failed
                - NotAvailable
                type: string
              version:
                description: |-
                  Version is the effective image version of the model. Populated from
                  the spec.image tag (`amdenterpriseai/aim-qwen-qwen3-32b:0.11.0` →
                  `0.11.0`) during reconciliation. Empty for models that have no
                  spec.image (e.g. fine-tuned models derived from a parent) or that
                  reference an image by digest.

                  This mirrors AIMProfileStatus.Version so the two surfaces stay in
                  lock-step: a single image-tag extraction rule governs what users
                  see in the kubectl printcolumn for both kinds.

                  The image-author-declared version (i.e. the
                  `org.opencontainers.image.version` OCI label) is preserved separately
                  under .status.imageMetadata.oci.version for users that want to inspect
                  what the image build pipeline stamped. The two values usually agree;
                  when the OCI label is missing or empty, this field still surfaces a
                  useful version from the tag itself.
                type: string
            type: object
        type: object
        x-kubernetes-validations:
        - message: exactly one of spec.image or spec.profiles must be set on v1alpha2
          optionalOldSelf: true
          rule: (has(self.spec.image) && size(self.spec.image) > 0) != (has(self.spec.profiles)
            || has(self.spec.derivedFrom)) || (oldSelf.hasValue() && !((has(oldSelf.value().spec.image)
            && size(oldSelf.value().spec.image) > 0) != (has(oldSelf.value().spec.profiles)
            || has(oldSelf.value().spec.derivedFrom))))
        - message: spec.profileCopy is forbidden on v1alpha2; use spec.profiles
          optionalOldSelf: true
          rule: '!has(self.spec.profileCopy) || (oldSelf.hasValue() && has(oldSelf.value().spec.profileCopy))'
        - message: spec.derivedFrom is deprecated on v1alpha2; use spec.profiles
          optionalOldSelf: true
          rule: '!has(self.spec.derivedFrom) || (oldSelf.hasValue() && has(oldSelf.value().spec.derivedFrom))'
        - message: spec.custom is forbidden on v1alpha2
          optionalOldSelf: true
          rule: '!has(self.spec.custom) || (oldSelf.hasValue() && has(oldSelf.value().spec.custom))'
        - message: spec.customTemplates is forbidden on v1alpha2
          optionalOldSelf: true
          rule: '!has(self.spec.customTemplates) || size(self.spec.customTemplates)
            == 0 || (oldSelf.hasValue() && has(oldSelf.value().spec.customTemplates)
            && size(oldSelf.value().spec.customTemplates) > 0)'
        - message: spec.modelSources is forbidden on v1alpha2; use spec.profiles.overrides.modelSources
          optionalOldSelf: true
          rule: '!has(self.spec.modelSources) || size(self.spec.modelSources) == 0
            || (oldSelf.hasValue() && has(oldSelf.value().spec.modelSources) && size(oldSelf.value().spec.modelSources)
            > 0)'
        - message: spec.profiles cannot be combined with spec.aimId, spec.discovery,
            spec.defaultServiceTemplate, spec.runtimeConfigName, spec.env, or spec.imageMetadata
          rule: (!has(self.spec.profiles) && !has(self.spec.derivedFrom)) || (!has(self.spec.aimId)
            && !has(self.spec.discovery) && !has(self.spec.defaultServiceTemplate)
            && !has(self.spec.runtimeConfigName) && !has(self.spec.env) && !has(self.spec.imageMetadata))
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: aimprofilecaches.aim.eai.amd.com
spec:
  group: aim.eai.amd.com
  names:
    categories:
    - aim
    - all
    kind: AIMProfileCache
    listKind: AIMProfileCacheList
    plural: aimprofilecaches
    shortNames:
    - aimpc
    singular: aimprofilecache
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.profileName
      name: Profile
      type: string
    - jsonPath: .spec.profileScope
      name: Scope
      type: string
    - jsonPath: .spec.mode
      name: Mode
      type: string
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: AIMProfileCache pre-warms model artifacts for a specified profile's
          model sources.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AIMProfileCacheSpec defines the desired state of AIMProfileCache.
            properties:
              env:
                description: |-
                  Env specifies environment variables for authentication when downloading models.
                  These variables are used for authentication with model registries (e.g., HuggingFace tokens).
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: |-
                        Name of the environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        fileKeyRef:
                          description: |-
                            FileKeyRef selects a key of the env file.
                            Requires the EnvFiles feature gate to be enabled.
                          properties:
                            key:
                              description: |-
                                The key within the env file. An invalid key will prevent the pod from starting.
                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                              type: string
                            optional:
                              default: false
                              description: |-
                                Specify whether the file or its key must be defined. If the file or key
                                does not exist, then the env var is not published.
                                If optional is set to true and the specified key does not exist,
                                the environment variable will not be set in the Pod's containers.

                                If optional is set to false and the specified key does not exist,
                                an error will be returned during Pod creation.
                              type: boolean
                            path:
                              description: |-
                                The path within the volume from which to select the file.
                                Must be relative and may not contain the '..' path or start with '..'.
                              type: string
                            volumeName:
                              description: The name of the volume mount containing
                                the env file.
                              type: string
                          required:
                          - key
                          - path
                          - volumeName
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              mode:
                default: Shared
                description: |-
                  Mode controls the ownership behavior of artifacts created by this profile cache.
                  - Dedicated: artifacts are owned by this profile cache and garbage collected when it's deleted.
                  - Shared (default): artifacts have no owner references and persist independently.
                enum:
                - Dedicated
                - Shared
                type: string
              profileName:
                description: |-
                  ProfileName is the name of the AIMProfile or AIMClusterProfile to cache.
                  The controller resolves model sources from the referenced profile's spec.modelSources.
                minLength: 1
                type: string
              profileScope:
                allOf:
                - enum:
                  - Namespace
                  - Cluster
                  - Merged
                  - Unknown
                - enum:
                  - Namespace
                  - Cluster
                description: ProfileScope indicates whether the profile is namespace-scoped
                  or cluster-scoped.
                type: string
              requiresAdapterDisk:
                description: |-
                  RequiresAdapterDisk requests that the backing model artifact carry a shared
                  ReadWriteMany adapter disk for LoRA serving. Set by the AIMService planner
                  when the service serves adapters. When set, the cache stamps an adapterDisk
                  onto the artifact it creates and won't adopt one lacking a disk; size and
                  class come from AIMRuntimeConfig.Storage.
                type: boolean
              runtimeConfigName:
                description: |-
                  Name is the name of the runtime config to use for this resource. If a runtime config with this name exists both
                  as a namespace and a cluster runtime config, the values are merged together, the namespace config taking priority
                  over the cluster config when there are conflicts. If this field is empty or set to `default`, the namespace / cluster
                  runtime config with the name `default` is used, if it exists.
                type: string
              storageClassName:
                description: |-
                  StorageClassName specifies the storage class for cache volumes.
                  When not specified, uses the cluster default storage class.
                type: string
            required:
            - profileName
            - profileScope
            type: object
          status:
            description: AIMProfileCacheStatus defines the observed state of AIMProfileCache.
            properties:
              artifacts:
                additionalProperties:
                  properties:
                    model:
                      description: Model is the name of the model that is cached
                      type: string
                    mountPoint:
                      description: MountPoint is the mount point for the artifact
                      type: string
                    name:
                      description: Name of the AIMArtifact resource
                      type: string
                    persistentVolumeClaim:
                      description: PersistentVolumeClaim name if available
                      type: string
                    status:
                      description: Status of the artifact
                      type: string
                    uid:
                      description: UID of the AIMArtifact resource
                      type: string
                  required:
                  - model
                  - name
                  - status
                  - uid
                  type: object
                description: Artifacts maps artifact names to their resolved AIMArtifact
                  resources.
                type: object
              conditions:
                description: Conditions represent the latest observations of the profile
                  cache state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              status:
                default: Pending
                description: Status represents the current high-level status of the
                  profile cache.
                enum:
                - Pending
                - Progressing
                - Ready
                - Failed
                - Degraded
                - NotAvailable
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: aimprofiles.aim.eai.amd.com
spec:
  group: aim.eai.amd.com
  names:
    categories:
    - aim
    - all
    kind: AIMProfile
    listKind: AIMProfileList
    plural: aimprofiles
    shortNames:
    - aimprf
    singular: aimprofile
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.aimId
      name: AimId
      type: string
    - jsonPath: .spec.engine
      name: Engine
      type: string
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.origin
      name: Origin
      type: string
    - jsonPath: .status.hardwareSummary
      name: Hardware
      type: string
    - jsonPath: .spec.metric
      name: Metric
      type: string
    - jsonPath: .spec.precision
      name: Precision
      type: string
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .spec.acceleratorPartitioningMode
      name: Partitioning
      priority: 1
      type: string
    - jsonPath: .spec.primary
      name: Primary
      type: boolean
    - jsonPath: .status.version
      name: Version
      type: string
    - jsonPath: .spec.manualSelectionOnly
      name: Manual
      priority: 1
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: |-
          AIMProfile is the Schema for namespace-scoped AIM profiles.
          A profile is a self-contained runtime configuration that answers five questions without
          consulting any other resource: model architecture, accelerator, K8s resources, runtime
          config, and container image.

          Deployable profiles have both aimId and modelSources populated; base
          profiles (custom-model derivation source material) have neither. Mixed
          (one of the two set) is rejected to keep status.deployable derivable from
          spec.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AIMProfileSpec defines the desired state of a namespace-scoped
              AIMProfile.
            properties:
              acceleratorCount:
                description: |-
                  AcceleratorCount is the number of accelerator units required.
                  For AcceleratorType=gpu, this is the device count (e.g., 1, 2, 4, 8
                  for tensor-parallel sizes). For AcceleratorType=cpu, this is the
                  number of CPU cores (e.g., 128 for EPYC_ZEN5, 192 for EPYC_9965).
                  Combined with cluster-level configuration to compute default
                  resource requests in status.resources.

                  For AcceleratorType=gpu, the per-unit interpretation depends on
                  AcceleratorPartitioningMode: under "unpartitioned" (default) one unit is
                  one whole GPU; under "partitioned" or a specific scheme one unit is one
                  partition slice (e.g. CPX-NPS4 = 1/8 of a GPU).
                format: int32
                minimum: 0
                type: integer
              acceleratorModel:
                description: |-
                  AcceleratorModel is the accelerator identifier for node selection.
                  Maps to a node label key using the Exists operator:
                    feature.node.kubernetes.io/aim-accelerator.{value}: Exists
                  Supports both specific models (e.g., "MI300X") and architecture-level
                  fallbacks (e.g., "EPYC_ZEN5") — the AcceleratorDetector labels nodes
                  with all applicable identifiers.
                maxLength: 63
                pattern: ^[A-Za-z0-9]([A-Za-z0-9._-]*[A-Za-z0-9])?$
                type: string
              acceleratorPartitioningMode:
                default: unpartitioned
                description: |-
                  AcceleratorPartitioningMode declares the GPU partition state the profile
                  requires. Free-form string with reserved values:

                    ""              - omitted; CRD-defaulted to "unpartitioned".
                    "unpartitioned" - hardware-default partition state. Matches unpartitioned
                                      MI300X (canonical SPX-NPS1) AND non-partitionable
                                      hardware (Radeon, etc.) — any node whose detector
                                      stamped aim-accelerator.partitioning-scheme.default.
                    "partitioned"   - any actively partitioned mode. Excludes both
                                      unpartitioned partitionable hardware and
                                      non-partitionable hardware.
                    "<C>-<M>"       - specific compute+memory scheme, e.g. "CPX-NPS4". Matches
                                      only nodes carrying that exact scheme label; does not
                                      match non-partitionable hardware.

                  Other values (e.g. "CPX" alone, or typos) are accepted but fail-safe to
                  zero matching nodes under this iteration's label schema — compute-only /
                  memory-only matching is not supported here. AIM Engine does NOT validate
                  against AMD's hardware compatibility matrix; invalid combinations simply
                  report MatchingNodes == 0.

                  Resolves to a single Exists or DoesNotExist node-affinity term on the
                  aim-accelerator.partitioning-scheme.* labels published by the
                  AcceleratorDetector, AND-ed with the acceleratorModel term.
                maxLength: 63
                pattern: ^[A-Za-z0-9]([A-Za-z0-9._-]*[A-Za-z0-9])?$
                type: string
              acceleratorType:
                allOf:
                - enum:
                  - gpu
                  - cpu
                - enum:
                  - gpu
                  - cpu
                description: |-
                  AcceleratorType determines the resource derivation strategy: gpu or cpu.
                  AIM Engine computes default resource requests from this field combined
                  with AcceleratorCount and cluster-level configuration.
                type: string
              aimId:
                description: |-
                  AimId is the model architecture identifier (e.g., "qwen/qwen3-32b").
                  Primary matching axis for profile selection and custom weight onboarding.

                  AimId is required for deployable profiles. Iteration 1 producers always
                  emit deployable profiles, so AimId is effectively required there. Empty
                  AimId is reserved for base profiles emitted by base-image discovery
                  (custom-model derivation source material), which are not deployable
                  until derived.

                  Once set, AimId is immutable.
                type: string
                x-kubernetes-validations:
                - message: aimId is immutable once set
                  rule: oldSelf == '' || self == oldSelf
              caching:
                description: Caching configures model caching behavior for this namespace-scoped
                  profile.
                properties:
                  enabled:
                    default: false
                    description: Enabled controls whether caching is enabled for this
                      profile.
                    type: boolean
                  env:
                    description: |-
                      Env specifies environment variables for model download during caching.
                      If not set, falls back to the profile's ContainerEnv.
                    items:
                      description: EnvVar represents an environment variable present
                        in a Container.
                      properties:
                        name:
                          description: |-
                            Name of the environment variable.
                            May consist of any printable ASCII characters except '='.
                          type: string
                        value:
                          description: |-
                            Variable references $(VAR_NAME) are expanded
                            using the previously defined environment variables in the container and
                            any service environment variables. If a variable cannot be resolved,
                            the reference in the input string will be unchanged. Double $$ are reduced
                            to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                            "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                            Escaped references will never be expanded, regardless of whether the variable
                            exists or not.
                            Defaults to "".
                          type: string
                        valueFrom:
                          description: Source for the environment variable's value.
                            Cannot be used if value is not empty.
                          properties:
                            configMapKeyRef:
                              description: Selects a key of a ConfigMap.
                              properties:
                                key:
                                  description: The key to select.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the ConfigMap or its
                                    key must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                            fieldRef:
                              description: |-
                                Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                              properties:
                                apiVersion:
                                  description: Version of the schema the FieldPath
                                    is written in terms of, defaults to "v1".
                                  type: string
                                fieldPath:
                                  description: Path of the field to select in the
                                    specified API version.
                                  type: string
                              required:
                              - fieldPath
                              type: object
                              x-kubernetes-map-type: atomic
                            fileKeyRef:
                              description: |-
                                FileKeyRef selects a key of the env file.
                                Requires the EnvFiles feature gate to be enabled.
                              properties:
                                key:
                                  description: |-
                                    The key within the env file. An invalid key will prevent the pod from starting.
                                    The keys defined within a source may consist of any printable ASCII characters except '='.
                                    During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                  type: string
                                optional:
                                  default: false
                                  description: |-
                                    Specify whether the file or its key must be defined. If the file or key
                                    does not exist, then the env var is not published.
                                    If optional is set to true and the specified key does not exist,
                                    the environment variable will not be set in the Pod's containers.

                                    If optional is set to false and the specified key does not exist,
                                    an error will be returned during Pod creation.
                                  type: boolean
                                path:
                                  description: |-
                                    The path within the volume from which to select the file.
                                    Must be relative and may not contain the '..' path or start with '..'.
                                  type: string
                                volumeName:
                                  description: The name of the volume mount containing
                                    the env file.
                                  type: string
                              required:
                              - key
                              - path
                              - volumeName
                              type: object
                              x-kubernetes-map-type: atomic
                            resourceFieldRef:
                              description: |-
                                Selects a resource of the container: only resources limits and requests
                                (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                              properties:
                                containerName:
                                  description: 'Container name: required for volumes,
                                    optional for env vars'
                                  type: string
                                divisor:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: Specifies the output format of the
                                    exposed resources, defaults to "1"
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                resource:
                                  description: 'Required: resource to select'
                                  type: string
                              required:
                              - resource
                              type: object
                              x-kubernetes-map-type: atomic
                            secretKeyRef:
                              description: Selects a key of a secret in the pod's
                                namespace
                              properties:
                                key:
                                  description: The key of the secret to select from.  Must
                                    be a valid secret key.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the Secret or its key
                                    must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                type: object
              containerEnv:
                description: ContainerEnv specifies container-level env vars for the
                  AIM runtime process (K8s pod spec).
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: |-
                        Name of the environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        fileKeyRef:
                          description: |-
                            FileKeyRef selects a key of the env file.
                            Requires the EnvFiles feature gate to be enabled.
                          properties:
                            key:
                              description: |-
                                The key within the env file. An invalid key will prevent the pod from starting.
                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                              type: string
                            optional:
                              default: false
                              description: |-
                                Specify whether the file or its key must be defined. If the file or key
                                does not exist, then the env var is not published.
                                If optional is set to true and the specified key does not exist,
                                the environment variable will not be set in the Pod's containers.

                                If optional is set to false and the specified key does not exist,
                                an error will be returned during Pod creation.
                              type: boolean
                            path:
                              description: |-
                                The path within the volume from which to select the file.
                                Must be relative and may not contain the '..' path or start with '..'.
                              type: string
                            volumeName:
                              description: The name of the volume mount containing
                                the env file.
                              type: string
                          required:
                          - key
                          - path
                          - volumeName
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              engine:
                description: Engine identifies the inference engine (e.g., "vllm",
                  "tgi").
                type: string
              engineArgs:
                description: |-
                  EngineArgs contains inference engine CLI arguments as a free-form JSON object.
                  Passed to the inference engine (e.g., vLLM) at startup.
                x-kubernetes-preserve-unknown-fields: true
              engineEnv:
                additionalProperties:
                  type: string
                description: |-
                  EngineEnv contains environment variables for the inference engine subprocess.
                  Applied via os.execv, distinct from container-level ContainerEnv.
                type: object
              features:
                description: |-
                  Features lists optional capabilities the profile's image honours, e.g.
                  "adapters" for LoRA serving. A service declaring spec.adapters is rejected
                  (ConfigValid=False) unless its resolved profile lists "adapters" here.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              image:
                description: |-
                  Image is the deployment container image. Required.
                  For purpose-built profiles: the full AIM image.
                  For custom weight profiles: the base image (e.g., aim-base:0.8.5).
                minLength: 1
                type: string
              imagePullSecrets:
                description: ImagePullSecrets lists secrets for pulling container
                  images.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              manualSelectionOnly:
                default: false
                description: |-
                  ManualSelectionOnly is DEPRECATED and no longer honored by the resolver.
                  It was a binary gate excluding a profile from automatic AIMService
                  selection; that intent is now expressed through the graded `type`
                  hierarchy (optimized > general > preview > unoptimized) combined with the
                  selector's `minimumType` floor. The field is retained for backward
                  compatibility (existing objects and aim-build profile YAMLs still set it)
                  but has no effect on selection; it will be removed in a future API
                  version. Use `type: unoptimized` (+ a selector `minimumType`) instead.

                  Deprecated: superseded by `type` + selector `minimumType`; ignored by the resolver.
                type: boolean
              metric:
                allOf:
                - enum:
                  - latency
                  - throughput
                - enum:
                  - latency
                  - throughput
                description: Metric is the optimization target for this profile.
                type: string
              modelId:
                description: |-
                  ModelId is the specific model / HuggingFace URI (e.g., "qwen/qwen3-32b-fp8").
                  Determines the cache path (/workspace/cache/{modelId}) and serves as a secondary
                  discriminator for custom weight matching.
                type: string
              modelSources:
                description: |-
                  ModelSources specifies model artifact sources for this profile.
                  Populated during discovery or set by user.
                items:
                  description: |-
                    AIMModelSource describes a model artifact that must be downloaded for inference.
                    Discovery extracts these from the container's configuration to enable caching and validation.
                  properties:
                    env:
                      description: |-
                        Env specifies per-source credential overrides.
                        These variables are used for authentication when downloading this specific source.
                        Takes precedence over base-level env for the same variable name.
                      items:
                        description: EnvVar represents an environment variable present
                          in a Container.
                        properties:
                          name:
                            description: |-
                              Name of the environment variable.
                              May consist of any printable ASCII characters except '='.
                            type: string
                          value:
                            description: |-
                              Variable references $(VAR_NAME) are expanded
                              using the previously defined environment variables in the container and
                              any service environment variables. If a variable cannot be resolved,
                              the reference in the input string will be unchanged. Double $$ are reduced
                              to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                              "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                              Escaped references will never be expanded, regardless of whether the variable
                              exists or not.
                              Defaults to "".
                            type: string
                          valueFrom:
                            description: Source for the environment variable's value.
                              Cannot be used if value is not empty.
                            properties:
                              configMapKeyRef:
                                description: Selects a key of a ConfigMap.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the ConfigMap or
                                      its key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              fieldRef:
                                description: |-
                                  Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                  spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                properties:
                                  apiVersion:
                                    description: Version of the schema the FieldPath
                                      is written in terms of, defaults to "v1".
                                    type: string
                                  fieldPath:
                                    description: Path of the field to select in the
                                      specified API version.
                                    type: string
                                required:
                                - fieldPath
                                type: object
                                x-kubernetes-map-type: atomic
                              fileKeyRef:
                                description: |-
                                  FileKeyRef selects a key of the env file.
                                  Requires the EnvFiles feature gate to be enabled.
                                properties:
                                  key:
                                    description: |-
                                      The key within the env file. An invalid key will prevent the pod from starting.
                                      The keys defined within a source may consist of any printable ASCII characters except '='.
                                      During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                    type: string
                                  optional:
                                    default: false
                                    description: |-
                                      Specify whether the file or its key must be defined. If the file or key
                                      does not exist, then the env var is not published.
                                      If optional is set to true and the specified key does not exist,
                                      the environment variable will not be set in the Pod's containers.

                                      If optional is set to false and the specified key does not exist,
                                      an error will be returned during Pod creation.
                                    type: boolean
                                  path:
                                    description: |-
                                      The path within the volume from which to select the file.
                                      Must be relative and may not contain the '..' path or start with '..'.
                                    type: string
                                  volumeName:
                                    description: The name of the volume mount containing
                                      the env file.
                                    type: string
                                required:
                                - key
                                - path
                                - volumeName
                                type: object
                                x-kubernetes-map-type: atomic
                              resourceFieldRef:
                                description: |-
                                  Selects a resource of the container: only resources limits and requests
                                  (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                properties:
                                  containerName:
                                    description: 'Container name: required for volumes,
                                      optional for env vars'
                                    type: string
                                  divisor:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: Specifies the output format of the
                                      exposed resources, defaults to "1"
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  resource:
                                    description: 'Required: resource to select'
                                    type: string
                                required:
                                - resource
                                type: object
                                x-kubernetes-map-type: atomic
                              secretKeyRef:
                                description: Selects a key of a secret in the pod's
                                  namespace
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                        required:
                        - name
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - name
                      x-kubernetes-list-type: map
                    modelId:
                      description: |-
                        ModelID is the canonical identifier in {org}/{name} format.
                        Determines the cache mount path: /workspace/cache/{modelId}
                        For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                        For S3 sources, users define their own organizational structure.
                      pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                      type: string
                    precision:
                      allOf:
                      - enum:
                        - auto
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                      - enum:
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                      description: |-
                        Precision describes the runtime precision this source is compatible with.
                        Used to match model sources to profiles during custom weight onboarding.
                      type: string
                    size:
                      anyOf:
                      - type: integer
                      - type: string
                      description: |-
                        Size is the expected storage space required for this model artifact.
                        Used for PVC sizing and capacity planning during cache creation.
                        Optional - if not specified, the download job will discover the size automatically.
                        Can be set explicitly to pre-allocate storage or override auto-discovery.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    sourceUri:
                      description: |-
                        SourceURI is the location from which the model should be downloaded.
                        Supported schemes:
                        - hf://org/model - Hugging Face Hub model
                        - s3://bucket/key - S3-compatible storage
                      pattern: ^(hf|s3)://[^ \t\r\n]+$
                      type: string
                  required:
                  - modelId
                  - sourceUri
                  type: object
                type: array
              precision:
                allOf:
                - enum:
                  - auto
                  - fp4
                  - fp8
                  - fp16
                  - fp32
                  - bf16
                  - int4
                  - int8
                - enum:
                  - fp4
                  - fp8
                  - fp16
                  - fp32
                  - bf16
                  - int4
                  - int8
                description: Precision is the numeric precision used by this profile.
                type: string
              primary:
                default: false
                description: |-
                  Primary marks this as a default/recommended profile. When true, the profile is
                  advertised for standard deployment and copied automatically for custom weight models.
                  Defaults to false when not specified.
                type: boolean
              profileId:
                description: |-
                  ProfileId is the on-disk profile identifier from the AIM image
                  (e.g., "vllm-mi300x-fp8-tp1-latency"). Populated during discovery to link this
                  CRD back to the profile YAML inside the container. Not required for manually
                  created profiles.
                type: string
              resources:
                description: |-
                  Resources is an optional override for K8s resource requests/limits.
                  When set, merged on top of the defaults that AIM Engine computes from
                  AcceleratorType, AcceleratorCount, and cluster-level configuration.
                  The resolved result is written to status.resources.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              serviceAccountName:
                description: ServiceAccountName specifies the service account for
                  workloads.
                type: string
              type:
                allOf:
                - enum:
                  - optimized
                  - general
                  - preview
                  - unoptimized
                - enum:
                  - optimized
                  - general
                  - preview
                  - unoptimized
                description: 'Type indicates the optimization level. Hierarchy: optimized
                  > general > preview > unoptimized.'
                type: string
            required:
            - image
            - primary
            type: object
          status:
            description: AIMProfileStatus defines the observed state of AIMProfile
              / AIMClusterProfile.
            properties:
              baseImage:
                description: |-
                  BaseImage is the AIM_BASE_IMAGE_REF the inspector extracted from the
                  source image when this profile was materialised by AIMModel discovery.
                  Used by derivation flows (AIMService overlays, AIMProfileSet) to rebase
                  the deployment image onto the source's base when overriding model
                  sources, so private mirrors stay self-contained. Empty for
                  user-authored profiles.
                type: string
              conditions:
                description: Conditions represent the latest observations of profile
                  state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              deployable:
                default: false
                description: |-
                  Deployable reports whether the profile is materialised enough to back an
                  AIMService: true when spec.aimId and spec.modelSources are both
                  populated, false for base profiles awaiting derivation.

                  Image discovery of a deployable AIM image emits deployable profiles;
                  base-image discovery emits base profiles (no aimId/modelSources) that
                  a custom-model AIMModel derives into deployable copies.
                type: boolean
              hardwareSummary:
                description: |-
                  HardwareSummary is a human-readable string describing the hardware requirements.
                  Format: "{count} x {model}" for GPU (e.g., "1 x MI300X") or "CPU" for CPU-only.
                type: string
              matchingNodes:
                description: |-
                  MatchingNodes is the count of cluster nodes matching both the accelerator
                  model label and status.resources requests. Zero means NotAvailable.
                format: int32
                type: integer
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              origin:
                description: |-
                  Origin classifies how this profile was produced:
                    - discovered: emitted by image discovery (AIMModel.spec.image).
                    - derived: emitted by an AIMProfileSet or
                      AIMModel.spec.profiles.derivedFrom.
                    - user-authored: created independently by a user.

                  Backfilled by the AIMProfile reconciler when not stamped at creation
                  time; user-authored profiles default to `user-authored`.
                enum:
                - discovered
                - derived
                - user-authored
                type: string
              resolvedNodeAffinity:
                description: |-
                  ResolvedNodeAffinity contains the computed node affinity rules derived from
                  spec.acceleratorModel. Used by AIMService when building InferenceService pods.
                properties:
                  preferredDuringSchedulingIgnoredDuringExecution:
                    description: |-
                      The scheduler will prefer to schedule pods to nodes that satisfy
                      the affinity expressions specified by this field, but it may choose
                      a node that violates one or more of the expressions. The node that is
                      most preferred is the one with the greatest sum of weights, i.e.
                      for each node that meets all of the scheduling requirements (resource
                      request, requiredDuringScheduling affinity expressions, etc.),
                      compute a sum by iterating through the elements of this field and adding
                      "weight" to the sum if the node matches the corresponding matchExpressions; the
                      node(s) with the highest sum are the most preferred.
                    items:
                      description: |-
                        An empty preferred scheduling term matches all objects with implicit weight 0
                        (i.e. it's a no-op). A null preferred scheduling term matches no objects (i.e. is also a no-op).
                      properties:
                        preference:
                          description: A node selector term, associated with the corresponding
                            weight.
                          properties:
                            matchExpressions:
                              description: A list of node selector requirements by
                                node's labels.
                              items:
                                description: |-
                                  A node selector requirement is a selector that contains values, a key, and an operator
                                  that relates the key and values.
                                properties:
                                  key:
                                    description: The label key that the selector applies
                                      to.
                                    type: string
                                  operator:
                                    description: |-
                                      Represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                    type: string
                                  values:
                                    description: |-
                                      An array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. If the operator is Gt or Lt, the values
                                      array must have a single element, which will be interpreted as an integer.
                                      This array is replaced during a strategic merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchFields:
                              description: A list of node selector requirements by
                                node's fields.
                              items:
                                description: |-
                                  A node selector requirement is a selector that contains values, a key, and an operator
                                  that relates the key and values.
                                properties:
                                  key:
                                    description: The label key that the selector applies
                                      to.
                                    type: string
                                  operator:
                                    description: |-
                                      Represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                    type: string
                                  values:
                                    description: |-
                                      An array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. If the operator is Gt or Lt, the values
                                      array must have a single element, which will be interpreted as an integer.
                                      This array is replaced during a strategic merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                          type: object
                          x-kubernetes-map-type: atomic
                        weight:
                          description: Weight associated with matching the corresponding
                            nodeSelectorTerm, in the range 1-100.
                          format: int32
                          type: integer
                      required:
                      - preference
                      - weight
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  requiredDuringSchedulingIgnoredDuringExecution:
                    description: |-
                      If the affinity requirements specified by this field are not met at
                      scheduling time, the pod will not be scheduled onto the node.
                      If the affinity requirements specified by this field cease to be met
                      at some point during pod execution (e.g. due to an update), the system
                      may or may not try to eventually evict the pod from its node.
                    properties:
                      nodeSelectorTerms:
                        description: Required. A list of node selector terms. The
                          terms are ORed.
                        items:
                          description: |-
                            A null or empty node selector term matches no objects. The requirements of
                            them are ANDed.
                            The TopologySelectorTerm type implements a subset of the NodeSelectorTerm.
                          properties:
                            matchExpressions:
                              description: A list of node selector requirements by
                                node's labels.
                              items:
                                description: |-
                                  A node selector requirement is a selector that contains values, a key, and an operator
                                  that relates the key and values.
                                properties:
                                  key:
                                    description: The label key that the selector applies
                                      to.
                                    type: string
                                  operator:
                                    description: |-
                                      Represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                    type: string
                                  values:
                                    description: |-
                                      An array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. If the operator is Gt or Lt, the values
                                      array must have a single element, which will be interpreted as an integer.
                                      This array is replaced during a strategic merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchFields:
                              description: A list of node selector requirements by
                                node's fields.
                              items:
                                description: |-
                                  A node selector requirement is a selector that contains values, a key, and an operator
                                  that relates the key and values.
                                properties:
                                  key:
                                    description: The label key that the selector applies
                                      to.
                                    type: string
                                  operator:
                                    description: |-
                                      Represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                    type: string
                                  values:
                                    description: |-
                                      An array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. If the operator is Gt or Lt, the values
                                      array must have a single element, which will be interpreted as an integer.
                                      This array is replaced during a strategic merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                          type: object
                          x-kubernetes-map-type: atomic
                        type: array
                        x-kubernetes-list-type: atomic
                    required:
                    - nodeSelectorTerms
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
              resources:
                description: |-
                  Resources contains the definitive K8s resource requests/limits used for deployment.
                  Computed by AIM Engine from AcceleratorType, AcceleratorCount, and cluster-level
                  configuration, then merged with any spec.resources override.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              sourceModel:
                description: |-
                  SourceModel identifies the producing AIM(Cluster)Model for profiles
                  owned by AIMModel reconcilers. Empty for user-authored profiles.
                properties:
                  kind:
                    description: Kind is the producing model's kind ("AIMModel" or
                      "AIMClusterModel").
                    enum:
                    - AIMModel
                    - AIMClusterModel
                    type: string
                  name:
                    description: Name is the producing model's name.
                    type: string
                  namespace:
                    description: |-
                      Namespace is the producing model's namespace. Empty when Kind is
                      AIMClusterModel (cluster-scoped).
                    type: string
                required:
                - kind
                - name
                type: object
              status:
                default: Pending
                description: |-
                  Status represents the current high-level status of this profile.
                  Ready: at least one cluster node matches the profile's accelerator labels and resource requests.
                  NotAvailable: no matching nodes found.
                enum:
                - Pending
                - Progressing
                - Ready
                - Degraded
                - Failed
                - NotAvailable
                type: string
              version:
                description: Version is extracted from the spec.image tag during reconciliation
                  (e.g., "0.8.5").
                type: string
            required:
            - deployable
            type: object
        type: object
        x-kubernetes-validations:
        - message: spec.aimId and spec.modelSources must both be set (deployable profile)
            or both be empty (base profile)
          rule: (has(self.spec.aimId) && size(self.spec.aimId) > 0) == (has(self.spec.modelSources)
            && size(self.spec.modelSources) > 0)
        - message: acceleratorPartitioningMode must be 'unpartitioned' (or unset)
            when acceleratorType is cpu
          rule: '!(has(self.spec.acceleratorType) && self.spec.acceleratorType ==
            ''cpu'') || !has(self.spec.acceleratorPartitioningMode) || size(self.spec.acceleratorPartitioningMode)
            == 0 || self.spec.acceleratorPartitioningMode == ''unpartitioned'''
    selectableFields:
    - jsonPath: .spec.aimId
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: aimprofilesets.aim.eai.amd.com
spec:
  group: aim.eai.amd.com
  names:
    categories:
    - aim
    - all
    kind: AIMProfileSet
    listKind: AIMProfileSetList
    plural: aimprofilesets
    shortNames:
    - aimprfset
    singular: aimprofileset
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.managedProfiles.total
      name: Managed
      type: integer
    - jsonPath: .status.managedProfiles.ready
      name: Ready
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: AIMProfileSet is the Schema for namespace-scoped profile derivation
          resources.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              AIMProfileSetSpec defines the desired state of AIMProfileSet and is also reused by AIMModel.profileCopy.

              The selector-non-empty rule accepts every documented narrowing field
              (including modelRef and origin) and lets sourceRef-only specs through:
              when sourceRef is set, the discovery cache itself acts as the source
              scope and a separate selector is not required.
            properties:
              image:
                description: Image overrides the runtime image used by the derived
                  profiles.
                type: string
              imagePullSecrets:
                description: ImagePullSecrets lists secrets used for inspecting and
                  pulling container images.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              overrides:
                description: Overrides mutates the copied profile spec after selection
                  and version filtering.
                properties:
                  acceleratorCount:
                    description: AcceleratorCount replaces the copied profile's acceleratorCount.
                    format: int32
                    type: integer
                  acceleratorModel:
                    description: AcceleratorModel replaces the copied profile's acceleratorModel.
                    type: string
                  acceleratorPartitioningMode:
                    description: |-
                      AcceleratorPartitioningMode replaces the copied profile's
                      acceleratorPartitioningMode. Complete replacement, not a merge; an empty
                      override string leaves the source profile's mode untouched. Same reserved
                      values as AIMProfileSpecCommon.AcceleratorPartitioningMode. Whenever this
                      override is set (to any value, including "unpartitioned"), the CEL rule on the
                      enclosing spec also requires AcceleratorCount to be set: partition mode
                      changes the per-unit interpretation of acceleratorCount, and CEL cannot
                      read the base profile to tell whether the meaning actually changed, so it
                      conservatively requires the count be restated.
                    type: string
                  aimId:
                    description: |-
                      AimId stamps the derived profile's spec.aimId. When set, wins over
                      the source profile's aimId. REQUIRED when the enclosing selector has
                      role=base (the source base profile carries no aimId of its own).
                    type: string
                  containerEnv:
                    description: ContainerEnv merges by env var name, overriding matching
                      source entries.
                    items:
                      description: EnvVar represents an environment variable present
                        in a Container.
                      properties:
                        name:
                          description: |-
                            Name of the environment variable.
                            May consist of any printable ASCII characters except '='.
                          type: string
                        value:
                          description: |-
                            Variable references $(VAR_NAME) are expanded
                            using the previously defined environment variables in the container and
                            any service environment variables. If a variable cannot be resolved,
                            the reference in the input string will be unchanged. Double $$ are reduced
                            to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                            "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                            Escaped references will never be expanded, regardless of whether the variable
                            exists or not.
                            Defaults to "".
                          type: string
                        valueFrom:
                          description: Source for the environment variable's value.
                            Cannot be used if value is not empty.
                          properties:
                            configMapKeyRef:
                              description: Selects a key of a ConfigMap.
                              properties:
                                key:
                                  description: The key to select.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the ConfigMap or its
                                    key must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                            fieldRef:
                              description: |-
                                Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                              properties:
                                apiVersion:
                                  description: Version of the schema the FieldPath
                                    is written in terms of, defaults to "v1".
                                  type: string
                                fieldPath:
                                  description: Path of the field to select in the
                                    specified API version.
                                  type: string
                              required:
                              - fieldPath
                              type: object
                              x-kubernetes-map-type: atomic
                            fileKeyRef:
                              description: |-
                                FileKeyRef selects a key of the env file.
                                Requires the EnvFiles feature gate to be enabled.
                              properties:
                                key:
                                  description: |-
                                    The key within the env file. An invalid key will prevent the pod from starting.
                                    The keys defined within a source may consist of any printable ASCII characters except '='.
                                    During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                  type: string
                                optional:
                                  default: false
                                  description: |-
                                    Specify whether the file or its key must be defined. If the file or key
                                    does not exist, then the env var is not published.
                                    If optional is set to true and the specified key does not exist,
                                    the environment variable will not be set in the Pod's containers.

                                    If optional is set to false and the specified key does not exist,
                                    an error will be returned during Pod creation.
                                  type: boolean
                                path:
                                  description: |-
                                    The path within the volume from which to select the file.
                                    Must be relative and may not contain the '..' path or start with '..'.
                                  type: string
                                volumeName:
                                  description: The name of the volume mount containing
                                    the env file.
                                  type: string
                              required:
                              - key
                              - path
                              - volumeName
                              type: object
                              x-kubernetes-map-type: atomic
                            resourceFieldRef:
                              description: |-
                                Selects a resource of the container: only resources limits and requests
                                (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                              properties:
                                containerName:
                                  description: 'Container name: required for volumes,
                                    optional for env vars'
                                  type: string
                                divisor:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: Specifies the output format of the
                                    exposed resources, defaults to "1"
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                resource:
                                  description: 'Required: resource to select'
                                  type: string
                              required:
                              - resource
                              type: object
                              x-kubernetes-map-type: atomic
                            secretKeyRef:
                              description: Selects a key of a secret in the pod's
                                namespace
                              properties:
                                key:
                                  description: The key of the secret to select from.  Must
                                    be a valid secret key.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the Secret or its key
                                    must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  engineArgs:
                    description: EngineArgs shallow-merges on top of the source engineArgs,
                      overriding matching keys.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  engineEnv:
                    additionalProperties:
                      type: string
                    description: EngineEnv merges by key, overriding matching source
                      entries.
                    type: object
                  image:
                    description: |-
                      Image overrides the runtime container image used by the derived
                      profiles. When empty the deployment image is rebased onto the source
                      profile's base-image (status.baseImage) so private mirrors stay
                      self-contained.
                    type: string
                  modelId:
                    description: |-
                      ModelId stamps the derived profile's spec.modelId. When set, wins
                      over both the source profile's modelId AND the auto-derivation from
                      modelSources[0].modelId. REQUIRED when the enclosing selector has
                      role=base.
                    type: string
                  modelSources:
                    description: |-
                      ModelSources replaces the copied profile's modelSources. When
                      modelSources[0].modelId is set and overrides.modelId is unset, the
                      derived profile's modelId is auto-derived from modelSources[0]; an
                      explicit overrides.modelId always wins.
                    items:
                      description: |-
                        AIMModelSource describes a model artifact that must be downloaded for inference.
                        Discovery extracts these from the container's configuration to enable caching and validation.
                      properties:
                        env:
                          description: |-
                            Env specifies per-source credential overrides.
                            These variables are used for authentication when downloading this specific source.
                            Takes precedence over base-level env for the same variable name.
                          items:
                            description: EnvVar represents an environment variable
                              present in a Container.
                            properties:
                              name:
                                description: |-
                                  Name of the environment variable.
                                  May consist of any printable ASCII characters except '='.
                                type: string
                              value:
                                description: |-
                                  Variable references $(VAR_NAME) are expanded
                                  using the previously defined environment variables in the container and
                                  any service environment variables. If a variable cannot be resolved,
                                  the reference in the input string will be unchanged. Double $$ are reduced
                                  to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                  "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                  Escaped references will never be expanded, regardless of whether the variable
                                  exists or not.
                                  Defaults to "".
                                type: string
                              valueFrom:
                                description: Source for the environment variable's
                                  value. Cannot be used if value is not empty.
                                properties:
                                  configMapKeyRef:
                                    description: Selects a key of a ConfigMap.
                                    properties:
                                      key:
                                        description: The key to select.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the ConfigMap
                                          or its key must be defined
                                        type: boolean
                                    required:
                                    - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  fieldRef:
                                    description: |-
                                      Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                      spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                    properties:
                                      apiVersion:
                                        description: Version of the schema the FieldPath
                                          is written in terms of, defaults to "v1".
                                        type: string
                                      fieldPath:
                                        description: Path of the field to select in
                                          the specified API version.
                                        type: string
                                    required:
                                    - fieldPath
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  fileKeyRef:
                                    description: |-
                                      FileKeyRef selects a key of the env file.
                                      Requires the EnvFiles feature gate to be enabled.
                                    properties:
                                      key:
                                        description: |-
                                          The key within the env file. An invalid key will prevent the pod from starting.
                                          The keys defined within a source may consist of any printable ASCII characters except '='.
                                          During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                        type: string
                                      optional:
                                        default: false
                                        description: |-
                                          Specify whether the file or its key must be defined. If the file or key
                                          does not exist, then the env var is not published.
                                          If optional is set to true and the specified key does not exist,
                                          the environment variable will not be set in the Pod's containers.

                                          If optional is set to false and the specified key does not exist,
                                          an error will be returned during Pod creation.
                                        type: boolean
                                      path:
                                        description: |-
                                          The path within the volume from which to select the file.
                                          Must be relative and may not contain the '..' path or start with '..'.
                                        type: string
                                      volumeName:
                                        description: The name of the volume mount
                                          containing the env file.
                                        type: string
                                    required:
                                    - key
                                    - path
                                    - volumeName
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  resourceFieldRef:
                                    description: |-
                                      Selects a resource of the container: only resources limits and requests
                                      (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                    properties:
                                      containerName:
                                        description: 'Container name: required for
                                          volumes, optional for env vars'
                                        type: string
                                      divisor:
                                        anyOf:
                                        - type: integer
                                        - type: string
                                        description: Specifies the output format of
                                          the exposed resources, defaults to "1"
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      resource:
                                        description: 'Required: resource to select'
                                        type: string
                                    required:
                                    - resource
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  secretKeyRef:
                                    description: Selects a key of a secret in the
                                      pod's namespace
                                    properties:
                                      key:
                                        description: The key of the secret to select
                                          from.  Must be a valid secret key.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the Secret or
                                          its key must be defined
                                        type: boolean
                                    required:
                                    - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                type: object
                            required:
                            - name
                            type: object
                          type: array
                          x-kubernetes-list-map-keys:
                          - name
                          x-kubernetes-list-type: map
                        modelId:
                          description: |-
                            ModelID is the canonical identifier in {org}/{name} format.
                            Determines the cache mount path: /workspace/cache/{modelId}
                            For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                            For S3 sources, users define their own organizational structure.
                          pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                          type: string
                        precision:
                          allOf:
                          - enum:
                            - auto
                            - fp4
                            - fp8
                            - fp16
                            - fp32
                            - bf16
                            - int4
                            - int8
                          - enum:
                            - fp4
                            - fp8
                            - fp16
                            - fp32
                            - bf16
                            - int4
                            - int8
                          description: |-
                            Precision describes the runtime precision this source is compatible with.
                            Used to match model sources to profiles during custom weight onboarding.
                          type: string
                        size:
                          anyOf:
                          - type: integer
                          - type: string
                          description: |-
                            Size is the expected storage space required for this model artifact.
                            Used for PVC sizing and capacity planning during cache creation.
                            Optional - if not specified, the download job will discover the size automatically.
                            Can be set explicitly to pre-allocate storage or override auto-discovery.
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        sourceUri:
                          description: |-
                            SourceURI is the location from which the model should be downloaded.
                            Supported schemes:
                            - hf://org/model - Hugging Face Hub model
                            - s3://bucket/key - S3-compatible storage
                          pattern: ^(hf|s3)://[^ \t\r\n]+$
                          type: string
                      required:
                      - modelId
                      - sourceUri
                      type: object
                    type: array
                  profileId:
                    description: |-
                      ProfileId stamps the derived profile's spec.profileId. Optional —
                      most callers leave this empty and let the source profile's profileId
                      carry through (or the reconciler synthesise one).
                    type: string
                type: object
              selector:
                description: Selector chooses which source profiles to derive from.
                properties:
                  acceleratorCount:
                    description: AcceleratorCount filters by accelerator unit count.
                    format: int32
                    type: integer
                  acceleratorModel:
                    description: AcceleratorModel filters by accelerator identifier.
                    type: string
                  acceleratorPartitioningMode:
                    description: |-
                      AcceleratorPartitioningMode filters candidates by their declared
                      partitioning mode. Partial-order match (NOT strict equality):
                        ""              - no filter on this field.
                        "unpartitioned" - matches profiles with mode "" or "unpartitioned".
                        "partitioned"   - matches profiles whose mode is non-trivial (anything
                                          other than "" / "unpartitioned").
                        "<C>"           - selector-only convenience: matches profiles with mode
                                          "<C>-*" (prefix on the scheme). Not a valid profile-spec
                                          value (e.g. selector "CPX" matches "CPX-NPS1", "CPX-NPS4").
                        "<C>-<M>"       - exact-string match on the scheme.
                    type: string
                  acceleratorType:
                    description: AcceleratorType filters by accelerator resource type.
                    enum:
                    - gpu
                    - cpu
                    type: string
                  aimId:
                    description: AimId filters by model architecture identifier (e.g.,
                      "qwen/qwen3-32b").
                    type: string
                  engine:
                    description: Engine filters by inference engine.
                    type: string
                  engineArgs:
                    description: |-
                      EngineArgs partially matches source engineArgs: every provided top-level key must
                      exist in the source object with an equal value.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  metric:
                    description: Metric filters by optimization target.
                    enum:
                    - latency
                    - throughput
                    type: string
                  minimumType:
                    description: |-
                      MinimumType filters by a minimum optimization level: candidates whose
                      type is this tier OR BETTER are accepted (hierarchy: optimized > general
                      > preview > unoptimized). This is the floor counterpart to the exact-match
                      Type field; the two AND together when both are set.

                      The sentinel "any" disables the floor (accept every tier). When this
                      field is empty the AIMService resolver applies a default floor of
                      "optimized" so auto-selection prefers production-grade profiles and never
                      silently picks an unoptimized one; to opt a service into lower tiers
                      (e.g. CPU/EPYC profiles published as unoptimized) set minimumType
                      explicitly to "unoptimized" or "any". Derivation selectors
                      (AIMProfileSet / AIMModel.profiles) treat empty as "any" so copying is
                      never tier-restricted by default.
                    enum:
                    - optimized
                    - general
                    - preview
                    - unoptimized
                    - any
                    type: string
                  modelId:
                    description: ModelId filters by the source profile's specific
                      model identifier.
                    type: string
                  modelRef:
                    description: |-
                      ModelRef narrows candidates to those produced by a specific
                      AIM(Cluster)Model, matched via the `aim.eai.amd.com/source-model[-scope]`
                      labels stamped by the AIMModel reconcilers. Iteration 1 (v1alpha2 only).
                    properties:
                      name:
                        description: Name is the owning AIM(Cluster)Model name. Required.
                        minLength: 1
                        type: string
                      scope:
                        default: Auto
                        description: Scope controls how Name is resolved against AIMModel
                          vs AIMClusterModel.
                        enum:
                        - Auto
                        - Namespace
                        - Cluster
                        type: string
                    required:
                    - name
                    type: object
                  origin:
                    description: |-
                      Origin filters by the `aim.eai.amd.com/profile-origin` label. When unset
                      (empty) the selector does not filter by origin. Iteration 1 (v1alpha2
                      only).
                    enum:
                    - discovered
                    - derived
                    - user-authored
                    type: string
                  precision:
                    description: Precision filters by numeric precision.
                    enum:
                    - auto
                    - fp4
                    - fp8
                    - fp16
                    - fp32
                    - bf16
                    - int4
                    - int8
                    type: string
                  profileId:
                    description: ProfileId filters by the source profile's profile
                      identifier.
                    type: string
                  role:
                    default: deployable
                    description: |-
                      Role filters by the `aim.eai.amd.com/profile-role` label. Defaults to
                      `deployable`. `base` filters to base profiles emitted by base-image
                      discovery (custom-model derivation source material).
                    enum:
                    - base
                    - deployable
                    type: string
                  type:
                    description: Type filters by optimization level (exact match).
                    enum:
                    - optimized
                    - general
                    - preview
                    - unoptimized
                    type: string
                type: object
              serviceAccountName:
                description: ServiceAccountName is propagated to managed profiles
                  for downstream workloads.
                type: string
              sourceRef:
                description: |-
                  SourceRef points to an alternate discovery cache source.
                  When omitted, derivation uses visible AIMProfile and AIMClusterProfile objects.
                  For AIMProfileSet, the referenced ConfigMap is read from the same namespace.
                  For AIMClusterProfileSet, it is read from the operator namespace.
                properties:
                  name:
                    description: Name is the discovery cache ConfigMap name.
                    type: string
                required:
                - name
                type: object
              version:
                description: Version pins matching to a specific source profile version
                  when VersionPolicy is pinned.
                type: string
              versionPolicy:
                default: pinned
                description: VersionPolicy controls how matching profiles are filtered
                  by version.
                enum:
                - pinned
                - latest
                - all
                type: string
            type: object
            x-kubernetes-validations:
            - message: selector must set at least one matching field, or set sourceRef
              rule: has(self.sourceRef) || has(self.selector.aimId) || has(self.selector.modelId)
                || has(self.selector.profileId) || has(self.selector.engine) || has(self.selector.metric)
                || has(self.selector.precision) || has(self.selector.type) || has(self.selector.acceleratorModel)
                || has(self.selector.acceleratorType) || has(self.selector.acceleratorCount)
                || has(self.selector.engineArgs) || has(self.selector.modelRef) ||
                has(self.selector.origin)
            - message: version is required when versionPolicy is pinned
              rule: has(self.version) || (has(self.versionPolicy) && self.versionPolicy
                != 'pinned')
            - message: latest/all versionPolicy must not set version
              rule: '!has(self.versionPolicy) || (self.versionPolicy != ''latest''
                && self.versionPolicy != ''all'') || !has(self.version)'
            - message: selector.aimId/modelId/profileId are not allowed when selector.role=base;
                set overrides.aimId/modelId/profileId instead (base profiles have
                no source identity to filter on)
              rule: '!(has(self.selector.role) && self.selector.role == ''base'')
                || (!has(self.selector.aimId) && !has(self.selector.modelId) && !has(self.selector.profileId))'
            - message: selector.role=base requires overrides.aimId and overrides.modelId
                (base profiles carry no identity of their own; overrides supply it)
              rule: '!(has(self.selector.role) && self.selector.role == ''base'')
                || (has(self.overrides) && has(self.overrides.aimId) && has(self.overrides.modelId))'
            - message: overrides.acceleratorCount must be specified together with
                any overrides.acceleratorPartitioningMode; partition mode changes
                the per-unit interpretation of acceleratorCount
              rule: '!has(self.overrides) || !has(self.overrides.acceleratorPartitioningMode)
                || size(self.overrides.acceleratorPartitioningMode) == 0 || has(self.overrides.acceleratorCount)'
          status:
            description: AIMProfileSetStatus defines the observed state of AIMProfileSet
              / AIMClusterProfileSet.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the profile set's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              managedProfiles:
                description: ManagedProfiles summarizes the direct derivative profiles
                  owned or managed by this profile set.
                properties:
                  base:
                    description: |-
                      Base is the number of managed profiles whose spec is structurally
                      incomplete (missing aimId or modelSources). Base profiles cannot back
                      an AIMService directly and exist purely as source material for
                      derivation via AIMModel.spec.profiles.derivedFrom with
                      selector.role=base. A non-zero value is the canonical operational
                      signal that this model is a base-image model (the source of
                      custom-model derivation).
                    format: int32
                    type: integer
                  deployable:
                    description: |-
                      Deployable is the number of managed profiles whose spec is materialised
                      enough to back an AIMService (carries aimId and modelSources). For a
                      normal officially-discovered AIMModel this equals Total. For a
                      base-image AIMModel used as a source for custom-model derivation
                      this is 0 — the model only emits base profiles that callers must
                      derive into deployable profiles.
                    format: int32
                    type: integer
                  notAvailable:
                    description: NotAvailable is the number of derivative profiles
                      whose own status is NotAvailable.
                    format: int32
                    type: integer
                  ready:
                    description: Ready is the number of derivative profiles whose
                      own status is Ready.
                    format: int32
                    type: integer
                  total:
                    description: Total is the number of derivative profiles the controller
                      currently manages or intends to manage.
                    format: int32
                    type: integer
                required:
                - base
                - deployable
                - notAvailable
                - ready
                - total
                type: object
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              status:
                default: Pending
                description: Status represents the overall high-level status for the
                  profile set.
                enum:
                - Pending
                - Progressing
                - Ready
                - Degraded
                - Failed
                - NotAvailable
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: aimruntimeconfigs.aim.eai.amd.com
spec:
  group: aim.eai.amd.com
  names:
    categories:
    - aim
    - all
    kind: AIMRuntimeConfig
    listKind: AIMRuntimeConfigList
    plural: aimruntimeconfigs
    shortNames:
    - aimrc
    singular: aimruntimeconfig
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AIMRuntimeConfig is the Schema for namespace-scoped AIM runtime
          configurations.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AIMRuntimeConfigSpec defines namespace-scoped overrides for
              AIM resources.
            properties:
              artifact:
                description: |-
                  Artifact controls artifact-level defaults such as eviction policy.
                  This field only applies to RuntimeConfig/ClusterRuntimeConfig and is not available for services.
                properties:
                  defaultRetentionPriority:
                    description: |-
                      DefaultRetentionPriority sets the default retention priority for AIMArtifacts
                      that do not specify one in their spec. When set, artifacts without an explicit
                      retentionPriority become eligible for automatic eviction at this priority level.
                      Lower values are evicted first. If not set, artifacts without an explicit
                      retentionPriority are never automatically evicted.
                    format: int32
                    minimum: 0
                    type: integer
                  modelDownloadImage:
                    description: |-
                      ModelDownloadImage specifies the default container image for artifact
                      download and size-check jobs. Applies when an AIMArtifact does not set
                      spec.modelDownloadImage. When neither is set, the operator falls back
                      to its build-time default (matching the release version).
                    type: string
                type: object
              artifactCache:
                description: |-
                  ArtifactCache configures the S3-backed artifact cache for HuggingFace models.
                  When enabled, the controller checks internal S3 before downloading from HuggingFace.
                properties:
                  enabled:
                    description: Enabled controls whether the S3 artifact cache is
                      active.
                    type: boolean
                  env:
                    description: |-
                      Env provides S3 endpoint configuration for the cache bucket.
                      Injected into download jobs when the source is rewritten to s3://.
                      Typical vars: AWS_ENDPOINT_URL, S3_NO_SSL. Credentials optional (anonymous access).
                    items:
                      description: EnvVar represents an environment variable present
                        in a Container.
                      properties:
                        name:
                          description: |-
                            Name of the environment variable.
                            May consist of any printable ASCII characters except '='.
                          type: string
                        value:
                          description: |-
                            Variable references $(VAR_NAME) are expanded
                            using the previously defined environment variables in the container and
                            any service environment variables. If a variable cannot be resolved,
                            the reference in the input string will be unchanged. Double $$ are reduced
                            to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                            "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                            Escaped references will never be expanded, regardless of whether the variable
                            exists or not.
                            Defaults to "".
                          type: string
                        valueFrom:
                          description: Source for the environment variable's value.
                            Cannot be used if value is not empty.
                          properties:
                            configMapKeyRef:
                              description: Selects a key of a ConfigMap.
                              properties:
                                key:
                                  description: The key to select.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the ConfigMap or its
                                    key must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                            fieldRef:
                              description: |-
                                Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                              properties:
                                apiVersion:
                                  description: Version of the schema the FieldPath
                                    is written in terms of, defaults to "v1".
                                  type: string
                                fieldPath:
                                  description: Path of the field to select in the
                                    specified API version.
                                  type: string
                              required:
                              - fieldPath
                              type: object
                              x-kubernetes-map-type: atomic
                            fileKeyRef:
                              description: |-
                                FileKeyRef selects a key of the env file.
                                Requires the EnvFiles feature gate to be enabled.
                              properties:
                                key:
                                  description: |-
                                    The key within the env file. An invalid key will prevent the pod from starting.
                                    The keys defined within a source may consist of any printable ASCII characters except '='.
                                    During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                  type: string
                                optional:
                                  default: false
                                  description: |-
                                    Specify whether the file or its key must be defined. If the file or key
                                    does not exist, then the env var is not published.
                                    If optional is set to true and the specified key does not exist,
                                    the environment variable will not be set in the Pod's containers.

                                    If optional is set to false and the specified key does not exist,
                                    an error will be returned during Pod creation.
                                  type: boolean
                                path:
                                  description: |-
                                    The path within the volume from which to select the file.
                                    Must be relative and may not contain the '..' path or start with '..'.
                                  type: string
                                volumeName:
                                  description: The name of the volume mount containing
                                    the env file.
                                  type: string
                              required:
                              - key
                              - path
                              - volumeName
                              type: object
                              x-kubernetes-map-type: atomic
                            resourceFieldRef:
                              description: |-
                                Selects a resource of the container: only resources limits and requests
                                (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                              properties:
                                containerName:
                                  description: 'Container name: required for volumes,
                                    optional for env vars'
                                  type: string
                                divisor:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: Specifies the output format of the
                                    exposed resources, defaults to "1"
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                resource:
                                  description: 'Required: resource to select'
                                  type: string
                              required:
                              - resource
                              type: object
                              x-kubernetes-map-type: atomic
                            secretKeyRef:
                              description: Selects a key of a secret in the pod's
                                namespace
                              properties:
                                key:
                                  description: The key of the secret to select from.  Must
                                    be a valid secret key.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the Secret or its key
                                    must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  s3Uri:
                    description: S3URI is the base S3 path for cached artifacts (e.g.
                      s3://aim-cache/artifacts).
                    type: string
                type: object
              defaultStorageClassName:
                description: |-
                  DEPRECATED: Use Storage.DefaultStorageClassName instead. This field will be removed in a future version.
                  For backward compatibility, if this field is set and Storage.DefaultStorageClassName is not set,
                  the value will be automatically migrated.
                type: string
              env:
                description: |-
                  Env specifies environment variables for inference containers.
                  When set on AIMService, these take highest precedence in the merge hierarchy.
                  When set on RuntimeConfig, these provide namespace/cluster-level defaults.
                  Merge order (highest to lowest): Service.Env > Template.Env > RuntimeConfig.Env > Profile.Env
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: |-
                        Name of the environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        fileKeyRef:
                          description: |-
                            FileKeyRef selects a key of the env file.
                            Requires the EnvFiles feature gate to be enabled.
                          properties:
                            key:
                              description: |-
                                The key within the env file. An invalid key will prevent the pod from starting.
                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                              type: string
                            optional:
                              default: false
                              description: |-
                                Specify whether the file or its key must be defined. If the file or key
                                does not exist, then the env var is not published.
                                If optional is set to true and the specified key does not exist,
                                the environment variable will not be set in the Pod's containers.

                                If optional is set to false and the specified key does not exist,
                                an error will be returned during Pod creation.
                              type: boolean
                            path:
                              description: |-
                                The path within the volume from which to select the file.
                                Must be relative and may not contain the '..' path or start with '..'.
                              type: string
                            volumeName:
                              description: The name of the volume mount containing
                                the env file.
                              type: string
                          required:
                          - key
                          - path
                          - volumeName
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              labelPropagation:
                description: |-
                  LabelPropagation controls how labels from parent AIM resources are propagated to child resources.
                  When enabled, labels matching the specified patterns are automatically copied from parent resources
                  (e.g., AIMService, AIMTemplateCache) to their child resources (e.g., Deployments, Services, PVCs).
                  This is useful for propagating organizational metadata like cost centers, team identifiers,
                  or compliance labels through the resource hierarchy.
                properties:
                  enabled:
                    default: false
                    description: |-
                      Enabled, if true, allows propagating parent labels to all child resources it creates directly
                      Only label keys that match the ones in Match are propagated.
                    type: boolean
                  match:
                    description: |-
                      Match is a list of label keys that will be propagated to any child resources created.
                      Wildcards are supported, so for example `org.my/my-key-*` would match any label with that prefix.
                    items:
                      type: string
                    type: array
                type: object
              model:
                description: |-
                  Model controls model creation and discovery defaults.
                  This field only applies to RuntimeConfig/ClusterRuntimeConfig and is not available for services.
                properties:
                  autoDiscovery:
                    description: |-
                      AutoDiscovery controls whether models run discovery by default.
                      When true, models run discovery jobs to extract metadata and auto-create templates.
                      When false, discovery is skipped. Discovery failures are non-fatal and reported via conditions.
                    type: boolean
                type: object
              pvcHeadroomPercent:
                description: |-
                  DEPRECATED: Use Storage.PVCHeadroomPercent instead. This field will be removed in a future version.
                  For backward compatibility, if this field is set and Storage.PVCHeadroomPercent is not set,
                  the value will be automatically migrated.
                format: int32
                type: integer
              routing:
                description: |-
                  Routing controls HTTP routing configuration for this service.
                  When set, these values override namespace/cluster runtime config defaults.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: |-
                      Annotations defines default annotations to add to all HTTPRoute resources.
                      Services can add additional annotations or override these via spec.routing.annotations.
                      When both are specified, service annotations take precedence for conflicting keys.
                      Common use cases include ingress controller settings, rate limiting, monitoring labels,
                      and security policies that should apply to all services using this config.
                    type: object
                  enabled:
                    description: |-
                      Enabled controls whether HTTP routing is managed for inference services using this config.
                      When true, the operator creates HTTPRoute resources for services that reference this config.
                      When false or unset, routing must be explicitly enabled on each service.
                      This provides a namespace or cluster-wide default that individual services can override.
                    type: boolean
                  gatewayRef:
                    description: |-
                      GatewayRef specifies the Gateway API Gateway resource that should receive HTTPRoutes.
                      This identifies the parent gateway for routing traffic to inference services.
                      The gateway can be in any namespace (cross-namespace references are supported).
                      If routing is enabled but GatewayRef is not specified, service reconciliation will fail
                      with a validation error.
                    properties:
                      group:
                        default: gateway.networking.k8s.io
                        description: |-
                          Group is the group of the referent.
                          When unspecified, "gateway.networking.k8s.io" is inferred.
                          To set the core API group (such as for a "Service" kind referent),
                          Group must be explicitly set to "" (empty string).

                          Support: Core
                        maxLength: 253
                        pattern: ^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                        type: string
                      kind:
                        default: Gateway
                        description: |-
                          Kind is kind of the referent.

                          There are two kinds of parent resources with "Core" support:

                          * Gateway (Gateway conformance profile)
                          * Service (Mesh conformance profile, ClusterIP Services only)

                          Support for other resources is Implementation-Specific.
                        maxLength: 63
                        minLength: 1
                        pattern: ^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$
                        type: string
                      name:
                        description: |-
                          Name is the name of the referent.

                          Support: Core
                        maxLength: 253
                        minLength: 1
                        type: string
                      namespace:
                        description: |-
                          Namespace is the namespace of the referent. When unspecified, this refers
                          to the local namespace of the Route.

                          Note that there are specific rules for ParentRefs which cross namespace
                          boundaries. Cross-namespace references are only valid if they are explicitly
                          allowed by something in the namespace they are referring to. For example:
                          Gateway has the AllowedRoutes field, and ReferenceGrant provides a
                          generic way to enable any other kind of cross-namespace reference.

                          <gateway:experimental:description>
                          ParentRefs from a Route to a Service in the same namespace are "producer"
                          routes, which apply default routing rules to inbound connections from
                          any namespace to the Service.

                          ParentRefs from a Route to a Service in a different namespace are
                          "consumer" routes, and these routing rules are only applied to outbound
                          connections originating from the same namespace as the Route, for which
                          the intended destination of the connections are a Service targeted as a
                          ParentRef of the Route.
                          </gateway:experimental:description>

                          Support: Core
                        maxLength: 63
                        minLength: 1
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                        type: string
                      port:
                        description: |-
                          Port is the network port this Route targets. It can be interpreted
                          differently based on the type of parent resource.

                          When the parent resource is a Gateway, this targets all listeners
                          listening on the specified port that also support this kind of Route(and
                          select this Route). It's not recommended to set `Port` unless the
                          networking behaviors specified in a Route must apply to a specific port
                          as opposed to a listener(s) whose port(s) may be changed. When both Port
                          and SectionName are specified, the name and port of the selected listener
                          must match both specified values.

                          <gateway:experimental:description>
                          When the parent resource is a Service, this targets a specific port in the
                          Service spec. When both Port (experimental) and SectionName are specified,
                          the name and port of the selected port must match both specified values.
                          </gateway:experimental:description>

                          Implementations MAY choose to support other parent resources.
                          Implementations supporting other types of parent resources MUST clearly
                          document how/if Port is interpreted.

                          For the purpose of status, an attachment is considered successful as
                          long as the parent resource accepts it partially. For example, Gateway
                          listeners can restrict which Routes can attach to them by Route kind,
                          namespace, or hostname. If 1 of 2 Gateway listeners accept attachment
                          from the referencing Route, the Route MUST be considered successfully
                          attached. If no Gateway listeners accept attachment from this Route,
                          the Route MUST be considered detached from the Gateway.

                          Support: Extended
                        format: int32
                        maximum: 65535
                        minimum: 1
                        type: integer
                      sectionName:
                        description: |-
                          SectionName is the name of a section within the target resource. In the
                          following resources, SectionName is interpreted as the following:

                          * Gateway: Listener name. When both Port (experimental) and SectionName
                          are specified, the name and port of the selected listener must match
                          both specified values.
                          * Service: Port name. When both Port (experimental) and SectionName
                          are specified, the name and port of the selected listener must match
                          both specified values.

                          Implementations MAY choose to support attaching Routes to other resources.
                          If that is the case, they MUST clearly document how SectionName is
                          interpreted.

                          When unspecified (empty string), this will reference the entire resource.
                          For the purpose of status, an attachment is considered successful if at
                          least one section in the parent resource accepts it. For example, Gateway
                          listeners can restrict which Routes can attach to them by Route kind,
                          namespace, or hostname. If 1 of 2 Gateway listeners accept attachment from
                          the referencing Route, the Route MUST be considered successfully
                          attached. If no Gateway listeners accept attachment from this Route, the
                          Route MUST be considered detached from the Gateway.

                          Support: Core
                        maxLength: 253
                        minLength: 1
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                        type: string
                    required:
                    - name
                    type: object
                  hostnames:
                    description: |-
                      Hostnames pins generated HTTPRoutes to these hostnames so a route only
                      attaches to the matching Gateway listener instead of every listener on
                      the parent gateway. Without a hostname, an HTTPRoute matches all of the
                      parent gateway's listener hostnames, which can expose a service on
                      listeners that do not enforce the intended authentication.

                      This field is required when the parent gateway exposes more than one
                      listener: in that case a service with routing enabled but no hostnames
                      configured will not get an HTTPRoute and reports ConfigValid=False with
                      reason RouteHostnameRequired. When the parent gateway has a single
                      listener, leaving this empty preserves the existing behavior (the route
                      inherits that listener's hostnames).

                      Individual services can override this list via spec.routing.hostnames.
                    items:
                      description: |-
                        Hostname is the fully qualified domain name of a network host. This matches
                        the RFC 1123 definition of a hostname with 2 notable exceptions:

                         1. IPs are not allowed.
                         2. A hostname may be prefixed with a wildcard label (`*.`). The wildcard
                            label must appear by itself as the first label.

                        Hostname can be "precise" which is a domain name without the terminating
                        dot of a network host (e.g. "foo.example.com") or "wildcard", which is a
                        domain name prefixed with a single wildcard label (e.g. `*.example.com`).

                        Note that as per RFC1035 and RFC1123, a *label* must consist of lower case
                        alphanumeric characters or '-', and must start and end with an alphanumeric
                        character. No other punctuation is allowed.
                      maxLength: 253
                      minLength: 1
                      pattern: ^(\*\.)?[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                      type: string
                    type: array
                  pathTemplate:
                    description: |-
                      PathTemplate defines the HTTP path template for routes, evaluated using JSONPath expressions.
                      The template is rendered against the AIMService object to generate unique paths.

                      Example templates:
                      - `/{.metadata.namespace}/{.metadata.name}` - namespace and service name
                      - `/{.metadata.namespace}/{.metadata.labels['team']}/inference` - with label
                      - `/models/{.metadata.name}` - based on service name

                      The template must:
                      - Use valid JSONPath expressions wrapped in {...}
                      - Reference fields that exist on the service
                      - Produce a path ≤ 200 characters after rendering
                      - Result in valid URL path segments (lowercase, RFC 1123 compliant)

                      If evaluation fails, the service enters Degraded state with PathTemplateInvalid reason.
                      Individual services can override this template via spec.routing.pathTemplate.
                    type: string
                  requestTimeout:
                    description: |-
                      RequestTimeout defines the HTTP request timeout for routes.
                      This sets the maximum duration for a request to complete before timing out.
                      The timeout applies to the entire request/response cycle.
                      If not specified, no timeout is set on the route.
                      Individual services can override this value via spec.routing.requestTimeout.
                    type: string
                type: object
              storage:
                description: |-
                  Storage configures storage defaults for this service's PVCs and caches.
                  When set, these values override namespace/cluster runtime config defaults.
                properties:
                  adapterDiskSize:
                    anyOf:
                    - type: integer
                    - type: string
                    description: |-
                      AdapterDiskSize is the cluster default size for the shared adapter disk PVC
                      (built-in default when unset). An artifact's adapterDisk.size wins over it.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  adapterDiskStorageClassName:
                    description: |-
                      AdapterDiskStorageClassName is the storage class for the shared
                      ReadWriteMany adapter disk. It must be RWX-capable (e.g. longhorn, NFS) and
                      is resolved before the (typically RWO) DefaultStorageClassName.
                    type: string
                  defaultStorageClassName:
                    description: |-
                      DefaultStorageClassName specifies the storage class to use for artifacts and PVCs
                      when the consuming resource (AIMArtifact, AIMTemplateCache, AIMServiceTemplate) does not
                      specify a storage class. If this field is empty, the cluster's default storage class is used.
                    type: string
                  downloadFilter:
                    description: |-
                      DownloadFilter controls which files are included or excluded during artifact downloads.
                      When set here, applies as the default for all artifacts using this runtime config.
                      Individual artifacts can override this with their own downloadFilter.
                      When no filter is configured at any level, subdirectory files are excluded by default.
                      Set to an empty object (downloadFilter: {}) to explicitly allow all files.
                    properties:
                      exclude:
                        description: |-
                          Exclude specifies glob patterns for files to skip.
                          Files matching any exclude pattern are removed after include filtering.
                          Patterns use fnmatch syntax (e.g., ["*/*", "*.bin"]).
                          Use ["*/*"] to exclude all files in subdirectories (the default when no filter is set).
                        items:
                          type: string
                        type: array
                      include:
                        description: |-
                          Include specifies glob patterns for files to download.
                          Only files matching at least one pattern are considered.
                          If empty, all files pass the include check.
                          Patterns use fnmatch syntax (e.g., ["*.safetensors", "config.json"]).
                        items:
                          type: string
                        type: array
                    type: object
                  pvcHeadroomPercent:
                    default: 10
                    description: |-
                      PVCHeadroomPercent specifies the percentage of extra space to add to PVCs
                      for model storage. This accounts for filesystem overhead and temporary files
                      during model loading. The value represents a percentage (e.g., 10 means 10% extra space).
                      If not specified, defaults to 10%.
                    format: int32
                    minimum: 0
                    type: integer
                type: object
            type: object
          status:
            description: AIMRuntimeConfigStatus records the resolved config reference
              surfaced to consumers.
            properties:
              conditions:
                description: Conditions communicate reconciliation progress.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the last reconciled generation.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: aimservices.aim.eai.amd.com
spec:
  group: aim.eai.amd.com
  names:
    categories:
    - aim
    - all
    kind: AIMService
    listKind: AIMServiceList
    plural: aimservices
    shortNames:
    - aimsvc
    singular: aimservice
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.resolvedModel.name
      name: Model
      type: string
    - jsonPath: .status.resolvedTemplate.name
      name: Template
      type: string
    - jsonPath: .status.resolvedProfile.name
      name: Profile
      priority: 1
      type: string
    - jsonPath: .status.runtime.replicas
      name: Replicas
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    deprecated: true
    deprecationWarning: aim.eai.amd.com/v1alpha1 AIMService is deprecated; use aim.eai.amd.com/v1alpha2
      (spec.profile) instead. The v1alpha1 model/template fields may be removed in
      a future release.
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          AIMService manages a KServe-based AIM inference service for the selected model and template.
          Note: KServe uses {name}-{namespace} format which must not exceed 63 characters.
          This constraint is validated at runtime since CEL cannot access metadata.namespace.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              AIMServiceSpec defines the desired state of AIMService.

              Binds a canonical model to an AIMServiceTemplate and configures replicas,
              caching behavior, and optional overrides. The template governs the base
              runtime selection knobs, while the overrides field allows service-specific
              customization.

              With v1alpha2, a Profile can be used instead of a Template. Template and Profile
              are mutually exclusive — at least one resolution path must be specified.
            properties:
              adapterMode:
                default: static
                description: |-
                  AdapterMode is the immutable adapter contract for the service, mapped
                  directly to the AIM_ADAPTER_MODE container env. static (default) freezes
                  spec.adapters and mounts the adapter disk only when adapters are declared;
                  dynamic allows editing spec.adapters and mounts the disk even at zero
                  adapters (so add/remove never restarts the pod). It is immutable after
                  creation.
                enum:
                - static
                - dynamic
                type: string
                x-kubernetes-validations:
                - message: spec.adapterMode is immutable after creation
                  rule: self == oldSelf
              adapters:
                description: |-
                  Adapters is the load-bearing list of LoRA adapters this service serves.
                  The list may only be edited after creation when adapterMode is dynamic
                  (static freezes it). Omitting the list serves no adapters.
                  Supported on both the template (v1alpha1) and profile (v1alpha2) pipelines:
                  the base model the adapters attach to is resolved from the service's
                  template cache or profile cache respectively. In dynamic mode the list may
                  be edited after creation: adding an adapter stages it into the service's
                  subtree and the aim-runtime hot-loads it; removing one lets the runtime
                  unload it (subtree cleanup is reclaimed out-of-band). The InferenceService
                  is never modified for adapter changes — it mounts the whole per-service
                  subtree read-only. Entries are pure references; (kind, name) pairs must be
                  unique. Omitting the list serves no adapters.
                items:
                  description: |-
                    AIMServiceAdapterReference is a typed reference to a LoRA adapter served by this
                    service. Today it is resolved as a pure reference to an existing adapter
                    artifact. The inline bootstrap fields (sourceUri/modelId/rank) are reserved:
                    the schema accepts them, but create-if-missing self-healing is not yet wired,
                    so a referenced adapter artifact must currently exist.
                  properties:
                    kind:
                      description: Kind is the kind of the referenced adapter. Required;
                        restricted to AIMArtifact in v1.
                      enum:
                      - AIMArtifact
                      type: string
                    modelId:
                      description: |-
                        ModelID is the adapter's canonical model id. Required when SourceURI is set.
                        RESERVED: only meaningful alongside SourceURI.
                      type: string
                    name:
                      description: Name is the metadata.name of the referenced adapter
                        artifact.
                      maxLength: 253
                      minLength: 1
                      type: string
                    rank:
                      description: |-
                        Rank is the optional LoRA rank for the bootstrapped adapter. RESERVED: only
                        meaningful alongside SourceURI.
                      format: int32
                      minimum: 1
                      type: integer
                    sourceUri:
                      description: |-
                        SourceURI is an optional create-if-missing bootstrap source. When set and no
                        adapter artifact named Name exists, a later release will create one from this
                        source; a pre-existing artifact always wins. RESERVED: not yet acted on.
                      type: string
                  required:
                  - kind
                  - name
                  type: object
                  x-kubernetes-validations:
                  - message: modelId is required when sourceUri is set
                    rule: '!has(self.sourceUri) || has(self.modelId)'
                  - message: modelId and rank are only allowed together with sourceUri
                    rule: has(self.sourceUri) || (!has(self.modelId) && !has(self.rank))
                maxItems: 64
                type: array
                x-kubernetes-list-type: atomic
              autoScaling:
                description: |-
                  AutoScaling configures advanced autoscaling behavior using KEDA.
                  Supports custom metrics from OpenTelemetry backend.
                  When specified, MinReplicas and MaxReplicas should also be set.
                properties:
                  cooldownPeriod:
                    description: |-
                      CooldownPeriod is the seconds-of-inactivity budget KEDA waits before
                      scaling back to minReplicaCount. Under scale-to-zero, defaults to a
                      memory-derived value (300-1200s); otherwise unset (KEDA default of 300).
                    format: int32
                    minimum: 0
                    type: integer
                  metrics:
                    description: |-
                      Metrics is a list of metrics to be used for autoscaling.
                      Each metric defines a source (PodMetric) and target values.
                    items:
                      description: |-
                        AIMServiceMetricsSpec defines a single metric for autoscaling.
                        Specifies the metric source type and configuration.
                      properties:
                        podmetric:
                          description: |-
                            PodMetric refers to a metric describing each pod in the current scale target.
                            Used when Type is "PodMetric". Supports backends like OpenTelemetry for custom metrics.
                          properties:
                            metric:
                              description: |-
                                Metric contains the metric identification and backend configuration.
                                Defines which metrics to collect and how to query them.
                              properties:
                                backend:
                                  default: opentelemetry
                                  description: |-
                                    Backend defines the metrics backend to use.
                                    If not specified, defaults to "opentelemetry".
                                  enum:
                                  - opentelemetry
                                  type: string
                                metricNames:
                                  description: |-
                                    MetricNames specifies which metrics to collect from pods and send to ServerAddress.
                                    Example: ["vllm:num_requests_running"]
                                  items:
                                    type: string
                                  type: array
                                operationOverTime:
                                  description: |-
                                    OperationOverTime specifies the operation to aggregate metrics over time.
                                    Valid values: "last_one", "avg", "max", "min", "rate", "count"
                                    Default: "last_one"
                                  type: string
                                query:
                                  description: |-
                                    Query specifies the query to run to retrieve metrics from the backend.
                                    The query syntax depends on the backend being used.
                                    Example: "vllm:num_requests_running" for OpenTelemetry.
                                  type: string
                                serverAddress:
                                  description: |-
                                    ServerAddress specifies the address of the metrics backend server.
                                    If not specified, defaults to "keda-otel-scaler.keda.svc:4317" for OpenTelemetry backend.
                                  type: string
                              type: object
                              x-kubernetes-validations:
                              - message: podmetric.metric must set query or metricNames
                                  so the scaler has something to query
                                rule: has(self.query) || (has(self.metricNames) &&
                                  size(self.metricNames) > 0)
                            target:
                              description: |-
                                Target specifies the target value for the metric.
                                The autoscaler will scale to maintain this target value.
                              properties:
                                averageUtilization:
                                  description: |-
                                    AverageUtilization is the target value of the average of the resource metric across all relevant pods,
                                    represented as a percentage of the requested value of the resource for the pods.
                                    Used when Type is "Utilization". Only valid for Resource metric source type.
                                    Example: 80 for 80% utilization
                                  format: int32
                                  type: integer
                                averageValue:
                                  description: |-
                                    AverageValue is the target value of the average of the metric across all relevant pods (as a quantity).
                                    Used when Type is "AverageValue".
                                    Example: "100m" for 100 millicores per pod
                                  type: string
                                type:
                                  description: |-
                                    Type specifies how to interpret the metric value.
                                    "Value": absolute value target (use Value field)
                                    "AverageValue": average value across all pods (use AverageValue field)
                                    "Utilization": percentage utilization for resource metrics (use AverageUtilization field)
                                  enum:
                                  - Value
                                  - AverageValue
                                  - Utilization
                                  type: string
                                value:
                                  description: |-
                                    Value is the target value of the metric (as a quantity).
                                    Used when Type is "Value".
                                    Example: "1" for 1 request, "100m" for 100 millicores
                                  type: string
                              required:
                              - type
                              type: object
                              x-kubernetes-validations:
                              - message: target type Value requires target.value
                                rule: self.type != 'Value' || has(self.value)
                              - message: target type AverageValue requires target.averageValue
                                rule: self.type != 'AverageValue' || has(self.averageValue)
                              - message: target type Utilization requires target.averageUtilization
                                rule: self.type != 'Utilization' || has(self.averageUtilization)
                          required:
                          - metric
                          - target
                          type: object
                        type:
                          description: |-
                            Type is the type of metric source.
                            Valid values: "PodMetric" (per-pod custom metrics).
                          enum:
                          - PodMetric
                          type: string
                      required:
                      - type
                      type: object
                      x-kubernetes-validations:
                      - message: metrics entry of type PodMetric must set podmetric
                        rule: self.type != 'PodMetric' || has(self.podmetric)
                    type: array
                  pollingInterval:
                    description: |-
                      PollingInterval is the KEDA polling interval in seconds. Defaults to 5
                      when spec.minReplicas == 0 (so a single request reliably activates the
                      deployment within ~10s); otherwise unset (KEDA default of 30 applies).
                    format: int32
                    minimum: 1
                    type: integer
                type: object
                x-kubernetes-validations:
                - message: 'spec.autoScaling must not be empty: set at least one of
                    metrics, pollingInterval, or cooldownPeriod, or omit the autoScaling
                    block to use default scaling'
                  rule: (has(self.metrics) && size(self.metrics) > 0) || has(self.pollingInterval)
                    || has(self.cooldownPeriod)
              cacheModel:
                description: |-
                  DEPRECATED: Use Caching.Mode instead. This field will be removed in a future version.
                  This field is no longer honored by the controller.
                type: boolean
              caching:
                description: |-
                  Caching controls caching behavior for this service.
                  When nil, defaults to Shared mode.
                properties:
                  env:
                    description: |-
                      Env supplies credentials for model downloads (for example a HuggingFace
                      token via secretKeyRef). Unlike the inference container env, these
                      variables reach only the model-download Job, so download-only secrets are
                      never injected into the serving container. They are also reachable for
                      cluster-scoped and overlay profiles, where the profile's own caching.env
                      does not exist. Merged over the profile's caching.env (service wins).
                    items:
                      description: EnvVar represents an environment variable present
                        in a Container.
                      properties:
                        name:
                          description: |-
                            Name of the environment variable.
                            May consist of any printable ASCII characters except '='.
                          type: string
                        value:
                          description: |-
                            Variable references $(VAR_NAME) are expanded
                            using the previously defined environment variables in the container and
                            any service environment variables. If a variable cannot be resolved,
                            the reference in the input string will be unchanged. Double $$ are reduced
                            to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                            "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                            Escaped references will never be expanded, regardless of whether the variable
                            exists or not.
                            Defaults to "".
                          type: string
                        valueFrom:
                          description: Source for the environment variable's value.
                            Cannot be used if value is not empty.
                          properties:
                            configMapKeyRef:
                              description: Selects a key of a ConfigMap.
                              properties:
                                key:
                                  description: The key to select.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the ConfigMap or its
                                    key must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                            fieldRef:
                              description: |-
                                Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                              properties:
                                apiVersion:
                                  description: Version of the schema the FieldPath
                                    is written in terms of, defaults to "v1".
                                  type: string
                                fieldPath:
                                  description: Path of the field to select in the
                                    specified API version.
                                  type: string
                              required:
                              - fieldPath
                              type: object
                              x-kubernetes-map-type: atomic
                            fileKeyRef:
                              description: |-
                                FileKeyRef selects a key of the env file.
                                Requires the EnvFiles feature gate to be enabled.
                              properties:
                                key:
                                  description: |-
                                    The key within the env file. An invalid key will prevent the pod from starting.
                                    The keys defined within a source may consist of any printable ASCII characters except '='.
                                    During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                  type: string
                                optional:
                                  default: false
                                  description: |-
                                    Specify whether the file or its key must be defined. If the file or key
                                    does not exist, then the env var is not published.
                                    If optional is set to true and the specified key does not exist,
                                    the environment variable will not be set in the Pod's containers.

                                    If optional is set to false and the specified key does not exist,
                                    an error will be returned during Pod creation.
                                  type: boolean
                                path:
                                  description: |-
                                    The path within the volume from which to select the file.
                                    Must be relative and may not contain the '..' path or start with '..'.
                                  type: string
                                volumeName:
                                  description: The name of the volume mount containing
                                    the env file.
                                  type: string
                              required:
                              - key
                              - path
                              - volumeName
                              type: object
                              x-kubernetes-map-type: atomic
                            resourceFieldRef:
                              description: |-
                                Selects a resource of the container: only resources limits and requests
                                (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                              properties:
                                containerName:
                                  description: 'Container name: required for volumes,
                                    optional for env vars'
                                  type: string
                                divisor:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: Specifies the output format of the
                                    exposed resources, defaults to "1"
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                resource:
                                  description: 'Required: resource to select'
                                  type: string
                              required:
                              - resource
                              type: object
                              x-kubernetes-map-type: atomic
                            secretKeyRef:
                              description: Selects a key of a secret in the pod's
                                namespace
                              properties:
                                key:
                                  description: The key of the secret to select from.  Must
                                    be a valid secret key.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the Secret or its key
                                    must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  mode:
                    default: Shared
                    description: |-
                      Mode controls when to use caching.
                      Canonical values:
                      - Shared (default): reuse/create shared cache assets
                      - Dedicated: create service-owned dedicated cache assets

                      Legacy values are accepted and normalized:
                      - Always -> Shared
                      - Auto -> Shared
                      - Never -> Dedicated
                    enum:
                    - Dedicated
                    - Shared
                    - Auto
                    - Always
                    - Never
                    type: string
                type: object
                x-kubernetes-validations:
                - message: caching mode is immutable after creation
                  rule: self.mode == oldSelf.mode
              env:
                description: |-
                  Env specifies environment variables for inference containers.
                  When set on AIMService, these take highest precedence in the merge hierarchy.
                  When set on RuntimeConfig, these provide namespace/cluster-level defaults.
                  Merge order (highest to lowest): Service.Env > Template.Env > RuntimeConfig.Env > Profile.Env
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: |-
                        Name of the environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        fileKeyRef:
                          description: |-
                            FileKeyRef selects a key of the env file.
                            Requires the EnvFiles feature gate to be enabled.
                          properties:
                            key:
                              description: |-
                                The key within the env file. An invalid key will prevent the pod from starting.
                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                              type: string
                            optional:
                              default: false
                              description: |-
                                Specify whether the file or its key must be defined. If the file or key
                                does not exist, then the env var is not published.
                                If optional is set to true and the specified key does not exist,
                                the environment variable will not be set in the Pod's containers.

                                If optional is set to false and the specified key does not exist,
                                an error will be returned during Pod creation.
                              type: boolean
                            path:
                              description: |-
                                The path within the volume from which to select the file.
                                Must be relative and may not contain the '..' path or start with '..'.
                              type: string
                            volumeName:
                              description: The name of the volume mount containing
                                the env file.
                              type: string
                          required:
                          - key
                          - path
                          - volumeName
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              imagePullSecrets:
                description: ImagePullSecrets references secrets for pulling AIM container
                  images.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              maxReplicas:
                description: |-
                  MaxReplicas specifies the maximum number of replicas for autoscaling.
                  Required when MinReplicas is set or when AutoScaling configuration is provided.
                format: int32
                minimum: 1
                type: integer
              minReplicas:
                description: |-
                  MinReplicas specifies the minimum number of replicas for autoscaling.
                  Defaults to 1. Set to 0 to enable scale-to-zero: KEDA idles the predictor
                  to zero replicas when idle and brings it back up on the next request.
                  When specified with MaxReplicas, enables autoscaling for the service.
                format: int32
                minimum: 0
                type: integer
              model:
                description: |-
                  Model specifies which model to deploy using one of the available reference methods.
                  Use `name` to reference an existing AIMModel/AIMClusterModel by name, or use `image`
                  to specify a container image URI directly (which will auto-create a model if needed).
                  Required for v1alpha1 (template path), not permitted for v1alpha2 (profile path).
                properties:
                  custom:
                    description: |-
                      Custom specifies a custom model configuration with explicit base image,
                      model sources, and hardware requirements. The controller will search for
                      an existing matching AIMModel or auto-create one if not found.
                    properties:
                      baseImage:
                        description: |-
                          BaseImage is the container image URI for the AIM base image.
                          This will be used as the image for the auto-created AIMModel.
                          Example: `ghcr.io/silogen/aim-base:0.7.0`
                        type: string
                      hardware:
                        description: |-
                          Hardware specifies the GPU and CPU requirements for this custom model.
                          GPU is optional - if not set, no GPUs are requested (CPU-only model).
                        properties:
                          cpu:
                            description: CPU specifies CPU requirements.
                            properties:
                              limits:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Limits is the maximum number of CPU cores
                                  to allow.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              requests:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Requests is the number of CPU cores to
                                  request. Required and must be > 0.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            required:
                            - requests
                            type: object
                          gpu:
                            description: GPU specifies GPU requirements. If not set,
                              no GPUs are requested (CPU-only model).
                            properties:
                              minVram:
                                anyOf:
                                - type: integer
                                - type: string
                                description: |-
                                  MinVRAM limits deployment to GPUs having at least this much VRAM.
                                  Used for capacity planning when the model size is known but any GPU with
                                  sufficient VRAM is acceptable.
                                  Cannot be combined with model.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              model:
                                description: |-
                                  Model limits deployment to a specific GPU model.
                                  Example: "MI300X"
                                  Cannot be combined with minVram.
                                maxLength: 64
                                type: string
                              requests:
                                description: |-
                                  Requests is the number of GPUs to set as requests/limits.
                                  Set to 0 to target GPU nodes without consuming GPU resources (useful for testing).
                                format: int32
                                minimum: 0
                                type: integer
                              resourceName:
                                default: amd.com/gpu
                                description: |-
                                  ResourceName is the Kubernetes resource name for GPU resources.
                                  Defaults to "amd.com/gpu" if not specified.
                                type: string
                            type: object
                            x-kubernetes-validations:
                            - message: model and minVram are mutually exclusive; specify
                                either a specific GPU model OR a minimum VRAM requirement,
                                not both
                              rule: '!(has(self.model) && size(self.model) > 0 &&
                                has(self.minVram))'
                        type: object
                        x-kubernetes-validations:
                        - message: at least one of gpu or cpu must be specified
                          rule: has(self.gpu) || has(self.cpu)
                      modelSources:
                        description: |-
                          ModelSources specifies the model sources to use.
                          The controller will search for or create an AIMModel with these sources.
                          The size field is optional - if not specified, it will be discovered by the download job.
                          AIM runtime currently supports only one model source.
                        items:
                          description: |-
                            AIMModelSource describes a model artifact that must be downloaded for inference.
                            Discovery extracts these from the container's configuration to enable caching and validation.
                          properties:
                            env:
                              description: |-
                                Env specifies per-source credential overrides.
                                These variables are used for authentication when downloading this specific source.
                                Takes precedence over base-level env for the same variable name.
                              items:
                                description: EnvVar represents an environment variable
                                  present in a Container.
                                properties:
                                  name:
                                    description: |-
                                      Name of the environment variable.
                                      May consist of any printable ASCII characters except '='.
                                    type: string
                                  value:
                                    description: |-
                                      Variable references $(VAR_NAME) are expanded
                                      using the previously defined environment variables in the container and
                                      any service environment variables. If a variable cannot be resolved,
                                      the reference in the input string will be unchanged. Double $$ are reduced
                                      to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                      "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                      Escaped references will never be expanded, regardless of whether the variable
                                      exists or not.
                                      Defaults to "".
                                    type: string
                                  valueFrom:
                                    description: Source for the environment variable's
                                      value. Cannot be used if value is not empty.
                                    properties:
                                      configMapKeyRef:
                                        description: Selects a key of a ConfigMap.
                                        properties:
                                          key:
                                            description: The key to select.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the ConfigMap
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fieldRef:
                                        description: |-
                                          Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                          spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                        properties:
                                          apiVersion:
                                            description: Version of the schema the
                                              FieldPath is written in terms of, defaults
                                              to "v1".
                                            type: string
                                          fieldPath:
                                            description: Path of the field to select
                                              in the specified API version.
                                            type: string
                                        required:
                                        - fieldPath
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fileKeyRef:
                                        description: |-
                                          FileKeyRef selects a key of the env file.
                                          Requires the EnvFiles feature gate to be enabled.
                                        properties:
                                          key:
                                            description: |-
                                              The key within the env file. An invalid key will prevent the pod from starting.
                                              The keys defined within a source may consist of any printable ASCII characters except '='.
                                              During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                            type: string
                                          optional:
                                            default: false
                                            description: |-
                                              Specify whether the file or its key must be defined. If the file or key
                                              does not exist, then the env var is not published.
                                              If optional is set to true and the specified key does not exist,
                                              the environment variable will not be set in the Pod's containers.

                                              If optional is set to false and the specified key does not exist,
                                              an error will be returned during Pod creation.
                                            type: boolean
                                          path:
                                            description: |-
                                              The path within the volume from which to select the file.
                                              Must be relative and may not contain the '..' path or start with '..'.
                                            type: string
                                          volumeName:
                                            description: The name of the volume mount
                                              containing the env file.
                                            type: string
                                        required:
                                        - key
                                        - path
                                        - volumeName
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      resourceFieldRef:
                                        description: |-
                                          Selects a resource of the container: only resources limits and requests
                                          (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                        properties:
                                          containerName:
                                            description: 'Container name: required
                                              for volumes, optional for env vars'
                                            type: string
                                          divisor:
                                            anyOf:
                                            - type: integer
                                            - type: string
                                            description: Specifies the output format
                                              of the exposed resources, defaults to
                                              "1"
                                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                            x-kubernetes-int-or-string: true
                                          resource:
                                            description: 'Required: resource to select'
                                            type: string
                                        required:
                                        - resource
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      secretKeyRef:
                                        description: Selects a key of a secret in
                                          the pod's namespace
                                        properties:
                                          key:
                                            description: The key of the secret to
                                              select from.  Must be a valid secret
                                              key.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the Secret
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                    type: object
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            modelId:
                              description: |-
                                ModelID is the canonical identifier in {org}/{name} format.
                                Determines the cache mount path: /workspace/cache/{modelId}
                                For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                                For S3 sources, users define their own organizational structure.
                              pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                              type: string
                            precision:
                              allOf:
                              - enum:
                                - auto
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              - enum:
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              description: |-
                                Precision describes the runtime precision this source is compatible with.
                                Used to match model sources to profiles during custom weight onboarding.
                              type: string
                            size:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                Size is the expected storage space required for this model artifact.
                                Used for PVC sizing and capacity planning during cache creation.
                                Optional - if not specified, the download job will discover the size automatically.
                                Can be set explicitly to pre-allocate storage or override auto-discovery.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            sourceUri:
                              description: |-
                                SourceURI is the location from which the model should be downloaded.
                                Supported schemes:
                                - hf://org/model - Hugging Face Hub model
                                - s3://bucket/key - S3-compatible storage
                              pattern: ^(hf|s3)://[^ \t\r\n]+$
                              type: string
                          required:
                          - modelId
                          - sourceUri
                          type: object
                        maxItems: 1
                        minItems: 1
                        type: array
                    required:
                    - baseImage
                    - hardware
                    - modelSources
                    type: object
                    x-kubernetes-validations:
                    - message: at least one model source must be specified
                      rule: size(self.modelSources) >= 1
                  image:
                    description: |-
                      Image specifies a container image URI directly.
                      The controller searches for an existing model with this image, or creates one if none exists.
                      Auto-created models are namespace-scoped and can be reused by other services.
                      Example: `ghcr.io/silogen/llama-3-8b:v1.2.0`
                    type: string
                  name:
                    description: |-
                      Name references an existing AIMModel or AIMClusterModel by metadata.name.
                      The controller looks for a namespace-scoped AIMModel first, then falls back to cluster-scoped AIMClusterModel.
                      Example: `meta-llama-3-8b`
                    type: string
                type: object
                x-kubernetes-validations:
                - message: exactly one of name, image, or custom must be specified
                  rule: '(has(self.name) ? 1 : 0) + (has(self.image) ? 1 : 0) + (has(self.custom)
                    ? 1 : 0) == 1'
                - message: model selection is immutable after creation
                  rule: self == oldSelf
              overrides:
                description: |-
                  Overrides allows overriding specific template parameters for this service.
                  When specified, these values take precedence over the template values.
                properties:
                  hardware:
                    description: |-
                      Hardware specifies GPU and CPU requirements for each replica.
                      For GPU models, defines the GPU count and model types required for deployment.
                      For CPU-only models, defines CPU resource requirements.
                      This field is immutable after creation.
                    properties:
                      cpu:
                        description: CPU specifies CPU requirements.
                        properties:
                          limits:
                            anyOf:
                            - type: integer
                            - type: string
                            description: Limits is the maximum number of CPU cores
                              to allow.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          requests:
                            anyOf:
                            - type: integer
                            - type: string
                            description: Requests is the number of CPU cores to request.
                              Required and must be > 0.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                        required:
                        - requests
                        type: object
                      gpu:
                        description: GPU specifies GPU requirements. If not set, no
                          GPUs are requested (CPU-only model).
                        properties:
                          minVram:
                            anyOf:
                            - type: integer
                            - type: string
                            description: |-
                              MinVRAM limits deployment to GPUs having at least this much VRAM.
                              Used for capacity planning when the model size is known but any GPU with
                              sufficient VRAM is acceptable.
                              Cannot be combined with model.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          model:
                            description: |-
                              Model limits deployment to a specific GPU model.
                              Example: "MI300X"
                              Cannot be combined with minVram.
                            maxLength: 64
                            type: string
                          requests:
                            description: |-
                              Requests is the number of GPUs to set as requests/limits.
                              Set to 0 to target GPU nodes without consuming GPU resources (useful for testing).
                            format: int32
                            minimum: 0
                            type: integer
                          resourceName:
                            default: amd.com/gpu
                            description: |-
                              ResourceName is the Kubernetes resource name for GPU resources.
                              Defaults to "amd.com/gpu" if not specified.
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: model and minVram are mutually exclusive; specify
                            either a specific GPU model OR a minimum VRAM requirement,
                            not both
                          rule: '!(has(self.model) && size(self.model) > 0 && has(self.minVram))'
                    type: object
                    x-kubernetes-validations:
                    - message: hardware is immutable
                      rule: self == oldSelf
                    - message: at least one of gpu or cpu must be specified
                      rule: has(self.gpu) || has(self.cpu)
                  metric:
                    allOf:
                    - enum:
                      - latency
                      - throughput
                    - enum:
                      - latency
                      - throughput
                    description: |-
                      Metric selects the optimization goal.

                      - `latency`: prioritize low end‑to‑end latency
                      - `throughput`: prioritize sustained requests/second
                    type: string
                    x-kubernetes-validations:
                    - message: metric is immutable
                      rule: self == oldSelf
                  precision:
                    allOf:
                    - enum:
                      - auto
                      - fp4
                      - fp8
                      - fp16
                      - fp32
                      - bf16
                      - int4
                      - int8
                    - enum:
                      - auto
                      - fp4
                      - fp8
                      - fp16
                      - fp32
                      - bf16
                      - int4
                      - int8
                    description: Precision selects the numeric precision used by the
                      runtime.
                    type: string
                    x-kubernetes-validations:
                    - message: precision is immutable
                      rule: self == oldSelf
                type: object
              priorityClassName:
                description: |-
                  PriorityClassName specifies the priority class for the inference pods.
                  This maps directly to the Kubernetes PriorityClassName field on the pod spec.
                  If empty, no priority class is set.
                type: string
              profile:
                description: |-
                  Profile contains profile selection configuration (v1alpha2 only).
                  When set, the service uses a profile-based reconciliation path.
                  Mutually exclusive with Template.
                properties:
                  name:
                    description: |-
                      Name is the name of the AIMProfile or AIMClusterProfile to use.
                      The controller looks for a namespace-scoped AIMProfile first, then falls back to AIMClusterProfile.
                      Mutually exclusive with Selector. An empty string is treated as unset.
                    type: string
                  selector:
                    description: |-
                      Selector narrows candidate AIMProfile / AIMClusterProfile objects via the
                      shared provenance labels (role, source-model, origin) and spec filters
                      (aimId, precision, acceleratorModel, ...). The controller forces
                      `selector.role = Deployable` at evaluation time; user-supplied values
                      for that field are rejected by CEL on v1alpha2.

                      For every selector-driven AIMService the controller requires at least
                      one of `selector.aimId` or `selector.modelRef.name` so the watch
                      fan-out can reach the service via an O(1) index lookup. The top-level
                      `spec.model.name` shortcut is treated as if the user had set
                      `selector.modelRef.name` to the same value when not explicit.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount filters by accelerator unit
                          count.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel filters by accelerator identifier.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode filters candidates by their declared
                          partitioning mode. Partial-order match (NOT strict equality):
                            ""              - no filter on this field.
                            "unpartitioned" - matches profiles with mode "" or "unpartitioned".
                            "partitioned"   - matches profiles whose mode is non-trivial (anything
                                              other than "" / "unpartitioned").
                            "<C>"           - selector-only convenience: matches profiles with mode
                                              "<C>-*" (prefix on the scheme). Not a valid profile-spec
                                              value (e.g. selector "CPX" matches "CPX-NPS1", "CPX-NPS4").
                            "<C>-<M>"       - exact-string match on the scheme.
                        type: string
                      acceleratorType:
                        description: AcceleratorType filters by accelerator resource
                          type.
                        enum:
                        - gpu
                        - cpu
                        type: string
                      aimId:
                        description: AimId filters by model architecture identifier
                          (e.g., "qwen/qwen3-32b").
                        type: string
                      engine:
                        description: Engine filters by inference engine.
                        type: string
                      engineArgs:
                        description: |-
                          EngineArgs partially matches source engineArgs: every provided top-level key must
                          exist in the source object with an equal value.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      metric:
                        description: Metric filters by optimization target.
                        enum:
                        - latency
                        - throughput
                        type: string
                      minimumType:
                        description: |-
                          MinimumType filters by a minimum optimization level: candidates whose
                          type is this tier OR BETTER are accepted (hierarchy: optimized > general
                          > preview > unoptimized). This is the floor counterpart to the exact-match
                          Type field; the two AND together when both are set.

                          The sentinel "any" disables the floor (accept every tier). When this
                          field is empty the AIMService resolver applies a default floor of
                          "optimized" so auto-selection prefers production-grade profiles and never
                          silently picks an unoptimized one; to opt a service into lower tiers
                          (e.g. CPU/EPYC profiles published as unoptimized) set minimumType
                          explicitly to "unoptimized" or "any". Derivation selectors
                          (AIMProfileSet / AIMModel.profiles) treat empty as "any" so copying is
                          never tier-restricted by default.
                        enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                        - any
                        type: string
                      modelId:
                        description: ModelId filters by the source profile's specific
                          model identifier.
                        type: string
                      modelRef:
                        description: |-
                          ModelRef narrows candidates to those produced by a specific
                          AIM(Cluster)Model, matched via the `aim.eai.amd.com/source-model[-scope]`
                          labels stamped by the AIMModel reconcilers. Iteration 1 (v1alpha2 only).
                        properties:
                          name:
                            description: Name is the owning AIM(Cluster)Model name.
                              Required.
                            minLength: 1
                            type: string
                          scope:
                            default: Auto
                            description: Scope controls how Name is resolved against
                              AIMModel vs AIMClusterModel.
                            enum:
                            - Auto
                            - Namespace
                            - Cluster
                            type: string
                        required:
                        - name
                        type: object
                      origin:
                        description: |-
                          Origin filters by the `aim.eai.amd.com/profile-origin` label. When unset
                          (empty) the selector does not filter by origin. Iteration 1 (v1alpha2
                          only).
                        enum:
                        - discovered
                        - derived
                        - user-authored
                        type: string
                      precision:
                        description: Precision filters by numeric precision.
                        enum:
                        - auto
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                        type: string
                      profileId:
                        description: ProfileId filters by the source profile's profile
                          identifier.
                        type: string
                      role:
                        default: deployable
                        description: |-
                          Role filters by the `aim.eai.amd.com/profile-role` label. Defaults to
                          `deployable`. `base` filters to base profiles emitted by base-image
                          discovery (custom-model derivation source material).
                        enum:
                        - base
                        - deployable
                        type: string
                      type:
                        description: Type filters by optimization level (exact match).
                        enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                        type: string
                    type: object
                type: object
                x-kubernetes-validations:
                - message: spec.profile.name and spec.profile.selector are mutually
                    exclusive
                  rule: '!((has(self.name) && size(self.name) > 0) && has(self.selector))'
                - message: spec.profile must set name or selector
                  rule: (has(self.name) && size(self.name) > 0) || has(self.selector)
              profileOverrides:
                description: |-
                  ProfileOverrides allows overriding specific profile parameters for this service.
                  Only valid when Profile is set.
                properties:
                  acceleratorCount:
                    description: AcceleratorCount replaces the referenced profile's
                      acceleratorCount.
                    format: int32
                    type: integer
                  acceleratorModel:
                    description: |-
                      AcceleratorModel replaces the referenced profile's acceleratorModel
                      (e.g. "MI300X" -> "MI325X"). Validation against actual cluster
                      availability is left to the AIMServiceTemplate / runtime layers.
                    type: string
                  acceleratorPartitioningMode:
                    description: |-
                      AcceleratorPartitioningMode replaces the referenced profile's
                      acceleratorPartitioningMode. Complete replacement, not a merge — single
                      string, no substruct ambiguity. Empty string means "no override" (the
                      resolved overlay inherits the base profile's mode). Use this to deploy a
                      profile written for whole GPUs onto a partition slice. Whenever this
                      override is set (to any value, including "unpartitioned"), the CEL rule on
                      AIMService also requires an acceleratorCount override: partition mode
                      changes the per-unit interpretation of acceleratorCount, and CEL cannot
                      read the base profile to tell whether the meaning actually changed, so it
                      conservatively requires the count be restated. See
                      AcceleratorPartitioningMode on AIMProfileSpecCommon for the reserved
                      values ("unpartitioned", "partitioned", "<C>-<M>").
                    type: string
                  containerEnv:
                    description: |-
                      ContainerEnv merges by env-var name on top of the profile's
                      containerEnv. Matching names override; new names are appended.
                      AIM framework variables (AIM_*) reserved for the controller are
                      applied after the overlay's containerEnv and cannot be overridden
                      here.
                    items:
                      description: EnvVar represents an environment variable present
                        in a Container.
                      properties:
                        name:
                          description: |-
                            Name of the environment variable.
                            May consist of any printable ASCII characters except '='.
                          type: string
                        value:
                          description: |-
                            Variable references $(VAR_NAME) are expanded
                            using the previously defined environment variables in the container and
                            any service environment variables. If a variable cannot be resolved,
                            the reference in the input string will be unchanged. Double $$ are reduced
                            to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                            "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                            Escaped references will never be expanded, regardless of whether the variable
                            exists or not.
                            Defaults to "".
                          type: string
                        valueFrom:
                          description: Source for the environment variable's value.
                            Cannot be used if value is not empty.
                          properties:
                            configMapKeyRef:
                              description: Selects a key of a ConfigMap.
                              properties:
                                key:
                                  description: The key to select.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the ConfigMap or its
                                    key must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                            fieldRef:
                              description: |-
                                Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                              properties:
                                apiVersion:
                                  description: Version of the schema the FieldPath
                                    is written in terms of, defaults to "v1".
                                  type: string
                                fieldPath:
                                  description: Path of the field to select in the
                                    specified API version.
                                  type: string
                              required:
                              - fieldPath
                              type: object
                              x-kubernetes-map-type: atomic
                            fileKeyRef:
                              description: |-
                                FileKeyRef selects a key of the env file.
                                Requires the EnvFiles feature gate to be enabled.
                              properties:
                                key:
                                  description: |-
                                    The key within the env file. An invalid key will prevent the pod from starting.
                                    The keys defined within a source may consist of any printable ASCII characters except '='.
                                    During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                  type: string
                                optional:
                                  default: false
                                  description: |-
                                    Specify whether the file or its key must be defined. If the file or key
                                    does not exist, then the env var is not published.
                                    If optional is set to true and the specified key does not exist,
                                    the environment variable will not be set in the Pod's containers.

                                    If optional is set to false and the specified key does not exist,
                                    an error will be returned during Pod creation.
                                  type: boolean
                                path:
                                  description: |-
                                    The path within the volume from which to select the file.
                                    Must be relative and may not contain the '..' path or start with '..'.
                                  type: string
                                volumeName:
                                  description: The name of the volume mount containing
                                    the env file.
                                  type: string
                              required:
                              - key
                              - path
                              - volumeName
                              type: object
                              x-kubernetes-map-type: atomic
                            resourceFieldRef:
                              description: |-
                                Selects a resource of the container: only resources limits and requests
                                (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                              properties:
                                containerName:
                                  description: 'Container name: required for volumes,
                                    optional for env vars'
                                  type: string
                                divisor:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: Specifies the output format of the
                                    exposed resources, defaults to "1"
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                resource:
                                  description: 'Required: resource to select'
                                  type: string
                              required:
                              - resource
                              type: object
                              x-kubernetes-map-type: atomic
                            secretKeyRef:
                              description: Selects a key of a secret in the pod's
                                namespace
                              properties:
                                key:
                                  description: The key of the secret to select from.  Must
                                    be a valid secret key.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the Secret or its key
                                    must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  engineArgs:
                    description: |-
                      EngineArgs shallow-merges on top of the profile's engineArgs,
                      overriding matching top-level keys. Values are passed verbatim
                      to the inference engine CLI.
                    x-kubernetes-preserve-unknown-fields: true
                  engineEnv:
                    additionalProperties:
                      type: string
                    description: |-
                      EngineEnv merges by key on top of the profile's engineEnv. These
                      variables flow into the inference engine's runtime configuration.
                    type: object
                  modelSources:
                    description: |-
                      ModelSources replaces the referenced profile's modelSources entirely.
                      Use this to point a profile at user-supplied weights (e.g. a fine-tuned
                      checkpoint) without forking the profile itself. The first source's
                      modelId becomes the overlay profile's modelId.
                    items:
                      description: |-
                        AIMModelSource describes a model artifact that must be downloaded for inference.
                        Discovery extracts these from the container's configuration to enable caching and validation.
                      properties:
                        env:
                          description: |-
                            Env specifies per-source credential overrides.
                            These variables are used for authentication when downloading this specific source.
                            Takes precedence over base-level env for the same variable name.
                          items:
                            description: EnvVar represents an environment variable
                              present in a Container.
                            properties:
                              name:
                                description: |-
                                  Name of the environment variable.
                                  May consist of any printable ASCII characters except '='.
                                type: string
                              value:
                                description: |-
                                  Variable references $(VAR_NAME) are expanded
                                  using the previously defined environment variables in the container and
                                  any service environment variables. If a variable cannot be resolved,
                                  the reference in the input string will be unchanged. Double $$ are reduced
                                  to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                  "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                  Escaped references will never be expanded, regardless of whether the variable
                                  exists or not.
                                  Defaults to "".
                                type: string
                              valueFrom:
                                description: Source for the environment variable's
                                  value. Cannot be used if value is not empty.
                                properties:
                                  configMapKeyRef:
                                    description: Selects a key of a ConfigMap.
                                    properties:
                                      key:
                                        description: The key to select.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the ConfigMap
                                          or its key must be defined
                                        type: boolean
                                    required:
                                    - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  fieldRef:
                                    description: |-
                                      Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                      spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                    properties:
                                      apiVersion:
                                        description: Version of the schema the FieldPath
                                          is written in terms of, defaults to "v1".
                                        type: string
                                      fieldPath:
                                        description: Path of the field to select in
                                          the specified API version.
                                        type: string
                                    required:
                                    - fieldPath
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  fileKeyRef:
                                    description: |-
                                      FileKeyRef selects a key of the env file.
                                      Requires the EnvFiles feature gate to be enabled.
                                    properties:
                                      key:
                                        description: |-
                                          The key within the env file. An invalid key will prevent the pod from starting.
                                          The keys defined within a source may consist of any printable ASCII characters except '='.
                                          During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                        type: string
                                      optional:
                                        default: false
                                        description: |-
                                          Specify whether the file or its key must be defined. If the file or key
                                          does not exist, then the env var is not published.
                                          If optional is set to true and the specified key does not exist,
                                          the environment variable will not be set in the Pod's containers.

                                          If optional is set to false and the specified key does not exist,
                                          an error will be returned during Pod creation.
                                        type: boolean
                                      path:
                                        description: |-
                                          The path within the volume from which to select the file.
                                          Must be relative and may not contain the '..' path or start with '..'.
                                        type: string
                                      volumeName:
                                        description: The name of the volume mount
                                          containing the env file.
                                        type: string
                                    required:
                                    - key
                                    - path
                                    - volumeName
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  resourceFieldRef:
                                    description: |-
                                      Selects a resource of the container: only resources limits and requests
                                      (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                    properties:
                                      containerName:
                                        description: 'Container name: required for
                                          volumes, optional for env vars'
                                        type: string
                                      divisor:
                                        anyOf:
                                        - type: integer
                                        - type: string
                                        description: Specifies the output format of
                                          the exposed resources, defaults to "1"
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      resource:
                                        description: 'Required: resource to select'
                                        type: string
                                    required:
                                    - resource
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  secretKeyRef:
                                    description: Selects a key of a secret in the
                                      pod's namespace
                                    properties:
                                      key:
                                        description: The key of the secret to select
                                          from.  Must be a valid secret key.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the Secret or
                                          its key must be defined
                                        type: boolean
                                    required:
                                    - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                type: object
                            required:
                            - name
                            type: object
                          type: array
                          x-kubernetes-list-map-keys:
                          - name
                          x-kubernetes-list-type: map
                        modelId:
                          description: |-
                            ModelID is the canonical identifier in {org}/{name} format.
                            Determines the cache mount path: /workspace/cache/{modelId}
                            For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                            For S3 sources, users define their own organizational structure.
                          pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                          type: string
                        precision:
                          allOf:
                          - enum:
                            - auto
                            - fp4
                            - fp8
                            - fp16
                            - fp32
                            - bf16
                            - int4
                            - int8
                          - enum:
                            - fp4
                            - fp8
                            - fp16
                            - fp32
                            - bf16
                            - int4
                            - int8
                          description: |-
                            Precision describes the runtime precision this source is compatible with.
                            Used to match model sources to profiles during custom weight onboarding.
                          type: string
                        size:
                          anyOf:
                          - type: integer
                          - type: string
                          description: |-
                            Size is the expected storage space required for this model artifact.
                            Used for PVC sizing and capacity planning during cache creation.
                            Optional - if not specified, the download job will discover the size automatically.
                            Can be set explicitly to pre-allocate storage or override auto-discovery.
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        sourceUri:
                          description: |-
                            SourceURI is the location from which the model should be downloaded.
                            Supported schemes:
                            - hf://org/model - Hugging Face Hub model
                            - s3://bucket/key - S3-compatible storage
                          pattern: ^(hf|s3)://[^ \t\r\n]+$
                          type: string
                      required:
                      - modelId
                      - sourceUri
                      type: object
                    type: array
                type: object
              replicas:
                default: 1
                description: |-
                  Replicas specifies the number of replicas for this service.
                  When not specified, defaults to 1 replica.
                  This value overrides any replica settings from the template.
                  For autoscaling, use MinReplicas and MaxReplicas instead.
                format: int32
                type: integer
              resources:
                description: |-
                  Resources overrides the container resource requirements for this service.
                  When specified, these values take precedence over the template and image defaults.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              routing:
                description: |-
                  Routing controls HTTP routing configuration for this service.
                  When set, these values override namespace/cluster runtime config defaults.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: |-
                      Annotations defines default annotations to add to all HTTPRoute resources.
                      Services can add additional annotations or override these via spec.routing.annotations.
                      When both are specified, service annotations take precedence for conflicting keys.
                      Common use cases include ingress controller settings, rate limiting, monitoring labels,
                      and security policies that should apply to all services using this config.
                    type: object
                  enabled:
                    description: |-
                      Enabled controls whether HTTP routing is managed for inference services using this config.
                      When true, the operator creates HTTPRoute resources for services that reference this config.
                      When false or unset, routing must be explicitly enabled on each service.
                      This provides a namespace or cluster-wide default that individual services can override.
                    type: boolean
                  gatewayRef:
                    description: |-
                      GatewayRef specifies the Gateway API Gateway resource that should receive HTTPRoutes.
                      This identifies the parent gateway for routing traffic to inference services.
                      The gateway can be in any namespace (cross-namespace references are supported).
                      If routing is enabled but GatewayRef is not specified, service reconciliation will fail
                      with a validation error.
                    properties:
                      group:
                        default: gateway.networking.k8s.io
                        description: |-
                          Group is the group of the referent.
                          When unspecified, "gateway.networking.k8s.io" is inferred.
                          To set the core API group (such as for a "Service" kind referent),
                          Group must be explicitly set to "" (empty string).

                          Support: Core
                        maxLength: 253
                        pattern: ^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                        type: string
                      kind:
                        default: Gateway
                        description: |-
                          Kind is kind of the referent.

                          There are two kinds of parent resources with "Core" support:

                          * Gateway (Gateway conformance profile)
                          * Service (Mesh conformance profile, ClusterIP Services only)

                          Support for other resources is Implementation-Specific.
                        maxLength: 63
                        minLength: 1
                        pattern: ^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$
                        type: string
                      name:
                        description: |-
                          Name is the name of the referent.

                          Support: Core
                        maxLength: 253
                        minLength: 1
                        type: string
                      namespace:
                        description: |-
                          Namespace is the namespace of the referent. When unspecified, this refers
                          to the local namespace of the Route.

                          Note that there are specific rules for ParentRefs which cross namespace
                          boundaries. Cross-namespace references are only valid if they are explicitly
                          allowed by something in the namespace they are referring to. For example:
                          Gateway has the AllowedRoutes field, and ReferenceGrant provides a
                          generic way to enable any other kind of cross-namespace reference.

                          <gateway:experimental:description>
                          ParentRefs from a Route to a Service in the same namespace are "producer"
                          routes, which apply default routing rules to inbound connections from
                          any namespace to the Service.

                          ParentRefs from a Route to a Service in a different namespace are
                          "consumer" routes, and these routing rules are only applied to outbound
                          connections originating from the same namespace as the Route, for which
                          the intended destination of the connections are a Service targeted as a
                          ParentRef of the Route.
                          </gateway:experimental:description>

                          Support: Core
                        maxLength: 63
                        minLength: 1
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                        type: string
                      port:
                        description: |-
                          Port is the network port this Route targets. It can be interpreted
                          differently based on the type of parent resource.

                          When the parent resource is a Gateway, this targets all listeners
                          listening on the specified port that also support this kind of Route(and
                          select this Route). It's not recommended to set `Port` unless the
                          networking behaviors specified in a Route must apply to a specific port
                          as opposed to a listener(s) whose port(s) may be changed. When both Port
                          and SectionName are specified, the name and port of the selected listener
                          must match both specified values.

                          <gateway:experimental:description>
                          When the parent resource is a Service, this targets a specific port in the
                          Service spec. When both Port (experimental) and SectionName are specified,
                          the name and port of the selected port must match both specified values.
                          </gateway:experimental:description>

                          Implementations MAY choose to support other parent resources.
                          Implementations supporting other types of parent resources MUST clearly
                          document how/if Port is interpreted.

                          For the purpose of status, an attachment is considered successful as
                          long as the parent resource accepts it partially. For example, Gateway
                          listeners can restrict which Routes can attach to them by Route kind,
                          namespace, or hostname. If 1 of 2 Gateway listeners accept attachment
                          from the referencing Route, the Route MUST be considered successfully
                          attached. If no Gateway listeners accept attachment from this Route,
                          the Route MUST be considered detached from the Gateway.

                          Support: Extended
                        format: int32
                        maximum: 65535
                        minimum: 1
                        type: integer
                      sectionName:
                        description: |-
                          SectionName is the name of a section within the target resource. In the
                          following resources, SectionName is interpreted as the following:

                          * Gateway: Listener name. When both Port (experimental) and SectionName
                          are specified, the name and port of the selected listener must match
                          both specified values.
                          * Service: Port name. When both Port (experimental) and SectionName
                          are specified, the name and port of the selected listener must match
                          both specified values.

                          Implementations MAY choose to support attaching Routes to other resources.
                          If that is the case, they MUST clearly document how SectionName is
                          interpreted.

                          When unspecified (empty string), this will reference the entire resource.
                          For the purpose of status, an attachment is considered successful if at
                          least one section in the parent resource accepts it. For example, Gateway
                          listeners can restrict which Routes can attach to them by Route kind,
                          namespace, or hostname. If 1 of 2 Gateway listeners accept attachment from
                          the referencing Route, the Route MUST be considered successfully
                          attached. If no Gateway listeners accept attachment from this Route, the
                          Route MUST be considered detached from the Gateway.

                          Support: Core
                        maxLength: 253
                        minLength: 1
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                        type: string
                    required:
                    - name
                    type: object
                  hostnames:
                    description: |-
                      Hostnames pins generated HTTPRoutes to these hostnames so a route only
                      attaches to the matching Gateway listener instead of every listener on
                      the parent gateway. Without a hostname, an HTTPRoute matches all of the
                      parent gateway's listener hostnames, which can expose a service on
                      listeners that do not enforce the intended authentication.

                      This field is required when the parent gateway exposes more than one
                      listener: in that case a service with routing enabled but no hostnames
                      configured will not get an HTTPRoute and reports ConfigValid=False with
                      reason RouteHostnameRequired. When the parent gateway has a single
                      listener, leaving this empty preserves the existing behavior (the route
                      inherits that listener's hostnames).

                      Individual services can override this list via spec.routing.hostnames.
                    items:
                      description: |-
                        Hostname is the fully qualified domain name of a network host. This matches
                        the RFC 1123 definition of a hostname with 2 notable exceptions:

                         1. IPs are not allowed.
                         2. A hostname may be prefixed with a wildcard label (`*.`). The wildcard
                            label must appear by itself as the first label.

                        Hostname can be "precise" which is a domain name without the terminating
                        dot of a network host (e.g. "foo.example.com") or "wildcard", which is a
                        domain name prefixed with a single wildcard label (e.g. `*.example.com`).

                        Note that as per RFC1035 and RFC1123, a *label* must consist of lower case
                        alphanumeric characters or '-', and must start and end with an alphanumeric
                        character. No other punctuation is allowed.
                      maxLength: 253
                      minLength: 1
                      pattern: ^(\*\.)?[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                      type: string
                    type: array
                  pathTemplate:
                    description: |-
                      PathTemplate defines the HTTP path template for routes, evaluated using JSONPath expressions.
                      The template is rendered against the AIMService object to generate unique paths.

                      Example templates:
                      - `/{.metadata.namespace}/{.metadata.name}` - namespace and service name
                      - `/{.metadata.namespace}/{.metadata.labels['team']}/inference` - with label
                      - `/models/{.metadata.name}` - based on service name

                      The template must:
                      - Use valid JSONPath expressions wrapped in {...}
                      - Reference fields that exist on the service
                      - Produce a path ≤ 200 characters after rendering
                      - Result in valid URL path segments (lowercase, RFC 1123 compliant)

                      If evaluation fails, the service enters Degraded state with PathTemplateInvalid reason.
                      Individual services can override this template via spec.routing.pathTemplate.
                    type: string
                  requestTimeout:
                    description: |-
                      RequestTimeout defines the HTTP request timeout for routes.
                      This sets the maximum duration for a request to complete before timing out.
                      The timeout applies to the entire request/response cycle.
                      If not specified, no timeout is set on the route.
                      Individual services can override this value via spec.routing.requestTimeout.
                    type: string
                type: object
              runtimeConfigName:
                description: |-
                  Name is the name of the runtime config to use for this resource. If a runtime config with this name exists both
                  as a namespace and a cluster runtime config, the values are merged together, the namespace config taking priority
                  over the cluster config when there are conflicts. If this field is empty or set to `default`, the namespace / cluster
                  runtime config with the name `default` is used, if it exists.
                type: string
              serviceAccountName:
                description: |-
                  ServiceAccountName specifies the Kubernetes service account to use for the inference workload.
                  This service account is used by the deployed inference pods.
                  If empty, the default service account for the namespace is used.
                type: string
              storage:
                description: |-
                  Storage configures storage defaults for this service's PVCs and caches.
                  When set, these values override namespace/cluster runtime config defaults.
                properties:
                  adapterDiskSize:
                    anyOf:
                    - type: integer
                    - type: string
                    description: |-
                      AdapterDiskSize is the cluster default size for the shared adapter disk PVC
                      (built-in default when unset). An artifact's adapterDisk.size wins over it.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  adapterDiskStorageClassName:
                    description: |-
                      AdapterDiskStorageClassName is the storage class for the shared
                      ReadWriteMany adapter disk. It must be RWX-capable (e.g. longhorn, NFS) and
                      is resolved before the (typically RWO) DefaultStorageClassName.
                    type: string
                  defaultStorageClassName:
                    description: |-
                      DefaultStorageClassName specifies the storage class to use for artifacts and PVCs
                      when the consuming resource (AIMArtifact, AIMTemplateCache, AIMServiceTemplate) does not
                      specify a storage class. If this field is empty, the cluster's default storage class is used.
                    type: string
                  downloadFilter:
                    description: |-
                      DownloadFilter controls which files are included or excluded during artifact downloads.
                      When set here, applies as the default for all artifacts using this runtime config.
                      Individual artifacts can override this with their own downloadFilter.
                      When no filter is configured at any level, subdirectory files are excluded by default.
                      Set to an empty object (downloadFilter: {}) to explicitly allow all files.
                    properties:
                      exclude:
                        description: |-
                          Exclude specifies glob patterns for files to skip.
                          Files matching any exclude pattern are removed after include filtering.
                          Patterns use fnmatch syntax (e.g., ["*/*", "*.bin"]).
                          Use ["*/*"] to exclude all files in subdirectories (the default when no filter is set).
                        items:
                          type: string
                        type: array
                      include:
                        description: |-
                          Include specifies glob patterns for files to download.
                          Only files matching at least one pattern are considered.
                          If empty, all files pass the include check.
                          Patterns use fnmatch syntax (e.g., ["*.safetensors", "config.json"]).
                        items:
                          type: string
                        type: array
                    type: object
                  pvcHeadroomPercent:
                    default: 10
                    description: |-
                      PVCHeadroomPercent specifies the percentage of extra space to add to PVCs
                      for model storage. This accounts for filesystem overhead and temporary files
                      during model loading. The value represents a percentage (e.g., 10 means 10% extra space).
                      If not specified, defaults to 10%.
                    format: int32
                    minimum: 0
                    type: integer
                type: object
              template:
                description: |-
                  Template contains template selection and configuration.
                  Use Template.Name to specify an explicit template, or omit to auto-select.
                  Mutually exclusive with Profile (v1alpha2).
                properties:
                  allowUnoptimized:
                    description: |-
                      AllowUnoptimized, if true, will allow automatic selection of templates
                      that resolve to an unoptimized profile.
                    type: boolean
                  name:
                    description: |-
                      Name is the name of the AIMServiceTemplate or AIMClusterServiceTemplate to use.
                      The template selects the runtime profile and GPU parameters.
                      When not specified, a template will be automatically selected based on the model.
                    type: string
                type: object
                x-kubernetes-validations:
                - message: template selection is immutable after creation
                  rule: self == oldSelf
            type: object
            x-kubernetes-validations:
            - message: minReplicas must be less than or equal to maxReplicas
              rule: '!has(self.minReplicas) || !has(self.maxReplicas) || self.minReplicas
                <= self.maxReplicas'
          status:
            description: AIMServiceStatus defines the observed state of AIMService.
            properties:
              adapterDiskPersistentVolumeClaim:
                description: |-
                  AdapterDiskPersistentVolumeClaim is the resolved shared adapter-disk PVC
                  (from the base model artifact's status). It is recorded here once resolved
                  and reused when a transient parent-resolution gap would otherwise leave it
                  empty, so a blip never re-renders the InferenceService without its adapter
                  mount and restarts a running predictor. Never cleared once set.
                type: string
              adapterSubtreeSyncKey:
                description: |-
                  AdapterSubtreeSyncKey records the declared adapter set most recently
                  reconciled onto the service's adapter subtree by the subtree-sync Job
                  (a hash of the sorted spec.adapters names). The controller re-runs the
                  sync Job — which creates the subtree and prunes adapter directories no
                  longer declared — whenever this drifts from the current desired set, so
                  editing spec.adapters reclaims removed adapters without re-run loops.
                type: string
              adapters:
                description: |-
                  Adapters reports the per-adapter disk-side status for services that declare
                  spec.adapters. One entry per declared adapter. Observation is
                  best-effort/eventual; the disk state the controller wrote is authoritative.
                items:
                  description: AIMServiceAdapterStatus is the per-adapter status aggregated
                    onto an AIMService.
                  properties:
                    adapterPath:
                      description: AdapterPath is the on-disk directory name (mirrored
                        from the artifact).
                      type: string
                    lastError:
                      description: |-
                        LastError carries the most recent error for this adapter (e.g. a mirrored
                        failing reason from the underlying artifact).
                      type: string
                    lastObserved:
                      description: LastObserved is when the controller last observed
                        this adapter's state.
                      format: date-time
                      type: string
                    loadedReplicas:
                      description: |-
                        LoadedReplicas reports how many serving replicas have the adapter loaded,
                        as "loaded/total" (e.g. "3/3"). RESERVED: engine-reported, not yet populated.
                      type: string
                    modelId:
                      description: ModelID is the adapter's canonical model id (mirrored
                        from the artifact).
                      type: string
                    name:
                      description: Name is the adapter reference name.
                      type: string
                    state:
                      description: State is the disk-side state of the adapter for
                        this service.
                      enum:
                      - Pending
                      - Downloading
                      - Downloaded
                      - Deleting
                      - Loaded
                      - LoadRejected
                      type: string
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              cache:
                description: Cache captures cache-related status for this service.
                properties:
                  profileCacheRef:
                    description: |-
                      ProfileCacheRef references the AIMProfileCache being used, if any.
                      Set by the v1alpha2 (profile-based) reconciliation path.
                    properties:
                      kind:
                        description: Kind is the fully-qualified kind of the resolved
                          reference, when known.
                        type: string
                      name:
                        description: Name is the resource name that satisfied the
                          reference.
                        type: string
                      namespace:
                        description: |-
                          Namespace identifies where the resource was found when namespace-scoped.
                          Empty indicates a cluster-scoped resource.
                        type: string
                      scope:
                        description: Scope indicates whether the resolved resource
                          was namespace or cluster scoped.
                        enum:
                        - Namespace
                        - Cluster
                        - Merged
                        - Unknown
                        type: string
                      uid:
                        description: UID captures the unique identifier of the resolved
                          reference, when known.
                        type: string
                    type: object
                  retryAttempts:
                    description: |-
                      RetryAttempts tracks how many times this service has attempted to retry a failed cache.
                      Each service gets exactly one retry attempt. When a cache enters Failed state,
                      this counter is incremented from 0 to 1 after deleting failed Artifacts.
                      If the retry fails (cache enters Failed again with attempts == 1), the service degrades.
                    type: integer
                  templateCacheRef:
                    description: |-
                      TemplateCacheRef references the AIMTemplateCache being used, if any.
                      Set by the v1alpha1 (template-based) reconciliation path.
                    properties:
                      kind:
                        description: Kind is the fully-qualified kind of the resolved
                          reference, when known.
                        type: string
                      name:
                        description: Name is the resource name that satisfied the
                          reference.
                        type: string
                      namespace:
                        description: |-
                          Namespace identifies where the resource was found when namespace-scoped.
                          Empty indicates a cluster-scoped resource.
                        type: string
                      scope:
                        description: Scope indicates whether the resolved resource
                          was namespace or cluster scoped.
                        enum:
                        - Namespace
                        - Cluster
                        - Merged
                        - Unknown
                        type: string
                      uid:
                        description: UID captures the unique identifier of the resolved
                          reference, when known.
                        type: string
                    type: object
                type: object
              conditions:
                description: Conditions represent the latest observations of template
                  state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              resolvedModel:
                description: ResolvedModel captures metadata about the image that
                  was resolved.
                properties:
                  kind:
                    description: Kind is the fully-qualified kind of the resolved
                      reference, when known.
                    type: string
                  name:
                    description: Name is the resource name that satisfied the reference.
                    type: string
                  namespace:
                    description: |-
                      Namespace identifies where the resource was found when namespace-scoped.
                      Empty indicates a cluster-scoped resource.
                    type: string
                  scope:
                    description: Scope indicates whether the resolved resource was
                      namespace or cluster scoped.
                    enum:
                    - Namespace
                    - Cluster
                    - Merged
                    - Unknown
                    type: string
                  uid:
                    description: UID captures the unique identifier of the resolved
                      reference, when known.
                    type: string
                type: object
              resolvedProfile:
                description: |-
                  ResolvedProfile captures metadata about the profile that satisfied the reference.
                  Set when the service uses a profile-based reconciliation path (v1alpha2).
                properties:
                  kind:
                    description: Kind is the fully-qualified kind of the resolved
                      reference, when known.
                    type: string
                  name:
                    description: Name is the resource name that satisfied the reference.
                    type: string
                  namespace:
                    description: |-
                      Namespace identifies where the resource was found when namespace-scoped.
                      Empty indicates a cluster-scoped resource.
                    type: string
                  scope:
                    description: Scope indicates whether the resolved resource was
                      namespace or cluster scoped.
                    enum:
                    - Namespace
                    - Cluster
                    - Merged
                    - Unknown
                    type: string
                  uid:
                    description: UID captures the unique identifier of the resolved
                      reference, when known.
                    type: string
                type: object
              resolvedRuntimeConfig:
                description: ResolvedRuntimeConfig captures metadata about the runtime
                  config that was resolved.
                properties:
                  kind:
                    description: Kind is the fully-qualified kind of the resolved
                      reference, when known.
                    type: string
                  name:
                    description: Name is the resource name that satisfied the reference.
                    type: string
                  namespace:
                    description: |-
                      Namespace identifies where the resource was found when namespace-scoped.
                      Empty indicates a cluster-scoped resource.
                    type: string
                  scope:
                    description: Scope indicates whether the resolved resource was
                      namespace or cluster scoped.
                    enum:
                    - Namespace
                    - Cluster
                    - Merged
                    - Unknown
                    type: string
                  uid:
                    description: UID captures the unique identifier of the resolved
                      reference, when known.
                    type: string
                type: object
              resolvedTemplate:
                description: ResolvedTemplate captures metadata about the template
                  that satisfied the reference.
                properties:
                  kind:
                    description: Kind is the fully-qualified kind of the resolved
                      reference, when known.
                    type: string
                  name:
                    description: Name is the resource name that satisfied the reference.
                    type: string
                  namespace:
                    description: |-
                      Namespace identifies where the resource was found when namespace-scoped.
                      Empty indicates a cluster-scoped resource.
                    type: string
                  scope:
                    description: Scope indicates whether the resolved resource was
                      namespace or cluster scoped.
                    enum:
                    - Namespace
                    - Cluster
                    - Merged
                    - Unknown
                    type: string
                  uid:
                    description: UID captures the unique identifier of the resolved
                      reference, when known.
                    type: string
                type: object
              routing:
                description: Routing surfaces information about the configured HTTP
                  routing, when enabled.
                properties:
                  path:
                    description: |-
                      Path is the HTTP path prefix used when routing is enabled.
                      Example: `/tenant/svc-uuid`
                    type: string
                type: object
              runtime:
                description: Runtime captures runtime status including replica counts.
                properties:
                  currentReplicas:
                    description: CurrentReplicas is the current number of replicas
                      as reported by the HPA.
                    format: int32
                    type: integer
                  desiredReplicas:
                    description: DesiredReplicas is the desired number of replicas
                      as determined by the HPA.
                    format: int32
                    type: integer
                  maxReplicas:
                    description: MaxReplicas is the maximum number of replicas configured
                      for autoscaling.
                    format: int32
                    type: integer
                  minReplicas:
                    description: MinReplicas is the minimum number of replicas configured
                      for autoscaling.
                    format: int32
                    type: integer
                  replicas:
                    description: |-
                      Replicas is a formatted display string for kubectl output.
                      Shows "current" for fixed replicas or "current/desired (min-max)" for autoscaling.
                    type: string
                required:
                - currentReplicas
                - desiredReplicas
                - maxReplicas
                - minReplicas
                type: object
              status:
                default: Pending
                description: |-
                  Status represents the current high‑level status of the service lifecycle.
                  Values: `Pending`, `Starting`, `Running`, `Degraded`, `Failed`.
                enum:
                - Pending
                - Starting
                - Running
                - Degraded
                - Failed
                type: string
            type: object
        type: object
        x-kubernetes-validations:
        - message: spec.profileOverrides requires spec.profile to be set
          rule: '!has(self.spec.profileOverrides) || has(self.spec.profile)'
        - message: spec.profile and spec.template are mutually exclusive
          rule: '!(has(self.spec.profile) && has(self.spec.template))'
        - message: one of spec.model or spec.profile must be specified
          rule: has(self.spec.model) || has(self.spec.profile)
        - message: acceleratorCount must be specified together with any acceleratorPartitioningMode
            override; partition mode changes the per-unit interpretation of acceleratorCount
          rule: '!has(self.spec.profileOverrides) || !has(self.spec.profileOverrides.acceleratorPartitioningMode)
            || size(self.spec.profileOverrides.acceleratorPartitioningMode) == 0 ||
            has(self.spec.profileOverrides.acceleratorCount)'
        - message: spec.adapters entries must have unique (kind, name) pairs
          rule: '!has(self.spec.adapters) || self.spec.adapters.all(a, self.spec.adapters.exists_one(b,
            b.kind == a.kind && b.name == a.name))'
        - message: spec.adapters is immutable unless spec.adapterMode is dynamic
          rule: self.spec.adapterMode == 'dynamic' || (has(self.spec.adapters) ==
            has(oldSelf.spec.adapters) && (!has(self.spec.adapters) || self.spec.adapters
            == oldSelf.spec.adapters))
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.resolvedModel.name
      name: Model
      type: string
    - jsonPath: .status.resolvedProfile.name
      name: Profile
      type: string
    - jsonPath: .status.runtime.replicas
      name: Replicas
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: |-
          AIMService manages a KServe-based AIM inference service for the selected model and template.
          Note: KServe uses {name}-{namespace} format which must not exceed 63 characters.
          This constraint is validated at runtime since CEL cannot access metadata.namespace.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              AIMServiceSpec defines the desired state of AIMService.

              Binds a canonical model to an AIMServiceTemplate and configures replicas,
              caching behavior, and optional overrides. The template governs the base
              runtime selection knobs, while the overrides field allows service-specific
              customization.

              With v1alpha2, a Profile can be used instead of a Template. Template and Profile
              are mutually exclusive — at least one resolution path must be specified.
            properties:
              adapterMode:
                default: static
                description: |-
                  AdapterMode is the immutable adapter contract for the service, mapped
                  directly to the AIM_ADAPTER_MODE container env. static (default) freezes
                  spec.adapters and mounts the adapter disk only when adapters are declared;
                  dynamic allows editing spec.adapters and mounts the disk even at zero
                  adapters (so add/remove never restarts the pod). It is immutable after
                  creation.
                enum:
                - static
                - dynamic
                type: string
                x-kubernetes-validations:
                - message: spec.adapterMode is immutable after creation
                  rule: self == oldSelf
              adapters:
                description: |-
                  Adapters is the load-bearing list of LoRA adapters this service serves.
                  The list may only be edited after creation when adapterMode is dynamic
                  (static freezes it). Omitting the list serves no adapters.
                  Supported on both the template (v1alpha1) and profile (v1alpha2) pipelines:
                  the base model the adapters attach to is resolved from the service's
                  template cache or profile cache respectively. In dynamic mode the list may
                  be edited after creation: adding an adapter stages it into the service's
                  subtree and the aim-runtime hot-loads it; removing one lets the runtime
                  unload it (subtree cleanup is reclaimed out-of-band). The InferenceService
                  is never modified for adapter changes — it mounts the whole per-service
                  subtree read-only. Entries are pure references; (kind, name) pairs must be
                  unique. Omitting the list serves no adapters.
                items:
                  description: |-
                    AIMServiceAdapterReference is a typed reference to a LoRA adapter served by this
                    service. Today it is resolved as a pure reference to an existing adapter
                    artifact. The inline bootstrap fields (sourceUri/modelId/rank) are reserved:
                    the schema accepts them, but create-if-missing self-healing is not yet wired,
                    so a referenced adapter artifact must currently exist.
                  properties:
                    kind:
                      description: Kind is the kind of the referenced adapter. Required;
                        restricted to AIMArtifact in v1.
                      enum:
                      - AIMArtifact
                      type: string
                    modelId:
                      description: |-
                        ModelID is the adapter's canonical model id. Required when SourceURI is set.
                        RESERVED: only meaningful alongside SourceURI.
                      type: string
                    name:
                      description: Name is the metadata.name of the referenced adapter
                        artifact.
                      maxLength: 253
                      minLength: 1
                      type: string
                    rank:
                      description: |-
                        Rank is the optional LoRA rank for the bootstrapped adapter. RESERVED: only
                        meaningful alongside SourceURI.
                      format: int32
                      minimum: 1
                      type: integer
                    sourceUri:
                      description: |-
                        SourceURI is an optional create-if-missing bootstrap source. When set and no
                        adapter artifact named Name exists, a later release will create one from this
                        source; a pre-existing artifact always wins. RESERVED: not yet acted on.
                      type: string
                  required:
                  - kind
                  - name
                  type: object
                  x-kubernetes-validations:
                  - message: modelId is required when sourceUri is set
                    rule: '!has(self.sourceUri) || has(self.modelId)'
                  - message: modelId and rank are only allowed together with sourceUri
                    rule: has(self.sourceUri) || (!has(self.modelId) && !has(self.rank))
                maxItems: 64
                type: array
                x-kubernetes-list-type: atomic
              autoScaling:
                description: |-
                  AutoScaling configures advanced autoscaling behavior using KEDA.
                  Supports custom metrics from OpenTelemetry backend.
                  When specified, MinReplicas and MaxReplicas should also be set.
                properties:
                  cooldownPeriod:
                    description: |-
                      CooldownPeriod is the seconds-of-inactivity budget KEDA waits before
                      scaling back to minReplicaCount. Under scale-to-zero, defaults to a
                      memory-derived value (300-1200s); otherwise unset (KEDA default of 300).
                    format: int32
                    minimum: 0
                    type: integer
                  metrics:
                    description: |-
                      Metrics is a list of metrics to be used for autoscaling.
                      Each metric defines a source (PodMetric) and target values.
                    items:
                      description: |-
                        AIMServiceMetricsSpec defines a single metric for autoscaling.
                        Specifies the metric source type and configuration.
                      properties:
                        podmetric:
                          description: |-
                            PodMetric refers to a metric describing each pod in the current scale target.
                            Used when Type is "PodMetric". Supports backends like OpenTelemetry for custom metrics.
                          properties:
                            metric:
                              description: |-
                                Metric contains the metric identification and backend configuration.
                                Defines which metrics to collect and how to query them.
                              properties:
                                backend:
                                  default: opentelemetry
                                  description: |-
                                    Backend defines the metrics backend to use.
                                    If not specified, defaults to "opentelemetry".
                                  enum:
                                  - opentelemetry
                                  type: string
                                metricNames:
                                  description: |-
                                    MetricNames specifies which metrics to collect from pods and send to ServerAddress.
                                    Example: ["vllm:num_requests_running"]
                                  items:
                                    type: string
                                  type: array
                                operationOverTime:
                                  description: |-
                                    OperationOverTime specifies the operation to aggregate metrics over time.
                                    Valid values: "last_one", "avg", "max", "min", "rate", "count"
                                    Default: "last_one"
                                  type: string
                                query:
                                  description: |-
                                    Query specifies the query to run to retrieve metrics from the backend.
                                    The query syntax depends on the backend being used.
                                    Example: "vllm:num_requests_running" for OpenTelemetry.
                                  type: string
                                serverAddress:
                                  description: |-
                                    ServerAddress specifies the address of the metrics backend server.
                                    If not specified, defaults to "keda-otel-scaler.keda.svc:4317" for OpenTelemetry backend.
                                  type: string
                              type: object
                              x-kubernetes-validations:
                              - message: podmetric.metric must set query or metricNames
                                  so the scaler has something to query
                                rule: has(self.query) || (has(self.metricNames) &&
                                  size(self.metricNames) > 0)
                            target:
                              description: |-
                                Target specifies the target value for the metric.
                                The autoscaler will scale to maintain this target value.
                              properties:
                                averageUtilization:
                                  description: |-
                                    AverageUtilization is the target value of the average of the resource metric across all relevant pods,
                                    represented as a percentage of the requested value of the resource for the pods.
                                    Used when Type is "Utilization". Only valid for Resource metric source type.
                                    Example: 80 for 80% utilization
                                  format: int32
                                  type: integer
                                averageValue:
                                  description: |-
                                    AverageValue is the target value of the average of the metric across all relevant pods (as a quantity).
                                    Used when Type is "AverageValue".
                                    Example: "100m" for 100 millicores per pod
                                  type: string
                                type:
                                  description: |-
                                    Type specifies how to interpret the metric value.
                                    "Value": absolute value target (use Value field)
                                    "AverageValue": average value across all pods (use AverageValue field)
                                    "Utilization": percentage utilization for resource metrics (use AverageUtilization field)
                                  enum:
                                  - Value
                                  - AverageValue
                                  - Utilization
                                  type: string
                                value:
                                  description: |-
                                    Value is the target value of the metric (as a quantity).
                                    Used when Type is "Value".
                                    Example: "1" for 1 request, "100m" for 100 millicores
                                  type: string
                              required:
                              - type
                              type: object
                              x-kubernetes-validations:
                              - message: target type Value requires target.value
                                rule: self.type != 'Value' || has(self.value)
                              - message: target type AverageValue requires target.averageValue
                                rule: self.type != 'AverageValue' || has(self.averageValue)
                              - message: target type Utilization requires target.averageUtilization
                                rule: self.type != 'Utilization' || has(self.averageUtilization)
                          required:
                          - metric
                          - target
                          type: object
                        type:
                          description: |-
                            Type is the type of metric source.
                            Valid values: "PodMetric" (per-pod custom metrics).
                          enum:
                          - PodMetric
                          type: string
                      required:
                      - type
                      type: object
                      x-kubernetes-validations:
                      - message: metrics entry of type PodMetric must set podmetric
                        rule: self.type != 'PodMetric' || has(self.podmetric)
                    type: array
                  pollingInterval:
                    description: |-
                      PollingInterval is the KEDA polling interval in seconds. Defaults to 5
                      when spec.minReplicas == 0 (so a single request reliably activates the
                      deployment within ~10s); otherwise unset (KEDA default of 30 applies).
                    format: int32
                    minimum: 1
                    type: integer
                type: object
                x-kubernetes-validations:
                - message: 'spec.autoScaling must not be empty: set at least one of
                    metrics, pollingInterval, or cooldownPeriod, or omit the autoScaling
                    block to use default scaling'
                  rule: (has(self.metrics) && size(self.metrics) > 0) || has(self.pollingInterval)
                    || has(self.cooldownPeriod)
              cacheModel:
                description: |-
                  DEPRECATED: Use Caching.Mode instead. This field will be removed in a future version.
                  This field is no longer honored by the controller.
                type: boolean
              caching:
                description: |-
                  Caching controls caching behavior for this service.
                  When nil, defaults to Shared mode.
                properties:
                  env:
                    description: |-
                      Env supplies credentials for model downloads (for example a HuggingFace
                      token via secretKeyRef). Unlike the inference container env, these
                      variables reach only the model-download Job, so download-only secrets are
                      never injected into the serving container. They are also reachable for
                      cluster-scoped and overlay profiles, where the profile's own caching.env
                      does not exist. Merged over the profile's caching.env (service wins).
                    items:
                      description: EnvVar represents an environment variable present
                        in a Container.
                      properties:
                        name:
                          description: |-
                            Name of the environment variable.
                            May consist of any printable ASCII characters except '='.
                          type: string
                        value:
                          description: |-
                            Variable references $(VAR_NAME) are expanded
                            using the previously defined environment variables in the container and
                            any service environment variables. If a variable cannot be resolved,
                            the reference in the input string will be unchanged. Double $$ are reduced
                            to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                            "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                            Escaped references will never be expanded, regardless of whether the variable
                            exists or not.
                            Defaults to "".
                          type: string
                        valueFrom:
                          description: Source for the environment variable's value.
                            Cannot be used if value is not empty.
                          properties:
                            configMapKeyRef:
                              description: Selects a key of a ConfigMap.
                              properties:
                                key:
                                  description: The key to select.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the ConfigMap or its
                                    key must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                            fieldRef:
                              description: |-
                                Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                              properties:
                                apiVersion:
                                  description: Version of the schema the FieldPath
                                    is written in terms of, defaults to "v1".
                                  type: string
                                fieldPath:
                                  description: Path of the field to select in the
                                    specified API version.
                                  type: string
                              required:
                              - fieldPath
                              type: object
                              x-kubernetes-map-type: atomic
                            fileKeyRef:
                              description: |-
                                FileKeyRef selects a key of the env file.
                                Requires the EnvFiles feature gate to be enabled.
                              properties:
                                key:
                                  description: |-
                                    The key within the env file. An invalid key will prevent the pod from starting.
                                    The keys defined within a source may consist of any printable ASCII characters except '='.
                                    During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                  type: string
                                optional:
                                  default: false
                                  description: |-
                                    Specify whether the file or its key must be defined. If the file or key
                                    does not exist, then the env var is not published.
                                    If optional is set to true and the specified key does not exist,
                                    the environment variable will not be set in the Pod's containers.

                                    If optional is set to false and the specified key does not exist,
                                    an error will be returned during Pod creation.
                                  type: boolean
                                path:
                                  description: |-
                                    The path within the volume from which to select the file.
                                    Must be relative and may not contain the '..' path or start with '..'.
                                  type: string
                                volumeName:
                                  description: The name of the volume mount containing
                                    the env file.
                                  type: string
                              required:
                              - key
                              - path
                              - volumeName
                              type: object
                              x-kubernetes-map-type: atomic
                            resourceFieldRef:
                              description: |-
                                Selects a resource of the container: only resources limits and requests
                                (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                              properties:
                                containerName:
                                  description: 'Container name: required for volumes,
                                    optional for env vars'
                                  type: string
                                divisor:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: Specifies the output format of the
                                    exposed resources, defaults to "1"
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                resource:
                                  description: 'Required: resource to select'
                                  type: string
                              required:
                              - resource
                              type: object
                              x-kubernetes-map-type: atomic
                            secretKeyRef:
                              description: Selects a key of a secret in the pod's
                                namespace
                              properties:
                                key:
                                  description: The key of the secret to select from.  Must
                                    be a valid secret key.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the Secret or its key
                                    must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  mode:
                    default: Shared
                    description: |-
                      Mode controls when to use caching.
                      Canonical values:
                      - Shared (default): reuse/create shared cache assets
                      - Dedicated: create service-owned dedicated cache assets

                      Legacy values are accepted and normalized:
                      - Always -> Shared
                      - Auto -> Shared
                      - Never -> Dedicated
                    enum:
                    - Dedicated
                    - Shared
                    - Auto
                    - Always
                    - Never
                    type: string
                type: object
                x-kubernetes-validations:
                - message: caching mode is immutable after creation
                  rule: self.mode == oldSelf.mode
              env:
                description: |-
                  Env specifies environment variables for inference containers.
                  When set on AIMService, these take highest precedence in the merge hierarchy.
                  When set on RuntimeConfig, these provide namespace/cluster-level defaults.
                  Merge order (highest to lowest): Service.Env > Template.Env > RuntimeConfig.Env > Profile.Env
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: |-
                        Name of the environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        fileKeyRef:
                          description: |-
                            FileKeyRef selects a key of the env file.
                            Requires the EnvFiles feature gate to be enabled.
                          properties:
                            key:
                              description: |-
                                The key within the env file. An invalid key will prevent the pod from starting.
                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                              type: string
                            optional:
                              default: false
                              description: |-
                                Specify whether the file or its key must be defined. If the file or key
                                does not exist, then the env var is not published.
                                If optional is set to true and the specified key does not exist,
                                the environment variable will not be set in the Pod's containers.

                                If optional is set to false and the specified key does not exist,
                                an error will be returned during Pod creation.
                              type: boolean
                            path:
                              description: |-
                                The path within the volume from which to select the file.
                                Must be relative and may not contain the '..' path or start with '..'.
                              type: string
                            volumeName:
                              description: The name of the volume mount containing
                                the env file.
                              type: string
                          required:
                          - key
                          - path
                          - volumeName
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              imagePullSecrets:
                description: ImagePullSecrets references secrets for pulling AIM container
                  images.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              maxReplicas:
                description: |-
                  MaxReplicas specifies the maximum number of replicas for autoscaling.
                  Required when MinReplicas is set or when AutoScaling configuration is provided.
                format: int32
                minimum: 1
                type: integer
              minReplicas:
                description: |-
                  MinReplicas specifies the minimum number of replicas for autoscaling.
                  Defaults to 1. Set to 0 to enable scale-to-zero: KEDA idles the predictor
                  to zero replicas when idle and brings it back up on the next request.
                  When specified with MaxReplicas, enables autoscaling for the service.
                format: int32
                minimum: 0
                type: integer
              model:
                description: |-
                  Model specifies which model to deploy using one of the available reference methods.
                  Use `name` to reference an existing AIMModel/AIMClusterModel by name, or use `image`
                  to specify a container image URI directly (which will auto-create a model if needed).
                  Required for v1alpha1 (template path), not permitted for v1alpha2 (profile path).
                properties:
                  custom:
                    description: |-
                      Custom specifies a custom model configuration with explicit base image,
                      model sources, and hardware requirements. The controller will search for
                      an existing matching AIMModel or auto-create one if not found.
                    properties:
                      baseImage:
                        description: |-
                          BaseImage is the container image URI for the AIM base image.
                          This will be used as the image for the auto-created AIMModel.
                          Example: `ghcr.io/silogen/aim-base:0.7.0`
                        type: string
                      hardware:
                        description: |-
                          Hardware specifies the GPU and CPU requirements for this custom model.
                          GPU is optional - if not set, no GPUs are requested (CPU-only model).
                        properties:
                          cpu:
                            description: CPU specifies CPU requirements.
                            properties:
                              limits:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Limits is the maximum number of CPU cores
                                  to allow.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              requests:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Requests is the number of CPU cores to
                                  request. Required and must be > 0.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            required:
                            - requests
                            type: object
                          gpu:
                            description: GPU specifies GPU requirements. If not set,
                              no GPUs are requested (CPU-only model).
                            properties:
                              minVram:
                                anyOf:
                                - type: integer
                                - type: string
                                description: |-
                                  MinVRAM limits deployment to GPUs having at least this much VRAM.
                                  Used for capacity planning when the model size is known but any GPU with
                                  sufficient VRAM is acceptable.
                                  Cannot be combined with model.
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              model:
                                description: |-
                                  Model limits deployment to a specific GPU model.
                                  Example: "MI300X"
                                  Cannot be combined with minVram.
                                maxLength: 64
                                type: string
                              requests:
                                description: |-
                                  Requests is the number of GPUs to set as requests/limits.
                                  Set to 0 to target GPU nodes without consuming GPU resources (useful for testing).
                                format: int32
                                minimum: 0
                                type: integer
                              resourceName:
                                default: amd.com/gpu
                                description: |-
                                  ResourceName is the Kubernetes resource name for GPU resources.
                                  Defaults to "amd.com/gpu" if not specified.
                                type: string
                            type: object
                            x-kubernetes-validations:
                            - message: model and minVram are mutually exclusive; specify
                                either a specific GPU model OR a minimum VRAM requirement,
                                not both
                              rule: '!(has(self.model) && size(self.model) > 0 &&
                                has(self.minVram))'
                        type: object
                        x-kubernetes-validations:
                        - message: at least one of gpu or cpu must be specified
                          rule: has(self.gpu) || has(self.cpu)
                      modelSources:
                        description: |-
                          ModelSources specifies the model sources to use.
                          The controller will search for or create an AIMModel with these sources.
                          The size field is optional - if not specified, it will be discovered by the download job.
                          AIM runtime currently supports only one model source.
                        items:
                          description: |-
                            AIMModelSource describes a model artifact that must be downloaded for inference.
                            Discovery extracts these from the container's configuration to enable caching and validation.
                          properties:
                            env:
                              description: |-
                                Env specifies per-source credential overrides.
                                These variables are used for authentication when downloading this specific source.
                                Takes precedence over base-level env for the same variable name.
                              items:
                                description: EnvVar represents an environment variable
                                  present in a Container.
                                properties:
                                  name:
                                    description: |-
                                      Name of the environment variable.
                                      May consist of any printable ASCII characters except '='.
                                    type: string
                                  value:
                                    description: |-
                                      Variable references $(VAR_NAME) are expanded
                                      using the previously defined environment variables in the container and
                                      any service environment variables. If a variable cannot be resolved,
                                      the reference in the input string will be unchanged. Double $$ are reduced
                                      to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                      "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                      Escaped references will never be expanded, regardless of whether the variable
                                      exists or not.
                                      Defaults to "".
                                    type: string
                                  valueFrom:
                                    description: Source for the environment variable's
                                      value. Cannot be used if value is not empty.
                                    properties:
                                      configMapKeyRef:
                                        description: Selects a key of a ConfigMap.
                                        properties:
                                          key:
                                            description: The key to select.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the ConfigMap
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fieldRef:
                                        description: |-
                                          Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                          spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                        properties:
                                          apiVersion:
                                            description: Version of the schema the
                                              FieldPath is written in terms of, defaults
                                              to "v1".
                                            type: string
                                          fieldPath:
                                            description: Path of the field to select
                                              in the specified API version.
                                            type: string
                                        required:
                                        - fieldPath
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fileKeyRef:
                                        description: |-
                                          FileKeyRef selects a key of the env file.
                                          Requires the EnvFiles feature gate to be enabled.
                                        properties:
                                          key:
                                            description: |-
                                              The key within the env file. An invalid key will prevent the pod from starting.
                                              The keys defined within a source may consist of any printable ASCII characters except '='.
                                              During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                            type: string
                                          optional:
                                            default: false
                                            description: |-
                                              Specify whether the file or its key must be defined. If the file or key
                                              does not exist, then the env var is not published.
                                              If optional is set to true and the specified key does not exist,
                                              the environment variable will not be set in the Pod's containers.

                                              If optional is set to false and the specified key does not exist,
                                              an error will be returned during Pod creation.
                                            type: boolean
                                          path:
                                            description: |-
                                              The path within the volume from which to select the file.
                                              Must be relative and may not contain the '..' path or start with '..'.
                                            type: string
                                          volumeName:
                                            description: The name of the volume mount
                                              containing the env file.
                                            type: string
                                        required:
                                        - key
                                        - path
                                        - volumeName
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      resourceFieldRef:
                                        description: |-
                                          Selects a resource of the container: only resources limits and requests
                                          (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                        properties:
                                          containerName:
                                            description: 'Container name: required
                                              for volumes, optional for env vars'
                                            type: string
                                          divisor:
                                            anyOf:
                                            - type: integer
                                            - type: string
                                            description: Specifies the output format
                                              of the exposed resources, defaults to
                                              "1"
                                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                            x-kubernetes-int-or-string: true
                                          resource:
                                            description: 'Required: resource to select'
                                            type: string
                                        required:
                                        - resource
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      secretKeyRef:
                                        description: Selects a key of a secret in
                                          the pod's namespace
                                        properties:
                                          key:
                                            description: The key of the secret to
                                              select from.  Must be a valid secret
                                              key.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the Secret
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                    type: object
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            modelId:
                              description: |-
                                ModelID is the canonical identifier in {org}/{name} format.
                                Determines the cache mount path: /workspace/cache/{modelId}
                                For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                                For S3 sources, users define their own organizational structure.
                              pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                              type: string
                            precision:
                              allOf:
                              - enum:
                                - auto
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              - enum:
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              description: |-
                                Precision describes the runtime precision this source is compatible with.
                                Used to match model sources to profiles during custom weight onboarding.
                              type: string
                            size:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                Size is the expected storage space required for this model artifact.
                                Used for PVC sizing and capacity planning during cache creation.
                                Optional - if not specified, the download job will discover the size automatically.
                                Can be set explicitly to pre-allocate storage or override auto-discovery.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            sourceUri:
                              description: |-
                                SourceURI is the location from which the model should be downloaded.
                                Supported schemes:
                                - hf://org/model - Hugging Face Hub model
                                - s3://bucket/key - S3-compatible storage
                              pattern: ^(hf|s3)://[^ \t\r\n]+$
                              type: string
                          required:
                          - modelId
                          - sourceUri
                          type: object
                        maxItems: 1
                        minItems: 1
                        type: array
                    required:
                    - baseImage
                    - hardware
                    - modelSources
                    type: object
                    x-kubernetes-validations:
                    - message: at least one model source must be specified
                      rule: size(self.modelSources) >= 1
                  image:
                    description: |-
                      Image specifies a container image URI directly.
                      The controller searches for an existing model with this image, or creates one if none exists.
                      Auto-created models are namespace-scoped and can be reused by other services.
                      Example: `ghcr.io/silogen/llama-3-8b:v1.2.0`
                    type: string
                  name:
                    description: |-
                      Name references an existing AIMModel or AIMClusterModel by metadata.name.
                      The controller looks for a namespace-scoped AIMModel first, then falls back to cluster-scoped AIMClusterModel.
                      Example: `meta-llama-3-8b`
                    type: string
                type: object
                x-kubernetes-validations:
                - message: exactly one of name, image, or custom must be specified
                  rule: '(has(self.name) ? 1 : 0) + (has(self.image) ? 1 : 0) + (has(self.custom)
                    ? 1 : 0) == 1'
                - message: model selection is immutable after creation
                  rule: self == oldSelf
              overrides:
                description: |-
                  Overrides allows overriding specific template parameters for this service.
                  When specified, these values take precedence over the template values.
                properties:
                  hardware:
                    description: |-
                      Hardware specifies GPU and CPU requirements for each replica.
                      For GPU models, defines the GPU count and model types required for deployment.
                      For CPU-only models, defines CPU resource requirements.
                      This field is immutable after creation.
                    properties:
                      cpu:
                        description: CPU specifies CPU requirements.
                        properties:
                          limits:
                            anyOf:
                            - type: integer
                            - type: string
                            description: Limits is the maximum number of CPU cores
                              to allow.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          requests:
                            anyOf:
                            - type: integer
                            - type: string
                            description: Requests is the number of CPU cores to request.
                              Required and must be > 0.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                        required:
                        - requests
                        type: object
                      gpu:
                        description: GPU specifies GPU requirements. If not set, no
                          GPUs are requested (CPU-only model).
                        properties:
                          minVram:
                            anyOf:
                            - type: integer
                            - type: string
                            description: |-
                              MinVRAM limits deployment to GPUs having at least this much VRAM.
                              Used for capacity planning when the model size is known but any GPU with
                              sufficient VRAM is acceptable.
                              Cannot be combined with model.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          model:
                            description: |-
                              Model limits deployment to a specific GPU model.
                              Example: "MI300X"
                              Cannot be combined with minVram.
                            maxLength: 64
                            type: string
                          requests:
                            description: |-
                              Requests is the number of GPUs to set as requests/limits.
                              Set to 0 to target GPU nodes without consuming GPU resources (useful for testing).
                            format: int32
                            minimum: 0
                            type: integer
                          resourceName:
                            default: amd.com/gpu
                            description: |-
                              ResourceName is the Kubernetes resource name for GPU resources.
                              Defaults to "amd.com/gpu" if not specified.
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: model and minVram are mutually exclusive; specify
                            either a specific GPU model OR a minimum VRAM requirement,
                            not both
                          rule: '!(has(self.model) && size(self.model) > 0 && has(self.minVram))'
                    type: object
                    x-kubernetes-validations:
                    - message: hardware is immutable
                      rule: self == oldSelf
                    - message: at least one of gpu or cpu must be specified
                      rule: has(self.gpu) || has(self.cpu)
                  metric:
                    allOf:
                    - enum:
                      - latency
                      - throughput
                    - enum:
                      - latency
                      - throughput
                    description: |-
                      Metric selects the optimization goal.

                      - `latency`: prioritize low end‑to‑end latency
                      - `throughput`: prioritize sustained requests/second
                    type: string
                    x-kubernetes-validations:
                    - message: metric is immutable
                      rule: self == oldSelf
                  precision:
                    allOf:
                    - enum:
                      - auto
                      - fp4
                      - fp8
                      - fp16
                      - fp32
                      - bf16
                      - int4
                      - int8
                    - enum:
                      - auto
                      - fp4
                      - fp8
                      - fp16
                      - fp32
                      - bf16
                      - int4
                      - int8
                    description: Precision selects the numeric precision used by the
                      runtime.
                    type: string
                    x-kubernetes-validations:
                    - message: precision is immutable
                      rule: self == oldSelf
                type: object
              priorityClassName:
                description: |-
                  PriorityClassName specifies the priority class for the inference pods.
                  This maps directly to the Kubernetes PriorityClassName field on the pod spec.
                  If empty, no priority class is set.
                type: string
              profile:
                description: |-
                  Profile contains profile selection configuration (v1alpha2 only).
                  When set, the service uses a profile-based reconciliation path.
                  Mutually exclusive with Template.
                properties:
                  name:
                    description: |-
                      Name is the name of the AIMProfile or AIMClusterProfile to use.
                      The controller looks for a namespace-scoped AIMProfile first, then falls back to AIMClusterProfile.
                      Mutually exclusive with Selector. An empty string is treated as unset.
                    type: string
                  selector:
                    description: |-
                      Selector narrows candidate AIMProfile / AIMClusterProfile objects via the
                      shared provenance labels (role, source-model, origin) and spec filters
                      (aimId, precision, acceleratorModel, ...). The controller forces
                      `selector.role = Deployable` at evaluation time; user-supplied values
                      for that field are rejected by CEL on v1alpha2.

                      For every selector-driven AIMService the controller requires at least
                      one of `selector.aimId` or `selector.modelRef.name` so the watch
                      fan-out can reach the service via an O(1) index lookup. The top-level
                      `spec.model.name` shortcut is treated as if the user had set
                      `selector.modelRef.name` to the same value when not explicit.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount filters by accelerator unit
                          count.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel filters by accelerator identifier.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode filters candidates by their declared
                          partitioning mode. Partial-order match (NOT strict equality):
                            ""              - no filter on this field.
                            "unpartitioned" - matches profiles with mode "" or "unpartitioned".
                            "partitioned"   - matches profiles whose mode is non-trivial (anything
                                              other than "" / "unpartitioned").
                            "<C>"           - selector-only convenience: matches profiles with mode
                                              "<C>-*" (prefix on the scheme). Not a valid profile-spec
                                              value (e.g. selector "CPX" matches "CPX-NPS1", "CPX-NPS4").
                            "<C>-<M>"       - exact-string match on the scheme.
                        type: string
                      acceleratorType:
                        description: AcceleratorType filters by accelerator resource
                          type.
                        enum:
                        - gpu
                        - cpu
                        type: string
                      aimId:
                        description: AimId filters by model architecture identifier
                          (e.g., "qwen/qwen3-32b").
                        type: string
                      engine:
                        description: Engine filters by inference engine.
                        type: string
                      engineArgs:
                        description: |-
                          EngineArgs partially matches source engineArgs: every provided top-level key must
                          exist in the source object with an equal value.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      metric:
                        description: Metric filters by optimization target.
                        enum:
                        - latency
                        - throughput
                        type: string
                      minimumType:
                        description: |-
                          MinimumType filters by a minimum optimization level: candidates whose
                          type is this tier OR BETTER are accepted (hierarchy: optimized > general
                          > preview > unoptimized). This is the floor counterpart to the exact-match
                          Type field; the two AND together when both are set.

                          The sentinel "any" disables the floor (accept every tier). When this
                          field is empty the AIMService resolver applies a default floor of
                          "optimized" so auto-selection prefers production-grade profiles and never
                          silently picks an unoptimized one; to opt a service into lower tiers
                          (e.g. CPU/EPYC profiles published as unoptimized) set minimumType
                          explicitly to "unoptimized" or "any". Derivation selectors
                          (AIMProfileSet / AIMModel.profiles) treat empty as "any" so copying is
                          never tier-restricted by default.
                        enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                        - any
                        type: string
                      modelId:
                        description: ModelId filters by the source profile's specific
                          model identifier.
                        type: string
                      modelRef:
                        description: |-
                          ModelRef narrows candidates to those produced by a specific
                          AIM(Cluster)Model, matched via the `aim.eai.amd.com/source-model[-scope]`
                          labels stamped by the AIMModel reconcilers. Iteration 1 (v1alpha2 only).
                        properties:
                          name:
                            description: Name is the owning AIM(Cluster)Model name.
                              Required.
                            minLength: 1
                            type: string
                          scope:
                            default: Auto
                            description: Scope controls how Name is resolved against
                              AIMModel vs AIMClusterModel.
                            enum:
                            - Auto
                            - Namespace
                            - Cluster
                            type: string
                        required:
                        - name
                        type: object
                      origin:
                        description: |-
                          Origin filters by the `aim.eai.amd.com/profile-origin` label. When unset
                          (empty) the selector does not filter by origin. Iteration 1 (v1alpha2
                          only).
                        enum:
                        - discovered
                        - derived
                        - user-authored
                        type: string
                      precision:
                        description: Precision filters by numeric precision.
                        enum:
                        - auto
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                        type: string
                      profileId:
                        description: ProfileId filters by the source profile's profile
                          identifier.
                        type: string
                      role:
                        default: deployable
                        description: |-
                          Role filters by the `aim.eai.amd.com/profile-role` label. Defaults to
                          `deployable`. `base` filters to base profiles emitted by base-image
                          discovery (custom-model derivation source material).
                        enum:
                        - base
                        - deployable
                        type: string
                      type:
                        description: Type filters by optimization level (exact match).
                        enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                        type: string
                    type: object
                type: object
                x-kubernetes-validations:
                - message: spec.profile.name and spec.profile.selector are mutually
                    exclusive
                  rule: '!((has(self.name) && size(self.name) > 0) && has(self.selector))'
                - message: spec.profile must set name or selector
                  rule: (has(self.name) && size(self.name) > 0) || has(self.selector)
              profileOverrides:
                description: |-
                  ProfileOverrides allows overriding specific profile parameters for this service.
                  Only valid when Profile is set.
                properties:
                  acceleratorCount:
                    description: AcceleratorCount replaces the referenced profile's
                      acceleratorCount.
                    format: int32
                    type: integer
                  acceleratorModel:
                    description: |-
                      AcceleratorModel replaces the referenced profile's acceleratorModel
                      (e.g. "MI300X" -> "MI325X"). Validation against actual cluster
                      availability is left to the AIMServiceTemplate / runtime layers.
                    type: string
                  acceleratorPartitioningMode:
                    description: |-
                      AcceleratorPartitioningMode replaces the referenced profile's
                      acceleratorPartitioningMode. Complete replacement, not a merge — single
                      string, no substruct ambiguity. Empty string means "no override" (the
                      resolved overlay inherits the base profile's mode). Use this to deploy a
                      profile written for whole GPUs onto a partition slice. Whenever this
                      override is set (to any value, including "unpartitioned"), the CEL rule on
                      AIMService also requires an acceleratorCount override: partition mode
                      changes the per-unit interpretation of acceleratorCount, and CEL cannot
                      read the base profile to tell whether the meaning actually changed, so it
                      conservatively requires the count be restated. See
                      AcceleratorPartitioningMode on AIMProfileSpecCommon for the reserved
                      values ("unpartitioned", "partitioned", "<C>-<M>").
                    type: string
                  containerEnv:
                    description: |-
                      ContainerEnv merges by env-var name on top of the profile's
                      containerEnv. Matching names override; new names are appended.
                      AIM framework variables (AIM_*) reserved for the controller are
                      applied after the overlay's containerEnv and cannot be overridden
                      here.
                    items:
                      description: EnvVar represents an environment variable present
                        in a Container.
                      properties:
                        name:
                          description: |-
                            Name of the environment variable.
                            May consist of any printable ASCII characters except '='.
                          type: string
                        value:
                          description: |-
                            Variable references $(VAR_NAME) are expanded
                            using the previously defined environment variables in the container and
                            any service environment variables. If a variable cannot be resolved,
                            the reference in the input string will be unchanged. Double $$ are reduced
                            to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                            "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                            Escaped references will never be expanded, regardless of whether the variable
                            exists or not.
                            Defaults to "".
                          type: string
                        valueFrom:
                          description: Source for the environment variable's value.
                            Cannot be used if value is not empty.
                          properties:
                            configMapKeyRef:
                              description: Selects a key of a ConfigMap.
                              properties:
                                key:
                                  description: The key to select.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the ConfigMap or its
                                    key must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                            fieldRef:
                              description: |-
                                Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                              properties:
                                apiVersion:
                                  description: Version of the schema the FieldPath
                                    is written in terms of, defaults to "v1".
                                  type: string
                                fieldPath:
                                  description: Path of the field to select in the
                                    specified API version.
                                  type: string
                              required:
                              - fieldPath
                              type: object
                              x-kubernetes-map-type: atomic
                            fileKeyRef:
                              description: |-
                                FileKeyRef selects a key of the env file.
                                Requires the EnvFiles feature gate to be enabled.
                              properties:
                                key:
                                  description: |-
                                    The key within the env file. An invalid key will prevent the pod from starting.
                                    The keys defined within a source may consist of any printable ASCII characters except '='.
                                    During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                  type: string
                                optional:
                                  default: false
                                  description: |-
                                    Specify whether the file or its key must be defined. If the file or key
                                    does not exist, then the env var is not published.
                                    If optional is set to true and the specified key does not exist,
                                    the environment variable will not be set in the Pod's containers.

                                    If optional is set to false and the specified key does not exist,
                                    an error will be returned during Pod creation.
                                  type: boolean
                                path:
                                  description: |-
                                    The path within the volume from which to select the file.
                                    Must be relative and may not contain the '..' path or start with '..'.
                                  type: string
                                volumeName:
                                  description: The name of the volume mount containing
                                    the env file.
                                  type: string
                              required:
                              - key
                              - path
                              - volumeName
                              type: object
                              x-kubernetes-map-type: atomic
                            resourceFieldRef:
                              description: |-
                                Selects a resource of the container: only resources limits and requests
                                (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                              properties:
                                containerName:
                                  description: 'Container name: required for volumes,
                                    optional for env vars'
                                  type: string
                                divisor:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: Specifies the output format of the
                                    exposed resources, defaults to "1"
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                resource:
                                  description: 'Required: resource to select'
                                  type: string
                              required:
                              - resource
                              type: object
                              x-kubernetes-map-type: atomic
                            secretKeyRef:
                              description: Selects a key of a secret in the pod's
                                namespace
                              properties:
                                key:
                                  description: The key of the secret to select from.  Must
                                    be a valid secret key.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the Secret or its key
                                    must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  engineArgs:
                    description: |-
                      EngineArgs shallow-merges on top of the profile's engineArgs,
                      overriding matching top-level keys. Values are passed verbatim
                      to the inference engine CLI.
                    x-kubernetes-preserve-unknown-fields: true
                  engineEnv:
                    additionalProperties:
                      type: string
                    description: |-
                      EngineEnv merges by key on top of the profile's engineEnv. These
                      variables flow into the inference engine's runtime configuration.
                    type: object
                  modelSources:
                    description: |-
                      ModelSources replaces the referenced profile's modelSources entirely.
                      Use this to point a profile at user-supplied weights (e.g. a fine-tuned
                      checkpoint) without forking the profile itself. The first source's
                      modelId becomes the overlay profile's modelId.
                    items:
                      description: |-
                        AIMModelSource describes a model artifact that must be downloaded for inference.
                        Discovery extracts these from the container's configuration to enable caching and validation.
                      properties:
                        env:
                          description: |-
                            Env specifies per-source credential overrides.
                            These variables are used for authentication when downloading this specific source.
                            Takes precedence over base-level env for the same variable name.
                          items:
                            description: EnvVar represents an environment variable
                              present in a Container.
                            properties:
                              name:
                                description: |-
                                  Name of the environment variable.
                                  May consist of any printable ASCII characters except '='.
                                type: string
                              value:
                                description: |-
                                  Variable references $(VAR_NAME) are expanded
                                  using the previously defined environment variables in the container and
                                  any service environment variables. If a variable cannot be resolved,
                                  the reference in the input string will be unchanged. Double $$ are reduced
                                  to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                  "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                  Escaped references will never be expanded, regardless of whether the variable
                                  exists or not.
                                  Defaults to "".
                                type: string
                              valueFrom:
                                description: Source for the environment variable's
                                  value. Cannot be used if value is not empty.
                                properties:
                                  configMapKeyRef:
                                    description: Selects a key of a ConfigMap.
                                    properties:
                                      key:
                                        description: The key to select.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the ConfigMap
                                          or its key must be defined
                                        type: boolean
                                    required:
                                    - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  fieldRef:
                                    description: |-
                                      Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                      spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                    properties:
                                      apiVersion:
                                        description: Version of the schema the FieldPath
                                          is written in terms of, defaults to "v1".
                                        type: string
                                      fieldPath:
                                        description: Path of the field to select in
                                          the specified API version.
                                        type: string
                                    required:
                                    - fieldPath
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  fileKeyRef:
                                    description: |-
                                      FileKeyRef selects a key of the env file.
                                      Requires the EnvFiles feature gate to be enabled.
                                    properties:
                                      key:
                                        description: |-
                                          The key within the env file. An invalid key will prevent the pod from starting.
                                          The keys defined within a source may consist of any printable ASCII characters except '='.
                                          During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                        type: string
                                      optional:
                                        default: false
                                        description: |-
                                          Specify whether the file or its key must be defined. If the file or key
                                          does not exist, then the env var is not published.
                                          If optional is set to true and the specified key does not exist,
                                          the environment variable will not be set in the Pod's containers.

                                          If optional is set to false and the specified key does not exist,
                                          an error will be returned during Pod creation.
                                        type: boolean
                                      path:
                                        description: |-
                                          The path within the volume from which to select the file.
                                          Must be relative and may not contain the '..' path or start with '..'.
                                        type: string
                                      volumeName:
                                        description: The name of the volume mount
                                          containing the env file.
                                        type: string
                                    required:
                                    - key
                                    - path
                                    - volumeName
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  resourceFieldRef:
                                    description: |-
                                      Selects a resource of the container: only resources limits and requests
                                      (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                    properties:
                                      containerName:
                                        description: 'Container name: required for
                                          volumes, optional for env vars'
                                        type: string
                                      divisor:
                                        anyOf:
                                        - type: integer
                                        - type: string
                                        description: Specifies the output format of
                                          the exposed resources, defaults to "1"
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      resource:
                                        description: 'Required: resource to select'
                                        type: string
                                    required:
                                    - resource
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  secretKeyRef:
                                    description: Selects a key of a secret in the
                                      pod's namespace
                                    properties:
                                      key:
                                        description: The key of the secret to select
                                          from.  Must be a valid secret key.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the Secret or
                                          its key must be defined
                                        type: boolean
                                    required:
                                    - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                type: object
                            required:
                            - name
                            type: object
                          type: array
                          x-kubernetes-list-map-keys:
                          - name
                          x-kubernetes-list-type: map
                        modelId:
                          description: |-
                            ModelID is the canonical identifier in {org}/{name} format.
                            Determines the cache mount path: /workspace/cache/{modelId}
                            For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                            For S3 sources, users define their own organizational structure.
                          pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                          type: string
                        precision:
                          allOf:
                          - enum:
                            - auto
                            - fp4
                            - fp8
                            - fp16
                            - fp32
                            - bf16
                            - int4
                            - int8
                          - enum:
                            - fp4
                            - fp8
                            - fp16
                            - fp32
                            - bf16
                            - int4
                            - int8
                          description: |-
                            Precision describes the runtime precision this source is compatible with.
                            Used to match model sources to profiles during custom weight onboarding.
                          type: string
                        size:
                          anyOf:
                          - type: integer
                          - type: string
                          description: |-
                            Size is the expected storage space required for this model artifact.
                            Used for PVC sizing and capacity planning during cache creation.
                            Optional - if not specified, the download job will discover the size automatically.
                            Can be set explicitly to pre-allocate storage or override auto-discovery.
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        sourceUri:
                          description: |-
                            SourceURI is the location from which the model should be downloaded.
                            Supported schemes:
                            - hf://org/model - Hugging Face Hub model
                            - s3://bucket/key - S3-compatible storage
                          pattern: ^(hf|s3)://[^ \t\r\n]+$
                          type: string
                      required:
                      - modelId
                      - sourceUri
                      type: object
                    type: array
                type: object
              replicas:
                default: 1
                description: |-
                  Replicas specifies the number of replicas for this service.
                  When not specified, defaults to 1 replica.
                  This value overrides any replica settings from the template.
                  For autoscaling, use MinReplicas and MaxReplicas instead.
                format: int32
                type: integer
              resources:
                description: |-
                  Resources overrides the container resource requirements for this service.
                  When specified, these values take precedence over the template and image defaults.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              routing:
                description: |-
                  Routing controls HTTP routing configuration for this service.
                  When set, these values override namespace/cluster runtime config defaults.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: |-
                      Annotations defines default annotations to add to all HTTPRoute resources.
                      Services can add additional annotations or override these via spec.routing.annotations.
                      When both are specified, service annotations take precedence for conflicting keys.
                      Common use cases include ingress controller settings, rate limiting, monitoring labels,
                      and security policies that should apply to all services using this config.
                    type: object
                  enabled:
                    description: |-
                      Enabled controls whether HTTP routing is managed for inference services using this config.
                      When true, the operator creates HTTPRoute resources for services that reference this config.
                      When false or unset, routing must be explicitly enabled on each service.
                      This provides a namespace or cluster-wide default that individual services can override.
                    type: boolean
                  gatewayRef:
                    description: |-
                      GatewayRef specifies the Gateway API Gateway resource that should receive HTTPRoutes.
                      This identifies the parent gateway for routing traffic to inference services.
                      The gateway can be in any namespace (cross-namespace references are supported).
                      If routing is enabled but GatewayRef is not specified, service reconciliation will fail
                      with a validation error.
                    properties:
                      group:
                        default: gateway.networking.k8s.io
                        description: |-
                          Group is the group of the referent.
                          When unspecified, "gateway.networking.k8s.io" is inferred.
                          To set the core API group (such as for a "Service" kind referent),
                          Group must be explicitly set to "" (empty string).

                          Support: Core
                        maxLength: 253
                        pattern: ^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                        type: string
                      kind:
                        default: Gateway
                        description: |-
                          Kind is kind of the referent.

                          There are two kinds of parent resources with "Core" support:

                          * Gateway (Gateway conformance profile)
                          * Service (Mesh conformance profile, ClusterIP Services only)

                          Support for other resources is Implementation-Specific.
                        maxLength: 63
                        minLength: 1
                        pattern: ^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$
                        type: string
                      name:
                        description: |-
                          Name is the name of the referent.

                          Support: Core
                        maxLength: 253
                        minLength: 1
                        type: string
                      namespace:
                        description: |-
                          Namespace is the namespace of the referent. When unspecified, this refers
                          to the local namespace of the Route.

                          Note that there are specific rules for ParentRefs which cross namespace
                          boundaries. Cross-namespace references are only valid if they are explicitly
                          allowed by something in the namespace they are referring to. For example:
                          Gateway has the AllowedRoutes field, and ReferenceGrant provides a
                          generic way to enable any other kind of cross-namespace reference.

                          <gateway:experimental:description>
                          ParentRefs from a Route to a Service in the same namespace are "producer"
                          routes, which apply default routing rules to inbound connections from
                          any namespace to the Service.

                          ParentRefs from a Route to a Service in a different namespace are
                          "consumer" routes, and these routing rules are only applied to outbound
                          connections originating from the same namespace as the Route, for which
                          the intended destination of the connections are a Service targeted as a
                          ParentRef of the Route.
                          </gateway:experimental:description>

                          Support: Core
                        maxLength: 63
                        minLength: 1
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                        type: string
                      port:
                        description: |-
                          Port is the network port this Route targets. It can be interpreted
                          differently based on the type of parent resource.

                          When the parent resource is a Gateway, this targets all listeners
                          listening on the specified port that also support this kind of Route(and
                          select this Route). It's not recommended to set `Port` unless the
                          networking behaviors specified in a Route must apply to a specific port
                          as opposed to a listener(s) whose port(s) may be changed. When both Port
                          and SectionName are specified, the name and port of the selected listener
                          must match both specified values.

                          <gateway:experimental:description>
                          When the parent resource is a Service, this targets a specific port in the
                          Service spec. When both Port (experimental) and SectionName are specified,
                          the name and port of the selected port must match both specified values.
                          </gateway:experimental:description>

                          Implementations MAY choose to support other parent resources.
                          Implementations supporting other types of parent resources MUST clearly
                          document how/if Port is interpreted.

                          For the purpose of status, an attachment is considered successful as
                          long as the parent resource accepts it partially. For example, Gateway
                          listeners can restrict which Routes can attach to them by Route kind,
                          namespace, or hostname. If 1 of 2 Gateway listeners accept attachment
                          from the referencing Route, the Route MUST be considered successfully
                          attached. If no Gateway listeners accept attachment from this Route,
                          the Route MUST be considered detached from the Gateway.

                          Support: Extended
                        format: int32
                        maximum: 65535
                        minimum: 1
                        type: integer
                      sectionName:
                        description: |-
                          SectionName is the name of a section within the target resource. In the
                          following resources, SectionName is interpreted as the following:

                          * Gateway: Listener name. When both Port (experimental) and SectionName
                          are specified, the name and port of the selected listener must match
                          both specified values.
                          * Service: Port name. When both Port (experimental) and SectionName
                          are specified, the name and port of the selected listener must match
                          both specified values.

                          Implementations MAY choose to support attaching Routes to other resources.
                          If that is the case, they MUST clearly document how SectionName is
                          interpreted.

                          When unspecified (empty string), this will reference the entire resource.
                          For the purpose of status, an attachment is considered successful if at
                          least one section in the parent resource accepts it. For example, Gateway
                          listeners can restrict which Routes can attach to them by Route kind,
                          namespace, or hostname. If 1 of 2 Gateway listeners accept attachment from
                          the referencing Route, the Route MUST be considered successfully
                          attached. If no Gateway listeners accept attachment from this Route, the
                          Route MUST be considered detached from the Gateway.

                          Support: Core
                        maxLength: 253
                        minLength: 1
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                        type: string
                    required:
                    - name
                    type: object
                  hostnames:
                    description: |-
                      Hostnames pins generated HTTPRoutes to these hostnames so a route only
                      attaches to the matching Gateway listener instead of every listener on
                      the parent gateway. Without a hostname, an HTTPRoute matches all of the
                      parent gateway's listener hostnames, which can expose a service on
                      listeners that do not enforce the intended authentication.

                      This field is required when the parent gateway exposes more than one
                      listener: in that case a service with routing enabled but no hostnames
                      configured will not get an HTTPRoute and reports ConfigValid=False with
                      reason RouteHostnameRequired. When the parent gateway has a single
                      listener, leaving this empty preserves the existing behavior (the route
                      inherits that listener's hostnames).

                      Individual services can override this list via spec.routing.hostnames.
                    items:
                      description: |-
                        Hostname is the fully qualified domain name of a network host. This matches
                        the RFC 1123 definition of a hostname with 2 notable exceptions:

                         1. IPs are not allowed.
                         2. A hostname may be prefixed with a wildcard label (`*.`). The wildcard
                            label must appear by itself as the first label.

                        Hostname can be "precise" which is a domain name without the terminating
                        dot of a network host (e.g. "foo.example.com") or "wildcard", which is a
                        domain name prefixed with a single wildcard label (e.g. `*.example.com`).

                        Note that as per RFC1035 and RFC1123, a *label* must consist of lower case
                        alphanumeric characters or '-', and must start and end with an alphanumeric
                        character. No other punctuation is allowed.
                      maxLength: 253
                      minLength: 1
                      pattern: ^(\*\.)?[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                      type: string
                    type: array
                  pathTemplate:
                    description: |-
                      PathTemplate defines the HTTP path template for routes, evaluated using JSONPath expressions.
                      The template is rendered against the AIMService object to generate unique paths.

                      Example templates:
                      - `/{.metadata.namespace}/{.metadata.name}` - namespace and service name
                      - `/{.metadata.namespace}/{.metadata.labels['team']}/inference` - with label
                      - `/models/{.metadata.name}` - based on service name

                      The template must:
                      - Use valid JSONPath expressions wrapped in {...}
                      - Reference fields that exist on the service
                      - Produce a path ≤ 200 characters after rendering
                      - Result in valid URL path segments (lowercase, RFC 1123 compliant)

                      If evaluation fails, the service enters Degraded state with PathTemplateInvalid reason.
                      Individual services can override this template via spec.routing.pathTemplate.
                    type: string
                  requestTimeout:
                    description: |-
                      RequestTimeout defines the HTTP request timeout for routes.
                      This sets the maximum duration for a request to complete before timing out.
                      The timeout applies to the entire request/response cycle.
                      If not specified, no timeout is set on the route.
                      Individual services can override this value via spec.routing.requestTimeout.
                    type: string
                type: object
              runtimeConfigName:
                description: |-
                  Name is the name of the runtime config to use for this resource. If a runtime config with this name exists both
                  as a namespace and a cluster runtime config, the values are merged together, the namespace config taking priority
                  over the cluster config when there are conflicts. If this field is empty or set to `default`, the namespace / cluster
                  runtime config with the name `default` is used, if it exists.
                type: string
              serviceAccountName:
                description: |-
                  ServiceAccountName specifies the Kubernetes service account to use for the inference workload.
                  This service account is used by the deployed inference pods.
                  If empty, the default service account for the namespace is used.
                type: string
              storage:
                description: |-
                  Storage configures storage defaults for this service's PVCs and caches.
                  When set, these values override namespace/cluster runtime config defaults.
                properties:
                  adapterDiskSize:
                    anyOf:
                    - type: integer
                    - type: string
                    description: |-
                      AdapterDiskSize is the cluster default size for the shared adapter disk PVC
                      (built-in default when unset). An artifact's adapterDisk.size wins over it.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  adapterDiskStorageClassName:
                    description: |-
                      AdapterDiskStorageClassName is the storage class for the shared
                      ReadWriteMany adapter disk. It must be RWX-capable (e.g. longhorn, NFS) and
                      is resolved before the (typically RWO) DefaultStorageClassName.
                    type: string
                  defaultStorageClassName:
                    description: |-
                      DefaultStorageClassName specifies the storage class to use for artifacts and PVCs
                      when the consuming resource (AIMArtifact, AIMTemplateCache, AIMServiceTemplate) does not
                      specify a storage class. If this field is empty, the cluster's default storage class is used.
                    type: string
                  downloadFilter:
                    description: |-
                      DownloadFilter controls which files are included or excluded during artifact downloads.
                      When set here, applies as the default for all artifacts using this runtime config.
                      Individual artifacts can override this with their own downloadFilter.
                      When no filter is configured at any level, subdirectory files are excluded by default.
                      Set to an empty object (downloadFilter: {}) to explicitly allow all files.
                    properties:
                      exclude:
                        description: |-
                          Exclude specifies glob patterns for files to skip.
                          Files matching any exclude pattern are removed after include filtering.
                          Patterns use fnmatch syntax (e.g., ["*/*", "*.bin"]).
                          Use ["*/*"] to exclude all files in subdirectories (the default when no filter is set).
                        items:
                          type: string
                        type: array
                      include:
                        description: |-
                          Include specifies glob patterns for files to download.
                          Only files matching at least one pattern are considered.
                          If empty, all files pass the include check.
                          Patterns use fnmatch syntax (e.g., ["*.safetensors", "config.json"]).
                        items:
                          type: string
                        type: array
                    type: object
                  pvcHeadroomPercent:
                    default: 10
                    description: |-
                      PVCHeadroomPercent specifies the percentage of extra space to add to PVCs
                      for model storage. This accounts for filesystem overhead and temporary files
                      during model loading. The value represents a percentage (e.g., 10 means 10% extra space).
                      If not specified, defaults to 10%.
                    format: int32
                    minimum: 0
                    type: integer
                type: object
              template:
                description: |-
                  Template contains template selection and configuration.
                  Use Template.Name to specify an explicit template, or omit to auto-select.
                  Mutually exclusive with Profile (v1alpha2).
                properties:
                  allowUnoptimized:
                    description: |-
                      AllowUnoptimized, if true, will allow automatic selection of templates
                      that resolve to an unoptimized profile.
                    type: boolean
                  name:
                    description: |-
                      Name is the name of the AIMServiceTemplate or AIMClusterServiceTemplate to use.
                      The template selects the runtime profile and GPU parameters.
                      When not specified, a template will be automatically selected based on the model.
                    type: string
                type: object
                x-kubernetes-validations:
                - message: template selection is immutable after creation
                  rule: self == oldSelf
            type: object
            x-kubernetes-validations:
            - message: minReplicas must be less than or equal to maxReplicas
              rule: '!has(self.minReplicas) || !has(self.maxReplicas) || self.minReplicas
                <= self.maxReplicas'
          status:
            description: AIMServiceStatus defines the observed state of AIMService.
            properties:
              adapterDiskPersistentVolumeClaim:
                description: |-
                  AdapterDiskPersistentVolumeClaim is the resolved shared adapter-disk PVC
                  (from the base model artifact's status). It is recorded here once resolved
                  and reused when a transient parent-resolution gap would otherwise leave it
                  empty, so a blip never re-renders the InferenceService without its adapter
                  mount and restarts a running predictor. Never cleared once set.
                type: string
              adapterSubtreeSyncKey:
                description: |-
                  AdapterSubtreeSyncKey records the declared adapter set most recently
                  reconciled onto the service's adapter subtree by the subtree-sync Job
                  (a hash of the sorted spec.adapters names). The controller re-runs the
                  sync Job — which creates the subtree and prunes adapter directories no
                  longer declared — whenever this drifts from the current desired set, so
                  editing spec.adapters reclaims removed adapters without re-run loops.
                type: string
              adapters:
                description: |-
                  Adapters reports the per-adapter disk-side status for services that declare
                  spec.adapters. One entry per declared adapter. Observation is
                  best-effort/eventual; the disk state the controller wrote is authoritative.
                items:
                  description: AIMServiceAdapterStatus is the per-adapter status aggregated
                    onto an AIMService.
                  properties:
                    adapterPath:
                      description: AdapterPath is the on-disk directory name (mirrored
                        from the artifact).
                      type: string
                    lastError:
                      description: |-
                        LastError carries the most recent error for this adapter (e.g. a mirrored
                        failing reason from the underlying artifact).
                      type: string
                    lastObserved:
                      description: LastObserved is when the controller last observed
                        this adapter's state.
                      format: date-time
                      type: string
                    loadedReplicas:
                      description: |-
                        LoadedReplicas reports how many serving replicas have the adapter loaded,
                        as "loaded/total" (e.g. "3/3"). RESERVED: engine-reported, not yet populated.
                      type: string
                    modelId:
                      description: ModelID is the adapter's canonical model id (mirrored
                        from the artifact).
                      type: string
                    name:
                      description: Name is the adapter reference name.
                      type: string
                    state:
                      description: State is the disk-side state of the adapter for
                        this service.
                      enum:
                      - Pending
                      - Downloading
                      - Downloaded
                      - Deleting
                      - Loaded
                      - LoadRejected
                      type: string
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              cache:
                description: Cache captures cache-related status for this service.
                properties:
                  profileCacheRef:
                    description: |-
                      ProfileCacheRef references the AIMProfileCache being used, if any.
                      Set by the v1alpha2 (profile-based) reconciliation path.
                    properties:
                      kind:
                        description: Kind is the fully-qualified kind of the resolved
                          reference, when known.
                        type: string
                      name:
                        description: Name is the resource name that satisfied the
                          reference.
                        type: string
                      namespace:
                        description: |-
                          Namespace identifies where the resource was found when namespace-scoped.
                          Empty indicates a cluster-scoped resource.
                        type: string
                      scope:
                        description: Scope indicates whether the resolved resource
                          was namespace or cluster scoped.
                        enum:
                        - Namespace
                        - Cluster
                        - Merged
                        - Unknown
                        type: string
                      uid:
                        description: UID captures the unique identifier of the resolved
                          reference, when known.
                        type: string
                    type: object
                  retryAttempts:
                    description: |-
                      RetryAttempts tracks how many times this service has attempted to retry a failed cache.
                      Each service gets exactly one retry attempt. When a cache enters Failed state,
                      this counter is incremented from 0 to 1 after deleting failed Artifacts.
                      If the retry fails (cache enters Failed again with attempts == 1), the service degrades.
                    type: integer
                  templateCacheRef:
                    description: |-
                      TemplateCacheRef references the AIMTemplateCache being used, if any.
                      Set by the v1alpha1 (template-based) reconciliation path.
                    properties:
                      kind:
                        description: Kind is the fully-qualified kind of the resolved
                          reference, when known.
                        type: string
                      name:
                        description: Name is the resource name that satisfied the
                          reference.
                        type: string
                      namespace:
                        description: |-
                          Namespace identifies where the resource was found when namespace-scoped.
                          Empty indicates a cluster-scoped resource.
                        type: string
                      scope:
                        description: Scope indicates whether the resolved resource
                          was namespace or cluster scoped.
                        enum:
                        - Namespace
                        - Cluster
                        - Merged
                        - Unknown
                        type: string
                      uid:
                        description: UID captures the unique identifier of the resolved
                          reference, when known.
                        type: string
                    type: object
                type: object
              conditions:
                description: Conditions represent the latest observations of template
                  state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              resolvedModel:
                description: ResolvedModel captures metadata about the image that
                  was resolved.
                properties:
                  kind:
                    description: Kind is the fully-qualified kind of the resolved
                      reference, when known.
                    type: string
                  name:
                    description: Name is the resource name that satisfied the reference.
                    type: string
                  namespace:
                    description: |-
                      Namespace identifies where the resource was found when namespace-scoped.
                      Empty indicates a cluster-scoped resource.
                    type: string
                  scope:
                    description: Scope indicates whether the resolved resource was
                      namespace or cluster scoped.
                    enum:
                    - Namespace
                    - Cluster
                    - Merged
                    - Unknown
                    type: string
                  uid:
                    description: UID captures the unique identifier of the resolved
                      reference, when known.
                    type: string
                type: object
              resolvedProfile:
                description: |-
                  ResolvedProfile captures metadata about the profile that satisfied the reference.
                  Set when the service uses a profile-based reconciliation path (v1alpha2).
                properties:
                  kind:
                    description: Kind is the fully-qualified kind of the resolved
                      reference, when known.
                    type: string
                  name:
                    description: Name is the resource name that satisfied the reference.
                    type: string
                  namespace:
                    description: |-
                      Namespace identifies where the resource was found when namespace-scoped.
                      Empty indicates a cluster-scoped resource.
                    type: string
                  scope:
                    description: Scope indicates whether the resolved resource was
                      namespace or cluster scoped.
                    enum:
                    - Namespace
                    - Cluster
                    - Merged
                    - Unknown
                    type: string
                  uid:
                    description: UID captures the unique identifier of the resolved
                      reference, when known.
                    type: string
                type: object
              resolvedRuntimeConfig:
                description: ResolvedRuntimeConfig captures metadata about the runtime
                  config that was resolved.
                properties:
                  kind:
                    description: Kind is the fully-qualified kind of the resolved
                      reference, when known.
                    type: string
                  name:
                    description: Name is the resource name that satisfied the reference.
                    type: string
                  namespace:
                    description: |-
                      Namespace identifies where the resource was found when namespace-scoped.
                      Empty indicates a cluster-scoped resource.
                    type: string
                  scope:
                    description: Scope indicates whether the resolved resource was
                      namespace or cluster scoped.
                    enum:
                    - Namespace
                    - Cluster
                    - Merged
                    - Unknown
                    type: string
                  uid:
                    description: UID captures the unique identifier of the resolved
                      reference, when known.
                    type: string
                type: object
              resolvedTemplate:
                description: ResolvedTemplate captures metadata about the template
                  that satisfied the reference.
                properties:
                  kind:
                    description: Kind is the fully-qualified kind of the resolved
                      reference, when known.
                    type: string
                  name:
                    description: Name is the resource name that satisfied the reference.
                    type: string
                  namespace:
                    description: |-
                      Namespace identifies where the resource was found when namespace-scoped.
                      Empty indicates a cluster-scoped resource.
                    type: string
                  scope:
                    description: Scope indicates whether the resolved resource was
                      namespace or cluster scoped.
                    enum:
                    - Namespace
                    - Cluster
                    - Merged
                    - Unknown
                    type: string
                  uid:
                    description: UID captures the unique identifier of the resolved
                      reference, when known.
                    type: string
                type: object
              routing:
                description: Routing surfaces information about the configured HTTP
                  routing, when enabled.
                properties:
                  path:
                    description: |-
                      Path is the HTTP path prefix used when routing is enabled.
                      Example: `/tenant/svc-uuid`
                    type: string
                type: object
              runtime:
                description: Runtime captures runtime status including replica counts.
                properties:
                  currentReplicas:
                    description: CurrentReplicas is the current number of replicas
                      as reported by the HPA.
                    format: int32
                    type: integer
                  desiredReplicas:
                    description: DesiredReplicas is the desired number of replicas
                      as determined by the HPA.
                    format: int32
                    type: integer
                  maxReplicas:
                    description: MaxReplicas is the maximum number of replicas configured
                      for autoscaling.
                    format: int32
                    type: integer
                  minReplicas:
                    description: MinReplicas is the minimum number of replicas configured
                      for autoscaling.
                    format: int32
                    type: integer
                  replicas:
                    description: |-
                      Replicas is a formatted display string for kubectl output.
                      Shows "current" for fixed replicas or "current/desired (min-max)" for autoscaling.
                    type: string
                required:
                - currentReplicas
                - desiredReplicas
                - maxReplicas
                - minReplicas
                type: object
              status:
                default: Pending
                description: |-
                  Status represents the current high‑level status of the service lifecycle.
                  Values: `Pending`, `Starting`, `Running`, `Degraded`, `Failed`.
                enum:
                - Pending
                - Starting
                - Running
                - Degraded
                - Failed
                type: string
            type: object
        type: object
        x-kubernetes-validations:
        - message: spec.profileOverrides requires spec.profile to be set
          rule: '!has(self.spec.profileOverrides) || has(self.spec.profile)'
        - message: spec.template is not supported on v1alpha2; use spec.profile or
            spec.model
          optionalOldSelf: true
          rule: '!has(self.spec.template) || (oldSelf.hasValue() && has(oldSelf.value().spec.template))'
        - message: spec.overrides is not supported on v1alpha2; use spec.profileOverrides
          optionalOldSelf: true
          rule: '!has(self.spec.overrides) || (oldSelf.hasValue() && has(oldSelf.value().spec.overrides))'
        - message: one of spec.model or spec.profile must be specified
          rule: has(self.spec.model) || has(self.spec.profile)
        - message: spec.profile.selector.role on v1alpha2 may only be omitted or set
            to deployable; base is reserved for AIMProfileSet selectors
          rule: '!has(self.spec.profile) || !has(self.spec.profile.selector) || !has(self.spec.profile.selector.role)
            || self.spec.profile.selector.role == ''deployable'''
        - message: spec.profile.selector must narrow on aimId, modelRef.name, or be
            paired with spec.model.name
          rule: '!has(self.spec.profile) || !has(self.spec.profile.selector) || has(self.spec.profile.selector.aimId)
            || has(self.spec.profile.selector.modelRef) || has(self.spec.model)'
        - message: acceleratorCount must be specified together with any acceleratorPartitioningMode
            override; partition mode changes the per-unit interpretation of acceleratorCount
          rule: '!has(self.spec.profileOverrides) || !has(self.spec.profileOverrides.acceleratorPartitioningMode)
            || size(self.spec.profileOverrides.acceleratorPartitioningMode) == 0 ||
            has(self.spec.profileOverrides.acceleratorCount)'
        - message: spec.adapters requires spec.profile
          rule: '!has(self.spec.adapters) || size(self.spec.adapters) == 0 || has(self.spec.profile)'
        - message: spec.adapters entries must have unique (kind, name) pairs
          rule: '!has(self.spec.adapters) || self.spec.adapters.all(a, self.spec.adapters.exists_one(b,
            b.kind == a.kind && b.name == a.name))'
        - message: spec.adapters is immutable unless spec.adapterMode is dynamic
          rule: self.spec.adapterMode == 'dynamic' || (has(self.spec.adapters) ==
            has(oldSelf.spec.adapters) && (!has(self.spec.adapters) || self.spec.adapters
            == oldSelf.spec.adapters))
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: aimservicetemplates.aim.eai.amd.com
spec:
  group: aim.eai.amd.com
  names:
    categories:
    - aim
    - all
    kind: AIMServiceTemplate
    listKind: AIMServiceTemplateList
    plural: aimservicetemplates
    shortNames:
    - aimtpl
    singular: aimservicetemplate
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.modelName
      name: Model
      type: string
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.hardwareSummary
      name: Hardware
      type: string
    - jsonPath: .status.profile.metadata.metric
      name: Metric
      type: string
    - jsonPath: .status.profile.metadata.precision
      name: Precision
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AIMServiceTemplate is the Schema for namespace-scoped AIM service
          templates.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              AIMServiceTemplateSpec defines the desired state of AIMServiceTemplate (namespace-scoped).

              A namespaced and versioned template that selects a runtime profile
              for a given AIM model (by canonical name). Templates are intentionally
              narrow: they describe runtime selection knobs for the AIM container and do
              not redefine the full Kubernetes deployment shape.
            properties:
              aimId:
                description: |-
                  AimId is the AIM product family identifier (e.g., "meta-llama/Llama-3-8B").
                  Required when customProfile is set; used to assemble the profile YAML aim_id field
                  and to compute the custom profile ID for AIM_PROFILE_ID.
                type: string
              caching:
                description: |-
                  Caching configures model caching behavior for this namespace-scoped template.
                  When enabled, models will be cached using the specified environment variables
                  during download.
                properties:
                  enabled:
                    default: false
                    description: |-
                      Enabled controls whether caching is enabled for this template.
                      Defaults to `false`.
                    type: boolean
                  env:
                    description: |-
                      Env specifies environment variables to use when downloading the model for caching.
                      These variables are available to the model download process and can be used
                      to configure download behavior, authentication, proxies, etc.
                      If not set, falls back to the template's top-level Env field.
                    items:
                      description: EnvVar represents an environment variable present
                        in a Container.
                      properties:
                        name:
                          description: |-
                            Name of the environment variable.
                            May consist of any printable ASCII characters except '='.
                          type: string
                        value:
                          description: |-
                            Variable references $(VAR_NAME) are expanded
                            using the previously defined environment variables in the container and
                            any service environment variables. If a variable cannot be resolved,
                            the reference in the input string will be unchanged. Double $$ are reduced
                            to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                            "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                            Escaped references will never be expanded, regardless of whether the variable
                            exists or not.
                            Defaults to "".
                          type: string
                        valueFrom:
                          description: Source for the environment variable's value.
                            Cannot be used if value is not empty.
                          properties:
                            configMapKeyRef:
                              description: Selects a key of a ConfigMap.
                              properties:
                                key:
                                  description: The key to select.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the ConfigMap or its
                                    key must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                            fieldRef:
                              description: |-
                                Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                              properties:
                                apiVersion:
                                  description: Version of the schema the FieldPath
                                    is written in terms of, defaults to "v1".
                                  type: string
                                fieldPath:
                                  description: Path of the field to select in the
                                    specified API version.
                                  type: string
                              required:
                              - fieldPath
                              type: object
                              x-kubernetes-map-type: atomic
                            fileKeyRef:
                              description: |-
                                FileKeyRef selects a key of the env file.
                                Requires the EnvFiles feature gate to be enabled.
                              properties:
                                key:
                                  description: |-
                                    The key within the env file. An invalid key will prevent the pod from starting.
                                    The keys defined within a source may consist of any printable ASCII characters except '='.
                                    During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                  type: string
                                optional:
                                  default: false
                                  description: |-
                                    Specify whether the file or its key must be defined. If the file or key
                                    does not exist, then the env var is not published.
                                    If optional is set to true and the specified key does not exist,
                                    the environment variable will not be set in the Pod's containers.

                                    If optional is set to false and the specified key does not exist,
                                    an error will be returned during Pod creation.
                                  type: boolean
                                path:
                                  description: |-
                                    The path within the volume from which to select the file.
                                    Must be relative and may not contain the '..' path or start with '..'.
                                  type: string
                                volumeName:
                                  description: The name of the volume mount containing
                                    the env file.
                                  type: string
                              required:
                              - key
                              - path
                              - volumeName
                              type: object
                              x-kubernetes-map-type: atomic
                            resourceFieldRef:
                              description: |-
                                Selects a resource of the container: only resources limits and requests
                                (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                              properties:
                                containerName:
                                  description: 'Container name: required for volumes,
                                    optional for env vars'
                                  type: string
                                divisor:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: Specifies the output format of the
                                    exposed resources, defaults to "1"
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                resource:
                                  description: 'Required: resource to select'
                                  type: string
                              required:
                              - resource
                              type: object
                              x-kubernetes-map-type: atomic
                            secretKeyRef:
                              description: Selects a key of a secret in the pod's
                                namespace
                              properties:
                                key:
                                  description: The key of the secret to select from.  Must
                                    be a valid secret key.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the Secret or its key
                                    must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                type: object
              customProfile:
                description: |-
                  CustomProfile defines inline custom profile data for the inference engine.
                  When set, the controller assembles a profile YAML from this data and template metadata,
                  creates a ConfigMap, and mounts it into discovery and inference containers.
                  Requires aimId, modelId, hardware, metric, and precision to also be set.
                properties:
                  engineArgs:
                    description: |-
                      EngineArgs contains inference engine arguments as a free-form JSON object.
                      These are passed as CLI arguments to the inference engine (e.g., vLLM).
                      Do not include "model" — it is injected separately by the runtime.
                    x-kubernetes-preserve-unknown-fields: true
                  envVars:
                    additionalProperties:
                      type: string
                    description: |-
                      EnvVars contains environment variables applied to the inference engine process.
                      These are written into the profile YAML and applied by the AIM runtime via os.execv,
                      distinct from container-level Env which targets the AIM runtime container itself.
                      Keys must match ^[A-Z0-9_]+$ (uppercase with underscores).
                    type: object
                type: object
              env:
                description: |-
                  Env specifies environment variables for inference containers.
                  These variables are passed to the inference runtime and can be used
                  to configure runtime behavior, authentication, or other settings.
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: |-
                        Name of the environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        fileKeyRef:
                          description: |-
                            FileKeyRef selects a key of the env file.
                            Requires the EnvFiles feature gate to be enabled.
                          properties:
                            key:
                              description: |-
                                The key within the env file. An invalid key will prevent the pod from starting.
                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                              type: string
                            optional:
                              default: false
                              description: |-
                                Specify whether the file or its key must be defined. If the file or key
                                does not exist, then the env var is not published.
                                If optional is set to true and the specified key does not exist,
                                the environment variable will not be set in the Pod's containers.

                                If optional is set to false and the specified key does not exist,
                                an error will be returned during Pod creation.
                              type: boolean
                            path:
                              description: |-
                                The path within the volume from which to select the file.
                                Must be relative and may not contain the '..' path or start with '..'.
                              type: string
                            volumeName:
                              description: The name of the volume mount containing
                                the env file.
                              type: string
                          required:
                          - key
                          - path
                          - volumeName
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              hardware:
                description: |-
                  Hardware specifies GPU and CPU requirements for each replica.
                  For GPU models, defines the GPU count and model types required for deployment.
                  For CPU-only models, defines CPU resource requirements.
                  This field is immutable after creation.
                properties:
                  cpu:
                    description: CPU specifies CPU requirements.
                    properties:
                      limits:
                        anyOf:
                        - type: integer
                        - type: string
                        description: Limits is the maximum number of CPU cores to
                          allow.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      requests:
                        anyOf:
                        - type: integer
                        - type: string
                        description: Requests is the number of CPU cores to request.
                          Required and must be > 0.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    required:
                    - requests
                    type: object
                  gpu:
                    description: GPU specifies GPU requirements. If not set, no GPUs
                      are requested (CPU-only model).
                    properties:
                      minVram:
                        anyOf:
                        - type: integer
                        - type: string
                        description: |-
                          MinVRAM limits deployment to GPUs having at least this much VRAM.
                          Used for capacity planning when the model size is known but any GPU with
                          sufficient VRAM is acceptable.
                          Cannot be combined with model.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      model:
                        description: |-
                          Model limits deployment to a specific GPU model.
                          Example: "MI300X"
                          Cannot be combined with minVram.
                        maxLength: 64
                        type: string
                      requests:
                        description: |-
                          Requests is the number of GPUs to set as requests/limits.
                          Set to 0 to target GPU nodes without consuming GPU resources (useful for testing).
                        format: int32
                        minimum: 0
                        type: integer
                      resourceName:
                        default: amd.com/gpu
                        description: |-
                          ResourceName is the Kubernetes resource name for GPU resources.
                          Defaults to "amd.com/gpu" if not specified.
                        type: string
                    type: object
                    x-kubernetes-validations:
                    - message: model and minVram are mutually exclusive; specify either
                        a specific GPU model OR a minimum VRAM requirement, not both
                      rule: '!(has(self.model) && size(self.model) > 0 && has(self.minVram))'
                type: object
                x-kubernetes-validations:
                - message: hardware is immutable
                  rule: self == oldSelf
                - message: at least one of gpu or cpu must be specified
                  rule: has(self.gpu) || has(self.cpu)
              imagePullSecrets:
                description: |-
                  ImagePullSecrets lists secrets containing credentials for pulling container images.
                  These secrets are used for:
                  - Discovery dry-run jobs that inspect the model container
                  - Pulling the image for inference services
                  The secrets are merged with any model or runtime config defaults.
                  For namespace-scoped templates, secrets must exist in the same namespace.
                  For cluster-scoped templates, secrets must exist in the operator namespace.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              metric:
                allOf:
                - enum:
                  - latency
                  - throughput
                - enum:
                  - latency
                  - throughput
                description: |-
                  Metric selects the optimization goal.

                  - `latency`: prioritize low end‑to‑end latency
                  - `throughput`: prioritize sustained requests/second
                type: string
                x-kubernetes-validations:
                - message: metric is immutable
                  rule: self == oldSelf
              modelId:
                description: |-
                  ModelId is the specific model identifier / HuggingFace URI (e.g., "Qwen/Qwen3-32B-FP8").
                  Required when customProfile is set; used for profile YAML model_id field
                  and for weight pre-caching via the discovery job.
                type: string
              modelName:
                description: |-
                  ModelName is the model name. Matches `metadata.name` of an AIMModel or AIMClusterModel. Immutable.

                  Example: `meta/llama-3-8b:1.1+20240915`
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: model name is immutable
                  rule: self == oldSelf
              modelSources:
                description: |-
                  ModelSources specifies the model sources required to run this template.
                  When provided, the discovery dry-run will be skipped and these sources will be used directly.
                  This allows users to explicitly declare model dependencies without requiring a discovery job.
                  If omitted, a discovery job will be run to automatically determine the required model sources.
                items:
                  description: |-
                    AIMModelSource describes a model artifact that must be downloaded for inference.
                    Discovery extracts these from the container's configuration to enable caching and validation.
                  properties:
                    env:
                      description: |-
                        Env specifies per-source credential overrides.
                        These variables are used for authentication when downloading this specific source.
                        Takes precedence over base-level env for the same variable name.
                      items:
                        description: EnvVar represents an environment variable present
                          in a Container.
                        properties:
                          name:
                            description: |-
                              Name of the environment variable.
                              May consist of any printable ASCII characters except '='.
                            type: string
                          value:
                            description: |-
                              Variable references $(VAR_NAME) are expanded
                              using the previously defined environment variables in the container and
                              any service environment variables. If a variable cannot be resolved,
                              the reference in the input string will be unchanged. Double $$ are reduced
                              to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                              "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                              Escaped references will never be expanded, regardless of whether the variable
                              exists or not.
                              Defaults to "".
                            type: string
                          valueFrom:
                            description: Source for the environment variable's value.
                              Cannot be used if value is not empty.
                            properties:
                              configMapKeyRef:
                                description: Selects a key of a ConfigMap.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the ConfigMap or
                                      its key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              fieldRef:
                                description: |-
                                  Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                  spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                properties:
                                  apiVersion:
                                    description: Version of the schema the FieldPath
                                      is written in terms of, defaults to "v1".
                                    type: string
                                  fieldPath:
                                    description: Path of the field to select in the
                                      specified API version.
                                    type: string
                                required:
                                - fieldPath
                                type: object
                                x-kubernetes-map-type: atomic
                              fileKeyRef:
                                description: |-
                                  FileKeyRef selects a key of the env file.
                                  Requires the EnvFiles feature gate to be enabled.
                                properties:
                                  key:
                                    description: |-
                                      The key within the env file. An invalid key will prevent the pod from starting.
                                      The keys defined within a source may consist of any printable ASCII characters except '='.
                                      During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                    type: string
                                  optional:
                                    default: false
                                    description: |-
                                      Specify whether the file or its key must be defined. If the file or key
                                      does not exist, then the env var is not published.
                                      If optional is set to true and the specified key does not exist,
                                      the environment variable will not be set in the Pod's containers.

                                      If optional is set to false and the specified key does not exist,
                                      an error will be returned during Pod creation.
                                    type: boolean
                                  path:
                                    description: |-
                                      The path within the volume from which to select the file.
                                      Must be relative and may not contain the '..' path or start with '..'.
                                    type: string
                                  volumeName:
                                    description: The name of the volume mount containing
                                      the env file.
                                    type: string
                                required:
                                - key
                                - path
                                - volumeName
                                type: object
                                x-kubernetes-map-type: atomic
                              resourceFieldRef:
                                description: |-
                                  Selects a resource of the container: only resources limits and requests
                                  (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                properties:
                                  containerName:
                                    description: 'Container name: required for volumes,
                                      optional for env vars'
                                    type: string
                                  divisor:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: Specifies the output format of the
                                      exposed resources, defaults to "1"
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  resource:
                                    description: 'Required: resource to select'
                                    type: string
                                required:
                                - resource
                                type: object
                                x-kubernetes-map-type: atomic
                              secretKeyRef:
                                description: Selects a key of a secret in the pod's
                                  namespace
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                        required:
                        - name
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - name
                      x-kubernetes-list-type: map
                    modelId:
                      description: |-
                        ModelID is the canonical identifier in {org}/{name} format.
                        Determines the cache mount path: /workspace/cache/{modelId}
                        For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                        For S3 sources, users define their own organizational structure.
                      pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                      type: string
                    precision:
                      allOf:
                      - enum:
                        - auto
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                      - enum:
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                      description: |-
                        Precision describes the runtime precision this source is compatible with.
                        Used to match model sources to profiles during custom weight onboarding.
                      type: string
                    size:
                      anyOf:
                      - type: integer
                      - type: string
                      description: |-
                        Size is the expected storage space required for this model artifact.
                        Used for PVC sizing and capacity planning during cache creation.
                        Optional - if not specified, the download job will discover the size automatically.
                        Can be set explicitly to pre-allocate storage or override auto-discovery.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    sourceUri:
                      description: |-
                        SourceURI is the location from which the model should be downloaded.
                        Supported schemes:
                        - hf://org/model - Hugging Face Hub model
                        - s3://bucket/key - S3-compatible storage
                      pattern: ^(hf|s3)://[^ \t\r\n]+$
                      type: string
                  required:
                  - modelId
                  - sourceUri
                  type: object
                type: array
              precision:
                allOf:
                - enum:
                  - auto
                  - fp4
                  - fp8
                  - fp16
                  - fp32
                  - bf16
                  - int4
                  - int8
                - enum:
                  - auto
                  - fp4
                  - fp8
                  - fp16
                  - fp32
                  - bf16
                  - int4
                  - int8
                description: Precision selects the numeric precision used by the runtime.
                type: string
                x-kubernetes-validations:
                - message: precision is immutable
                  rule: self == oldSelf
              profileId:
                description: |-
                  ProfileId is the specific AIM profile ID that this template should use.
                  When set, the discovery job will be instructed to use this specific profile.
                type: string
              resources:
                description: |-
                  Resources defines the default container resource requirements applied to services derived from this template.
                  Service-specific values override the template defaults.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              runtimeConfigName:
                description: |-
                  Name is the name of the runtime config to use for this resource. If a runtime config with this name exists both
                  as a namespace and a cluster runtime config, the values are merged together, the namespace config taking priority
                  over the cluster config when there are conflicts. If this field is empty or set to `default`, the namespace / cluster
                  runtime config with the name `default` is used, if it exists.
                type: string
              serviceAccountName:
                description: |-
                  ServiceAccountName specifies the Kubernetes service account to use for workloads related to this template.
                  This includes discovery dry-run jobs and inference services created from this template.
                  If empty, the default service account for the namespace is used.
                type: string
              type:
                allOf:
                - enum:
                  - optimized
                  - general
                  - preview
                  - unoptimized
                - enum:
                  - optimized
                  - general
                  - preview
                  - unoptimized
                description: |-
                  Type indicates the optimization level of this template.
                  - optimized: Template has been tuned for performance
                  - preview: Template is experimental/pre-release
                  - general: General-purpose tuning between optimized and preview
                  - unoptimized: Default, no specific optimizations applied
                  When nil, the type is determined by discovery. When set, overrides discovery.
                type: string
            required:
            - modelName
            type: object
            x-kubernetes-validations:
            - message: when customProfile is set, aimId, modelId, hardware, metric,
                and precision are required
              rule: '!has(self.customProfile) || (has(self.aimId) && has(self.modelId)
                && has(self.hardware) && has(self.metric) && has(self.precision))'
          status:
            description: AIMServiceTemplateStatus defines the observed state of AIMServiceTemplate.
            properties:
              conditions:
                description: Conditions represent the latest observations of template
                  state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              discovery:
                description: |-
                  Discovery contains state tracking for the discovery process, including
                  retry attempts and backoff timing for the circuit breaker pattern.
                properties:
                  attempts:
                    description: |-
                      Attempts is the number of discovery job attempts that have been made.
                      This counter increments each time a new discovery job is created after a failure.
                    format: int32
                    type: integer
                  identityCheckHash:
                    description: |-
                      IdentityCheckHash records the hash from the most recent identity
                      rediscovery attempt for a Ready template. When the current hash matches
                      this value, the controller will not invalidate a Ready template just to
                      retry identity (aimId/modelId) extraction. Bumping the operator-internal
                      hash version forces a one-shot revisit across all eligible templates
                      without operator intervention.
                    type: string
                  lastAttemptTime:
                    description: |-
                      LastAttemptTime is the timestamp of the most recent discovery job creation.
                      Used to calculate exponential backoff before the next retry.
                    format: date-time
                    type: string
                  lastFailureReason:
                    description: |-
                      LastFailureReason captures the reason for the most recent discovery failure.
                      Used to classify failures as terminal vs transient.
                    type: string
                  lastIdentityCheckTime:
                    description: |-
                      LastIdentityCheckTime is the timestamp of the most recent identity
                      rediscovery attempt initiation. Acts as a hard floor against rediscovery
                      thrashing in the presence of bugs that prevent IdentityCheckHash from
                      being recorded.
                    format: date-time
                    type: string
                  specHash:
                    description: |-
                      SpecHash is a hash of the template spec fields that affect discovery.
                      When the spec changes, the circuit breaker resets to allow fresh attempts.
                    type: string
                type: object
              discoveryJob:
                description: DiscoveryJob is a reference to the job that was run for
                  discovery
                properties:
                  kind:
                    description: Kind is the fully-qualified kind of the resolved
                      reference, when known.
                    type: string
                  name:
                    description: Name is the resource name that satisfied the reference.
                    type: string
                  namespace:
                    description: |-
                      Namespace identifies where the resource was found when namespace-scoped.
                      Empty indicates a cluster-scoped resource.
                    type: string
                  scope:
                    description: Scope indicates whether the resolved resource was
                      namespace or cluster scoped.
                    enum:
                    - Namespace
                    - Cluster
                    - Merged
                    - Unknown
                    type: string
                  uid:
                    description: UID captures the unique identifier of the resolved
                      reference, when known.
                    type: string
                type: object
              hardwareSummary:
                description: |-
                  HardwareSummary is a human-readable display string for the hardware requirements.
                  Format: "{count} x {model}" for GPU (e.g., "2 x MI300X") or "CPU" for CPU-only.
                  This is a computed field for display purposes only.
                type: string
              modelSources:
                description: |-
                  ModelSources list the models that this template requires to run. These are the models that will be
                  cached, if this template is cached.
                items:
                  description: |-
                    AIMModelSource describes a model artifact that must be downloaded for inference.
                    Discovery extracts these from the container's configuration to enable caching and validation.
                  properties:
                    env:
                      description: |-
                        Env specifies per-source credential overrides.
                        These variables are used for authentication when downloading this specific source.
                        Takes precedence over base-level env for the same variable name.
                      items:
                        description: EnvVar represents an environment variable present
                          in a Container.
                        properties:
                          name:
                            description: |-
                              Name of the environment variable.
                              May consist of any printable ASCII characters except '='.
                            type: string
                          value:
                            description: |-
                              Variable references $(VAR_NAME) are expanded
                              using the previously defined environment variables in the container and
                              any service environment variables. If a variable cannot be resolved,
                              the reference in the input string will be unchanged. Double $$ are reduced
                              to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                              "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                              Escaped references will never be expanded, regardless of whether the variable
                              exists or not.
                              Defaults to "".
                            type: string
                          valueFrom:
                            description: Source for the environment variable's value.
                              Cannot be used if value is not empty.
                            properties:
                              configMapKeyRef:
                                description: Selects a key of a ConfigMap.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the ConfigMap or
                                      its key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              fieldRef:
                                description: |-
                                  Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                  spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                properties:
                                  apiVersion:
                                    description: Version of the schema the FieldPath
                                      is written in terms of, defaults to "v1".
                                    type: string
                                  fieldPath:
                                    description: Path of the field to select in the
                                      specified API version.
                                    type: string
                                required:
                                - fieldPath
                                type: object
                                x-kubernetes-map-type: atomic
                              fileKeyRef:
                                description: |-
                                  FileKeyRef selects a key of the env file.
                                  Requires the EnvFiles feature gate to be enabled.
                                properties:
                                  key:
                                    description: |-
                                      The key within the env file. An invalid key will prevent the pod from starting.
                                      The keys defined within a source may consist of any printable ASCII characters except '='.
                                      During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                    type: string
                                  optional:
                                    default: false
                                    description: |-
                                      Specify whether the file or its key must be defined. If the file or key
                                      does not exist, then the env var is not published.
                                      If optional is set to true and the specified key does not exist,
                                      the environment variable will not be set in the Pod's containers.

                                      If optional is set to false and the specified key does not exist,
                                      an error will be returned during Pod creation.
                                    type: boolean
                                  path:
                                    description: |-
                                      The path within the volume from which to select the file.
                                      Must be relative and may not contain the '..' path or start with '..'.
                                    type: string
                                  volumeName:
                                    description: The name of the volume mount containing
                                      the env file.
                                    type: string
                                required:
                                - key
                                - path
                                - volumeName
                                type: object
                                x-kubernetes-map-type: atomic
                              resourceFieldRef:
                                description: |-
                                  Selects a resource of the container: only resources limits and requests
                                  (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                properties:
                                  containerName:
                                    description: 'Container name: required for volumes,
                                      optional for env vars'
                                    type: string
                                  divisor:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: Specifies the output format of the
                                      exposed resources, defaults to "1"
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  resource:
                                    description: 'Required: resource to select'
                                    type: string
                                required:
                                - resource
                                type: object
                                x-kubernetes-map-type: atomic
                              secretKeyRef:
                                description: Selects a key of a secret in the pod's
                                  namespace
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                        required:
                        - name
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - name
                      x-kubernetes-list-type: map
                    modelId:
                      description: |-
                        ModelID is the canonical identifier in {org}/{name} format.
                        Determines the cache mount path: /workspace/cache/{modelId}
                        For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                        For S3 sources, users define their own organizational structure.
                      pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                      type: string
                    precision:
                      allOf:
                      - enum:
                        - auto
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                      - enum:
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                      description: |-
                        Precision describes the runtime precision this source is compatible with.
                        Used to match model sources to profiles during custom weight onboarding.
                      type: string
                    size:
                      anyOf:
                      - type: integer
                      - type: string
                      description: |-
                        Size is the expected storage space required for this model artifact.
                        Used for PVC sizing and capacity planning during cache creation.
                        Optional - if not specified, the download job will discover the size automatically.
                        Can be set explicitly to pre-allocate storage or override auto-discovery.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    sourceUri:
                      description: |-
                        SourceURI is the location from which the model should be downloaded.
                        Supported schemes:
                        - hf://org/model - Hugging Face Hub model
                        - s3://bucket/key - S3-compatible storage
                      pattern: ^(hf|s3)://[^ \t\r\n]+$
                      type: string
                  required:
                  - modelId
                  - sourceUri
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              profile:
                description: |-
                  Profile contains the full discovery result profile as a free-form JSON object.
                  This includes metadata, engine args, environment variables, and model details.
                properties:
                  engine_args:
                    description: |-
                      EngineArgs contains runtime-specific engine configuration as a free-form JSON object.
                      The structure depends on the inference engine being used (e.g., vLLM, TGI).
                      These arguments are passed to the runtime container to configure model loading and inference.
                    x-kubernetes-preserve-unknown-fields: true
                  env_vars:
                    additionalProperties:
                      type: string
                    description: |-
                      EnvVars contains environment variables required by the runtime for this profile.
                      These may include engine-specific settings, optimization flags, or hardware configuration.
                    type: object
                  metadata:
                    description: Metadata provides structured information about this
                      deployment profile's characteristics.
                    properties:
                      aimId:
                        description: |-
                          AimID is the model-family identifier from the profile YAML's top-level `aim_id`.
                          Used for cross-scope fine-tune template matching (AIMModel.spec.aimId -> template.spec.aimId).
                        type: string
                      engine:
                        description: Engine identifies the inference engine used for
                          this profile (e.g., "vllm", "tgi").
                        type: string
                      gpu:
                        description: GPU specifies the GPU model this profile is optimized
                          for (e.g., "MI300X", "MI325X").
                        type: string
                      gpuCount:
                        description: GPUCount indicates how many GPUs are required
                          per replica for this profile.
                        format: int32
                        type: integer
                      metric:
                        description: Metric indicates the optimization goal for this
                          profile ("latency" or "throughput").
                        enum:
                        - latency
                        - throughput
                        type: string
                      modelId:
                        description: ModelID is the per-profile model variant identifier
                          from the profile YAML's top-level `model_id`.
                        type: string
                      precision:
                        description: Precision specifies the numeric precision used
                          in this profile (e.g., "fp16", "fp8").
                        enum:
                        - auto
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                        type: string
                      type:
                        description: Type indicates the optimization level of this
                          profile (optimized, preview, unoptimized).
                        enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                        type: string
                    type: object
                  originalDiscoveryOutput:
                    description: |-
                      OriginalDiscoveryOutput contains the raw discovery job JSON output.
                      This preserves the complete discovery result from the dry-run container,
                      including all fields that may not be mapped to structured fields above.
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              resolvedCache:
                description: ResolvedCache captures metadata about which cache is
                  used for this template
                properties:
                  kind:
                    description: Kind is the fully-qualified kind of the resolved
                      reference, when known.
                    type: string
                  name:
                    description: Name is the resource name that satisfied the reference.
                    type: string
                  namespace:
                    description: |-
                      Namespace identifies where the resource was found when namespace-scoped.
                      Empty indicates a cluster-scoped resource.
                    type: string
                  scope:
                    description: Scope indicates whether the resolved resource was
                      namespace or cluster scoped.
                    enum:
                    - Namespace
                    - Cluster
                    - Merged
                    - Unknown
                    type: string
                  uid:
                    description: UID captures the unique identifier of the resolved
                      reference, when known.
                    type: string
                type: object
              resolvedHardware:
                description: |-
                  ResolvedHardware contains the resolved hardware requirements for this template.
                  These values are computed from discovery results and spec defaults, and represent
                  what will actually be used when creating InferenceServices.
                  Resolution order: discovery output > spec values > defaults.
                properties:
                  cpu:
                    description: CPU specifies CPU requirements.
                    properties:
                      limits:
                        anyOf:
                        - type: integer
                        - type: string
                        description: Limits is the maximum number of CPU cores to
                          allow.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      requests:
                        anyOf:
                        - type: integer
                        - type: string
                        description: Requests is the number of CPU cores to request.
                          Required and must be > 0.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    required:
                    - requests
                    type: object
                  gpu:
                    description: GPU specifies GPU requirements. If not set, no GPUs
                      are requested (CPU-only model).
                    properties:
                      minVram:
                        anyOf:
                        - type: integer
                        - type: string
                        description: |-
                          MinVRAM limits deployment to GPUs having at least this much VRAM.
                          Used for capacity planning when the model size is known but any GPU with
                          sufficient VRAM is acceptable.
                          Cannot be combined with model.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      model:
                        description: |-
                          Model limits deployment to a specific GPU model.
                          Example: "MI300X"
                          Cannot be combined with minVram.
                        maxLength: 64
                        type: string
                      requests:
                        description: |-
                          Requests is the number of GPUs to set as requests/limits.
                          Set to 0 to target GPU nodes without consuming GPU resources (useful for testing).
                        format: int32
                        minimum: 0
                        type: integer
                      resourceName:
                        default: amd.com/gpu
                        description: |-
                          ResourceName is the Kubernetes resource name for GPU resources.
                          Defaults to "amd.com/gpu" if not specified.
                        type: string
                    type: object
                    x-kubernetes-validations:
                    - message: model and minVram are mutually exclusive; specify either
                        a specific GPU model OR a minimum VRAM requirement, not both
                      rule: '!(has(self.model) && size(self.model) > 0 && has(self.minVram))'
                type: object
                x-kubernetes-validations:
                - message: at least one of gpu or cpu must be specified
                  rule: has(self.gpu) || has(self.cpu)
              resolvedModel:
                description: ResolvedModel captures metadata about the image that
                  was resolved.
                properties:
                  kind:
                    description: Kind is the fully-qualified kind of the resolved
                      reference, when known.
                    type: string
                  name:
                    description: Name is the resource name that satisfied the reference.
                    type: string
                  namespace:
                    description: |-
                      Namespace identifies where the resource was found when namespace-scoped.
                      Empty indicates a cluster-scoped resource.
                    type: string
                  scope:
                    description: Scope indicates whether the resolved resource was
                      namespace or cluster scoped.
                    enum:
                    - Namespace
                    - Cluster
                    - Merged
                    - Unknown
                    type: string
                  uid:
                    description: UID captures the unique identifier of the resolved
                      reference, when known.
                    type: string
                type: object
              resolvedNodeAffinity:
                description: |-
                  ResolvedNodeAffinity contains the computed node affinity rules for GPU scheduling.
                  This is derived from GPU model and minVRAM requirements, merged with any user-specified
                  affinity from the spec. The service controller uses this directly when creating InferenceServices.
                properties:
                  preferredDuringSchedulingIgnoredDuringExecution:
                    description: |-
                      The scheduler will prefer to schedule pods to nodes that satisfy
                      the affinity expressions specified by this field, but it may choose
                      a node that violates one or more of the expressions. The node that is
                      most preferred is the one with the greatest sum of weights, i.e.
                      for each node that meets all of the scheduling requirements (resource
                      request, requiredDuringScheduling affinity expressions, etc.),
                      compute a sum by iterating through the elements of this field and adding
                      "weight" to the sum if the node matches the corresponding matchExpressions; the
                      node(s) with the highest sum are the most preferred.
                    items:
                      description: |-
                        An empty preferred scheduling term matches all objects with implicit weight 0
                        (i.e. it's a no-op). A null preferred scheduling term matches no objects (i.e. is also a no-op).
                      properties:
                        preference:
                          description: A node selector term, associated with the corresponding
                            weight.
                          properties:
                            matchExpressions:
                              description: A list of node selector requirements by
                                node's labels.
                              items:
                                description: |-
                                  A node selector requirement is a selector that contains values, a key, and an operator
                                  that relates the key and values.
                                properties:
                                  key:
                                    description: The label key that the selector applies
                                      to.
                                    type: string
                                  operator:
                                    description: |-
                                      Represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                    type: string
                                  values:
                                    description: |-
                                      An array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. If the operator is Gt or Lt, the values
                                      array must have a single element, which will be interpreted as an integer.
                                      This array is replaced during a strategic merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchFields:
                              description: A list of node selector requirements by
                                node's fields.
                              items:
                                description: |-
                                  A node selector requirement is a selector that contains values, a key, and an operator
                                  that relates the key and values.
                                properties:
                                  key:
                                    description: The label key that the selector applies
                                      to.
                                    type: string
                                  operator:
                                    description: |-
                                      Represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                    type: string
                                  values:
                                    description: |-
                                      An array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. If the operator is Gt or Lt, the values
                                      array must have a single element, which will be interpreted as an integer.
                                      This array is replaced during a strategic merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                          type: object
                          x-kubernetes-map-type: atomic
                        weight:
                          description: Weight associated with matching the corresponding
                            nodeSelectorTerm, in the range 1-100.
                          format: int32
                          type: integer
                      required:
                      - preference
                      - weight
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  requiredDuringSchedulingIgnoredDuringExecution:
                    description: |-
                      If the affinity requirements specified by this field are not met at
                      scheduling time, the pod will not be scheduled onto the node.
                      If the affinity requirements specified by this field cease to be met
                      at some point during pod execution (e.g. due to an update), the system
                      may or may not try to eventually evict the pod from its node.
                    properties:
                      nodeSelectorTerms:
                        description: Required. A list of node selector terms. The
                          terms are ORed.
                        items:
                          description: |-
                            A null or empty node selector term matches no objects. The requirements of
                            them are ANDed.
                            The TopologySelectorTerm type implements a subset of the NodeSelectorTerm.
                          properties:
                            matchExpressions:
                              description: A list of node selector requirements by
                                node's labels.
                              items:
                                description: |-
                                  A node selector requirement is a selector that contains values, a key, and an operator
                                  that relates the key and values.
                                properties:
                                  key:
                                    description: The label key that the selector applies
                                      to.
                                    type: string
                                  operator:
                                    description: |-
                                      Represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                    type: string
                                  values:
                                    description: |-
                                      An array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. If the operator is Gt or Lt, the values
                                      array must have a single element, which will be interpreted as an integer.
                                      This array is replaced during a strategic merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchFields:
                              description: A list of node selector requirements by
                                node's fields.
                              items:
                                description: |-
                                  A node selector requirement is a selector that contains values, a key, and an operator
                                  that relates the key and values.
                                properties:
                                  key:
                                    description: The label key that the selector applies
                                      to.
                                    type: string
                                  operator:
                                    description: |-
                                      Represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                    type: string
                                  values:
                                    description: |-
                                      An array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. If the operator is Gt or Lt, the values
                                      array must have a single element, which will be interpreted as an integer.
                                      This array is replaced during a strategic merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                          type: object
                          x-kubernetes-map-type: atomic
                        type: array
                        x-kubernetes-list-type: atomic
                    required:
                    - nodeSelectorTerms
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
              resolvedRuntimeConfig:
                description: ResolvedRuntimeConfig captures metadata about the runtime
                  config that was resolved.
                properties:
                  kind:
                    description: Kind is the fully-qualified kind of the resolved
                      reference, when known.
                    type: string
                  name:
                    description: Name is the resource name that satisfied the reference.
                    type: string
                  namespace:
                    description: |-
                      Namespace identifies where the resource was found when namespace-scoped.
                      Empty indicates a cluster-scoped resource.
                    type: string
                  scope:
                    description: Scope indicates whether the resolved resource was
                      namespace or cluster scoped.
                    enum:
                    - Namespace
                    - Cluster
                    - Merged
                    - Unknown
                    type: string
                  uid:
                    description: UID captures the unique identifier of the resolved
                      reference, when known.
                    type: string
                type: object
              status:
                default: Pending
                description: |-
                  Status represents the current high‑level status of the template lifecycle.
                  Values: `Pending`, `Progressing`, `Ready`, `Degraded`, `Failed`.
                enum:
                - Pending
                - Progressing
                - Ready
                - Degraded
                - Failed
                - NotAvailable
                type: string
              version:
                description: |-
                  Version is the AIM version extracted from the owning model's image tag
                  (e.g., "0.8.5" from "aim-base:0.8.5"). Set during discovery.
                  Used for version-based template matching with fine-tuned models.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: aimtemplatecaches.aim.eai.amd.com
spec:
  group: aim.eai.amd.com
  names:
    categories:
    - aim
    - all
    kind: AIMTemplateCache
    listKind: AIMTemplateCacheList
    plural: aimtemplatecaches
    shortNames:
    - aimtc
    singular: aimtemplatecache
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.templateName
      name: Template
      type: string
    - jsonPath: .spec.mode
      name: Mode
      type: string
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.resolvedTemplateKind
      name: Kind
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AIMTemplateCache pre-warms artifacts for a specified template.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AIMTemplateCacheSpec defines the desired state of AIMTemplateCache
            properties:
              downloadImage:
                description: |-
                  DownloadImage specifies the container image used to download and initialize artifacts.
                  When not specified, the controller uses the default model download image.
                type: string
              env:
                description: |-
                  Env specifies environment variables to use for authentication when downloading models.
                  These variables are used for authentication with model registries (e.g., HuggingFace tokens).
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: |-
                        Name of the environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        fileKeyRef:
                          description: |-
                            FileKeyRef selects a key of the env file.
                            Requires the EnvFiles feature gate to be enabled.
                          properties:
                            key:
                              description: |-
                                The key within the env file. An invalid key will prevent the pod from starting.
                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                              type: string
                            optional:
                              default: false
                              description: |-
                                Specify whether the file or its key must be defined. If the file or key
                                does not exist, then the env var is not published.
                                If optional is set to true and the specified key does not exist,
                                the environment variable will not be set in the Pod's containers.

                                If optional is set to false and the specified key does not exist,
                                an error will be returned during Pod creation.
                              type: boolean
                            path:
                              description: |-
                                The path within the volume from which to select the file.
                                Must be relative and may not contain the '..' path or start with '..'.
                              type: string
                            volumeName:
                              description: The name of the volume mount containing
                                the env file.
                              type: string
                          required:
                          - key
                          - path
                          - volumeName
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              imagePullSecrets:
                description: ImagePullSecrets references secrets for pulling AIM container
                  images.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              mode:
                default: Shared
                description: |-
                  Mode controls the ownership behavior of artifacts created by this template cache.
                  - Dedicated: artifacts are owned by this template cache and garbage collected when it's deleted.
                  - Shared (default): artifacts have no owner references and persist independently.
                  When a Shared template cache encounters artifacts with owner references, it promotes them
                  to shared by removing the owner references, ensuring they persist for long-term use.
                enum:
                - Dedicated
                - Shared
                type: string
              modelSources:
                description: |-
                  ModelSources specifies the model sources to cache for this template.
                  These sources are typically copied from the resolved template's model sources.
                items:
                  description: |-
                    AIMModelSource describes a model artifact that must be downloaded for inference.
                    Discovery extracts these from the container's configuration to enable caching and validation.
                  properties:
                    env:
                      description: |-
                        Env specifies per-source credential overrides.
                        These variables are used for authentication when downloading this specific source.
                        Takes precedence over base-level env for the same variable name.
                      items:
                        description: EnvVar represents an environment variable present
                          in a Container.
                        properties:
                          name:
                            description: |-
                              Name of the environment variable.
                              May consist of any printable ASCII characters except '='.
                            type: string
                          value:
                            description: |-
                              Variable references $(VAR_NAME) are expanded
                              using the previously defined environment variables in the container and
                              any service environment variables. If a variable cannot be resolved,
                              the reference in the input string will be unchanged. Double $$ are reduced
                              to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                              "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                              Escaped references will never be expanded, regardless of whether the variable
                              exists or not.
                              Defaults to "".
                            type: string
                          valueFrom:
                            description: Source for the environment variable's value.
                              Cannot be used if value is not empty.
                            properties:
                              configMapKeyRef:
                                description: Selects a key of a ConfigMap.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the ConfigMap or
                                      its key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              fieldRef:
                                description: |-
                                  Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                  spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                properties:
                                  apiVersion:
                                    description: Version of the schema the FieldPath
                                      is written in terms of, defaults to "v1".
                                    type: string
                                  fieldPath:
                                    description: Path of the field to select in the
                                      specified API version.
                                    type: string
                                required:
                                - fieldPath
                                type: object
                                x-kubernetes-map-type: atomic
                              fileKeyRef:
                                description: |-
                                  FileKeyRef selects a key of the env file.
                                  Requires the EnvFiles feature gate to be enabled.
                                properties:
                                  key:
                                    description: |-
                                      The key within the env file. An invalid key will prevent the pod from starting.
                                      The keys defined within a source may consist of any printable ASCII characters except '='.
                                      During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                    type: string
                                  optional:
                                    default: false
                                    description: |-
                                      Specify whether the file or its key must be defined. If the file or key
                                      does not exist, then the env var is not published.
                                      If optional is set to true and the specified key does not exist,
                                      the environment variable will not be set in the Pod's containers.

                                      If optional is set to false and the specified key does not exist,
                                      an error will be returned during Pod creation.
                                    type: boolean
                                  path:
                                    description: |-
                                      The path within the volume from which to select the file.
                                      Must be relative and may not contain the '..' path or start with '..'.
                                    type: string
                                  volumeName:
                                    description: The name of the volume mount containing
                                      the env file.
                                    type: string
                                required:
                                - key
                                - path
                                - volumeName
                                type: object
                                x-kubernetes-map-type: atomic
                              resourceFieldRef:
                                description: |-
                                  Selects a resource of the container: only resources limits and requests
                                  (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                properties:
                                  containerName:
                                    description: 'Container name: required for volumes,
                                      optional for env vars'
                                    type: string
                                  divisor:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: Specifies the output format of the
                                      exposed resources, defaults to "1"
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  resource:
                                    description: 'Required: resource to select'
                                    type: string
                                required:
                                - resource
                                type: object
                                x-kubernetes-map-type: atomic
                              secretKeyRef:
                                description: Selects a key of a secret in the pod's
                                  namespace
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                        required:
                        - name
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - name
                      x-kubernetes-list-type: map
                    modelId:
                      description: |-
                        ModelID is the canonical identifier in {org}/{name} format.
                        Determines the cache mount path: /workspace/cache/{modelId}
                        For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                        For S3 sources, users define their own organizational structure.
                      pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                      type: string
                    precision:
                      allOf:
                      - enum:
                        - auto
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                      - enum:
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                      description: |-
                        Precision describes the runtime precision this source is compatible with.
                        Used to match model sources to profiles during custom weight onboarding.
                      type: string
                    size:
                      anyOf:
                      - type: integer
                      - type: string
                      description: |-
                        Size is the expected storage space required for this model artifact.
                        Used for PVC sizing and capacity planning during cache creation.
                        Optional - if not specified, the download job will discover the size automatically.
                        Can be set explicitly to pre-allocate storage or override auto-discovery.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    sourceUri:
                      description: |-
                        SourceURI is the location from which the model should be downloaded.
                        Supported schemes:
                        - hf://org/model - Hugging Face Hub model
                        - s3://bucket/key - S3-compatible storage
                      pattern: ^(hf|s3)://[^ \t\r\n]+$
                      type: string
                  required:
                  - modelId
                  - sourceUri
                  type: object
                type: array
              runtimeConfigName:
                description: |-
                  Name is the name of the runtime config to use for this resource. If a runtime config with this name exists both
                  as a namespace and a cluster runtime config, the values are merged together, the namespace config taking priority
                  over the cluster config when there are conflicts. If this field is empty or set to `default`, the namespace / cluster
                  runtime config with the name `default` is used, if it exists.
                type: string
              storageClassName:
                description: |-
                  StorageClassName specifies the storage class for cache volumes.
                  When not specified, uses the cluster default storage class.
                type: string
              templateName:
                description: |-
                  TemplateName is the name of the AIMServiceTemplate or AIMClusterServiceTemplate to cache.
                  The controller will first look for a namespace-scoped AIMServiceTemplate in the same namespace.
                  If not found, it will look for a cluster-scoped AIMClusterServiceTemplate with the same name.
                  Namespace-scoped templates take priority over cluster-scoped templates.
                minLength: 1
                type: string
              templateScope:
                description: |-
                  TemplateScope indicates whether the template is namespace-scoped or cluster-scoped.
                  This field is set by the controller during template resolution.
                enum:
                - Namespace
                - Cluster
                - Unknown
                type: string
            required:
            - templateName
            - templateScope
            type: object
          status:
            description: AIMTemplateCacheStatus defines the observed state of AIMTemplateCache
            properties:
              artifacts:
                additionalProperties:
                  properties:
                    model:
                      description: Model is the name of the model that is cached
                      type: string
                    mountPoint:
                      description: MountPoint is the mount point for the artifact
                      type: string
                    name:
                      description: Name of the AIMArtifact resource
                      type: string
                    persistentVolumeClaim:
                      description: PersistentVolumeClaim name if available
                      type: string
                    status:
                      description: Status of the artifact
                      type: string
                    uid:
                      description: UID of the AIMArtifact resource
                      type: string
                  required:
                  - model
                  - name
                  - status
                  - uid
                  type: object
                description: Artifacts maps model names to their resolved AIMArtifact
                  resources.
                type: object
              conditions:
                description: Conditions represent the latest observations of the template
                  cache state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              resolvedRuntimeConfig:
                description: ResolvedRuntimeConfig captures metadata about the runtime
                  config that was resolved.
                properties:
                  kind:
                    description: Kind is the fully-qualified kind of the resolved
                      reference, when known.
                    type: string
                  name:
                    description: Name is the resource name that satisfied the reference.
                    type: string
                  namespace:
                    description: |-
                      Namespace identifies where the resource was found when namespace-scoped.
                      Empty indicates a cluster-scoped resource.
                    type: string
                  scope:
                    description: Scope indicates whether the resolved resource was
                      namespace or cluster scoped.
                    enum:
                    - Namespace
                    - Cluster
                    - Merged
                    - Unknown
                    type: string
                  uid:
                    description: UID captures the unique identifier of the resolved
                      reference, when known.
                    type: string
                type: object
              resolvedTemplateKind:
                description: |-
                  ResolvedTemplateKind indicates whether the template resolved to a namespace-scoped
                  AIMServiceTemplate or cluster-scoped AIMClusterServiceTemplate.
                  Values: "AIMServiceTemplate", "AIMClusterServiceTemplate"
                type: string
              status:
                default: Pending
                description: Status represents the current high-level status of the
                  template cache.
                enum:
                - Pending
                - Progressing
                - Ready
                - Failed
                - Degraded
                - NotAvailable
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
