---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: aimprofilesets.aim.eai.amd.com
spec:
  group: aim.eai.amd.com
  names:
    categories:
    - aim
    - all
    kind: AIMProfileSet
    listKind: AIMProfileSetList
    plural: aimprofilesets
    shortNames:
    - aimprfset
    singular: aimprofileset
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.managedProfiles.total
      name: Managed
      type: integer
    - jsonPath: .status.managedProfiles.ready
      name: Ready
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: AIMProfileSet is the Schema for namespace-scoped profile derivation
          resources.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              AIMProfileSetSpec defines the desired state of AIMProfileSet and is also reused by AIMModel.profileCopy.

              The selector-non-empty rule accepts every documented narrowing field
              (including modelRef and origin) and lets sourceRef-only specs through:
              when sourceRef is set, the discovery cache itself acts as the source
              scope and a separate selector is not required.
            properties:
              image:
                description: Image overrides the runtime image used by the derived
                  profiles.
                type: string
              imagePullSecrets:
                description: ImagePullSecrets lists secrets used for inspecting and
                  pulling container images.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              overrides:
                description: Overrides mutates the copied profile spec after selection
                  and version filtering.
                properties:
                  acceleratorCount:
                    description: AcceleratorCount replaces the copied profile's acceleratorCount.
                    format: int32
                    type: integer
                  acceleratorModel:
                    description: AcceleratorModel replaces the copied profile's acceleratorModel.
                    type: string
                  aimId:
                    description: |-
                      AimId stamps the derived profile's spec.aimId. When set, wins over
                      the source profile's aimId. REQUIRED when the enclosing selector has
                      role=base (the source base profile carries no aimId of its own).
                    type: string
                  containerEnv:
                    description: ContainerEnv merges by env var name, overriding matching
                      source entries.
                    items:
                      description: EnvVar represents an environment variable present
                        in a Container.
                      properties:
                        name:
                          description: |-
                            Name of the environment variable.
                            May consist of any printable ASCII characters except '='.
                          type: string
                        value:
                          description: |-
                            Variable references $(VAR_NAME) are expanded
                            using the previously defined environment variables in the container and
                            any service environment variables. If a variable cannot be resolved,
                            the reference in the input string will be unchanged. Double $$ are reduced
                            to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                            "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                            Escaped references will never be expanded, regardless of whether the variable
                            exists or not.
                            Defaults to "".
                          type: string
                        valueFrom:
                          description: Source for the environment variable's value.
                            Cannot be used if value is not empty.
                          properties:
                            configMapKeyRef:
                              description: Selects a key of a ConfigMap.
                              properties:
                                key:
                                  description: The key to select.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the ConfigMap or its
                                    key must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                            fieldRef:
                              description: |-
                                Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                              properties:
                                apiVersion:
                                  description: Version of the schema the FieldPath
                                    is written in terms of, defaults to "v1".
                                  type: string
                                fieldPath:
                                  description: Path of the field to select in the
                                    specified API version.
                                  type: string
                              required:
                              - fieldPath
                              type: object
                              x-kubernetes-map-type: atomic
                            fileKeyRef:
                              description: |-
                                FileKeyRef selects a key of the env file.
                                Requires the EnvFiles feature gate to be enabled.
                              properties:
                                key:
                                  description: |-
                                    The key within the env file. An invalid key will prevent the pod from starting.
                                    The keys defined within a source may consist of any printable ASCII characters except '='.
                                    During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                  type: string
                                optional:
                                  default: false
                                  description: |-
                                    Specify whether the file or its key must be defined. If the file or key
                                    does not exist, then the env var is not published.
                                    If optional is set to true and the specified key does not exist,
                                    the environment variable will not be set in the Pod's containers.

                                    If optional is set to false and the specified key does not exist,
                                    an error will be returned during Pod creation.
                                  type: boolean
                                path:
                                  description: |-
                                    The path within the volume from which to select the file.
                                    Must be relative and may not contain the '..' path or start with '..'.
                                  type: string
                                volumeName:
                                  description: The name of the volume mount containing
                                    the env file.
                                  type: string
                              required:
                              - key
                              - path
                              - volumeName
                              type: object
                              x-kubernetes-map-type: atomic
                            resourceFieldRef:
                              description: |-
                                Selects a resource of the container: only resources limits and requests
                                (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                              properties:
                                containerName:
                                  description: 'Container name: required for volumes,
                                    optional for env vars'
                                  type: string
                                divisor:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: Specifies the output format of the
                                    exposed resources, defaults to "1"
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                resource:
                                  description: 'Required: resource to select'
                                  type: string
                              required:
                              - resource
                              type: object
                              x-kubernetes-map-type: atomic
                            secretKeyRef:
                              description: Selects a key of a secret in the pod's
                                namespace
                              properties:
                                key:
                                  description: The key of the secret to select from.  Must
                                    be a valid secret key.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the Secret or its key
                                    must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  engineArgs:
                    description: EngineArgs shallow-merges on top of the source engineArgs,
                      overriding matching keys.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  engineEnv:
                    additionalProperties:
                      type: string
                    description: EngineEnv merges by key, overriding matching source
                      entries.
                    type: object
                  image:
                    description: |-
                      Image overrides the runtime container image used by the derived
                      profiles. When empty the deployment image is rebased onto the source
                      profile's base-image (status.baseImage) so private mirrors stay
                      self-contained.
                    type: string
                  modelId:
                    description: |-
                      ModelId stamps the derived profile's spec.modelId. When set, wins
                      over both the source profile's modelId AND the auto-derivation from
                      modelSources[0].modelId. REQUIRED when the enclosing selector has
                      role=base.
                    type: string
                  modelSources:
                    description: |-
                      ModelSources replaces the copied profile's modelSources. When
                      modelSources[0].modelId is set and overrides.modelId is unset, the
                      derived profile's modelId is auto-derived from modelSources[0]; an
                      explicit overrides.modelId always wins.
                    items:
                      description: |-
                        AIMModelSource describes a model artifact that must be downloaded for inference.
                        Discovery extracts these from the container's configuration to enable caching and validation.
                      properties:
                        env:
                          description: |-
                            Env specifies per-source credential overrides.
                            These variables are used for authentication when downloading this specific source.
                            Takes precedence over base-level env for the same variable name.
                          items:
                            description: EnvVar represents an environment variable
                              present in a Container.
                            properties:
                              name:
                                description: |-
                                  Name of the environment variable.
                                  May consist of any printable ASCII characters except '='.
                                type: string
                              value:
                                description: |-
                                  Variable references $(VAR_NAME) are expanded
                                  using the previously defined environment variables in the container and
                                  any service environment variables. If a variable cannot be resolved,
                                  the reference in the input string will be unchanged. Double $$ are reduced
                                  to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                  "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                  Escaped references will never be expanded, regardless of whether the variable
                                  exists or not.
                                  Defaults to "".
                                type: string
                              valueFrom:
                                description: Source for the environment variable's
                                  value. Cannot be used if value is not empty.
                                properties:
                                  configMapKeyRef:
                                    description: Selects a key of a ConfigMap.
                                    properties:
                                      key:
                                        description: The key to select.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the ConfigMap
                                          or its key must be defined
                                        type: boolean
                                    required:
                                    - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  fieldRef:
                                    description: |-
                                      Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                      spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                    properties:
                                      apiVersion:
                                        description: Version of the schema the FieldPath
                                          is written in terms of, defaults to "v1".
                                        type: string
                                      fieldPath:
                                        description: Path of the field to select in
                                          the specified API version.
                                        type: string
                                    required:
                                    - fieldPath
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  fileKeyRef:
                                    description: |-
                                      FileKeyRef selects a key of the env file.
                                      Requires the EnvFiles feature gate to be enabled.
                                    properties:
                                      key:
                                        description: |-
                                          The key within the env file. An invalid key will prevent the pod from starting.
                                          The keys defined within a source may consist of any printable ASCII characters except '='.
                                          During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                        type: string
                                      optional:
                                        default: false
                                        description: |-
                                          Specify whether the file or its key must be defined. If the file or key
                                          does not exist, then the env var is not published.
                                          If optional is set to true and the specified key does not exist,
                                          the environment variable will not be set in the Pod's containers.

                                          If optional is set to false and the specified key does not exist,
                                          an error will be returned during Pod creation.
                                        type: boolean
                                      path:
                                        description: |-
                                          The path within the volume from which to select the file.
                                          Must be relative and may not contain the '..' path or start with '..'.
                                        type: string
                                      volumeName:
                                        description: The name of the volume mount
                                          containing the env file.
                                        type: string
                                    required:
                                    - key
                                    - path
                                    - volumeName
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  resourceFieldRef:
                                    description: |-
                                      Selects a resource of the container: only resources limits and requests
                                      (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                    properties:
                                      containerName:
                                        description: 'Container name: required for
                                          volumes, optional for env vars'
                                        type: string
                                      divisor:
                                        anyOf:
                                        - type: integer
                                        - type: string
                                        description: Specifies the output format of
                                          the exposed resources, defaults to "1"
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      resource:
                                        description: 'Required: resource to select'
                                        type: string
                                    required:
                                    - resource
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  secretKeyRef:
                                    description: Selects a key of a secret in the
                                      pod's namespace
                                    properties:
                                      key:
                                        description: The key of the secret to select
                                          from.  Must be a valid secret key.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the Secret or
                                          its key must be defined
                                        type: boolean
                                    required:
                                    - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                type: object
                            required:
                            - name
                            type: object
                          type: array
                          x-kubernetes-list-map-keys:
                          - name
                          x-kubernetes-list-type: map
                        modelId:
                          description: |-
                            ModelID is the canonical identifier in {org}/{name} format.
                            Determines the cache mount path: /workspace/cache/{modelId}
                            For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                            For S3 sources, users define their own organizational structure.
                          pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                          type: string
                        precision:
                          allOf:
                          - enum:
                            - auto
                            - fp4
                            - fp8
                            - fp16
                            - fp32
                            - bf16
                            - int4
                            - int8
                          - enum:
                            - fp4
                            - fp8
                            - fp16
                            - fp32
                            - bf16
                            - int4
                            - int8
                          description: |-
                            Precision describes the runtime precision this source is compatible with.
                            Used to match model sources to profiles during custom weight onboarding.
                          type: string
                        size:
                          anyOf:
                          - type: integer
                          - type: string
                          description: |-
                            Size is the expected storage space required for this model artifact.
                            Used for PVC sizing and capacity planning during cache creation.
                            Optional - if not specified, the download job will discover the size automatically.
                            Can be set explicitly to pre-allocate storage or override auto-discovery.
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        sourceUri:
                          description: |-
                            SourceURI is the location from which the model should be downloaded.
                            Supported schemes:
                            - hf://org/model - Hugging Face Hub model
                            - s3://bucket/key - S3-compatible storage
                          pattern: ^(hf|s3)://[^ \t\r\n]+$
                          type: string
                      required:
                      - modelId
                      - sourceUri
                      type: object
                    type: array
                  profileId:
                    description: |-
                      ProfileId stamps the derived profile's spec.profileId. Optional —
                      most callers leave this empty and let the source profile's profileId
                      carry through (or the reconciler synthesise one).
                    type: string
                type: object
              selector:
                description: Selector chooses which source profiles to derive from.
                properties:
                  acceleratorCount:
                    description: AcceleratorCount filters by accelerator unit count.
                    format: int32
                    type: integer
                  acceleratorModel:
                    description: AcceleratorModel filters by accelerator identifier.
                    type: string
                  acceleratorType:
                    description: AcceleratorType filters by accelerator resource type.
                    enum:
                    - gpu
                    - cpu
                    type: string
                  aimId:
                    description: AimId filters by model architecture identifier (e.g.,
                      "qwen/qwen3-32b").
                    type: string
                  engine:
                    description: Engine filters by inference engine.
                    type: string
                  engineArgs:
                    description: |-
                      EngineArgs partially matches source engineArgs: every provided top-level key must
                      exist in the source object with an equal value.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  metric:
                    description: Metric filters by optimization target.
                    enum:
                    - latency
                    - throughput
                    type: string
                  modelId:
                    description: ModelId filters by the source profile's specific
                      model identifier.
                    type: string
                  modelRef:
                    description: |-
                      ModelRef narrows candidates to those produced by a specific
                      AIM(Cluster)Model, matched via the `aim.eai.amd.com/source-model[-scope]`
                      labels stamped by the AIMModel reconcilers. Iteration 1 (v1alpha2 only).
                    properties:
                      name:
                        description: Name is the owning AIM(Cluster)Model name. Required.
                        minLength: 1
                        type: string
                      scope:
                        default: Auto
                        description: Scope controls how Name is resolved against AIMModel
                          vs AIMClusterModel.
                        enum:
                        - Auto
                        - Namespace
                        - Cluster
                        type: string
                    required:
                    - name
                    type: object
                  origin:
                    description: |-
                      Origin filters by the `aim.eai.amd.com/profile-origin` label. When unset
                      (empty) the selector does not filter by origin. Iteration 1 (v1alpha2
                      only).
                    enum:
                    - discovered
                    - derived
                    - user-authored
                    type: string
                  precision:
                    description: Precision filters by numeric precision.
                    enum:
                    - auto
                    - fp4
                    - fp8
                    - fp16
                    - fp32
                    - bf16
                    - int4
                    - int8
                    type: string
                  profileId:
                    description: ProfileId filters by the source profile's profile
                      identifier.
                    type: string
                  role:
                    default: deployable
                    description: |-
                      Role filters by the `aim.eai.amd.com/profile-role` label. Defaults to
                      `deployable`. `base` filters to base profiles emitted by base-image
                      discovery (custom-model derivation source material).
                    enum:
                    - base
                    - deployable
                    type: string
                  type:
                    description: Type filters by optimization level.
                    enum:
                    - optimized
                    - general
                    - preview
                    - unoptimized
                    type: string
                type: object
              serviceAccountName:
                description: ServiceAccountName is propagated to managed profiles
                  for downstream workloads.
                type: string
              sourceRef:
                description: |-
                  SourceRef points to an alternate discovery cache source.
                  When omitted, derivation uses visible AIMProfile and AIMClusterProfile objects.
                  For AIMProfileSet, the referenced ConfigMap is read from the same namespace.
                  For AIMClusterProfileSet, it is read from the operator namespace.
                properties:
                  name:
                    description: Name is the discovery cache ConfigMap name.
                    type: string
                required:
                - name
                type: object
              version:
                description: Version pins matching to a specific source profile version
                  when VersionPolicy is pinned.
                type: string
              versionPolicy:
                default: pinned
                description: VersionPolicy controls how matching profiles are filtered
                  by version.
                enum:
                - pinned
                - latest
                - all
                type: string
            type: object
            x-kubernetes-validations:
            - message: selector must set at least one matching field, or set sourceRef
              rule: has(self.sourceRef) || has(self.selector.aimId) || has(self.selector.modelId)
                || has(self.selector.profileId) || has(self.selector.engine) || has(self.selector.metric)
                || has(self.selector.precision) || has(self.selector.type) || has(self.selector.acceleratorModel)
                || has(self.selector.acceleratorType) || has(self.selector.acceleratorCount)
                || has(self.selector.engineArgs) || has(self.selector.modelRef) ||
                has(self.selector.origin)
            - message: version is required when versionPolicy is pinned
              rule: has(self.version) || (has(self.versionPolicy) && self.versionPolicy
                != 'pinned')
            - message: latest/all versionPolicy must not set version
              rule: '!has(self.versionPolicy) || (self.versionPolicy != ''latest''
                && self.versionPolicy != ''all'') || !has(self.version)'
            - message: selector.aimId/modelId/profileId are not allowed when selector.role=base;
                set overrides.aimId/modelId/profileId instead (base profiles have
                no source identity to filter on)
              rule: '!(has(self.selector.role) && self.selector.role == ''base'')
                || (!has(self.selector.aimId) && !has(self.selector.modelId) && !has(self.selector.profileId))'
            - message: selector.role=base requires overrides.aimId and overrides.modelId
                (base profiles carry no identity of their own; overrides supply it)
              rule: '!(has(self.selector.role) && self.selector.role == ''base'')
                || (has(self.overrides) && has(self.overrides.aimId) && has(self.overrides.modelId))'
          status:
            description: AIMProfileSetStatus defines the observed state of AIMProfileSet
              / AIMClusterProfileSet.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the profile set's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              managedProfiles:
                description: ManagedProfiles summarizes the direct derivative profiles
                  owned or managed by this profile set.
                properties:
                  base:
                    description: |-
                      Base is the number of managed profiles whose spec is structurally
                      incomplete (missing aimId or modelSources). Base profiles cannot back
                      an AIMService directly and exist purely as source material for
                      derivation via AIMModel.spec.profiles.derivedFrom with
                      selector.role=base. A non-zero value is the canonical operational
                      signal that this model is a base-image model (the source of
                      custom-model derivation).
                    format: int32
                    type: integer
                  deployable:
                    description: |-
                      Deployable is the number of managed profiles whose spec is materialised
                      enough to back an AIMService (carries aimId and modelSources). For a
                      normal officially-discovered AIMModel this equals Total. For a
                      base-image AIMModel used as a source for custom-model derivation
                      this is 0 — the model only emits base profiles that callers must
                      derive into deployable profiles.
                    format: int32
                    type: integer
                  notAvailable:
                    description: NotAvailable is the number of derivative profiles
                      whose own status is NotAvailable.
                    format: int32
                    type: integer
                  ready:
                    description: Ready is the number of derivative profiles whose
                      own status is Ready.
                    format: int32
                    type: integer
                  total:
                    description: Total is the number of derivative profiles the controller
                      currently manages or intends to manage.
                    format: int32
                    type: integer
                required:
                - base
                - deployable
                - notAvailable
                - ready
                - total
                type: object
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              status:
                default: Pending
                description: Status represents the overall high-level status for the
                  profile set.
                enum:
                - Pending
                - Progressing
                - Ready
                - Degraded
                - Failed
                - NotAvailable
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
