---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: aimclustermodels.aim.eai.amd.com
spec:
  group: aim.eai.amd.com
  names:
    categories:
    - aim
    - all
    kind: AIMClusterModel
    listKind: AIMClusterModelList
    plural: aimclustermodels
    shortNames:
    - aimclmdl
    singular: aimclustermodel
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.sourceType
      name: Source
      type: string
    - jsonPath: .spec.image
      name: Image
      type: string
    - jsonPath: .status.version
      name: Version
      type: string
    - jsonPath: .status.imageMetadata.model.canonicalName
      name: Model
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    deprecated: true
    deprecationWarning: AIMClusterModel v1alpha1 is deprecated; use aim.eai.amd.com/v1alpha2
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          AIMClusterModel is a cluster-scoped model catalog entry for AIM container images.

          Cluster-scoped models can be referenced by AIMServices in any namespace, making them ideal for
          shared model deployments across teams and projects. Like namespace-scoped AIMModels, cluster models
          trigger discovery jobs to extract metadata and generate service templates.

          When both cluster and namespace models exist for the same container image, services will preferentially
          use the namespace-scoped AIMModel when referenced by image URI.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              AIMModelSpec defines the desired state of AIMModel.

              Per-version constraints (v1alpha1 forbids derivedFrom and profiles;
              v1alpha2 forbids profileCopy/custom/customTemplates/top-level modelSources,
              requires image XOR profiles, and forbids profiles mixed with legacy scalar
              fields) live on the version-specific AIMModel / AIMClusterModel root types
              in the per-version *_types.go files.
            properties:
              aimId:
                description: |-
                  AimId is the AIM product family identifier (e.g., "qwen/qwen3-32b").
                  When set together with modelSources, enables aimId-based template matching:
                  the controller finds official templates by aimId, filters by versionPolicy,
                  matches by modelId, and creates copies with the custom weight source.
                type: string
              custom:
                description: |-
                  Custom contains configuration for custom models (models with inline modelSources).
                  Only used when modelSources are specified; ignored for image-based models.
                properties:
                  hardware:
                    description: |-
                      Hardware specifies default hardware requirements for all templates.
                      Individual templates can override these defaults.
                      Required when modelSources is set and customTemplates is empty (unless aimId is set).
                    properties:
                      cpu:
                        description: CPU specifies CPU requirements.
                        properties:
                          limits:
                            anyOf:
                            - type: integer
                            - type: string
                            description: Limits is the maximum number of CPU cores
                              to allow.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          requests:
                            anyOf:
                            - type: integer
                            - type: string
                            description: Requests is the number of CPU cores to request.
                              Required and must be > 0.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                        required:
                        - requests
                        type: object
                      gpu:
                        description: GPU specifies GPU requirements. If not set, no
                          GPUs are requested (CPU-only model).
                        properties:
                          minVram:
                            anyOf:
                            - type: integer
                            - type: string
                            description: |-
                              MinVRAM limits deployment to GPUs having at least this much VRAM.
                              Used for capacity planning when the model size is known but any GPU with
                              sufficient VRAM is acceptable.
                              Cannot be combined with model.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          model:
                            description: |-
                              Model limits deployment to a specific GPU model.
                              Example: "MI300X"
                              Cannot be combined with minVram.
                            maxLength: 64
                            type: string
                          requests:
                            description: |-
                              Requests is the number of GPUs to set as requests/limits.
                              Set to 0 to target GPU nodes without consuming GPU resources (useful for testing).
                            format: int32
                            minimum: 0
                            type: integer
                          resourceName:
                            default: amd.com/gpu
                            description: |-
                              ResourceName is the Kubernetes resource name for GPU resources.
                              Defaults to "amd.com/gpu" if not specified.
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: model and minVram are mutually exclusive; specify
                            either a specific GPU model OR a minimum VRAM requirement,
                            not both
                          rule: '!(has(self.model) && size(self.model) > 0 && has(self.minVram))'
                    type: object
                    x-kubernetes-validations:
                    - message: at least one of gpu or cpu must be specified
                      rule: has(self.gpu) || has(self.cpu)
                  type:
                    allOf:
                    - enum:
                      - optimized
                      - general
                      - preview
                      - unoptimized
                    - enum:
                      - optimized
                      - general
                      - preview
                      - unoptimized
                    description: |-
                      Type specifies default type for all templates.
                      Individual templates can override this default.
                      When nil, templates default to "unoptimized".
                    type: string
                  versionPolicy:
                    default: pinned
                    description: |-
                      VersionPolicy controls how template versions are filtered during aimId-based matching.
                      - pinned (default): match templates whose status.version equals the model's image tag
                      - latest: match only templates at the newest available status.version
                      - all: match templates at any version
                      - any: deprecated alias of all, kept for backward compatibility
                      Only used when spec.aimId is set.
                    enum:
                    - pinned
                    - latest
                    - any
                    - all
                    type: string
                type: object
              customTemplates:
                description: |-
                  CustomTemplates defines explicit template configurations for this model.
                  These templates are created directly without running a discovery job.
                  Can be used with or without modelSources to define custom deployment configurations.
                  If omitted when modelSources is set, a single template is auto-generated
                  using the custom.hardware requirements.
                items:
                  description: |-
                    AIMCustomTemplate defines a custom template configuration for a model.
                    When modelSources are specified directly on AIMModel, customTemplates allow
                    defining explicit hardware requirements and profiles, skipping the discovery job.
                    This is an existing struct (not a CRD); it appears as an element of AIMModel.spec.customTemplates[].
                  properties:
                    aimId:
                      description: |-
                        AimId is the AIM product family identifier (e.g., "meta-llama/Llama-3-8B").
                        Required when customProfile is set.
                      type: string
                    customProfile:
                      description: |-
                        CustomProfile defines inline custom profile data for the inference engine.
                        When set, the resulting template will have a custom profile ConfigMap mounted.
                        Requires aimId, modelId, hardware, profile.metric, and profile.precision.
                      properties:
                        engineArgs:
                          description: |-
                            EngineArgs contains inference engine arguments as a free-form JSON object.
                            These are passed as CLI arguments to the inference engine (e.g., vLLM).
                            Do not include "model" — it is injected separately by the runtime.
                          x-kubernetes-preserve-unknown-fields: true
                        envVars:
                          additionalProperties:
                            type: string
                          description: |-
                            EnvVars contains environment variables applied to the inference engine process.
                            These are written into the profile YAML and applied by the AIM runtime via os.execv,
                            distinct from container-level Env which targets the AIM runtime container itself.
                            Keys must match ^[A-Z0-9_]+$ (uppercase with underscores).
                          type: object
                      type: object
                    env:
                      description: |-
                        Env specifies environment variable overrides when this template is selected.
                        These are container-level env vars applied to the AIM runtime container.
                      items:
                        description: EnvVar represents an environment variable present
                          in a Container.
                        properties:
                          name:
                            description: |-
                              Name of the environment variable.
                              May consist of any printable ASCII characters except '='.
                            type: string
                          value:
                            description: |-
                              Variable references $(VAR_NAME) are expanded
                              using the previously defined environment variables in the container and
                              any service environment variables. If a variable cannot be resolved,
                              the reference in the input string will be unchanged. Double $$ are reduced
                              to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                              "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                              Escaped references will never be expanded, regardless of whether the variable
                              exists or not.
                              Defaults to "".
                            type: string
                          valueFrom:
                            description: Source for the environment variable's value.
                              Cannot be used if value is not empty.
                            properties:
                              configMapKeyRef:
                                description: Selects a key of a ConfigMap.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the ConfigMap or
                                      its key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              fieldRef:
                                description: |-
                                  Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                  spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                properties:
                                  apiVersion:
                                    description: Version of the schema the FieldPath
                                      is written in terms of, defaults to "v1".
                                    type: string
                                  fieldPath:
                                    description: Path of the field to select in the
                                      specified API version.
                                    type: string
                                required:
                                - fieldPath
                                type: object
                                x-kubernetes-map-type: atomic
                              fileKeyRef:
                                description: |-
                                  FileKeyRef selects a key of the env file.
                                  Requires the EnvFiles feature gate to be enabled.
                                properties:
                                  key:
                                    description: |-
                                      The key within the env file. An invalid key will prevent the pod from starting.
                                      The keys defined within a source may consist of any printable ASCII characters except '='.
                                      During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                    type: string
                                  optional:
                                    default: false
                                    description: |-
                                      Specify whether the file or its key must be defined. If the file or key
                                      does not exist, then the env var is not published.
                                      If optional is set to true and the specified key does not exist,
                                      the environment variable will not be set in the Pod's containers.

                                      If optional is set to false and the specified key does not exist,
                                      an error will be returned during Pod creation.
                                    type: boolean
                                  path:
                                    description: |-
                                      The path within the volume from which to select the file.
                                      Must be relative and may not contain the '..' path or start with '..'.
                                    type: string
                                  volumeName:
                                    description: The name of the volume mount containing
                                      the env file.
                                    type: string
                                required:
                                - key
                                - path
                                - volumeName
                                type: object
                                x-kubernetes-map-type: atomic
                              resourceFieldRef:
                                description: |-
                                  Selects a resource of the container: only resources limits and requests
                                  (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                properties:
                                  containerName:
                                    description: 'Container name: required for volumes,
                                      optional for env vars'
                                    type: string
                                  divisor:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: Specifies the output format of the
                                      exposed resources, defaults to "1"
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  resource:
                                    description: 'Required: resource to select'
                                    type: string
                                required:
                                - resource
                                type: object
                                x-kubernetes-map-type: atomic
                              secretKeyRef:
                                description: Selects a key of a secret in the pod's
                                  namespace
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                        required:
                        - name
                        type: object
                      maxItems: 64
                      type: array
                      x-kubernetes-list-map-keys:
                      - name
                      x-kubernetes-list-type: map
                    hardware:
                      description: |-
                        Hardware specifies GPU and CPU requirements for this template.
                        Optional when spec.hardware is set (inherits from spec).
                        When both are set, values are merged field-by-field with template taking precedence.
                      properties:
                        cpu:
                          description: CPU specifies CPU requirements.
                          properties:
                            limits:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Limits is the maximum number of CPU cores
                                to allow.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            requests:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Requests is the number of CPU cores to
                                request. Required and must be > 0.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                          required:
                          - requests
                          type: object
                        gpu:
                          description: GPU specifies GPU requirements. If not set,
                            no GPUs are requested (CPU-only model).
                          properties:
                            minVram:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                MinVRAM limits deployment to GPUs having at least this much VRAM.
                                Used for capacity planning when the model size is known but any GPU with
                                sufficient VRAM is acceptable.
                                Cannot be combined with model.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            model:
                              description: |-
                                Model limits deployment to a specific GPU model.
                                Example: "MI300X"
                                Cannot be combined with minVram.
                              maxLength: 64
                              type: string
                            requests:
                              description: |-
                                Requests is the number of GPUs to set as requests/limits.
                                Set to 0 to target GPU nodes without consuming GPU resources (useful for testing).
                              format: int32
                              minimum: 0
                              type: integer
                            resourceName:
                              default: amd.com/gpu
                              description: |-
                                ResourceName is the Kubernetes resource name for GPU resources.
                                Defaults to "amd.com/gpu" if not specified.
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: model and minVram are mutually exclusive; specify
                              either a specific GPU model OR a minimum VRAM requirement,
                              not both
                            rule: '!(has(self.model) && size(self.model) > 0 && has(self.minVram))'
                      type: object
                      x-kubernetes-validations:
                      - message: at least one of gpu or cpu must be specified
                        rule: has(self.gpu) || has(self.cpu)
                    modelId:
                      description: |-
                        ModelId is the specific model identifier / HuggingFace URI (e.g., "Qwen/Qwen3-32B-FP8").
                        Required when customProfile is set.
                      type: string
                    name:
                      description: Name is the template name. If not provided, auto-generated
                        from model name + profile.
                      maxLength: 63
                      type: string
                    profile:
                      description: |-
                        Profile declares runtime profile variables for template selection.
                        Used when multiple templates exist to select based on metric/precision.
                      properties:
                        metric:
                          allOf:
                          - enum:
                            - latency
                            - throughput
                          - enum:
                            - latency
                            - throughput
                          description: Metric specifies the optimization target (e.g.,
                            latency, throughput).
                          type: string
                        precision:
                          allOf:
                          - enum:
                            - auto
                            - fp4
                            - fp8
                            - fp16
                            - fp32
                            - bf16
                            - int4
                            - int8
                          - enum:
                            - auto
                            - fp4
                            - fp8
                            - fp16
                            - fp32
                            - bf16
                            - int4
                            - int8
                          description: Precision specifies the numerical precision
                            (e.g., fp8, fp16, bf16).
                          type: string
                      type: object
                    type:
                      allOf:
                      - enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                      - enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                      default: unoptimized
                      description: |-
                        Type indicates the optimization status of this template.
                        - optimized: Template has been tuned for performance
                        - general: General-purpose tuning between optimized and preview
                        - preview: Template is experimental/pre-release
                        - unoptimized: Default, no specific optimizations applied
                      type: string
                  type: object
                  x-kubernetes-validations:
                  - message: when customProfile is set, aimId, modelId, hardware,
                      profile.metric, and profile.precision are required
                    rule: '!has(self.customProfile) || (has(self.aimId) && has(self.modelId)
                      && has(self.hardware) && has(self.profile) && has(self.profile.metric)
                      && has(self.profile.precision))'
                maxItems: 16
                type: array
              defaultServiceTemplate:
                description: |-
                  DefaultServiceTemplate specifies the default AIMServiceTemplate to use when creating services for this model.
                  When set, services that reference this model will use this template if no template is explicitly specified.
                  If this is not set, a template will be automatically selected.
                type: string
              derivedFrom:
                description: |-
                  DerivedFrom is the legacy flat shape of v1alpha2's profile-derivation
                  onboarding surface. New objects must use spec.profiles instead; the
                  field is retained so existing v1alpha2 objects (and the v1alpha2
                  reconciler reading them) round-trip cleanly.

                  DEPRECATED: prefer spec.profiles.derivedFrom on v1alpha2.
                properties:
                  image:
                    description: Image overrides the runtime image used by the derived
                      profiles.
                    type: string
                  imagePullSecrets:
                    description: ImagePullSecrets lists secrets used for inspecting
                      and pulling container images.
                    items:
                      description: |-
                        LocalObjectReference contains enough information to let you locate the
                        referenced object inside the same namespace.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    type: array
                  overrides:
                    description: Overrides mutates the copied profile spec after selection
                      and version filtering.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount replaces the copied profile's
                          acceleratorCount.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel replaces the copied profile's
                          acceleratorModel.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode replaces the copied profile's
                          acceleratorPartitioningMode. Complete replacement, not a merge; an empty
                          override string leaves the source profile's mode untouched. Same reserved
                          values as AIMProfileSpecCommon.AcceleratorPartitioningMode. Whenever this
                          override is set (to any value, including "unpartitioned"), the CEL rule on the
                          enclosing spec also requires AcceleratorCount to be set: partition mode
                          changes the per-unit interpretation of acceleratorCount, and CEL cannot
                          read the base profile to tell whether the meaning actually changed, so it
                          conservatively requires the count be restated.
                        type: string
                      aimId:
                        description: |-
                          AimId stamps the derived profile's spec.aimId. When set, wins over
                          the source profile's aimId. REQUIRED when the enclosing selector has
                          role=base (the source base profile carries no aimId of its own).
                        type: string
                      containerEnv:
                        description: ContainerEnv merges by env var name, overriding
                          matching source entries.
                        items:
                          description: EnvVar represents an environment variable present
                            in a Container.
                          properties:
                            name:
                              description: |-
                                Name of the environment variable.
                                May consist of any printable ASCII characters except '='.
                              type: string
                            value:
                              description: |-
                                Variable references $(VAR_NAME) are expanded
                                using the previously defined environment variables in the container and
                                any service environment variables. If a variable cannot be resolved,
                                the reference in the input string will be unchanged. Double $$ are reduced
                                to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                Escaped references will never be expanded, regardless of whether the variable
                                exists or not.
                                Defaults to "".
                              type: string
                            valueFrom:
                              description: Source for the environment variable's value.
                                Cannot be used if value is not empty.
                              properties:
                                configMapKeyRef:
                                  description: Selects a key of a ConfigMap.
                                  properties:
                                    key:
                                      description: The key to select.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the ConfigMap or
                                        its key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fieldRef:
                                  description: |-
                                    Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                    spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                  properties:
                                    apiVersion:
                                      description: Version of the schema the FieldPath
                                        is written in terms of, defaults to "v1".
                                      type: string
                                    fieldPath:
                                      description: Path of the field to select in
                                        the specified API version.
                                      type: string
                                  required:
                                  - fieldPath
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fileKeyRef:
                                  description: |-
                                    FileKeyRef selects a key of the env file.
                                    Requires the EnvFiles feature gate to be enabled.
                                  properties:
                                    key:
                                      description: |-
                                        The key within the env file. An invalid key will prevent the pod from starting.
                                        The keys defined within a source may consist of any printable ASCII characters except '='.
                                        During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                      type: string
                                    optional:
                                      default: false
                                      description: |-
                                        Specify whether the file or its key must be defined. If the file or key
                                        does not exist, then the env var is not published.
                                        If optional is set to true and the specified key does not exist,
                                        the environment variable will not be set in the Pod's containers.

                                        If optional is set to false and the specified key does not exist,
                                        an error will be returned during Pod creation.
                                      type: boolean
                                    path:
                                      description: |-
                                        The path within the volume from which to select the file.
                                        Must be relative and may not contain the '..' path or start with '..'.
                                      type: string
                                    volumeName:
                                      description: The name of the volume mount containing
                                        the env file.
                                      type: string
                                  required:
                                  - key
                                  - path
                                  - volumeName
                                  type: object
                                  x-kubernetes-map-type: atomic
                                resourceFieldRef:
                                  description: |-
                                    Selects a resource of the container: only resources limits and requests
                                    (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                  properties:
                                    containerName:
                                      description: 'Container name: required for volumes,
                                        optional for env vars'
                                      type: string
                                    divisor:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      description: Specifies the output format of
                                        the exposed resources, defaults to "1"
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    resource:
                                      description: 'Required: resource to select'
                                      type: string
                                  required:
                                  - resource
                                  type: object
                                  x-kubernetes-map-type: atomic
                                secretKeyRef:
                                  description: Selects a key of a secret in the pod's
                                    namespace
                                  properties:
                                    key:
                                      description: The key of the secret to select
                                        from.  Must be a valid secret key.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the Secret or its
                                        key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      engineArgs:
                        description: EngineArgs shallow-merges on top of the source
                          engineArgs, overriding matching keys.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      engineEnv:
                        additionalProperties:
                          type: string
                        description: EngineEnv merges by key, overriding matching
                          source entries.
                        type: object
                      image:
                        description: |-
                          Image overrides the runtime container image used by the derived
                          profiles. When empty the deployment image is rebased onto the source
                          profile's base-image (status.baseImage) so private mirrors stay
                          self-contained.
                        type: string
                      modelId:
                        description: |-
                          ModelId stamps the derived profile's spec.modelId. When set, wins
                          over both the source profile's modelId AND the auto-derivation from
                          modelSources[0].modelId. REQUIRED when the enclosing selector has
                          role=base.
                        type: string
                      modelSources:
                        description: |-
                          ModelSources replaces the copied profile's modelSources. When
                          modelSources[0].modelId is set and overrides.modelId is unset, the
                          derived profile's modelId is auto-derived from modelSources[0]; an
                          explicit overrides.modelId always wins.
                        items:
                          description: |-
                            AIMModelSource describes a model artifact that must be downloaded for inference.
                            Discovery extracts these from the container's configuration to enable caching and validation.
                          properties:
                            env:
                              description: |-
                                Env specifies per-source credential overrides.
                                These variables are used for authentication when downloading this specific source.
                                Takes precedence over base-level env for the same variable name.
                              items:
                                description: EnvVar represents an environment variable
                                  present in a Container.
                                properties:
                                  name:
                                    description: |-
                                      Name of the environment variable.
                                      May consist of any printable ASCII characters except '='.
                                    type: string
                                  value:
                                    description: |-
                                      Variable references $(VAR_NAME) are expanded
                                      using the previously defined environment variables in the container and
                                      any service environment variables. If a variable cannot be resolved,
                                      the reference in the input string will be unchanged. Double $$ are reduced
                                      to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                      "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                      Escaped references will never be expanded, regardless of whether the variable
                                      exists or not.
                                      Defaults to "".
                                    type: string
                                  valueFrom:
                                    description: Source for the environment variable's
                                      value. Cannot be used if value is not empty.
                                    properties:
                                      configMapKeyRef:
                                        description: Selects a key of a ConfigMap.
                                        properties:
                                          key:
                                            description: The key to select.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the ConfigMap
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fieldRef:
                                        description: |-
                                          Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                          spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                        properties:
                                          apiVersion:
                                            description: Version of the schema the
                                              FieldPath is written in terms of, defaults
                                              to "v1".
                                            type: string
                                          fieldPath:
                                            description: Path of the field to select
                                              in the specified API version.
                                            type: string
                                        required:
                                        - fieldPath
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fileKeyRef:
                                        description: |-
                                          FileKeyRef selects a key of the env file.
                                          Requires the EnvFiles feature gate to be enabled.
                                        properties:
                                          key:
                                            description: |-
                                              The key within the env file. An invalid key will prevent the pod from starting.
                                              The keys defined within a source may consist of any printable ASCII characters except '='.
                                              During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                            type: string
                                          optional:
                                            default: false
                                            description: |-
                                              Specify whether the file or its key must be defined. If the file or key
                                              does not exist, then the env var is not published.
                                              If optional is set to true and the specified key does not exist,
                                              the environment variable will not be set in the Pod's containers.

                                              If optional is set to false and the specified key does not exist,
                                              an error will be returned during Pod creation.
                                            type: boolean
                                          path:
                                            description: |-
                                              The path within the volume from which to select the file.
                                              Must be relative and may not contain the '..' path or start with '..'.
                                            type: string
                                          volumeName:
                                            description: The name of the volume mount
                                              containing the env file.
                                            type: string
                                        required:
                                        - key
                                        - path
                                        - volumeName
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      resourceFieldRef:
                                        description: |-
                                          Selects a resource of the container: only resources limits and requests
                                          (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                        properties:
                                          containerName:
                                            description: 'Container name: required
                                              for volumes, optional for env vars'
                                            type: string
                                          divisor:
                                            anyOf:
                                            - type: integer
                                            - type: string
                                            description: Specifies the output format
                                              of the exposed resources, defaults to
                                              "1"
                                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                            x-kubernetes-int-or-string: true
                                          resource:
                                            description: 'Required: resource to select'
                                            type: string
                                        required:
                                        - resource
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      secretKeyRef:
                                        description: Selects a key of a secret in
                                          the pod's namespace
                                        properties:
                                          key:
                                            description: The key of the secret to
                                              select from.  Must be a valid secret
                                              key.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the Secret
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                    type: object
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            modelId:
                              description: |-
                                ModelID is the canonical identifier in {org}/{name} format.
                                Determines the cache mount path: /workspace/cache/{modelId}
                                For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                                For S3 sources, users define their own organizational structure.
                              pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                              type: string
                            precision:
                              allOf:
                              - enum:
                                - auto
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              - enum:
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              description: |-
                                Precision describes the runtime precision this source is compatible with.
                                Used to match model sources to profiles during custom weight onboarding.
                              type: string
                            size:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                Size is the expected storage space required for this model artifact.
                                Used for PVC sizing and capacity planning during cache creation.
                                Optional - if not specified, the download job will discover the size automatically.
                                Can be set explicitly to pre-allocate storage or override auto-discovery.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            sourceUri:
                              description: |-
                                SourceURI is the location from which the model should be downloaded.
                                Supported schemes:
                                - hf://org/model - Hugging Face Hub model
                                - s3://bucket/key - S3-compatible storage
                              pattern: ^(hf|s3)://[^ \t\r\n]+$
                              type: string
                          required:
                          - modelId
                          - sourceUri
                          type: object
                        type: array
                      profileId:
                        description: |-
                          ProfileId stamps the derived profile's spec.profileId. Optional —
                          most callers leave this empty and let the source profile's profileId
                          carry through (or the reconciler synthesise one).
                        type: string
                    type: object
                  selector:
                    description: Selector chooses which source profiles to derive
                      from.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount filters by accelerator unit
                          count.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel filters by accelerator identifier.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode filters candidates by their declared
                          partitioning mode. Partial-order match (NOT strict equality):
                            ""              - no filter on this field.
                            "unpartitioned" - matches profiles with mode "" or "unpartitioned".
                            "partitioned"   - matches profiles whose mode is non-trivial (anything
                                              other than "" / "unpartitioned").
                            "<C>"           - selector-only convenience: matches profiles with mode
                                              "<C>-*" (prefix on the scheme). Not a valid profile-spec
                                              value (e.g. selector "CPX" matches "CPX-NPS1", "CPX-NPS4").
                            "<C>-<M>"       - exact-string match on the scheme.
                        type: string
                      acceleratorType:
                        description: AcceleratorType filters by accelerator resource
                          type.
                        enum:
                        - gpu
                        - cpu
                        type: string
                      aimId:
                        description: AimId filters by model architecture identifier
                          (e.g., "qwen/qwen3-32b").
                        type: string
                      engine:
                        description: Engine filters by inference engine.
                        type: string
                      engineArgs:
                        description: |-
                          EngineArgs partially matches source engineArgs: every provided top-level key must
                          exist in the source object with an equal value.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      metric:
                        description: Metric filters by optimization target.
                        enum:
                        - latency
                        - throughput
                        type: string
                      minimumType:
                        description: |-
                          MinimumType filters by a minimum optimization level: candidates whose
                          type is this tier OR BETTER are accepted (hierarchy: optimized > general
                          > preview > unoptimized). This is the floor counterpart to the exact-match
                          Type field; the two AND together when both are set.

                          The sentinel "any" disables the floor (accept every tier). When this
                          field is empty the AIMService resolver applies a default floor of
                          "optimized" so auto-selection prefers production-grade profiles and never
                          silently picks an unoptimized one; to opt a service into lower tiers
                          (e.g. CPU/EPYC profiles published as unoptimized) set minimumType
                          explicitly to "unoptimized" or "any". Derivation selectors
                          (AIMProfileSet / AIMModel.profiles) treat empty as "any" so copying is
                          never tier-restricted by default.
                        enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                        - any
                        type: string
                      modelId:
                        description: ModelId filters by the source profile's specific
                          model identifier.
                        type: string
                      modelRef:
                        description: |-
                          ModelRef narrows candidates to those produced by a specific
                          AIM(Cluster)Model, matched via the `aim.eai.amd.com/source-model[-scope]`
                          labels stamped by the AIMModel reconcilers. Iteration 1 (v1alpha2 only).
                        properties:
                          name:
                            description: Name is the owning AIM(Cluster)Model name.
                              Required.
                            minLength: 1
                            type: string
                          scope:
                            default: Auto
                            description: Scope controls how Name is resolved against
                              AIMModel vs AIMClusterModel.
                            enum:
                            - Auto
                            - Namespace
                            - Cluster
                            type: string
                        required:
                        - name
                        type: object
                      origin:
                        description: |-
                          Origin filters by the `aim.eai.amd.com/profile-origin` label. When unset
                          (empty) the selector does not filter by origin. Iteration 1 (v1alpha2
                          only).
                        enum:
                        - discovered
                        - derived
                        - user-authored
                        type: string
                      precision:
                        description: Precision filters by numeric precision.
                        enum:
                        - auto
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                        type: string
                      profileId:
                        description: ProfileId filters by the source profile's profile
                          identifier.
                        type: string
                      role:
                        default: deployable
                        description: |-
                          Role filters by the `aim.eai.amd.com/profile-role` label. Defaults to
                          `deployable`. `base` filters to base profiles emitted by base-image
                          discovery (custom-model derivation source material).
                        enum:
                        - base
                        - deployable
                        type: string
                      type:
                        description: Type filters by optimization level (exact match).
                        enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                        type: string
                    type: object
                  serviceAccountName:
                    description: ServiceAccountName is propagated to managed profiles
                      for downstream workloads.
                    type: string
                  sourceRef:
                    description: |-
                      SourceRef points to an alternate discovery cache source.
                      When omitted, derivation uses visible AIMProfile and AIMClusterProfile objects.
                      For AIMProfileSet, the referenced ConfigMap is read from the same namespace.
                      For AIMClusterProfileSet, it is read from the operator namespace.
                    properties:
                      name:
                        description: Name is the discovery cache ConfigMap name.
                        type: string
                    required:
                    - name
                    type: object
                  version:
                    description: Version pins matching to a specific source profile
                      version when VersionPolicy is pinned.
                    type: string
                  versionPolicy:
                    default: pinned
                    description: VersionPolicy controls how matching profiles are
                      filtered by version.
                    enum:
                    - pinned
                    - latest
                    - all
                    type: string
                type: object
                x-kubernetes-validations:
                - message: selector must set at least one matching field, or set sourceRef
                  rule: has(self.sourceRef) || has(self.selector.aimId) || has(self.selector.modelId)
                    || has(self.selector.profileId) || has(self.selector.engine) ||
                    has(self.selector.metric) || has(self.selector.precision) || has(self.selector.type)
                    || has(self.selector.acceleratorModel) || has(self.selector.acceleratorType)
                    || has(self.selector.acceleratorCount) || has(self.selector.engineArgs)
                    || has(self.selector.modelRef) || has(self.selector.origin)
                - message: version is required when versionPolicy is pinned
                  rule: has(self.version) || (has(self.versionPolicy) && self.versionPolicy
                    != 'pinned')
                - message: latest/all versionPolicy must not set version
                  rule: '!has(self.versionPolicy) || (self.versionPolicy != ''latest''
                    && self.versionPolicy != ''all'') || !has(self.version)'
                - message: selector.aimId/modelId/profileId are not allowed when selector.role=base;
                    set overrides.aimId/modelId/profileId instead (base profiles have
                    no source identity to filter on)
                  rule: '!(has(self.selector.role) && self.selector.role == ''base'')
                    || (!has(self.selector.aimId) && !has(self.selector.modelId) &&
                    !has(self.selector.profileId))'
                - message: selector.role=base requires overrides.aimId and overrides.modelId
                    (base profiles carry no identity of their own; overrides supply
                    it)
                  rule: '!(has(self.selector.role) && self.selector.role == ''base'')
                    || (has(self.overrides) && has(self.overrides.aimId) && has(self.overrides.modelId))'
                - message: overrides.acceleratorCount must be specified together with
                    any overrides.acceleratorPartitioningMode; partition mode changes
                    the per-unit interpretation of acceleratorCount
                  rule: '!has(self.overrides) || !has(self.overrides.acceleratorPartitioningMode)
                    || size(self.overrides.acceleratorPartitioningMode) == 0 || has(self.overrides.acceleratorCount)'
              discovery:
                description: |-
                  Discovery controls discovery behavior for this model.
                  When unset, uses runtime config defaults.
                properties:
                  createServiceTemplates:
                    default: true
                    description: CreateServiceTemplates controls whether (cluster)
                      service templates are auto-created from the image metadata.
                    type: boolean
                  extractMetadata:
                    default: true
                    description: |-
                      ExtractMetadata controls whether metadata extraction runs for this model.
                      During metadata extraction, the controller connects to the image registry and
                      extracts the image's labels.
                    type: boolean
                type: object
              env:
                description: |-
                  Env specifies environment variables for authentication during model discovery and metadata extraction.
                  These variables are used for authentication with model registries (e.g., HuggingFace tokens).
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: |-
                        Name of the environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        fileKeyRef:
                          description: |-
                            FileKeyRef selects a key of the env file.
                            Requires the EnvFiles feature gate to be enabled.
                          properties:
                            key:
                              description: |-
                                The key within the env file. An invalid key will prevent the pod from starting.
                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                              type: string
                            optional:
                              default: false
                              description: |-
                                Specify whether the file or its key must be defined. If the file or key
                                does not exist, then the env var is not published.
                                If optional is set to true and the specified key does not exist,
                                the environment variable will not be set in the Pod's containers.

                                If optional is set to false and the specified key does not exist,
                                an error will be returned during Pod creation.
                              type: boolean
                            path:
                              description: |-
                                The path within the volume from which to select the file.
                                Must be relative and may not contain the '..' path or start with '..'.
                              type: string
                            volumeName:
                              description: The name of the volume mount containing
                                the env file.
                              type: string
                          required:
                          - key
                          - path
                          - volumeName
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              image:
                description: |-
                  Image is the container image URI for this AIM model.
                  This image is inspected by the operator to select runtime profiles used by templates.
                  Discovery behavior is controlled by the discovery field and runtime config's AutoDiscovery setting.
                  Required unless aimId is set with versionPolicy latest or any, or profileCopy is set.
                type: string
              imageMetadata:
                description: |-
                  ImageMetadata is the metadata that is used to determine which recommended service templates to create,
                  and to drive clients with richer metadata regarding this particular model. For most cases the user does
                  not need to set this field manually, for images that have the supported labels embedded in them
                  the `AIM(Cluster)Model.status.imageMetadata` field is automatically filled from the container image labels.
                  This field is intended to be used when there are network restrictions, or in other similar situations.
                  If this field is set, the remote extraction will not be performed at all.
                properties:
                  baseImageRef:
                    description: |-
                      BaseImageRef is the value of the AIM_BASE_IMAGE_REF environment variable
                      baked into the image's OCI config at build time. For AIM model images this
                      records the base image (e.g. "ghcr.io/silogen/aim-base:0.8.5") that the
                      model image was built from. Used by the AIMModel controller to resolve
                      the deployment image for fine-tuned models whose spec.image is omitted
                      (versionPolicy=latest or any).
                    type: string
                  model:
                    description: Model contains AMD Silogen model-specific metadata.
                    properties:
                      canonicalName:
                        description: |-
                          CanonicalName is the canonical model identifier (e.g., mistralai/Mixtral-8x22B-Instruct-v0.1).
                          Extracted from: org.amd.silogen.model.canonicalName
                        type: string
                      descriptionFull:
                        description: |-
                          DescriptionFull is the full description.
                          Extracted from: org.amd.silogen.description.full
                        type: string
                      hfTokenRequired:
                        description: |-
                          HFTokenRequired indicates if a HuggingFace token is required.
                          Extracted from: org.amd.silogen.hfToken.required
                        type: boolean
                      recommendedDeployments:
                        description: |-
                          RecommendedDeployments contains recommended deployment configurations.
                          Extracted from: org.amd.silogen.model.recommendedDeployments (parsed from JSON array)
                        items:
                          description: RecommendedDeployment describes a recommended
                            deployment configuration for a model.
                          properties:
                            description:
                              description: Description provides additional context
                                about this deployment configuration
                              type: string
                            gpuCount:
                              description: GPUCount is the number of GPUs required
                              format: int32
                              type: integer
                            gpuModel:
                              description: |-
                                GPUModel is the GPU model name (e.g., MI300X, MI325X).
                                The legacy v1alpha1 schema only models GPU accelerators; CPU profiles
                                emitted via OCI labels (gpuModel="CPU") are not first-class here and
                                are handled exclusively by the v1alpha2 native discovery pipeline.
                              type: string
                            metric:
                              description: Metric is the optimization target (e.g.,
                                latency, throughput)
                              type: string
                            precision:
                              description: Precision is the recommended precision
                                (e.g., fp8, fp16, bf16)
                              type: string
                            profileId:
                              description: |-
                                ProfileId is the unique identifier of the AIM profile for this deployment.
                                When set, templates created from this deployment will use this profile ID
                                to deterministically select the correct runtime profile in the AIM container.
                              type: string
                          type: object
                        type: array
                      releaseNotes:
                        description: |-
                          ReleaseNotes contains release notes for this version.
                          Extracted from: org.amd.silogen.release.notes
                        type: string
                      source:
                        description: |-
                          Source is the URL where the model can be found.
                          Extracted from: org.amd.silogen.model.source
                        type: string
                      tags:
                        description: |-
                          Tags are descriptive tags (e.g., ["text-generation", "chat", "instruction"]).
                          Extracted from: org.amd.silogen.model.tags (comma-separated)
                        items:
                          type: string
                        type: array
                      title:
                        description: |-
                          Title is the Silogen-specific title for the model.
                          Extracted from: org.amd.silogen.title
                        type: string
                      variants:
                        description: |-
                          Variants lists model variants.
                          Extracted from: org.amd.silogen.model.variants (comma-separated)
                        items:
                          type: string
                        type: array
                      versions:
                        description: |-
                          Versions lists available versions.
                          Extracted from: org.amd.silogen.model.versions (comma-separated)
                        items:
                          type: string
                        type: array
                    type: object
                  oci:
                    description: OCI contains standard OCI image metadata.
                    properties:
                      authors:
                        description: |-
                          Authors is contact details of the authors.
                          Extracted from: org.opencontainers.image.authors
                        type: string
                      created:
                        description: |-
                          Created is the creation timestamp.
                          Extracted from: org.opencontainers.image.created
                        type: string
                      description:
                        description: |-
                          Description is a brief description.
                          Extracted from: org.opencontainers.image.description
                        type: string
                      documentation:
                        description: |-
                          Documentation is the URL to documentation.
                          Extracted from: org.opencontainers.image.documentation
                        type: string
                      licenses:
                        description: |-
                          Licenses is the SPDX license identifier(s).
                          Extracted from: org.opencontainers.image.licenses
                        type: string
                      revision:
                        description: |-
                          Revision is the source control revision.
                          Extracted from: org.opencontainers.image.revision
                        type: string
                      source:
                        description: |-
                          Source is the URL to the source code repository.
                          Extracted from: org.opencontainers.image.source
                        type: string
                      title:
                        description: |-
                          Title is the human-readable title.
                          Extracted from: org.opencontainers.image.title
                        type: string
                      vendor:
                        description: |-
                          Vendor is the organization that produced the image.
                          Extracted from: org.opencontainers.image.vendor
                        type: string
                      version:
                        description: |-
                          Version is the image version.
                          Extracted from: org.opencontainers.image.version
                        type: string
                    type: object
                  originalLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      OriginalLabels contains the raw OCI image labels as a JSON object.
                      This preserves all labels from the image, including those not mapped to structured fields.
                    type: object
                type: object
              imagePullSecrets:
                description: |-
                  ImagePullSecrets lists secrets containing credentials for pulling the model container image.
                  These secrets are used for:
                  - OCI registry metadata extraction during discovery
                  - Pulling the image for inference services
                  The secrets are merged with any runtime config defaults.
                  For namespace-scoped models, secrets must exist in the same namespace.
                  For cluster-scoped models, secrets must exist in the operator namespace.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              modelSources:
                description: |-
                  ModelSources specifies the model sources to use for this model.
                  When specified, these sources are used instead of auto-discovery from the container image.
                  This enables pre-creating custom models with explicit model sources.
                  The size field is optional - if not specified, it will be discovered by the download job.
                  AIM runtime currently supports only one model source.
                items:
                  description: |-
                    AIMModelSource describes a model artifact that must be downloaded for inference.
                    Discovery extracts these from the container's configuration to enable caching and validation.
                  properties:
                    env:
                      description: |-
                        Env specifies per-source credential overrides.
                        These variables are used for authentication when downloading this specific source.
                        Takes precedence over base-level env for the same variable name.
                      items:
                        description: EnvVar represents an environment variable present
                          in a Container.
                        properties:
                          name:
                            description: |-
                              Name of the environment variable.
                              May consist of any printable ASCII characters except '='.
                            type: string
                          value:
                            description: |-
                              Variable references $(VAR_NAME) are expanded
                              using the previously defined environment variables in the container and
                              any service environment variables. If a variable cannot be resolved,
                              the reference in the input string will be unchanged. Double $$ are reduced
                              to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                              "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                              Escaped references will never be expanded, regardless of whether the variable
                              exists or not.
                              Defaults to "".
                            type: string
                          valueFrom:
                            description: Source for the environment variable's value.
                              Cannot be used if value is not empty.
                            properties:
                              configMapKeyRef:
                                description: Selects a key of a ConfigMap.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the ConfigMap or
                                      its key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              fieldRef:
                                description: |-
                                  Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                  spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                properties:
                                  apiVersion:
                                    description: Version of the schema the FieldPath
                                      is written in terms of, defaults to "v1".
                                    type: string
                                  fieldPath:
                                    description: Path of the field to select in the
                                      specified API version.
                                    type: string
                                required:
                                - fieldPath
                                type: object
                                x-kubernetes-map-type: atomic
                              fileKeyRef:
                                description: |-
                                  FileKeyRef selects a key of the env file.
                                  Requires the EnvFiles feature gate to be enabled.
                                properties:
                                  key:
                                    description: |-
                                      The key within the env file. An invalid key will prevent the pod from starting.
                                      The keys defined within a source may consist of any printable ASCII characters except '='.
                                      During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                    type: string
                                  optional:
                                    default: false
                                    description: |-
                                      Specify whether the file or its key must be defined. If the file or key
                                      does not exist, then the env var is not published.
                                      If optional is set to true and the specified key does not exist,
                                      the environment variable will not be set in the Pod's containers.

                                      If optional is set to false and the specified key does not exist,
                                      an error will be returned during Pod creation.
                                    type: boolean
                                  path:
                                    description: |-
                                      The path within the volume from which to select the file.
                                      Must be relative and may not contain the '..' path or start with '..'.
                                    type: string
                                  volumeName:
                                    description: The name of the volume mount containing
                                      the env file.
                                    type: string
                                required:
                                - key
                                - path
                                - volumeName
                                type: object
                                x-kubernetes-map-type: atomic
                              resourceFieldRef:
                                description: |-
                                  Selects a resource of the container: only resources limits and requests
                                  (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                properties:
                                  containerName:
                                    description: 'Container name: required for volumes,
                                      optional for env vars'
                                    type: string
                                  divisor:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: Specifies the output format of the
                                      exposed resources, defaults to "1"
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  resource:
                                    description: 'Required: resource to select'
                                    type: string
                                required:
                                - resource
                                type: object
                                x-kubernetes-map-type: atomic
                              secretKeyRef:
                                description: Selects a key of a secret in the pod's
                                  namespace
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                        required:
                        - name
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - name
                      x-kubernetes-list-type: map
                    modelId:
                      description: |-
                        ModelID is the canonical identifier in {org}/{name} format.
                        Determines the cache mount path: /workspace/cache/{modelId}
                        For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                        For S3 sources, users define their own organizational structure.
                      pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                      type: string
                    precision:
                      allOf:
                      - enum:
                        - auto
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                      - enum:
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                      description: |-
                        Precision describes the runtime precision this source is compatible with.
                        Used to match model sources to profiles during custom weight onboarding.
                      type: string
                    size:
                      anyOf:
                      - type: integer
                      - type: string
                      description: |-
                        Size is the expected storage space required for this model artifact.
                        Used for PVC sizing and capacity planning during cache creation.
                        Optional - if not specified, the download job will discover the size automatically.
                        Can be set explicitly to pre-allocate storage or override auto-discovery.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    sourceUri:
                      description: |-
                        SourceURI is the location from which the model should be downloaded.
                        Supported schemes:
                        - hf://org/model - Hugging Face Hub model
                        - s3://bucket/key - S3-compatible storage
                      pattern: ^(hf|s3)://[^ \t\r\n]+$
                      type: string
                  required:
                  - modelId
                  - sourceUri
                  type: object
                maxItems: 1
                type: array
              profileCopy:
                description: |-
                  ProfileCopy reuses the AIMProfileSet derivation shape so an AIMModel can
                  publish derivative AIMProfiles directly. The controller may synthesize a
                  child AIMProfileSet and fill SourceRef when image-backed discovery is
                  involved. Mutually exclusive with all deprecated v1alpha1 fields.

                  DEPRECATED on v1alpha2: use spec.profiles. v1alpha1 still accepts
                  ProfileCopy. v1alpha2 CRD CEL forbids ProfileCopy.
                properties:
                  image:
                    description: Image overrides the runtime image used by the derived
                      profiles.
                    type: string
                  imagePullSecrets:
                    description: ImagePullSecrets lists secrets used for inspecting
                      and pulling container images.
                    items:
                      description: |-
                        LocalObjectReference contains enough information to let you locate the
                        referenced object inside the same namespace.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    type: array
                  overrides:
                    description: Overrides mutates the copied profile spec after selection
                      and version filtering.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount replaces the copied profile's
                          acceleratorCount.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel replaces the copied profile's
                          acceleratorModel.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode replaces the copied profile's
                          acceleratorPartitioningMode. Complete replacement, not a merge; an empty
                          override string leaves the source profile's mode untouched. Same reserved
                          values as AIMProfileSpecCommon.AcceleratorPartitioningMode. Whenever this
                          override is set (to any value, including "unpartitioned"), the CEL rule on the
                          enclosing spec also requires AcceleratorCount to be set: partition mode
                          changes the per-unit interpretation of acceleratorCount, and CEL cannot
                          read the base profile to tell whether the meaning actually changed, so it
                          conservatively requires the count be restated.
                        type: string
                      aimId:
                        description: |-
                          AimId stamps the derived profile's spec.aimId. When set, wins over
                          the source profile's aimId. REQUIRED when the enclosing selector has
                          role=base (the source base profile carries no aimId of its own).
                        type: string
                      containerEnv:
                        description: ContainerEnv merges by env var name, overriding
                          matching source entries.
                        items:
                          description: EnvVar represents an environment variable present
                            in a Container.
                          properties:
                            name:
                              description: |-
                                Name of the environment variable.
                                May consist of any printable ASCII characters except '='.
                              type: string
                            value:
                              description: |-
                                Variable references $(VAR_NAME) are expanded
                                using the previously defined environment variables in the container and
                                any service environment variables. If a variable cannot be resolved,
                                the reference in the input string will be unchanged. Double $$ are reduced
                                to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                Escaped references will never be expanded, regardless of whether the variable
                                exists or not.
                                Defaults to "".
                              type: string
                            valueFrom:
                              description: Source for the environment variable's value.
                                Cannot be used if value is not empty.
                              properties:
                                configMapKeyRef:
                                  description: Selects a key of a ConfigMap.
                                  properties:
                                    key:
                                      description: The key to select.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the ConfigMap or
                                        its key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fieldRef:
                                  description: |-
                                    Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                    spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                  properties:
                                    apiVersion:
                                      description: Version of the schema the FieldPath
                                        is written in terms of, defaults to "v1".
                                      type: string
                                    fieldPath:
                                      description: Path of the field to select in
                                        the specified API version.
                                      type: string
                                  required:
                                  - fieldPath
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fileKeyRef:
                                  description: |-
                                    FileKeyRef selects a key of the env file.
                                    Requires the EnvFiles feature gate to be enabled.
                                  properties:
                                    key:
                                      description: |-
                                        The key within the env file. An invalid key will prevent the pod from starting.
                                        The keys defined within a source may consist of any printable ASCII characters except '='.
                                        During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                      type: string
                                    optional:
                                      default: false
                                      description: |-
                                        Specify whether the file or its key must be defined. If the file or key
                                        does not exist, then the env var is not published.
                                        If optional is set to true and the specified key does not exist,
                                        the environment variable will not be set in the Pod's containers.

                                        If optional is set to false and the specified key does not exist,
                                        an error will be returned during Pod creation.
                                      type: boolean
                                    path:
                                      description: |-
                                        The path within the volume from which to select the file.
                                        Must be relative and may not contain the '..' path or start with '..'.
                                      type: string
                                    volumeName:
                                      description: The name of the volume mount containing
                                        the env file.
                                      type: string
                                  required:
                                  - key
                                  - path
                                  - volumeName
                                  type: object
                                  x-kubernetes-map-type: atomic
                                resourceFieldRef:
                                  description: |-
                                    Selects a resource of the container: only resources limits and requests
                                    (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                  properties:
                                    containerName:
                                      description: 'Container name: required for volumes,
                                        optional for env vars'
                                      type: string
                                    divisor:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      description: Specifies the output format of
                                        the exposed resources, defaults to "1"
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    resource:
                                      description: 'Required: resource to select'
                                      type: string
                                  required:
                                  - resource
                                  type: object
                                  x-kubernetes-map-type: atomic
                                secretKeyRef:
                                  description: Selects a key of a secret in the pod's
                                    namespace
                                  properties:
                                    key:
                                      description: The key of the secret to select
                                        from.  Must be a valid secret key.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the Secret or its
                                        key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      engineArgs:
                        description: EngineArgs shallow-merges on top of the source
                          engineArgs, overriding matching keys.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      engineEnv:
                        additionalProperties:
                          type: string
                        description: EngineEnv merges by key, overriding matching
                          source entries.
                        type: object
                      image:
                        description: |-
                          Image overrides the runtime container image used by the derived
                          profiles. When empty the deployment image is rebased onto the source
                          profile's base-image (status.baseImage) so private mirrors stay
                          self-contained.
                        type: string
                      modelId:
                        description: |-
                          ModelId stamps the derived profile's spec.modelId. When set, wins
                          over both the source profile's modelId AND the auto-derivation from
                          modelSources[0].modelId. REQUIRED when the enclosing selector has
                          role=base.
                        type: string
                      modelSources:
                        description: |-
                          ModelSources replaces the copied profile's modelSources. When
                          modelSources[0].modelId is set and overrides.modelId is unset, the
                          derived profile's modelId is auto-derived from modelSources[0]; an
                          explicit overrides.modelId always wins.
                        items:
                          description: |-
                            AIMModelSource describes a model artifact that must be downloaded for inference.
                            Discovery extracts these from the container's configuration to enable caching and validation.
                          properties:
                            env:
                              description: |-
                                Env specifies per-source credential overrides.
                                These variables are used for authentication when downloading this specific source.
                                Takes precedence over base-level env for the same variable name.
                              items:
                                description: EnvVar represents an environment variable
                                  present in a Container.
                                properties:
                                  name:
                                    description: |-
                                      Name of the environment variable.
                                      May consist of any printable ASCII characters except '='.
                                    type: string
                                  value:
                                    description: |-
                                      Variable references $(VAR_NAME) are expanded
                                      using the previously defined environment variables in the container and
                                      any service environment variables. If a variable cannot be resolved,
                                      the reference in the input string will be unchanged. Double $$ are reduced
                                      to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                      "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                      Escaped references will never be expanded, regardless of whether the variable
                                      exists or not.
                                      Defaults to "".
                                    type: string
                                  valueFrom:
                                    description: Source for the environment variable's
                                      value. Cannot be used if value is not empty.
                                    properties:
                                      configMapKeyRef:
                                        description: Selects a key of a ConfigMap.
                                        properties:
                                          key:
                                            description: The key to select.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the ConfigMap
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fieldRef:
                                        description: |-
                                          Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                          spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                        properties:
                                          apiVersion:
                                            description: Version of the schema the
                                              FieldPath is written in terms of, defaults
                                              to "v1".
                                            type: string
                                          fieldPath:
                                            description: Path of the field to select
                                              in the specified API version.
                                            type: string
                                        required:
                                        - fieldPath
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fileKeyRef:
                                        description: |-
                                          FileKeyRef selects a key of the env file.
                                          Requires the EnvFiles feature gate to be enabled.
                                        properties:
                                          key:
                                            description: |-
                                              The key within the env file. An invalid key will prevent the pod from starting.
                                              The keys defined within a source may consist of any printable ASCII characters except '='.
                                              During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                            type: string
                                          optional:
                                            default: false
                                            description: |-
                                              Specify whether the file or its key must be defined. If the file or key
                                              does not exist, then the env var is not published.
                                              If optional is set to true and the specified key does not exist,
                                              the environment variable will not be set in the Pod's containers.

                                              If optional is set to false and the specified key does not exist,
                                              an error will be returned during Pod creation.
                                            type: boolean
                                          path:
                                            description: |-
                                              The path within the volume from which to select the file.
                                              Must be relative and may not contain the '..' path or start with '..'.
                                            type: string
                                          volumeName:
                                            description: The name of the volume mount
                                              containing the env file.
                                            type: string
                                        required:
                                        - key
                                        - path
                                        - volumeName
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      resourceFieldRef:
                                        description: |-
                                          Selects a resource of the container: only resources limits and requests
                                          (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                        properties:
                                          containerName:
                                            description: 'Container name: required
                                              for volumes, optional for env vars'
                                            type: string
                                          divisor:
                                            anyOf:
                                            - type: integer
                                            - type: string
                                            description: Specifies the output format
                                              of the exposed resources, defaults to
                                              "1"
                                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                            x-kubernetes-int-or-string: true
                                          resource:
                                            description: 'Required: resource to select'
                                            type: string
                                        required:
                                        - resource
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      secretKeyRef:
                                        description: Selects a key of a secret in
                                          the pod's namespace
                                        properties:
                                          key:
                                            description: The key of the secret to
                                              select from.  Must be a valid secret
                                              key.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the Secret
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                    type: object
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            modelId:
                              description: |-
                                ModelID is the canonical identifier in {org}/{name} format.
                                Determines the cache mount path: /workspace/cache/{modelId}
                                For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                                For S3 sources, users define their own organizational structure.
                              pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                              type: string
                            precision:
                              allOf:
                              - enum:
                                - auto
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              - enum:
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              description: |-
                                Precision describes the runtime precision this source is compatible with.
                                Used to match model sources to profiles during custom weight onboarding.
                              type: string
                            size:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                Size is the expected storage space required for this model artifact.
                                Used for PVC sizing and capacity planning during cache creation.
                                Optional - if not specified, the download job will discover the size automatically.
                                Can be set explicitly to pre-allocate storage or override auto-discovery.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            sourceUri:
                              description: |-
                                SourceURI is the location from which the model should be downloaded.
                                Supported schemes:
                                - hf://org/model - Hugging Face Hub model
                                - s3://bucket/key - S3-compatible storage
                              pattern: ^(hf|s3)://[^ \t\r\n]+$
                              type: string
                          required:
                          - modelId
                          - sourceUri
                          type: object
                        type: array
                      profileId:
                        description: |-
                          ProfileId stamps the derived profile's spec.profileId. Optional —
                          most callers leave this empty and let the source profile's profileId
                          carry through (or the reconciler synthesise one).
                        type: string
                    type: object
                  selector:
                    description: Selector chooses which source profiles to derive
                      from.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount filters by accelerator unit
                          count.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel filters by accelerator identifier.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode filters candidates by their declared
                          partitioning mode. Partial-order match (NOT strict equality):
                            ""              - no filter on this field.
                            "unpartitioned" - matches profiles with mode "" or "unpartitioned".
                            "partitioned"   - matches profiles whose mode is non-trivial (anything
                                              other than "" / "unpartitioned").
                            "<C>"           - selector-only convenience: matches profiles with mode
                                              "<C>-*" (prefix on the scheme). Not a valid profile-spec
                                              value (e.g. selector "CPX" matches "CPX-NPS1", "CPX-NPS4").
                            "<C>-<M>"       - exact-string match on the scheme.
                        type: string
                      acceleratorType:
                        description: AcceleratorType filters by accelerator resource
                          type.
                        enum:
                        - gpu
                        - cpu
                        type: string
                      aimId:
                        description: AimId filters by model architecture identifier
                          (e.g., "qwen/qwen3-32b").
                        type: string
                      engine:
                        description: Engine filters by inference engine.
                        type: string
                      engineArgs:
                        description: |-
                          EngineArgs partially matches source engineArgs: every provided top-level key must
                          exist in the source object with an equal value.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      metric:
                        description: Metric filters by optimization target.
                        enum:
                        - latency
                        - throughput
                        type: string
                      minimumType:
                        description: |-
                          MinimumType filters by a minimum optimization level: candidates whose
                          type is this tier OR BETTER are accepted (hierarchy: optimized > general
                          > preview > unoptimized). This is the floor counterpart to the exact-match
                          Type field; the two AND together when both are set.

                          The sentinel "any" disables the floor (accept every tier). When this
                          field is empty the AIMService resolver applies a default floor of
                          "optimized" so auto-selection prefers production-grade profiles and never
                          silently picks an unoptimized one; to opt a service into lower tiers
                          (e.g. CPU/EPYC profiles published as unoptimized) set minimumType
                          explicitly to "unoptimized" or "any". Derivation selectors
                          (AIMProfileSet / AIMModel.profiles) treat empty as "any" so copying is
                          never tier-restricted by default.
                        enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                        - any
                        type: string
                      modelId:
                        description: ModelId filters by the source profile's specific
                          model identifier.
                        type: string
                      modelRef:
                        description: |-
                          ModelRef narrows candidates to those produced by a specific
                          AIM(Cluster)Model, matched via the `aim.eai.amd.com/source-model[-scope]`
                          labels stamped by the AIMModel reconcilers. Iteration 1 (v1alpha2 only).
                        properties:
                          name:
                            description: Name is the owning AIM(Cluster)Model name.
                              Required.
                            minLength: 1
                            type: string
                          scope:
                            default: Auto
                            description: Scope controls how Name is resolved against
                              AIMModel vs AIMClusterModel.
                            enum:
                            - Auto
                            - Namespace
                            - Cluster
                            type: string
                        required:
                        - name
                        type: object
                      origin:
                        description: |-
                          Origin filters by the `aim.eai.amd.com/profile-origin` label. When unset
                          (empty) the selector does not filter by origin. Iteration 1 (v1alpha2
                          only).
                        enum:
                        - discovered
                        - derived
                        - user-authored
                        type: string
                      precision:
                        description: Precision filters by numeric precision.
                        enum:
                        - auto
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                        type: string
                      profileId:
                        description: ProfileId filters by the source profile's profile
                          identifier.
                        type: string
                      role:
                        default: deployable
                        description: |-
                          Role filters by the `aim.eai.amd.com/profile-role` label. Defaults to
                          `deployable`. `base` filters to base profiles emitted by base-image
                          discovery (custom-model derivation source material).
                        enum:
                        - base
                        - deployable
                        type: string
                      type:
                        description: Type filters by optimization level (exact match).
                        enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                        type: string
                    type: object
                  serviceAccountName:
                    description: ServiceAccountName is propagated to managed profiles
                      for downstream workloads.
                    type: string
                  sourceRef:
                    description: |-
                      SourceRef points to an alternate discovery cache source.
                      When omitted, derivation uses visible AIMProfile and AIMClusterProfile objects.
                      For AIMProfileSet, the referenced ConfigMap is read from the same namespace.
                      For AIMClusterProfileSet, it is read from the operator namespace.
                    properties:
                      name:
                        description: Name is the discovery cache ConfigMap name.
                        type: string
                    required:
                    - name
                    type: object
                  version:
                    description: Version pins matching to a specific source profile
                      version when VersionPolicy is pinned.
                    type: string
                  versionPolicy:
                    default: pinned
                    description: VersionPolicy controls how matching profiles are
                      filtered by version.
                    enum:
                    - pinned
                    - latest
                    - all
                    type: string
                type: object
                x-kubernetes-validations:
                - message: selector must set at least one matching field, or set sourceRef
                  rule: has(self.sourceRef) || has(self.selector.aimId) || has(self.selector.modelId)
                    || has(self.selector.profileId) || has(self.selector.engine) ||
                    has(self.selector.metric) || has(self.selector.precision) || has(self.selector.type)
                    || has(self.selector.acceleratorModel) || has(self.selector.acceleratorType)
                    || has(self.selector.acceleratorCount) || has(self.selector.engineArgs)
                    || has(self.selector.modelRef) || has(self.selector.origin)
                - message: version is required when versionPolicy is pinned
                  rule: has(self.version) || (has(self.versionPolicy) && self.versionPolicy
                    != 'pinned')
                - message: latest/all versionPolicy must not set version
                  rule: '!has(self.versionPolicy) || (self.versionPolicy != ''latest''
                    && self.versionPolicy != ''all'') || !has(self.version)'
                - message: selector.aimId/modelId/profileId are not allowed when selector.role=base;
                    set overrides.aimId/modelId/profileId instead (base profiles have
                    no source identity to filter on)
                  rule: '!(has(self.selector.role) && self.selector.role == ''base'')
                    || (!has(self.selector.aimId) && !has(self.selector.modelId) &&
                    !has(self.selector.profileId))'
                - message: selector.role=base requires overrides.aimId and overrides.modelId
                    (base profiles carry no identity of their own; overrides supply
                    it)
                  rule: '!(has(self.selector.role) && self.selector.role == ''base'')
                    || (has(self.overrides) && has(self.overrides.aimId) && has(self.overrides.modelId))'
                - message: overrides.acceleratorCount must be specified together with
                    any overrides.acceleratorPartitioningMode; partition mode changes
                    the per-unit interpretation of acceleratorCount
                  rule: '!has(self.overrides) || !has(self.overrides.acceleratorPartitioningMode)
                    || size(self.overrides.acceleratorPartitioningMode) == 0 || has(self.overrides.acceleratorCount)'
              profiles:
                description: |-
                  Profiles is the v1alpha2 fine-tune / custom-model onboarding surface.
                  It groups the source descriptor (`profiles.derivedFrom.selector` /
                  `profiles.derivedFrom.sourceRef`), the version filter
                  (`profiles.versionPolicy` / `profiles.version`), and the modifications
                  applied to copies (`profiles.overrides`). When set, the AIMModel
                  reconciler synthesises a child AIMProfileSet from this block.

                  Mutually exclusive with spec.image (exactly one of the two is required
                  for v1alpha2 AIMModel). v1alpha1 rejects spec.profiles via per-version
                  CEL.
                properties:
                  derivedFrom:
                    description: DerivedFrom identifies the source profiles to copy
                      from.
                    properties:
                      selector:
                        description: |-
                          Selector chooses which source profiles to derive from. Discovery of a
                          deployable AIM image stamps role=deployable; selector.role=base targets
                          the base profiles emitted by base-image discovery (custom-model
                          derivation source material).
                        properties:
                          acceleratorCount:
                            description: AcceleratorCount filters by accelerator unit
                              count.
                            format: int32
                            type: integer
                          acceleratorModel:
                            description: AcceleratorModel filters by accelerator identifier.
                            type: string
                          acceleratorPartitioningMode:
                            description: |-
                              AcceleratorPartitioningMode filters candidates by their declared
                              partitioning mode. Partial-order match (NOT strict equality):
                                ""              - no filter on this field.
                                "unpartitioned" - matches profiles with mode "" or "unpartitioned".
                                "partitioned"   - matches profiles whose mode is non-trivial (anything
                                                  other than "" / "unpartitioned").
                                "<C>"           - selector-only convenience: matches profiles with mode
                                                  "<C>-*" (prefix on the scheme). Not a valid profile-spec
                                                  value (e.g. selector "CPX" matches "CPX-NPS1", "CPX-NPS4").
                                "<C>-<M>"       - exact-string match on the scheme.
                            type: string
                          acceleratorType:
                            description: AcceleratorType filters by accelerator resource
                              type.
                            enum:
                            - gpu
                            - cpu
                            type: string
                          aimId:
                            description: AimId filters by model architecture identifier
                              (e.g., "qwen/qwen3-32b").
                            type: string
                          engine:
                            description: Engine filters by inference engine.
                            type: string
                          engineArgs:
                            description: |-
                              EngineArgs partially matches source engineArgs: every provided top-level key must
                              exist in the source object with an equal value.
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          metric:
                            description: Metric filters by optimization target.
                            enum:
                            - latency
                            - throughput
                            type: string
                          minimumType:
                            description: |-
                              MinimumType filters by a minimum optimization level: candidates whose
                              type is this tier OR BETTER are accepted (hierarchy: optimized > general
                              > preview > unoptimized). This is the floor counterpart to the exact-match
                              Type field; the two AND together when both are set.

                              The sentinel "any" disables the floor (accept every tier). When this
                              field is empty the AIMService resolver applies a default floor of
                              "optimized" so auto-selection prefers production-grade profiles and never
                              silently picks an unoptimized one; to opt a service into lower tiers
                              (e.g. CPU/EPYC profiles published as unoptimized) set minimumType
                              explicitly to "unoptimized" or "any". Derivation selectors
                              (AIMProfileSet / AIMModel.profiles) treat empty as "any" so copying is
                              never tier-restricted by default.
                            enum:
                            - optimized
                            - general
                            - preview
                            - unoptimized
                            - any
                            type: string
                          modelId:
                            description: ModelId filters by the source profile's specific
                              model identifier.
                            type: string
                          modelRef:
                            description: |-
                              ModelRef narrows candidates to those produced by a specific
                              AIM(Cluster)Model, matched via the `aim.eai.amd.com/source-model[-scope]`
                              labels stamped by the AIMModel reconcilers. Iteration 1 (v1alpha2 only).
                            properties:
                              name:
                                description: Name is the owning AIM(Cluster)Model
                                  name. Required.
                                minLength: 1
                                type: string
                              scope:
                                default: Auto
                                description: Scope controls how Name is resolved against
                                  AIMModel vs AIMClusterModel.
                                enum:
                                - Auto
                                - Namespace
                                - Cluster
                                type: string
                            required:
                            - name
                            type: object
                          origin:
                            description: |-
                              Origin filters by the `aim.eai.amd.com/profile-origin` label. When unset
                              (empty) the selector does not filter by origin. Iteration 1 (v1alpha2
                              only).
                            enum:
                            - discovered
                            - derived
                            - user-authored
                            type: string
                          precision:
                            description: Precision filters by numeric precision.
                            enum:
                            - auto
                            - fp4
                            - fp8
                            - fp16
                            - fp32
                            - bf16
                            - int4
                            - int8
                            type: string
                          profileId:
                            description: ProfileId filters by the source profile's
                              profile identifier.
                            type: string
                          role:
                            default: deployable
                            description: |-
                              Role filters by the `aim.eai.amd.com/profile-role` label. Defaults to
                              `deployable`. `base` filters to base profiles emitted by base-image
                              discovery (custom-model derivation source material).
                            enum:
                            - base
                            - deployable
                            type: string
                          type:
                            description: Type filters by optimization level (exact
                              match).
                            enum:
                            - optimized
                            - general
                            - preview
                            - unoptimized
                            type: string
                        type: object
                      sourceRef:
                        description: |-
                          SourceRef points to an alternate discovery cache source (a
                          pre-populated ConfigMap of profile YAMLs) instead of using the
                          visible AIMProfile / AIMClusterProfile objects.
                        properties:
                          name:
                            description: Name is the discovery cache ConfigMap name.
                            type: string
                        required:
                        - name
                        type: object
                    type: object
                  overrides:
                    description: |-
                      Overrides mutates the copied profile spec after selection and version
                      filtering. Use overrides.image to override the deployment container
                      image used by the derived profiles.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount replaces the copied profile's
                          acceleratorCount.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel replaces the copied profile's
                          acceleratorModel.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode replaces the copied profile's
                          acceleratorPartitioningMode. Complete replacement, not a merge; an empty
                          override string leaves the source profile's mode untouched. Same reserved
                          values as AIMProfileSpecCommon.AcceleratorPartitioningMode. Whenever this
                          override is set (to any value, including "unpartitioned"), the CEL rule on the
                          enclosing spec also requires AcceleratorCount to be set: partition mode
                          changes the per-unit interpretation of acceleratorCount, and CEL cannot
                          read the base profile to tell whether the meaning actually changed, so it
                          conservatively requires the count be restated.
                        type: string
                      aimId:
                        description: |-
                          AimId stamps the derived profile's spec.aimId. When set, wins over
                          the source profile's aimId. REQUIRED when the enclosing selector has
                          role=base (the source base profile carries no aimId of its own).
                        type: string
                      containerEnv:
                        description: ContainerEnv merges by env var name, overriding
                          matching source entries.
                        items:
                          description: EnvVar represents an environment variable present
                            in a Container.
                          properties:
                            name:
                              description: |-
                                Name of the environment variable.
                                May consist of any printable ASCII characters except '='.
                              type: string
                            value:
                              description: |-
                                Variable references $(VAR_NAME) are expanded
                                using the previously defined environment variables in the container and
                                any service environment variables. If a variable cannot be resolved,
                                the reference in the input string will be unchanged. Double $$ are reduced
                                to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                Escaped references will never be expanded, regardless of whether the variable
                                exists or not.
                                Defaults to "".
                              type: string
                            valueFrom:
                              description: Source for the environment variable's value.
                                Cannot be used if value is not empty.
                              properties:
                                configMapKeyRef:
                                  description: Selects a key of a ConfigMap.
                                  properties:
                                    key:
                                      description: The key to select.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the ConfigMap or
                                        its key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fieldRef:
                                  description: |-
                                    Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                    spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                  properties:
                                    apiVersion:
                                      description: Version of the schema the FieldPath
                                        is written in terms of, defaults to "v1".
                                      type: string
                                    fieldPath:
                                      description: Path of the field to select in
                                        the specified API version.
                                      type: string
                                  required:
                                  - fieldPath
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fileKeyRef:
                                  description: |-
                                    FileKeyRef selects a key of the env file.
                                    Requires the EnvFiles feature gate to be enabled.
                                  properties:
                                    key:
                                      description: |-
                                        The key within the env file. An invalid key will prevent the pod from starting.
                                        The keys defined within a source may consist of any printable ASCII characters except '='.
                                        During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                      type: string
                                    optional:
                                      default: false
                                      description: |-
                                        Specify whether the file or its key must be defined. If the file or key
                                        does not exist, then the env var is not published.
                                        If optional is set to true and the specified key does not exist,
                                        the environment variable will not be set in the Pod's containers.

                                        If optional is set to false and the specified key does not exist,
                                        an error will be returned during Pod creation.
                                      type: boolean
                                    path:
                                      description: |-
                                        The path within the volume from which to select the file.
                                        Must be relative and may not contain the '..' path or start with '..'.
                                      type: string
                                    volumeName:
                                      description: The name of the volume mount containing
                                        the env file.
                                      type: string
                                  required:
                                  - key
                                  - path
                                  - volumeName
                                  type: object
                                  x-kubernetes-map-type: atomic
                                resourceFieldRef:
                                  description: |-
                                    Selects a resource of the container: only resources limits and requests
                                    (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                  properties:
                                    containerName:
                                      description: 'Container name: required for volumes,
                                        optional for env vars'
                                      type: string
                                    divisor:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      description: Specifies the output format of
                                        the exposed resources, defaults to "1"
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    resource:
                                      description: 'Required: resource to select'
                                      type: string
                                  required:
                                  - resource
                                  type: object
                                  x-kubernetes-map-type: atomic
                                secretKeyRef:
                                  description: Selects a key of a secret in the pod's
                                    namespace
                                  properties:
                                    key:
                                      description: The key of the secret to select
                                        from.  Must be a valid secret key.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the Secret or its
                                        key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      engineArgs:
                        description: EngineArgs shallow-merges on top of the source
                          engineArgs, overriding matching keys.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      engineEnv:
                        additionalProperties:
                          type: string
                        description: EngineEnv merges by key, overriding matching
                          source entries.
                        type: object
                      image:
                        description: |-
                          Image overrides the runtime container image used by the derived
                          profiles. When empty the deployment image is rebased onto the source
                          profile's base-image (status.baseImage) so private mirrors stay
                          self-contained.
                        type: string
                      modelId:
                        description: |-
                          ModelId stamps the derived profile's spec.modelId. When set, wins
                          over both the source profile's modelId AND the auto-derivation from
                          modelSources[0].modelId. REQUIRED when the enclosing selector has
                          role=base.
                        type: string
                      modelSources:
                        description: |-
                          ModelSources replaces the copied profile's modelSources. When
                          modelSources[0].modelId is set and overrides.modelId is unset, the
                          derived profile's modelId is auto-derived from modelSources[0]; an
                          explicit overrides.modelId always wins.
                        items:
                          description: |-
                            AIMModelSource describes a model artifact that must be downloaded for inference.
                            Discovery extracts these from the container's configuration to enable caching and validation.
                          properties:
                            env:
                              description: |-
                                Env specifies per-source credential overrides.
                                These variables are used for authentication when downloading this specific source.
                                Takes precedence over base-level env for the same variable name.
                              items:
                                description: EnvVar represents an environment variable
                                  present in a Container.
                                properties:
                                  name:
                                    description: |-
                                      Name of the environment variable.
                                      May consist of any printable ASCII characters except '='.
                                    type: string
                                  value:
                                    description: |-
                                      Variable references $(VAR_NAME) are expanded
                                      using the previously defined environment variables in the container and
                                      any service environment variables. If a variable cannot be resolved,
                                      the reference in the input string will be unchanged. Double $$ are reduced
                                      to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                      "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                      Escaped references will never be expanded, regardless of whether the variable
                                      exists or not.
                                      Defaults to "".
                                    type: string
                                  valueFrom:
                                    description: Source for the environment variable's
                                      value. Cannot be used if value is not empty.
                                    properties:
                                      configMapKeyRef:
                                        description: Selects a key of a ConfigMap.
                                        properties:
                                          key:
                                            description: The key to select.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the ConfigMap
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fieldRef:
                                        description: |-
                                          Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                          spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                        properties:
                                          apiVersion:
                                            description: Version of the schema the
                                              FieldPath is written in terms of, defaults
                                              to "v1".
                                            type: string
                                          fieldPath:
                                            description: Path of the field to select
                                              in the specified API version.
                                            type: string
                                        required:
                                        - fieldPath
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fileKeyRef:
                                        description: |-
                                          FileKeyRef selects a key of the env file.
                                          Requires the EnvFiles feature gate to be enabled.
                                        properties:
                                          key:
                                            description: |-
                                              The key within the env file. An invalid key will prevent the pod from starting.
                                              The keys defined within a source may consist of any printable ASCII characters except '='.
                                              During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                            type: string
                                          optional:
                                            default: false
                                            description: |-
                                              Specify whether the file or its key must be defined. If the file or key
                                              does not exist, then the env var is not published.
                                              If optional is set to true and the specified key does not exist,
                                              the environment variable will not be set in the Pod's containers.

                                              If optional is set to false and the specified key does not exist,
                                              an error will be returned during Pod creation.
                                            type: boolean
                                          path:
                                            description: |-
                                              The path within the volume from which to select the file.
                                              Must be relative and may not contain the '..' path or start with '..'.
                                            type: string
                                          volumeName:
                                            description: The name of the volume mount
                                              containing the env file.
                                            type: string
                                        required:
                                        - key
                                        - path
                                        - volumeName
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      resourceFieldRef:
                                        description: |-
                                          Selects a resource of the container: only resources limits and requests
                                          (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                        properties:
                                          containerName:
                                            description: 'Container name: required
                                              for volumes, optional for env vars'
                                            type: string
                                          divisor:
                                            anyOf:
                                            - type: integer
                                            - type: string
                                            description: Specifies the output format
                                              of the exposed resources, defaults to
                                              "1"
                                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                            x-kubernetes-int-or-string: true
                                          resource:
                                            description: 'Required: resource to select'
                                            type: string
                                        required:
                                        - resource
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      secretKeyRef:
                                        description: Selects a key of a secret in
                                          the pod's namespace
                                        properties:
                                          key:
                                            description: The key of the secret to
                                              select from.  Must be a valid secret
                                              key.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the Secret
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                    type: object
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            modelId:
                              description: |-
                                ModelID is the canonical identifier in {org}/{name} format.
                                Determines the cache mount path: /workspace/cache/{modelId}
                                For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                                For S3 sources, users define their own organizational structure.
                              pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                              type: string
                            precision:
                              allOf:
                              - enum:
                                - auto
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              - enum:
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              description: |-
                                Precision describes the runtime precision this source is compatible with.
                                Used to match model sources to profiles during custom weight onboarding.
                              type: string
                            size:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                Size is the expected storage space required for this model artifact.
                                Used for PVC sizing and capacity planning during cache creation.
                                Optional - if not specified, the download job will discover the size automatically.
                                Can be set explicitly to pre-allocate storage or override auto-discovery.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            sourceUri:
                              description: |-
                                SourceURI is the location from which the model should be downloaded.
                                Supported schemes:
                                - hf://org/model - Hugging Face Hub model
                                - s3://bucket/key - S3-compatible storage
                              pattern: ^(hf|s3)://[^ \t\r\n]+$
                              type: string
                          required:
                          - modelId
                          - sourceUri
                          type: object
                        type: array
                      profileId:
                        description: |-
                          ProfileId stamps the derived profile's spec.profileId. Optional —
                          most callers leave this empty and let the source profile's profileId
                          carry through (or the reconciler synthesise one).
                        type: string
                    type: object
                  version:
                    description: |-
                      Version pins matching to a specific source profile version when
                      VersionPolicy is `pinned`.
                    type: string
                  versionPolicy:
                    default: pinned
                    description: VersionPolicy controls how matching profiles are
                      filtered by version.
                    enum:
                    - pinned
                    - latest
                    - all
                    type: string
                type: object
                x-kubernetes-validations:
                - message: profiles.derivedFrom is required when spec.profiles is
                    set
                  rule: has(self.derivedFrom)
                - message: profiles.version is required when versionPolicy is pinned
                  rule: has(self.version) || (has(self.versionPolicy) && self.versionPolicy
                    != 'pinned')
                - message: latest/all versionPolicy must not set version
                  rule: '!has(self.versionPolicy) || (self.versionPolicy != ''latest''
                    && self.versionPolicy != ''all'') || !has(self.version)'
                - message: derivedFrom.selector.aimId/modelId/profileId are not allowed
                    when selector.role=base; set overrides.aimId/modelId/profileId
                    instead (base profiles have no source identity to filter on)
                  rule: '!(has(self.derivedFrom.selector.role) && self.derivedFrom.selector.role
                    == ''base'') || (!has(self.derivedFrom.selector.aimId) && !has(self.derivedFrom.selector.modelId)
                    && !has(self.derivedFrom.selector.profileId))'
                - message: derivedFrom.selector.role=base requires overrides.aimId
                    and overrides.modelId (base profiles carry no identity of their
                    own; overrides supply it)
                  rule: '!(has(self.derivedFrom.selector.role) && self.derivedFrom.selector.role
                    == ''base'') || (has(self.overrides) && has(self.overrides.aimId)
                    && has(self.overrides.modelId))'
              resources:
                description: |-
                  Resources defines the default resource requirements for services using this model.
                  Template- or service-level values override these defaults.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              runtimeConfigName:
                description: |-
                  Name is the name of the runtime config to use for this resource. If a runtime config with this name exists both
                  as a namespace and a cluster runtime config, the values are merged together, the namespace config taking priority
                  over the cluster config when there are conflicts. If this field is empty or set to `default`, the namespace / cluster
                  runtime config with the name `default` is used, if it exists.
                type: string
              serviceAccountName:
                description: |-
                  ServiceAccountName specifies the Kubernetes service account to use for workloads related to this model.
                  This includes metadata extraction jobs and any other model-related operations.
                  If empty, the default service account for the namespace is used.
                type: string
            type: object
            x-kubernetes-validations:
            - message: when using modelSources without aimId, set custom.hardware
                or set hardware on each customTemplate
              rule: '!has(self.modelSources) || size(self.modelSources) == 0 || has(self.aimId)
                || (has(self.custom) && has(self.custom.hardware)) || !has(self.customTemplates)
                || size(self.customTemplates) == 0 || self.customTemplates.all(t,
                has(t.hardware) || (has(self.custom) && has(self.custom.hardware)))'
            - message: image is required unless aimId is set with versionPolicy latest
                or any, or profileCopy/derivedFrom/profiles is set
              rule: (has(self.image) && size(self.image) > 0) || has(self.profileCopy)
                || has(self.derivedFrom) || has(self.profiles) || (has(self.aimId)
                && has(self.custom) && has(self.custom.versionPolicy) && self.custom.versionPolicy
                != 'pinned')
            - message: profileCopy cannot be combined with deprecated legacy AIMModel
                fields
              rule: '!has(self.profileCopy) || (!has(self.discovery) && !has(self.defaultServiceTemplate)
                && !has(self.custom) && (!has(self.customTemplates) || size(self.customTemplates)
                == 0) && (!has(self.modelSources) || size(self.modelSources) == 0)
                && (!has(self.runtimeConfigName) || size(self.runtimeConfigName) ==
                0) && (!has(self.env) || size(self.env) == 0) && !has(self.imageMetadata))'
          status:
            description: AIMModelStatus defines the observed state of AIMModel.
            properties:
              aimId:
                description: |-
                  AimId is the resolved model architecture identifier for this model.
                  Populated by the v1alpha2 controller from spec.aimId or discovered metadata.
                type: string
              baseImage:
                description: |-
                  BaseImage is the extracted AIM base image reference (AIM_BASE_IMAGE_REF) when known.
                  Used when resolving deployment images for fine-tuned models that have no spec.image.
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the model's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              discoveredProfiles:
                description: DiscoveredProfiles summarizes the profiles found during
                  image discovery.
                properties:
                  byHardware:
                    description: |-
                      ByHardware groups discovered profiles by their hardware footprint and
                      reports whether each group is currently supported by the cluster. The
                      groups are stable across reconciles (sorted by acceleratorType,
                      acceleratorModel, acceleratorCount) so kubectl/jq queries are cheap.
                      Useful when debugging "why doesn't the {metric, precision} profile I
                      expect appear in my cluster?" — the breakdown surfaces every shape the
                      image emits, not only the ones materialised as AIMProfile objects.
                    items:
                      description: |-
                        ProfileHardwareGroup is one accelerator footprint within a model's
                        discovery catalog, with the metric/precision combos shipped under it.
                      properties:
                        acceleratorCount:
                          description: |-
                            AcceleratorCount is the number of accelerator units the profile
                            requests. For gpu: device count (e.g., 1, 2, 4, 8 for tensor-parallel
                            sizes). For cpu: number of CPU cores (e.g., 128 for EPYC_ZEN5,
                            192 for EPYC_9965).
                          format: int32
                          type: integer
                        acceleratorModel:
                          description: |-
                            AcceleratorModel is the accelerator identifier (e.g., "MI300X",
                            "EPYC_ZEN5"). Empty for profiles with no accelerator requirement.
                          type: string
                        acceleratorType:
                          description: AcceleratorType is the resource family (gpu,
                            cpu).
                          enum:
                          - gpu
                          - cpu
                          type: string
                        profiles:
                          description: |-
                            Profiles lists the {metric, precision} combinations discovered under
                            this hardware footprint. Reported even when the group is unsupported
                            so users can see what they're missing.
                          items:
                            description: |-
                              ProfileHardwareGroupEntry identifies one profile within a hardware group
                              by its {metric, precision} pair. Sufficient for users to spot whether the
                              optimization variant they want is shipped at all.
                            properties:
                              metric:
                                description: Metric is the optimization target (latency,
                                  throughput).
                                enum:
                                - latency
                                - throughput
                                type: string
                              precision:
                                description: Precision is the numeric precision (fp4,
                                  fp8, bf16, …).
                                enum:
                                - auto
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                                type: string
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        supported:
                          description: |-
                            Supported reports whether this hardware footprint is currently
                            satisfied by at least one cluster node. When false, all profiles in
                            this group are skipped during materialisation.
                          type: boolean
                      required:
                      - supported
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  supported:
                    description: Supported is the number of discovered profiles currently
                      supported by the cluster.
                    format: int32
                    type: integer
                  total:
                    description: Total is the number of profiles discovered from the
                      image.
                    format: int32
                    type: integer
                  unsupported:
                    description: Unsupported is the number of discovered profiles
                      currently not supported by the cluster.
                    format: int32
                    type: integer
                type: object
              discovery:
                description: Discovery tracks the state of the image-discovery Job
                  used to populate the discovery cache.
                properties:
                  attempts:
                    description: |-
                      Attempts is the number of discovery job attempts that have been made.
                      Increments each time a new discovery job is created after a failure.
                    format: int32
                    type: integer
                  lastAttemptTime:
                    description: |-
                      LastAttemptTime is the timestamp of the most recent discovery job creation.
                      Used to calculate exponential backoff before the next retry.
                    format: date-time
                    type: string
                  lastFailureReason:
                    description: LastFailureReason captures the reason for the most
                      recent discovery failure.
                    type: string
                  specHash:
                    description: |-
                      SpecHash is a hash of the model spec fields that invalidate cached discovery
                      (image, imagePullSecrets, serviceAccountName, discoveryCommandVersion).
                      When it changes, the operator drops the cache and re-runs the discovery Job.
                    type: string
                type: object
              discoveryCacheRef:
                description: |-
                  DiscoveryCacheRef points at the normalized discovery cache ConfigMap for image-backed flows.
                  Populated by the v1alpha2 controller after image inspection succeeds.
                properties:
                  name:
                    description: Name is the ConfigMap name.
                    type: string
                  namespace:
                    description: Namespace is the ConfigMap namespace.
                    type: string
                type: object
              imageMetadata:
                description: ImageMetadata is the metadata extracted from an AIM image
                properties:
                  baseImageRef:
                    description: |-
                      BaseImageRef is the value of the AIM_BASE_IMAGE_REF environment variable
                      baked into the image's OCI config at build time. For AIM model images this
                      records the base image (e.g. "ghcr.io/silogen/aim-base:0.8.5") that the
                      model image was built from. Used by the AIMModel controller to resolve
                      the deployment image for fine-tuned models whose spec.image is omitted
                      (versionPolicy=latest or any).
                    type: string
                  model:
                    description: Model contains AMD Silogen model-specific metadata.
                    properties:
                      canonicalName:
                        description: |-
                          CanonicalName is the canonical model identifier (e.g., mistralai/Mixtral-8x22B-Instruct-v0.1).
                          Extracted from: org.amd.silogen.model.canonicalName
                        type: string
                      descriptionFull:
                        description: |-
                          DescriptionFull is the full description.
                          Extracted from: org.amd.silogen.description.full
                        type: string
                      hfTokenRequired:
                        description: |-
                          HFTokenRequired indicates if a HuggingFace token is required.
                          Extracted from: org.amd.silogen.hfToken.required
                        type: boolean
                      recommendedDeployments:
                        description: |-
                          RecommendedDeployments contains recommended deployment configurations.
                          Extracted from: org.amd.silogen.model.recommendedDeployments (parsed from JSON array)
                        items:
                          description: RecommendedDeployment describes a recommended
                            deployment configuration for a model.
                          properties:
                            description:
                              description: Description provides additional context
                                about this deployment configuration
                              type: string
                            gpuCount:
                              description: GPUCount is the number of GPUs required
                              format: int32
                              type: integer
                            gpuModel:
                              description: |-
                                GPUModel is the GPU model name (e.g., MI300X, MI325X).
                                The legacy v1alpha1 schema only models GPU accelerators; CPU profiles
                                emitted via OCI labels (gpuModel="CPU") are not first-class here and
                                are handled exclusively by the v1alpha2 native discovery pipeline.
                              type: string
                            metric:
                              description: Metric is the optimization target (e.g.,
                                latency, throughput)
                              type: string
                            precision:
                              description: Precision is the recommended precision
                                (e.g., fp8, fp16, bf16)
                              type: string
                            profileId:
                              description: |-
                                ProfileId is the unique identifier of the AIM profile for this deployment.
                                When set, templates created from this deployment will use this profile ID
                                to deterministically select the correct runtime profile in the AIM container.
                              type: string
                          type: object
                        type: array
                      releaseNotes:
                        description: |-
                          ReleaseNotes contains release notes for this version.
                          Extracted from: org.amd.silogen.release.notes
                        type: string
                      source:
                        description: |-
                          Source is the URL where the model can be found.
                          Extracted from: org.amd.silogen.model.source
                        type: string
                      tags:
                        description: |-
                          Tags are descriptive tags (e.g., ["text-generation", "chat", "instruction"]).
                          Extracted from: org.amd.silogen.model.tags (comma-separated)
                        items:
                          type: string
                        type: array
                      title:
                        description: |-
                          Title is the Silogen-specific title for the model.
                          Extracted from: org.amd.silogen.title
                        type: string
                      variants:
                        description: |-
                          Variants lists model variants.
                          Extracted from: org.amd.silogen.model.variants (comma-separated)
                        items:
                          type: string
                        type: array
                      versions:
                        description: |-
                          Versions lists available versions.
                          Extracted from: org.amd.silogen.model.versions (comma-separated)
                        items:
                          type: string
                        type: array
                    type: object
                  oci:
                    description: OCI contains standard OCI image metadata.
                    properties:
                      authors:
                        description: |-
                          Authors is contact details of the authors.
                          Extracted from: org.opencontainers.image.authors
                        type: string
                      created:
                        description: |-
                          Created is the creation timestamp.
                          Extracted from: org.opencontainers.image.created
                        type: string
                      description:
                        description: |-
                          Description is a brief description.
                          Extracted from: org.opencontainers.image.description
                        type: string
                      documentation:
                        description: |-
                          Documentation is the URL to documentation.
                          Extracted from: org.opencontainers.image.documentation
                        type: string
                      licenses:
                        description: |-
                          Licenses is the SPDX license identifier(s).
                          Extracted from: org.opencontainers.image.licenses
                        type: string
                      revision:
                        description: |-
                          Revision is the source control revision.
                          Extracted from: org.opencontainers.image.revision
                        type: string
                      source:
                        description: |-
                          Source is the URL to the source code repository.
                          Extracted from: org.opencontainers.image.source
                        type: string
                      title:
                        description: |-
                          Title is the human-readable title.
                          Extracted from: org.opencontainers.image.title
                        type: string
                      vendor:
                        description: |-
                          Vendor is the organization that produced the image.
                          Extracted from: org.opencontainers.image.vendor
                        type: string
                      version:
                        description: |-
                          Version is the image version.
                          Extracted from: org.opencontainers.image.version
                        type: string
                    type: object
                  originalLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      OriginalLabels contains the raw OCI image labels as a JSON object.
                      This preserves all labels from the image, including those not mapped to structured fields.
                    type: object
                type: object
              kind:
                description: |-
                  Kind classifies the v1alpha2 onboarding flow that produced this
                  model's profiles (Image / Derived / Custom). See AIMModelKind for
                  the per-value semantics. Populated by the v1alpha2 controller from
                  the spec shape; left empty by the v1alpha1 controller.
                enum:
                - Image
                - Derived
                - Custom
                type: string
              managedProfiles:
                description: ManagedProfiles summarizes the direct promoted or derived
                  profiles owned or managed by this model.
                properties:
                  base:
                    description: |-
                      Base is the number of managed profiles whose spec is structurally
                      incomplete (missing aimId or modelSources). Base profiles cannot back
                      an AIMService directly and exist purely as source material for
                      derivation via AIMModel.spec.profiles.derivedFrom with
                      selector.role=base. A non-zero value is the canonical operational
                      signal that this model is a base-image model (the source of
                      custom-model derivation).
                    format: int32
                    type: integer
                  deployable:
                    description: |-
                      Deployable is the number of managed profiles whose spec is materialised
                      enough to back an AIMService (carries aimId and modelSources). For a
                      normal officially-discovered AIMModel this equals Total. For a
                      base-image AIMModel used as a source for custom-model derivation
                      this is 0 — the model only emits base profiles that callers must
                      derive into deployable profiles.
                    format: int32
                    type: integer
                  notAvailable:
                    description: NotAvailable is the number of derivative profiles
                      whose own status is NotAvailable.
                    format: int32
                    type: integer
                  ready:
                    description: Ready is the number of derivative profiles whose
                      own status is Ready.
                    format: int32
                    type: integer
                  total:
                    description: Total is the number of derivative profiles the controller
                      currently manages or intends to manage.
                    format: int32
                    type: integer
                required:
                - base
                - deployable
                - notAvailable
                - ready
                - total
                type: object
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller
                format: int64
                type: integer
              profileSetRef:
                description: ProfileSetRef identifies the child profile set synthesized
                  for derivation flows.
                properties:
                  name:
                    description: Name is the profile set name.
                    type: string
                  namespace:
                    description: Namespace is the profile set namespace.
                    type: string
                type: object
              resolvedRuntimeConfig:
                description: ResolvedRuntimeConfig captures metadata about the runtime
                  config that was resolved.
                properties:
                  kind:
                    description: Kind is the fully-qualified kind of the resolved
                      reference, when known.
                    type: string
                  name:
                    description: Name is the resource name that satisfied the reference.
                    type: string
                  namespace:
                    description: |-
                      Namespace identifies where the resource was found when namespace-scoped.
                      Empty indicates a cluster-scoped resource.
                    type: string
                  scope:
                    description: Scope indicates whether the resolved resource was
                      namespace or cluster scoped.
                    enum:
                    - Namespace
                    - Cluster
                    - Merged
                    - Unknown
                    type: string
                  uid:
                    description: UID captures the unique identifier of the resolved
                      reference, when known.
                    type: string
                type: object
              sourceType:
                description: |-
                  SourceType indicates how this model's artifacts are sourced.
                  - "Image": Model discovered from container image labels
                  - "Custom": Model uses explicit spec.modelSources
                  Set by the controller based on whether spec.modelSources is populated.

                  Note: only populated by the v1alpha1 controller; v1alpha2 consumers
                  should read .status.kind instead, which distinguishes fine-tunes
                  (Derived) from BYO base-image overlays (Custom).
                enum:
                - Image
                - Custom
                type: string
              status:
                default: Pending
                description: Status represents the overall status of the image based
                  on its templates
                enum:
                - Pending
                - Progressing
                - Ready
                - Degraded
                - Failed
                - NotAvailable
                type: string
              version:
                description: |-
                  Version is the effective image version of the model. Populated from
                  the spec.image tag (`amdenterpriseai/aim-qwen-qwen3-32b:0.11.0` →
                  `0.11.0`) during reconciliation. Empty for models that have no
                  spec.image (e.g. fine-tuned models derived from a parent) or that
                  reference an image by digest.

                  This mirrors AIMProfileStatus.Version so the two surfaces stay in
                  lock-step: a single image-tag extraction rule governs what users
                  see in the kubectl printcolumn for both kinds.

                  The image-author-declared version (i.e. the
                  `org.opencontainers.image.version` OCI label) is preserved separately
                  under .status.imageMetadata.oci.version for users that want to inspect
                  what the image build pipeline stamped. The two values usually agree;
                  when the OCI label is missing or empty, this field still surfaces a
                  useful version from the tag itself.
                type: string
            type: object
        type: object
        x-kubernetes-validations:
        - message: spec.derivedFrom is only supported on v1alpha2; use spec.profileCopy
            on v1alpha1
          rule: '!has(self.spec.derivedFrom)'
        - message: spec.profiles is only supported on v1alpha2; use spec.profileCopy
            on v1alpha1
          rule: '!has(self.spec.profiles)'
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.kind
      name: Kind
      type: string
    - jsonPath: .status.aimId
      name: AimID
      type: string
    - jsonPath: .status.version
      name: Version
      type: string
    - jsonPath: .status.managedProfiles.total
      name: Managed
      type: integer
    - jsonPath: .status.managedProfiles.ready
      name: Ready
      type: integer
    - jsonPath: .status.managedProfiles.base
      name: Base
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: |-
          AIMClusterModel is the Schema for cluster-scoped v1alpha2 AIM model resources.
          See AIMModel (api/v1alpha2/aimmodel_types.go) for the rationale of the
          CEL rules below — AIMClusterModel mirrors the namespace-scoped contract.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              AIMModelSpec defines the desired state of AIMModel.

              Per-version constraints (v1alpha1 forbids derivedFrom and profiles;
              v1alpha2 forbids profileCopy/custom/customTemplates/top-level modelSources,
              requires image XOR profiles, and forbids profiles mixed with legacy scalar
              fields) live on the version-specific AIMModel / AIMClusterModel root types
              in the per-version *_types.go files.
            properties:
              aimId:
                description: |-
                  AimId is the AIM product family identifier (e.g., "qwen/qwen3-32b").
                  When set together with modelSources, enables aimId-based template matching:
                  the controller finds official templates by aimId, filters by versionPolicy,
                  matches by modelId, and creates copies with the custom weight source.
                type: string
              custom:
                description: |-
                  Custom contains configuration for custom models (models with inline modelSources).
                  Only used when modelSources are specified; ignored for image-based models.
                properties:
                  hardware:
                    description: |-
                      Hardware specifies default hardware requirements for all templates.
                      Individual templates can override these defaults.
                      Required when modelSources is set and customTemplates is empty (unless aimId is set).
                    properties:
                      cpu:
                        description: CPU specifies CPU requirements.
                        properties:
                          limits:
                            anyOf:
                            - type: integer
                            - type: string
                            description: Limits is the maximum number of CPU cores
                              to allow.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          requests:
                            anyOf:
                            - type: integer
                            - type: string
                            description: Requests is the number of CPU cores to request.
                              Required and must be > 0.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                        required:
                        - requests
                        type: object
                      gpu:
                        description: GPU specifies GPU requirements. If not set, no
                          GPUs are requested (CPU-only model).
                        properties:
                          minVram:
                            anyOf:
                            - type: integer
                            - type: string
                            description: |-
                              MinVRAM limits deployment to GPUs having at least this much VRAM.
                              Used for capacity planning when the model size is known but any GPU with
                              sufficient VRAM is acceptable.
                              Cannot be combined with model.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          model:
                            description: |-
                              Model limits deployment to a specific GPU model.
                              Example: "MI300X"
                              Cannot be combined with minVram.
                            maxLength: 64
                            type: string
                          requests:
                            description: |-
                              Requests is the number of GPUs to set as requests/limits.
                              Set to 0 to target GPU nodes without consuming GPU resources (useful for testing).
                            format: int32
                            minimum: 0
                            type: integer
                          resourceName:
                            default: amd.com/gpu
                            description: |-
                              ResourceName is the Kubernetes resource name for GPU resources.
                              Defaults to "amd.com/gpu" if not specified.
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: model and minVram are mutually exclusive; specify
                            either a specific GPU model OR a minimum VRAM requirement,
                            not both
                          rule: '!(has(self.model) && size(self.model) > 0 && has(self.minVram))'
                    type: object
                    x-kubernetes-validations:
                    - message: at least one of gpu or cpu must be specified
                      rule: has(self.gpu) || has(self.cpu)
                  type:
                    allOf:
                    - enum:
                      - optimized
                      - general
                      - preview
                      - unoptimized
                    - enum:
                      - optimized
                      - general
                      - preview
                      - unoptimized
                    description: |-
                      Type specifies default type for all templates.
                      Individual templates can override this default.
                      When nil, templates default to "unoptimized".
                    type: string
                  versionPolicy:
                    default: pinned
                    description: |-
                      VersionPolicy controls how template versions are filtered during aimId-based matching.
                      - pinned (default): match templates whose status.version equals the model's image tag
                      - latest: match only templates at the newest available status.version
                      - all: match templates at any version
                      - any: deprecated alias of all, kept for backward compatibility
                      Only used when spec.aimId is set.
                    enum:
                    - pinned
                    - latest
                    - any
                    - all
                    type: string
                type: object
              customTemplates:
                description: |-
                  CustomTemplates defines explicit template configurations for this model.
                  These templates are created directly without running a discovery job.
                  Can be used with or without modelSources to define custom deployment configurations.
                  If omitted when modelSources is set, a single template is auto-generated
                  using the custom.hardware requirements.
                items:
                  description: |-
                    AIMCustomTemplate defines a custom template configuration for a model.
                    When modelSources are specified directly on AIMModel, customTemplates allow
                    defining explicit hardware requirements and profiles, skipping the discovery job.
                    This is an existing struct (not a CRD); it appears as an element of AIMModel.spec.customTemplates[].
                  properties:
                    aimId:
                      description: |-
                        AimId is the AIM product family identifier (e.g., "meta-llama/Llama-3-8B").
                        Required when customProfile is set.
                      type: string
                    customProfile:
                      description: |-
                        CustomProfile defines inline custom profile data for the inference engine.
                        When set, the resulting template will have a custom profile ConfigMap mounted.
                        Requires aimId, modelId, hardware, profile.metric, and profile.precision.
                      properties:
                        engineArgs:
                          description: |-
                            EngineArgs contains inference engine arguments as a free-form JSON object.
                            These are passed as CLI arguments to the inference engine (e.g., vLLM).
                            Do not include "model" — it is injected separately by the runtime.
                          x-kubernetes-preserve-unknown-fields: true
                        envVars:
                          additionalProperties:
                            type: string
                          description: |-
                            EnvVars contains environment variables applied to the inference engine process.
                            These are written into the profile YAML and applied by the AIM runtime via os.execv,
                            distinct from container-level Env which targets the AIM runtime container itself.
                            Keys must match ^[A-Z0-9_]+$ (uppercase with underscores).
                          type: object
                      type: object
                    env:
                      description: |-
                        Env specifies environment variable overrides when this template is selected.
                        These are container-level env vars applied to the AIM runtime container.
                      items:
                        description: EnvVar represents an environment variable present
                          in a Container.
                        properties:
                          name:
                            description: |-
                              Name of the environment variable.
                              May consist of any printable ASCII characters except '='.
                            type: string
                          value:
                            description: |-
                              Variable references $(VAR_NAME) are expanded
                              using the previously defined environment variables in the container and
                              any service environment variables. If a variable cannot be resolved,
                              the reference in the input string will be unchanged. Double $$ are reduced
                              to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                              "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                              Escaped references will never be expanded, regardless of whether the variable
                              exists or not.
                              Defaults to "".
                            type: string
                          valueFrom:
                            description: Source for the environment variable's value.
                              Cannot be used if value is not empty.
                            properties:
                              configMapKeyRef:
                                description: Selects a key of a ConfigMap.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the ConfigMap or
                                      its key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              fieldRef:
                                description: |-
                                  Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                  spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                properties:
                                  apiVersion:
                                    description: Version of the schema the FieldPath
                                      is written in terms of, defaults to "v1".
                                    type: string
                                  fieldPath:
                                    description: Path of the field to select in the
                                      specified API version.
                                    type: string
                                required:
                                - fieldPath
                                type: object
                                x-kubernetes-map-type: atomic
                              fileKeyRef:
                                description: |-
                                  FileKeyRef selects a key of the env file.
                                  Requires the EnvFiles feature gate to be enabled.
                                properties:
                                  key:
                                    description: |-
                                      The key within the env file. An invalid key will prevent the pod from starting.
                                      The keys defined within a source may consist of any printable ASCII characters except '='.
                                      During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                    type: string
                                  optional:
                                    default: false
                                    description: |-
                                      Specify whether the file or its key must be defined. If the file or key
                                      does not exist, then the env var is not published.
                                      If optional is set to true and the specified key does not exist,
                                      the environment variable will not be set in the Pod's containers.

                                      If optional is set to false and the specified key does not exist,
                                      an error will be returned during Pod creation.
                                    type: boolean
                                  path:
                                    description: |-
                                      The path within the volume from which to select the file.
                                      Must be relative and may not contain the '..' path or start with '..'.
                                    type: string
                                  volumeName:
                                    description: The name of the volume mount containing
                                      the env file.
                                    type: string
                                required:
                                - key
                                - path
                                - volumeName
                                type: object
                                x-kubernetes-map-type: atomic
                              resourceFieldRef:
                                description: |-
                                  Selects a resource of the container: only resources limits and requests
                                  (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                properties:
                                  containerName:
                                    description: 'Container name: required for volumes,
                                      optional for env vars'
                                    type: string
                                  divisor:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: Specifies the output format of the
                                      exposed resources, defaults to "1"
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  resource:
                                    description: 'Required: resource to select'
                                    type: string
                                required:
                                - resource
                                type: object
                                x-kubernetes-map-type: atomic
                              secretKeyRef:
                                description: Selects a key of a secret in the pod's
                                  namespace
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                        required:
                        - name
                        type: object
                      maxItems: 64
                      type: array
                      x-kubernetes-list-map-keys:
                      - name
                      x-kubernetes-list-type: map
                    hardware:
                      description: |-
                        Hardware specifies GPU and CPU requirements for this template.
                        Optional when spec.hardware is set (inherits from spec).
                        When both are set, values are merged field-by-field with template taking precedence.
                      properties:
                        cpu:
                          description: CPU specifies CPU requirements.
                          properties:
                            limits:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Limits is the maximum number of CPU cores
                                to allow.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            requests:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Requests is the number of CPU cores to
                                request. Required and must be > 0.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                          required:
                          - requests
                          type: object
                        gpu:
                          description: GPU specifies GPU requirements. If not set,
                            no GPUs are requested (CPU-only model).
                          properties:
                            minVram:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                MinVRAM limits deployment to GPUs having at least this much VRAM.
                                Used for capacity planning when the model size is known but any GPU with
                                sufficient VRAM is acceptable.
                                Cannot be combined with model.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            model:
                              description: |-
                                Model limits deployment to a specific GPU model.
                                Example: "MI300X"
                                Cannot be combined with minVram.
                              maxLength: 64
                              type: string
                            requests:
                              description: |-
                                Requests is the number of GPUs to set as requests/limits.
                                Set to 0 to target GPU nodes without consuming GPU resources (useful for testing).
                              format: int32
                              minimum: 0
                              type: integer
                            resourceName:
                              default: amd.com/gpu
                              description: |-
                                ResourceName is the Kubernetes resource name for GPU resources.
                                Defaults to "amd.com/gpu" if not specified.
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: model and minVram are mutually exclusive; specify
                              either a specific GPU model OR a minimum VRAM requirement,
                              not both
                            rule: '!(has(self.model) && size(self.model) > 0 && has(self.minVram))'
                      type: object
                      x-kubernetes-validations:
                      - message: at least one of gpu or cpu must be specified
                        rule: has(self.gpu) || has(self.cpu)
                    modelId:
                      description: |-
                        ModelId is the specific model identifier / HuggingFace URI (e.g., "Qwen/Qwen3-32B-FP8").
                        Required when customProfile is set.
                      type: string
                    name:
                      description: Name is the template name. If not provided, auto-generated
                        from model name + profile.
                      maxLength: 63
                      type: string
                    profile:
                      description: |-
                        Profile declares runtime profile variables for template selection.
                        Used when multiple templates exist to select based on metric/precision.
                      properties:
                        metric:
                          allOf:
                          - enum:
                            - latency
                            - throughput
                          - enum:
                            - latency
                            - throughput
                          description: Metric specifies the optimization target (e.g.,
                            latency, throughput).
                          type: string
                        precision:
                          allOf:
                          - enum:
                            - auto
                            - fp4
                            - fp8
                            - fp16
                            - fp32
                            - bf16
                            - int4
                            - int8
                          - enum:
                            - auto
                            - fp4
                            - fp8
                            - fp16
                            - fp32
                            - bf16
                            - int4
                            - int8
                          description: Precision specifies the numerical precision
                            (e.g., fp8, fp16, bf16).
                          type: string
                      type: object
                    type:
                      allOf:
                      - enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                      - enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                      default: unoptimized
                      description: |-
                        Type indicates the optimization status of this template.
                        - optimized: Template has been tuned for performance
                        - general: General-purpose tuning between optimized and preview
                        - preview: Template is experimental/pre-release
                        - unoptimized: Default, no specific optimizations applied
                      type: string
                  type: object
                  x-kubernetes-validations:
                  - message: when customProfile is set, aimId, modelId, hardware,
                      profile.metric, and profile.precision are required
                    rule: '!has(self.customProfile) || (has(self.aimId) && has(self.modelId)
                      && has(self.hardware) && has(self.profile) && has(self.profile.metric)
                      && has(self.profile.precision))'
                maxItems: 16
                type: array
              defaultServiceTemplate:
                description: |-
                  DefaultServiceTemplate specifies the default AIMServiceTemplate to use when creating services for this model.
                  When set, services that reference this model will use this template if no template is explicitly specified.
                  If this is not set, a template will be automatically selected.
                type: string
              derivedFrom:
                description: |-
                  DerivedFrom is the legacy flat shape of v1alpha2's profile-derivation
                  onboarding surface. New objects must use spec.profiles instead; the
                  field is retained so existing v1alpha2 objects (and the v1alpha2
                  reconciler reading them) round-trip cleanly.

                  DEPRECATED: prefer spec.profiles.derivedFrom on v1alpha2.
                properties:
                  image:
                    description: Image overrides the runtime image used by the derived
                      profiles.
                    type: string
                  imagePullSecrets:
                    description: ImagePullSecrets lists secrets used for inspecting
                      and pulling container images.
                    items:
                      description: |-
                        LocalObjectReference contains enough information to let you locate the
                        referenced object inside the same namespace.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    type: array
                  overrides:
                    description: Overrides mutates the copied profile spec after selection
                      and version filtering.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount replaces the copied profile's
                          acceleratorCount.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel replaces the copied profile's
                          acceleratorModel.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode replaces the copied profile's
                          acceleratorPartitioningMode. Complete replacement, not a merge; an empty
                          override string leaves the source profile's mode untouched. Same reserved
                          values as AIMProfileSpecCommon.AcceleratorPartitioningMode. Whenever this
                          override is set (to any value, including "unpartitioned"), the CEL rule on the
                          enclosing spec also requires AcceleratorCount to be set: partition mode
                          changes the per-unit interpretation of acceleratorCount, and CEL cannot
                          read the base profile to tell whether the meaning actually changed, so it
                          conservatively requires the count be restated.
                        type: string
                      aimId:
                        description: |-
                          AimId stamps the derived profile's spec.aimId. When set, wins over
                          the source profile's aimId. REQUIRED when the enclosing selector has
                          role=base (the source base profile carries no aimId of its own).
                        type: string
                      containerEnv:
                        description: ContainerEnv merges by env var name, overriding
                          matching source entries.
                        items:
                          description: EnvVar represents an environment variable present
                            in a Container.
                          properties:
                            name:
                              description: |-
                                Name of the environment variable.
                                May consist of any printable ASCII characters except '='.
                              type: string
                            value:
                              description: |-
                                Variable references $(VAR_NAME) are expanded
                                using the previously defined environment variables in the container and
                                any service environment variables. If a variable cannot be resolved,
                                the reference in the input string will be unchanged. Double $$ are reduced
                                to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                Escaped references will never be expanded, regardless of whether the variable
                                exists or not.
                                Defaults to "".
                              type: string
                            valueFrom:
                              description: Source for the environment variable's value.
                                Cannot be used if value is not empty.
                              properties:
                                configMapKeyRef:
                                  description: Selects a key of a ConfigMap.
                                  properties:
                                    key:
                                      description: The key to select.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the ConfigMap or
                                        its key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fieldRef:
                                  description: |-
                                    Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                    spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                  properties:
                                    apiVersion:
                                      description: Version of the schema the FieldPath
                                        is written in terms of, defaults to "v1".
                                      type: string
                                    fieldPath:
                                      description: Path of the field to select in
                                        the specified API version.
                                      type: string
                                  required:
                                  - fieldPath
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fileKeyRef:
                                  description: |-
                                    FileKeyRef selects a key of the env file.
                                    Requires the EnvFiles feature gate to be enabled.
                                  properties:
                                    key:
                                      description: |-
                                        The key within the env file. An invalid key will prevent the pod from starting.
                                        The keys defined within a source may consist of any printable ASCII characters except '='.
                                        During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                      type: string
                                    optional:
                                      default: false
                                      description: |-
                                        Specify whether the file or its key must be defined. If the file or key
                                        does not exist, then the env var is not published.
                                        If optional is set to true and the specified key does not exist,
                                        the environment variable will not be set in the Pod's containers.

                                        If optional is set to false and the specified key does not exist,
                                        an error will be returned during Pod creation.
                                      type: boolean
                                    path:
                                      description: |-
                                        The path within the volume from which to select the file.
                                        Must be relative and may not contain the '..' path or start with '..'.
                                      type: string
                                    volumeName:
                                      description: The name of the volume mount containing
                                        the env file.
                                      type: string
                                  required:
                                  - key
                                  - path
                                  - volumeName
                                  type: object
                                  x-kubernetes-map-type: atomic
                                resourceFieldRef:
                                  description: |-
                                    Selects a resource of the container: only resources limits and requests
                                    (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                  properties:
                                    containerName:
                                      description: 'Container name: required for volumes,
                                        optional for env vars'
                                      type: string
                                    divisor:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      description: Specifies the output format of
                                        the exposed resources, defaults to "1"
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    resource:
                                      description: 'Required: resource to select'
                                      type: string
                                  required:
                                  - resource
                                  type: object
                                  x-kubernetes-map-type: atomic
                                secretKeyRef:
                                  description: Selects a key of a secret in the pod's
                                    namespace
                                  properties:
                                    key:
                                      description: The key of the secret to select
                                        from.  Must be a valid secret key.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the Secret or its
                                        key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      engineArgs:
                        description: EngineArgs shallow-merges on top of the source
                          engineArgs, overriding matching keys.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      engineEnv:
                        additionalProperties:
                          type: string
                        description: EngineEnv merges by key, overriding matching
                          source entries.
                        type: object
                      image:
                        description: |-
                          Image overrides the runtime container image used by the derived
                          profiles. When empty the deployment image is rebased onto the source
                          profile's base-image (status.baseImage) so private mirrors stay
                          self-contained.
                        type: string
                      modelId:
                        description: |-
                          ModelId stamps the derived profile's spec.modelId. When set, wins
                          over both the source profile's modelId AND the auto-derivation from
                          modelSources[0].modelId. REQUIRED when the enclosing selector has
                          role=base.
                        type: string
                      modelSources:
                        description: |-
                          ModelSources replaces the copied profile's modelSources. When
                          modelSources[0].modelId is set and overrides.modelId is unset, the
                          derived profile's modelId is auto-derived from modelSources[0]; an
                          explicit overrides.modelId always wins.
                        items:
                          description: |-
                            AIMModelSource describes a model artifact that must be downloaded for inference.
                            Discovery extracts these from the container's configuration to enable caching and validation.
                          properties:
                            env:
                              description: |-
                                Env specifies per-source credential overrides.
                                These variables are used for authentication when downloading this specific source.
                                Takes precedence over base-level env for the same variable name.
                              items:
                                description: EnvVar represents an environment variable
                                  present in a Container.
                                properties:
                                  name:
                                    description: |-
                                      Name of the environment variable.
                                      May consist of any printable ASCII characters except '='.
                                    type: string
                                  value:
                                    description: |-
                                      Variable references $(VAR_NAME) are expanded
                                      using the previously defined environment variables in the container and
                                      any service environment variables. If a variable cannot be resolved,
                                      the reference in the input string will be unchanged. Double $$ are reduced
                                      to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                      "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                      Escaped references will never be expanded, regardless of whether the variable
                                      exists or not.
                                      Defaults to "".
                                    type: string
                                  valueFrom:
                                    description: Source for the environment variable's
                                      value. Cannot be used if value is not empty.
                                    properties:
                                      configMapKeyRef:
                                        description: Selects a key of a ConfigMap.
                                        properties:
                                          key:
                                            description: The key to select.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the ConfigMap
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fieldRef:
                                        description: |-
                                          Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                          spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                        properties:
                                          apiVersion:
                                            description: Version of the schema the
                                              FieldPath is written in terms of, defaults
                                              to "v1".
                                            type: string
                                          fieldPath:
                                            description: Path of the field to select
                                              in the specified API version.
                                            type: string
                                        required:
                                        - fieldPath
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fileKeyRef:
                                        description: |-
                                          FileKeyRef selects a key of the env file.
                                          Requires the EnvFiles feature gate to be enabled.
                                        properties:
                                          key:
                                            description: |-
                                              The key within the env file. An invalid key will prevent the pod from starting.
                                              The keys defined within a source may consist of any printable ASCII characters except '='.
                                              During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                            type: string
                                          optional:
                                            default: false
                                            description: |-
                                              Specify whether the file or its key must be defined. If the file or key
                                              does not exist, then the env var is not published.
                                              If optional is set to true and the specified key does not exist,
                                              the environment variable will not be set in the Pod's containers.

                                              If optional is set to false and the specified key does not exist,
                                              an error will be returned during Pod creation.
                                            type: boolean
                                          path:
                                            description: |-
                                              The path within the volume from which to select the file.
                                              Must be relative and may not contain the '..' path or start with '..'.
                                            type: string
                                          volumeName:
                                            description: The name of the volume mount
                                              containing the env file.
                                            type: string
                                        required:
                                        - key
                                        - path
                                        - volumeName
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      resourceFieldRef:
                                        description: |-
                                          Selects a resource of the container: only resources limits and requests
                                          (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                        properties:
                                          containerName:
                                            description: 'Container name: required
                                              for volumes, optional for env vars'
                                            type: string
                                          divisor:
                                            anyOf:
                                            - type: integer
                                            - type: string
                                            description: Specifies the output format
                                              of the exposed resources, defaults to
                                              "1"
                                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                            x-kubernetes-int-or-string: true
                                          resource:
                                            description: 'Required: resource to select'
                                            type: string
                                        required:
                                        - resource
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      secretKeyRef:
                                        description: Selects a key of a secret in
                                          the pod's namespace
                                        properties:
                                          key:
                                            description: The key of the secret to
                                              select from.  Must be a valid secret
                                              key.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the Secret
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                    type: object
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            modelId:
                              description: |-
                                ModelID is the canonical identifier in {org}/{name} format.
                                Determines the cache mount path: /workspace/cache/{modelId}
                                For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                                For S3 sources, users define their own organizational structure.
                              pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                              type: string
                            precision:
                              allOf:
                              - enum:
                                - auto
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              - enum:
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              description: |-
                                Precision describes the runtime precision this source is compatible with.
                                Used to match model sources to profiles during custom weight onboarding.
                              type: string
                            size:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                Size is the expected storage space required for this model artifact.
                                Used for PVC sizing and capacity planning during cache creation.
                                Optional - if not specified, the download job will discover the size automatically.
                                Can be set explicitly to pre-allocate storage or override auto-discovery.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            sourceUri:
                              description: |-
                                SourceURI is the location from which the model should be downloaded.
                                Supported schemes:
                                - hf://org/model - Hugging Face Hub model
                                - s3://bucket/key - S3-compatible storage
                              pattern: ^(hf|s3)://[^ \t\r\n]+$
                              type: string
                          required:
                          - modelId
                          - sourceUri
                          type: object
                        type: array
                      profileId:
                        description: |-
                          ProfileId stamps the derived profile's spec.profileId. Optional —
                          most callers leave this empty and let the source profile's profileId
                          carry through (or the reconciler synthesise one).
                        type: string
                    type: object
                  selector:
                    description: Selector chooses which source profiles to derive
                      from.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount filters by accelerator unit
                          count.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel filters by accelerator identifier.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode filters candidates by their declared
                          partitioning mode. Partial-order match (NOT strict equality):
                            ""              - no filter on this field.
                            "unpartitioned" - matches profiles with mode "" or "unpartitioned".
                            "partitioned"   - matches profiles whose mode is non-trivial (anything
                                              other than "" / "unpartitioned").
                            "<C>"           - selector-only convenience: matches profiles with mode
                                              "<C>-*" (prefix on the scheme). Not a valid profile-spec
                                              value (e.g. selector "CPX" matches "CPX-NPS1", "CPX-NPS4").
                            "<C>-<M>"       - exact-string match on the scheme.
                        type: string
                      acceleratorType:
                        description: AcceleratorType filters by accelerator resource
                          type.
                        enum:
                        - gpu
                        - cpu
                        type: string
                      aimId:
                        description: AimId filters by model architecture identifier
                          (e.g., "qwen/qwen3-32b").
                        type: string
                      engine:
                        description: Engine filters by inference engine.
                        type: string
                      engineArgs:
                        description: |-
                          EngineArgs partially matches source engineArgs: every provided top-level key must
                          exist in the source object with an equal value.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      metric:
                        description: Metric filters by optimization target.
                        enum:
                        - latency
                        - throughput
                        type: string
                      minimumType:
                        description: |-
                          MinimumType filters by a minimum optimization level: candidates whose
                          type is this tier OR BETTER are accepted (hierarchy: optimized > general
                          > preview > unoptimized). This is the floor counterpart to the exact-match
                          Type field; the two AND together when both are set.

                          The sentinel "any" disables the floor (accept every tier). When this
                          field is empty the AIMService resolver applies a default floor of
                          "optimized" so auto-selection prefers production-grade profiles and never
                          silently picks an unoptimized one; to opt a service into lower tiers
                          (e.g. CPU/EPYC profiles published as unoptimized) set minimumType
                          explicitly to "unoptimized" or "any". Derivation selectors
                          (AIMProfileSet / AIMModel.profiles) treat empty as "any" so copying is
                          never tier-restricted by default.
                        enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                        - any
                        type: string
                      modelId:
                        description: ModelId filters by the source profile's specific
                          model identifier.
                        type: string
                      modelRef:
                        description: |-
                          ModelRef narrows candidates to those produced by a specific
                          AIM(Cluster)Model, matched via the `aim.eai.amd.com/source-model[-scope]`
                          labels stamped by the AIMModel reconcilers. Iteration 1 (v1alpha2 only).
                        properties:
                          name:
                            description: Name is the owning AIM(Cluster)Model name.
                              Required.
                            minLength: 1
                            type: string
                          scope:
                            default: Auto
                            description: Scope controls how Name is resolved against
                              AIMModel vs AIMClusterModel.
                            enum:
                            - Auto
                            - Namespace
                            - Cluster
                            type: string
                        required:
                        - name
                        type: object
                      origin:
                        description: |-
                          Origin filters by the `aim.eai.amd.com/profile-origin` label. When unset
                          (empty) the selector does not filter by origin. Iteration 1 (v1alpha2
                          only).
                        enum:
                        - discovered
                        - derived
                        - user-authored
                        type: string
                      precision:
                        description: Precision filters by numeric precision.
                        enum:
                        - auto
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                        type: string
                      profileId:
                        description: ProfileId filters by the source profile's profile
                          identifier.
                        type: string
                      role:
                        default: deployable
                        description: |-
                          Role filters by the `aim.eai.amd.com/profile-role` label. Defaults to
                          `deployable`. `base` filters to base profiles emitted by base-image
                          discovery (custom-model derivation source material).
                        enum:
                        - base
                        - deployable
                        type: string
                      type:
                        description: Type filters by optimization level (exact match).
                        enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                        type: string
                    type: object
                  serviceAccountName:
                    description: ServiceAccountName is propagated to managed profiles
                      for downstream workloads.
                    type: string
                  sourceRef:
                    description: |-
                      SourceRef points to an alternate discovery cache source.
                      When omitted, derivation uses visible AIMProfile and AIMClusterProfile objects.
                      For AIMProfileSet, the referenced ConfigMap is read from the same namespace.
                      For AIMClusterProfileSet, it is read from the operator namespace.
                    properties:
                      name:
                        description: Name is the discovery cache ConfigMap name.
                        type: string
                    required:
                    - name
                    type: object
                  version:
                    description: Version pins matching to a specific source profile
                      version when VersionPolicy is pinned.
                    type: string
                  versionPolicy:
                    default: pinned
                    description: VersionPolicy controls how matching profiles are
                      filtered by version.
                    enum:
                    - pinned
                    - latest
                    - all
                    type: string
                type: object
                x-kubernetes-validations:
                - message: selector must set at least one matching field, or set sourceRef
                  rule: has(self.sourceRef) || has(self.selector.aimId) || has(self.selector.modelId)
                    || has(self.selector.profileId) || has(self.selector.engine) ||
                    has(self.selector.metric) || has(self.selector.precision) || has(self.selector.type)
                    || has(self.selector.acceleratorModel) || has(self.selector.acceleratorType)
                    || has(self.selector.acceleratorCount) || has(self.selector.engineArgs)
                    || has(self.selector.modelRef) || has(self.selector.origin)
                - message: version is required when versionPolicy is pinned
                  rule: has(self.version) || (has(self.versionPolicy) && self.versionPolicy
                    != 'pinned')
                - message: latest/all versionPolicy must not set version
                  rule: '!has(self.versionPolicy) || (self.versionPolicy != ''latest''
                    && self.versionPolicy != ''all'') || !has(self.version)'
                - message: selector.aimId/modelId/profileId are not allowed when selector.role=base;
                    set overrides.aimId/modelId/profileId instead (base profiles have
                    no source identity to filter on)
                  rule: '!(has(self.selector.role) && self.selector.role == ''base'')
                    || (!has(self.selector.aimId) && !has(self.selector.modelId) &&
                    !has(self.selector.profileId))'
                - message: selector.role=base requires overrides.aimId and overrides.modelId
                    (base profiles carry no identity of their own; overrides supply
                    it)
                  rule: '!(has(self.selector.role) && self.selector.role == ''base'')
                    || (has(self.overrides) && has(self.overrides.aimId) && has(self.overrides.modelId))'
                - message: overrides.acceleratorCount must be specified together with
                    any overrides.acceleratorPartitioningMode; partition mode changes
                    the per-unit interpretation of acceleratorCount
                  rule: '!has(self.overrides) || !has(self.overrides.acceleratorPartitioningMode)
                    || size(self.overrides.acceleratorPartitioningMode) == 0 || has(self.overrides.acceleratorCount)'
              discovery:
                description: |-
                  Discovery controls discovery behavior for this model.
                  When unset, uses runtime config defaults.
                properties:
                  createServiceTemplates:
                    default: true
                    description: CreateServiceTemplates controls whether (cluster)
                      service templates are auto-created from the image metadata.
                    type: boolean
                  extractMetadata:
                    default: true
                    description: |-
                      ExtractMetadata controls whether metadata extraction runs for this model.
                      During metadata extraction, the controller connects to the image registry and
                      extracts the image's labels.
                    type: boolean
                type: object
              env:
                description: |-
                  Env specifies environment variables for authentication during model discovery and metadata extraction.
                  These variables are used for authentication with model registries (e.g., HuggingFace tokens).
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: |-
                        Name of the environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        fileKeyRef:
                          description: |-
                            FileKeyRef selects a key of the env file.
                            Requires the EnvFiles feature gate to be enabled.
                          properties:
                            key:
                              description: |-
                                The key within the env file. An invalid key will prevent the pod from starting.
                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                              type: string
                            optional:
                              default: false
                              description: |-
                                Specify whether the file or its key must be defined. If the file or key
                                does not exist, then the env var is not published.
                                If optional is set to true and the specified key does not exist,
                                the environment variable will not be set in the Pod's containers.

                                If optional is set to false and the specified key does not exist,
                                an error will be returned during Pod creation.
                              type: boolean
                            path:
                              description: |-
                                The path within the volume from which to select the file.
                                Must be relative and may not contain the '..' path or start with '..'.
                              type: string
                            volumeName:
                              description: The name of the volume mount containing
                                the env file.
                              type: string
                          required:
                          - key
                          - path
                          - volumeName
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              image:
                description: |-
                  Image is the container image URI for this AIM model.
                  This image is inspected by the operator to select runtime profiles used by templates.
                  Discovery behavior is controlled by the discovery field and runtime config's AutoDiscovery setting.
                  Required unless aimId is set with versionPolicy latest or any, or profileCopy is set.
                type: string
              imageMetadata:
                description: |-
                  ImageMetadata is the metadata that is used to determine which recommended service templates to create,
                  and to drive clients with richer metadata regarding this particular model. For most cases the user does
                  not need to set this field manually, for images that have the supported labels embedded in them
                  the `AIM(Cluster)Model.status.imageMetadata` field is automatically filled from the container image labels.
                  This field is intended to be used when there are network restrictions, or in other similar situations.
                  If this field is set, the remote extraction will not be performed at all.
                properties:
                  baseImageRef:
                    description: |-
                      BaseImageRef is the value of the AIM_BASE_IMAGE_REF environment variable
                      baked into the image's OCI config at build time. For AIM model images this
                      records the base image (e.g. "ghcr.io/silogen/aim-base:0.8.5") that the
                      model image was built from. Used by the AIMModel controller to resolve
                      the deployment image for fine-tuned models whose spec.image is omitted
                      (versionPolicy=latest or any).
                    type: string
                  model:
                    description: Model contains AMD Silogen model-specific metadata.
                    properties:
                      canonicalName:
                        description: |-
                          CanonicalName is the canonical model identifier (e.g., mistralai/Mixtral-8x22B-Instruct-v0.1).
                          Extracted from: org.amd.silogen.model.canonicalName
                        type: string
                      descriptionFull:
                        description: |-
                          DescriptionFull is the full description.
                          Extracted from: org.amd.silogen.description.full
                        type: string
                      hfTokenRequired:
                        description: |-
                          HFTokenRequired indicates if a HuggingFace token is required.
                          Extracted from: org.amd.silogen.hfToken.required
                        type: boolean
                      recommendedDeployments:
                        description: |-
                          RecommendedDeployments contains recommended deployment configurations.
                          Extracted from: org.amd.silogen.model.recommendedDeployments (parsed from JSON array)
                        items:
                          description: RecommendedDeployment describes a recommended
                            deployment configuration for a model.
                          properties:
                            description:
                              description: Description provides additional context
                                about this deployment configuration
                              type: string
                            gpuCount:
                              description: GPUCount is the number of GPUs required
                              format: int32
                              type: integer
                            gpuModel:
                              description: |-
                                GPUModel is the GPU model name (e.g., MI300X, MI325X).
                                The legacy v1alpha1 schema only models GPU accelerators; CPU profiles
                                emitted via OCI labels (gpuModel="CPU") are not first-class here and
                                are handled exclusively by the v1alpha2 native discovery pipeline.
                              type: string
                            metric:
                              description: Metric is the optimization target (e.g.,
                                latency, throughput)
                              type: string
                            precision:
                              description: Precision is the recommended precision
                                (e.g., fp8, fp16, bf16)
                              type: string
                            profileId:
                              description: |-
                                ProfileId is the unique identifier of the AIM profile for this deployment.
                                When set, templates created from this deployment will use this profile ID
                                to deterministically select the correct runtime profile in the AIM container.
                              type: string
                          type: object
                        type: array
                      releaseNotes:
                        description: |-
                          ReleaseNotes contains release notes for this version.
                          Extracted from: org.amd.silogen.release.notes
                        type: string
                      source:
                        description: |-
                          Source is the URL where the model can be found.
                          Extracted from: org.amd.silogen.model.source
                        type: string
                      tags:
                        description: |-
                          Tags are descriptive tags (e.g., ["text-generation", "chat", "instruction"]).
                          Extracted from: org.amd.silogen.model.tags (comma-separated)
                        items:
                          type: string
                        type: array
                      title:
                        description: |-
                          Title is the Silogen-specific title for the model.
                          Extracted from: org.amd.silogen.title
                        type: string
                      variants:
                        description: |-
                          Variants lists model variants.
                          Extracted from: org.amd.silogen.model.variants (comma-separated)
                        items:
                          type: string
                        type: array
                      versions:
                        description: |-
                          Versions lists available versions.
                          Extracted from: org.amd.silogen.model.versions (comma-separated)
                        items:
                          type: string
                        type: array
                    type: object
                  oci:
                    description: OCI contains standard OCI image metadata.
                    properties:
                      authors:
                        description: |-
                          Authors is contact details of the authors.
                          Extracted from: org.opencontainers.image.authors
                        type: string
                      created:
                        description: |-
                          Created is the creation timestamp.
                          Extracted from: org.opencontainers.image.created
                        type: string
                      description:
                        description: |-
                          Description is a brief description.
                          Extracted from: org.opencontainers.image.description
                        type: string
                      documentation:
                        description: |-
                          Documentation is the URL to documentation.
                          Extracted from: org.opencontainers.image.documentation
                        type: string
                      licenses:
                        description: |-
                          Licenses is the SPDX license identifier(s).
                          Extracted from: org.opencontainers.image.licenses
                        type: string
                      revision:
                        description: |-
                          Revision is the source control revision.
                          Extracted from: org.opencontainers.image.revision
                        type: string
                      source:
                        description: |-
                          Source is the URL to the source code repository.
                          Extracted from: org.opencontainers.image.source
                        type: string
                      title:
                        description: |-
                          Title is the human-readable title.
                          Extracted from: org.opencontainers.image.title
                        type: string
                      vendor:
                        description: |-
                          Vendor is the organization that produced the image.
                          Extracted from: org.opencontainers.image.vendor
                        type: string
                      version:
                        description: |-
                          Version is the image version.
                          Extracted from: org.opencontainers.image.version
                        type: string
                    type: object
                  originalLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      OriginalLabels contains the raw OCI image labels as a JSON object.
                      This preserves all labels from the image, including those not mapped to structured fields.
                    type: object
                type: object
              imagePullSecrets:
                description: |-
                  ImagePullSecrets lists secrets containing credentials for pulling the model container image.
                  These secrets are used for:
                  - OCI registry metadata extraction during discovery
                  - Pulling the image for inference services
                  The secrets are merged with any runtime config defaults.
                  For namespace-scoped models, secrets must exist in the same namespace.
                  For cluster-scoped models, secrets must exist in the operator namespace.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              modelSources:
                description: |-
                  ModelSources specifies the model sources to use for this model.
                  When specified, these sources are used instead of auto-discovery from the container image.
                  This enables pre-creating custom models with explicit model sources.
                  The size field is optional - if not specified, it will be discovered by the download job.
                  AIM runtime currently supports only one model source.
                items:
                  description: |-
                    AIMModelSource describes a model artifact that must be downloaded for inference.
                    Discovery extracts these from the container's configuration to enable caching and validation.
                  properties:
                    env:
                      description: |-
                        Env specifies per-source credential overrides.
                        These variables are used for authentication when downloading this specific source.
                        Takes precedence over base-level env for the same variable name.
                      items:
                        description: EnvVar represents an environment variable present
                          in a Container.
                        properties:
                          name:
                            description: |-
                              Name of the environment variable.
                              May consist of any printable ASCII characters except '='.
                            type: string
                          value:
                            description: |-
                              Variable references $(VAR_NAME) are expanded
                              using the previously defined environment variables in the container and
                              any service environment variables. If a variable cannot be resolved,
                              the reference in the input string will be unchanged. Double $$ are reduced
                              to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                              "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                              Escaped references will never be expanded, regardless of whether the variable
                              exists or not.
                              Defaults to "".
                            type: string
                          valueFrom:
                            description: Source for the environment variable's value.
                              Cannot be used if value is not empty.
                            properties:
                              configMapKeyRef:
                                description: Selects a key of a ConfigMap.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the ConfigMap or
                                      its key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              fieldRef:
                                description: |-
                                  Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                  spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                properties:
                                  apiVersion:
                                    description: Version of the schema the FieldPath
                                      is written in terms of, defaults to "v1".
                                    type: string
                                  fieldPath:
                                    description: Path of the field to select in the
                                      specified API version.
                                    type: string
                                required:
                                - fieldPath
                                type: object
                                x-kubernetes-map-type: atomic
                              fileKeyRef:
                                description: |-
                                  FileKeyRef selects a key of the env file.
                                  Requires the EnvFiles feature gate to be enabled.
                                properties:
                                  key:
                                    description: |-
                                      The key within the env file. An invalid key will prevent the pod from starting.
                                      The keys defined within a source may consist of any printable ASCII characters except '='.
                                      During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                    type: string
                                  optional:
                                    default: false
                                    description: |-
                                      Specify whether the file or its key must be defined. If the file or key
                                      does not exist, then the env var is not published.
                                      If optional is set to true and the specified key does not exist,
                                      the environment variable will not be set in the Pod's containers.

                                      If optional is set to false and the specified key does not exist,
                                      an error will be returned during Pod creation.
                                    type: boolean
                                  path:
                                    description: |-
                                      The path within the volume from which to select the file.
                                      Must be relative and may not contain the '..' path or start with '..'.
                                    type: string
                                  volumeName:
                                    description: The name of the volume mount containing
                                      the env file.
                                    type: string
                                required:
                                - key
                                - path
                                - volumeName
                                type: object
                                x-kubernetes-map-type: atomic
                              resourceFieldRef:
                                description: |-
                                  Selects a resource of the container: only resources limits and requests
                                  (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                properties:
                                  containerName:
                                    description: 'Container name: required for volumes,
                                      optional for env vars'
                                    type: string
                                  divisor:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: Specifies the output format of the
                                      exposed resources, defaults to "1"
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  resource:
                                    description: 'Required: resource to select'
                                    type: string
                                required:
                                - resource
                                type: object
                                x-kubernetes-map-type: atomic
                              secretKeyRef:
                                description: Selects a key of a secret in the pod's
                                  namespace
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                        required:
                        - name
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - name
                      x-kubernetes-list-type: map
                    modelId:
                      description: |-
                        ModelID is the canonical identifier in {org}/{name} format.
                        Determines the cache mount path: /workspace/cache/{modelId}
                        For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                        For S3 sources, users define their own organizational structure.
                      pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                      type: string
                    precision:
                      allOf:
                      - enum:
                        - auto
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                      - enum:
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                      description: |-
                        Precision describes the runtime precision this source is compatible with.
                        Used to match model sources to profiles during custom weight onboarding.
                      type: string
                    size:
                      anyOf:
                      - type: integer
                      - type: string
                      description: |-
                        Size is the expected storage space required for this model artifact.
                        Used for PVC sizing and capacity planning during cache creation.
                        Optional - if not specified, the download job will discover the size automatically.
                        Can be set explicitly to pre-allocate storage or override auto-discovery.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    sourceUri:
                      description: |-
                        SourceURI is the location from which the model should be downloaded.
                        Supported schemes:
                        - hf://org/model - Hugging Face Hub model
                        - s3://bucket/key - S3-compatible storage
                      pattern: ^(hf|s3)://[^ \t\r\n]+$
                      type: string
                  required:
                  - modelId
                  - sourceUri
                  type: object
                maxItems: 1
                type: array
              profileCopy:
                description: |-
                  ProfileCopy reuses the AIMProfileSet derivation shape so an AIMModel can
                  publish derivative AIMProfiles directly. The controller may synthesize a
                  child AIMProfileSet and fill SourceRef when image-backed discovery is
                  involved. Mutually exclusive with all deprecated v1alpha1 fields.

                  DEPRECATED on v1alpha2: use spec.profiles. v1alpha1 still accepts
                  ProfileCopy. v1alpha2 CRD CEL forbids ProfileCopy.
                properties:
                  image:
                    description: Image overrides the runtime image used by the derived
                      profiles.
                    type: string
                  imagePullSecrets:
                    description: ImagePullSecrets lists secrets used for inspecting
                      and pulling container images.
                    items:
                      description: |-
                        LocalObjectReference contains enough information to let you locate the
                        referenced object inside the same namespace.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    type: array
                  overrides:
                    description: Overrides mutates the copied profile spec after selection
                      and version filtering.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount replaces the copied profile's
                          acceleratorCount.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel replaces the copied profile's
                          acceleratorModel.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode replaces the copied profile's
                          acceleratorPartitioningMode. Complete replacement, not a merge; an empty
                          override string leaves the source profile's mode untouched. Same reserved
                          values as AIMProfileSpecCommon.AcceleratorPartitioningMode. Whenever this
                          override is set (to any value, including "unpartitioned"), the CEL rule on the
                          enclosing spec also requires AcceleratorCount to be set: partition mode
                          changes the per-unit interpretation of acceleratorCount, and CEL cannot
                          read the base profile to tell whether the meaning actually changed, so it
                          conservatively requires the count be restated.
                        type: string
                      aimId:
                        description: |-
                          AimId stamps the derived profile's spec.aimId. When set, wins over
                          the source profile's aimId. REQUIRED when the enclosing selector has
                          role=base (the source base profile carries no aimId of its own).
                        type: string
                      containerEnv:
                        description: ContainerEnv merges by env var name, overriding
                          matching source entries.
                        items:
                          description: EnvVar represents an environment variable present
                            in a Container.
                          properties:
                            name:
                              description: |-
                                Name of the environment variable.
                                May consist of any printable ASCII characters except '='.
                              type: string
                            value:
                              description: |-
                                Variable references $(VAR_NAME) are expanded
                                using the previously defined environment variables in the container and
                                any service environment variables. If a variable cannot be resolved,
                                the reference in the input string will be unchanged. Double $$ are reduced
                                to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                Escaped references will never be expanded, regardless of whether the variable
                                exists or not.
                                Defaults to "".
                              type: string
                            valueFrom:
                              description: Source for the environment variable's value.
                                Cannot be used if value is not empty.
                              properties:
                                configMapKeyRef:
                                  description: Selects a key of a ConfigMap.
                                  properties:
                                    key:
                                      description: The key to select.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the ConfigMap or
                                        its key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fieldRef:
                                  description: |-
                                    Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                    spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                  properties:
                                    apiVersion:
                                      description: Version of the schema the FieldPath
                                        is written in terms of, defaults to "v1".
                                      type: string
                                    fieldPath:
                                      description: Path of the field to select in
                                        the specified API version.
                                      type: string
                                  required:
                                  - fieldPath
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fileKeyRef:
                                  description: |-
                                    FileKeyRef selects a key of the env file.
                                    Requires the EnvFiles feature gate to be enabled.
                                  properties:
                                    key:
                                      description: |-
                                        The key within the env file. An invalid key will prevent the pod from starting.
                                        The keys defined within a source may consist of any printable ASCII characters except '='.
                                        During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                      type: string
                                    optional:
                                      default: false
                                      description: |-
                                        Specify whether the file or its key must be defined. If the file or key
                                        does not exist, then the env var is not published.
                                        If optional is set to true and the specified key does not exist,
                                        the environment variable will not be set in the Pod's containers.

                                        If optional is set to false and the specified key does not exist,
                                        an error will be returned during Pod creation.
                                      type: boolean
                                    path:
                                      description: |-
                                        The path within the volume from which to select the file.
                                        Must be relative and may not contain the '..' path or start with '..'.
                                      type: string
                                    volumeName:
                                      description: The name of the volume mount containing
                                        the env file.
                                      type: string
                                  required:
                                  - key
                                  - path
                                  - volumeName
                                  type: object
                                  x-kubernetes-map-type: atomic
                                resourceFieldRef:
                                  description: |-
                                    Selects a resource of the container: only resources limits and requests
                                    (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                  properties:
                                    containerName:
                                      description: 'Container name: required for volumes,
                                        optional for env vars'
                                      type: string
                                    divisor:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      description: Specifies the output format of
                                        the exposed resources, defaults to "1"
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    resource:
                                      description: 'Required: resource to select'
                                      type: string
                                  required:
                                  - resource
                                  type: object
                                  x-kubernetes-map-type: atomic
                                secretKeyRef:
                                  description: Selects a key of a secret in the pod's
                                    namespace
                                  properties:
                                    key:
                                      description: The key of the secret to select
                                        from.  Must be a valid secret key.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the Secret or its
                                        key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      engineArgs:
                        description: EngineArgs shallow-merges on top of the source
                          engineArgs, overriding matching keys.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      engineEnv:
                        additionalProperties:
                          type: string
                        description: EngineEnv merges by key, overriding matching
                          source entries.
                        type: object
                      image:
                        description: |-
                          Image overrides the runtime container image used by the derived
                          profiles. When empty the deployment image is rebased onto the source
                          profile's base-image (status.baseImage) so private mirrors stay
                          self-contained.
                        type: string
                      modelId:
                        description: |-
                          ModelId stamps the derived profile's spec.modelId. When set, wins
                          over both the source profile's modelId AND the auto-derivation from
                          modelSources[0].modelId. REQUIRED when the enclosing selector has
                          role=base.
                        type: string
                      modelSources:
                        description: |-
                          ModelSources replaces the copied profile's modelSources. When
                          modelSources[0].modelId is set and overrides.modelId is unset, the
                          derived profile's modelId is auto-derived from modelSources[0]; an
                          explicit overrides.modelId always wins.
                        items:
                          description: |-
                            AIMModelSource describes a model artifact that must be downloaded for inference.
                            Discovery extracts these from the container's configuration to enable caching and validation.
                          properties:
                            env:
                              description: |-
                                Env specifies per-source credential overrides.
                                These variables are used for authentication when downloading this specific source.
                                Takes precedence over base-level env for the same variable name.
                              items:
                                description: EnvVar represents an environment variable
                                  present in a Container.
                                properties:
                                  name:
                                    description: |-
                                      Name of the environment variable.
                                      May consist of any printable ASCII characters except '='.
                                    type: string
                                  value:
                                    description: |-
                                      Variable references $(VAR_NAME) are expanded
                                      using the previously defined environment variables in the container and
                                      any service environment variables. If a variable cannot be resolved,
                                      the reference in the input string will be unchanged. Double $$ are reduced
                                      to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                      "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                      Escaped references will never be expanded, regardless of whether the variable
                                      exists or not.
                                      Defaults to "".
                                    type: string
                                  valueFrom:
                                    description: Source for the environment variable's
                                      value. Cannot be used if value is not empty.
                                    properties:
                                      configMapKeyRef:
                                        description: Selects a key of a ConfigMap.
                                        properties:
                                          key:
                                            description: The key to select.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the ConfigMap
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fieldRef:
                                        description: |-
                                          Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                          spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                        properties:
                                          apiVersion:
                                            description: Version of the schema the
                                              FieldPath is written in terms of, defaults
                                              to "v1".
                                            type: string
                                          fieldPath:
                                            description: Path of the field to select
                                              in the specified API version.
                                            type: string
                                        required:
                                        - fieldPath
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fileKeyRef:
                                        description: |-
                                          FileKeyRef selects a key of the env file.
                                          Requires the EnvFiles feature gate to be enabled.
                                        properties:
                                          key:
                                            description: |-
                                              The key within the env file. An invalid key will prevent the pod from starting.
                                              The keys defined within a source may consist of any printable ASCII characters except '='.
                                              During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                            type: string
                                          optional:
                                            default: false
                                            description: |-
                                              Specify whether the file or its key must be defined. If the file or key
                                              does not exist, then the env var is not published.
                                              If optional is set to true and the specified key does not exist,
                                              the environment variable will not be set in the Pod's containers.

                                              If optional is set to false and the specified key does not exist,
                                              an error will be returned during Pod creation.
                                            type: boolean
                                          path:
                                            description: |-
                                              The path within the volume from which to select the file.
                                              Must be relative and may not contain the '..' path or start with '..'.
                                            type: string
                                          volumeName:
                                            description: The name of the volume mount
                                              containing the env file.
                                            type: string
                                        required:
                                        - key
                                        - path
                                        - volumeName
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      resourceFieldRef:
                                        description: |-
                                          Selects a resource of the container: only resources limits and requests
                                          (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                        properties:
                                          containerName:
                                            description: 'Container name: required
                                              for volumes, optional for env vars'
                                            type: string
                                          divisor:
                                            anyOf:
                                            - type: integer
                                            - type: string
                                            description: Specifies the output format
                                              of the exposed resources, defaults to
                                              "1"
                                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                            x-kubernetes-int-or-string: true
                                          resource:
                                            description: 'Required: resource to select'
                                            type: string
                                        required:
                                        - resource
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      secretKeyRef:
                                        description: Selects a key of a secret in
                                          the pod's namespace
                                        properties:
                                          key:
                                            description: The key of the secret to
                                              select from.  Must be a valid secret
                                              key.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the Secret
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                    type: object
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            modelId:
                              description: |-
                                ModelID is the canonical identifier in {org}/{name} format.
                                Determines the cache mount path: /workspace/cache/{modelId}
                                For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                                For S3 sources, users define their own organizational structure.
                              pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                              type: string
                            precision:
                              allOf:
                              - enum:
                                - auto
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              - enum:
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              description: |-
                                Precision describes the runtime precision this source is compatible with.
                                Used to match model sources to profiles during custom weight onboarding.
                              type: string
                            size:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                Size is the expected storage space required for this model artifact.
                                Used for PVC sizing and capacity planning during cache creation.
                                Optional - if not specified, the download job will discover the size automatically.
                                Can be set explicitly to pre-allocate storage or override auto-discovery.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            sourceUri:
                              description: |-
                                SourceURI is the location from which the model should be downloaded.
                                Supported schemes:
                                - hf://org/model - Hugging Face Hub model
                                - s3://bucket/key - S3-compatible storage
                              pattern: ^(hf|s3)://[^ \t\r\n]+$
                              type: string
                          required:
                          - modelId
                          - sourceUri
                          type: object
                        type: array
                      profileId:
                        description: |-
                          ProfileId stamps the derived profile's spec.profileId. Optional —
                          most callers leave this empty and let the source profile's profileId
                          carry through (or the reconciler synthesise one).
                        type: string
                    type: object
                  selector:
                    description: Selector chooses which source profiles to derive
                      from.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount filters by accelerator unit
                          count.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel filters by accelerator identifier.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode filters candidates by their declared
                          partitioning mode. Partial-order match (NOT strict equality):
                            ""              - no filter on this field.
                            "unpartitioned" - matches profiles with mode "" or "unpartitioned".
                            "partitioned"   - matches profiles whose mode is non-trivial (anything
                                              other than "" / "unpartitioned").
                            "<C>"           - selector-only convenience: matches profiles with mode
                                              "<C>-*" (prefix on the scheme). Not a valid profile-spec
                                              value (e.g. selector "CPX" matches "CPX-NPS1", "CPX-NPS4").
                            "<C>-<M>"       - exact-string match on the scheme.
                        type: string
                      acceleratorType:
                        description: AcceleratorType filters by accelerator resource
                          type.
                        enum:
                        - gpu
                        - cpu
                        type: string
                      aimId:
                        description: AimId filters by model architecture identifier
                          (e.g., "qwen/qwen3-32b").
                        type: string
                      engine:
                        description: Engine filters by inference engine.
                        type: string
                      engineArgs:
                        description: |-
                          EngineArgs partially matches source engineArgs: every provided top-level key must
                          exist in the source object with an equal value.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      metric:
                        description: Metric filters by optimization target.
                        enum:
                        - latency
                        - throughput
                        type: string
                      minimumType:
                        description: |-
                          MinimumType filters by a minimum optimization level: candidates whose
                          type is this tier OR BETTER are accepted (hierarchy: optimized > general
                          > preview > unoptimized). This is the floor counterpart to the exact-match
                          Type field; the two AND together when both are set.

                          The sentinel "any" disables the floor (accept every tier). When this
                          field is empty the AIMService resolver applies a default floor of
                          "optimized" so auto-selection prefers production-grade profiles and never
                          silently picks an unoptimized one; to opt a service into lower tiers
                          (e.g. CPU/EPYC profiles published as unoptimized) set minimumType
                          explicitly to "unoptimized" or "any". Derivation selectors
                          (AIMProfileSet / AIMModel.profiles) treat empty as "any" so copying is
                          never tier-restricted by default.
                        enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                        - any
                        type: string
                      modelId:
                        description: ModelId filters by the source profile's specific
                          model identifier.
                        type: string
                      modelRef:
                        description: |-
                          ModelRef narrows candidates to those produced by a specific
                          AIM(Cluster)Model, matched via the `aim.eai.amd.com/source-model[-scope]`
                          labels stamped by the AIMModel reconcilers. Iteration 1 (v1alpha2 only).
                        properties:
                          name:
                            description: Name is the owning AIM(Cluster)Model name.
                              Required.
                            minLength: 1
                            type: string
                          scope:
                            default: Auto
                            description: Scope controls how Name is resolved against
                              AIMModel vs AIMClusterModel.
                            enum:
                            - Auto
                            - Namespace
                            - Cluster
                            type: string
                        required:
                        - name
                        type: object
                      origin:
                        description: |-
                          Origin filters by the `aim.eai.amd.com/profile-origin` label. When unset
                          (empty) the selector does not filter by origin. Iteration 1 (v1alpha2
                          only).
                        enum:
                        - discovered
                        - derived
                        - user-authored
                        type: string
                      precision:
                        description: Precision filters by numeric precision.
                        enum:
                        - auto
                        - fp4
                        - fp8
                        - fp16
                        - fp32
                        - bf16
                        - int4
                        - int8
                        type: string
                      profileId:
                        description: ProfileId filters by the source profile's profile
                          identifier.
                        type: string
                      role:
                        default: deployable
                        description: |-
                          Role filters by the `aim.eai.amd.com/profile-role` label. Defaults to
                          `deployable`. `base` filters to base profiles emitted by base-image
                          discovery (custom-model derivation source material).
                        enum:
                        - base
                        - deployable
                        type: string
                      type:
                        description: Type filters by optimization level (exact match).
                        enum:
                        - optimized
                        - general
                        - preview
                        - unoptimized
                        type: string
                    type: object
                  serviceAccountName:
                    description: ServiceAccountName is propagated to managed profiles
                      for downstream workloads.
                    type: string
                  sourceRef:
                    description: |-
                      SourceRef points to an alternate discovery cache source.
                      When omitted, derivation uses visible AIMProfile and AIMClusterProfile objects.
                      For AIMProfileSet, the referenced ConfigMap is read from the same namespace.
                      For AIMClusterProfileSet, it is read from the operator namespace.
                    properties:
                      name:
                        description: Name is the discovery cache ConfigMap name.
                        type: string
                    required:
                    - name
                    type: object
                  version:
                    description: Version pins matching to a specific source profile
                      version when VersionPolicy is pinned.
                    type: string
                  versionPolicy:
                    default: pinned
                    description: VersionPolicy controls how matching profiles are
                      filtered by version.
                    enum:
                    - pinned
                    - latest
                    - all
                    type: string
                type: object
                x-kubernetes-validations:
                - message: selector must set at least one matching field, or set sourceRef
                  rule: has(self.sourceRef) || has(self.selector.aimId) || has(self.selector.modelId)
                    || has(self.selector.profileId) || has(self.selector.engine) ||
                    has(self.selector.metric) || has(self.selector.precision) || has(self.selector.type)
                    || has(self.selector.acceleratorModel) || has(self.selector.acceleratorType)
                    || has(self.selector.acceleratorCount) || has(self.selector.engineArgs)
                    || has(self.selector.modelRef) || has(self.selector.origin)
                - message: version is required when versionPolicy is pinned
                  rule: has(self.version) || (has(self.versionPolicy) && self.versionPolicy
                    != 'pinned')
                - message: latest/all versionPolicy must not set version
                  rule: '!has(self.versionPolicy) || (self.versionPolicy != ''latest''
                    && self.versionPolicy != ''all'') || !has(self.version)'
                - message: selector.aimId/modelId/profileId are not allowed when selector.role=base;
                    set overrides.aimId/modelId/profileId instead (base profiles have
                    no source identity to filter on)
                  rule: '!(has(self.selector.role) && self.selector.role == ''base'')
                    || (!has(self.selector.aimId) && !has(self.selector.modelId) &&
                    !has(self.selector.profileId))'
                - message: selector.role=base requires overrides.aimId and overrides.modelId
                    (base profiles carry no identity of their own; overrides supply
                    it)
                  rule: '!(has(self.selector.role) && self.selector.role == ''base'')
                    || (has(self.overrides) && has(self.overrides.aimId) && has(self.overrides.modelId))'
                - message: overrides.acceleratorCount must be specified together with
                    any overrides.acceleratorPartitioningMode; partition mode changes
                    the per-unit interpretation of acceleratorCount
                  rule: '!has(self.overrides) || !has(self.overrides.acceleratorPartitioningMode)
                    || size(self.overrides.acceleratorPartitioningMode) == 0 || has(self.overrides.acceleratorCount)'
              profiles:
                description: |-
                  Profiles is the v1alpha2 fine-tune / custom-model onboarding surface.
                  It groups the source descriptor (`profiles.derivedFrom.selector` /
                  `profiles.derivedFrom.sourceRef`), the version filter
                  (`profiles.versionPolicy` / `profiles.version`), and the modifications
                  applied to copies (`profiles.overrides`). When set, the AIMModel
                  reconciler synthesises a child AIMProfileSet from this block.

                  Mutually exclusive with spec.image (exactly one of the two is required
                  for v1alpha2 AIMModel). v1alpha1 rejects spec.profiles via per-version
                  CEL.
                properties:
                  derivedFrom:
                    description: DerivedFrom identifies the source profiles to copy
                      from.
                    properties:
                      selector:
                        description: |-
                          Selector chooses which source profiles to derive from. Discovery of a
                          deployable AIM image stamps role=deployable; selector.role=base targets
                          the base profiles emitted by base-image discovery (custom-model
                          derivation source material).
                        properties:
                          acceleratorCount:
                            description: AcceleratorCount filters by accelerator unit
                              count.
                            format: int32
                            type: integer
                          acceleratorModel:
                            description: AcceleratorModel filters by accelerator identifier.
                            type: string
                          acceleratorPartitioningMode:
                            description: |-
                              AcceleratorPartitioningMode filters candidates by their declared
                              partitioning mode. Partial-order match (NOT strict equality):
                                ""              - no filter on this field.
                                "unpartitioned" - matches profiles with mode "" or "unpartitioned".
                                "partitioned"   - matches profiles whose mode is non-trivial (anything
                                                  other than "" / "unpartitioned").
                                "<C>"           - selector-only convenience: matches profiles with mode
                                                  "<C>-*" (prefix on the scheme). Not a valid profile-spec
                                                  value (e.g. selector "CPX" matches "CPX-NPS1", "CPX-NPS4").
                                "<C>-<M>"       - exact-string match on the scheme.
                            type: string
                          acceleratorType:
                            description: AcceleratorType filters by accelerator resource
                              type.
                            enum:
                            - gpu
                            - cpu
                            type: string
                          aimId:
                            description: AimId filters by model architecture identifier
                              (e.g., "qwen/qwen3-32b").
                            type: string
                          engine:
                            description: Engine filters by inference engine.
                            type: string
                          engineArgs:
                            description: |-
                              EngineArgs partially matches source engineArgs: every provided top-level key must
                              exist in the source object with an equal value.
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          metric:
                            description: Metric filters by optimization target.
                            enum:
                            - latency
                            - throughput
                            type: string
                          minimumType:
                            description: |-
                              MinimumType filters by a minimum optimization level: candidates whose
                              type is this tier OR BETTER are accepted (hierarchy: optimized > general
                              > preview > unoptimized). This is the floor counterpart to the exact-match
                              Type field; the two AND together when both are set.

                              The sentinel "any" disables the floor (accept every tier). When this
                              field is empty the AIMService resolver applies a default floor of
                              "optimized" so auto-selection prefers production-grade profiles and never
                              silently picks an unoptimized one; to opt a service into lower tiers
                              (e.g. CPU/EPYC profiles published as unoptimized) set minimumType
                              explicitly to "unoptimized" or "any". Derivation selectors
                              (AIMProfileSet / AIMModel.profiles) treat empty as "any" so copying is
                              never tier-restricted by default.
                            enum:
                            - optimized
                            - general
                            - preview
                            - unoptimized
                            - any
                            type: string
                          modelId:
                            description: ModelId filters by the source profile's specific
                              model identifier.
                            type: string
                          modelRef:
                            description: |-
                              ModelRef narrows candidates to those produced by a specific
                              AIM(Cluster)Model, matched via the `aim.eai.amd.com/source-model[-scope]`
                              labels stamped by the AIMModel reconcilers. Iteration 1 (v1alpha2 only).
                            properties:
                              name:
                                description: Name is the owning AIM(Cluster)Model
                                  name. Required.
                                minLength: 1
                                type: string
                              scope:
                                default: Auto
                                description: Scope controls how Name is resolved against
                                  AIMModel vs AIMClusterModel.
                                enum:
                                - Auto
                                - Namespace
                                - Cluster
                                type: string
                            required:
                            - name
                            type: object
                          origin:
                            description: |-
                              Origin filters by the `aim.eai.amd.com/profile-origin` label. When unset
                              (empty) the selector does not filter by origin. Iteration 1 (v1alpha2
                              only).
                            enum:
                            - discovered
                            - derived
                            - user-authored
                            type: string
                          precision:
                            description: Precision filters by numeric precision.
                            enum:
                            - auto
                            - fp4
                            - fp8
                            - fp16
                            - fp32
                            - bf16
                            - int4
                            - int8
                            type: string
                          profileId:
                            description: ProfileId filters by the source profile's
                              profile identifier.
                            type: string
                          role:
                            default: deployable
                            description: |-
                              Role filters by the `aim.eai.amd.com/profile-role` label. Defaults to
                              `deployable`. `base` filters to base profiles emitted by base-image
                              discovery (custom-model derivation source material).
                            enum:
                            - base
                            - deployable
                            type: string
                          type:
                            description: Type filters by optimization level (exact
                              match).
                            enum:
                            - optimized
                            - general
                            - preview
                            - unoptimized
                            type: string
                        type: object
                      sourceRef:
                        description: |-
                          SourceRef points to an alternate discovery cache source (a
                          pre-populated ConfigMap of profile YAMLs) instead of using the
                          visible AIMProfile / AIMClusterProfile objects.
                        properties:
                          name:
                            description: Name is the discovery cache ConfigMap name.
                            type: string
                        required:
                        - name
                        type: object
                    type: object
                  overrides:
                    description: |-
                      Overrides mutates the copied profile spec after selection and version
                      filtering. Use overrides.image to override the deployment container
                      image used by the derived profiles.
                    properties:
                      acceleratorCount:
                        description: AcceleratorCount replaces the copied profile's
                          acceleratorCount.
                        format: int32
                        type: integer
                      acceleratorModel:
                        description: AcceleratorModel replaces the copied profile's
                          acceleratorModel.
                        type: string
                      acceleratorPartitioningMode:
                        description: |-
                          AcceleratorPartitioningMode replaces the copied profile's
                          acceleratorPartitioningMode. Complete replacement, not a merge; an empty
                          override string leaves the source profile's mode untouched. Same reserved
                          values as AIMProfileSpecCommon.AcceleratorPartitioningMode. Whenever this
                          override is set (to any value, including "unpartitioned"), the CEL rule on the
                          enclosing spec also requires AcceleratorCount to be set: partition mode
                          changes the per-unit interpretation of acceleratorCount, and CEL cannot
                          read the base profile to tell whether the meaning actually changed, so it
                          conservatively requires the count be restated.
                        type: string
                      aimId:
                        description: |-
                          AimId stamps the derived profile's spec.aimId. When set, wins over
                          the source profile's aimId. REQUIRED when the enclosing selector has
                          role=base (the source base profile carries no aimId of its own).
                        type: string
                      containerEnv:
                        description: ContainerEnv merges by env var name, overriding
                          matching source entries.
                        items:
                          description: EnvVar represents an environment variable present
                            in a Container.
                          properties:
                            name:
                              description: |-
                                Name of the environment variable.
                                May consist of any printable ASCII characters except '='.
                              type: string
                            value:
                              description: |-
                                Variable references $(VAR_NAME) are expanded
                                using the previously defined environment variables in the container and
                                any service environment variables. If a variable cannot be resolved,
                                the reference in the input string will be unchanged. Double $$ are reduced
                                to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                Escaped references will never be expanded, regardless of whether the variable
                                exists or not.
                                Defaults to "".
                              type: string
                            valueFrom:
                              description: Source for the environment variable's value.
                                Cannot be used if value is not empty.
                              properties:
                                configMapKeyRef:
                                  description: Selects a key of a ConfigMap.
                                  properties:
                                    key:
                                      description: The key to select.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the ConfigMap or
                                        its key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fieldRef:
                                  description: |-
                                    Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                    spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                  properties:
                                    apiVersion:
                                      description: Version of the schema the FieldPath
                                        is written in terms of, defaults to "v1".
                                      type: string
                                    fieldPath:
                                      description: Path of the field to select in
                                        the specified API version.
                                      type: string
                                  required:
                                  - fieldPath
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fileKeyRef:
                                  description: |-
                                    FileKeyRef selects a key of the env file.
                                    Requires the EnvFiles feature gate to be enabled.
                                  properties:
                                    key:
                                      description: |-
                                        The key within the env file. An invalid key will prevent the pod from starting.
                                        The keys defined within a source may consist of any printable ASCII characters except '='.
                                        During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                      type: string
                                    optional:
                                      default: false
                                      description: |-
                                        Specify whether the file or its key must be defined. If the file or key
                                        does not exist, then the env var is not published.
                                        If optional is set to true and the specified key does not exist,
                                        the environment variable will not be set in the Pod's containers.

                                        If optional is set to false and the specified key does not exist,
                                        an error will be returned during Pod creation.
                                      type: boolean
                                    path:
                                      description: |-
                                        The path within the volume from which to select the file.
                                        Must be relative and may not contain the '..' path or start with '..'.
                                      type: string
                                    volumeName:
                                      description: The name of the volume mount containing
                                        the env file.
                                      type: string
                                  required:
                                  - key
                                  - path
                                  - volumeName
                                  type: object
                                  x-kubernetes-map-type: atomic
                                resourceFieldRef:
                                  description: |-
                                    Selects a resource of the container: only resources limits and requests
                                    (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                  properties:
                                    containerName:
                                      description: 'Container name: required for volumes,
                                        optional for env vars'
                                      type: string
                                    divisor:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      description: Specifies the output format of
                                        the exposed resources, defaults to "1"
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    resource:
                                      description: 'Required: resource to select'
                                      type: string
                                  required:
                                  - resource
                                  type: object
                                  x-kubernetes-map-type: atomic
                                secretKeyRef:
                                  description: Selects a key of a secret in the pod's
                                    namespace
                                  properties:
                                    key:
                                      description: The key of the secret to select
                                        from.  Must be a valid secret key.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the Secret or its
                                        key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      engineArgs:
                        description: EngineArgs shallow-merges on top of the source
                          engineArgs, overriding matching keys.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      engineEnv:
                        additionalProperties:
                          type: string
                        description: EngineEnv merges by key, overriding matching
                          source entries.
                        type: object
                      image:
                        description: |-
                          Image overrides the runtime container image used by the derived
                          profiles. When empty the deployment image is rebased onto the source
                          profile's base-image (status.baseImage) so private mirrors stay
                          self-contained.
                        type: string
                      modelId:
                        description: |-
                          ModelId stamps the derived profile's spec.modelId. When set, wins
                          over both the source profile's modelId AND the auto-derivation from
                          modelSources[0].modelId. REQUIRED when the enclosing selector has
                          role=base.
                        type: string
                      modelSources:
                        description: |-
                          ModelSources replaces the copied profile's modelSources. When
                          modelSources[0].modelId is set and overrides.modelId is unset, the
                          derived profile's modelId is auto-derived from modelSources[0]; an
                          explicit overrides.modelId always wins.
                        items:
                          description: |-
                            AIMModelSource describes a model artifact that must be downloaded for inference.
                            Discovery extracts these from the container's configuration to enable caching and validation.
                          properties:
                            env:
                              description: |-
                                Env specifies per-source credential overrides.
                                These variables are used for authentication when downloading this specific source.
                                Takes precedence over base-level env for the same variable name.
                              items:
                                description: EnvVar represents an environment variable
                                  present in a Container.
                                properties:
                                  name:
                                    description: |-
                                      Name of the environment variable.
                                      May consist of any printable ASCII characters except '='.
                                    type: string
                                  value:
                                    description: |-
                                      Variable references $(VAR_NAME) are expanded
                                      using the previously defined environment variables in the container and
                                      any service environment variables. If a variable cannot be resolved,
                                      the reference in the input string will be unchanged. Double $$ are reduced
                                      to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                      "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                      Escaped references will never be expanded, regardless of whether the variable
                                      exists or not.
                                      Defaults to "".
                                    type: string
                                  valueFrom:
                                    description: Source for the environment variable's
                                      value. Cannot be used if value is not empty.
                                    properties:
                                      configMapKeyRef:
                                        description: Selects a key of a ConfigMap.
                                        properties:
                                          key:
                                            description: The key to select.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the ConfigMap
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fieldRef:
                                        description: |-
                                          Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                          spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                        properties:
                                          apiVersion:
                                            description: Version of the schema the
                                              FieldPath is written in terms of, defaults
                                              to "v1".
                                            type: string
                                          fieldPath:
                                            description: Path of the field to select
                                              in the specified API version.
                                            type: string
                                        required:
                                        - fieldPath
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fileKeyRef:
                                        description: |-
                                          FileKeyRef selects a key of the env file.
                                          Requires the EnvFiles feature gate to be enabled.
                                        properties:
                                          key:
                                            description: |-
                                              The key within the env file. An invalid key will prevent the pod from starting.
                                              The keys defined within a source may consist of any printable ASCII characters except '='.
                                              During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                            type: string
                                          optional:
                                            default: false
                                            description: |-
                                              Specify whether the file or its key must be defined. If the file or key
                                              does not exist, then the env var is not published.
                                              If optional is set to true and the specified key does not exist,
                                              the environment variable will not be set in the Pod's containers.

                                              If optional is set to false and the specified key does not exist,
                                              an error will be returned during Pod creation.
                                            type: boolean
                                          path:
                                            description: |-
                                              The path within the volume from which to select the file.
                                              Must be relative and may not contain the '..' path or start with '..'.
                                            type: string
                                          volumeName:
                                            description: The name of the volume mount
                                              containing the env file.
                                            type: string
                                        required:
                                        - key
                                        - path
                                        - volumeName
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      resourceFieldRef:
                                        description: |-
                                          Selects a resource of the container: only resources limits and requests
                                          (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                        properties:
                                          containerName:
                                            description: 'Container name: required
                                              for volumes, optional for env vars'
                                            type: string
                                          divisor:
                                            anyOf:
                                            - type: integer
                                            - type: string
                                            description: Specifies the output format
                                              of the exposed resources, defaults to
                                              "1"
                                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                            x-kubernetes-int-or-string: true
                                          resource:
                                            description: 'Required: resource to select'
                                            type: string
                                        required:
                                        - resource
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      secretKeyRef:
                                        description: Selects a key of a secret in
                                          the pod's namespace
                                        properties:
                                          key:
                                            description: The key of the secret to
                                              select from.  Must be a valid secret
                                              key.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the Secret
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                    type: object
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            modelId:
                              description: |-
                                ModelID is the canonical identifier in {org}/{name} format.
                                Determines the cache mount path: /workspace/cache/{modelId}
                                For HuggingFace sources, this typically mirrors the URI path (e.g., meta-llama/Llama-3-8B).
                                For S3 sources, users define their own organizational structure.
                              pattern: ^[a-zA-Z0-9_-]+/[a-zA-Z0-9._-]+$
                              type: string
                            precision:
                              allOf:
                              - enum:
                                - auto
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              - enum:
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                              description: |-
                                Precision describes the runtime precision this source is compatible with.
                                Used to match model sources to profiles during custom weight onboarding.
                              type: string
                            size:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                Size is the expected storage space required for this model artifact.
                                Used for PVC sizing and capacity planning during cache creation.
                                Optional - if not specified, the download job will discover the size automatically.
                                Can be set explicitly to pre-allocate storage or override auto-discovery.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            sourceUri:
                              description: |-
                                SourceURI is the location from which the model should be downloaded.
                                Supported schemes:
                                - hf://org/model - Hugging Face Hub model
                                - s3://bucket/key - S3-compatible storage
                              pattern: ^(hf|s3)://[^ \t\r\n]+$
                              type: string
                          required:
                          - modelId
                          - sourceUri
                          type: object
                        type: array
                      profileId:
                        description: |-
                          ProfileId stamps the derived profile's spec.profileId. Optional —
                          most callers leave this empty and let the source profile's profileId
                          carry through (or the reconciler synthesise one).
                        type: string
                    type: object
                  version:
                    description: |-
                      Version pins matching to a specific source profile version when
                      VersionPolicy is `pinned`.
                    type: string
                  versionPolicy:
                    default: pinned
                    description: VersionPolicy controls how matching profiles are
                      filtered by version.
                    enum:
                    - pinned
                    - latest
                    - all
                    type: string
                type: object
                x-kubernetes-validations:
                - message: profiles.derivedFrom is required when spec.profiles is
                    set
                  rule: has(self.derivedFrom)
                - message: profiles.version is required when versionPolicy is pinned
                  rule: has(self.version) || (has(self.versionPolicy) && self.versionPolicy
                    != 'pinned')
                - message: latest/all versionPolicy must not set version
                  rule: '!has(self.versionPolicy) || (self.versionPolicy != ''latest''
                    && self.versionPolicy != ''all'') || !has(self.version)'
                - message: derivedFrom.selector.aimId/modelId/profileId are not allowed
                    when selector.role=base; set overrides.aimId/modelId/profileId
                    instead (base profiles have no source identity to filter on)
                  rule: '!(has(self.derivedFrom.selector.role) && self.derivedFrom.selector.role
                    == ''base'') || (!has(self.derivedFrom.selector.aimId) && !has(self.derivedFrom.selector.modelId)
                    && !has(self.derivedFrom.selector.profileId))'
                - message: derivedFrom.selector.role=base requires overrides.aimId
                    and overrides.modelId (base profiles carry no identity of their
                    own; overrides supply it)
                  rule: '!(has(self.derivedFrom.selector.role) && self.derivedFrom.selector.role
                    == ''base'') || (has(self.overrides) && has(self.overrides.aimId)
                    && has(self.overrides.modelId))'
              resources:
                description: |-
                  Resources defines the default resource requirements for services using this model.
                  Template- or service-level values override these defaults.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              runtimeConfigName:
                description: |-
                  Name is the name of the runtime config to use for this resource. If a runtime config with this name exists both
                  as a namespace and a cluster runtime config, the values are merged together, the namespace config taking priority
                  over the cluster config when there are conflicts. If this field is empty or set to `default`, the namespace / cluster
                  runtime config with the name `default` is used, if it exists.
                type: string
              serviceAccountName:
                description: |-
                  ServiceAccountName specifies the Kubernetes service account to use for workloads related to this model.
                  This includes metadata extraction jobs and any other model-related operations.
                  If empty, the default service account for the namespace is used.
                type: string
            type: object
            x-kubernetes-validations:
            - message: when using modelSources without aimId, set custom.hardware
                or set hardware on each customTemplate
              rule: '!has(self.modelSources) || size(self.modelSources) == 0 || has(self.aimId)
                || (has(self.custom) && has(self.custom.hardware)) || !has(self.customTemplates)
                || size(self.customTemplates) == 0 || self.customTemplates.all(t,
                has(t.hardware) || (has(self.custom) && has(self.custom.hardware)))'
            - message: image is required unless aimId is set with versionPolicy latest
                or any, or profileCopy/derivedFrom/profiles is set
              rule: (has(self.image) && size(self.image) > 0) || has(self.profileCopy)
                || has(self.derivedFrom) || has(self.profiles) || (has(self.aimId)
                && has(self.custom) && has(self.custom.versionPolicy) && self.custom.versionPolicy
                != 'pinned')
            - message: profileCopy cannot be combined with deprecated legacy AIMModel
                fields
              rule: '!has(self.profileCopy) || (!has(self.discovery) && !has(self.defaultServiceTemplate)
                && !has(self.custom) && (!has(self.customTemplates) || size(self.customTemplates)
                == 0) && (!has(self.modelSources) || size(self.modelSources) == 0)
                && (!has(self.runtimeConfigName) || size(self.runtimeConfigName) ==
                0) && (!has(self.env) || size(self.env) == 0) && !has(self.imageMetadata))'
          status:
            description: AIMModelStatus defines the observed state of AIMModel.
            properties:
              aimId:
                description: |-
                  AimId is the resolved model architecture identifier for this model.
                  Populated by the v1alpha2 controller from spec.aimId or discovered metadata.
                type: string
              baseImage:
                description: |-
                  BaseImage is the extracted AIM base image reference (AIM_BASE_IMAGE_REF) when known.
                  Used when resolving deployment images for fine-tuned models that have no spec.image.
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the model's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              discoveredProfiles:
                description: DiscoveredProfiles summarizes the profiles found during
                  image discovery.
                properties:
                  byHardware:
                    description: |-
                      ByHardware groups discovered profiles by their hardware footprint and
                      reports whether each group is currently supported by the cluster. The
                      groups are stable across reconciles (sorted by acceleratorType,
                      acceleratorModel, acceleratorCount) so kubectl/jq queries are cheap.
                      Useful when debugging "why doesn't the {metric, precision} profile I
                      expect appear in my cluster?" — the breakdown surfaces every shape the
                      image emits, not only the ones materialised as AIMProfile objects.
                    items:
                      description: |-
                        ProfileHardwareGroup is one accelerator footprint within a model's
                        discovery catalog, with the metric/precision combos shipped under it.
                      properties:
                        acceleratorCount:
                          description: |-
                            AcceleratorCount is the number of accelerator units the profile
                            requests. For gpu: device count (e.g., 1, 2, 4, 8 for tensor-parallel
                            sizes). For cpu: number of CPU cores (e.g., 128 for EPYC_ZEN5,
                            192 for EPYC_9965).
                          format: int32
                          type: integer
                        acceleratorModel:
                          description: |-
                            AcceleratorModel is the accelerator identifier (e.g., "MI300X",
                            "EPYC_ZEN5"). Empty for profiles with no accelerator requirement.
                          type: string
                        acceleratorType:
                          description: AcceleratorType is the resource family (gpu,
                            cpu).
                          enum:
                          - gpu
                          - cpu
                          type: string
                        profiles:
                          description: |-
                            Profiles lists the {metric, precision} combinations discovered under
                            this hardware footprint. Reported even when the group is unsupported
                            so users can see what they're missing.
                          items:
                            description: |-
                              ProfileHardwareGroupEntry identifies one profile within a hardware group
                              by its {metric, precision} pair. Sufficient for users to spot whether the
                              optimization variant they want is shipped at all.
                            properties:
                              metric:
                                description: Metric is the optimization target (latency,
                                  throughput).
                                enum:
                                - latency
                                - throughput
                                type: string
                              precision:
                                description: Precision is the numeric precision (fp4,
                                  fp8, bf16, …).
                                enum:
                                - auto
                                - fp4
                                - fp8
                                - fp16
                                - fp32
                                - bf16
                                - int4
                                - int8
                                type: string
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        supported:
                          description: |-
                            Supported reports whether this hardware footprint is currently
                            satisfied by at least one cluster node. When false, all profiles in
                            this group are skipped during materialisation.
                          type: boolean
                      required:
                      - supported
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  supported:
                    description: Supported is the number of discovered profiles currently
                      supported by the cluster.
                    format: int32
                    type: integer
                  total:
                    description: Total is the number of profiles discovered from the
                      image.
                    format: int32
                    type: integer
                  unsupported:
                    description: Unsupported is the number of discovered profiles
                      currently not supported by the cluster.
                    format: int32
                    type: integer
                type: object
              discovery:
                description: Discovery tracks the state of the image-discovery Job
                  used to populate the discovery cache.
                properties:
                  attempts:
                    description: |-
                      Attempts is the number of discovery job attempts that have been made.
                      Increments each time a new discovery job is created after a failure.
                    format: int32
                    type: integer
                  lastAttemptTime:
                    description: |-
                      LastAttemptTime is the timestamp of the most recent discovery job creation.
                      Used to calculate exponential backoff before the next retry.
                    format: date-time
                    type: string
                  lastFailureReason:
                    description: LastFailureReason captures the reason for the most
                      recent discovery failure.
                    type: string
                  specHash:
                    description: |-
                      SpecHash is a hash of the model spec fields that invalidate cached discovery
                      (image, imagePullSecrets, serviceAccountName, discoveryCommandVersion).
                      When it changes, the operator drops the cache and re-runs the discovery Job.
                    type: string
                type: object
              discoveryCacheRef:
                description: |-
                  DiscoveryCacheRef points at the normalized discovery cache ConfigMap for image-backed flows.
                  Populated by the v1alpha2 controller after image inspection succeeds.
                properties:
                  name:
                    description: Name is the ConfigMap name.
                    type: string
                  namespace:
                    description: Namespace is the ConfigMap namespace.
                    type: string
                type: object
              imageMetadata:
                description: ImageMetadata is the metadata extracted from an AIM image
                properties:
                  baseImageRef:
                    description: |-
                      BaseImageRef is the value of the AIM_BASE_IMAGE_REF environment variable
                      baked into the image's OCI config at build time. For AIM model images this
                      records the base image (e.g. "ghcr.io/silogen/aim-base:0.8.5") that the
                      model image was built from. Used by the AIMModel controller to resolve
                      the deployment image for fine-tuned models whose spec.image is omitted
                      (versionPolicy=latest or any).
                    type: string
                  model:
                    description: Model contains AMD Silogen model-specific metadata.
                    properties:
                      canonicalName:
                        description: |-
                          CanonicalName is the canonical model identifier (e.g., mistralai/Mixtral-8x22B-Instruct-v0.1).
                          Extracted from: org.amd.silogen.model.canonicalName
                        type: string
                      descriptionFull:
                        description: |-
                          DescriptionFull is the full description.
                          Extracted from: org.amd.silogen.description.full
                        type: string
                      hfTokenRequired:
                        description: |-
                          HFTokenRequired indicates if a HuggingFace token is required.
                          Extracted from: org.amd.silogen.hfToken.required
                        type: boolean
                      recommendedDeployments:
                        description: |-
                          RecommendedDeployments contains recommended deployment configurations.
                          Extracted from: org.amd.silogen.model.recommendedDeployments (parsed from JSON array)
                        items:
                          description: RecommendedDeployment describes a recommended
                            deployment configuration for a model.
                          properties:
                            description:
                              description: Description provides additional context
                                about this deployment configuration
                              type: string
                            gpuCount:
                              description: GPUCount is the number of GPUs required
                              format: int32
                              type: integer
                            gpuModel:
                              description: |-
                                GPUModel is the GPU model name (e.g., MI300X, MI325X).
                                The legacy v1alpha1 schema only models GPU accelerators; CPU profiles
                                emitted via OCI labels (gpuModel="CPU") are not first-class here and
                                are handled exclusively by the v1alpha2 native discovery pipeline.
                              type: string
                            metric:
                              description: Metric is the optimization target (e.g.,
                                latency, throughput)
                              type: string
                            precision:
                              description: Precision is the recommended precision
                                (e.g., fp8, fp16, bf16)
                              type: string
                            profileId:
                              description: |-
                                ProfileId is the unique identifier of the AIM profile for this deployment.
                                When set, templates created from this deployment will use this profile ID
                                to deterministically select the correct runtime profile in the AIM container.
                              type: string
                          type: object
                        type: array
                      releaseNotes:
                        description: |-
                          ReleaseNotes contains release notes for this version.
                          Extracted from: org.amd.silogen.release.notes
                        type: string
                      source:
                        description: |-
                          Source is the URL where the model can be found.
                          Extracted from: org.amd.silogen.model.source
                        type: string
                      tags:
                        description: |-
                          Tags are descriptive tags (e.g., ["text-generation", "chat", "instruction"]).
                          Extracted from: org.amd.silogen.model.tags (comma-separated)
                        items:
                          type: string
                        type: array
                      title:
                        description: |-
                          Title is the Silogen-specific title for the model.
                          Extracted from: org.amd.silogen.title
                        type: string
                      variants:
                        description: |-
                          Variants lists model variants.
                          Extracted from: org.amd.silogen.model.variants (comma-separated)
                        items:
                          type: string
                        type: array
                      versions:
                        description: |-
                          Versions lists available versions.
                          Extracted from: org.amd.silogen.model.versions (comma-separated)
                        items:
                          type: string
                        type: array
                    type: object
                  oci:
                    description: OCI contains standard OCI image metadata.
                    properties:
                      authors:
                        description: |-
                          Authors is contact details of the authors.
                          Extracted from: org.opencontainers.image.authors
                        type: string
                      created:
                        description: |-
                          Created is the creation timestamp.
                          Extracted from: org.opencontainers.image.created
                        type: string
                      description:
                        description: |-
                          Description is a brief description.
                          Extracted from: org.opencontainers.image.description
                        type: string
                      documentation:
                        description: |-
                          Documentation is the URL to documentation.
                          Extracted from: org.opencontainers.image.documentation
                        type: string
                      licenses:
                        description: |-
                          Licenses is the SPDX license identifier(s).
                          Extracted from: org.opencontainers.image.licenses
                        type: string
                      revision:
                        description: |-
                          Revision is the source control revision.
                          Extracted from: org.opencontainers.image.revision
                        type: string
                      source:
                        description: |-
                          Source is the URL to the source code repository.
                          Extracted from: org.opencontainers.image.source
                        type: string
                      title:
                        description: |-
                          Title is the human-readable title.
                          Extracted from: org.opencontainers.image.title
                        type: string
                      vendor:
                        description: |-
                          Vendor is the organization that produced the image.
                          Extracted from: org.opencontainers.image.vendor
                        type: string
                      version:
                        description: |-
                          Version is the image version.
                          Extracted from: org.opencontainers.image.version
                        type: string
                    type: object
                  originalLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      OriginalLabels contains the raw OCI image labels as a JSON object.
                      This preserves all labels from the image, including those not mapped to structured fields.
                    type: object
                type: object
              kind:
                description: |-
                  Kind classifies the v1alpha2 onboarding flow that produced this
                  model's profiles (Image / Derived / Custom). See AIMModelKind for
                  the per-value semantics. Populated by the v1alpha2 controller from
                  the spec shape; left empty by the v1alpha1 controller.
                enum:
                - Image
                - Derived
                - Custom
                type: string
              managedProfiles:
                description: ManagedProfiles summarizes the direct promoted or derived
                  profiles owned or managed by this model.
                properties:
                  base:
                    description: |-
                      Base is the number of managed profiles whose spec is structurally
                      incomplete (missing aimId or modelSources). Base profiles cannot back
                      an AIMService directly and exist purely as source material for
                      derivation via AIMModel.spec.profiles.derivedFrom with
                      selector.role=base. A non-zero value is the canonical operational
                      signal that this model is a base-image model (the source of
                      custom-model derivation).
                    format: int32
                    type: integer
                  deployable:
                    description: |-
                      Deployable is the number of managed profiles whose spec is materialised
                      enough to back an AIMService (carries aimId and modelSources). For a
                      normal officially-discovered AIMModel this equals Total. For a
                      base-image AIMModel used as a source for custom-model derivation
                      this is 0 — the model only emits base profiles that callers must
                      derive into deployable profiles.
                    format: int32
                    type: integer
                  notAvailable:
                    description: NotAvailable is the number of derivative profiles
                      whose own status is NotAvailable.
                    format: int32
                    type: integer
                  ready:
                    description: Ready is the number of derivative profiles whose
                      own status is Ready.
                    format: int32
                    type: integer
                  total:
                    description: Total is the number of derivative profiles the controller
                      currently manages or intends to manage.
                    format: int32
                    type: integer
                required:
                - base
                - deployable
                - notAvailable
                - ready
                - total
                type: object
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller
                format: int64
                type: integer
              profileSetRef:
                description: ProfileSetRef identifies the child profile set synthesized
                  for derivation flows.
                properties:
                  name:
                    description: Name is the profile set name.
                    type: string
                  namespace:
                    description: Namespace is the profile set namespace.
                    type: string
                type: object
              resolvedRuntimeConfig:
                description: ResolvedRuntimeConfig captures metadata about the runtime
                  config that was resolved.
                properties:
                  kind:
                    description: Kind is the fully-qualified kind of the resolved
                      reference, when known.
                    type: string
                  name:
                    description: Name is the resource name that satisfied the reference.
                    type: string
                  namespace:
                    description: |-
                      Namespace identifies where the resource was found when namespace-scoped.
                      Empty indicates a cluster-scoped resource.
                    type: string
                  scope:
                    description: Scope indicates whether the resolved resource was
                      namespace or cluster scoped.
                    enum:
                    - Namespace
                    - Cluster
                    - Merged
                    - Unknown
                    type: string
                  uid:
                    description: UID captures the unique identifier of the resolved
                      reference, when known.
                    type: string
                type: object
              sourceType:
                description: |-
                  SourceType indicates how this model's artifacts are sourced.
                  - "Image": Model discovered from container image labels
                  - "Custom": Model uses explicit spec.modelSources
                  Set by the controller based on whether spec.modelSources is populated.

                  Note: only populated by the v1alpha1 controller; v1alpha2 consumers
                  should read .status.kind instead, which distinguishes fine-tunes
                  (Derived) from BYO base-image overlays (Custom).
                enum:
                - Image
                - Custom
                type: string
              status:
                default: Pending
                description: Status represents the overall status of the image based
                  on its templates
                enum:
                - Pending
                - Progressing
                - Ready
                - Degraded
                - Failed
                - NotAvailable
                type: string
              version:
                description: |-
                  Version is the effective image version of the model. Populated from
                  the spec.image tag (`amdenterpriseai/aim-qwen-qwen3-32b:0.11.0` →
                  `0.11.0`) during reconciliation. Empty for models that have no
                  spec.image (e.g. fine-tuned models derived from a parent) or that
                  reference an image by digest.

                  This mirrors AIMProfileStatus.Version so the two surfaces stay in
                  lock-step: a single image-tag extraction rule governs what users
                  see in the kubectl printcolumn for both kinds.

                  The image-author-declared version (i.e. the
                  `org.opencontainers.image.version` OCI label) is preserved separately
                  under .status.imageMetadata.oci.version for users that want to inspect
                  what the image build pipeline stamped. The two values usually agree;
                  when the OCI label is missing or empty, this field still surfaces a
                  useful version from the tag itself.
                type: string
            type: object
        type: object
        x-kubernetes-validations:
        - message: exactly one of spec.image or spec.profiles must be set on v1alpha2
          optionalOldSelf: true
          rule: (has(self.spec.image) && size(self.spec.image) > 0) != (has(self.spec.profiles)
            || has(self.spec.derivedFrom)) || (oldSelf.hasValue() && !((has(oldSelf.value().spec.image)
            && size(oldSelf.value().spec.image) > 0) != (has(oldSelf.value().spec.profiles)
            || has(oldSelf.value().spec.derivedFrom))))
        - message: spec.profileCopy is forbidden on v1alpha2; use spec.profiles
          optionalOldSelf: true
          rule: '!has(self.spec.profileCopy) || (oldSelf.hasValue() && has(oldSelf.value().spec.profileCopy))'
        - message: spec.derivedFrom is deprecated on v1alpha2; use spec.profiles
          optionalOldSelf: true
          rule: '!has(self.spec.derivedFrom) || (oldSelf.hasValue() && has(oldSelf.value().spec.derivedFrom))'
        - message: spec.custom is forbidden on v1alpha2
          optionalOldSelf: true
          rule: '!has(self.spec.custom) || (oldSelf.hasValue() && has(oldSelf.value().spec.custom))'
        - message: spec.customTemplates is forbidden on v1alpha2
          optionalOldSelf: true
          rule: '!has(self.spec.customTemplates) || size(self.spec.customTemplates)
            == 0 || (oldSelf.hasValue() && has(oldSelf.value().spec.customTemplates)
            && size(oldSelf.value().spec.customTemplates) > 0)'
        - message: spec.modelSources is forbidden on v1alpha2; use spec.profiles.overrides.modelSources
          optionalOldSelf: true
          rule: '!has(self.spec.modelSources) || size(self.spec.modelSources) == 0
            || (oldSelf.hasValue() && has(oldSelf.value().spec.modelSources) && size(oldSelf.value().spec.modelSources)
            > 0)'
        - message: spec.profiles cannot be combined with spec.aimId, spec.discovery,
            spec.defaultServiceTemplate, spec.runtimeConfigName, spec.env, or spec.imageMetadata
          rule: (!has(self.spec.profiles) && !has(self.spec.derivedFrom)) || (!has(self.spec.aimId)
            && !has(self.spec.discovery) && !has(self.spec.defaultServiceTemplate)
            && !has(self.spec.runtimeConfigName) && !has(self.spec.env) && !has(self.spec.imageMetadata))
    served: true
    storage: true
    subresources:
      status: {}
